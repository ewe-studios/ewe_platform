//! Auto-generated API clients for gcp/securitycenter.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/securitycenter`

#![cfg(feature = "gcp/securitycenter")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/folders/{foldersId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_assets_group_execute()` to send, or `securitycenter_folders_assets_group` for simplest API.

pub fn securitycenter_folders_assets_group_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GroupAssetsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/assets:group",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_assets_group_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_assets_group_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_assets_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_assets_group_builder()` + `securitycenter_folders_assets_group_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_assets_group(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GroupAssetsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_assets_group_builder(client, parent, body)?;
    securitycenter_folders_assets_group_execute(builder)
}

/// GET v1/folders/{foldersId}/assets
/// Lists an organization's assets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_assets_list_execute()` to send, or `securitycenter_folders_assets_list` for simplest API.

pub fn securitycenter_folders_assets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    compareDuration: Option<&str>,
    fieldMask: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readTime: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/assets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = compareDuration {
        query_parts.push(format!("compareDuration={}", val));
    }
    if let Some(val) = fieldMask {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime {
        query_parts.push(format!("readTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/assets
/// Lists an organization's assets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_assets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_assets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_assets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/assets
/// Lists an organization's assets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_assets_list_builder()` + `securitycenter_folders_assets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_assets_list(
    client: &SimpleHttpClient,
    parent: &str,
    compareDuration: Option<&str>,
    fieldMask: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readTime: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_assets_list_builder(
        client,
        parent,
        compareDuration,
        fieldMask,
        filter,
        orderBy,
        pageSize,
        pageToken,
        readTime,
    )?;
    securitycenter_folders_assets_list_execute(builder)
}

/// GET v1/folders/{foldersId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_assets_update_security_marks_execute()` to send, or `securitycenter_folders_assets_update_security_marks` for simplest API.

pub fn securitycenter_folders_assets_update_security_marks_builder(
    client: &SimpleHttpClient,
    name: &str,
    startTime: Option<&str>,
    updateMask: Option<&str>,
    body: &SecurityMarks,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/assets/{}/securityMarks",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_assets_update_security_marks_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_assets_update_security_marks_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_assets_update_security_marks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityMarks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_assets_update_security_marks_builder()` + `securitycenter_folders_assets_update_security_marks_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_assets_update_security_marks(
    client: &SimpleHttpClient,
    name: &str,
    startTime: Option<&str>,
    updateMask: Option<&str>,
    body: &SecurityMarks,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_assets_update_security_marks_builder(
        client, name, startTime, updateMask, body,
    )?;
    securitycenter_folders_assets_update_security_marks_execute(builder)
}

/// GET v1/folders/{foldersId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_big_query_exports_create_execute()` to send, or `securitycenter_folders_big_query_exports_create` for simplest API.

pub fn securitycenter_folders_big_query_exports_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    bigQueryExportId: Option<&str>,
    body: &GoogleCloudSecuritycenterV1BigQueryExport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/bigQueryExports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bigQueryExportId {
        query_parts.push(format!("bigQueryExportId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_big_query_exports_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_big_query_exports_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_big_query_exports_create_builder()` + `securitycenter_folders_big_query_exports_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_create(
    client: &SimpleHttpClient,
    parent: &str,
    bigQueryExportId: Option<&str>,
    body: &GoogleCloudSecuritycenterV1BigQueryExport,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_big_query_exports_create_builder(
        client,
        parent,
        bigQueryExportId,
        body,
    )?;
    securitycenter_folders_big_query_exports_create_execute(builder)
}

/// GET v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_big_query_exports_delete_execute()` to send, or `securitycenter_folders_big_query_exports_delete` for simplest API.

pub fn securitycenter_folders_big_query_exports_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/bigQueryExports/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_big_query_exports_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_big_query_exports_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_big_query_exports_delete_builder()` + `securitycenter_folders_big_query_exports_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_big_query_exports_delete_builder(client, name)?;
    securitycenter_folders_big_query_exports_delete_execute(builder)
}

/// GET v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_big_query_exports_get_execute()` to send, or `securitycenter_folders_big_query_exports_get` for simplest API.

pub fn securitycenter_folders_big_query_exports_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/bigQueryExports/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_big_query_exports_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_big_query_exports_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_big_query_exports_get_builder()` + `securitycenter_folders_big_query_exports_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_big_query_exports_get_builder(client, name)?;
    securitycenter_folders_big_query_exports_get_execute(builder)
}

/// GET v1/folders/{foldersId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_big_query_exports_list_execute()` to send, or `securitycenter_folders_big_query_exports_list` for simplest API.

pub fn securitycenter_folders_big_query_exports_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/bigQueryExports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_big_query_exports_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_big_query_exports_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBigQueryExportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBigQueryExportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_big_query_exports_list_builder()` + `securitycenter_folders_big_query_exports_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBigQueryExportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_big_query_exports_list_builder(client, parent, pageSize, pageToken)?;
    securitycenter_folders_big_query_exports_list_execute(builder)
}

/// GET v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_big_query_exports_patch_execute()` to send, or `securitycenter_folders_big_query_exports_patch` for simplest API.

pub fn securitycenter_folders_big_query_exports_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1BigQueryExport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/bigQueryExports/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_big_query_exports_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_big_query_exports_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_big_query_exports_patch_builder()` + `securitycenter_folders_big_query_exports_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1BigQueryExport,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_big_query_exports_patch_builder(client, name, updateMask, body)?;
    securitycenter_folders_big_query_exports_patch_execute(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_validate_custom_module_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_validate_custom_module` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_validate_custom_module_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ValidateEventThreatDetectionCustomModuleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings:validateCustomModule",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_validate_custom_module_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_event_threat_detection_settings_validate_custom_module_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_validate_custom_module_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValidateEventThreatDetectionCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_validate_custom_module_builder()` + `securitycenter_folders_event_threat_detection_settings_validate_custom_module_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_validate_custom_module(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ValidateEventThreatDetectionCustomModuleRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_event_threat_detection_settings_validate_custom_module_builder(
            client, parent, body,
        )?;
    securitycenter_folders_event_threat_detection_settings_validate_custom_module_execute(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_custom_modules_create_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_custom_modules_create` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EventThreatDetectionCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/customModules",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_event_threat_detection_settings_custom_modules_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_create_builder()` + `securitycenter_folders_event_threat_detection_settings_custom_modules_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EventThreatDetectionCustomModule,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_event_threat_detection_settings_custom_modules_create_builder(
            client, parent, body,
        )?;
    securitycenter_folders_event_threat_detection_settings_custom_modules_create_execute(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_custom_modules_delete_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_custom_modules_delete` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/customModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_event_threat_detection_settings_custom_modules_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_delete_builder()` + `securitycenter_folders_event_threat_detection_settings_custom_modules_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_event_threat_detection_settings_custom_modules_delete_builder(
            client, name,
        )?;
    securitycenter_folders_event_threat_detection_settings_custom_modules_delete_execute(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_custom_modules_get_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_custom_modules_get` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/customModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_event_threat_detection_settings_custom_modules_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_get_builder()` + `securitycenter_folders_event_threat_detection_settings_custom_modules_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_event_threat_detection_settings_custom_modules_get_builder(
            client, name,
        )?;
    securitycenter_folders_event_threat_detection_settings_custom_modules_get_execute(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_custom_modules_list` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/customModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_event_threat_detection_settings_custom_modules_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListEventThreatDetectionCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_builder()` + `securitycenter_folders_event_threat_detection_settings_custom_modules_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListEventThreatDetectionCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_event_threat_detection_settings_custom_modules_list_builder(
            client, parent, pageSize, pageToken,
        )?;
    securitycenter_folders_event_threat_detection_settings_custom_modules_list_execute(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/customModules:listDescendant",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_builder()` + `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_builder(client, parent, pageSize, pageToken)?;
    securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_execute(
        builder,
    )
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_custom_modules_patch_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_custom_modules_patch` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &EventThreatDetectionCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/customModules/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_event_threat_detection_settings_custom_modules_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_patch_builder()` + `securitycenter_folders_event_threat_detection_settings_custom_modules_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &EventThreatDetectionCustomModule,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_event_threat_detection_settings_custom_modules_patch_builder(
            client, name, updateMask, body,
        )?;
    securitycenter_folders_event_threat_detection_settings_custom_modules_patch_execute(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/effectiveCustomModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EffectiveEventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EffectiveEventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_builder()` + `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EffectiveEventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_builder(client, name)?;
    securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_execute(
        builder,
    )
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/effectiveCustomModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_builder()` + `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_builder(client, parent, pageSize, pageToken)?;
    securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_execute(
        builder,
    )
}

/// GET v1/folders/{foldersId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_findings_bulk_mute_execute()` to send, or `securitycenter_folders_findings_bulk_mute` for simplest API.

pub fn securitycenter_folders_findings_bulk_mute_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BulkMuteFindingsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/findings:bulkMute",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_findings_bulk_mute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_findings_bulk_mute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_findings_bulk_mute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_findings_bulk_mute_builder()` + `securitycenter_folders_findings_bulk_mute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_findings_bulk_mute(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BulkMuteFindingsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_findings_bulk_mute_builder(client, parent, body)?;
    securitycenter_folders_findings_bulk_mute_execute(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_locations_mute_configs_delete_execute()` to send, or `securitycenter_folders_locations_mute_configs_delete` for simplest API.

pub fn securitycenter_folders_locations_mute_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/locations/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_locations_mute_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_locations_mute_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_locations_mute_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_locations_mute_configs_delete_builder()` + `securitycenter_folders_locations_mute_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_locations_mute_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_locations_mute_configs_delete_builder(client, name)?;
    securitycenter_folders_locations_mute_configs_delete_execute(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_locations_mute_configs_get_execute()` to send, or `securitycenter_folders_locations_mute_configs_get` for simplest API.

pub fn securitycenter_folders_locations_mute_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/locations/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_locations_mute_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_locations_mute_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_locations_mute_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_locations_mute_configs_get_builder()` + `securitycenter_folders_locations_mute_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_locations_mute_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_locations_mute_configs_get_builder(client, name)?;
    securitycenter_folders_locations_mute_configs_get_execute(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_locations_mute_configs_patch_execute()` to send, or `securitycenter_folders_locations_mute_configs_patch` for simplest API.

pub fn securitycenter_folders_locations_mute_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/locations/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_locations_mute_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_locations_mute_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_locations_mute_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_locations_mute_configs_patch_builder()` + `securitycenter_folders_locations_mute_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_locations_mute_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_locations_mute_configs_patch_builder(
        client, name, updateMask, body,
    )?;
    securitycenter_folders_locations_mute_configs_patch_execute(builder)
}

/// GET v1/folders/{foldersId}/muteConfigs
/// Creates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_mute_configs_create_execute()` to send, or `securitycenter_folders_mute_configs_create` for simplest API.

pub fn securitycenter_folders_mute_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    muteConfigId: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/muteConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = muteConfigId {
        query_parts.push(format!("muteConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/muteConfigs
/// Creates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_mute_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_mute_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/muteConfigs
/// Creates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_mute_configs_create_builder()` + `securitycenter_folders_mute_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    muteConfigId: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_mute_configs_create_builder(client, parent, muteConfigId, body)?;
    securitycenter_folders_mute_configs_create_execute(builder)
}

/// GET v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_mute_configs_delete_execute()` to send, or `securitycenter_folders_mute_configs_delete` for simplest API.

pub fn securitycenter_folders_mute_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_mute_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_mute_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_mute_configs_delete_builder()` + `securitycenter_folders_mute_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_mute_configs_delete_builder(client, name)?;
    securitycenter_folders_mute_configs_delete_execute(builder)
}

/// GET v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_mute_configs_get_execute()` to send, or `securitycenter_folders_mute_configs_get` for simplest API.

pub fn securitycenter_folders_mute_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_mute_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_mute_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_mute_configs_get_builder()` + `securitycenter_folders_mute_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_mute_configs_get_builder(client, name)?;
    securitycenter_folders_mute_configs_get_execute(builder)
}

/// GET v1/folders/{foldersId}/muteConfigs
/// Lists mute configs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_mute_configs_list_execute()` to send, or `securitycenter_folders_mute_configs_list` for simplest API.

pub fn securitycenter_folders_mute_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/muteConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/muteConfigs
/// Lists mute configs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_mute_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_mute_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMuteConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMuteConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/muteConfigs
/// Lists mute configs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_mute_configs_list_builder()` + `securitycenter_folders_mute_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMuteConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_mute_configs_list_builder(client, parent, pageSize, pageToken)?;
    securitycenter_folders_mute_configs_list_execute(builder)
}

/// GET v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_mute_configs_patch_execute()` to send, or `securitycenter_folders_mute_configs_patch` for simplest API.

pub fn securitycenter_folders_mute_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_mute_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_mute_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_mute_configs_patch_builder()` + `securitycenter_folders_mute_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_mute_configs_patch_builder(client, name, updateMask, body)?;
    securitycenter_folders_mute_configs_patch_execute(builder)
}

/// GET v1/folders/{foldersId}/notificationConfigs
/// Creates a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_notification_configs_create_execute()` to send, or `securitycenter_folders_notification_configs_create` for simplest API.

pub fn securitycenter_folders_notification_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    configId: Option<&str>,
    body: &NotificationConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/notificationConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = configId {
        query_parts.push(format!("configId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/notificationConfigs
/// Creates a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_notification_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_notification_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/notificationConfigs
/// Creates a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_notification_configs_create_builder()` + `securitycenter_folders_notification_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    configId: Option<&str>,
    body: &NotificationConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_notification_configs_create_builder(client, parent, configId, body)?;
    securitycenter_folders_notification_configs_create_execute(builder)
}

/// GET v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_notification_configs_delete_execute()` to send, or `securitycenter_folders_notification_configs_delete` for simplest API.

pub fn securitycenter_folders_notification_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/notificationConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_notification_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_notification_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_notification_configs_delete_builder()` + `securitycenter_folders_notification_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_notification_configs_delete_builder(client, name)?;
    securitycenter_folders_notification_configs_delete_execute(builder)
}

/// GET v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_notification_configs_get_execute()` to send, or `securitycenter_folders_notification_configs_get` for simplest API.

pub fn securitycenter_folders_notification_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/notificationConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_notification_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_notification_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_notification_configs_get_builder()` + `securitycenter_folders_notification_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_notification_configs_get_builder(client, name)?;
    securitycenter_folders_notification_configs_get_execute(builder)
}

/// GET v1/folders/{foldersId}/notificationConfigs
/// Lists notification configs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_notification_configs_list_execute()` to send, or `securitycenter_folders_notification_configs_list` for simplest API.

pub fn securitycenter_folders_notification_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/notificationConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/notificationConfigs
/// Lists notification configs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_notification_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_notification_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNotificationConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/notificationConfigs
/// Lists notification configs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_notification_configs_list_builder()` + `securitycenter_folders_notification_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_notification_configs_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    securitycenter_folders_notification_configs_list_execute(builder)
}

/// GET v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_notification_configs_patch_execute()` to send, or `securitycenter_folders_notification_configs_patch` for simplest API.

pub fn securitycenter_folders_notification_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &NotificationConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/notificationConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_notification_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_notification_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_notification_configs_patch_builder()` + `securitycenter_folders_notification_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &NotificationConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_notification_configs_patch_builder(client, name, updateMask, body)?;
    securitycenter_folders_notification_configs_patch_execute(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_custom_modules_create_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_custom_modules_create` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/customModules",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_security_health_analytics_settings_custom_modules_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_create_builder()` + `securitycenter_folders_security_health_analytics_settings_custom_modules_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_security_health_analytics_settings_custom_modules_create_builder(
            client, parent, body,
        )?;
    securitycenter_folders_security_health_analytics_settings_custom_modules_create_execute(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_custom_modules_delete_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_custom_modules_delete` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/customModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_security_health_analytics_settings_custom_modules_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_delete_builder()` + `securitycenter_folders_security_health_analytics_settings_custom_modules_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_security_health_analytics_settings_custom_modules_delete_builder(
            client, name,
        )?;
    securitycenter_folders_security_health_analytics_settings_custom_modules_delete_execute(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_custom_modules_get_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_custom_modules_get` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/customModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_security_health_analytics_settings_custom_modules_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_get_builder()` + `securitycenter_folders_security_health_analytics_settings_custom_modules_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_security_health_analytics_settings_custom_modules_get_builder(
            client, name,
        )?;
    securitycenter_folders_security_health_analytics_settings_custom_modules_get_execute(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_custom_modules_list` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/customModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_security_health_analytics_settings_custom_modules_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecurityHealthAnalyticsCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_builder()` + `securitycenter_folders_security_health_analytics_settings_custom_modules_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecurityHealthAnalyticsCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_security_health_analytics_settings_custom_modules_list_builder(
            client, parent, pageSize, pageToken,
        )?;
    securitycenter_folders_security_health_analytics_settings_custom_modules_list_execute(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/customModules:listDescendant",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_builder()` + `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_builder(client, parent, pageSize, pageToken)?;
    securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_execute(
        builder,
    )
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_custom_modules_patch_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_custom_modules_patch` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/customModules/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_security_health_analytics_settings_custom_modules_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_patch_builder()` + `securitycenter_folders_security_health_analytics_settings_custom_modules_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_security_health_analytics_settings_custom_modules_patch_builder(
            client, name, updateMask, body,
        )?;
    securitycenter_folders_security_health_analytics_settings_custom_modules_patch_execute(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SimulateSecurityHealthAnalyticsCustomModuleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/customModules:simulate",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SimulateSecurityHealthAnalyticsCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_builder()` + `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_simulate(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SimulateSecurityHealthAnalyticsCustomModuleRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_builder(
            client, parent, body,
        )?;
    securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_execute(
        builder,
    )
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/effectiveCustomModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_builder()` + `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_builder(client, name)?;
    securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_execute(
        builder,
    )
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/effectiveCustomModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_builder()` + `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_builder(client, parent, pageSize, pageToken)?;
    securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_execute(
        builder,
    )
}

/// GET v1/folders/{foldersId}/sources
/// Lists all sources belonging to an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_sources_list_execute()` to send, or `securitycenter_folders_sources_list` for simplest API.

pub fn securitycenter_folders_sources_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/sources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/sources
/// Lists all sources belonging to an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_sources_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_sources_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/sources
/// Lists all sources belonging to an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_sources_list_builder()` + `securitycenter_folders_sources_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_sources_list_builder(client, parent, pageSize, pageToken)?;
    securitycenter_folders_sources_list_execute(builder)
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings:group
/// Filters an organization or source's findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_sources_findings_group_execute()` to send, or `securitycenter_folders_sources_findings_group` for simplest API.

pub fn securitycenter_folders_sources_findings_group_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GroupFindingsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/sources/{}/findings:group",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings:group
/// Filters an organization or source's findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_sources_findings_group_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_sources_findings_group_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupFindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings:group
/// Filters an organization or source's findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_sources_findings_group_builder()` + `securitycenter_folders_sources_findings_group_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_group(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GroupFindingsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_sources_findings_group_builder(client, parent, body)?;
    securitycenter_folders_sources_findings_group_execute(builder)
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings
/// Lists an organization or source's findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_sources_findings_list_execute()` to send, or `securitycenter_folders_sources_findings_list` for simplest API.

pub fn securitycenter_folders_sources_findings_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    compareDuration: Option<&str>,
    fieldMask: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readTime: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/sources/{}/findings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = compareDuration {
        query_parts.push(format!("compareDuration={}", val));
    }
    if let Some(val) = fieldMask {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime {
        query_parts.push(format!("readTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings
/// Lists an organization or source's findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_sources_findings_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_sources_findings_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings
/// Lists an organization or source's findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_sources_findings_list_builder()` + `securitycenter_folders_sources_findings_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_list(
    client: &SimpleHttpClient,
    parent: &str,
    compareDuration: Option<&str>,
    fieldMask: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readTime: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_sources_findings_list_builder(
        client,
        parent,
        compareDuration,
        fieldMask,
        filter,
        orderBy,
        pageSize,
        pageToken,
        readTime,
    )?;
    securitycenter_folders_sources_findings_list_execute(builder)
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_sources_findings_patch_execute()` to send, or `securitycenter_folders_sources_findings_patch` for simplest API.

pub fn securitycenter_folders_sources_findings_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Finding,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/sources/{}/findings/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_sources_findings_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_sources_findings_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_sources_findings_patch_builder()` + `securitycenter_folders_sources_findings_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Finding,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_sources_findings_patch_builder(client, name, updateMask, body)?;
    securitycenter_folders_sources_findings_patch_execute(builder)
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_sources_findings_set_mute_execute()` to send, or `securitycenter_folders_sources_findings_set_mute` for simplest API.

pub fn securitycenter_folders_sources_findings_set_mute_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetMuteRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/sources/{}/findings/{}:setMute",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_sources_findings_set_mute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_sources_findings_set_mute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_set_mute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_sources_findings_set_mute_builder()` + `securitycenter_folders_sources_findings_set_mute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_set_mute(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetMuteRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_sources_findings_set_mute_builder(client, name, body)?;
    securitycenter_folders_sources_findings_set_mute_execute(builder)
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_sources_findings_set_state_execute()` to send, or `securitycenter_folders_sources_findings_set_state` for simplest API.

pub fn securitycenter_folders_sources_findings_set_state_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetFindingStateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/sources/{}/findings/{}:setState",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_sources_findings_set_state_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_sources_findings_set_state_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_set_state_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_sources_findings_set_state_builder()` + `securitycenter_folders_sources_findings_set_state_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_set_state(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetFindingStateRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_sources_findings_set_state_builder(client, name, body)?;
    securitycenter_folders_sources_findings_set_state_execute(builder)
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_sources_findings_update_security_marks_execute()` to send, or `securitycenter_folders_sources_findings_update_security_marks` for simplest API.

pub fn securitycenter_folders_sources_findings_update_security_marks_builder(
    client: &SimpleHttpClient,
    name: &str,
    startTime: Option<&str>,
    updateMask: Option<&str>,
    body: &SecurityMarks,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/sources/{}/findings/{}/securityMarks",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_sources_findings_update_security_marks_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_sources_findings_update_security_marks_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_update_security_marks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityMarks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_sources_findings_update_security_marks_builder()` + `securitycenter_folders_sources_findings_update_security_marks_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_update_security_marks(
    client: &SimpleHttpClient,
    name: &str,
    startTime: Option<&str>,
    updateMask: Option<&str>,
    body: &SecurityMarks,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_sources_findings_update_security_marks_builder(
        client, name, startTime, updateMask, body,
    )?;
    securitycenter_folders_sources_findings_update_security_marks_execute(builder)
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_sources_findings_external_systems_patch_execute()` to send, or `securitycenter_folders_sources_findings_external_systems_patch` for simplest API.

pub fn securitycenter_folders_sources_findings_external_systems_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1ExternalSystem,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/sources/{}/findings/{}/externalSystems/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_folders_sources_findings_external_systems_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_folders_sources_findings_external_systems_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_external_systems_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ExternalSystem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1ExternalSystem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_sources_findings_external_systems_patch_builder()` + `securitycenter_folders_sources_findings_external_systems_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_external_systems_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1ExternalSystem,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ExternalSystem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_sources_findings_external_systems_patch_builder(
        client, name, updateMask, body,
    )?;
    securitycenter_folders_sources_findings_external_systems_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/organizationSettings
/// Gets the settings for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_get_organization_settings_execute()` to send, or `securitycenter_organizations_get_organization_settings` for simplest API.

pub fn securitycenter_organizations_get_organization_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/organizationSettings",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/organizationSettings
/// Gets the settings for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_get_organization_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_get_organization_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_get_organization_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OrganizationSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OrganizationSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/organizationSettings
/// Gets the settings for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_get_organization_settings_builder()` + `securitycenter_organizations_get_organization_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_get_organization_settings(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OrganizationSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_get_organization_settings_builder(client, name)?;
    securitycenter_organizations_get_organization_settings_execute(builder)
}

/// GET v1/organizations/{organizationsId}/organizationSettings
/// Updates an organization's settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_update_organization_settings_execute()` to send, or `securitycenter_organizations_update_organization_settings` for simplest API.

pub fn securitycenter_organizations_update_organization_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &OrganizationSettings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/organizationSettings",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/organizationSettings
/// Updates an organization's settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_update_organization_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_update_organization_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_update_organization_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OrganizationSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OrganizationSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/organizationSettings
/// Updates an organization's settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_update_organization_settings_builder()` + `securitycenter_organizations_update_organization_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_update_organization_settings(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &OrganizationSettings,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OrganizationSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_update_organization_settings_builder(
        client, name, updateMask, body,
    )?;
    securitycenter_organizations_update_organization_settings_execute(builder)
}

/// GET v1/organizations/{organizationsId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_assets_group_execute()` to send, or `securitycenter_organizations_assets_group` for simplest API.

pub fn securitycenter_organizations_assets_group_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GroupAssetsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/assets:group",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_assets_group_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_assets_group_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_assets_group_builder()` + `securitycenter_organizations_assets_group_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_group(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GroupAssetsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_assets_group_builder(client, parent, body)?;
    securitycenter_organizations_assets_group_execute(builder)
}

/// GET v1/organizations/{organizationsId}/assets
/// Lists an organization's assets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_assets_list_execute()` to send, or `securitycenter_organizations_assets_list` for simplest API.

pub fn securitycenter_organizations_assets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    compareDuration: Option<&str>,
    fieldMask: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readTime: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/assets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = compareDuration {
        query_parts.push(format!("compareDuration={}", val));
    }
    if let Some(val) = fieldMask {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime {
        query_parts.push(format!("readTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/assets
/// Lists an organization's assets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_assets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_assets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/assets
/// Lists an organization's assets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_assets_list_builder()` + `securitycenter_organizations_assets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_list(
    client: &SimpleHttpClient,
    parent: &str,
    compareDuration: Option<&str>,
    fieldMask: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readTime: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_assets_list_builder(
        client,
        parent,
        compareDuration,
        fieldMask,
        filter,
        orderBy,
        pageSize,
        pageToken,
        readTime,
    )?;
    securitycenter_organizations_assets_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/assets:runDiscovery
/// Runs asset discovery. The discovery is tracked with a long-running operation. This API can only be called with limited frequency for an organization. If it is called too frequently the caller will receive a TOO_MANY_REQUESTS error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_assets_run_discovery_execute()` to send, or `securitycenter_organizations_assets_run_discovery` for simplest API.

pub fn securitycenter_organizations_assets_run_discovery_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &RunAssetDiscoveryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/assets:runDiscovery",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/assets:runDiscovery
/// Runs asset discovery. The discovery is tracked with a long-running operation. This API can only be called with limited frequency for an organization. If it is called too frequently the caller will receive a TOO_MANY_REQUESTS error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_assets_run_discovery_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_assets_run_discovery_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_run_discovery_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/assets:runDiscovery
/// Runs asset discovery. The discovery is tracked with a long-running operation. This API can only be called with limited frequency for an organization. If it is called too frequently the caller will receive a TOO_MANY_REQUESTS error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_assets_run_discovery_builder()` + `securitycenter_organizations_assets_run_discovery_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_run_discovery(
    client: &SimpleHttpClient,
    parent: &str,
    body: &RunAssetDiscoveryRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_assets_run_discovery_builder(client, parent, body)?;
    securitycenter_organizations_assets_run_discovery_execute(builder)
}

/// GET v1/organizations/{organizationsId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_assets_update_security_marks_execute()` to send, or `securitycenter_organizations_assets_update_security_marks` for simplest API.

pub fn securitycenter_organizations_assets_update_security_marks_builder(
    client: &SimpleHttpClient,
    name: &str,
    startTime: Option<&str>,
    updateMask: Option<&str>,
    body: &SecurityMarks,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/assets/{}/securityMarks",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_assets_update_security_marks_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_assets_update_security_marks_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_update_security_marks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityMarks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_assets_update_security_marks_builder()` + `securitycenter_organizations_assets_update_security_marks_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_update_security_marks(
    client: &SimpleHttpClient,
    name: &str,
    startTime: Option<&str>,
    updateMask: Option<&str>,
    body: &SecurityMarks,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_assets_update_security_marks_builder(
        client, name, startTime, updateMask, body,
    )?;
    securitycenter_organizations_assets_update_security_marks_execute(builder)
}

/// GET v1/organizations/{organizationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_attack_paths_list_execute()` to send, or `securitycenter_organizations_attack_paths_list` for simplest API.

pub fn securitycenter_organizations_attack_paths_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/attackPaths",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_attack_paths_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_attack_paths_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_attack_paths_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAttackPathsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_attack_paths_list_builder()` + `securitycenter_organizations_attack_paths_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_attack_paths_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_attack_paths_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    securitycenter_organizations_attack_paths_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_big_query_exports_create_execute()` to send, or `securitycenter_organizations_big_query_exports_create` for simplest API.

pub fn securitycenter_organizations_big_query_exports_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    bigQueryExportId: Option<&str>,
    body: &GoogleCloudSecuritycenterV1BigQueryExport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/bigQueryExports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bigQueryExportId {
        query_parts.push(format!("bigQueryExportId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_big_query_exports_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_big_query_exports_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_big_query_exports_create_builder()` + `securitycenter_organizations_big_query_exports_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_create(
    client: &SimpleHttpClient,
    parent: &str,
    bigQueryExportId: Option<&str>,
    body: &GoogleCloudSecuritycenterV1BigQueryExport,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_big_query_exports_create_builder(
        client,
        parent,
        bigQueryExportId,
        body,
    )?;
    securitycenter_organizations_big_query_exports_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_big_query_exports_delete_execute()` to send, or `securitycenter_organizations_big_query_exports_delete` for simplest API.

pub fn securitycenter_organizations_big_query_exports_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/bigQueryExports/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_big_query_exports_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_big_query_exports_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_big_query_exports_delete_builder()` + `securitycenter_organizations_big_query_exports_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_big_query_exports_delete_builder(client, name)?;
    securitycenter_organizations_big_query_exports_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_big_query_exports_get_execute()` to send, or `securitycenter_organizations_big_query_exports_get` for simplest API.

pub fn securitycenter_organizations_big_query_exports_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/bigQueryExports/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_big_query_exports_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_big_query_exports_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_big_query_exports_get_builder()` + `securitycenter_organizations_big_query_exports_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_big_query_exports_get_builder(client, name)?;
    securitycenter_organizations_big_query_exports_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_big_query_exports_list_execute()` to send, or `securitycenter_organizations_big_query_exports_list` for simplest API.

pub fn securitycenter_organizations_big_query_exports_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/bigQueryExports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_big_query_exports_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_big_query_exports_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBigQueryExportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBigQueryExportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_big_query_exports_list_builder()` + `securitycenter_organizations_big_query_exports_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBigQueryExportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_big_query_exports_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    securitycenter_organizations_big_query_exports_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_big_query_exports_patch_execute()` to send, or `securitycenter_organizations_big_query_exports_patch` for simplest API.

pub fn securitycenter_organizations_big_query_exports_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1BigQueryExport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/bigQueryExports/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_big_query_exports_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_big_query_exports_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_big_query_exports_patch_builder()` + `securitycenter_organizations_big_query_exports_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1BigQueryExport,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_big_query_exports_patch_builder(
        client, name, updateMask, body,
    )?;
    securitycenter_organizations_big_query_exports_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_validate_custom_module` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_validate_custom_module_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ValidateEventThreatDetectionCustomModuleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings:validateCustomModule",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_event_threat_detection_settings_validate_custom_module_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_validate_custom_module_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValidateEventThreatDetectionCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_builder()` + `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_validate_custom_module(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ValidateEventThreatDetectionCustomModuleRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_event_threat_detection_settings_validate_custom_module_builder(client, parent, body)?;
    securitycenter_organizations_event_threat_detection_settings_validate_custom_module_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_custom_modules_create` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EventThreatDetectionCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/customModules",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_event_threat_detection_settings_custom_modules_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_builder()` + `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EventThreatDetectionCustomModule,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_event_threat_detection_settings_custom_modules_create_builder(
            client, parent, body,
        )?;
    securitycenter_organizations_event_threat_detection_settings_custom_modules_create_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_custom_modules_delete` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/customModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_builder()` + `securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_builder(
            client, name,
        )?;
    securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_custom_modules_get_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_custom_modules_get` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/customModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_event_threat_detection_settings_custom_modules_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_get_builder()` + `securitycenter_organizations_event_threat_detection_settings_custom_modules_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_event_threat_detection_settings_custom_modules_get_builder(
            client, name,
        )?;
    securitycenter_organizations_event_threat_detection_settings_custom_modules_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_custom_modules_list` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/customModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_event_threat_detection_settings_custom_modules_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListEventThreatDetectionCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_builder()` + `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListEventThreatDetectionCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_event_threat_detection_settings_custom_modules_list_builder(
            client, parent, pageSize, pageToken,
        )?;
    securitycenter_organizations_event_threat_detection_settings_custom_modules_list_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/customModules:listDescendant",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_builder()` + `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_builder(client, parent, pageSize, pageToken)?;
    securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_execute(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_custom_modules_patch` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &EventThreatDetectionCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/customModules/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_builder()` + `securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &EventThreatDetectionCustomModule,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_builder(
            client, name, updateMask, body,
        )?;
    securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/effectiveCustomModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EffectiveEventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EffectiveEventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_builder()` + `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EffectiveEventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_builder(client, name)?;
    securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/effectiveCustomModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_builder()` + `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_builder(client, parent, pageSize, pageToken)?;
    securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_findings_bulk_mute_execute()` to send, or `securitycenter_organizations_findings_bulk_mute` for simplest API.

pub fn securitycenter_organizations_findings_bulk_mute_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BulkMuteFindingsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/findings:bulkMute",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_findings_bulk_mute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_findings_bulk_mute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_findings_bulk_mute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_findings_bulk_mute_builder()` + `securitycenter_organizations_findings_bulk_mute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_findings_bulk_mute(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BulkMuteFindingsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_findings_bulk_mute_builder(client, parent, body)?;
    securitycenter_organizations_findings_bulk_mute_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_locations_mute_configs_delete_execute()` to send, or `securitycenter_organizations_locations_mute_configs_delete` for simplest API.

pub fn securitycenter_organizations_locations_mute_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/locations/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_locations_mute_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_locations_mute_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_locations_mute_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_locations_mute_configs_delete_builder()` + `securitycenter_organizations_locations_mute_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_locations_mute_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_locations_mute_configs_delete_builder(client, name)?;
    securitycenter_organizations_locations_mute_configs_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_locations_mute_configs_get_execute()` to send, or `securitycenter_organizations_locations_mute_configs_get` for simplest API.

pub fn securitycenter_organizations_locations_mute_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/locations/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_locations_mute_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_locations_mute_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_locations_mute_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_locations_mute_configs_get_builder()` + `securitycenter_organizations_locations_mute_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_locations_mute_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_locations_mute_configs_get_builder(client, name)?;
    securitycenter_organizations_locations_mute_configs_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_locations_mute_configs_patch_execute()` to send, or `securitycenter_organizations_locations_mute_configs_patch` for simplest API.

pub fn securitycenter_organizations_locations_mute_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/locations/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_locations_mute_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_locations_mute_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_locations_mute_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_locations_mute_configs_patch_builder()` + `securitycenter_organizations_locations_mute_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_locations_mute_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_locations_mute_configs_patch_builder(
        client, name, updateMask, body,
    )?;
    securitycenter_organizations_locations_mute_configs_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/muteConfigs
/// Creates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_mute_configs_create_execute()` to send, or `securitycenter_organizations_mute_configs_create` for simplest API.

pub fn securitycenter_organizations_mute_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    muteConfigId: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/muteConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = muteConfigId {
        query_parts.push(format!("muteConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/muteConfigs
/// Creates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_mute_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_mute_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/muteConfigs
/// Creates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_mute_configs_create_builder()` + `securitycenter_organizations_mute_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    muteConfigId: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_mute_configs_create_builder(
        client,
        parent,
        muteConfigId,
        body,
    )?;
    securitycenter_organizations_mute_configs_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_mute_configs_delete_execute()` to send, or `securitycenter_organizations_mute_configs_delete` for simplest API.

pub fn securitycenter_organizations_mute_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_mute_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_mute_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_mute_configs_delete_builder()` + `securitycenter_organizations_mute_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_mute_configs_delete_builder(client, name)?;
    securitycenter_organizations_mute_configs_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_mute_configs_get_execute()` to send, or `securitycenter_organizations_mute_configs_get` for simplest API.

pub fn securitycenter_organizations_mute_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_mute_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_mute_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_mute_configs_get_builder()` + `securitycenter_organizations_mute_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_mute_configs_get_builder(client, name)?;
    securitycenter_organizations_mute_configs_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/muteConfigs
/// Lists mute configs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_mute_configs_list_execute()` to send, or `securitycenter_organizations_mute_configs_list` for simplest API.

pub fn securitycenter_organizations_mute_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/muteConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/muteConfigs
/// Lists mute configs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_mute_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_mute_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMuteConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMuteConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/muteConfigs
/// Lists mute configs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_mute_configs_list_builder()` + `securitycenter_organizations_mute_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMuteConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_mute_configs_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    securitycenter_organizations_mute_configs_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_mute_configs_patch_execute()` to send, or `securitycenter_organizations_mute_configs_patch` for simplest API.

pub fn securitycenter_organizations_mute_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_mute_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_mute_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_mute_configs_patch_builder()` + `securitycenter_organizations_mute_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_mute_configs_patch_builder(client, name, updateMask, body)?;
    securitycenter_organizations_mute_configs_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/notificationConfigs
/// Creates a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_notification_configs_create_execute()` to send, or `securitycenter_organizations_notification_configs_create` for simplest API.

pub fn securitycenter_organizations_notification_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    configId: Option<&str>,
    body: &NotificationConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/notificationConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = configId {
        query_parts.push(format!("configId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/notificationConfigs
/// Creates a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_notification_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_notification_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/notificationConfigs
/// Creates a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_notification_configs_create_builder()` + `securitycenter_organizations_notification_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    configId: Option<&str>,
    body: &NotificationConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_notification_configs_create_builder(
        client, parent, configId, body,
    )?;
    securitycenter_organizations_notification_configs_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_notification_configs_delete_execute()` to send, or `securitycenter_organizations_notification_configs_delete` for simplest API.

pub fn securitycenter_organizations_notification_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/notificationConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_notification_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_notification_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_notification_configs_delete_builder()` + `securitycenter_organizations_notification_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_notification_configs_delete_builder(client, name)?;
    securitycenter_organizations_notification_configs_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_notification_configs_get_execute()` to send, or `securitycenter_organizations_notification_configs_get` for simplest API.

pub fn securitycenter_organizations_notification_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/notificationConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_notification_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_notification_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_notification_configs_get_builder()` + `securitycenter_organizations_notification_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_notification_configs_get_builder(client, name)?;
    securitycenter_organizations_notification_configs_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/notificationConfigs
/// Lists notification configs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_notification_configs_list_execute()` to send, or `securitycenter_organizations_notification_configs_list` for simplest API.

pub fn securitycenter_organizations_notification_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/notificationConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/notificationConfigs
/// Lists notification configs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_notification_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_notification_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNotificationConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/notificationConfigs
/// Lists notification configs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_notification_configs_list_builder()` + `securitycenter_organizations_notification_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_notification_configs_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    securitycenter_organizations_notification_configs_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_notification_configs_patch_execute()` to send, or `securitycenter_organizations_notification_configs_patch` for simplest API.

pub fn securitycenter_organizations_notification_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &NotificationConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/notificationConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_notification_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_notification_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_notification_configs_patch_builder()` + `securitycenter_organizations_notification_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &NotificationConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_notification_configs_patch_builder(
        client, name, updateMask, body,
    )?;
    securitycenter_organizations_notification_configs_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_operations_cancel_execute()` to send, or `securitycenter_organizations_operations_cancel` for simplest API.

pub fn securitycenter_organizations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_operations_cancel_builder()` + `securitycenter_organizations_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_operations_cancel_builder(client, name)?;
    securitycenter_organizations_operations_cancel_execute(builder)
}

/// GET v1/organizations/{organizationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_operations_delete_execute()` to send, or `securitycenter_organizations_operations_delete` for simplest API.

pub fn securitycenter_organizations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_operations_delete_builder()` + `securitycenter_organizations_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_operations_delete_builder(client, name)?;
    securitycenter_organizations_operations_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_operations_get_execute()` to send, or `securitycenter_organizations_operations_get` for simplest API.

pub fn securitycenter_organizations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_operations_get_builder()` + `securitycenter_organizations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_operations_get_builder(client, name)?;
    securitycenter_organizations_operations_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_operations_list_execute()` to send, or `securitycenter_organizations_operations_list` for simplest API.

pub fn securitycenter_organizations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_operations_list_builder()` + `securitycenter_organizations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    securitycenter_organizations_operations_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs:batchCreate
/// Creates a ResourceValueConfig for an organization. Maps user's tags to difference resource values for use by the attack path simulation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_resource_value_configs_batch_create_execute()` to send, or `securitycenter_organizations_resource_value_configs_batch_create` for simplest API.

pub fn securitycenter_organizations_resource_value_configs_batch_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BatchCreateResourceValueConfigsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/resourceValueConfigs:batchCreate",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs:batchCreate
/// Creates a ResourceValueConfig for an organization. Maps user's tags to difference resource values for use by the attack path simulation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_resource_value_configs_batch_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_resource_value_configs_batch_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_batch_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchCreateResourceValueConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchCreateResourceValueConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs:batchCreate
/// Creates a ResourceValueConfig for an organization. Maps user's tags to difference resource values for use by the attack path simulation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_resource_value_configs_batch_create_builder()` + `securitycenter_organizations_resource_value_configs_batch_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_batch_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BatchCreateResourceValueConfigsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchCreateResourceValueConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_resource_value_configs_batch_create_builder(
        client, parent, body,
    )?;
    securitycenter_organizations_resource_value_configs_batch_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Deletes a ResourceValueConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_resource_value_configs_delete_execute()` to send, or `securitycenter_organizations_resource_value_configs_delete` for simplest API.

pub fn securitycenter_organizations_resource_value_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/resourceValueConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Deletes a ResourceValueConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_resource_value_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_resource_value_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Deletes a ResourceValueConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_resource_value_configs_delete_builder()` + `securitycenter_organizations_resource_value_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_resource_value_configs_delete_builder(client, name)?;
    securitycenter_organizations_resource_value_configs_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Gets a ResourceValueConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_resource_value_configs_get_execute()` to send, or `securitycenter_organizations_resource_value_configs_get` for simplest API.

pub fn securitycenter_organizations_resource_value_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/resourceValueConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Gets a ResourceValueConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_resource_value_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_resource_value_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ResourceValueConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1ResourceValueConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Gets a ResourceValueConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_resource_value_configs_get_builder()` + `securitycenter_organizations_resource_value_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ResourceValueConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_resource_value_configs_get_builder(client, name)?;
    securitycenter_organizations_resource_value_configs_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs
/// Lists all ResourceValueConfigs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_resource_value_configs_list_execute()` to send, or `securitycenter_organizations_resource_value_configs_list` for simplest API.

pub fn securitycenter_organizations_resource_value_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/resourceValueConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs
/// Lists all ResourceValueConfigs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_resource_value_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_resource_value_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListResourceValueConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListResourceValueConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs
/// Lists all ResourceValueConfigs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_resource_value_configs_list_builder()` + `securitycenter_organizations_resource_value_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListResourceValueConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_resource_value_configs_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    securitycenter_organizations_resource_value_configs_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Updates an existing ResourceValueConfigs with new rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_resource_value_configs_patch_execute()` to send, or `securitycenter_organizations_resource_value_configs_patch` for simplest API.

pub fn securitycenter_organizations_resource_value_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1ResourceValueConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/resourceValueConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Updates an existing ResourceValueConfigs with new rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_resource_value_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_resource_value_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ResourceValueConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1ResourceValueConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Updates an existing ResourceValueConfigs with new rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_resource_value_configs_patch_builder()` + `securitycenter_organizations_resource_value_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1ResourceValueConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ResourceValueConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_resource_value_configs_patch_builder(
        client, name, updateMask, body,
    )?;
    securitycenter_organizations_resource_value_configs_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_custom_modules_create` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/customModules",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_security_health_analytics_settings_custom_modules_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_builder()` + `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_custom_modules_create_builder(client, parent, body)?;
    securitycenter_organizations_security_health_analytics_settings_custom_modules_create_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_custom_modules_delete` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/customModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_builder()` + `securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_builder(client, name)?;
    securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_custom_modules_get_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_custom_modules_get` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/customModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_security_health_analytics_settings_custom_modules_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_get_builder()` + `securitycenter_organizations_security_health_analytics_settings_custom_modules_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_security_health_analytics_settings_custom_modules_get_builder(
            client, name,
        )?;
    securitycenter_organizations_security_health_analytics_settings_custom_modules_get_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_custom_modules_list` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/customModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_security_health_analytics_settings_custom_modules_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecurityHealthAnalyticsCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_builder()` + `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecurityHealthAnalyticsCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_custom_modules_list_builder(client, parent, pageSize, pageToken)?;
    securitycenter_organizations_security_health_analytics_settings_custom_modules_list_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/customModules:listDescendant",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_builder()` + `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_builder(client, parent, pageSize, pageToken)?;
    securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_custom_modules_patch` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/customModules/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_builder()` + `securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_builder(client, name, updateMask, body)?;
    securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SimulateSecurityHealthAnalyticsCustomModuleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/customModules:simulate",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SimulateSecurityHealthAnalyticsCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_builder()` + `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SimulateSecurityHealthAnalyticsCustomModuleRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_builder(client, parent, body)?;
    securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/effectiveCustomModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_builder()` + `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_builder(client, name)?;
    securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/effectiveCustomModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_builder()` + `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_builder(client, parent, pageSize, pageToken)?;
    securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}
/// Get the simulation by name or the latest simulation for the given organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_simulations_get_execute()` to send, or `securitycenter_organizations_simulations_get` for simplest API.

pub fn securitycenter_organizations_simulations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/simulations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}
/// Get the simulation by name or the latest simulation for the given organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_simulations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_simulations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Simulation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Simulation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}
/// Get the simulation by name or the latest simulation for the given organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_simulations_get_builder()` + `securitycenter_organizations_simulations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Simulation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_simulations_get_builder(client, name)?;
    securitycenter_organizations_simulations_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackExposureResults/{attackExposureResultsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_execute()` to send, or `securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list` for simplest API.

pub fn securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/simulations/{}/attackExposureResults/{}/attackPaths",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackExposureResults/{attackExposureResultsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAttackPathsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackExposureResults/{attackExposureResultsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_builder()` + `securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_builder(
            client, parent, filter, pageSize, pageToken,
        )?;
    securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackExposureResults/{attackExposureResultsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_execute()` to send, or `securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list` for simplest API.

pub fn securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/simulations/{}/attackExposureResults/{}/valuedResources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackExposureResults/{attackExposureResultsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListValuedResourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackExposureResults/{attackExposureResultsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_builder()` + `securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_builder(client, parent, filter, orderBy, pageSize, pageToken)?;
    securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_simulations_attack_paths_list_execute()` to send, or `securitycenter_organizations_simulations_attack_paths_list` for simplest API.

pub fn securitycenter_organizations_simulations_attack_paths_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/simulations/{}/attackPaths",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_simulations_attack_paths_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_simulations_attack_paths_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_attack_paths_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAttackPathsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_simulations_attack_paths_list_builder()` + `securitycenter_organizations_simulations_attack_paths_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_attack_paths_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_simulations_attack_paths_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    securitycenter_organizations_simulations_attack_paths_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources/{valuedResourcesId}
/// Get the valued resource by name
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_simulations_valued_resources_get_execute()` to send, or `securitycenter_organizations_simulations_valued_resources_get` for simplest API.

pub fn securitycenter_organizations_simulations_valued_resources_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/simulations/{}/valuedResources/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources/{valuedResourcesId}
/// Get the valued resource by name
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_simulations_valued_resources_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_simulations_valued_resources_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_valued_resources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ValuedResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValuedResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources/{valuedResourcesId}
/// Get the valued resource by name
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_simulations_valued_resources_get_builder()` + `securitycenter_organizations_simulations_valued_resources_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_valued_resources_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ValuedResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_simulations_valued_resources_get_builder(client, name)?;
    securitycenter_organizations_simulations_valued_resources_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_simulations_valued_resources_list_execute()` to send, or `securitycenter_organizations_simulations_valued_resources_list` for simplest API.

pub fn securitycenter_organizations_simulations_valued_resources_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/simulations/{}/valuedResources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_simulations_valued_resources_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_simulations_valued_resources_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_valued_resources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListValuedResourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_simulations_valued_resources_list_builder()` + `securitycenter_organizations_simulations_valued_resources_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_valued_resources_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_simulations_valued_resources_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    securitycenter_organizations_simulations_valued_resources_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources/{valuedResourcesId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_simulations_valued_resources_attack_paths_list_execute()` to send, or `securitycenter_organizations_simulations_valued_resources_attack_paths_list` for simplest API.

pub fn securitycenter_organizations_simulations_valued_resources_attack_paths_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/simulations/{}/valuedResources/{}/attackPaths",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources/{valuedResourcesId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_simulations_valued_resources_attack_paths_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_simulations_valued_resources_attack_paths_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_valued_resources_attack_paths_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAttackPathsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources/{valuedResourcesId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_simulations_valued_resources_attack_paths_list_builder()` + `securitycenter_organizations_simulations_valued_resources_attack_paths_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_valued_resources_attack_paths_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_simulations_valued_resources_attack_paths_list_builder(
            client, parent, filter, pageSize, pageToken,
        )?;
    securitycenter_organizations_simulations_valued_resources_attack_paths_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources
/// Creates a source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_create_execute()` to send, or `securitycenter_organizations_sources_create` for simplest API.

pub fn securitycenter_organizations_sources_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Source,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources
/// Creates a source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_sources_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_sources_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources
/// Creates a source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_create_builder()` + `securitycenter_organizations_sources_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Source,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_sources_create_builder(client, parent, body)?;
    securitycenter_organizations_sources_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}
/// Gets a source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_get_execute()` to send, or `securitycenter_organizations_sources_get` for simplest API.

pub fn securitycenter_organizations_sources_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}
/// Gets a source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_sources_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_sources_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}
/// Gets a source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_get_builder()` + `securitycenter_organizations_sources_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_sources_get_builder(client, name)?;
    securitycenter_organizations_sources_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}:getIamPolicy
/// Gets the access control policy on the specified Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_get_iam_policy_execute()` to send, or `securitycenter_organizations_sources_get_iam_policy` for simplest API.

pub fn securitycenter_organizations_sources_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}:getIamPolicy
/// Gets the access control policy on the specified Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_sources_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_sources_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}:getIamPolicy
/// Gets the access control policy on the specified Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_get_iam_policy_builder()` + `securitycenter_organizations_sources_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_sources_get_iam_policy_builder(client, resource, body)?;
    securitycenter_organizations_sources_get_iam_policy_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources
/// Lists all sources belonging to an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_list_execute()` to send, or `securitycenter_organizations_sources_list` for simplest API.

pub fn securitycenter_organizations_sources_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sources
/// Lists all sources belonging to an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_sources_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_sources_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources
/// Lists all sources belonging to an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_list_builder()` + `securitycenter_organizations_sources_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_sources_list_builder(client, parent, pageSize, pageToken)?;
    securitycenter_organizations_sources_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}
/// Updates a source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_patch_execute()` to send, or `securitycenter_organizations_sources_patch` for simplest API.

pub fn securitycenter_organizations_sources_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Source,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}
/// Updates a source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_sources_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_sources_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}
/// Updates a source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_patch_builder()` + `securitycenter_organizations_sources_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Source,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_sources_patch_builder(client, name, updateMask, body)?;
    securitycenter_organizations_sources_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}:setIamPolicy
/// Sets the access control policy on the specified Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_set_iam_policy_execute()` to send, or `securitycenter_organizations_sources_set_iam_policy` for simplest API.

pub fn securitycenter_organizations_sources_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}:setIamPolicy
/// Sets the access control policy on the specified Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_sources_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_sources_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}:setIamPolicy
/// Sets the access control policy on the specified Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_set_iam_policy_builder()` + `securitycenter_organizations_sources_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_sources_set_iam_policy_builder(client, resource, body)?;
    securitycenter_organizations_sources_set_iam_policy_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}:testIamPermissions
/// Returns the permissions that a caller has on the specified source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_test_iam_permissions_execute()` to send, or `securitycenter_organizations_sources_test_iam_permissions` for simplest API.

pub fn securitycenter_organizations_sources_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}:testIamPermissions
/// Returns the permissions that a caller has on the specified source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_sources_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_sources_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}:testIamPermissions
/// Returns the permissions that a caller has on the specified source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_test_iam_permissions_builder()` + `securitycenter_organizations_sources_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_sources_test_iam_permissions_builder(client, resource, body)?;
    securitycenter_organizations_sources_test_iam_permissions_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings
/// Creates a finding. The corresponding source must exist for finding creation to succeed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_findings_create_execute()` to send, or `securitycenter_organizations_sources_findings_create` for simplest API.

pub fn securitycenter_organizations_sources_findings_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    findingId: Option<&str>,
    body: &Finding,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{}/findings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = findingId {
        query_parts.push(format!("findingId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings
/// Creates a finding. The corresponding source must exist for finding creation to succeed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_sources_findings_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_sources_findings_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings
/// Creates a finding. The corresponding source must exist for finding creation to succeed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_findings_create_builder()` + `securitycenter_organizations_sources_findings_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_create(
    client: &SimpleHttpClient,
    parent: &str,
    findingId: Option<&str>,
    body: &Finding,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_sources_findings_create_builder(
        client, parent, findingId, body,
    )?;
    securitycenter_organizations_sources_findings_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings:group
/// Filters an organization or source's findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_findings_group_execute()` to send, or `securitycenter_organizations_sources_findings_group` for simplest API.

pub fn securitycenter_organizations_sources_findings_group_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GroupFindingsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{}/findings:group",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings:group
/// Filters an organization or source's findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_sources_findings_group_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_sources_findings_group_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupFindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings:group
/// Filters an organization or source's findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_findings_group_builder()` + `securitycenter_organizations_sources_findings_group_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_group(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GroupFindingsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_sources_findings_group_builder(client, parent, body)?;
    securitycenter_organizations_sources_findings_group_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings
/// Lists an organization or source's findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_findings_list_execute()` to send, or `securitycenter_organizations_sources_findings_list` for simplest API.

pub fn securitycenter_organizations_sources_findings_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    compareDuration: Option<&str>,
    fieldMask: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readTime: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{}/findings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = compareDuration {
        query_parts.push(format!("compareDuration={}", val));
    }
    if let Some(val) = fieldMask {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime {
        query_parts.push(format!("readTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings
/// Lists an organization or source's findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_sources_findings_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_sources_findings_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings
/// Lists an organization or source's findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_findings_list_builder()` + `securitycenter_organizations_sources_findings_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_list(
    client: &SimpleHttpClient,
    parent: &str,
    compareDuration: Option<&str>,
    fieldMask: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readTime: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_sources_findings_list_builder(
        client,
        parent,
        compareDuration,
        fieldMask,
        filter,
        orderBy,
        pageSize,
        pageToken,
        readTime,
    )?;
    securitycenter_organizations_sources_findings_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_findings_patch_execute()` to send, or `securitycenter_organizations_sources_findings_patch` for simplest API.

pub fn securitycenter_organizations_sources_findings_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Finding,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{}/findings/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_sources_findings_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_sources_findings_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_findings_patch_builder()` + `securitycenter_organizations_sources_findings_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Finding,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_sources_findings_patch_builder(
        client, name, updateMask, body,
    )?;
    securitycenter_organizations_sources_findings_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_findings_set_mute_execute()` to send, or `securitycenter_organizations_sources_findings_set_mute` for simplest API.

pub fn securitycenter_organizations_sources_findings_set_mute_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetMuteRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{}/findings/{}:setMute",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_sources_findings_set_mute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_sources_findings_set_mute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_set_mute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_findings_set_mute_builder()` + `securitycenter_organizations_sources_findings_set_mute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_set_mute(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetMuteRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_sources_findings_set_mute_builder(client, name, body)?;
    securitycenter_organizations_sources_findings_set_mute_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_findings_set_state_execute()` to send, or `securitycenter_organizations_sources_findings_set_state` for simplest API.

pub fn securitycenter_organizations_sources_findings_set_state_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetFindingStateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{}/findings/{}:setState",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_sources_findings_set_state_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_sources_findings_set_state_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_set_state_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_findings_set_state_builder()` + `securitycenter_organizations_sources_findings_set_state_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_set_state(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetFindingStateRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_sources_findings_set_state_builder(client, name, body)?;
    securitycenter_organizations_sources_findings_set_state_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_findings_update_security_marks_execute()` to send, or `securitycenter_organizations_sources_findings_update_security_marks` for simplest API.

pub fn securitycenter_organizations_sources_findings_update_security_marks_builder(
    client: &SimpleHttpClient,
    name: &str,
    startTime: Option<&str>,
    updateMask: Option<&str>,
    body: &SecurityMarks,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{}/findings/{}/securityMarks",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_sources_findings_update_security_marks_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_sources_findings_update_security_marks_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_update_security_marks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityMarks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_findings_update_security_marks_builder()` + `securitycenter_organizations_sources_findings_update_security_marks_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_update_security_marks(
    client: &SimpleHttpClient,
    name: &str,
    startTime: Option<&str>,
    updateMask: Option<&str>,
    body: &SecurityMarks,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_sources_findings_update_security_marks_builder(
        client, name, startTime, updateMask, body,
    )?;
    securitycenter_organizations_sources_findings_update_security_marks_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_findings_external_systems_patch_execute()` to send, or `securitycenter_organizations_sources_findings_external_systems_patch` for simplest API.

pub fn securitycenter_organizations_sources_findings_external_systems_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1ExternalSystem,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{}/findings/{}/externalSystems/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_sources_findings_external_systems_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_sources_findings_external_systems_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_external_systems_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ExternalSystem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1ExternalSystem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_findings_external_systems_patch_builder()` + `securitycenter_organizations_sources_findings_external_systems_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_external_systems_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1ExternalSystem,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ExternalSystem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_sources_findings_external_systems_patch_builder(
        client, name, updateMask, body,
    )?;
    securitycenter_organizations_sources_findings_external_systems_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_valued_resources_list_execute()` to send, or `securitycenter_organizations_valued_resources_list` for simplest API.

pub fn securitycenter_organizations_valued_resources_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/valuedResources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_organizations_valued_resources_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_organizations_valued_resources_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_valued_resources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListValuedResourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_valued_resources_list_builder()` + `securitycenter_organizations_valued_resources_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_valued_resources_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_valued_resources_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    securitycenter_organizations_valued_resources_list_execute(builder)
}

/// GET v1/projects/{projectsId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_assets_group_execute()` to send, or `securitycenter_projects_assets_group` for simplest API.

pub fn securitycenter_projects_assets_group_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GroupAssetsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/assets:group",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_assets_group_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_assets_group_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_assets_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_assets_group_builder()` + `securitycenter_projects_assets_group_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_assets_group(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GroupAssetsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_assets_group_builder(client, parent, body)?;
    securitycenter_projects_assets_group_execute(builder)
}

/// GET v1/projects/{projectsId}/assets
/// Lists an organization's assets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_assets_list_execute()` to send, or `securitycenter_projects_assets_list` for simplest API.

pub fn securitycenter_projects_assets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    compareDuration: Option<&str>,
    fieldMask: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readTime: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/assets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = compareDuration {
        query_parts.push(format!("compareDuration={}", val));
    }
    if let Some(val) = fieldMask {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime {
        query_parts.push(format!("readTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/assets
/// Lists an organization's assets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_assets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_assets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_assets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/assets
/// Lists an organization's assets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_assets_list_builder()` + `securitycenter_projects_assets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_assets_list(
    client: &SimpleHttpClient,
    parent: &str,
    compareDuration: Option<&str>,
    fieldMask: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readTime: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_assets_list_builder(
        client,
        parent,
        compareDuration,
        fieldMask,
        filter,
        orderBy,
        pageSize,
        pageToken,
        readTime,
    )?;
    securitycenter_projects_assets_list_execute(builder)
}

/// GET v1/projects/{projectsId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_assets_update_security_marks_execute()` to send, or `securitycenter_projects_assets_update_security_marks` for simplest API.

pub fn securitycenter_projects_assets_update_security_marks_builder(
    client: &SimpleHttpClient,
    name: &str,
    startTime: Option<&str>,
    updateMask: Option<&str>,
    body: &SecurityMarks,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/assets/{}/securityMarks",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_assets_update_security_marks_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_assets_update_security_marks_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_assets_update_security_marks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityMarks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_assets_update_security_marks_builder()` + `securitycenter_projects_assets_update_security_marks_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_assets_update_security_marks(
    client: &SimpleHttpClient,
    name: &str,
    startTime: Option<&str>,
    updateMask: Option<&str>,
    body: &SecurityMarks,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_assets_update_security_marks_builder(
        client, name, startTime, updateMask, body,
    )?;
    securitycenter_projects_assets_update_security_marks_execute(builder)
}

/// GET v1/projects/{projectsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_big_query_exports_create_execute()` to send, or `securitycenter_projects_big_query_exports_create` for simplest API.

pub fn securitycenter_projects_big_query_exports_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    bigQueryExportId: Option<&str>,
    body: &GoogleCloudSecuritycenterV1BigQueryExport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/bigQueryExports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bigQueryExportId {
        query_parts.push(format!("bigQueryExportId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_big_query_exports_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_big_query_exports_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_big_query_exports_create_builder()` + `securitycenter_projects_big_query_exports_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_create(
    client: &SimpleHttpClient,
    parent: &str,
    bigQueryExportId: Option<&str>,
    body: &GoogleCloudSecuritycenterV1BigQueryExport,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_big_query_exports_create_builder(
        client,
        parent,
        bigQueryExportId,
        body,
    )?;
    securitycenter_projects_big_query_exports_create_execute(builder)
}

/// GET v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_big_query_exports_delete_execute()` to send, or `securitycenter_projects_big_query_exports_delete` for simplest API.

pub fn securitycenter_projects_big_query_exports_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/bigQueryExports/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_big_query_exports_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_big_query_exports_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_big_query_exports_delete_builder()` + `securitycenter_projects_big_query_exports_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_big_query_exports_delete_builder(client, name)?;
    securitycenter_projects_big_query_exports_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_big_query_exports_get_execute()` to send, or `securitycenter_projects_big_query_exports_get` for simplest API.

pub fn securitycenter_projects_big_query_exports_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/bigQueryExports/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_big_query_exports_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_big_query_exports_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_big_query_exports_get_builder()` + `securitycenter_projects_big_query_exports_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_big_query_exports_get_builder(client, name)?;
    securitycenter_projects_big_query_exports_get_execute(builder)
}

/// GET v1/projects/{projectsId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_big_query_exports_list_execute()` to send, or `securitycenter_projects_big_query_exports_list` for simplest API.

pub fn securitycenter_projects_big_query_exports_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/bigQueryExports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_big_query_exports_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_big_query_exports_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBigQueryExportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBigQueryExportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_big_query_exports_list_builder()` + `securitycenter_projects_big_query_exports_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBigQueryExportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_big_query_exports_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    securitycenter_projects_big_query_exports_list_execute(builder)
}

/// GET v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_big_query_exports_patch_execute()` to send, or `securitycenter_projects_big_query_exports_patch` for simplest API.

pub fn securitycenter_projects_big_query_exports_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1BigQueryExport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/bigQueryExports/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_big_query_exports_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_big_query_exports_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_big_query_exports_patch_builder()` + `securitycenter_projects_big_query_exports_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1BigQueryExport,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_big_query_exports_patch_builder(client, name, updateMask, body)?;
    securitycenter_projects_big_query_exports_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_validate_custom_module_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_validate_custom_module` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_validate_custom_module_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ValidateEventThreatDetectionCustomModuleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings:validateCustomModule",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_validate_custom_module_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_event_threat_detection_settings_validate_custom_module_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_validate_custom_module_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValidateEventThreatDetectionCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_validate_custom_module_builder()` + `securitycenter_projects_event_threat_detection_settings_validate_custom_module_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_validate_custom_module(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ValidateEventThreatDetectionCustomModuleRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_event_threat_detection_settings_validate_custom_module_builder(
            client, parent, body,
        )?;
    securitycenter_projects_event_threat_detection_settings_validate_custom_module_execute(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_custom_modules_create_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_custom_modules_create` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EventThreatDetectionCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/customModules",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_event_threat_detection_settings_custom_modules_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_create_builder()` + `securitycenter_projects_event_threat_detection_settings_custom_modules_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EventThreatDetectionCustomModule,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_event_threat_detection_settings_custom_modules_create_builder(
            client, parent, body,
        )?;
    securitycenter_projects_event_threat_detection_settings_custom_modules_create_execute(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_custom_modules_delete_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_custom_modules_delete` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/customModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_event_threat_detection_settings_custom_modules_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_delete_builder()` + `securitycenter_projects_event_threat_detection_settings_custom_modules_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_event_threat_detection_settings_custom_modules_delete_builder(
            client, name,
        )?;
    securitycenter_projects_event_threat_detection_settings_custom_modules_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_custom_modules_get_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_custom_modules_get` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/customModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_event_threat_detection_settings_custom_modules_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_get_builder()` + `securitycenter_projects_event_threat_detection_settings_custom_modules_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_event_threat_detection_settings_custom_modules_get_builder(
            client, name,
        )?;
    securitycenter_projects_event_threat_detection_settings_custom_modules_get_execute(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_custom_modules_list` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/customModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_event_threat_detection_settings_custom_modules_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListEventThreatDetectionCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_builder()` + `securitycenter_projects_event_threat_detection_settings_custom_modules_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListEventThreatDetectionCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_event_threat_detection_settings_custom_modules_list_builder(
            client, parent, pageSize, pageToken,
        )?;
    securitycenter_projects_event_threat_detection_settings_custom_modules_list_execute(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/customModules:listDescendant",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_builder()` + `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_builder(client, parent, pageSize, pageToken)?;
    securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_custom_modules_patch_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_custom_modules_patch` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &EventThreatDetectionCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/customModules/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_event_threat_detection_settings_custom_modules_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_patch_builder()` + `securitycenter_projects_event_threat_detection_settings_custom_modules_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &EventThreatDetectionCustomModule,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_event_threat_detection_settings_custom_modules_patch_builder(
            client, name, updateMask, body,
        )?;
    securitycenter_projects_event_threat_detection_settings_custom_modules_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/effectiveCustomModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EffectiveEventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EffectiveEventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_builder()` + `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EffectiveEventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_builder(client, name)?;
    securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/effectiveCustomModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_builder()` + `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_builder(client, parent, pageSize, pageToken)?;
    securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_findings_bulk_mute_execute()` to send, or `securitycenter_projects_findings_bulk_mute` for simplest API.

pub fn securitycenter_projects_findings_bulk_mute_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BulkMuteFindingsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/findings:bulkMute",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_findings_bulk_mute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_findings_bulk_mute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_findings_bulk_mute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_findings_bulk_mute_builder()` + `securitycenter_projects_findings_bulk_mute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_findings_bulk_mute(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BulkMuteFindingsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_findings_bulk_mute_builder(client, parent, body)?;
    securitycenter_projects_findings_bulk_mute_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_locations_mute_configs_delete_execute()` to send, or `securitycenter_projects_locations_mute_configs_delete` for simplest API.

pub fn securitycenter_projects_locations_mute_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/locations/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_locations_mute_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_locations_mute_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_locations_mute_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_locations_mute_configs_delete_builder()` + `securitycenter_projects_locations_mute_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_locations_mute_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_locations_mute_configs_delete_builder(client, name)?;
    securitycenter_projects_locations_mute_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_locations_mute_configs_get_execute()` to send, or `securitycenter_projects_locations_mute_configs_get` for simplest API.

pub fn securitycenter_projects_locations_mute_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/locations/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_locations_mute_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_locations_mute_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_locations_mute_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_locations_mute_configs_get_builder()` + `securitycenter_projects_locations_mute_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_locations_mute_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_locations_mute_configs_get_builder(client, name)?;
    securitycenter_projects_locations_mute_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_locations_mute_configs_patch_execute()` to send, or `securitycenter_projects_locations_mute_configs_patch` for simplest API.

pub fn securitycenter_projects_locations_mute_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/locations/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_locations_mute_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_locations_mute_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_locations_mute_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_locations_mute_configs_patch_builder()` + `securitycenter_projects_locations_mute_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_locations_mute_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_locations_mute_configs_patch_builder(
        client, name, updateMask, body,
    )?;
    securitycenter_projects_locations_mute_configs_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/muteConfigs
/// Creates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_mute_configs_create_execute()` to send, or `securitycenter_projects_mute_configs_create` for simplest API.

pub fn securitycenter_projects_mute_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    muteConfigId: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/muteConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = muteConfigId {
        query_parts.push(format!("muteConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/muteConfigs
/// Creates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_mute_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_mute_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/muteConfigs
/// Creates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_mute_configs_create_builder()` + `securitycenter_projects_mute_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    muteConfigId: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_mute_configs_create_builder(client, parent, muteConfigId, body)?;
    securitycenter_projects_mute_configs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_mute_configs_delete_execute()` to send, or `securitycenter_projects_mute_configs_delete` for simplest API.

pub fn securitycenter_projects_mute_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_mute_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_mute_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_mute_configs_delete_builder()` + `securitycenter_projects_mute_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_mute_configs_delete_builder(client, name)?;
    securitycenter_projects_mute_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_mute_configs_get_execute()` to send, or `securitycenter_projects_mute_configs_get` for simplest API.

pub fn securitycenter_projects_mute_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_mute_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_mute_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_mute_configs_get_builder()` + `securitycenter_projects_mute_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_mute_configs_get_builder(client, name)?;
    securitycenter_projects_mute_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/muteConfigs
/// Lists mute configs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_mute_configs_list_execute()` to send, or `securitycenter_projects_mute_configs_list` for simplest API.

pub fn securitycenter_projects_mute_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/muteConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/muteConfigs
/// Lists mute configs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_mute_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_mute_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMuteConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMuteConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/muteConfigs
/// Lists mute configs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_mute_configs_list_builder()` + `securitycenter_projects_mute_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMuteConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_mute_configs_list_builder(client, parent, pageSize, pageToken)?;
    securitycenter_projects_mute_configs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_mute_configs_patch_execute()` to send, or `securitycenter_projects_mute_configs_patch` for simplest API.

pub fn securitycenter_projects_mute_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/muteConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_mute_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_mute_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_mute_configs_patch_builder()` + `securitycenter_projects_mute_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_mute_configs_patch_builder(client, name, updateMask, body)?;
    securitycenter_projects_mute_configs_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/notificationConfigs
/// Creates a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_notification_configs_create_execute()` to send, or `securitycenter_projects_notification_configs_create` for simplest API.

pub fn securitycenter_projects_notification_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    configId: Option<&str>,
    body: &NotificationConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/notificationConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = configId {
        query_parts.push(format!("configId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/notificationConfigs
/// Creates a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_notification_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_notification_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/notificationConfigs
/// Creates a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_notification_configs_create_builder()` + `securitycenter_projects_notification_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    configId: Option<&str>,
    body: &NotificationConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_notification_configs_create_builder(
        client, parent, configId, body,
    )?;
    securitycenter_projects_notification_configs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_notification_configs_delete_execute()` to send, or `securitycenter_projects_notification_configs_delete` for simplest API.

pub fn securitycenter_projects_notification_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/notificationConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_notification_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_notification_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_notification_configs_delete_builder()` + `securitycenter_projects_notification_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_notification_configs_delete_builder(client, name)?;
    securitycenter_projects_notification_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_notification_configs_get_execute()` to send, or `securitycenter_projects_notification_configs_get` for simplest API.

pub fn securitycenter_projects_notification_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/notificationConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_notification_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_notification_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_notification_configs_get_builder()` + `securitycenter_projects_notification_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_notification_configs_get_builder(client, name)?;
    securitycenter_projects_notification_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/notificationConfigs
/// Lists notification configs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_notification_configs_list_execute()` to send, or `securitycenter_projects_notification_configs_list` for simplest API.

pub fn securitycenter_projects_notification_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/notificationConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/notificationConfigs
/// Lists notification configs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_notification_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_notification_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNotificationConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/notificationConfigs
/// Lists notification configs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_notification_configs_list_builder()` + `securitycenter_projects_notification_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_notification_configs_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    securitycenter_projects_notification_configs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_notification_configs_patch_execute()` to send, or `securitycenter_projects_notification_configs_patch` for simplest API.

pub fn securitycenter_projects_notification_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &NotificationConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/notificationConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_notification_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_notification_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_notification_configs_patch_builder()` + `securitycenter_projects_notification_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &NotificationConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_notification_configs_patch_builder(client, name, updateMask, body)?;
    securitycenter_projects_notification_configs_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_custom_modules_create_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_custom_modules_create` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/customModules",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_security_health_analytics_settings_custom_modules_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_create_builder()` + `securitycenter_projects_security_health_analytics_settings_custom_modules_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_security_health_analytics_settings_custom_modules_create_builder(
            client, parent, body,
        )?;
    securitycenter_projects_security_health_analytics_settings_custom_modules_create_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_custom_modules_delete_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_custom_modules_delete` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/customModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_security_health_analytics_settings_custom_modules_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_delete_builder()` + `securitycenter_projects_security_health_analytics_settings_custom_modules_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_security_health_analytics_settings_custom_modules_delete_builder(
            client, name,
        )?;
    securitycenter_projects_security_health_analytics_settings_custom_modules_delete_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_custom_modules_get_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_custom_modules_get` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/customModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_security_health_analytics_settings_custom_modules_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_get_builder()` + `securitycenter_projects_security_health_analytics_settings_custom_modules_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_security_health_analytics_settings_custom_modules_get_builder(
            client, name,
        )?;
    securitycenter_projects_security_health_analytics_settings_custom_modules_get_execute(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_custom_modules_list` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/customModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_security_health_analytics_settings_custom_modules_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecurityHealthAnalyticsCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_builder()` + `securitycenter_projects_security_health_analytics_settings_custom_modules_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecurityHealthAnalyticsCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_security_health_analytics_settings_custom_modules_list_builder(
            client, parent, pageSize, pageToken,
        )?;
    securitycenter_projects_security_health_analytics_settings_custom_modules_list_execute(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/customModules:listDescendant",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_builder()` + `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_builder(client, parent, pageSize, pageToken)?;
    securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_execute(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_custom_modules_patch_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_custom_modules_patch` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/customModules/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_security_health_analytics_settings_custom_modules_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_patch_builder()` + `securitycenter_projects_security_health_analytics_settings_custom_modules_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_security_health_analytics_settings_custom_modules_patch_builder(
            client, name, updateMask, body,
        )?;
    securitycenter_projects_security_health_analytics_settings_custom_modules_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SimulateSecurityHealthAnalyticsCustomModuleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/customModules:simulate",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SimulateSecurityHealthAnalyticsCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_builder()` + `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_simulate(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SimulateSecurityHealthAnalyticsCustomModuleRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_builder(
            client, parent, body,
        )?;
    securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/effectiveCustomModules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_builder()` + `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_builder(client, name)?;
    securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/effectiveCustomModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_builder()` + `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_builder(client, parent, pageSize, pageToken)?;
    securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/sources
/// Lists all sources belonging to an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_sources_list_execute()` to send, or `securitycenter_projects_sources_list` for simplest API.

pub fn securitycenter_projects_sources_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/sources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/sources
/// Lists all sources belonging to an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_sources_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_sources_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/sources
/// Lists all sources belonging to an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_sources_list_builder()` + `securitycenter_projects_sources_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_sources_list_builder(client, parent, pageSize, pageToken)?;
    securitycenter_projects_sources_list_execute(builder)
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings:group
/// Filters an organization or source's findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_sources_findings_group_execute()` to send, or `securitycenter_projects_sources_findings_group` for simplest API.

pub fn securitycenter_projects_sources_findings_group_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GroupFindingsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/sources/{}/findings:group",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings:group
/// Filters an organization or source's findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_sources_findings_group_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_sources_findings_group_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupFindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings:group
/// Filters an organization or source's findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_sources_findings_group_builder()` + `securitycenter_projects_sources_findings_group_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_group(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GroupFindingsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_sources_findings_group_builder(client, parent, body)?;
    securitycenter_projects_sources_findings_group_execute(builder)
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings
/// Lists an organization or source's findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_sources_findings_list_execute()` to send, or `securitycenter_projects_sources_findings_list` for simplest API.

pub fn securitycenter_projects_sources_findings_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    compareDuration: Option<&str>,
    fieldMask: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readTime: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/sources/{}/findings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = compareDuration {
        query_parts.push(format!("compareDuration={}", val));
    }
    if let Some(val) = fieldMask {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime {
        query_parts.push(format!("readTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings
/// Lists an organization or source's findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_sources_findings_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_sources_findings_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings
/// Lists an organization or source's findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_sources_findings_list_builder()` + `securitycenter_projects_sources_findings_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_list(
    client: &SimpleHttpClient,
    parent: &str,
    compareDuration: Option<&str>,
    fieldMask: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readTime: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_sources_findings_list_builder(
        client,
        parent,
        compareDuration,
        fieldMask,
        filter,
        orderBy,
        pageSize,
        pageToken,
        readTime,
    )?;
    securitycenter_projects_sources_findings_list_execute(builder)
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_sources_findings_patch_execute()` to send, or `securitycenter_projects_sources_findings_patch` for simplest API.

pub fn securitycenter_projects_sources_findings_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Finding,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/sources/{}/findings/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_sources_findings_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_sources_findings_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_sources_findings_patch_builder()` + `securitycenter_projects_sources_findings_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Finding,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_sources_findings_patch_builder(client, name, updateMask, body)?;
    securitycenter_projects_sources_findings_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_sources_findings_set_mute_execute()` to send, or `securitycenter_projects_sources_findings_set_mute` for simplest API.

pub fn securitycenter_projects_sources_findings_set_mute_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetMuteRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/sources/{}/findings/{}:setMute",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_sources_findings_set_mute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_sources_findings_set_mute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_set_mute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_sources_findings_set_mute_builder()` + `securitycenter_projects_sources_findings_set_mute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_set_mute(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetMuteRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_sources_findings_set_mute_builder(client, name, body)?;
    securitycenter_projects_sources_findings_set_mute_execute(builder)
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_sources_findings_set_state_execute()` to send, or `securitycenter_projects_sources_findings_set_state` for simplest API.

pub fn securitycenter_projects_sources_findings_set_state_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetFindingStateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/sources/{}/findings/{}:setState",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_sources_findings_set_state_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_sources_findings_set_state_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_set_state_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_sources_findings_set_state_builder()` + `securitycenter_projects_sources_findings_set_state_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_set_state(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetFindingStateRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_sources_findings_set_state_builder(client, name, body)?;
    securitycenter_projects_sources_findings_set_state_execute(builder)
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_sources_findings_update_security_marks_execute()` to send, or `securitycenter_projects_sources_findings_update_security_marks` for simplest API.

pub fn securitycenter_projects_sources_findings_update_security_marks_builder(
    client: &SimpleHttpClient,
    name: &str,
    startTime: Option<&str>,
    updateMask: Option<&str>,
    body: &SecurityMarks,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/sources/{}/findings/{}/securityMarks",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_sources_findings_update_security_marks_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_sources_findings_update_security_marks_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_update_security_marks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityMarks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_sources_findings_update_security_marks_builder()` + `securitycenter_projects_sources_findings_update_security_marks_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_update_security_marks(
    client: &SimpleHttpClient,
    name: &str,
    startTime: Option<&str>,
    updateMask: Option<&str>,
    body: &SecurityMarks,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_sources_findings_update_security_marks_builder(
        client, name, startTime, updateMask, body,
    )?;
    securitycenter_projects_sources_findings_update_security_marks_execute(builder)
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_sources_findings_external_systems_patch_execute()` to send, or `securitycenter_projects_sources_findings_external_systems_patch` for simplest API.

pub fn securitycenter_projects_sources_findings_external_systems_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1ExternalSystem,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/sources/{}/findings/{}/externalSystems/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `securitycenter_projects_sources_findings_external_systems_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from securitycenter_projects_sources_findings_external_systems_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_external_systems_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ExternalSystem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1ExternalSystem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_sources_findings_external_systems_patch_builder()` + `securitycenter_projects_sources_findings_external_systems_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_external_systems_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudSecuritycenterV1ExternalSystem,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ExternalSystem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_sources_findings_external_systems_patch_builder(
        client, name, updateMask, body,
    )?;
    securitycenter_projects_sources_findings_external_systems_patch_execute(builder)
}
