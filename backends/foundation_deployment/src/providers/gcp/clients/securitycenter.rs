//! Auto-generated API clients for gcp/securitycenter.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/folders/{foldersId}/assets:group
/// Filters an organization''s assets and groups them by their specified properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_assets_group_execute()` to send, or `securitycenter_folders_assets_group` for simplest API.

pub fn securitycenter_folders_assets_group_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/assets:group",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/folders/{foldersId}/assets:group
/// Filters an organization''s assets and groups them by their specified properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_assets_group_execute()` or `securitycenter_folders_assets_group`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_assets_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_assets_group_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GroupAssetsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/folders/{foldersId}/assets:group
/// Filters an organization''s assets and groups them by their specified properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_assets_group_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_assets_group_task()`.
/// For the simplest API, use `securitycenter_folders_assets_group()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_assets_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_assets_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_assets_group_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_assets_group`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersAssetsGroupArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/folders/{foldersId}/assets:group
/// Filters an organization''s assets and groups them by their specified properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_assets_group_builder()` + `securitycenter_folders_assets_group_execute()`.
/// For task-level control, use `securitycenter_folders_assets_group_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_assets_group(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersAssetsGroupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_assets_group_builder(client, &args.parent)?;
    securitycenter_folders_assets_group_execute(builder)
}

/// GET v1/folders/{foldersId}/assets
/// Lists an organization''s assets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_assets_list_execute()` to send, or `securitycenter_folders_assets_list` for simplest API.

pub fn securitycenter_folders_assets_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    compareDuration: &Option<String>,
    fieldMask: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    readTime: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/assets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = compareDuration.as_ref() {
        query_parts.push(format!("compareDuration={}", val));
    }
    if let Some(val) = fieldMask.as_ref() {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime.as_ref() {
        query_parts.push(format!("readTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/assets
/// Lists an organization''s assets.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_assets_list_execute()` or `securitycenter_folders_assets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_assets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAssetsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/assets
/// Lists an organization''s assets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_assets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_assets_list_task()`.
/// For the simplest API, use `securitycenter_folders_assets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_assets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_assets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_assets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersAssetsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: compareDuration
    pub compareDuration: Option<String>,
    /// Query parameter: fieldMask
    pub fieldMask: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readTime
    pub readTime: Option<String>,
}

/// GET v1/folders/{foldersId}/assets
/// Lists an organization''s assets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_assets_list_builder()` + `securitycenter_folders_assets_list_execute()`.
/// For task-level control, use `securitycenter_folders_assets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_assets_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersAssetsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_assets_list_builder(
        client,
        &args.parent,
        &args.compareDuration,
        &args.fieldMask,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.readTime,
    )?;
    securitycenter_folders_assets_list_execute(builder)
}

/// PATCH v1/folders/{foldersId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_assets_update_security_marks_execute()` to send, or `securitycenter_folders_assets_update_security_marks` for simplest API.

pub fn securitycenter_folders_assets_update_security_marks_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    startTime: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/assets/{assetsId}/securityMarks",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = startTime.as_ref() {
        query_parts.push(format!("startTime={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/folders/{foldersId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_assets_update_security_marks_execute()` or `securitycenter_folders_assets_update_security_marks`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_assets_update_security_marks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_assets_update_security_marks_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityMarks>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityMarks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/folders/{foldersId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_assets_update_security_marks_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_assets_update_security_marks_task()`.
/// For the simplest API, use `securitycenter_folders_assets_update_security_marks()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_assets_update_security_marks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_assets_update_security_marks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_assets_update_security_marks_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_assets_update_security_marks`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersAssetsUpdateSecurityMarksArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: startTime
    pub startTime: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/folders/{foldersId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_assets_update_security_marks_builder()` + `securitycenter_folders_assets_update_security_marks_execute()`.
/// For task-level control, use `securitycenter_folders_assets_update_security_marks_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_assets_update_security_marks(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersAssetsUpdateSecurityMarksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_assets_update_security_marks_builder(
        client,
        &args.name,
        &args.startTime,
        &args.updateMask,
    )?;
    securitycenter_folders_assets_update_security_marks_execute(builder)
}

/// POST v1/folders/{foldersId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_big_query_exports_create_execute()` to send, or `securitycenter_folders_big_query_exports_create` for simplest API.

pub fn securitycenter_folders_big_query_exports_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    bigQueryExportId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/bigQueryExports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bigQueryExportId.as_ref() {
        query_parts.push(format!("bigQueryExportId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/folders/{foldersId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_big_query_exports_create_execute()` or `securitycenter_folders_big_query_exports_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_big_query_exports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/folders/{foldersId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_big_query_exports_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_big_query_exports_create_task()`.
/// For the simplest API, use `securitycenter_folders_big_query_exports_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_big_query_exports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_big_query_exports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_big_query_exports_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_big_query_exports_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersBigQueryExportsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bigQueryExportId
    pub bigQueryExportId: Option<String>,
}

/// POST v1/folders/{foldersId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_big_query_exports_create_builder()` + `securitycenter_folders_big_query_exports_create_execute()`.
/// For task-level control, use `securitycenter_folders_big_query_exports_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersBigQueryExportsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_big_query_exports_create_builder(
        client,
        &args.parent,
        &args.bigQueryExportId,
    )?;
    securitycenter_folders_big_query_exports_create_execute(builder)
}

/// DELETE v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_big_query_exports_delete_execute()` to send, or `securitycenter_folders_big_query_exports_delete` for simplest API.

pub fn securitycenter_folders_big_query_exports_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/bigQueryExports/{bigQueryExportsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_big_query_exports_delete_execute()` or `securitycenter_folders_big_query_exports_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_big_query_exports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_big_query_exports_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_big_query_exports_delete_task()`.
/// For the simplest API, use `securitycenter_folders_big_query_exports_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_big_query_exports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_big_query_exports_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_folders_big_query_exports_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_big_query_exports_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersBigQueryExportsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_big_query_exports_delete_builder()` + `securitycenter_folders_big_query_exports_delete_execute()`.
/// For task-level control, use `securitycenter_folders_big_query_exports_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersBigQueryExportsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_big_query_exports_delete_builder(client, &args.name)?;
    securitycenter_folders_big_query_exports_delete_execute(builder)
}

/// GET v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_big_query_exports_get_execute()` to send, or `securitycenter_folders_big_query_exports_get` for simplest API.

pub fn securitycenter_folders_big_query_exports_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/bigQueryExports/{bigQueryExportsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_big_query_exports_get_execute()` or `securitycenter_folders_big_query_exports_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_big_query_exports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_big_query_exports_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_big_query_exports_get_task()`.
/// For the simplest API, use `securitycenter_folders_big_query_exports_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_big_query_exports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_big_query_exports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_big_query_exports_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_big_query_exports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersBigQueryExportsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_big_query_exports_get_builder()` + `securitycenter_folders_big_query_exports_get_execute()`.
/// For task-level control, use `securitycenter_folders_big_query_exports_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersBigQueryExportsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_big_query_exports_get_builder(client, &args.name)?;
    securitycenter_folders_big_query_exports_get_execute(builder)
}

/// GET v1/folders/{foldersId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_big_query_exports_list_execute()` to send, or `securitycenter_folders_big_query_exports_list` for simplest API.

pub fn securitycenter_folders_big_query_exports_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/bigQueryExports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_big_query_exports_list_execute()` or `securitycenter_folders_big_query_exports_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_big_query_exports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBigQueryExportsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBigQueryExportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_big_query_exports_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_big_query_exports_list_task()`.
/// For the simplest API, use `securitycenter_folders_big_query_exports_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_big_query_exports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_big_query_exports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBigQueryExportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_big_query_exports_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_big_query_exports_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersBigQueryExportsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_big_query_exports_list_builder()` + `securitycenter_folders_big_query_exports_list_execute()`.
/// For task-level control, use `securitycenter_folders_big_query_exports_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersBigQueryExportsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBigQueryExportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_big_query_exports_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    securitycenter_folders_big_query_exports_list_execute(builder)
}

/// PATCH v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_big_query_exports_patch_execute()` to send, or `securitycenter_folders_big_query_exports_patch` for simplest API.

pub fn securitycenter_folders_big_query_exports_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/bigQueryExports/{bigQueryExportsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_big_query_exports_patch_execute()` or `securitycenter_folders_big_query_exports_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_big_query_exports_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_big_query_exports_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_big_query_exports_patch_task()`.
/// For the simplest API, use `securitycenter_folders_big_query_exports_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_big_query_exports_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_big_query_exports_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_big_query_exports_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_big_query_exports_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersBigQueryExportsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/folders/{foldersId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_big_query_exports_patch_builder()` + `securitycenter_folders_big_query_exports_patch_execute()`.
/// For task-level control, use `securitycenter_folders_big_query_exports_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersBigQueryExportsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_big_query_exports_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_folders_big_query_exports_patch_execute(builder)
}

/// POST v1/folders/{foldersId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_validate_custom_module_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_validate_custom_module` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_validate_custom_module_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings:validateCustomModule",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/folders/{foldersId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_event_threat_detection_settings_validate_custom_module_execute()` or `securitycenter_folders_event_threat_detection_settings_validate_custom_module`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_validate_custom_module_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_validate_custom_module_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValidateEventThreatDetectionCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/folders/{foldersId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_event_threat_detection_settings_validate_custom_module_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_validate_custom_module_task()`.
/// For the simplest API, use `securitycenter_folders_event_threat_detection_settings_validate_custom_module()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_validate_custom_module_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_event_threat_detection_settings_validate_custom_module_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_event_threat_detection_settings_validate_custom_module_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_event_threat_detection_settings_validate_custom_module`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersEventThreatDetectionSettingsValidateCustomModuleArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/folders/{foldersId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_validate_custom_module_builder()` + `securitycenter_folders_event_threat_detection_settings_validate_custom_module_execute()`.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_validate_custom_module_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_validate_custom_module(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersEventThreatDetectionSettingsValidateCustomModuleArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_event_threat_detection_settings_validate_custom_module_builder(
            client,
            &args.parent,
        )?;
    securitycenter_folders_event_threat_detection_settings_validate_custom_module_execute(builder)
}

/// POST v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_custom_modules_create_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_custom_modules_create` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/customModules",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_event_threat_detection_settings_custom_modules_create_execute()` or `securitycenter_folders_event_threat_detection_settings_custom_modules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_custom_modules_create_task()`.
/// For the simplest API, use `securitycenter_folders_event_threat_detection_settings_custom_modules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_folders_event_threat_detection_settings_custom_modules_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_event_threat_detection_settings_custom_modules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_create_builder()` + `securitycenter_folders_event_threat_detection_settings_custom_modules_create_execute()`.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_custom_modules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_event_threat_detection_settings_custom_modules_create_builder(
            client,
            &args.parent,
        )?;
    securitycenter_folders_event_threat_detection_settings_custom_modules_create_execute(builder)
}

/// DELETE v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_custom_modules_delete_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_custom_modules_delete` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_event_threat_detection_settings_custom_modules_delete_execute()` or `securitycenter_folders_event_threat_detection_settings_custom_modules_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_custom_modules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_custom_modules_delete_task()`.
/// For the simplest API, use `securitycenter_folders_event_threat_detection_settings_custom_modules_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_custom_modules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        securitycenter_folders_event_threat_detection_settings_custom_modules_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_event_threat_detection_settings_custom_modules_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_delete_builder()` + `securitycenter_folders_event_threat_detection_settings_custom_modules_delete_execute()`.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_custom_modules_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_event_threat_detection_settings_custom_modules_delete_builder(
            client, &args.name,
        )?;
    securitycenter_folders_event_threat_detection_settings_custom_modules_delete_execute(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_custom_modules_get_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_custom_modules_get` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_event_threat_detection_settings_custom_modules_get_execute()` or `securitycenter_folders_event_threat_detection_settings_custom_modules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_custom_modules_get_task()`.
/// For the simplest API, use `securitycenter_folders_event_threat_detection_settings_custom_modules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_folders_event_threat_detection_settings_custom_modules_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_event_threat_detection_settings_custom_modules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_get_builder()` + `securitycenter_folders_event_threat_detection_settings_custom_modules_get_execute()`.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_custom_modules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_event_threat_detection_settings_custom_modules_get_builder(
            client, &args.name,
        )?;
    securitycenter_folders_event_threat_detection_settings_custom_modules_get_execute(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_custom_modules_list` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/customModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_execute()` or `securitycenter_folders_event_threat_detection_settings_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListEventThreatDetectionCustomModulesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListEventThreatDetectionCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_folders_event_threat_detection_settings_custom_modules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_event_threat_detection_settings_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_builder()` + `securitycenter_folders_event_threat_detection_settings_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListEventThreatDetectionCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_event_threat_detection_settings_custom_modules_list_builder(
            client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )?;
    securitycenter_folders_event_threat_detection_settings_custom_modules_list_execute(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/customModules:listDescendant",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_execute()` or `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_task()`.
/// For the simplest API, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesListDescendantArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_builder()` + `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_execute()`.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesListDescendantArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_execute(
        builder,
    )
}

/// PATCH v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_custom_modules_patch_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_custom_modules_patch` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_event_threat_detection_settings_custom_modules_patch_execute()` or `securitycenter_folders_event_threat_detection_settings_custom_modules_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_custom_modules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_custom_modules_patch_task()`.
/// For the simplest API, use `securitycenter_folders_event_threat_detection_settings_custom_modules_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_custom_modules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_folders_event_threat_detection_settings_custom_modules_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_event_threat_detection_settings_custom_modules_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/folders/{foldersId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_patch_builder()` + `securitycenter_folders_event_threat_detection_settings_custom_modules_patch_execute()`.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_custom_modules_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_event_threat_detection_settings_custom_modules_patch_builder(
            client,
            &args.name,
            &args.updateMask,
        )?;
    securitycenter_folders_event_threat_detection_settings_custom_modules_patch_execute(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_execute()` or `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EffectiveEventThreatDetectionCustomModule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EffectiveEventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_task()`.
/// For the simplest API, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EffectiveEventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersEventThreatDetectionSettingsEffectiveCustomModulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_builder()` + `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_execute()`.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersEventThreatDetectionSettingsEffectiveCustomModulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EffectiveEventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_builder(client, &args.name)?;
    securitycenter_folders_event_threat_detection_settings_effective_custom_modules_get_execute(
        builder,
    )
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/effectiveCustomModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_execute()` or `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersEventThreatDetectionSettingsEffectiveCustomModulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_builder()` + `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersEventThreatDetectionSettingsEffectiveCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_execute(
        builder,
    )
}

/// POST v1/folders/{foldersId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_findings_bulk_mute_execute()` to send, or `securitycenter_folders_findings_bulk_mute` for simplest API.

pub fn securitycenter_folders_findings_bulk_mute_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/findings:bulkMute",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/folders/{foldersId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_findings_bulk_mute_execute()` or `securitycenter_folders_findings_bulk_mute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_findings_bulk_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_findings_bulk_mute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/folders/{foldersId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_findings_bulk_mute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_findings_bulk_mute_task()`.
/// For the simplest API, use `securitycenter_folders_findings_bulk_mute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_findings_bulk_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_findings_bulk_mute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_folders_findings_bulk_mute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_findings_bulk_mute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersFindingsBulkMuteArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/folders/{foldersId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_findings_bulk_mute_builder()` + `securitycenter_folders_findings_bulk_mute_execute()`.
/// For task-level control, use `securitycenter_folders_findings_bulk_mute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_findings_bulk_mute(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersFindingsBulkMuteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_findings_bulk_mute_builder(client, &args.parent)?;
    securitycenter_folders_findings_bulk_mute_execute(builder)
}

/// DELETE v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_locations_mute_configs_delete_execute()` to send, or `securitycenter_folders_locations_mute_configs_delete` for simplest API.

pub fn securitycenter_folders_locations_mute_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/locations/{locationsId}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_locations_mute_configs_delete_execute()` or `securitycenter_folders_locations_mute_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_locations_mute_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_locations_mute_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_locations_mute_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_locations_mute_configs_delete_task()`.
/// For the simplest API, use `securitycenter_folders_locations_mute_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_locations_mute_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_locations_mute_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_folders_locations_mute_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_locations_mute_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersLocationsMuteConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_locations_mute_configs_delete_builder()` + `securitycenter_folders_locations_mute_configs_delete_execute()`.
/// For task-level control, use `securitycenter_folders_locations_mute_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_locations_mute_configs_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersLocationsMuteConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_locations_mute_configs_delete_builder(client, &args.name)?;
    securitycenter_folders_locations_mute_configs_delete_execute(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_locations_mute_configs_get_execute()` to send, or `securitycenter_folders_locations_mute_configs_get` for simplest API.

pub fn securitycenter_folders_locations_mute_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/locations/{locationsId}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_locations_mute_configs_get_execute()` or `securitycenter_folders_locations_mute_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_locations_mute_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_locations_mute_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_locations_mute_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_locations_mute_configs_get_task()`.
/// For the simplest API, use `securitycenter_folders_locations_mute_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_locations_mute_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_locations_mute_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_locations_mute_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_locations_mute_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersLocationsMuteConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_locations_mute_configs_get_builder()` + `securitycenter_folders_locations_mute_configs_get_execute()`.
/// For task-level control, use `securitycenter_folders_locations_mute_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_locations_mute_configs_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersLocationsMuteConfigsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_locations_mute_configs_get_builder(client, &args.name)?;
    securitycenter_folders_locations_mute_configs_get_execute(builder)
}

/// PATCH v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_locations_mute_configs_patch_execute()` to send, or `securitycenter_folders_locations_mute_configs_patch` for simplest API.

pub fn securitycenter_folders_locations_mute_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/locations/{locationsId}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_locations_mute_configs_patch_execute()` or `securitycenter_folders_locations_mute_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_locations_mute_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_locations_mute_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_locations_mute_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_locations_mute_configs_patch_task()`.
/// For the simplest API, use `securitycenter_folders_locations_mute_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_locations_mute_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_locations_mute_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_locations_mute_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_locations_mute_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersLocationsMuteConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/folders/{foldersId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_locations_mute_configs_patch_builder()` + `securitycenter_folders_locations_mute_configs_patch_execute()`.
/// For task-level control, use `securitycenter_folders_locations_mute_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_locations_mute_configs_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersLocationsMuteConfigsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_locations_mute_configs_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_folders_locations_mute_configs_patch_execute(builder)
}

/// POST v1/folders/{foldersId}/muteConfigs
/// Creates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_mute_configs_create_execute()` to send, or `securitycenter_folders_mute_configs_create` for simplest API.

pub fn securitycenter_folders_mute_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    muteConfigId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/muteConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = muteConfigId.as_ref() {
        query_parts.push(format!("muteConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/folders/{foldersId}/muteConfigs
/// Creates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_mute_configs_create_execute()` or `securitycenter_folders_mute_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_mute_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/folders/{foldersId}/muteConfigs
/// Creates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_mute_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_mute_configs_create_task()`.
/// For the simplest API, use `securitycenter_folders_mute_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_mute_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_mute_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_mute_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_mute_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersMuteConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: muteConfigId
    pub muteConfigId: Option<String>,
}

/// POST v1/folders/{foldersId}/muteConfigs
/// Creates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_mute_configs_create_builder()` + `securitycenter_folders_mute_configs_create_execute()`.
/// For task-level control, use `securitycenter_folders_mute_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersMuteConfigsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_mute_configs_create_builder(
        client,
        &args.parent,
        &args.muteConfigId,
    )?;
    securitycenter_folders_mute_configs_create_execute(builder)
}

/// DELETE v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_mute_configs_delete_execute()` to send, or `securitycenter_folders_mute_configs_delete` for simplest API.

pub fn securitycenter_folders_mute_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_mute_configs_delete_execute()` or `securitycenter_folders_mute_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_mute_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_mute_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_mute_configs_delete_task()`.
/// For the simplest API, use `securitycenter_folders_mute_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_mute_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_mute_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_folders_mute_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_mute_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersMuteConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_mute_configs_delete_builder()` + `securitycenter_folders_mute_configs_delete_execute()`.
/// For task-level control, use `securitycenter_folders_mute_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersMuteConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_mute_configs_delete_builder(client, &args.name)?;
    securitycenter_folders_mute_configs_delete_execute(builder)
}

/// GET v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_mute_configs_get_execute()` to send, or `securitycenter_folders_mute_configs_get` for simplest API.

pub fn securitycenter_folders_mute_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_mute_configs_get_execute()` or `securitycenter_folders_mute_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_mute_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_mute_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_mute_configs_get_task()`.
/// For the simplest API, use `securitycenter_folders_mute_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_mute_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_mute_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_mute_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_mute_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersMuteConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_mute_configs_get_builder()` + `securitycenter_folders_mute_configs_get_execute()`.
/// For task-level control, use `securitycenter_folders_mute_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersMuteConfigsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_mute_configs_get_builder(client, &args.name)?;
    securitycenter_folders_mute_configs_get_execute(builder)
}

/// GET v1/folders/{foldersId}/muteConfigs
/// Lists mute configs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_mute_configs_list_execute()` to send, or `securitycenter_folders_mute_configs_list` for simplest API.

pub fn securitycenter_folders_mute_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/muteConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/muteConfigs
/// Lists mute configs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_mute_configs_list_execute()` or `securitycenter_folders_mute_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_mute_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMuteConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMuteConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/muteConfigs
/// Lists mute configs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_mute_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_mute_configs_list_task()`.
/// For the simplest API, use `securitycenter_folders_mute_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_mute_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_mute_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMuteConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_mute_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_mute_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersMuteConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/muteConfigs
/// Lists mute configs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_mute_configs_list_builder()` + `securitycenter_folders_mute_configs_list_execute()`.
/// For task-level control, use `securitycenter_folders_mute_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersMuteConfigsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMuteConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_mute_configs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    securitycenter_folders_mute_configs_list_execute(builder)
}

/// PATCH v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_mute_configs_patch_execute()` to send, or `securitycenter_folders_mute_configs_patch` for simplest API.

pub fn securitycenter_folders_mute_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_mute_configs_patch_execute()` or `securitycenter_folders_mute_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_mute_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_mute_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_mute_configs_patch_task()`.
/// For the simplest API, use `securitycenter_folders_mute_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_mute_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_mute_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_mute_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_mute_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersMuteConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/folders/{foldersId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_mute_configs_patch_builder()` + `securitycenter_folders_mute_configs_patch_execute()`.
/// For task-level control, use `securitycenter_folders_mute_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersMuteConfigsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_mute_configs_patch_builder(client, &args.name, &args.updateMask)?;
    securitycenter_folders_mute_configs_patch_execute(builder)
}

/// POST v1/folders/{foldersId}/notificationConfigs
/// Creates a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_notification_configs_create_execute()` to send, or `securitycenter_folders_notification_configs_create` for simplest API.

pub fn securitycenter_folders_notification_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    configId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/notificationConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = configId.as_ref() {
        query_parts.push(format!("configId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/folders/{foldersId}/notificationConfigs
/// Creates a notification config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_notification_configs_create_execute()` or `securitycenter_folders_notification_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_notification_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/folders/{foldersId}/notificationConfigs
/// Creates a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_notification_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_notification_configs_create_task()`.
/// For the simplest API, use `securitycenter_folders_notification_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_notification_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_notification_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_notification_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_notification_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersNotificationConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: configId
    pub configId: Option<String>,
}

/// POST v1/folders/{foldersId}/notificationConfigs
/// Creates a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_notification_configs_create_builder()` + `securitycenter_folders_notification_configs_create_execute()`.
/// For task-level control, use `securitycenter_folders_notification_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersNotificationConfigsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_notification_configs_create_builder(
        client,
        &args.parent,
        &args.configId,
    )?;
    securitycenter_folders_notification_configs_create_execute(builder)
}

/// DELETE v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_notification_configs_delete_execute()` to send, or `securitycenter_folders_notification_configs_delete` for simplest API.

pub fn securitycenter_folders_notification_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/notificationConfigs/{notificationConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_notification_configs_delete_execute()` or `securitycenter_folders_notification_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_notification_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_notification_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_notification_configs_delete_task()`.
/// For the simplest API, use `securitycenter_folders_notification_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_notification_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_notification_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_folders_notification_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_notification_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersNotificationConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_notification_configs_delete_builder()` + `securitycenter_folders_notification_configs_delete_execute()`.
/// For task-level control, use `securitycenter_folders_notification_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersNotificationConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_notification_configs_delete_builder(client, &args.name)?;
    securitycenter_folders_notification_configs_delete_execute(builder)
}

/// GET v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_notification_configs_get_execute()` to send, or `securitycenter_folders_notification_configs_get` for simplest API.

pub fn securitycenter_folders_notification_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/notificationConfigs/{notificationConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_notification_configs_get_execute()` or `securitycenter_folders_notification_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_notification_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_notification_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_notification_configs_get_task()`.
/// For the simplest API, use `securitycenter_folders_notification_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_notification_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_notification_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_notification_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_notification_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersNotificationConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_notification_configs_get_builder()` + `securitycenter_folders_notification_configs_get_execute()`.
/// For task-level control, use `securitycenter_folders_notification_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersNotificationConfigsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_notification_configs_get_builder(client, &args.name)?;
    securitycenter_folders_notification_configs_get_execute(builder)
}

/// GET v1/folders/{foldersId}/notificationConfigs
/// Lists notification configs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_notification_configs_list_execute()` to send, or `securitycenter_folders_notification_configs_list` for simplest API.

pub fn securitycenter_folders_notification_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/notificationConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/notificationConfigs
/// Lists notification configs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_notification_configs_list_execute()` or `securitycenter_folders_notification_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_notification_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListNotificationConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNotificationConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/notificationConfigs
/// Lists notification configs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_notification_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_notification_configs_list_task()`.
/// For the simplest API, use `securitycenter_folders_notification_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_notification_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_notification_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_notification_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_notification_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersNotificationConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/notificationConfigs
/// Lists notification configs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_notification_configs_list_builder()` + `securitycenter_folders_notification_configs_list_execute()`.
/// For task-level control, use `securitycenter_folders_notification_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersNotificationConfigsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_notification_configs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    securitycenter_folders_notification_configs_list_execute(builder)
}

/// PATCH v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_notification_configs_patch_execute()` to send, or `securitycenter_folders_notification_configs_patch` for simplest API.

pub fn securitycenter_folders_notification_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/notificationConfigs/{notificationConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_notification_configs_patch_execute()` or `securitycenter_folders_notification_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_notification_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_notification_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_notification_configs_patch_task()`.
/// For the simplest API, use `securitycenter_folders_notification_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_notification_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_notification_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_notification_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_notification_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersNotificationConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/folders/{foldersId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_notification_configs_patch_builder()` + `securitycenter_folders_notification_configs_patch_execute()`.
/// For task-level control, use `securitycenter_folders_notification_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersNotificationConfigsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_notification_configs_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_folders_notification_configs_patch_execute(builder)
}

/// POST v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_custom_modules_create_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_custom_modules_create` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/customModules",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_security_health_analytics_settings_custom_modules_create_execute()` or `securitycenter_folders_security_health_analytics_settings_custom_modules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_create_task()`.
/// For the simplest API, use `securitycenter_folders_security_health_analytics_settings_custom_modules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_folders_security_health_analytics_settings_custom_modules_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_security_health_analytics_settings_custom_modules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_create_builder()` + `securitycenter_folders_security_health_analytics_settings_custom_modules_create_execute()`.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_security_health_analytics_settings_custom_modules_create_builder(
            client,
            &args.parent,
        )?;
    securitycenter_folders_security_health_analytics_settings_custom_modules_create_execute(builder)
}

/// DELETE v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_custom_modules_delete_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_custom_modules_delete` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_security_health_analytics_settings_custom_modules_delete_execute()` or `securitycenter_folders_security_health_analytics_settings_custom_modules_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_delete_task()`.
/// For the simplest API, use `securitycenter_folders_security_health_analytics_settings_custom_modules_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        securitycenter_folders_security_health_analytics_settings_custom_modules_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_security_health_analytics_settings_custom_modules_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_delete_builder()` + `securitycenter_folders_security_health_analytics_settings_custom_modules_delete_execute()`.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_security_health_analytics_settings_custom_modules_delete_builder(
            client, &args.name,
        )?;
    securitycenter_folders_security_health_analytics_settings_custom_modules_delete_execute(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_custom_modules_get_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_custom_modules_get` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_security_health_analytics_settings_custom_modules_get_execute()` or `securitycenter_folders_security_health_analytics_settings_custom_modules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_get_task()`.
/// For the simplest API, use `securitycenter_folders_security_health_analytics_settings_custom_modules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_folders_security_health_analytics_settings_custom_modules_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_security_health_analytics_settings_custom_modules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_get_builder()` + `securitycenter_folders_security_health_analytics_settings_custom_modules_get_execute()`.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_security_health_analytics_settings_custom_modules_get_builder(
            client, &args.name,
        )?;
    securitycenter_folders_security_health_analytics_settings_custom_modules_get_execute(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_custom_modules_list` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/customModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_execute()` or `securitycenter_folders_security_health_analytics_settings_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSecurityHealthAnalyticsCustomModulesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecurityHealthAnalyticsCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_security_health_analytics_settings_custom_modules_list_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_security_health_analytics_settings_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_builder()` + `securitycenter_folders_security_health_analytics_settings_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecurityHealthAnalyticsCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_security_health_analytics_settings_custom_modules_list_builder(
            client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )?;
    securitycenter_folders_security_health_analytics_settings_custom_modules_list_execute(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/customModules:listDescendant",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_execute()` or `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_task()`.
/// For the simplest API, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_builder()` + `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_execute()`.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_execute(
        builder,
    )
}

/// PATCH v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_custom_modules_patch_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_custom_modules_patch` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_security_health_analytics_settings_custom_modules_patch_execute()` or `securitycenter_folders_security_health_analytics_settings_custom_modules_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_patch_task()`.
/// For the simplest API, use `securitycenter_folders_security_health_analytics_settings_custom_modules_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_security_health_analytics_settings_custom_modules_patch_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_security_health_analytics_settings_custom_modules_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_patch_builder()` + `securitycenter_folders_security_health_analytics_settings_custom_modules_patch_execute()`.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_security_health_analytics_settings_custom_modules_patch_builder(
            client,
            &args.name,
            &args.updateMask,
        )?;
    securitycenter_folders_security_health_analytics_settings_custom_modules_patch_execute(builder)
}

/// POST v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/customModules:simulate",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_execute()` or `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SimulateSecurityHealthAnalyticsCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_task()`.
/// For the simplest API, use `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_security_health_analytics_settings_custom_modules_simulate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_builder()` + `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_execute()`.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_simulate(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_builder(
            client,
            &args.parent,
        )?;
    securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_execute(
        builder,
    )
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_execute()` or `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule,
                >,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_task()`.
/// For the simplest API, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSecurityHealthAnalyticsSettingsEffectiveCustomModulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_builder()` + `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_execute()`.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsEffectiveCustomModulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_builder(client, &args.name)?;
    securitycenter_folders_security_health_analytics_settings_effective_custom_modules_get_execute(
        builder,
    )
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/effectiveCustomModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_execute()` or `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_builder()` + `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_execute(
        builder,
    )
}

/// GET v1/folders/{foldersId}/sources
/// Lists all sources belonging to an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_sources_list_execute()` to send, or `securitycenter_folders_sources_list` for simplest API.

pub fn securitycenter_folders_sources_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/sources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/sources
/// Lists all sources belonging to an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_sources_list_execute()` or `securitycenter_folders_sources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/sources
/// Lists all sources belonging to an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_sources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_sources_list_task()`.
/// For the simplest API, use `securitycenter_folders_sources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_sources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_sources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSourcesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/sources
/// Lists all sources belonging to an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_sources_list_builder()` + `securitycenter_folders_sources_list_execute()`.
/// For task-level control, use `securitycenter_folders_sources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSourcesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_sources_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    securitycenter_folders_sources_list_execute(builder)
}

/// POST v1/folders/{foldersId}/sources/{sourcesId}/findings:group
/// Filters an organization or source''s findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_sources_findings_group_execute()` to send, or `securitycenter_folders_sources_findings_group` for simplest API.

pub fn securitycenter_folders_sources_findings_group_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/sources/{sourcesId}/findings:group",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/folders/{foldersId}/sources/{sourcesId}/findings:group
/// Filters an organization or source''s findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_sources_findings_group_execute()` or `securitycenter_folders_sources_findings_group`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_findings_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_group_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GroupFindingsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupFindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/folders/{foldersId}/sources/{sourcesId}/findings:group
/// Filters an organization or source''s findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_sources_findings_group_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_sources_findings_group_task()`.
/// For the simplest API, use `securitycenter_folders_sources_findings_group()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_findings_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_sources_findings_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_sources_findings_group_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_sources_findings_group`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSourcesFindingsGroupArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/folders/{foldersId}/sources/{sourcesId}/findings:group
/// Filters an organization or source''s findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_sources_findings_group_builder()` + `securitycenter_folders_sources_findings_group_execute()`.
/// For task-level control, use `securitycenter_folders_sources_findings_group_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_group(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSourcesFindingsGroupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_sources_findings_group_builder(client, &args.parent)?;
    securitycenter_folders_sources_findings_group_execute(builder)
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings
/// Lists an organization or source''s findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_sources_findings_list_execute()` to send, or `securitycenter_folders_sources_findings_list` for simplest API.

pub fn securitycenter_folders_sources_findings_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    compareDuration: &Option<String>,
    fieldMask: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    readTime: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/sources/{sourcesId}/findings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = compareDuration.as_ref() {
        query_parts.push(format!("compareDuration={}", val));
    }
    if let Some(val) = fieldMask.as_ref() {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime.as_ref() {
        query_parts.push(format!("readTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings
/// Lists an organization or source''s findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_sources_findings_list_execute()` or `securitycenter_folders_sources_findings_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_findings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFindingsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings
/// Lists an organization or source''s findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_sources_findings_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_sources_findings_list_task()`.
/// For the simplest API, use `securitycenter_folders_sources_findings_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_findings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_sources_findings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_sources_findings_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_sources_findings_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSourcesFindingsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: compareDuration
    pub compareDuration: Option<String>,
    /// Query parameter: fieldMask
    pub fieldMask: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readTime
    pub readTime: Option<String>,
}

/// GET v1/folders/{foldersId}/sources/{sourcesId}/findings
/// Lists an organization or source''s findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_sources_findings_list_builder()` + `securitycenter_folders_sources_findings_list_execute()`.
/// For task-level control, use `securitycenter_folders_sources_findings_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSourcesFindingsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_sources_findings_list_builder(
        client,
        &args.parent,
        &args.compareDuration,
        &args.fieldMask,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.readTime,
    )?;
    securitycenter_folders_sources_findings_list_execute(builder)
}

/// PATCH v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_sources_findings_patch_execute()` to send, or `securitycenter_folders_sources_findings_patch` for simplest API.

pub fn securitycenter_folders_sources_findings_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/sources/{sourcesId}/findings/{findingsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_sources_findings_patch_execute()` or `securitycenter_folders_sources_findings_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_findings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Finding>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_sources_findings_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_sources_findings_patch_task()`.
/// For the simplest API, use `securitycenter_folders_sources_findings_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_findings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_sources_findings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_folders_sources_findings_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_sources_findings_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSourcesFindingsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_sources_findings_patch_builder()` + `securitycenter_folders_sources_findings_patch_execute()`.
/// For task-level control, use `securitycenter_folders_sources_findings_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSourcesFindingsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_sources_findings_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_folders_sources_findings_patch_execute(builder)
}

/// POST v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_sources_findings_set_mute_execute()` to send, or `securitycenter_folders_sources_findings_set_mute` for simplest API.

pub fn securitycenter_folders_sources_findings_set_mute_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/sources/{sourcesId}/findings/{findingsId}:setMute",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_sources_findings_set_mute_execute()` or `securitycenter_folders_sources_findings_set_mute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_findings_set_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_set_mute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Finding>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_sources_findings_set_mute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_sources_findings_set_mute_task()`.
/// For the simplest API, use `securitycenter_folders_sources_findings_set_mute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_findings_set_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_sources_findings_set_mute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_folders_sources_findings_set_mute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_sources_findings_set_mute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSourcesFindingsSetMuteArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_sources_findings_set_mute_builder()` + `securitycenter_folders_sources_findings_set_mute_execute()`.
/// For task-level control, use `securitycenter_folders_sources_findings_set_mute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_set_mute(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSourcesFindingsSetMuteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_sources_findings_set_mute_builder(client, &args.name)?;
    securitycenter_folders_sources_findings_set_mute_execute(builder)
}

/// POST v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_sources_findings_set_state_execute()` to send, or `securitycenter_folders_sources_findings_set_state` for simplest API.

pub fn securitycenter_folders_sources_findings_set_state_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/sources/{sourcesId}/findings/{findingsId}:setState",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_sources_findings_set_state_execute()` or `securitycenter_folders_sources_findings_set_state`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_findings_set_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_set_state_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Finding>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_sources_findings_set_state_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_sources_findings_set_state_task()`.
/// For the simplest API, use `securitycenter_folders_sources_findings_set_state()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_findings_set_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_sources_findings_set_state_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_folders_sources_findings_set_state_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_sources_findings_set_state`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSourcesFindingsSetStateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_sources_findings_set_state_builder()` + `securitycenter_folders_sources_findings_set_state_execute()`.
/// For task-level control, use `securitycenter_folders_sources_findings_set_state_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_set_state(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSourcesFindingsSetStateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_sources_findings_set_state_builder(client, &args.name)?;
    securitycenter_folders_sources_findings_set_state_execute(builder)
}

/// PATCH v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_sources_findings_update_security_marks_execute()` to send, or `securitycenter_folders_sources_findings_update_security_marks` for simplest API.

pub fn securitycenter_folders_sources_findings_update_security_marks_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    startTime: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/sources/{sourcesId}/findings/{findingsId}/securityMarks",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = startTime.as_ref() {
        query_parts.push(format!("startTime={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_sources_findings_update_security_marks_execute()` or `securitycenter_folders_sources_findings_update_security_marks`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_findings_update_security_marks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_update_security_marks_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityMarks>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityMarks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_sources_findings_update_security_marks_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_sources_findings_update_security_marks_task()`.
/// For the simplest API, use `securitycenter_folders_sources_findings_update_security_marks()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_findings_update_security_marks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_sources_findings_update_security_marks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_sources_findings_update_security_marks_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_sources_findings_update_security_marks`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSourcesFindingsUpdateSecurityMarksArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: startTime
    pub startTime: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_sources_findings_update_security_marks_builder()` + `securitycenter_folders_sources_findings_update_security_marks_execute()`.
/// For task-level control, use `securitycenter_folders_sources_findings_update_security_marks_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_update_security_marks(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSourcesFindingsUpdateSecurityMarksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_sources_findings_update_security_marks_builder(
        client,
        &args.name,
        &args.startTime,
        &args.updateMask,
    )?;
    securitycenter_folders_sources_findings_update_security_marks_execute(builder)
}

/// PATCH v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_sources_findings_external_systems_patch_execute()` to send, or `securitycenter_folders_sources_findings_external_systems_patch` for simplest API.

pub fn securitycenter_folders_sources_findings_external_systems_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_sources_findings_external_systems_patch_execute()` or `securitycenter_folders_sources_findings_external_systems_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_findings_external_systems_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_external_systems_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1ExternalSystem>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1ExternalSystem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_sources_findings_external_systems_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_sources_findings_external_systems_patch_task()`.
/// For the simplest API, use `securitycenter_folders_sources_findings_external_systems_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_findings_external_systems_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_sources_findings_external_systems_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ExternalSystem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_sources_findings_external_systems_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_sources_findings_external_systems_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSourcesFindingsExternalSystemsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/folders/{foldersId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_sources_findings_external_systems_patch_builder()` + `securitycenter_folders_sources_findings_external_systems_patch_execute()`.
/// For task-level control, use `securitycenter_folders_sources_findings_external_systems_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_findings_external_systems_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSourcesFindingsExternalSystemsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ExternalSystem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_sources_findings_external_systems_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_folders_sources_findings_external_systems_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/organizationSettings
/// Gets the settings for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_get_organization_settings_execute()` to send, or `securitycenter_organizations_get_organization_settings` for simplest API.

pub fn securitycenter_organizations_get_organization_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/organizationSettings",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/organizationSettings
/// Gets the settings for an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_get_organization_settings_execute()` or `securitycenter_organizations_get_organization_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_get_organization_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_get_organization_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OrganizationSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OrganizationSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/organizationSettings
/// Gets the settings for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_get_organization_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_get_organization_settings_task()`.
/// For the simplest API, use `securitycenter_organizations_get_organization_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_get_organization_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_get_organization_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OrganizationSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_get_organization_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_get_organization_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsGetOrganizationSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/organizationSettings
/// Gets the settings for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_get_organization_settings_builder()` + `securitycenter_organizations_get_organization_settings_execute()`.
/// For task-level control, use `securitycenter_organizations_get_organization_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_get_organization_settings(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsGetOrganizationSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OrganizationSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_get_organization_settings_builder(client, &args.name)?;
    securitycenter_organizations_get_organization_settings_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/organizationSettings
/// Updates an organization''s settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_update_organization_settings_execute()` to send, or `securitycenter_organizations_update_organization_settings` for simplest API.

pub fn securitycenter_organizations_update_organization_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/organizationSettings",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/organizationSettings
/// Updates an organization''s settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_update_organization_settings_execute()` or `securitycenter_organizations_update_organization_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_update_organization_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_update_organization_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OrganizationSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OrganizationSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/organizationSettings
/// Updates an organization''s settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_update_organization_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_update_organization_settings_task()`.
/// For the simplest API, use `securitycenter_organizations_update_organization_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_update_organization_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_update_organization_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OrganizationSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_update_organization_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_update_organization_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsUpdateOrganizationSettingsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/organizations/{organizationsId}/organizationSettings
/// Updates an organization''s settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_update_organization_settings_builder()` + `securitycenter_organizations_update_organization_settings_execute()`.
/// For task-level control, use `securitycenter_organizations_update_organization_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_update_organization_settings(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsUpdateOrganizationSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OrganizationSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_update_organization_settings_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_organizations_update_organization_settings_execute(builder)
}

/// POST v1/organizations/{organizationsId}/assets:group
/// Filters an organization''s assets and groups them by their specified properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_assets_group_execute()` to send, or `securitycenter_organizations_assets_group` for simplest API.

pub fn securitycenter_organizations_assets_group_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/assets:group",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/assets:group
/// Filters an organization''s assets and groups them by their specified properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_assets_group_execute()` or `securitycenter_organizations_assets_group`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_assets_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_group_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GroupAssetsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/assets:group
/// Filters an organization''s assets and groups them by their specified properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_assets_group_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_assets_group_task()`.
/// For the simplest API, use `securitycenter_organizations_assets_group()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_assets_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_assets_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_assets_group_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_assets_group`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsAssetsGroupArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/organizations/{organizationsId}/assets:group
/// Filters an organization''s assets and groups them by their specified properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_assets_group_builder()` + `securitycenter_organizations_assets_group_execute()`.
/// For task-level control, use `securitycenter_organizations_assets_group_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_group(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsAssetsGroupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_assets_group_builder(client, &args.parent)?;
    securitycenter_organizations_assets_group_execute(builder)
}

/// GET v1/organizations/{organizationsId}/assets
/// Lists an organization''s assets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_assets_list_execute()` to send, or `securitycenter_organizations_assets_list` for simplest API.

pub fn securitycenter_organizations_assets_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    compareDuration: &Option<String>,
    fieldMask: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    readTime: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/assets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = compareDuration.as_ref() {
        query_parts.push(format!("compareDuration={}", val));
    }
    if let Some(val) = fieldMask.as_ref() {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime.as_ref() {
        query_parts.push(format!("readTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/assets
/// Lists an organization''s assets.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_assets_list_execute()` or `securitycenter_organizations_assets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAssetsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/assets
/// Lists an organization''s assets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_assets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_assets_list_task()`.
/// For the simplest API, use `securitycenter_organizations_assets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_assets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_assets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_assets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsAssetsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: compareDuration
    pub compareDuration: Option<String>,
    /// Query parameter: fieldMask
    pub fieldMask: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readTime
    pub readTime: Option<String>,
}

/// GET v1/organizations/{organizationsId}/assets
/// Lists an organization''s assets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_assets_list_builder()` + `securitycenter_organizations_assets_list_execute()`.
/// For task-level control, use `securitycenter_organizations_assets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsAssetsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_assets_list_builder(
        client,
        &args.parent,
        &args.compareDuration,
        &args.fieldMask,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.readTime,
    )?;
    securitycenter_organizations_assets_list_execute(builder)
}

/// POST v1/organizations/{organizationsId}/assets:runDiscovery
/// Runs asset discovery. The discovery is tracked with a long-running operation. This API can only be called with limited frequency for an organization. If it is called too frequently the caller will receive a TOO_MANY_REQUESTS error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_assets_run_discovery_execute()` to send, or `securitycenter_organizations_assets_run_discovery` for simplest API.

pub fn securitycenter_organizations_assets_run_discovery_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/assets:runDiscovery",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/assets:runDiscovery
/// Runs asset discovery. The discovery is tracked with a long-running operation. This API can only be called with limited frequency for an organization. If it is called too frequently the caller will receive a TOO_MANY_REQUESTS error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_assets_run_discovery_execute()` or `securitycenter_organizations_assets_run_discovery`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_assets_run_discovery_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_run_discovery_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/assets:runDiscovery
/// Runs asset discovery. The discovery is tracked with a long-running operation. This API can only be called with limited frequency for an organization. If it is called too frequently the caller will receive a TOO_MANY_REQUESTS error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_assets_run_discovery_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_assets_run_discovery_task()`.
/// For the simplest API, use `securitycenter_organizations_assets_run_discovery()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_assets_run_discovery_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_assets_run_discovery_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_assets_run_discovery_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_assets_run_discovery`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsAssetsRunDiscoveryArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/organizations/{organizationsId}/assets:runDiscovery
/// Runs asset discovery. The discovery is tracked with a long-running operation. This API can only be called with limited frequency for an organization. If it is called too frequently the caller will receive a TOO_MANY_REQUESTS error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_assets_run_discovery_builder()` + `securitycenter_organizations_assets_run_discovery_execute()`.
/// For task-level control, use `securitycenter_organizations_assets_run_discovery_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_run_discovery(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsAssetsRunDiscoveryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_assets_run_discovery_builder(client, &args.parent)?;
    securitycenter_organizations_assets_run_discovery_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_assets_update_security_marks_execute()` to send, or `securitycenter_organizations_assets_update_security_marks` for simplest API.

pub fn securitycenter_organizations_assets_update_security_marks_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    startTime: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/assets/{assetsId}/securityMarks",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = startTime.as_ref() {
        query_parts.push(format!("startTime={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_assets_update_security_marks_execute()` or `securitycenter_organizations_assets_update_security_marks`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_assets_update_security_marks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_update_security_marks_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityMarks>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityMarks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_assets_update_security_marks_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_assets_update_security_marks_task()`.
/// For the simplest API, use `securitycenter_organizations_assets_update_security_marks()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_assets_update_security_marks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_assets_update_security_marks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_assets_update_security_marks_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_assets_update_security_marks`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsAssetsUpdateSecurityMarksArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: startTime
    pub startTime: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/organizations/{organizationsId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_assets_update_security_marks_builder()` + `securitycenter_organizations_assets_update_security_marks_execute()`.
/// For task-level control, use `securitycenter_organizations_assets_update_security_marks_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_update_security_marks(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsAssetsUpdateSecurityMarksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_assets_update_security_marks_builder(
        client,
        &args.name,
        &args.startTime,
        &args.updateMask,
    )?;
    securitycenter_organizations_assets_update_security_marks_execute(builder)
}

/// GET v1/organizations/{organizationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_attack_paths_list_execute()` to send, or `securitycenter_organizations_attack_paths_list` for simplest API.

pub fn securitycenter_organizations_attack_paths_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/attackPaths",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_attack_paths_list_execute()` or `securitycenter_organizations_attack_paths_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_attack_paths_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_attack_paths_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAttackPathsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAttackPathsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_attack_paths_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_attack_paths_list_task()`.
/// For the simplest API, use `securitycenter_organizations_attack_paths_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_attack_paths_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_attack_paths_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_attack_paths_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_attack_paths_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsAttackPathsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_attack_paths_list_builder()` + `securitycenter_organizations_attack_paths_list_execute()`.
/// For task-level control, use `securitycenter_organizations_attack_paths_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_attack_paths_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsAttackPathsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_attack_paths_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    securitycenter_organizations_attack_paths_list_execute(builder)
}

/// POST v1/organizations/{organizationsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_big_query_exports_create_execute()` to send, or `securitycenter_organizations_big_query_exports_create` for simplest API.

pub fn securitycenter_organizations_big_query_exports_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    bigQueryExportId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/bigQueryExports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bigQueryExportId.as_ref() {
        query_parts.push(format!("bigQueryExportId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_big_query_exports_create_execute()` or `securitycenter_organizations_big_query_exports_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_big_query_exports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_big_query_exports_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_big_query_exports_create_task()`.
/// For the simplest API, use `securitycenter_organizations_big_query_exports_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_big_query_exports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_big_query_exports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_big_query_exports_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_big_query_exports_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsBigQueryExportsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bigQueryExportId
    pub bigQueryExportId: Option<String>,
}

/// POST v1/organizations/{organizationsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_big_query_exports_create_builder()` + `securitycenter_organizations_big_query_exports_create_execute()`.
/// For task-level control, use `securitycenter_organizations_big_query_exports_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsBigQueryExportsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_big_query_exports_create_builder(
        client,
        &args.parent,
        &args.bigQueryExportId,
    )?;
    securitycenter_organizations_big_query_exports_create_execute(builder)
}

/// DELETE v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_big_query_exports_delete_execute()` to send, or `securitycenter_organizations_big_query_exports_delete` for simplest API.

pub fn securitycenter_organizations_big_query_exports_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/bigQueryExports/{bigQueryExportsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_big_query_exports_delete_execute()` or `securitycenter_organizations_big_query_exports_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_big_query_exports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_big_query_exports_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_big_query_exports_delete_task()`.
/// For the simplest API, use `securitycenter_organizations_big_query_exports_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_big_query_exports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_big_query_exports_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_big_query_exports_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_big_query_exports_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsBigQueryExportsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_big_query_exports_delete_builder()` + `securitycenter_organizations_big_query_exports_delete_execute()`.
/// For task-level control, use `securitycenter_organizations_big_query_exports_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsBigQueryExportsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_big_query_exports_delete_builder(client, &args.name)?;
    securitycenter_organizations_big_query_exports_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_big_query_exports_get_execute()` to send, or `securitycenter_organizations_big_query_exports_get` for simplest API.

pub fn securitycenter_organizations_big_query_exports_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/bigQueryExports/{bigQueryExportsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_big_query_exports_get_execute()` or `securitycenter_organizations_big_query_exports_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_big_query_exports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_big_query_exports_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_big_query_exports_get_task()`.
/// For the simplest API, use `securitycenter_organizations_big_query_exports_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_big_query_exports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_big_query_exports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_big_query_exports_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_big_query_exports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsBigQueryExportsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_big_query_exports_get_builder()` + `securitycenter_organizations_big_query_exports_get_execute()`.
/// For task-level control, use `securitycenter_organizations_big_query_exports_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsBigQueryExportsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_big_query_exports_get_builder(client, &args.name)?;
    securitycenter_organizations_big_query_exports_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_big_query_exports_list_execute()` to send, or `securitycenter_organizations_big_query_exports_list` for simplest API.

pub fn securitycenter_organizations_big_query_exports_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/bigQueryExports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_big_query_exports_list_execute()` or `securitycenter_organizations_big_query_exports_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_big_query_exports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBigQueryExportsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBigQueryExportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_big_query_exports_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_big_query_exports_list_task()`.
/// For the simplest API, use `securitycenter_organizations_big_query_exports_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_big_query_exports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_big_query_exports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBigQueryExportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_big_query_exports_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_big_query_exports_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsBigQueryExportsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_big_query_exports_list_builder()` + `securitycenter_organizations_big_query_exports_list_execute()`.
/// For task-level control, use `securitycenter_organizations_big_query_exports_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsBigQueryExportsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBigQueryExportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_big_query_exports_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    securitycenter_organizations_big_query_exports_list_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_big_query_exports_patch_execute()` to send, or `securitycenter_organizations_big_query_exports_patch` for simplest API.

pub fn securitycenter_organizations_big_query_exports_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/bigQueryExports/{bigQueryExportsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_big_query_exports_patch_execute()` or `securitycenter_organizations_big_query_exports_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_big_query_exports_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_big_query_exports_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_big_query_exports_patch_task()`.
/// For the simplest API, use `securitycenter_organizations_big_query_exports_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_big_query_exports_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_big_query_exports_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_big_query_exports_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_big_query_exports_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsBigQueryExportsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/organizations/{organizationsId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_big_query_exports_patch_builder()` + `securitycenter_organizations_big_query_exports_patch_execute()`.
/// For task-level control, use `securitycenter_organizations_big_query_exports_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsBigQueryExportsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_big_query_exports_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_organizations_big_query_exports_patch_execute(builder)
}

/// POST v1/organizations/{organizationsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_validate_custom_module` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_validate_custom_module_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings:validateCustomModule",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_execute()` or `securitycenter_organizations_event_threat_detection_settings_validate_custom_module`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_validate_custom_module_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValidateEventThreatDetectionCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_task()`.
/// For the simplest API, use `securitycenter_organizations_event_threat_detection_settings_validate_custom_module()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_event_threat_detection_settings_validate_custom_module_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_organizations_event_threat_detection_settings_validate_custom_module_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_event_threat_detection_settings_validate_custom_module`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsEventThreatDetectionSettingsValidateCustomModuleArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/organizations/{organizationsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_builder()` + `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_execute()`.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_validate_custom_module(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsEventThreatDetectionSettingsValidateCustomModuleArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_event_threat_detection_settings_validate_custom_module_builder(client, &args.parent)?;
    securitycenter_organizations_event_threat_detection_settings_validate_custom_module_execute(
        builder,
    )
}

/// POST v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_custom_modules_create` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_create_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/customModules",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_execute()` or `securitycenter_organizations_event_threat_detection_settings_custom_modules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_task()`.
/// For the simplest API, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_organizations_event_threat_detection_settings_custom_modules_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_event_threat_detection_settings_custom_modules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_builder()` + `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_execute()`.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_event_threat_detection_settings_custom_modules_create_builder(
            client,
            &args.parent,
        )?;
    securitycenter_organizations_event_threat_detection_settings_custom_modules_create_execute(
        builder,
    )
}

/// DELETE v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_custom_modules_delete` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_execute()` or `securitycenter_organizations_event_threat_detection_settings_custom_modules_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_task()`.
/// For the simplest API, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_event_threat_detection_settings_custom_modules_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_builder()` + `securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_execute()`.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_builder(
            client, &args.name,
        )?;
    securitycenter_organizations_event_threat_detection_settings_custom_modules_delete_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_custom_modules_get_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_custom_modules_get` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_get_execute()` or `securitycenter_organizations_event_threat_detection_settings_custom_modules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_get_task()`.
/// For the simplest API, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_organizations_event_threat_detection_settings_custom_modules_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_event_threat_detection_settings_custom_modules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_get_builder()` + `securitycenter_organizations_event_threat_detection_settings_custom_modules_get_execute()`.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_event_threat_detection_settings_custom_modules_get_builder(
            client, &args.name,
        )?;
    securitycenter_organizations_event_threat_detection_settings_custom_modules_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_custom_modules_list` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/customModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_execute()` or `securitycenter_organizations_event_threat_detection_settings_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListEventThreatDetectionCustomModulesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListEventThreatDetectionCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_organizations_event_threat_detection_settings_custom_modules_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_event_threat_detection_settings_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_builder()` + `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListEventThreatDetectionCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_event_threat_detection_settings_custom_modules_list_builder(
            client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )?;
    securitycenter_organizations_event_threat_detection_settings_custom_modules_list_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/customModules:listDescendant",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_execute()` or `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_task()`.
/// For the simplest API, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesListDescendantArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_builder()` + `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_execute()`.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesListDescendantArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_custom_modules_patch` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_execute()` or `securitycenter_organizations_event_threat_detection_settings_custom_modules_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_task()`.
/// For the simplest API, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_event_threat_detection_settings_custom_modules_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_builder()` + `securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_execute()`.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_builder(
            client,
            &args.name,
            &args.updateMask,
        )?;
    securitycenter_organizations_event_threat_detection_settings_custom_modules_patch_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_execute()` or `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EffectiveEventThreatDetectionCustomModule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EffectiveEventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_task()`.
/// For the simplest API, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EffectiveEventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsEventThreatDetectionSettingsEffectiveCustomModulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_builder()` + `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_execute()`.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsEventThreatDetectionSettingsEffectiveCustomModulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EffectiveEventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_builder(client, &args.name)?;
    securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/effectiveCustomModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_execute()` or `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsEventThreatDetectionSettingsEffectiveCustomModulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_builder()` + `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsEventThreatDetectionSettingsEffectiveCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_execute(builder)
}

/// POST v1/organizations/{organizationsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_findings_bulk_mute_execute()` to send, or `securitycenter_organizations_findings_bulk_mute` for simplest API.

pub fn securitycenter_organizations_findings_bulk_mute_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/findings:bulkMute",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_findings_bulk_mute_execute()` or `securitycenter_organizations_findings_bulk_mute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_findings_bulk_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_findings_bulk_mute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_findings_bulk_mute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_findings_bulk_mute_task()`.
/// For the simplest API, use `securitycenter_organizations_findings_bulk_mute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_findings_bulk_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_findings_bulk_mute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_findings_bulk_mute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_findings_bulk_mute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsFindingsBulkMuteArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/organizations/{organizationsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_findings_bulk_mute_builder()` + `securitycenter_organizations_findings_bulk_mute_execute()`.
/// For task-level control, use `securitycenter_organizations_findings_bulk_mute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_findings_bulk_mute(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsFindingsBulkMuteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_findings_bulk_mute_builder(client, &args.parent)?;
    securitycenter_organizations_findings_bulk_mute_execute(builder)
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_locations_mute_configs_delete_execute()` to send, or `securitycenter_organizations_locations_mute_configs_delete` for simplest API.

pub fn securitycenter_organizations_locations_mute_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/locations/{locationsId}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_locations_mute_configs_delete_execute()` or `securitycenter_organizations_locations_mute_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_locations_mute_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_locations_mute_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_locations_mute_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_locations_mute_configs_delete_task()`.
/// For the simplest API, use `securitycenter_organizations_locations_mute_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_locations_mute_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_locations_mute_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_locations_mute_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_locations_mute_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsLocationsMuteConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_locations_mute_configs_delete_builder()` + `securitycenter_organizations_locations_mute_configs_delete_execute()`.
/// For task-level control, use `securitycenter_organizations_locations_mute_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_locations_mute_configs_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsLocationsMuteConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_locations_mute_configs_delete_builder(client, &args.name)?;
    securitycenter_organizations_locations_mute_configs_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_locations_mute_configs_get_execute()` to send, or `securitycenter_organizations_locations_mute_configs_get` for simplest API.

pub fn securitycenter_organizations_locations_mute_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/locations/{locationsId}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_locations_mute_configs_get_execute()` or `securitycenter_organizations_locations_mute_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_locations_mute_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_locations_mute_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_locations_mute_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_locations_mute_configs_get_task()`.
/// For the simplest API, use `securitycenter_organizations_locations_mute_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_locations_mute_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_locations_mute_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_locations_mute_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_locations_mute_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsLocationsMuteConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_locations_mute_configs_get_builder()` + `securitycenter_organizations_locations_mute_configs_get_execute()`.
/// For task-level control, use `securitycenter_organizations_locations_mute_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_locations_mute_configs_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsLocationsMuteConfigsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_locations_mute_configs_get_builder(client, &args.name)?;
    securitycenter_organizations_locations_mute_configs_get_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_locations_mute_configs_patch_execute()` to send, or `securitycenter_organizations_locations_mute_configs_patch` for simplest API.

pub fn securitycenter_organizations_locations_mute_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/locations/{locationsId}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_locations_mute_configs_patch_execute()` or `securitycenter_organizations_locations_mute_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_locations_mute_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_locations_mute_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_locations_mute_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_locations_mute_configs_patch_task()`.
/// For the simplest API, use `securitycenter_organizations_locations_mute_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_locations_mute_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_locations_mute_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_locations_mute_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_locations_mute_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsLocationsMuteConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_locations_mute_configs_patch_builder()` + `securitycenter_organizations_locations_mute_configs_patch_execute()`.
/// For task-level control, use `securitycenter_organizations_locations_mute_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_locations_mute_configs_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsLocationsMuteConfigsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_locations_mute_configs_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_organizations_locations_mute_configs_patch_execute(builder)
}

/// POST v1/organizations/{organizationsId}/muteConfigs
/// Creates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_mute_configs_create_execute()` to send, or `securitycenter_organizations_mute_configs_create` for simplest API.

pub fn securitycenter_organizations_mute_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    muteConfigId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/muteConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = muteConfigId.as_ref() {
        query_parts.push(format!("muteConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/muteConfigs
/// Creates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_mute_configs_create_execute()` or `securitycenter_organizations_mute_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_mute_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/muteConfigs
/// Creates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_mute_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_mute_configs_create_task()`.
/// For the simplest API, use `securitycenter_organizations_mute_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_mute_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_mute_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_mute_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_mute_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsMuteConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: muteConfigId
    pub muteConfigId: Option<String>,
}

/// POST v1/organizations/{organizationsId}/muteConfigs
/// Creates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_mute_configs_create_builder()` + `securitycenter_organizations_mute_configs_create_execute()`.
/// For task-level control, use `securitycenter_organizations_mute_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsMuteConfigsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_mute_configs_create_builder(
        client,
        &args.parent,
        &args.muteConfigId,
    )?;
    securitycenter_organizations_mute_configs_create_execute(builder)
}

/// DELETE v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_mute_configs_delete_execute()` to send, or `securitycenter_organizations_mute_configs_delete` for simplest API.

pub fn securitycenter_organizations_mute_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_mute_configs_delete_execute()` or `securitycenter_organizations_mute_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_mute_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_mute_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_mute_configs_delete_task()`.
/// For the simplest API, use `securitycenter_organizations_mute_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_mute_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_mute_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_mute_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_mute_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsMuteConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_mute_configs_delete_builder()` + `securitycenter_organizations_mute_configs_delete_execute()`.
/// For task-level control, use `securitycenter_organizations_mute_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsMuteConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_mute_configs_delete_builder(client, &args.name)?;
    securitycenter_organizations_mute_configs_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_mute_configs_get_execute()` to send, or `securitycenter_organizations_mute_configs_get` for simplest API.

pub fn securitycenter_organizations_mute_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_mute_configs_get_execute()` or `securitycenter_organizations_mute_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_mute_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_mute_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_mute_configs_get_task()`.
/// For the simplest API, use `securitycenter_organizations_mute_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_mute_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_mute_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_mute_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_mute_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsMuteConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_mute_configs_get_builder()` + `securitycenter_organizations_mute_configs_get_execute()`.
/// For task-level control, use `securitycenter_organizations_mute_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsMuteConfigsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_mute_configs_get_builder(client, &args.name)?;
    securitycenter_organizations_mute_configs_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/muteConfigs
/// Lists mute configs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_mute_configs_list_execute()` to send, or `securitycenter_organizations_mute_configs_list` for simplest API.

pub fn securitycenter_organizations_mute_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/muteConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/muteConfigs
/// Lists mute configs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_mute_configs_list_execute()` or `securitycenter_organizations_mute_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_mute_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMuteConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMuteConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/muteConfigs
/// Lists mute configs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_mute_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_mute_configs_list_task()`.
/// For the simplest API, use `securitycenter_organizations_mute_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_mute_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_mute_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMuteConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_mute_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_mute_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsMuteConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/muteConfigs
/// Lists mute configs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_mute_configs_list_builder()` + `securitycenter_organizations_mute_configs_list_execute()`.
/// For task-level control, use `securitycenter_organizations_mute_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsMuteConfigsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMuteConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_mute_configs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    securitycenter_organizations_mute_configs_list_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_mute_configs_patch_execute()` to send, or `securitycenter_organizations_mute_configs_patch` for simplest API.

pub fn securitycenter_organizations_mute_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_mute_configs_patch_execute()` or `securitycenter_organizations_mute_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_mute_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_mute_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_mute_configs_patch_task()`.
/// For the simplest API, use `securitycenter_organizations_mute_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_mute_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_mute_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_mute_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_mute_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsMuteConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/organizations/{organizationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_mute_configs_patch_builder()` + `securitycenter_organizations_mute_configs_patch_execute()`.
/// For task-level control, use `securitycenter_organizations_mute_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsMuteConfigsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_mute_configs_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_organizations_mute_configs_patch_execute(builder)
}

/// POST v1/organizations/{organizationsId}/notificationConfigs
/// Creates a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_notification_configs_create_execute()` to send, or `securitycenter_organizations_notification_configs_create` for simplest API.

pub fn securitycenter_organizations_notification_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    configId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/notificationConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = configId.as_ref() {
        query_parts.push(format!("configId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/notificationConfigs
/// Creates a notification config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_notification_configs_create_execute()` or `securitycenter_organizations_notification_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_notification_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/notificationConfigs
/// Creates a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_notification_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_notification_configs_create_task()`.
/// For the simplest API, use `securitycenter_organizations_notification_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_notification_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_notification_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_notification_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_notification_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsNotificationConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: configId
    pub configId: Option<String>,
}

/// POST v1/organizations/{organizationsId}/notificationConfigs
/// Creates a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_notification_configs_create_builder()` + `securitycenter_organizations_notification_configs_create_execute()`.
/// For task-level control, use `securitycenter_organizations_notification_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsNotificationConfigsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_notification_configs_create_builder(
        client,
        &args.parent,
        &args.configId,
    )?;
    securitycenter_organizations_notification_configs_create_execute(builder)
}

/// DELETE v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_notification_configs_delete_execute()` to send, or `securitycenter_organizations_notification_configs_delete` for simplest API.

pub fn securitycenter_organizations_notification_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/notificationConfigs/{notificationConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_notification_configs_delete_execute()` or `securitycenter_organizations_notification_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_notification_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_notification_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_notification_configs_delete_task()`.
/// For the simplest API, use `securitycenter_organizations_notification_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_notification_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_notification_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_notification_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_notification_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsNotificationConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_notification_configs_delete_builder()` + `securitycenter_organizations_notification_configs_delete_execute()`.
/// For task-level control, use `securitycenter_organizations_notification_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsNotificationConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_notification_configs_delete_builder(client, &args.name)?;
    securitycenter_organizations_notification_configs_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_notification_configs_get_execute()` to send, or `securitycenter_organizations_notification_configs_get` for simplest API.

pub fn securitycenter_organizations_notification_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/notificationConfigs/{notificationConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_notification_configs_get_execute()` or `securitycenter_organizations_notification_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_notification_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_notification_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_notification_configs_get_task()`.
/// For the simplest API, use `securitycenter_organizations_notification_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_notification_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_notification_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_notification_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_notification_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsNotificationConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_notification_configs_get_builder()` + `securitycenter_organizations_notification_configs_get_execute()`.
/// For task-level control, use `securitycenter_organizations_notification_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsNotificationConfigsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_notification_configs_get_builder(client, &args.name)?;
    securitycenter_organizations_notification_configs_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/notificationConfigs
/// Lists notification configs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_notification_configs_list_execute()` to send, or `securitycenter_organizations_notification_configs_list` for simplest API.

pub fn securitycenter_organizations_notification_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/notificationConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/notificationConfigs
/// Lists notification configs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_notification_configs_list_execute()` or `securitycenter_organizations_notification_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_notification_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListNotificationConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNotificationConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/notificationConfigs
/// Lists notification configs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_notification_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_notification_configs_list_task()`.
/// For the simplest API, use `securitycenter_organizations_notification_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_notification_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_notification_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_notification_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_notification_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsNotificationConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/notificationConfigs
/// Lists notification configs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_notification_configs_list_builder()` + `securitycenter_organizations_notification_configs_list_execute()`.
/// For task-level control, use `securitycenter_organizations_notification_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsNotificationConfigsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_notification_configs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    securitycenter_organizations_notification_configs_list_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_notification_configs_patch_execute()` to send, or `securitycenter_organizations_notification_configs_patch` for simplest API.

pub fn securitycenter_organizations_notification_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/notificationConfigs/{notificationConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_notification_configs_patch_execute()` or `securitycenter_organizations_notification_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_notification_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_notification_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_notification_configs_patch_task()`.
/// For the simplest API, use `securitycenter_organizations_notification_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_notification_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_notification_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_notification_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_notification_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsNotificationConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/organizations/{organizationsId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_notification_configs_patch_builder()` + `securitycenter_organizations_notification_configs_patch_execute()`.
/// For task-level control, use `securitycenter_organizations_notification_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsNotificationConfigsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_notification_configs_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_organizations_notification_configs_patch_execute(builder)
}

/// POST v1/organizations/{organizationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_operations_cancel_execute()` to send, or `securitycenter_organizations_operations_cancel` for simplest API.

pub fn securitycenter_organizations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_operations_cancel_execute()` or `securitycenter_organizations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_operations_cancel_task()`.
/// For the simplest API, use `securitycenter_organizations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/organizations/{organizationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_operations_cancel_builder()` + `securitycenter_organizations_operations_cancel_execute()`.
/// For task-level control, use `securitycenter_organizations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_cancel(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_operations_cancel_builder(client, &args.name)?;
    securitycenter_organizations_operations_cancel_execute(builder)
}

/// DELETE v1/organizations/{organizationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_operations_delete_execute()` to send, or `securitycenter_organizations_operations_delete` for simplest API.

pub fn securitycenter_organizations_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/organizations/{organizationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_operations_delete_execute()` or `securitycenter_organizations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/organizations/{organizationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_operations_delete_task()`.
/// For the simplest API, use `securitycenter_organizations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/organizations/{organizationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_operations_delete_builder()` + `securitycenter_organizations_operations_delete_execute()`.
/// For task-level control, use `securitycenter_organizations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_operations_delete_builder(client, &args.name)?;
    securitycenter_organizations_operations_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_operations_get_execute()` to send, or `securitycenter_organizations_operations_get` for simplest API.

pub fn securitycenter_organizations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_operations_get_execute()` or `securitycenter_organizations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_operations_get_task()`.
/// For the simplest API, use `securitycenter_organizations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_operations_get_builder()` + `securitycenter_organizations_operations_get_execute()`.
/// For task-level control, use `securitycenter_organizations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_operations_get_builder(client, &args.name)?;
    securitycenter_organizations_operations_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_operations_list_execute()` to send, or `securitycenter_organizations_operations_list` for simplest API.

pub fn securitycenter_organizations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_operations_list_execute()` or `securitycenter_organizations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_operations_list_task()`.
/// For the simplest API, use `securitycenter_organizations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_operations_list_builder()` + `securitycenter_organizations_operations_list_execute()`.
/// For task-level control, use `securitycenter_organizations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    securitycenter_organizations_operations_list_execute(builder)
}

/// POST v1/organizations/{organizationsId}/resourceValueConfigs:batchCreate
/// Creates a ResourceValueConfig for an organization. Maps user''s tags to difference resource values for use by the attack path simulation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_resource_value_configs_batch_create_execute()` to send, or `securitycenter_organizations_resource_value_configs_batch_create` for simplest API.

pub fn securitycenter_organizations_resource_value_configs_batch_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/resourceValueConfigs:batchCreate",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/resourceValueConfigs:batchCreate
/// Creates a ResourceValueConfig for an organization. Maps user''s tags to difference resource values for use by the attack path simulation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_resource_value_configs_batch_create_execute()` or `securitycenter_organizations_resource_value_configs_batch_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_resource_value_configs_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_batch_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchCreateResourceValueConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchCreateResourceValueConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/resourceValueConfigs:batchCreate
/// Creates a ResourceValueConfig for an organization. Maps user''s tags to difference resource values for use by the attack path simulation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_resource_value_configs_batch_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_resource_value_configs_batch_create_task()`.
/// For the simplest API, use `securitycenter_organizations_resource_value_configs_batch_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_resource_value_configs_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_resource_value_configs_batch_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchCreateResourceValueConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_resource_value_configs_batch_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_resource_value_configs_batch_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsResourceValueConfigsBatchCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/organizations/{organizationsId}/resourceValueConfigs:batchCreate
/// Creates a ResourceValueConfig for an organization. Maps user''s tags to difference resource values for use by the attack path simulation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_resource_value_configs_batch_create_builder()` + `securitycenter_organizations_resource_value_configs_batch_create_execute()`.
/// For task-level control, use `securitycenter_organizations_resource_value_configs_batch_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_batch_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsResourceValueConfigsBatchCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchCreateResourceValueConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_resource_value_configs_batch_create_builder(
        client,
        &args.parent,
    )?;
    securitycenter_organizations_resource_value_configs_batch_create_execute(builder)
}

/// DELETE v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Deletes a ResourceValueConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_resource_value_configs_delete_execute()` to send, or `securitycenter_organizations_resource_value_configs_delete` for simplest API.

pub fn securitycenter_organizations_resource_value_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/resourceValueConfigs/{resourceValueConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Deletes a ResourceValueConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_resource_value_configs_delete_execute()` or `securitycenter_organizations_resource_value_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_resource_value_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Deletes a ResourceValueConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_resource_value_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_resource_value_configs_delete_task()`.
/// For the simplest API, use `securitycenter_organizations_resource_value_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_resource_value_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_resource_value_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_resource_value_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_resource_value_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsResourceValueConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Deletes a ResourceValueConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_resource_value_configs_delete_builder()` + `securitycenter_organizations_resource_value_configs_delete_execute()`.
/// For task-level control, use `securitycenter_organizations_resource_value_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsResourceValueConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_resource_value_configs_delete_builder(client, &args.name)?;
    securitycenter_organizations_resource_value_configs_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Gets a ResourceValueConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_resource_value_configs_get_execute()` to send, or `securitycenter_organizations_resource_value_configs_get` for simplest API.

pub fn securitycenter_organizations_resource_value_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/resourceValueConfigs/{resourceValueConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Gets a ResourceValueConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_resource_value_configs_get_execute()` or `securitycenter_organizations_resource_value_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_resource_value_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1ResourceValueConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1ResourceValueConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Gets a ResourceValueConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_resource_value_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_resource_value_configs_get_task()`.
/// For the simplest API, use `securitycenter_organizations_resource_value_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_resource_value_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_resource_value_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ResourceValueConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_resource_value_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_resource_value_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsResourceValueConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Gets a ResourceValueConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_resource_value_configs_get_builder()` + `securitycenter_organizations_resource_value_configs_get_execute()`.
/// For task-level control, use `securitycenter_organizations_resource_value_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsResourceValueConfigsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ResourceValueConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_resource_value_configs_get_builder(client, &args.name)?;
    securitycenter_organizations_resource_value_configs_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs
/// Lists all ResourceValueConfigs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_resource_value_configs_list_execute()` to send, or `securitycenter_organizations_resource_value_configs_list` for simplest API.

pub fn securitycenter_organizations_resource_value_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/resourceValueConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs
/// Lists all ResourceValueConfigs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_resource_value_configs_list_execute()` or `securitycenter_organizations_resource_value_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_resource_value_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListResourceValueConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListResourceValueConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs
/// Lists all ResourceValueConfigs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_resource_value_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_resource_value_configs_list_task()`.
/// For the simplest API, use `securitycenter_organizations_resource_value_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_resource_value_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_resource_value_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListResourceValueConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_resource_value_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_resource_value_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsResourceValueConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs
/// Lists all ResourceValueConfigs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_resource_value_configs_list_builder()` + `securitycenter_organizations_resource_value_configs_list_execute()`.
/// For task-level control, use `securitycenter_organizations_resource_value_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsResourceValueConfigsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListResourceValueConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_resource_value_configs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    securitycenter_organizations_resource_value_configs_list_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Updates an existing ResourceValueConfigs with new rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_resource_value_configs_patch_execute()` to send, or `securitycenter_organizations_resource_value_configs_patch` for simplest API.

pub fn securitycenter_organizations_resource_value_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/resourceValueConfigs/{resourceValueConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Updates an existing ResourceValueConfigs with new rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_resource_value_configs_patch_execute()` or `securitycenter_organizations_resource_value_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_resource_value_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1ResourceValueConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1ResourceValueConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Updates an existing ResourceValueConfigs with new rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_resource_value_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_resource_value_configs_patch_task()`.
/// For the simplest API, use `securitycenter_organizations_resource_value_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_resource_value_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_resource_value_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ResourceValueConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_resource_value_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_resource_value_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsResourceValueConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/organizations/{organizationsId}/resourceValueConfigs/{resourceValueConfigsId}
/// Updates an existing ResourceValueConfigs with new rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_resource_value_configs_patch_builder()` + `securitycenter_organizations_resource_value_configs_patch_execute()`.
/// For task-level control, use `securitycenter_organizations_resource_value_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsResourceValueConfigsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ResourceValueConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_resource_value_configs_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_organizations_resource_value_configs_patch_execute(builder)
}

/// POST v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_custom_modules_create` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_create_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/customModules",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_execute()` or `securitycenter_organizations_security_health_analytics_settings_custom_modules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_task()`.
/// For the simplest API, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_organizations_security_health_analytics_settings_custom_modules_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_security_health_analytics_settings_custom_modules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_builder()` + `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_execute()`.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_custom_modules_create_builder(client, &args.parent)?;
    securitycenter_organizations_security_health_analytics_settings_custom_modules_create_execute(
        builder,
    )
}

/// DELETE v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_custom_modules_delete` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_execute()` or `securitycenter_organizations_security_health_analytics_settings_custom_modules_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_task()`.
/// For the simplest API, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_security_health_analytics_settings_custom_modules_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_builder()` + `securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_execute()`.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_builder(client, &args.name)?;
    securitycenter_organizations_security_health_analytics_settings_custom_modules_delete_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_custom_modules_get_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_custom_modules_get` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_get_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_get_execute()` or `securitycenter_organizations_security_health_analytics_settings_custom_modules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_get_task()`.
/// For the simplest API, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_organizations_security_health_analytics_settings_custom_modules_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_security_health_analytics_settings_custom_modules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_get_builder()` + `securitycenter_organizations_security_health_analytics_settings_custom_modules_get_execute()`.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_security_health_analytics_settings_custom_modules_get_builder(
            client, &args.name,
        )?;
    securitycenter_organizations_security_health_analytics_settings_custom_modules_get_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_custom_modules_list` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/customModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_execute()` or `securitycenter_organizations_security_health_analytics_settings_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSecurityHealthAnalyticsCustomModulesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecurityHealthAnalyticsCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_organizations_security_health_analytics_settings_custom_modules_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_security_health_analytics_settings_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_builder()` + `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecurityHealthAnalyticsCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_custom_modules_list_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    securitycenter_organizations_security_health_analytics_settings_custom_modules_list_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/customModules:listDescendant",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_execute()` or `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_task()`.
/// For the simplest API, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_builder()` + `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_execute()`.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_custom_modules_patch` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_execute()` or `securitycenter_organizations_security_health_analytics_settings_custom_modules_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_task()`.
/// For the simplest API, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_security_health_analytics_settings_custom_modules_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_builder()` + `securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_execute()`.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_builder(client, &args.name, &args.updateMask)?;
    securitycenter_organizations_security_health_analytics_settings_custom_modules_patch_execute(
        builder,
    )
}

/// POST v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/customModules:simulate",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_execute()` or `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SimulateSecurityHealthAnalyticsCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_task()`.
/// For the simplest API, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_builder()` + `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_execute()`.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_builder(client, &args.parent)?;
    securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_execute()` or `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule,
                >,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_task()`.
/// For the simplest API, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsEffectiveCustomModulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_builder()` + `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_execute()`.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsEffectiveCustomModulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_builder(client, &args.name)?;
    securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/effectiveCustomModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_execute()` or `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_builder()` + `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}
/// Get the simulation by name or the latest simulation for the given organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_simulations_get_execute()` to send, or `securitycenter_organizations_simulations_get` for simplest API.

pub fn securitycenter_organizations_simulations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/simulations/{simulationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}
/// Get the simulation by name or the latest simulation for the given organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_simulations_get_execute()` or `securitycenter_organizations_simulations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_simulations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Simulation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Simulation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}
/// Get the simulation by name or the latest simulation for the given organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_simulations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_simulations_get_task()`.
/// For the simplest API, use `securitycenter_organizations_simulations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_simulations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_simulations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Simulation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_simulations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_simulations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSimulationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}
/// Get the simulation by name or the latest simulation for the given organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_simulations_get_builder()` + `securitycenter_organizations_simulations_get_execute()`.
/// For task-level control, use `securitycenter_organizations_simulations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSimulationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Simulation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_simulations_get_builder(client, &args.name)?;
    securitycenter_organizations_simulations_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackExposureResults/{attackExposureResultsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_execute()` to send, or `securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list` for simplest API.

pub fn securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/simulations/{simulationsId}/attackExposureResults/{attackExposureResultsId}/attackPaths",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackExposureResults/{attackExposureResultsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_execute()` or `securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAttackPathsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAttackPathsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackExposureResults/{attackExposureResultsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_task()`.
/// For the simplest API, use `securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSimulationsAttackExposureResultsAttackPathsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackExposureResults/{attackExposureResultsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_builder()` + `securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_execute()`.
/// For task-level control, use `securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSimulationsAttackExposureResultsAttackPathsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_builder(
            client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )?;
    securitycenter_organizations_simulations_attack_exposure_results_attack_paths_list_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackExposureResults/{attackExposureResultsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_execute()` to send, or `securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list` for simplest API.

pub fn securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/simulations/{simulationsId}/attackExposureResults/{attackExposureResultsId}/valuedResources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackExposureResults/{attackExposureResultsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_execute()` or `securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListValuedResourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackExposureResults/{attackExposureResultsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_task()`.
/// For the simplest API, use `securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSimulationsAttackExposureResultsValuedResourcesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackExposureResults/{attackExposureResultsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_builder()` + `securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_execute()`.
/// For task-level control, use `securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSimulationsAttackExposureResultsValuedResourcesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_builder(client, &args.parent, &args.filter, &args.orderBy, &args.pageSize, &args.pageToken)?;
    securitycenter_organizations_simulations_attack_exposure_results_valued_resources_list_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_simulations_attack_paths_list_execute()` to send, or `securitycenter_organizations_simulations_attack_paths_list` for simplest API.

pub fn securitycenter_organizations_simulations_attack_paths_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/simulations/{simulationsId}/attackPaths",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_simulations_attack_paths_list_execute()` or `securitycenter_organizations_simulations_attack_paths_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_simulations_attack_paths_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_attack_paths_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAttackPathsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAttackPathsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_simulations_attack_paths_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_simulations_attack_paths_list_task()`.
/// For the simplest API, use `securitycenter_organizations_simulations_attack_paths_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_simulations_attack_paths_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_simulations_attack_paths_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_simulations_attack_paths_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_simulations_attack_paths_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSimulationsAttackPathsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_simulations_attack_paths_list_builder()` + `securitycenter_organizations_simulations_attack_paths_list_execute()`.
/// For task-level control, use `securitycenter_organizations_simulations_attack_paths_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_attack_paths_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSimulationsAttackPathsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_simulations_attack_paths_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    securitycenter_organizations_simulations_attack_paths_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources/{valuedResourcesId}
/// Get the valued resource by name
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_simulations_valued_resources_get_execute()` to send, or `securitycenter_organizations_simulations_valued_resources_get` for simplest API.

pub fn securitycenter_organizations_simulations_valued_resources_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/simulations/{simulationsId}/valuedResources/{valuedResourcesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources/{valuedResourcesId}
/// Get the valued resource by name
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_simulations_valued_resources_get_execute()` or `securitycenter_organizations_simulations_valued_resources_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_simulations_valued_resources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_valued_resources_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ValuedResource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValuedResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources/{valuedResourcesId}
/// Get the valued resource by name
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_simulations_valued_resources_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_simulations_valued_resources_get_task()`.
/// For the simplest API, use `securitycenter_organizations_simulations_valued_resources_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_simulations_valued_resources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_simulations_valued_resources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ValuedResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_simulations_valued_resources_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_simulations_valued_resources_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSimulationsValuedResourcesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources/{valuedResourcesId}
/// Get the valued resource by name
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_simulations_valued_resources_get_builder()` + `securitycenter_organizations_simulations_valued_resources_get_execute()`.
/// For task-level control, use `securitycenter_organizations_simulations_valued_resources_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_valued_resources_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSimulationsValuedResourcesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ValuedResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_simulations_valued_resources_get_builder(client, &args.name)?;
    securitycenter_organizations_simulations_valued_resources_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_simulations_valued_resources_list_execute()` to send, or `securitycenter_organizations_simulations_valued_resources_list` for simplest API.

pub fn securitycenter_organizations_simulations_valued_resources_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/simulations/{simulationsId}/valuedResources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_simulations_valued_resources_list_execute()` or `securitycenter_organizations_simulations_valued_resources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_simulations_valued_resources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_valued_resources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListValuedResourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_simulations_valued_resources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_simulations_valued_resources_list_task()`.
/// For the simplest API, use `securitycenter_organizations_simulations_valued_resources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_simulations_valued_resources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_simulations_valued_resources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_simulations_valued_resources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_simulations_valued_resources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSimulationsValuedResourcesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_simulations_valued_resources_list_builder()` + `securitycenter_organizations_simulations_valued_resources_list_execute()`.
/// For task-level control, use `securitycenter_organizations_simulations_valued_resources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_valued_resources_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSimulationsValuedResourcesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_simulations_valued_resources_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    securitycenter_organizations_simulations_valued_resources_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources/{valuedResourcesId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_simulations_valued_resources_attack_paths_list_execute()` to send, or `securitycenter_organizations_simulations_valued_resources_attack_paths_list` for simplest API.

pub fn securitycenter_organizations_simulations_valued_resources_attack_paths_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/simulations/{simulationsId}/valuedResources/{valuedResourcesId}/attackPaths",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources/{valuedResourcesId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_simulations_valued_resources_attack_paths_list_execute()` or `securitycenter_organizations_simulations_valued_resources_attack_paths_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_simulations_valued_resources_attack_paths_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_valued_resources_attack_paths_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAttackPathsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAttackPathsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources/{valuedResourcesId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_simulations_valued_resources_attack_paths_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_simulations_valued_resources_attack_paths_list_task()`.
/// For the simplest API, use `securitycenter_organizations_simulations_valued_resources_attack_paths_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_simulations_valued_resources_attack_paths_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_simulations_valued_resources_attack_paths_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_organizations_simulations_valued_resources_attack_paths_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_simulations_valued_resources_attack_paths_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSimulationsValuedResourcesAttackPathsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/simulations/{simulationsId}/valuedResources/{valuedResourcesId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_simulations_valued_resources_attack_paths_list_builder()` + `securitycenter_organizations_simulations_valued_resources_attack_paths_list_execute()`.
/// For task-level control, use `securitycenter_organizations_simulations_valued_resources_attack_paths_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_simulations_valued_resources_attack_paths_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSimulationsValuedResourcesAttackPathsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_simulations_valued_resources_attack_paths_list_builder(
            client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )?;
    securitycenter_organizations_simulations_valued_resources_attack_paths_list_execute(builder)
}

/// POST v1/organizations/{organizationsId}/sources
/// Creates a source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_create_execute()` to send, or `securitycenter_organizations_sources_create` for simplest API.

pub fn securitycenter_organizations_sources_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/sources
/// Creates a source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_sources_create_execute()` or `securitycenter_organizations_sources_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Source>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/sources
/// Creates a source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_sources_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_sources_create_task()`.
/// For the simplest API, use `securitycenter_organizations_sources_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_sources_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_sources_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_sources_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSourcesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/organizations/{organizationsId}/sources
/// Creates a source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_create_builder()` + `securitycenter_organizations_sources_create_execute()`.
/// For task-level control, use `securitycenter_organizations_sources_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSourcesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_sources_create_builder(client, &args.parent)?;
    securitycenter_organizations_sources_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}
/// Gets a source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_get_execute()` to send, or `securitycenter_organizations_sources_get` for simplest API.

pub fn securitycenter_organizations_sources_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{sourcesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}
/// Gets a source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_sources_get_execute()` or `securitycenter_organizations_sources_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Source>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}
/// Gets a source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_sources_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_sources_get_task()`.
/// For the simplest API, use `securitycenter_organizations_sources_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_sources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_sources_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_sources_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSourcesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}
/// Gets a source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_get_builder()` + `securitycenter_organizations_sources_get_execute()`.
/// For task-level control, use `securitycenter_organizations_sources_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSourcesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_sources_get_builder(client, &args.name)?;
    securitycenter_organizations_sources_get_execute(builder)
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}:getIamPolicy
/// Gets the access control policy on the specified Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_get_iam_policy_execute()` to send, or `securitycenter_organizations_sources_get_iam_policy` for simplest API.

pub fn securitycenter_organizations_sources_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{sourcesId}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}:getIamPolicy
/// Gets the access control policy on the specified Source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_sources_get_iam_policy_execute()` or `securitycenter_organizations_sources_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}:getIamPolicy
/// Gets the access control policy on the specified Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_sources_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_sources_get_iam_policy_task()`.
/// For the simplest API, use `securitycenter_organizations_sources_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_sources_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_sources_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_sources_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSourcesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}:getIamPolicy
/// Gets the access control policy on the specified Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_get_iam_policy_builder()` + `securitycenter_organizations_sources_get_iam_policy_execute()`.
/// For task-level control, use `securitycenter_organizations_sources_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_get_iam_policy(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSourcesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_sources_get_iam_policy_builder(client, &args.resource)?;
    securitycenter_organizations_sources_get_iam_policy_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources
/// Lists all sources belonging to an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_list_execute()` to send, or `securitycenter_organizations_sources_list` for simplest API.

pub fn securitycenter_organizations_sources_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sources
/// Lists all sources belonging to an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_sources_list_execute()` or `securitycenter_organizations_sources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/sources
/// Lists all sources belonging to an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_sources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_sources_list_task()`.
/// For the simplest API, use `securitycenter_organizations_sources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_sources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_sources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSourcesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/sources
/// Lists all sources belonging to an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_list_builder()` + `securitycenter_organizations_sources_list_execute()`.
/// For task-level control, use `securitycenter_organizations_sources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSourcesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_sources_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    securitycenter_organizations_sources_list_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/sources/{sourcesId}
/// Updates a source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_patch_execute()` to send, or `securitycenter_organizations_sources_patch` for simplest API.

pub fn securitycenter_organizations_sources_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{sourcesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/sources/{sourcesId}
/// Updates a source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_sources_patch_execute()` or `securitycenter_organizations_sources_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Source>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/sources/{sourcesId}
/// Updates a source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_sources_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_sources_patch_task()`.
/// For the simplest API, use `securitycenter_organizations_sources_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_sources_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_sources_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_sources_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSourcesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/organizations/{organizationsId}/sources/{sourcesId}
/// Updates a source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_patch_builder()` + `securitycenter_organizations_sources_patch_execute()`.
/// For task-level control, use `securitycenter_organizations_sources_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSourcesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_sources_patch_builder(client, &args.name, &args.updateMask)?;
    securitycenter_organizations_sources_patch_execute(builder)
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}:setIamPolicy
/// Sets the access control policy on the specified Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_set_iam_policy_execute()` to send, or `securitycenter_organizations_sources_set_iam_policy` for simplest API.

pub fn securitycenter_organizations_sources_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{sourcesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}:setIamPolicy
/// Sets the access control policy on the specified Source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_sources_set_iam_policy_execute()` or `securitycenter_organizations_sources_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}:setIamPolicy
/// Sets the access control policy on the specified Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_sources_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_sources_set_iam_policy_task()`.
/// For the simplest API, use `securitycenter_organizations_sources_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_sources_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_sources_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_sources_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSourcesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}:setIamPolicy
/// Sets the access control policy on the specified Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_set_iam_policy_builder()` + `securitycenter_organizations_sources_set_iam_policy_execute()`.
/// For task-level control, use `securitycenter_organizations_sources_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_set_iam_policy(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSourcesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_sources_set_iam_policy_builder(client, &args.resource)?;
    securitycenter_organizations_sources_set_iam_policy_execute(builder)
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}:testIamPermissions
/// Returns the permissions that a caller has on the specified source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_test_iam_permissions_execute()` to send, or `securitycenter_organizations_sources_test_iam_permissions` for simplest API.

pub fn securitycenter_organizations_sources_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{sourcesId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}:testIamPermissions
/// Returns the permissions that a caller has on the specified source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_sources_test_iam_permissions_execute()` or `securitycenter_organizations_sources_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}:testIamPermissions
/// Returns the permissions that a caller has on the specified source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_sources_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_sources_test_iam_permissions_task()`.
/// For the simplest API, use `securitycenter_organizations_sources_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_sources_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_sources_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_sources_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSourcesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}:testIamPermissions
/// Returns the permissions that a caller has on the specified source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_test_iam_permissions_builder()` + `securitycenter_organizations_sources_test_iam_permissions_execute()`.
/// For task-level control, use `securitycenter_organizations_sources_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSourcesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_sources_test_iam_permissions_builder(client, &args.resource)?;
    securitycenter_organizations_sources_test_iam_permissions_execute(builder)
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}/findings
/// Creates a finding. The corresponding source must exist for finding creation to succeed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_findings_create_execute()` to send, or `securitycenter_organizations_sources_findings_create` for simplest API.

pub fn securitycenter_organizations_sources_findings_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    findingId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{sourcesId}/findings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = findingId.as_ref() {
        query_parts.push(format!("findingId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}/findings
/// Creates a finding. The corresponding source must exist for finding creation to succeed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_sources_findings_create_execute()` or `securitycenter_organizations_sources_findings_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_findings_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Finding>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}/findings
/// Creates a finding. The corresponding source must exist for finding creation to succeed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_sources_findings_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_sources_findings_create_task()`.
/// For the simplest API, use `securitycenter_organizations_sources_findings_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_findings_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_sources_findings_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_sources_findings_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_sources_findings_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSourcesFindingsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: findingId
    pub findingId: Option<String>,
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}/findings
/// Creates a finding. The corresponding source must exist for finding creation to succeed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_findings_create_builder()` + `securitycenter_organizations_sources_findings_create_execute()`.
/// For task-level control, use `securitycenter_organizations_sources_findings_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSourcesFindingsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_sources_findings_create_builder(
        client,
        &args.parent,
        &args.findingId,
    )?;
    securitycenter_organizations_sources_findings_create_execute(builder)
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}/findings:group
/// Filters an organization or source''s findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_findings_group_execute()` to send, or `securitycenter_organizations_sources_findings_group` for simplest API.

pub fn securitycenter_organizations_sources_findings_group_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{sourcesId}/findings:group",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}/findings:group
/// Filters an organization or source''s findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_sources_findings_group_execute()` or `securitycenter_organizations_sources_findings_group`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_findings_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_group_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GroupFindingsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupFindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}/findings:group
/// Filters an organization or source''s findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_sources_findings_group_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_sources_findings_group_task()`.
/// For the simplest API, use `securitycenter_organizations_sources_findings_group()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_findings_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_sources_findings_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_sources_findings_group_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_sources_findings_group`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSourcesFindingsGroupArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}/findings:group
/// Filters an organization or source''s findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_findings_group_builder()` + `securitycenter_organizations_sources_findings_group_execute()`.
/// For task-level control, use `securitycenter_organizations_sources_findings_group_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_group(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSourcesFindingsGroupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_sources_findings_group_builder(client, &args.parent)?;
    securitycenter_organizations_sources_findings_group_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings
/// Lists an organization or source''s findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_findings_list_execute()` to send, or `securitycenter_organizations_sources_findings_list` for simplest API.

pub fn securitycenter_organizations_sources_findings_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    compareDuration: &Option<String>,
    fieldMask: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    readTime: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{sourcesId}/findings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = compareDuration.as_ref() {
        query_parts.push(format!("compareDuration={}", val));
    }
    if let Some(val) = fieldMask.as_ref() {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime.as_ref() {
        query_parts.push(format!("readTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings
/// Lists an organization or source''s findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_sources_findings_list_execute()` or `securitycenter_organizations_sources_findings_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_findings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFindingsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings
/// Lists an organization or source''s findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_sources_findings_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_sources_findings_list_task()`.
/// For the simplest API, use `securitycenter_organizations_sources_findings_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_findings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_sources_findings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_sources_findings_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_sources_findings_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSourcesFindingsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: compareDuration
    pub compareDuration: Option<String>,
    /// Query parameter: fieldMask
    pub fieldMask: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readTime
    pub readTime: Option<String>,
}

/// GET v1/organizations/{organizationsId}/sources/{sourcesId}/findings
/// Lists an organization or source''s findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_findings_list_builder()` + `securitycenter_organizations_sources_findings_list_execute()`.
/// For task-level control, use `securitycenter_organizations_sources_findings_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSourcesFindingsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_sources_findings_list_builder(
        client,
        &args.parent,
        &args.compareDuration,
        &args.fieldMask,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.readTime,
    )?;
    securitycenter_organizations_sources_findings_list_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_findings_patch_execute()` to send, or `securitycenter_organizations_sources_findings_patch` for simplest API.

pub fn securitycenter_organizations_sources_findings_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{sourcesId}/findings/{findingsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_sources_findings_patch_execute()` or `securitycenter_organizations_sources_findings_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_findings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Finding>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_sources_findings_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_sources_findings_patch_task()`.
/// For the simplest API, use `securitycenter_organizations_sources_findings_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_findings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_sources_findings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_sources_findings_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_sources_findings_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSourcesFindingsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_findings_patch_builder()` + `securitycenter_organizations_sources_findings_patch_execute()`.
/// For task-level control, use `securitycenter_organizations_sources_findings_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSourcesFindingsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_sources_findings_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_organizations_sources_findings_patch_execute(builder)
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_findings_set_mute_execute()` to send, or `securitycenter_organizations_sources_findings_set_mute` for simplest API.

pub fn securitycenter_organizations_sources_findings_set_mute_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{sourcesId}/findings/{findingsId}:setMute",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_sources_findings_set_mute_execute()` or `securitycenter_organizations_sources_findings_set_mute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_findings_set_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_set_mute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Finding>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_sources_findings_set_mute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_sources_findings_set_mute_task()`.
/// For the simplest API, use `securitycenter_organizations_sources_findings_set_mute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_findings_set_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_sources_findings_set_mute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_sources_findings_set_mute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_sources_findings_set_mute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSourcesFindingsSetMuteArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_findings_set_mute_builder()` + `securitycenter_organizations_sources_findings_set_mute_execute()`.
/// For task-level control, use `securitycenter_organizations_sources_findings_set_mute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_set_mute(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSourcesFindingsSetMuteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_sources_findings_set_mute_builder(client, &args.name)?;
    securitycenter_organizations_sources_findings_set_mute_execute(builder)
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_findings_set_state_execute()` to send, or `securitycenter_organizations_sources_findings_set_state` for simplest API.

pub fn securitycenter_organizations_sources_findings_set_state_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{sourcesId}/findings/{findingsId}:setState",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_sources_findings_set_state_execute()` or `securitycenter_organizations_sources_findings_set_state`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_findings_set_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_set_state_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Finding>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_sources_findings_set_state_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_sources_findings_set_state_task()`.
/// For the simplest API, use `securitycenter_organizations_sources_findings_set_state()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_findings_set_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_sources_findings_set_state_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_sources_findings_set_state_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_sources_findings_set_state`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSourcesFindingsSetStateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_findings_set_state_builder()` + `securitycenter_organizations_sources_findings_set_state_execute()`.
/// For task-level control, use `securitycenter_organizations_sources_findings_set_state_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_set_state(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSourcesFindingsSetStateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_sources_findings_set_state_builder(client, &args.name)?;
    securitycenter_organizations_sources_findings_set_state_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_findings_update_security_marks_execute()` to send, or `securitycenter_organizations_sources_findings_update_security_marks` for simplest API.

pub fn securitycenter_organizations_sources_findings_update_security_marks_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    startTime: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{sourcesId}/findings/{findingsId}/securityMarks",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = startTime.as_ref() {
        query_parts.push(format!("startTime={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_sources_findings_update_security_marks_execute()` or `securitycenter_organizations_sources_findings_update_security_marks`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_findings_update_security_marks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_update_security_marks_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityMarks>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityMarks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_sources_findings_update_security_marks_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_sources_findings_update_security_marks_task()`.
/// For the simplest API, use `securitycenter_organizations_sources_findings_update_security_marks()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_findings_update_security_marks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_sources_findings_update_security_marks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_sources_findings_update_security_marks_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_sources_findings_update_security_marks`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSourcesFindingsUpdateSecurityMarksArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: startTime
    pub startTime: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_findings_update_security_marks_builder()` + `securitycenter_organizations_sources_findings_update_security_marks_execute()`.
/// For task-level control, use `securitycenter_organizations_sources_findings_update_security_marks_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_update_security_marks(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSourcesFindingsUpdateSecurityMarksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_sources_findings_update_security_marks_builder(
        client,
        &args.name,
        &args.startTime,
        &args.updateMask,
    )?;
    securitycenter_organizations_sources_findings_update_security_marks_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_findings_external_systems_patch_execute()` to send, or `securitycenter_organizations_sources_findings_external_systems_patch` for simplest API.

pub fn securitycenter_organizations_sources_findings_external_systems_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_sources_findings_external_systems_patch_execute()` or `securitycenter_organizations_sources_findings_external_systems_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_findings_external_systems_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_external_systems_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1ExternalSystem>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1ExternalSystem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_sources_findings_external_systems_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_sources_findings_external_systems_patch_task()`.
/// For the simplest API, use `securitycenter_organizations_sources_findings_external_systems_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_findings_external_systems_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_sources_findings_external_systems_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ExternalSystem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_sources_findings_external_systems_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_sources_findings_external_systems_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSourcesFindingsExternalSystemsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/organizations/{organizationsId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_findings_external_systems_patch_builder()` + `securitycenter_organizations_sources_findings_external_systems_patch_execute()`.
/// For task-level control, use `securitycenter_organizations_sources_findings_external_systems_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_findings_external_systems_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSourcesFindingsExternalSystemsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ExternalSystem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_sources_findings_external_systems_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_organizations_sources_findings_external_systems_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_valued_resources_list_execute()` to send, or `securitycenter_organizations_valued_resources_list` for simplest API.

pub fn securitycenter_organizations_valued_resources_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/valuedResources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_valued_resources_list_execute()` or `securitycenter_organizations_valued_resources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_valued_resources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_valued_resources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListValuedResourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_valued_resources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_valued_resources_list_task()`.
/// For the simplest API, use `securitycenter_organizations_valued_resources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_valued_resources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_valued_resources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_valued_resources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_valued_resources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsValuedResourcesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_valued_resources_list_builder()` + `securitycenter_organizations_valued_resources_list_execute()`.
/// For task-level control, use `securitycenter_organizations_valued_resources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_valued_resources_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsValuedResourcesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_valued_resources_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    securitycenter_organizations_valued_resources_list_execute(builder)
}

/// POST v1/projects/{projectsId}/assets:group
/// Filters an organization''s assets and groups them by their specified properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_assets_group_execute()` to send, or `securitycenter_projects_assets_group` for simplest API.

pub fn securitycenter_projects_assets_group_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/assets:group",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/assets:group
/// Filters an organization''s assets and groups them by their specified properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_assets_group_execute()` or `securitycenter_projects_assets_group`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_assets_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_assets_group_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GroupAssetsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/assets:group
/// Filters an organization''s assets and groups them by their specified properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_assets_group_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_assets_group_task()`.
/// For the simplest API, use `securitycenter_projects_assets_group()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_assets_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_assets_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_assets_group_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_assets_group`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsAssetsGroupArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/assets:group
/// Filters an organization''s assets and groups them by their specified properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_assets_group_builder()` + `securitycenter_projects_assets_group_execute()`.
/// For task-level control, use `securitycenter_projects_assets_group_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_assets_group(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsAssetsGroupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_assets_group_builder(client, &args.parent)?;
    securitycenter_projects_assets_group_execute(builder)
}

/// GET v1/projects/{projectsId}/assets
/// Lists an organization''s assets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_assets_list_execute()` to send, or `securitycenter_projects_assets_list` for simplest API.

pub fn securitycenter_projects_assets_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    compareDuration: &Option<String>,
    fieldMask: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    readTime: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/assets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = compareDuration.as_ref() {
        query_parts.push(format!("compareDuration={}", val));
    }
    if let Some(val) = fieldMask.as_ref() {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime.as_ref() {
        query_parts.push(format!("readTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/assets
/// Lists an organization''s assets.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_assets_list_execute()` or `securitycenter_projects_assets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_assets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAssetsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/assets
/// Lists an organization''s assets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_assets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_assets_list_task()`.
/// For the simplest API, use `securitycenter_projects_assets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_assets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_assets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_assets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsAssetsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: compareDuration
    pub compareDuration: Option<String>,
    /// Query parameter: fieldMask
    pub fieldMask: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readTime
    pub readTime: Option<String>,
}

/// GET v1/projects/{projectsId}/assets
/// Lists an organization''s assets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_assets_list_builder()` + `securitycenter_projects_assets_list_execute()`.
/// For task-level control, use `securitycenter_projects_assets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_assets_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsAssetsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_assets_list_builder(
        client,
        &args.parent,
        &args.compareDuration,
        &args.fieldMask,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.readTime,
    )?;
    securitycenter_projects_assets_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_assets_update_security_marks_execute()` to send, or `securitycenter_projects_assets_update_security_marks` for simplest API.

pub fn securitycenter_projects_assets_update_security_marks_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    startTime: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/assets/{assetsId}/securityMarks",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = startTime.as_ref() {
        query_parts.push(format!("startTime={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_assets_update_security_marks_execute()` or `securitycenter_projects_assets_update_security_marks`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_assets_update_security_marks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_assets_update_security_marks_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityMarks>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityMarks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_assets_update_security_marks_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_assets_update_security_marks_task()`.
/// For the simplest API, use `securitycenter_projects_assets_update_security_marks()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_assets_update_security_marks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_assets_update_security_marks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_assets_update_security_marks_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_assets_update_security_marks`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsAssetsUpdateSecurityMarksArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: startTime
    pub startTime: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/assets/{assetsId}/securityMarks
/// Updates security marks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_assets_update_security_marks_builder()` + `securitycenter_projects_assets_update_security_marks_execute()`.
/// For task-level control, use `securitycenter_projects_assets_update_security_marks_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_assets_update_security_marks(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsAssetsUpdateSecurityMarksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_assets_update_security_marks_builder(
        client,
        &args.name,
        &args.startTime,
        &args.updateMask,
    )?;
    securitycenter_projects_assets_update_security_marks_execute(builder)
}

/// POST v1/projects/{projectsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_big_query_exports_create_execute()` to send, or `securitycenter_projects_big_query_exports_create` for simplest API.

pub fn securitycenter_projects_big_query_exports_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    bigQueryExportId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/bigQueryExports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bigQueryExportId.as_ref() {
        query_parts.push(format!("bigQueryExportId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_big_query_exports_create_execute()` or `securitycenter_projects_big_query_exports_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_big_query_exports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_big_query_exports_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_big_query_exports_create_task()`.
/// For the simplest API, use `securitycenter_projects_big_query_exports_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_big_query_exports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_big_query_exports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_big_query_exports_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_big_query_exports_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsBigQueryExportsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bigQueryExportId
    pub bigQueryExportId: Option<String>,
}

/// POST v1/projects/{projectsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_big_query_exports_create_builder()` + `securitycenter_projects_big_query_exports_create_execute()`.
/// For task-level control, use `securitycenter_projects_big_query_exports_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsBigQueryExportsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_big_query_exports_create_builder(
        client,
        &args.parent,
        &args.bigQueryExportId,
    )?;
    securitycenter_projects_big_query_exports_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_big_query_exports_delete_execute()` to send, or `securitycenter_projects_big_query_exports_delete` for simplest API.

pub fn securitycenter_projects_big_query_exports_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/bigQueryExports/{bigQueryExportsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_big_query_exports_delete_execute()` or `securitycenter_projects_big_query_exports_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_big_query_exports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_big_query_exports_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_big_query_exports_delete_task()`.
/// For the simplest API, use `securitycenter_projects_big_query_exports_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_big_query_exports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_big_query_exports_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_projects_big_query_exports_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_big_query_exports_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsBigQueryExportsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Deletes an existing BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_big_query_exports_delete_builder()` + `securitycenter_projects_big_query_exports_delete_execute()`.
/// For task-level control, use `securitycenter_projects_big_query_exports_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsBigQueryExportsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_big_query_exports_delete_builder(client, &args.name)?;
    securitycenter_projects_big_query_exports_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_big_query_exports_get_execute()` to send, or `securitycenter_projects_big_query_exports_get` for simplest API.

pub fn securitycenter_projects_big_query_exports_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/bigQueryExports/{bigQueryExportsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_big_query_exports_get_execute()` or `securitycenter_projects_big_query_exports_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_big_query_exports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_big_query_exports_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_big_query_exports_get_task()`.
/// For the simplest API, use `securitycenter_projects_big_query_exports_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_big_query_exports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_big_query_exports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_big_query_exports_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_big_query_exports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsBigQueryExportsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Gets a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_big_query_exports_get_builder()` + `securitycenter_projects_big_query_exports_get_execute()`.
/// For task-level control, use `securitycenter_projects_big_query_exports_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsBigQueryExportsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_big_query_exports_get_builder(client, &args.name)?;
    securitycenter_projects_big_query_exports_get_execute(builder)
}

/// GET v1/projects/{projectsId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_big_query_exports_list_execute()` to send, or `securitycenter_projects_big_query_exports_list` for simplest API.

pub fn securitycenter_projects_big_query_exports_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/bigQueryExports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_big_query_exports_list_execute()` or `securitycenter_projects_big_query_exports_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_big_query_exports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBigQueryExportsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBigQueryExportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_big_query_exports_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_big_query_exports_list_task()`.
/// For the simplest API, use `securitycenter_projects_big_query_exports_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_big_query_exports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_big_query_exports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBigQueryExportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_big_query_exports_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_big_query_exports_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsBigQueryExportsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/bigQueryExports
/// Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports under that level are also returned e.g. if requesting BigQuery exports under a folder, then all BigQuery exports immediately under the folder plus the ones created under the projects within the folder are returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_big_query_exports_list_builder()` + `securitycenter_projects_big_query_exports_list_execute()`.
/// For task-level control, use `securitycenter_projects_big_query_exports_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsBigQueryExportsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBigQueryExportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_big_query_exports_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    securitycenter_projects_big_query_exports_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_big_query_exports_patch_execute()` to send, or `securitycenter_projects_big_query_exports_patch` for simplest API.

pub fn securitycenter_projects_big_query_exports_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/bigQueryExports/{bigQueryExportsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_big_query_exports_patch_execute()` or `securitycenter_projects_big_query_exports_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_big_query_exports_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_big_query_exports_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_big_query_exports_patch_task()`.
/// For the simplest API, use `securitycenter_projects_big_query_exports_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_big_query_exports_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_big_query_exports_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_big_query_exports_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_big_query_exports_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsBigQueryExportsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/bigQueryExports/{bigQueryExportsId}
/// Updates a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_big_query_exports_patch_builder()` + `securitycenter_projects_big_query_exports_patch_execute()`.
/// For task-level control, use `securitycenter_projects_big_query_exports_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsBigQueryExportsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_big_query_exports_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_projects_big_query_exports_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_validate_custom_module_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_validate_custom_module` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_validate_custom_module_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings:validateCustomModule",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_event_threat_detection_settings_validate_custom_module_execute()` or `securitycenter_projects_event_threat_detection_settings_validate_custom_module`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_validate_custom_module_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_validate_custom_module_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValidateEventThreatDetectionCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_event_threat_detection_settings_validate_custom_module_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_validate_custom_module_task()`.
/// For the simplest API, use `securitycenter_projects_event_threat_detection_settings_validate_custom_module()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_validate_custom_module_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_event_threat_detection_settings_validate_custom_module_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_event_threat_detection_settings_validate_custom_module_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_event_threat_detection_settings_validate_custom_module`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsEventThreatDetectionSettingsValidateCustomModuleArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_validate_custom_module_builder()` + `securitycenter_projects_event_threat_detection_settings_validate_custom_module_execute()`.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_validate_custom_module_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_validate_custom_module(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsEventThreatDetectionSettingsValidateCustomModuleArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_event_threat_detection_settings_validate_custom_module_builder(
            client,
            &args.parent,
        )?;
    securitycenter_projects_event_threat_detection_settings_validate_custom_module_execute(builder)
}

/// POST v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_custom_modules_create_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_custom_modules_create` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/customModules",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_event_threat_detection_settings_custom_modules_create_execute()` or `securitycenter_projects_event_threat_detection_settings_custom_modules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_custom_modules_create_task()`.
/// For the simplest API, use `securitycenter_projects_event_threat_detection_settings_custom_modules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_event_threat_detection_settings_custom_modules_create_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_event_threat_detection_settings_custom_modules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_create_builder()` + `securitycenter_projects_event_threat_detection_settings_custom_modules_create_execute()`.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_custom_modules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_event_threat_detection_settings_custom_modules_create_builder(
            client,
            &args.parent,
        )?;
    securitycenter_projects_event_threat_detection_settings_custom_modules_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_custom_modules_delete_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_custom_modules_delete` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_event_threat_detection_settings_custom_modules_delete_execute()` or `securitycenter_projects_event_threat_detection_settings_custom_modules_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_custom_modules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_custom_modules_delete_task()`.
/// For the simplest API, use `securitycenter_projects_event_threat_detection_settings_custom_modules_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_custom_modules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_projects_event_threat_detection_settings_custom_modules_delete_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_event_threat_detection_settings_custom_modules_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Deletes the specified Event Threat Detection custom module and all of its descendants in the Resource Manager hierarchy. This method is only supported for resident custom modules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_delete_builder()` + `securitycenter_projects_event_threat_detection_settings_custom_modules_delete_execute()`.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_custom_modules_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_event_threat_detection_settings_custom_modules_delete_builder(
            client, &args.name,
        )?;
    securitycenter_projects_event_threat_detection_settings_custom_modules_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_custom_modules_get_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_custom_modules_get` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_event_threat_detection_settings_custom_modules_get_execute()` or `securitycenter_projects_event_threat_detection_settings_custom_modules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_custom_modules_get_task()`.
/// For the simplest API, use `securitycenter_projects_event_threat_detection_settings_custom_modules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_projects_event_threat_detection_settings_custom_modules_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_event_threat_detection_settings_custom_modules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Gets an Event Threat Detection custom module.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_get_builder()` + `securitycenter_projects_event_threat_detection_settings_custom_modules_get_execute()`.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_custom_modules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_event_threat_detection_settings_custom_modules_get_builder(
            client, &args.name,
        )?;
    securitycenter_projects_event_threat_detection_settings_custom_modules_get_execute(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_custom_modules_list` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/customModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_execute()` or `securitycenter_projects_event_threat_detection_settings_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListEventThreatDetectionCustomModulesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListEventThreatDetectionCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_projects_event_threat_detection_settings_custom_modules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_event_threat_detection_settings_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes resident modules defined at the scope of the parent along with modules inherited from ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_builder()` + `securitycenter_projects_event_threat_detection_settings_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListEventThreatDetectionCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_event_threat_detection_settings_custom_modules_list_builder(
            client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )?;
    securitycenter_projects_event_threat_detection_settings_custom_modules_list_execute(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/customModules:listDescendant",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_execute()` or `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_task()`.
/// For the simplest API, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesListDescendantArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_builder()` + `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_execute()`.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesListDescendantArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_execute(
        builder,
    )
}

/// PATCH v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_custom_modules_patch_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_custom_modules_patch` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_event_threat_detection_settings_custom_modules_patch_execute()` or `securitycenter_projects_event_threat_detection_settings_custom_modules_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_custom_modules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_custom_modules_patch_task()`.
/// For the simplest API, use `securitycenter_projects_event_threat_detection_settings_custom_modules_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_custom_modules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_projects_event_threat_detection_settings_custom_modules_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_event_threat_detection_settings_custom_modules_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/eventThreatDetectionSettings/customModules/{customModulesId}
/// Updates the Event Threat Detection custom module with the given name based on the given update mask. Updating the enablement state is supported for both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name or configuration of a module is supported for resident modules only. The type of a module cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_patch_builder()` + `securitycenter_projects_event_threat_detection_settings_custom_modules_patch_execute()`.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_custom_modules_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_event_threat_detection_settings_custom_modules_patch_builder(
            client,
            &args.name,
            &args.updateMask,
        )?;
    securitycenter_projects_event_threat_detection_settings_custom_modules_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_execute()` or `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EffectiveEventThreatDetectionCustomModule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EffectiveEventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_task()`.
/// For the simplest API, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EffectiveEventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsEventThreatDetectionSettingsEffectiveCustomModulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Gets an effective Event Threat Detection custom module at the given level.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_builder()` + `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_execute()`.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsEventThreatDetectionSettingsEffectiveCustomModulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EffectiveEventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_builder(client, &args.name)?;
    securitycenter_projects_event_threat_detection_settings_effective_custom_modules_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/effectiveCustomModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_execute()` or `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsEventThreatDetectionSettingsEffectiveCustomModulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_builder()` + `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsEventThreatDetectionSettingsEffectiveCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_execute(
        builder,
    )
}

/// POST v1/projects/{projectsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_findings_bulk_mute_execute()` to send, or `securitycenter_projects_findings_bulk_mute` for simplest API.

pub fn securitycenter_projects_findings_bulk_mute_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/findings:bulkMute",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_findings_bulk_mute_execute()` or `securitycenter_projects_findings_bulk_mute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_findings_bulk_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_findings_bulk_mute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_findings_bulk_mute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_findings_bulk_mute_task()`.
/// For the simplest API, use `securitycenter_projects_findings_bulk_mute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_findings_bulk_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_findings_bulk_mute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_projects_findings_bulk_mute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_findings_bulk_mute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsFindingsBulkMuteArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_findings_bulk_mute_builder()` + `securitycenter_projects_findings_bulk_mute_execute()`.
/// For task-level control, use `securitycenter_projects_findings_bulk_mute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_findings_bulk_mute(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsFindingsBulkMuteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_findings_bulk_mute_builder(client, &args.parent)?;
    securitycenter_projects_findings_bulk_mute_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_locations_mute_configs_delete_execute()` to send, or `securitycenter_projects_locations_mute_configs_delete` for simplest API.

pub fn securitycenter_projects_locations_mute_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/locations/{locationsId}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_locations_mute_configs_delete_execute()` or `securitycenter_projects_locations_mute_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_locations_mute_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_locations_mute_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_locations_mute_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_locations_mute_configs_delete_task()`.
/// For the simplest API, use `securitycenter_projects_locations_mute_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_locations_mute_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_locations_mute_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_projects_locations_mute_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_locations_mute_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsLocationsMuteConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_locations_mute_configs_delete_builder()` + `securitycenter_projects_locations_mute_configs_delete_execute()`.
/// For task-level control, use `securitycenter_projects_locations_mute_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_locations_mute_configs_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsLocationsMuteConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_locations_mute_configs_delete_builder(client, &args.name)?;
    securitycenter_projects_locations_mute_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_locations_mute_configs_get_execute()` to send, or `securitycenter_projects_locations_mute_configs_get` for simplest API.

pub fn securitycenter_projects_locations_mute_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/locations/{locationsId}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_locations_mute_configs_get_execute()` or `securitycenter_projects_locations_mute_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_locations_mute_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_locations_mute_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_locations_mute_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_locations_mute_configs_get_task()`.
/// For the simplest API, use `securitycenter_projects_locations_mute_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_locations_mute_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_locations_mute_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_locations_mute_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_locations_mute_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsLocationsMuteConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_locations_mute_configs_get_builder()` + `securitycenter_projects_locations_mute_configs_get_execute()`.
/// For task-level control, use `securitycenter_projects_locations_mute_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_locations_mute_configs_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsLocationsMuteConfigsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_locations_mute_configs_get_builder(client, &args.name)?;
    securitycenter_projects_locations_mute_configs_get_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_locations_mute_configs_patch_execute()` to send, or `securitycenter_projects_locations_mute_configs_patch` for simplest API.

pub fn securitycenter_projects_locations_mute_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/locations/{locationsId}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_locations_mute_configs_patch_execute()` or `securitycenter_projects_locations_mute_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_locations_mute_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_locations_mute_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_locations_mute_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_locations_mute_configs_patch_task()`.
/// For the simplest API, use `securitycenter_projects_locations_mute_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_locations_mute_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_locations_mute_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_locations_mute_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_locations_mute_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsLocationsMuteConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_locations_mute_configs_patch_builder()` + `securitycenter_projects_locations_mute_configs_patch_execute()`.
/// For task-level control, use `securitycenter_projects_locations_mute_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_locations_mute_configs_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsLocationsMuteConfigsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_locations_mute_configs_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_projects_locations_mute_configs_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/muteConfigs
/// Creates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_mute_configs_create_execute()` to send, or `securitycenter_projects_mute_configs_create` for simplest API.

pub fn securitycenter_projects_mute_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    muteConfigId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/muteConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = muteConfigId.as_ref() {
        query_parts.push(format!("muteConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/muteConfigs
/// Creates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_mute_configs_create_execute()` or `securitycenter_projects_mute_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_mute_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/muteConfigs
/// Creates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_mute_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_mute_configs_create_task()`.
/// For the simplest API, use `securitycenter_projects_mute_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_mute_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_mute_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_mute_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_mute_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsMuteConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: muteConfigId
    pub muteConfigId: Option<String>,
}

/// POST v1/projects/{projectsId}/muteConfigs
/// Creates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_mute_configs_create_builder()` + `securitycenter_projects_mute_configs_create_execute()`.
/// For task-level control, use `securitycenter_projects_mute_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsMuteConfigsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_mute_configs_create_builder(
        client,
        &args.parent,
        &args.muteConfigId,
    )?;
    securitycenter_projects_mute_configs_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_mute_configs_delete_execute()` to send, or `securitycenter_projects_mute_configs_delete` for simplest API.

pub fn securitycenter_projects_mute_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_mute_configs_delete_execute()` or `securitycenter_projects_mute_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_mute_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_mute_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_mute_configs_delete_task()`.
/// For the simplest API, use `securitycenter_projects_mute_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_mute_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_mute_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_projects_mute_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_mute_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsMuteConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Deletes an existing mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_mute_configs_delete_builder()` + `securitycenter_projects_mute_configs_delete_execute()`.
/// For task-level control, use `securitycenter_projects_mute_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsMuteConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_mute_configs_delete_builder(client, &args.name)?;
    securitycenter_projects_mute_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_mute_configs_get_execute()` to send, or `securitycenter_projects_mute_configs_get` for simplest API.

pub fn securitycenter_projects_mute_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_mute_configs_get_execute()` or `securitycenter_projects_mute_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_mute_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_mute_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_mute_configs_get_task()`.
/// For the simplest API, use `securitycenter_projects_mute_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_mute_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_mute_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_mute_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_mute_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsMuteConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Gets a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_mute_configs_get_builder()` + `securitycenter_projects_mute_configs_get_execute()`.
/// For task-level control, use `securitycenter_projects_mute_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsMuteConfigsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_mute_configs_get_builder(client, &args.name)?;
    securitycenter_projects_mute_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/muteConfigs
/// Lists mute configs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_mute_configs_list_execute()` to send, or `securitycenter_projects_mute_configs_list` for simplest API.

pub fn securitycenter_projects_mute_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/muteConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/muteConfigs
/// Lists mute configs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_mute_configs_list_execute()` or `securitycenter_projects_mute_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_mute_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMuteConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMuteConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/muteConfigs
/// Lists mute configs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_mute_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_mute_configs_list_task()`.
/// For the simplest API, use `securitycenter_projects_mute_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_mute_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_mute_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMuteConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_mute_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_mute_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsMuteConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/muteConfigs
/// Lists mute configs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_mute_configs_list_builder()` + `securitycenter_projects_mute_configs_list_execute()`.
/// For task-level control, use `securitycenter_projects_mute_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsMuteConfigsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMuteConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_mute_configs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    securitycenter_projects_mute_configs_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_mute_configs_patch_execute()` to send, or `securitycenter_projects_mute_configs_patch` for simplest API.

pub fn securitycenter_projects_mute_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/muteConfigs/{muteConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_mute_configs_patch_execute()` or `securitycenter_projects_mute_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_mute_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_mute_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_mute_configs_patch_task()`.
/// For the simplest API, use `securitycenter_projects_mute_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_mute_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_mute_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_mute_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_mute_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsMuteConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/muteConfigs/{muteConfigsId}
/// Updates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_mute_configs_patch_builder()` + `securitycenter_projects_mute_configs_patch_execute()`.
/// For task-level control, use `securitycenter_projects_mute_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsMuteConfigsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_mute_configs_patch_builder(client, &args.name, &args.updateMask)?;
    securitycenter_projects_mute_configs_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/notificationConfigs
/// Creates a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_notification_configs_create_execute()` to send, or `securitycenter_projects_notification_configs_create` for simplest API.

pub fn securitycenter_projects_notification_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    configId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/notificationConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = configId.as_ref() {
        query_parts.push(format!("configId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/notificationConfigs
/// Creates a notification config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_notification_configs_create_execute()` or `securitycenter_projects_notification_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_notification_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/notificationConfigs
/// Creates a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_notification_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_notification_configs_create_task()`.
/// For the simplest API, use `securitycenter_projects_notification_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_notification_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_notification_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_notification_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_notification_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsNotificationConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: configId
    pub configId: Option<String>,
}

/// POST v1/projects/{projectsId}/notificationConfigs
/// Creates a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_notification_configs_create_builder()` + `securitycenter_projects_notification_configs_create_execute()`.
/// For task-level control, use `securitycenter_projects_notification_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsNotificationConfigsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_notification_configs_create_builder(
        client,
        &args.parent,
        &args.configId,
    )?;
    securitycenter_projects_notification_configs_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_notification_configs_delete_execute()` to send, or `securitycenter_projects_notification_configs_delete` for simplest API.

pub fn securitycenter_projects_notification_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/notificationConfigs/{notificationConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_notification_configs_delete_execute()` or `securitycenter_projects_notification_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_notification_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_notification_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_notification_configs_delete_task()`.
/// For the simplest API, use `securitycenter_projects_notification_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_notification_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_notification_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_projects_notification_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_notification_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsNotificationConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Deletes a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_notification_configs_delete_builder()` + `securitycenter_projects_notification_configs_delete_execute()`.
/// For task-level control, use `securitycenter_projects_notification_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsNotificationConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_notification_configs_delete_builder(client, &args.name)?;
    securitycenter_projects_notification_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_notification_configs_get_execute()` to send, or `securitycenter_projects_notification_configs_get` for simplest API.

pub fn securitycenter_projects_notification_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/notificationConfigs/{notificationConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_notification_configs_get_execute()` or `securitycenter_projects_notification_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_notification_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_notification_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_notification_configs_get_task()`.
/// For the simplest API, use `securitycenter_projects_notification_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_notification_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_notification_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_notification_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_notification_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsNotificationConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Gets a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_notification_configs_get_builder()` + `securitycenter_projects_notification_configs_get_execute()`.
/// For task-level control, use `securitycenter_projects_notification_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsNotificationConfigsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_notification_configs_get_builder(client, &args.name)?;
    securitycenter_projects_notification_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/notificationConfigs
/// Lists notification configs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_notification_configs_list_execute()` to send, or `securitycenter_projects_notification_configs_list` for simplest API.

pub fn securitycenter_projects_notification_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/notificationConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/notificationConfigs
/// Lists notification configs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_notification_configs_list_execute()` or `securitycenter_projects_notification_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_notification_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListNotificationConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNotificationConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/notificationConfigs
/// Lists notification configs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_notification_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_notification_configs_list_task()`.
/// For the simplest API, use `securitycenter_projects_notification_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_notification_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_notification_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_notification_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_notification_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsNotificationConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/notificationConfigs
/// Lists notification configs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_notification_configs_list_builder()` + `securitycenter_projects_notification_configs_list_execute()`.
/// For task-level control, use `securitycenter_projects_notification_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsNotificationConfigsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_notification_configs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    securitycenter_projects_notification_configs_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_notification_configs_patch_execute()` to send, or `securitycenter_projects_notification_configs_patch` for simplest API.

pub fn securitycenter_projects_notification_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/notificationConfigs/{notificationConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_notification_configs_patch_execute()` or `securitycenter_projects_notification_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_notification_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_notification_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_notification_configs_patch_task()`.
/// For the simplest API, use `securitycenter_projects_notification_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_notification_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_notification_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_notification_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_notification_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsNotificationConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/notificationConfigs/{notificationConfigsId}
/// Updates a notification config. The following update fields are allowed: description, pubsub_topic, streaming_config.filter
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_notification_configs_patch_builder()` + `securitycenter_projects_notification_configs_patch_execute()`.
/// For task-level control, use `securitycenter_projects_notification_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsNotificationConfigsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_notification_configs_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_projects_notification_configs_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_custom_modules_create_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_custom_modules_create` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/customModules",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_security_health_analytics_settings_custom_modules_create_execute()` or `securitycenter_projects_security_health_analytics_settings_custom_modules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_create_task()`.
/// For the simplest API, use `securitycenter_projects_security_health_analytics_settings_custom_modules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_projects_security_health_analytics_settings_custom_modules_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_security_health_analytics_settings_custom_modules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_create_builder()` + `securitycenter_projects_security_health_analytics_settings_custom_modules_create_execute()`.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_security_health_analytics_settings_custom_modules_create_builder(
            client,
            &args.parent,
        )?;
    securitycenter_projects_security_health_analytics_settings_custom_modules_create_execute(
        builder,
    )
}

/// DELETE v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_custom_modules_delete_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_custom_modules_delete` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_security_health_analytics_settings_custom_modules_delete_execute()` or `securitycenter_projects_security_health_analytics_settings_custom_modules_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_delete_task()`.
/// For the simplest API, use `securitycenter_projects_security_health_analytics_settings_custom_modules_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        securitycenter_projects_security_health_analytics_settings_custom_modules_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_security_health_analytics_settings_custom_modules_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Deletes the specified SecurityHealthAnalyticsCustomModule and all of its descendants in the CRM hierarchy. This method is only supported for resident custom modules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_delete_builder()` + `securitycenter_projects_security_health_analytics_settings_custom_modules_delete_execute()`.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_delete(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_security_health_analytics_settings_custom_modules_delete_builder(
            client, &args.name,
        )?;
    securitycenter_projects_security_health_analytics_settings_custom_modules_delete_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_custom_modules_get_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_custom_modules_get` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_security_health_analytics_settings_custom_modules_get_execute()` or `securitycenter_projects_security_health_analytics_settings_custom_modules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_get_task()`.
/// For the simplest API, use `securitycenter_projects_security_health_analytics_settings_custom_modules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_security_health_analytics_settings_custom_modules_get_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_security_health_analytics_settings_custom_modules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Retrieves a SecurityHealthAnalyticsCustomModule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_get_builder()` + `securitycenter_projects_security_health_analytics_settings_custom_modules_get_execute()`.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_security_health_analytics_settings_custom_modules_get_builder(
            client, &args.name,
        )?;
    securitycenter_projects_security_health_analytics_settings_custom_modules_get_execute(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_custom_modules_list` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/customModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_execute()` or `securitycenter_projects_security_health_analytics_settings_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSecurityHealthAnalyticsCustomModulesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecurityHealthAnalyticsCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_security_health_analytics_settings_custom_modules_list_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_security_health_analytics_settings_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_builder()` + `securitycenter_projects_security_health_analytics_settings_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecurityHealthAnalyticsCustomModulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_security_health_analytics_settings_custom_modules_list_builder(
            client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )?;
    securitycenter_projects_security_health_analytics_settings_custom_modules_list_execute(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/customModules:listDescendant",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_execute()` or `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_task()`.
/// For the simplest API, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_builder()` + `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_execute()`.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_execute(builder)
}

/// PATCH v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_custom_modules_patch_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_custom_modules_patch` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/customModules/{customModulesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_security_health_analytics_settings_custom_modules_patch_execute()` or `securitycenter_projects_security_health_analytics_settings_custom_modules_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_patch_task()`.
/// For the simplest API, use `securitycenter_projects_security_health_analytics_settings_custom_modules_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_projects_security_health_analytics_settings_custom_modules_patch_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_security_health_analytics_settings_custom_modules_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules/{customModulesId}
/// Updates the SecurityHealthAnalyticsCustomModule under the given name based on the given update mask. Updating the enablement state is supported on both resident and inherited modules (though resident modules cannot have an enablement state of "inherited"). Updating the display name and custom config of a module is supported on resident modules only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_patch_builder()` + `securitycenter_projects_security_health_analytics_settings_custom_modules_patch_execute()`.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_security_health_analytics_settings_custom_modules_patch_builder(
            client,
            &args.name,
            &args.updateMask,
        )?;
    securitycenter_projects_security_health_analytics_settings_custom_modules_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/customModules:simulate",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_execute()` or `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SimulateSecurityHealthAnalyticsCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_task()`.
/// For the simplest API, use `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_security_health_analytics_settings_custom_modules_simulate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_builder()` + `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_execute()`.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_simulate(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_builder(
            client,
            &args.parent,
        )?;
    securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_execute()` or `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule,
                >,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_task()`.
/// For the simplest API, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSecurityHealthAnalyticsSettingsEffectiveCustomModulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules/{effectiveCustomModulesId}
/// Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_builder()` + `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_execute()`.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsEffectiveCustomModulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_builder(client, &args.name)?;
    securitycenter_projects_security_health_analytics_settings_effective_custom_modules_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/effectiveCustomModules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_execute()` or `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_builder()` + `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/sources
/// Lists all sources belonging to an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_sources_list_execute()` to send, or `securitycenter_projects_sources_list` for simplest API.

pub fn securitycenter_projects_sources_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/sources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/sources
/// Lists all sources belonging to an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_sources_list_execute()` or `securitycenter_projects_sources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/sources
/// Lists all sources belonging to an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_sources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_sources_list_task()`.
/// For the simplest API, use `securitycenter_projects_sources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_sources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_sources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSourcesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/sources
/// Lists all sources belonging to an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_sources_list_builder()` + `securitycenter_projects_sources_list_execute()`.
/// For task-level control, use `securitycenter_projects_sources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSourcesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_sources_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    securitycenter_projects_sources_list_execute(builder)
}

/// POST v1/projects/{projectsId}/sources/{sourcesId}/findings:group
/// Filters an organization or source''s findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_sources_findings_group_execute()` to send, or `securitycenter_projects_sources_findings_group` for simplest API.

pub fn securitycenter_projects_sources_findings_group_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/sources/{sourcesId}/findings:group",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/sources/{sourcesId}/findings:group
/// Filters an organization or source''s findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_sources_findings_group_execute()` or `securitycenter_projects_sources_findings_group`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_findings_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_group_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GroupFindingsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupFindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/sources/{sourcesId}/findings:group
/// Filters an organization or source''s findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_sources_findings_group_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_sources_findings_group_task()`.
/// For the simplest API, use `securitycenter_projects_sources_findings_group()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_findings_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_sources_findings_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_sources_findings_group_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_sources_findings_group`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSourcesFindingsGroupArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/sources/{sourcesId}/findings:group
/// Filters an organization or source''s findings and groups them by their specified properties. To group across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`, /v1/`folders/{folder_id}/sources/-/findings`, /v1/`projects/{project_id}/sources/-/findings`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_sources_findings_group_builder()` + `securitycenter_projects_sources_findings_group_execute()`.
/// For task-level control, use `securitycenter_projects_sources_findings_group_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_group(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSourcesFindingsGroupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_sources_findings_group_builder(client, &args.parent)?;
    securitycenter_projects_sources_findings_group_execute(builder)
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings
/// Lists an organization or source''s findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_sources_findings_list_execute()` to send, or `securitycenter_projects_sources_findings_list` for simplest API.

pub fn securitycenter_projects_sources_findings_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    compareDuration: &Option<String>,
    fieldMask: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    readTime: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/sources/{sourcesId}/findings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = compareDuration.as_ref() {
        query_parts.push(format!("compareDuration={}", val));
    }
    if let Some(val) = fieldMask.as_ref() {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime.as_ref() {
        query_parts.push(format!("readTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings
/// Lists an organization or source''s findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_sources_findings_list_execute()` or `securitycenter_projects_sources_findings_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_findings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFindingsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings
/// Lists an organization or source''s findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_sources_findings_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_sources_findings_list_task()`.
/// For the simplest API, use `securitycenter_projects_sources_findings_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_findings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_sources_findings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_sources_findings_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_sources_findings_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSourcesFindingsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: compareDuration
    pub compareDuration: Option<String>,
    /// Query parameter: fieldMask
    pub fieldMask: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readTime
    pub readTime: Option<String>,
}

/// GET v1/projects/{projectsId}/sources/{sourcesId}/findings
/// Lists an organization or source''s findings. To list across all sources provide a - as the source id. Example: /v1/`organizations/{organization_id}/sources/-/findings`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_sources_findings_list_builder()` + `securitycenter_projects_sources_findings_list_execute()`.
/// For task-level control, use `securitycenter_projects_sources_findings_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSourcesFindingsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_sources_findings_list_builder(
        client,
        &args.parent,
        &args.compareDuration,
        &args.fieldMask,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.readTime,
    )?;
    securitycenter_projects_sources_findings_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_sources_findings_patch_execute()` to send, or `securitycenter_projects_sources_findings_patch` for simplest API.

pub fn securitycenter_projects_sources_findings_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/sources/{sourcesId}/findings/{findingsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_sources_findings_patch_execute()` or `securitycenter_projects_sources_findings_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_findings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Finding>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_sources_findings_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_sources_findings_patch_task()`.
/// For the simplest API, use `securitycenter_projects_sources_findings_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_findings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_sources_findings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_projects_sources_findings_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_sources_findings_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSourcesFindingsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}
/// Creates or updates a finding. The corresponding source must exist for a finding creation to succeed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_sources_findings_patch_builder()` + `securitycenter_projects_sources_findings_patch_execute()`.
/// For task-level control, use `securitycenter_projects_sources_findings_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSourcesFindingsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_sources_findings_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_projects_sources_findings_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_sources_findings_set_mute_execute()` to send, or `securitycenter_projects_sources_findings_set_mute` for simplest API.

pub fn securitycenter_projects_sources_findings_set_mute_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/sources/{sourcesId}/findings/{findingsId}:setMute",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_sources_findings_set_mute_execute()` or `securitycenter_projects_sources_findings_set_mute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_findings_set_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_set_mute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Finding>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_sources_findings_set_mute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_sources_findings_set_mute_task()`.
/// For the simplest API, use `securitycenter_projects_sources_findings_set_mute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_findings_set_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_sources_findings_set_mute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_projects_sources_findings_set_mute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_sources_findings_set_mute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSourcesFindingsSetMuteArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}:setMute
/// Updates the mute state of a finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_sources_findings_set_mute_builder()` + `securitycenter_projects_sources_findings_set_mute_execute()`.
/// For task-level control, use `securitycenter_projects_sources_findings_set_mute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_set_mute(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSourcesFindingsSetMuteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_sources_findings_set_mute_builder(client, &args.name)?;
    securitycenter_projects_sources_findings_set_mute_execute(builder)
}

/// POST v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_sources_findings_set_state_execute()` to send, or `securitycenter_projects_sources_findings_set_state` for simplest API.

pub fn securitycenter_projects_sources_findings_set_state_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/sources/{sourcesId}/findings/{findingsId}:setState",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_sources_findings_set_state_execute()` or `securitycenter_projects_sources_findings_set_state`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_findings_set_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_set_state_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Finding>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_sources_findings_set_state_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_sources_findings_set_state_task()`.
/// For the simplest API, use `securitycenter_projects_sources_findings_set_state()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_findings_set_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_sources_findings_set_state_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_projects_sources_findings_set_state_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_sources_findings_set_state`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSourcesFindingsSetStateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}:setState
/// Updates the state of a finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_sources_findings_set_state_builder()` + `securitycenter_projects_sources_findings_set_state_execute()`.
/// For task-level control, use `securitycenter_projects_sources_findings_set_state_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_set_state(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSourcesFindingsSetStateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_sources_findings_set_state_builder(client, &args.name)?;
    securitycenter_projects_sources_findings_set_state_execute(builder)
}

/// PATCH v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_sources_findings_update_security_marks_execute()` to send, or `securitycenter_projects_sources_findings_update_security_marks` for simplest API.

pub fn securitycenter_projects_sources_findings_update_security_marks_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    startTime: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/sources/{sourcesId}/findings/{findingsId}/securityMarks",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = startTime.as_ref() {
        query_parts.push(format!("startTime={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_sources_findings_update_security_marks_execute()` or `securitycenter_projects_sources_findings_update_security_marks`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_findings_update_security_marks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_update_security_marks_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityMarks>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityMarks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_sources_findings_update_security_marks_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_sources_findings_update_security_marks_task()`.
/// For the simplest API, use `securitycenter_projects_sources_findings_update_security_marks()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_findings_update_security_marks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_sources_findings_update_security_marks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_sources_findings_update_security_marks_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_sources_findings_update_security_marks`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSourcesFindingsUpdateSecurityMarksArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: startTime
    pub startTime: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}/securityMarks
/// Updates security marks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_sources_findings_update_security_marks_builder()` + `securitycenter_projects_sources_findings_update_security_marks_execute()`.
/// For task-level control, use `securitycenter_projects_sources_findings_update_security_marks_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_update_security_marks(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSourcesFindingsUpdateSecurityMarksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityMarks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_sources_findings_update_security_marks_builder(
        client,
        &args.name,
        &args.startTime,
        &args.updateMask,
    )?;
    securitycenter_projects_sources_findings_update_security_marks_execute(builder)
}

/// PATCH v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_sources_findings_external_systems_patch_execute()` to send, or `securitycenter_projects_sources_findings_external_systems_patch` for simplest API.

pub fn securitycenter_projects_sources_findings_external_systems_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_sources_findings_external_systems_patch_execute()` or `securitycenter_projects_sources_findings_external_systems_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_findings_external_systems_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_external_systems_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1ExternalSystem>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1ExternalSystem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_sources_findings_external_systems_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_sources_findings_external_systems_patch_task()`.
/// For the simplest API, use `securitycenter_projects_sources_findings_external_systems_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_findings_external_systems_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_sources_findings_external_systems_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ExternalSystem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_sources_findings_external_systems_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_sources_findings_external_systems_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSourcesFindingsExternalSystemsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/sources/{sourcesId}/findings/{findingsId}/externalSystems/{externalSystemsId}
/// Updates external system. This is for a given finding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_sources_findings_external_systems_patch_builder()` + `securitycenter_projects_sources_findings_external_systems_patch_execute()`.
/// For task-level control, use `securitycenter_projects_sources_findings_external_systems_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_findings_external_systems_patch(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSourcesFindingsExternalSystemsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1ExternalSystem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_sources_findings_external_systems_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    securitycenter_projects_sources_findings_external_systems_patch_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GroupAssetsResponse
// =============================================================================

/// ResourceIdentifier implementation for GroupAssetsResponse with SecuritycenterFoldersAssetsGroupArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersAssetsGroupArgs> for GroupAssetsResponse {
    fn generate_resource_id(&self, input: &SecuritycenterFoldersAssetsGroupArgs) -> String {
        format!("gcp::securitycenter::GroupAssetsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GroupAssetsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAssetsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAssetsResponse with SecuritycenterFoldersAssetsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersAssetsListArgs> for ListAssetsResponse {
    fn generate_resource_id(&self, input: &SecuritycenterFoldersAssetsListArgs) -> String {
        format!("gcp::securitycenter::ListAssetsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListAssetsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityMarks
// =============================================================================

/// ResourceIdentifier implementation for SecurityMarks with SecuritycenterFoldersAssetsUpdateSecurityMarksArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersAssetsUpdateSecurityMarksArgs> for SecurityMarks {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersAssetsUpdateSecurityMarksArgs,
    ) -> String {
        format!("gcp::securitycenter::SecurityMarks/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::SecurityMarks"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport with SecuritycenterFoldersBigQueryExportsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersBigQueryExportsCreateArgs>
    for GoogleCloudSecuritycenterV1BigQueryExport
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersBigQueryExportsCreateArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterFoldersBigQueryExportsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersBigQueryExportsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersBigQueryExportsDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport with SecuritycenterFoldersBigQueryExportsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersBigQueryExportsGetArgs>
    for GoogleCloudSecuritycenterV1BigQueryExport
{
    fn generate_resource_id(&self, input: &SecuritycenterFoldersBigQueryExportsGetArgs) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListBigQueryExportsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListBigQueryExportsResponse with SecuritycenterFoldersBigQueryExportsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersBigQueryExportsListArgs>
    for ListBigQueryExportsResponse
{
    fn generate_resource_id(&self, input: &SecuritycenterFoldersBigQueryExportsListArgs) -> String {
        format!(
            "gcp::securitycenter::ListBigQueryExportsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListBigQueryExportsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport with SecuritycenterFoldersBigQueryExportsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersBigQueryExportsPatchArgs>
    for GoogleCloudSecuritycenterV1BigQueryExport
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersBigQueryExportsPatchArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ValidateEventThreatDetectionCustomModuleResponse
// =============================================================================

/// ResourceIdentifier implementation for ValidateEventThreatDetectionCustomModuleResponse with SecuritycenterFoldersEventThreatDetectionSettingsValidateCustomModuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersEventThreatDetectionSettingsValidateCustomModuleArgs>
    for ValidateEventThreatDetectionCustomModuleResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersEventThreatDetectionSettingsValidateCustomModuleArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ValidateEventThreatDetectionCustomModuleResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ValidateEventThreatDetectionCustomModuleResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EventThreatDetectionCustomModule
// =============================================================================

/// ResourceIdentifier implementation for EventThreatDetectionCustomModule with SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesCreateArgs>
    for EventThreatDetectionCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesCreateArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::EventThreatDetectionCustomModule/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::EventThreatDetectionCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EventThreatDetectionCustomModule
// =============================================================================

/// ResourceIdentifier implementation for EventThreatDetectionCustomModule with SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesGetArgs>
    for EventThreatDetectionCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesGetArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::EventThreatDetectionCustomModule/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::EventThreatDetectionCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListEventThreatDetectionCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListEventThreatDetectionCustomModulesResponse with SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesListArgs>
    for ListEventThreatDetectionCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListEventThreatDetectionCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListEventThreatDetectionCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDescendantEventThreatDetectionCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDescendantEventThreatDetectionCustomModulesResponse with SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesListDescendantArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesListDescendantArgs,
    > for ListDescendantEventThreatDetectionCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesListDescendantArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListDescendantEventThreatDetectionCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListDescendantEventThreatDetectionCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EventThreatDetectionCustomModule
// =============================================================================

/// ResourceIdentifier implementation for EventThreatDetectionCustomModule with SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesPatchArgs>
    for EventThreatDetectionCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesPatchArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::EventThreatDetectionCustomModule/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::EventThreatDetectionCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EffectiveEventThreatDetectionCustomModule
// =============================================================================

/// ResourceIdentifier implementation for EffectiveEventThreatDetectionCustomModule with SecuritycenterFoldersEventThreatDetectionSettingsEffectiveCustomModulesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterFoldersEventThreatDetectionSettingsEffectiveCustomModulesGetArgs,
    > for EffectiveEventThreatDetectionCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersEventThreatDetectionSettingsEffectiveCustomModulesGetArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::EffectiveEventThreatDetectionCustomModule/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::EffectiveEventThreatDetectionCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListEffectiveEventThreatDetectionCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListEffectiveEventThreatDetectionCustomModulesResponse with SecuritycenterFoldersEventThreatDetectionSettingsEffectiveCustomModulesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterFoldersEventThreatDetectionSettingsEffectiveCustomModulesListArgs,
    > for ListEffectiveEventThreatDetectionCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersEventThreatDetectionSettingsEffectiveCustomModulesListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListEffectiveEventThreatDetectionCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListEffectiveEventThreatDetectionCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SecuritycenterFoldersFindingsBulkMuteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersFindingsBulkMuteArgs> for Operation {
    fn generate_resource_id(&self, input: &SecuritycenterFoldersFindingsBulkMuteArgs) -> String {
        format!("gcp::securitycenter::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterFoldersLocationsMuteConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersLocationsMuteConfigsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersLocationsMuteConfigsDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig with SecuritycenterFoldersLocationsMuteConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersLocationsMuteConfigsGetArgs>
    for GoogleCloudSecuritycenterV1MuteConfig
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersLocationsMuteConfigsGetArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig with SecuritycenterFoldersLocationsMuteConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersLocationsMuteConfigsPatchArgs>
    for GoogleCloudSecuritycenterV1MuteConfig
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersLocationsMuteConfigsPatchArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig with SecuritycenterFoldersMuteConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersMuteConfigsCreateArgs>
    for GoogleCloudSecuritycenterV1MuteConfig
{
    fn generate_resource_id(&self, input: &SecuritycenterFoldersMuteConfigsCreateArgs) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterFoldersMuteConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersMuteConfigsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &SecuritycenterFoldersMuteConfigsDeleteArgs) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig with SecuritycenterFoldersMuteConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersMuteConfigsGetArgs>
    for GoogleCloudSecuritycenterV1MuteConfig
{
    fn generate_resource_id(&self, input: &SecuritycenterFoldersMuteConfigsGetArgs) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMuteConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMuteConfigsResponse with SecuritycenterFoldersMuteConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersMuteConfigsListArgs> for ListMuteConfigsResponse {
    fn generate_resource_id(&self, input: &SecuritycenterFoldersMuteConfigsListArgs) -> String {
        format!(
            "gcp::securitycenter::ListMuteConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListMuteConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig with SecuritycenterFoldersMuteConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersMuteConfigsPatchArgs>
    for GoogleCloudSecuritycenterV1MuteConfig
{
    fn generate_resource_id(&self, input: &SecuritycenterFoldersMuteConfigsPatchArgs) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationConfig
// =============================================================================

/// ResourceIdentifier implementation for NotificationConfig with SecuritycenterFoldersNotificationConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersNotificationConfigsCreateArgs> for NotificationConfig {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersNotificationConfigsCreateArgs,
    ) -> String {
        format!("gcp::securitycenter::NotificationConfig/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::NotificationConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterFoldersNotificationConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersNotificationConfigsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersNotificationConfigsDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationConfig
// =============================================================================

/// ResourceIdentifier implementation for NotificationConfig with SecuritycenterFoldersNotificationConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersNotificationConfigsGetArgs> for NotificationConfig {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersNotificationConfigsGetArgs,
    ) -> String {
        format!("gcp::securitycenter::NotificationConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::NotificationConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListNotificationConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListNotificationConfigsResponse with SecuritycenterFoldersNotificationConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersNotificationConfigsListArgs>
    for ListNotificationConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersNotificationConfigsListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListNotificationConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListNotificationConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationConfig
// =============================================================================

/// ResourceIdentifier implementation for NotificationConfig with SecuritycenterFoldersNotificationConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersNotificationConfigsPatchArgs> for NotificationConfig {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersNotificationConfigsPatchArgs,
    ) -> String {
        format!("gcp::securitycenter::NotificationConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::NotificationConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule with SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesCreateArgs>
    for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesCreateArgs,
    ) -> String {
        format!("gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule with SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesGetArgs>
    for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesGetArgs,
    ) -> String {
        format!("gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSecurityHealthAnalyticsCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSecurityHealthAnalyticsCustomModulesResponse with SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesListArgs>
    for ListSecurityHealthAnalyticsCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListSecurityHealthAnalyticsCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListSecurityHealthAnalyticsCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDescendantSecurityHealthAnalyticsCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDescendantSecurityHealthAnalyticsCustomModulesResponse with SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs,
    > for ListDescendantSecurityHealthAnalyticsCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListDescendantSecurityHealthAnalyticsCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListDescendantSecurityHealthAnalyticsCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule with SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesPatchArgs>
    for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesPatchArgs,
    ) -> String {
        format!("gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SimulateSecurityHealthAnalyticsCustomModuleResponse
// =============================================================================

/// ResourceIdentifier implementation for SimulateSecurityHealthAnalyticsCustomModuleResponse with SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs,
    > for SimulateSecurityHealthAnalyticsCustomModuleResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::SimulateSecurityHealthAnalyticsCustomModuleResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::SimulateSecurityHealthAnalyticsCustomModuleResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule with SecuritycenterFoldersSecurityHealthAnalyticsSettingsEffectiveCustomModulesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterFoldersSecurityHealthAnalyticsSettingsEffectiveCustomModulesGetArgs,
    > for GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsEffectiveCustomModulesGetArgs,
    ) -> String {
        format!("gcp::securitycenter::GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListEffectiveSecurityHealthAnalyticsCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListEffectiveSecurityHealthAnalyticsCustomModulesResponse with SecuritycenterFoldersSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterFoldersSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs,
    > for ListEffectiveSecurityHealthAnalyticsCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListEffectiveSecurityHealthAnalyticsCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListEffectiveSecurityHealthAnalyticsCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSourcesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSourcesResponse with SecuritycenterFoldersSourcesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersSourcesListArgs> for ListSourcesResponse {
    fn generate_resource_id(&self, input: &SecuritycenterFoldersSourcesListArgs) -> String {
        format!("gcp::securitycenter::ListSourcesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListSourcesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GroupFindingsResponse
// =============================================================================

/// ResourceIdentifier implementation for GroupFindingsResponse with SecuritycenterFoldersSourcesFindingsGroupArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersSourcesFindingsGroupArgs> for GroupFindingsResponse {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersSourcesFindingsGroupArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GroupFindingsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GroupFindingsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListFindingsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListFindingsResponse with SecuritycenterFoldersSourcesFindingsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersSourcesFindingsListArgs> for ListFindingsResponse {
    fn generate_resource_id(&self, input: &SecuritycenterFoldersSourcesFindingsListArgs) -> String {
        format!("gcp::securitycenter::ListFindingsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListFindingsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Finding
// =============================================================================

/// ResourceIdentifier implementation for Finding with SecuritycenterFoldersSourcesFindingsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersSourcesFindingsPatchArgs> for Finding {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersSourcesFindingsPatchArgs,
    ) -> String {
        format!("gcp::securitycenter::Finding/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Finding"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Finding
// =============================================================================

/// ResourceIdentifier implementation for Finding with SecuritycenterFoldersSourcesFindingsSetMuteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersSourcesFindingsSetMuteArgs> for Finding {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersSourcesFindingsSetMuteArgs,
    ) -> String {
        format!("gcp::securitycenter::Finding/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Finding"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Finding
// =============================================================================

/// ResourceIdentifier implementation for Finding with SecuritycenterFoldersSourcesFindingsSetStateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersSourcesFindingsSetStateArgs> for Finding {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersSourcesFindingsSetStateArgs,
    ) -> String {
        format!("gcp::securitycenter::Finding/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Finding"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityMarks
// =============================================================================

/// ResourceIdentifier implementation for SecurityMarks with SecuritycenterFoldersSourcesFindingsUpdateSecurityMarksArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersSourcesFindingsUpdateSecurityMarksArgs>
    for SecurityMarks
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersSourcesFindingsUpdateSecurityMarksArgs,
    ) -> String {
        format!("gcp::securitycenter::SecurityMarks/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::SecurityMarks"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1ExternalSystem
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1ExternalSystem with SecuritycenterFoldersSourcesFindingsExternalSystemsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterFoldersSourcesFindingsExternalSystemsPatchArgs>
    for GoogleCloudSecuritycenterV1ExternalSystem
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterFoldersSourcesFindingsExternalSystemsPatchArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1ExternalSystem/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1ExternalSystem"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OrganizationSettings
// =============================================================================

/// ResourceIdentifier implementation for OrganizationSettings with SecuritycenterOrganizationsGetOrganizationSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsGetOrganizationSettingsArgs>
    for OrganizationSettings
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsGetOrganizationSettingsArgs,
    ) -> String {
        format!("gcp::securitycenter::OrganizationSettings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::OrganizationSettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OrganizationSettings
// =============================================================================

/// ResourceIdentifier implementation for OrganizationSettings with SecuritycenterOrganizationsUpdateOrganizationSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsUpdateOrganizationSettingsArgs>
    for OrganizationSettings
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsUpdateOrganizationSettingsArgs,
    ) -> String {
        format!("gcp::securitycenter::OrganizationSettings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::OrganizationSettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GroupAssetsResponse
// =============================================================================

/// ResourceIdentifier implementation for GroupAssetsResponse with SecuritycenterOrganizationsAssetsGroupArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsAssetsGroupArgs> for GroupAssetsResponse {
    fn generate_resource_id(&self, input: &SecuritycenterOrganizationsAssetsGroupArgs) -> String {
        format!("gcp::securitycenter::GroupAssetsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GroupAssetsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAssetsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAssetsResponse with SecuritycenterOrganizationsAssetsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsAssetsListArgs> for ListAssetsResponse {
    fn generate_resource_id(&self, input: &SecuritycenterOrganizationsAssetsListArgs) -> String {
        format!("gcp::securitycenter::ListAssetsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListAssetsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SecuritycenterOrganizationsAssetsRunDiscoveryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsAssetsRunDiscoveryArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsAssetsRunDiscoveryArgs,
    ) -> String {
        format!("gcp::securitycenter::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityMarks
// =============================================================================

/// ResourceIdentifier implementation for SecurityMarks with SecuritycenterOrganizationsAssetsUpdateSecurityMarksArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsAssetsUpdateSecurityMarksArgs>
    for SecurityMarks
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsAssetsUpdateSecurityMarksArgs,
    ) -> String {
        format!("gcp::securitycenter::SecurityMarks/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::SecurityMarks"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAttackPathsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAttackPathsResponse with SecuritycenterOrganizationsAttackPathsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsAttackPathsListArgs>
    for ListAttackPathsResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsAttackPathsListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListAttackPathsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListAttackPathsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport with SecuritycenterOrganizationsBigQueryExportsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsBigQueryExportsCreateArgs>
    for GoogleCloudSecuritycenterV1BigQueryExport
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsBigQueryExportsCreateArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterOrganizationsBigQueryExportsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsBigQueryExportsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsBigQueryExportsDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport with SecuritycenterOrganizationsBigQueryExportsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsBigQueryExportsGetArgs>
    for GoogleCloudSecuritycenterV1BigQueryExport
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsBigQueryExportsGetArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListBigQueryExportsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListBigQueryExportsResponse with SecuritycenterOrganizationsBigQueryExportsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsBigQueryExportsListArgs>
    for ListBigQueryExportsResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsBigQueryExportsListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListBigQueryExportsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListBigQueryExportsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport with SecuritycenterOrganizationsBigQueryExportsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsBigQueryExportsPatchArgs>
    for GoogleCloudSecuritycenterV1BigQueryExport
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsBigQueryExportsPatchArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ValidateEventThreatDetectionCustomModuleResponse
// =============================================================================

/// ResourceIdentifier implementation for ValidateEventThreatDetectionCustomModuleResponse with SecuritycenterOrganizationsEventThreatDetectionSettingsValidateCustomModuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsEventThreatDetectionSettingsValidateCustomModuleArgs,
    > for ValidateEventThreatDetectionCustomModuleResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsEventThreatDetectionSettingsValidateCustomModuleArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ValidateEventThreatDetectionCustomModuleResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ValidateEventThreatDetectionCustomModuleResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EventThreatDetectionCustomModule
// =============================================================================

/// ResourceIdentifier implementation for EventThreatDetectionCustomModule with SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesCreateArgs,
    > for EventThreatDetectionCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesCreateArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::EventThreatDetectionCustomModule/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::EventThreatDetectionCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesDeleteArgs,
    > for Empty
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EventThreatDetectionCustomModule
// =============================================================================

/// ResourceIdentifier implementation for EventThreatDetectionCustomModule with SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesGetArgs>
    for EventThreatDetectionCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesGetArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::EventThreatDetectionCustomModule/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::EventThreatDetectionCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListEventThreatDetectionCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListEventThreatDetectionCustomModulesResponse with SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesListArgs>
    for ListEventThreatDetectionCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListEventThreatDetectionCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListEventThreatDetectionCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDescendantEventThreatDetectionCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDescendantEventThreatDetectionCustomModulesResponse with SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesListDescendantArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesListDescendantArgs,
    > for ListDescendantEventThreatDetectionCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesListDescendantArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListDescendantEventThreatDetectionCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListDescendantEventThreatDetectionCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EventThreatDetectionCustomModule
// =============================================================================

/// ResourceIdentifier implementation for EventThreatDetectionCustomModule with SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesPatchArgs,
    > for EventThreatDetectionCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesPatchArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::EventThreatDetectionCustomModule/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::EventThreatDetectionCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EffectiveEventThreatDetectionCustomModule
// =============================================================================

/// ResourceIdentifier implementation for EffectiveEventThreatDetectionCustomModule with SecuritycenterOrganizationsEventThreatDetectionSettingsEffectiveCustomModulesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsEventThreatDetectionSettingsEffectiveCustomModulesGetArgs,
    > for EffectiveEventThreatDetectionCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsEventThreatDetectionSettingsEffectiveCustomModulesGetArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::EffectiveEventThreatDetectionCustomModule/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::EffectiveEventThreatDetectionCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListEffectiveEventThreatDetectionCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListEffectiveEventThreatDetectionCustomModulesResponse with SecuritycenterOrganizationsEventThreatDetectionSettingsEffectiveCustomModulesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsEventThreatDetectionSettingsEffectiveCustomModulesListArgs,
    > for ListEffectiveEventThreatDetectionCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsEventThreatDetectionSettingsEffectiveCustomModulesListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListEffectiveEventThreatDetectionCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListEffectiveEventThreatDetectionCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SecuritycenterOrganizationsFindingsBulkMuteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsFindingsBulkMuteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsFindingsBulkMuteArgs,
    ) -> String {
        format!("gcp::securitycenter::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterOrganizationsLocationsMuteConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsLocationsMuteConfigsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsLocationsMuteConfigsDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig with SecuritycenterOrganizationsLocationsMuteConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsLocationsMuteConfigsGetArgs>
    for GoogleCloudSecuritycenterV1MuteConfig
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsLocationsMuteConfigsGetArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig with SecuritycenterOrganizationsLocationsMuteConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsLocationsMuteConfigsPatchArgs>
    for GoogleCloudSecuritycenterV1MuteConfig
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsLocationsMuteConfigsPatchArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig with SecuritycenterOrganizationsMuteConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsMuteConfigsCreateArgs>
    for GoogleCloudSecuritycenterV1MuteConfig
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsMuteConfigsCreateArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterOrganizationsMuteConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsMuteConfigsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsMuteConfigsDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig with SecuritycenterOrganizationsMuteConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsMuteConfigsGetArgs>
    for GoogleCloudSecuritycenterV1MuteConfig
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsMuteConfigsGetArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMuteConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMuteConfigsResponse with SecuritycenterOrganizationsMuteConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsMuteConfigsListArgs>
    for ListMuteConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsMuteConfigsListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListMuteConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListMuteConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig with SecuritycenterOrganizationsMuteConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsMuteConfigsPatchArgs>
    for GoogleCloudSecuritycenterV1MuteConfig
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsMuteConfigsPatchArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationConfig
// =============================================================================

/// ResourceIdentifier implementation for NotificationConfig with SecuritycenterOrganizationsNotificationConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsNotificationConfigsCreateArgs>
    for NotificationConfig
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsNotificationConfigsCreateArgs,
    ) -> String {
        format!("gcp::securitycenter::NotificationConfig/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::NotificationConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterOrganizationsNotificationConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsNotificationConfigsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsNotificationConfigsDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationConfig
// =============================================================================

/// ResourceIdentifier implementation for NotificationConfig with SecuritycenterOrganizationsNotificationConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsNotificationConfigsGetArgs>
    for NotificationConfig
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsNotificationConfigsGetArgs,
    ) -> String {
        format!("gcp::securitycenter::NotificationConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::NotificationConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListNotificationConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListNotificationConfigsResponse with SecuritycenterOrganizationsNotificationConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsNotificationConfigsListArgs>
    for ListNotificationConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsNotificationConfigsListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListNotificationConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListNotificationConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationConfig
// =============================================================================

/// ResourceIdentifier implementation for NotificationConfig with SecuritycenterOrganizationsNotificationConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsNotificationConfigsPatchArgs>
    for NotificationConfig
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsNotificationConfigsPatchArgs,
    ) -> String {
        format!("gcp::securitycenter::NotificationConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::NotificationConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterOrganizationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterOrganizationsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsOperationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsOperationsDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SecuritycenterOrganizationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &SecuritycenterOrganizationsOperationsGetArgs) -> String {
        format!("gcp::securitycenter::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with SecuritycenterOrganizationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsOperationsListArgs> for ListOperationsResponse {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsOperationsListArgs,
    ) -> String {
        format!("gcp::securitycenter::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BatchCreateResourceValueConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for BatchCreateResourceValueConfigsResponse with SecuritycenterOrganizationsResourceValueConfigsBatchCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsResourceValueConfigsBatchCreateArgs>
    for BatchCreateResourceValueConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsResourceValueConfigsBatchCreateArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::BatchCreateResourceValueConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::BatchCreateResourceValueConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterOrganizationsResourceValueConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsResourceValueConfigsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsResourceValueConfigsDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1ResourceValueConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1ResourceValueConfig with SecuritycenterOrganizationsResourceValueConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsResourceValueConfigsGetArgs>
    for GoogleCloudSecuritycenterV1ResourceValueConfig
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsResourceValueConfigsGetArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1ResourceValueConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1ResourceValueConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListResourceValueConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListResourceValueConfigsResponse with SecuritycenterOrganizationsResourceValueConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsResourceValueConfigsListArgs>
    for ListResourceValueConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsResourceValueConfigsListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListResourceValueConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListResourceValueConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1ResourceValueConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1ResourceValueConfig with SecuritycenterOrganizationsResourceValueConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsResourceValueConfigsPatchArgs>
    for GoogleCloudSecuritycenterV1ResourceValueConfig
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsResourceValueConfigsPatchArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1ResourceValueConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1ResourceValueConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule with SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesCreateArgs,
    > for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesCreateArgs,
    ) -> String {
        format!("gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesDeleteArgs,
    > for Empty
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule with SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesGetArgs,
    > for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesGetArgs,
    ) -> String {
        format!("gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSecurityHealthAnalyticsCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSecurityHealthAnalyticsCustomModulesResponse with SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesListArgs,
    > for ListSecurityHealthAnalyticsCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListSecurityHealthAnalyticsCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListSecurityHealthAnalyticsCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDescendantSecurityHealthAnalyticsCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDescendantSecurityHealthAnalyticsCustomModulesResponse with SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs,
    > for ListDescendantSecurityHealthAnalyticsCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListDescendantSecurityHealthAnalyticsCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListDescendantSecurityHealthAnalyticsCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule with SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesPatchArgs,
    > for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesPatchArgs,
    ) -> String {
        format!("gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SimulateSecurityHealthAnalyticsCustomModuleResponse
// =============================================================================

/// ResourceIdentifier implementation for SimulateSecurityHealthAnalyticsCustomModuleResponse with SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs,
    > for SimulateSecurityHealthAnalyticsCustomModuleResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::SimulateSecurityHealthAnalyticsCustomModuleResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::SimulateSecurityHealthAnalyticsCustomModuleResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule with SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsEffectiveCustomModulesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsEffectiveCustomModulesGetArgs,
    > for GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsEffectiveCustomModulesGetArgs,
    ) -> String {
        format!("gcp::securitycenter::GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListEffectiveSecurityHealthAnalyticsCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListEffectiveSecurityHealthAnalyticsCustomModulesResponse with SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs,
    > for ListEffectiveSecurityHealthAnalyticsCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListEffectiveSecurityHealthAnalyticsCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListEffectiveSecurityHealthAnalyticsCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Simulation
// =============================================================================

/// ResourceIdentifier implementation for Simulation with SecuritycenterOrganizationsSimulationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSimulationsGetArgs> for Simulation {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSimulationsGetArgs,
    ) -> String {
        format!("gcp::securitycenter::Simulation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Simulation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAttackPathsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAttackPathsResponse with SecuritycenterOrganizationsSimulationsAttackExposureResultsAttackPathsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsSimulationsAttackExposureResultsAttackPathsListArgs,
    > for ListAttackPathsResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSimulationsAttackExposureResultsAttackPathsListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListAttackPathsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListAttackPathsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListValuedResourcesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListValuedResourcesResponse with SecuritycenterOrganizationsSimulationsAttackExposureResultsValuedResourcesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterOrganizationsSimulationsAttackExposureResultsValuedResourcesListArgs,
    > for ListValuedResourcesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSimulationsAttackExposureResultsValuedResourcesListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListValuedResourcesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListValuedResourcesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAttackPathsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAttackPathsResponse with SecuritycenterOrganizationsSimulationsAttackPathsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSimulationsAttackPathsListArgs>
    for ListAttackPathsResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSimulationsAttackPathsListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListAttackPathsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListAttackPathsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ValuedResource
// =============================================================================

/// ResourceIdentifier implementation for ValuedResource with SecuritycenterOrganizationsSimulationsValuedResourcesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSimulationsValuedResourcesGetArgs>
    for ValuedResource
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSimulationsValuedResourcesGetArgs,
    ) -> String {
        format!("gcp::securitycenter::ValuedResource/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ValuedResource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListValuedResourcesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListValuedResourcesResponse with SecuritycenterOrganizationsSimulationsValuedResourcesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSimulationsValuedResourcesListArgs>
    for ListValuedResourcesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSimulationsValuedResourcesListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListValuedResourcesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListValuedResourcesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAttackPathsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAttackPathsResponse with SecuritycenterOrganizationsSimulationsValuedResourcesAttackPathsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSimulationsValuedResourcesAttackPathsListArgs>
    for ListAttackPathsResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSimulationsValuedResourcesAttackPathsListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListAttackPathsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListAttackPathsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Source
// =============================================================================

/// ResourceIdentifier implementation for Source with SecuritycenterOrganizationsSourcesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSourcesCreateArgs> for Source {
    fn generate_resource_id(&self, input: &SecuritycenterOrganizationsSourcesCreateArgs) -> String {
        format!("gcp::securitycenter::Source/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Source"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Source
// =============================================================================

/// ResourceIdentifier implementation for Source with SecuritycenterOrganizationsSourcesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSourcesGetArgs> for Source {
    fn generate_resource_id(&self, input: &SecuritycenterOrganizationsSourcesGetArgs) -> String {
        format!("gcp::securitycenter::Source/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Source"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with SecuritycenterOrganizationsSourcesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSourcesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSourcesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::securitycenter::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSourcesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSourcesResponse with SecuritycenterOrganizationsSourcesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSourcesListArgs> for ListSourcesResponse {
    fn generate_resource_id(&self, input: &SecuritycenterOrganizationsSourcesListArgs) -> String {
        format!("gcp::securitycenter::ListSourcesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListSourcesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Source
// =============================================================================

/// ResourceIdentifier implementation for Source with SecuritycenterOrganizationsSourcesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSourcesPatchArgs> for Source {
    fn generate_resource_id(&self, input: &SecuritycenterOrganizationsSourcesPatchArgs) -> String {
        format!("gcp::securitycenter::Source/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Source"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with SecuritycenterOrganizationsSourcesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSourcesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSourcesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::securitycenter::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with SecuritycenterOrganizationsSourcesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSourcesTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSourcesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Finding
// =============================================================================

/// ResourceIdentifier implementation for Finding with SecuritycenterOrganizationsSourcesFindingsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSourcesFindingsCreateArgs> for Finding {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSourcesFindingsCreateArgs,
    ) -> String {
        format!("gcp::securitycenter::Finding/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Finding"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GroupFindingsResponse
// =============================================================================

/// ResourceIdentifier implementation for GroupFindingsResponse with SecuritycenterOrganizationsSourcesFindingsGroupArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSourcesFindingsGroupArgs>
    for GroupFindingsResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSourcesFindingsGroupArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GroupFindingsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GroupFindingsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListFindingsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListFindingsResponse with SecuritycenterOrganizationsSourcesFindingsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSourcesFindingsListArgs>
    for ListFindingsResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSourcesFindingsListArgs,
    ) -> String {
        format!("gcp::securitycenter::ListFindingsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListFindingsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Finding
// =============================================================================

/// ResourceIdentifier implementation for Finding with SecuritycenterOrganizationsSourcesFindingsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSourcesFindingsPatchArgs> for Finding {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSourcesFindingsPatchArgs,
    ) -> String {
        format!("gcp::securitycenter::Finding/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Finding"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Finding
// =============================================================================

/// ResourceIdentifier implementation for Finding with SecuritycenterOrganizationsSourcesFindingsSetMuteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSourcesFindingsSetMuteArgs> for Finding {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSourcesFindingsSetMuteArgs,
    ) -> String {
        format!("gcp::securitycenter::Finding/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Finding"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Finding
// =============================================================================

/// ResourceIdentifier implementation for Finding with SecuritycenterOrganizationsSourcesFindingsSetStateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSourcesFindingsSetStateArgs> for Finding {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSourcesFindingsSetStateArgs,
    ) -> String {
        format!("gcp::securitycenter::Finding/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Finding"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityMarks
// =============================================================================

/// ResourceIdentifier implementation for SecurityMarks with SecuritycenterOrganizationsSourcesFindingsUpdateSecurityMarksArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSourcesFindingsUpdateSecurityMarksArgs>
    for SecurityMarks
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSourcesFindingsUpdateSecurityMarksArgs,
    ) -> String {
        format!("gcp::securitycenter::SecurityMarks/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::SecurityMarks"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1ExternalSystem
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1ExternalSystem with SecuritycenterOrganizationsSourcesFindingsExternalSystemsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsSourcesFindingsExternalSystemsPatchArgs>
    for GoogleCloudSecuritycenterV1ExternalSystem
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsSourcesFindingsExternalSystemsPatchArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1ExternalSystem/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1ExternalSystem"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListValuedResourcesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListValuedResourcesResponse with SecuritycenterOrganizationsValuedResourcesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterOrganizationsValuedResourcesListArgs>
    for ListValuedResourcesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterOrganizationsValuedResourcesListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListValuedResourcesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListValuedResourcesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GroupAssetsResponse
// =============================================================================

/// ResourceIdentifier implementation for GroupAssetsResponse with SecuritycenterProjectsAssetsGroupArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsAssetsGroupArgs> for GroupAssetsResponse {
    fn generate_resource_id(&self, input: &SecuritycenterProjectsAssetsGroupArgs) -> String {
        format!("gcp::securitycenter::GroupAssetsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GroupAssetsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAssetsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAssetsResponse with SecuritycenterProjectsAssetsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsAssetsListArgs> for ListAssetsResponse {
    fn generate_resource_id(&self, input: &SecuritycenterProjectsAssetsListArgs) -> String {
        format!("gcp::securitycenter::ListAssetsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListAssetsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityMarks
// =============================================================================

/// ResourceIdentifier implementation for SecurityMarks with SecuritycenterProjectsAssetsUpdateSecurityMarksArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsAssetsUpdateSecurityMarksArgs> for SecurityMarks {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsAssetsUpdateSecurityMarksArgs,
    ) -> String {
        format!("gcp::securitycenter::SecurityMarks/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::SecurityMarks"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport with SecuritycenterProjectsBigQueryExportsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsBigQueryExportsCreateArgs>
    for GoogleCloudSecuritycenterV1BigQueryExport
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsBigQueryExportsCreateArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterProjectsBigQueryExportsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsBigQueryExportsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsBigQueryExportsDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport with SecuritycenterProjectsBigQueryExportsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsBigQueryExportsGetArgs>
    for GoogleCloudSecuritycenterV1BigQueryExport
{
    fn generate_resource_id(&self, input: &SecuritycenterProjectsBigQueryExportsGetArgs) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListBigQueryExportsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListBigQueryExportsResponse with SecuritycenterProjectsBigQueryExportsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsBigQueryExportsListArgs>
    for ListBigQueryExportsResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsBigQueryExportsListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListBigQueryExportsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListBigQueryExportsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1BigQueryExport with SecuritycenterProjectsBigQueryExportsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsBigQueryExportsPatchArgs>
    for GoogleCloudSecuritycenterV1BigQueryExport
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsBigQueryExportsPatchArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1BigQueryExport"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ValidateEventThreatDetectionCustomModuleResponse
// =============================================================================

/// ResourceIdentifier implementation for ValidateEventThreatDetectionCustomModuleResponse with SecuritycenterProjectsEventThreatDetectionSettingsValidateCustomModuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsEventThreatDetectionSettingsValidateCustomModuleArgs>
    for ValidateEventThreatDetectionCustomModuleResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsEventThreatDetectionSettingsValidateCustomModuleArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ValidateEventThreatDetectionCustomModuleResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ValidateEventThreatDetectionCustomModuleResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EventThreatDetectionCustomModule
// =============================================================================

/// ResourceIdentifier implementation for EventThreatDetectionCustomModule with SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesCreateArgs>
    for EventThreatDetectionCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesCreateArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::EventThreatDetectionCustomModule/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::EventThreatDetectionCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EventThreatDetectionCustomModule
// =============================================================================

/// ResourceIdentifier implementation for EventThreatDetectionCustomModule with SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesGetArgs>
    for EventThreatDetectionCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesGetArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::EventThreatDetectionCustomModule/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::EventThreatDetectionCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListEventThreatDetectionCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListEventThreatDetectionCustomModulesResponse with SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesListArgs>
    for ListEventThreatDetectionCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListEventThreatDetectionCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListEventThreatDetectionCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDescendantEventThreatDetectionCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDescendantEventThreatDetectionCustomModulesResponse with SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesListDescendantArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesListDescendantArgs,
    > for ListDescendantEventThreatDetectionCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesListDescendantArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListDescendantEventThreatDetectionCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListDescendantEventThreatDetectionCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EventThreatDetectionCustomModule
// =============================================================================

/// ResourceIdentifier implementation for EventThreatDetectionCustomModule with SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesPatchArgs>
    for EventThreatDetectionCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesPatchArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::EventThreatDetectionCustomModule/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::EventThreatDetectionCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EffectiveEventThreatDetectionCustomModule
// =============================================================================

/// ResourceIdentifier implementation for EffectiveEventThreatDetectionCustomModule with SecuritycenterProjectsEventThreatDetectionSettingsEffectiveCustomModulesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterProjectsEventThreatDetectionSettingsEffectiveCustomModulesGetArgs,
    > for EffectiveEventThreatDetectionCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsEventThreatDetectionSettingsEffectiveCustomModulesGetArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::EffectiveEventThreatDetectionCustomModule/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::EffectiveEventThreatDetectionCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListEffectiveEventThreatDetectionCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListEffectiveEventThreatDetectionCustomModulesResponse with SecuritycenterProjectsEventThreatDetectionSettingsEffectiveCustomModulesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterProjectsEventThreatDetectionSettingsEffectiveCustomModulesListArgs,
    > for ListEffectiveEventThreatDetectionCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsEventThreatDetectionSettingsEffectiveCustomModulesListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListEffectiveEventThreatDetectionCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListEffectiveEventThreatDetectionCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SecuritycenterProjectsFindingsBulkMuteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsFindingsBulkMuteArgs> for Operation {
    fn generate_resource_id(&self, input: &SecuritycenterProjectsFindingsBulkMuteArgs) -> String {
        format!("gcp::securitycenter::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterProjectsLocationsMuteConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsLocationsMuteConfigsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsLocationsMuteConfigsDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig with SecuritycenterProjectsLocationsMuteConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsLocationsMuteConfigsGetArgs>
    for GoogleCloudSecuritycenterV1MuteConfig
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsLocationsMuteConfigsGetArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig with SecuritycenterProjectsLocationsMuteConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsLocationsMuteConfigsPatchArgs>
    for GoogleCloudSecuritycenterV1MuteConfig
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsLocationsMuteConfigsPatchArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig with SecuritycenterProjectsMuteConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsMuteConfigsCreateArgs>
    for GoogleCloudSecuritycenterV1MuteConfig
{
    fn generate_resource_id(&self, input: &SecuritycenterProjectsMuteConfigsCreateArgs) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterProjectsMuteConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsMuteConfigsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &SecuritycenterProjectsMuteConfigsDeleteArgs) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig with SecuritycenterProjectsMuteConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsMuteConfigsGetArgs>
    for GoogleCloudSecuritycenterV1MuteConfig
{
    fn generate_resource_id(&self, input: &SecuritycenterProjectsMuteConfigsGetArgs) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMuteConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMuteConfigsResponse with SecuritycenterProjectsMuteConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsMuteConfigsListArgs> for ListMuteConfigsResponse {
    fn generate_resource_id(&self, input: &SecuritycenterProjectsMuteConfigsListArgs) -> String {
        format!(
            "gcp::securitycenter::ListMuteConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListMuteConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1MuteConfig with SecuritycenterProjectsMuteConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsMuteConfigsPatchArgs>
    for GoogleCloudSecuritycenterV1MuteConfig
{
    fn generate_resource_id(&self, input: &SecuritycenterProjectsMuteConfigsPatchArgs) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1MuteConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationConfig
// =============================================================================

/// ResourceIdentifier implementation for NotificationConfig with SecuritycenterProjectsNotificationConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsNotificationConfigsCreateArgs>
    for NotificationConfig
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsNotificationConfigsCreateArgs,
    ) -> String {
        format!("gcp::securitycenter::NotificationConfig/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::NotificationConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterProjectsNotificationConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsNotificationConfigsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsNotificationConfigsDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationConfig
// =============================================================================

/// ResourceIdentifier implementation for NotificationConfig with SecuritycenterProjectsNotificationConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsNotificationConfigsGetArgs> for NotificationConfig {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsNotificationConfigsGetArgs,
    ) -> String {
        format!("gcp::securitycenter::NotificationConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::NotificationConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListNotificationConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListNotificationConfigsResponse with SecuritycenterProjectsNotificationConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsNotificationConfigsListArgs>
    for ListNotificationConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsNotificationConfigsListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListNotificationConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListNotificationConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationConfig
// =============================================================================

/// ResourceIdentifier implementation for NotificationConfig with SecuritycenterProjectsNotificationConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsNotificationConfigsPatchArgs> for NotificationConfig {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsNotificationConfigsPatchArgs,
    ) -> String {
        format!("gcp::securitycenter::NotificationConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::NotificationConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule with SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesCreateArgs>
    for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesCreateArgs,
    ) -> String {
        format!("gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesDeleteArgs,
    ) -> String {
        format!("gcp::securitycenter::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule with SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesGetArgs>
    for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesGetArgs,
    ) -> String {
        format!("gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSecurityHealthAnalyticsCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSecurityHealthAnalyticsCustomModulesResponse with SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesListArgs>
    for ListSecurityHealthAnalyticsCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListSecurityHealthAnalyticsCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListSecurityHealthAnalyticsCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDescendantSecurityHealthAnalyticsCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDescendantSecurityHealthAnalyticsCustomModulesResponse with SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs,
    > for ListDescendantSecurityHealthAnalyticsCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListDescendantSecurityHealthAnalyticsCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListDescendantSecurityHealthAnalyticsCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule with SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesPatchArgs>
    for GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesPatchArgs,
    ) -> String {
        format!("gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SimulateSecurityHealthAnalyticsCustomModuleResponse
// =============================================================================

/// ResourceIdentifier implementation for SimulateSecurityHealthAnalyticsCustomModuleResponse with SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs,
    > for SimulateSecurityHealthAnalyticsCustomModuleResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::SimulateSecurityHealthAnalyticsCustomModuleResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::SimulateSecurityHealthAnalyticsCustomModuleResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule with SecuritycenterProjectsSecurityHealthAnalyticsSettingsEffectiveCustomModulesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterProjectsSecurityHealthAnalyticsSettingsEffectiveCustomModulesGetArgs,
    > for GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsEffectiveCustomModulesGetArgs,
    ) -> String {
        format!("gcp::securitycenter::GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1EffectiveSecurityHealthAnalyticsCustomModule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListEffectiveSecurityHealthAnalyticsCustomModulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListEffectiveSecurityHealthAnalyticsCustomModulesResponse with SecuritycenterProjectsSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        SecuritycenterProjectsSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs,
    > for ListEffectiveSecurityHealthAnalyticsCustomModulesResponse
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::ListEffectiveSecurityHealthAnalyticsCustomModulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListEffectiveSecurityHealthAnalyticsCustomModulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSourcesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSourcesResponse with SecuritycenterProjectsSourcesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsSourcesListArgs> for ListSourcesResponse {
    fn generate_resource_id(&self, input: &SecuritycenterProjectsSourcesListArgs) -> String {
        format!("gcp::securitycenter::ListSourcesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListSourcesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GroupFindingsResponse
// =============================================================================

/// ResourceIdentifier implementation for GroupFindingsResponse with SecuritycenterProjectsSourcesFindingsGroupArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsSourcesFindingsGroupArgs> for GroupFindingsResponse {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsSourcesFindingsGroupArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GroupFindingsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GroupFindingsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListFindingsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListFindingsResponse with SecuritycenterProjectsSourcesFindingsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsSourcesFindingsListArgs> for ListFindingsResponse {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsSourcesFindingsListArgs,
    ) -> String {
        format!("gcp::securitycenter::ListFindingsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::ListFindingsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Finding
// =============================================================================

/// ResourceIdentifier implementation for Finding with SecuritycenterProjectsSourcesFindingsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsSourcesFindingsPatchArgs> for Finding {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsSourcesFindingsPatchArgs,
    ) -> String {
        format!("gcp::securitycenter::Finding/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Finding"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Finding
// =============================================================================

/// ResourceIdentifier implementation for Finding with SecuritycenterProjectsSourcesFindingsSetMuteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsSourcesFindingsSetMuteArgs> for Finding {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsSourcesFindingsSetMuteArgs,
    ) -> String {
        format!("gcp::securitycenter::Finding/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Finding"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Finding
// =============================================================================

/// ResourceIdentifier implementation for Finding with SecuritycenterProjectsSourcesFindingsSetStateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsSourcesFindingsSetStateArgs> for Finding {
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsSourcesFindingsSetStateArgs,
    ) -> String {
        format!("gcp::securitycenter::Finding/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::Finding"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityMarks
// =============================================================================

/// ResourceIdentifier implementation for SecurityMarks with SecuritycenterProjectsSourcesFindingsUpdateSecurityMarksArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsSourcesFindingsUpdateSecurityMarksArgs>
    for SecurityMarks
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsSourcesFindingsUpdateSecurityMarksArgs,
    ) -> String {
        format!("gcp::securitycenter::SecurityMarks/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::SecurityMarks"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1ExternalSystem
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudSecuritycenterV1ExternalSystem with SecuritycenterProjectsSourcesFindingsExternalSystemsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SecuritycenterProjectsSourcesFindingsExternalSystemsPatchArgs>
    for GoogleCloudSecuritycenterV1ExternalSystem
{
    fn generate_resource_id(
        &self,
        input: &SecuritycenterProjectsSourcesFindingsExternalSystemsPatchArgs,
    ) -> String {
        format!(
            "gcp::securitycenter::GoogleCloudSecuritycenterV1ExternalSystem/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::securitycenter::GoogleCloudSecuritycenterV1ExternalSystem"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
