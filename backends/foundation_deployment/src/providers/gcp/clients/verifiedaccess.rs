//! Auto-generated API clients for gcp/verifiedaccess.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v2/challenge:generate
/// Generates a new challenge.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `verifiedaccess_challenge_generate_execute()` to send, or `verifiedaccess_challenge_generate` for simplest API.

pub fn verifiedaccess_challenge_generate_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://verifiedaccess.googleapis.com/v2/challenge:generate",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/challenge:generate
/// Generates a new challenge.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `verifiedaccess_challenge_generate_execute()` or `verifiedaccess_challenge_generate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `verifiedaccess_challenge_generate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn verifiedaccess_challenge_generate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Challenge>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Challenge = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/challenge:generate
/// Generates a new challenge.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `verifiedaccess_challenge_generate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `verifiedaccess_challenge_generate_task()`.
/// For the simplest API, use `verifiedaccess_challenge_generate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `verifiedaccess_challenge_generate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn verifiedaccess_challenge_generate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Challenge>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = verifiedaccess_challenge_generate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v2/challenge:generate
/// Generates a new challenge.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `verifiedaccess_challenge_generate_builder()` + `verifiedaccess_challenge_generate_execute()`.
/// For task-level control, use `verifiedaccess_challenge_generate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn verifiedaccess_challenge_generate(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Challenge>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = verifiedaccess_challenge_generate_builder(client)?;
    verifiedaccess_challenge_generate_execute(builder)
}

/// POST v2/challenge:verify
/// Verifies the challenge response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `verifiedaccess_challenge_verify_execute()` to send, or `verifiedaccess_challenge_verify` for simplest API.

pub fn verifiedaccess_challenge_verify_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://verifiedaccess.googleapis.com/v2/challenge:verify",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/challenge:verify
/// Verifies the challenge response.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `verifiedaccess_challenge_verify_execute()` or `verifiedaccess_challenge_verify`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `verifiedaccess_challenge_verify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn verifiedaccess_challenge_verify_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VerifyChallengeResponseResult>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VerifyChallengeResponseResult = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/challenge:verify
/// Verifies the challenge response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `verifiedaccess_challenge_verify_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `verifiedaccess_challenge_verify_task()`.
/// For the simplest API, use `verifiedaccess_challenge_verify()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `verifiedaccess_challenge_verify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn verifiedaccess_challenge_verify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VerifyChallengeResponseResult>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = verifiedaccess_challenge_verify_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v2/challenge:verify
/// Verifies the challenge response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `verifiedaccess_challenge_verify_builder()` + `verifiedaccess_challenge_verify_execute()`.
/// For task-level control, use `verifiedaccess_challenge_verify_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn verifiedaccess_challenge_verify(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VerifyChallengeResponseResult>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = verifiedaccess_challenge_verify_builder(client)?;
    verifiedaccess_challenge_verify_execute(builder)
}
