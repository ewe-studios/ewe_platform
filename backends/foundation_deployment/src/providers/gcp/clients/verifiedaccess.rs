//! Auto-generated API clients for Google Cloud Platform - verifiedaccess.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v2/challenge:generate
/// Generates a new challenge.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `verifiedaccess_challenge_generate_execute()` to send, or `verifiedaccess_challenge_generate` for simplest API.

pub fn verifiedaccess_challenge_generate_builder(
    client: &SimpleHttpClient,
    body: &Empty,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/challenge:generate",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/challenge:generate
/// Generates a new challenge.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `verifiedaccess_challenge_generate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from verifiedaccess_challenge_generate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn verifiedaccess_challenge_generate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Challenge>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Challenge = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/challenge:generate
/// Generates a new challenge.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `verifiedaccess_challenge_generate_builder()` + `verifiedaccess_challenge_generate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn verifiedaccess_challenge_generate(
    client: &SimpleHttpClient,
    body: &Empty,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Challenge>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = verifiedaccess_challenge_generate_builder(client, body)?;
    verifiedaccess_challenge_generate_execute(builder)
}

/// GET v2/challenge:verify
/// Verifies the challenge response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `verifiedaccess_challenge_verify_execute()` to send, or `verifiedaccess_challenge_verify` for simplest API.

pub fn verifiedaccess_challenge_verify_builder(
    client: &SimpleHttpClient,
    body: &VerifyChallengeResponseRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/challenge:verify",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/challenge:verify
/// Verifies the challenge response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `verifiedaccess_challenge_verify_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from verifiedaccess_challenge_verify_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn verifiedaccess_challenge_verify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VerifyChallengeResponseResult>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VerifyChallengeResponseResult = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/challenge:verify
/// Verifies the challenge response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `verifiedaccess_challenge_verify_builder()` + `verifiedaccess_challenge_verify_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn verifiedaccess_challenge_verify(
    client: &SimpleHttpClient,
    body: &VerifyChallengeResponseRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VerifyChallengeResponseResult>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = verifiedaccess_challenge_verify_builder(client, body)?;
    verifiedaccess_challenge_verify_execute(builder)
}
