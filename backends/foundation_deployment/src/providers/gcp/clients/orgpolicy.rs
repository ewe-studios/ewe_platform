//! Auto-generated API clients for gcp/orgpolicy.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v2/folders/{foldersId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_folders_constraints_list_execute()` to send, or `orgpolicy_folders_constraints_list` for simplest API.

pub fn orgpolicy_folders_constraints_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://orgpolicy.googleapis.com/v2/folders/{}/constraints",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `orgpolicy_folders_constraints_list_execute()` or `orgpolicy_folders_constraints_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `orgpolicy_folders_constraints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_constraints_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudOrgpolicyV2ListConstraintsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2ListConstraintsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `orgpolicy_folders_constraints_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `orgpolicy_folders_constraints_list_task()`.
/// For the simplest API, use `orgpolicy_folders_constraints_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `orgpolicy_folders_constraints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn orgpolicy_folders_constraints_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListConstraintsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = orgpolicy_folders_constraints_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_folders_constraints_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyFoldersConstraintsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/folders/{foldersId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_folders_constraints_list_builder()` + `orgpolicy_folders_constraints_list_execute()`.
/// For task-level control, use `orgpolicy_folders_constraints_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_constraints_list(
    client: &SimpleHttpClient,
    args: &OrgpolicyFoldersConstraintsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListConstraintsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_folders_constraints_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    orgpolicy_folders_constraints_list_execute(builder)
}

/// GET v2/folders/{foldersId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_folders_policies_create_execute()` to send, or `orgpolicy_folders_policies_create` for simplest API.

pub fn orgpolicy_folders_policies_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudOrgpolicyV2Policy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://orgpolicy.googleapis.com/v2/folders/{}/policies",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/folders/{foldersId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `orgpolicy_folders_policies_create_execute()` or `orgpolicy_folders_policies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `orgpolicy_folders_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_policies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `orgpolicy_folders_policies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `orgpolicy_folders_policies_create_task()`.
/// For the simplest API, use `orgpolicy_folders_policies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `orgpolicy_folders_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn orgpolicy_folders_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = orgpolicy_folders_policies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_folders_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyFoldersPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudOrgpolicyV2Policy,
}

/// GET v2/folders/{foldersId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_folders_policies_create_builder()` + `orgpolicy_folders_policies_create_execute()`.
/// For task-level control, use `orgpolicy_folders_policies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_policies_create(
    client: &SimpleHttpClient,
    args: &OrgpolicyFoldersPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_folders_policies_create_builder(client, &args.parent, &args.body)?;
    orgpolicy_folders_policies_create_execute(builder)
}

/// GET v2/organizations/{organizationsId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_organizations_constraints_list_execute()` to send, or `orgpolicy_organizations_constraints_list` for simplest API.

pub fn orgpolicy_organizations_constraints_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://orgpolicy.googleapis.com/v2/organizations/{}/constraints",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `orgpolicy_organizations_constraints_list_execute()` or `orgpolicy_organizations_constraints_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `orgpolicy_organizations_constraints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_constraints_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudOrgpolicyV2ListConstraintsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2ListConstraintsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `orgpolicy_organizations_constraints_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `orgpolicy_organizations_constraints_list_task()`.
/// For the simplest API, use `orgpolicy_organizations_constraints_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `orgpolicy_organizations_constraints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn orgpolicy_organizations_constraints_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListConstraintsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = orgpolicy_organizations_constraints_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_organizations_constraints_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyOrganizationsConstraintsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_organizations_constraints_list_builder()` + `orgpolicy_organizations_constraints_list_execute()`.
/// For task-level control, use `orgpolicy_organizations_constraints_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_constraints_list(
    client: &SimpleHttpClient,
    args: &OrgpolicyOrganizationsConstraintsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListConstraintsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_organizations_constraints_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    orgpolicy_organizations_constraints_list_execute(builder)
}

/// GET v2/organizations/{organizationsId}/customConstraints
/// Creates a custom constraint. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the organization does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the constraint already exists on the given organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_organizations_custom_constraints_create_execute()` to send, or `orgpolicy_organizations_custom_constraints_create` for simplest API.

pub fn orgpolicy_organizations_custom_constraints_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudOrgpolicyV2CustomConstraint,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://orgpolicy.googleapis.com/v2/organizations/{}/customConstraints",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/organizations/{organizationsId}/customConstraints
/// Creates a custom constraint. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the organization does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the constraint already exists on the given organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `orgpolicy_organizations_custom_constraints_create_execute()` or `orgpolicy_organizations_custom_constraints_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `orgpolicy_organizations_custom_constraints_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_custom_constraints_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudOrgpolicyV2CustomConstraint>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2CustomConstraint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/customConstraints
/// Creates a custom constraint. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the organization does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the constraint already exists on the given organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `orgpolicy_organizations_custom_constraints_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `orgpolicy_organizations_custom_constraints_create_task()`.
/// For the simplest API, use `orgpolicy_organizations_custom_constraints_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `orgpolicy_organizations_custom_constraints_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn orgpolicy_organizations_custom_constraints_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2CustomConstraint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = orgpolicy_organizations_custom_constraints_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_organizations_custom_constraints_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyOrganizationsCustomConstraintsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudOrgpolicyV2CustomConstraint,
}

/// GET v2/organizations/{organizationsId}/customConstraints
/// Creates a custom constraint. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the organization does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the constraint already exists on the given organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_organizations_custom_constraints_create_builder()` + `orgpolicy_organizations_custom_constraints_create_execute()`.
/// For task-level control, use `orgpolicy_organizations_custom_constraints_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_custom_constraints_create(
    client: &SimpleHttpClient,
    args: &OrgpolicyOrganizationsCustomConstraintsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2CustomConstraint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_organizations_custom_constraints_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    orgpolicy_organizations_custom_constraints_create_execute(builder)
}

/// GET v2/organizations/{organizationsId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_organizations_policies_create_execute()` to send, or `orgpolicy_organizations_policies_create` for simplest API.

pub fn orgpolicy_organizations_policies_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudOrgpolicyV2Policy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://orgpolicy.googleapis.com/v2/organizations/{}/policies",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/organizations/{organizationsId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `orgpolicy_organizations_policies_create_execute()` or `orgpolicy_organizations_policies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `orgpolicy_organizations_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_policies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `orgpolicy_organizations_policies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `orgpolicy_organizations_policies_create_task()`.
/// For the simplest API, use `orgpolicy_organizations_policies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `orgpolicy_organizations_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn orgpolicy_organizations_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = orgpolicy_organizations_policies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_organizations_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyOrganizationsPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudOrgpolicyV2Policy,
}

/// GET v2/organizations/{organizationsId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_organizations_policies_create_builder()` + `orgpolicy_organizations_policies_create_execute()`.
/// For task-level control, use `orgpolicy_organizations_policies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_policies_create(
    client: &SimpleHttpClient,
    args: &OrgpolicyOrganizationsPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        orgpolicy_organizations_policies_create_builder(client, &args.parent, &args.body)?;
    orgpolicy_organizations_policies_create_execute(builder)
}

/// GET v2/projects/{projectsId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_projects_constraints_list_execute()` to send, or `orgpolicy_projects_constraints_list` for simplest API.

pub fn orgpolicy_projects_constraints_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://orgpolicy.googleapis.com/v2/projects/{}/constraints",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `orgpolicy_projects_constraints_list_execute()` or `orgpolicy_projects_constraints_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `orgpolicy_projects_constraints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_constraints_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudOrgpolicyV2ListConstraintsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2ListConstraintsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `orgpolicy_projects_constraints_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `orgpolicy_projects_constraints_list_task()`.
/// For the simplest API, use `orgpolicy_projects_constraints_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `orgpolicy_projects_constraints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn orgpolicy_projects_constraints_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListConstraintsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = orgpolicy_projects_constraints_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_projects_constraints_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyProjectsConstraintsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_projects_constraints_list_builder()` + `orgpolicy_projects_constraints_list_execute()`.
/// For task-level control, use `orgpolicy_projects_constraints_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_constraints_list(
    client: &SimpleHttpClient,
    args: &OrgpolicyProjectsConstraintsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListConstraintsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_projects_constraints_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    orgpolicy_projects_constraints_list_execute(builder)
}

/// GET v2/projects/{projectsId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_projects_policies_create_execute()` to send, or `orgpolicy_projects_policies_create` for simplest API.

pub fn orgpolicy_projects_policies_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudOrgpolicyV2Policy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://orgpolicy.googleapis.com/v2/projects/{}/policies",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/projects/{projectsId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `orgpolicy_projects_policies_create_execute()` or `orgpolicy_projects_policies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `orgpolicy_projects_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_policies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `orgpolicy_projects_policies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `orgpolicy_projects_policies_create_task()`.
/// For the simplest API, use `orgpolicy_projects_policies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `orgpolicy_projects_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn orgpolicy_projects_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = orgpolicy_projects_policies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_projects_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyProjectsPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudOrgpolicyV2Policy,
}

/// GET v2/projects/{projectsId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_projects_policies_create_builder()` + `orgpolicy_projects_policies_create_execute()`.
/// For task-level control, use `orgpolicy_projects_policies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_policies_create(
    client: &SimpleHttpClient,
    args: &OrgpolicyProjectsPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_projects_policies_create_builder(client, &args.parent, &args.body)?;
    orgpolicy_projects_policies_create_execute(builder)
}
