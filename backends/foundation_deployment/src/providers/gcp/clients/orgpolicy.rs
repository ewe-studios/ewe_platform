//! Auto-generated API clients for gcp/orgpolicy.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/orgpolicy`

#![cfg(feature = "gcp/orgpolicy")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v2/folders/{foldersId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_folders_constraints_list_execute()` to send, or `orgpolicy_folders_constraints_list` for simplest API.

pub fn orgpolicy_folders_constraints_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/folders/{}/constraints",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_folders_constraints_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_folders_constraints_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_constraints_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListConstraintsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2ListConstraintsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_folders_constraints_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyFoldersConstraintsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/folders/{foldersId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_folders_constraints_list_builder()` + `orgpolicy_folders_constraints_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_constraints_list(
    client: &SimpleHttpClient,
    args: &OrgpolicyFoldersConstraintsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListConstraintsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_folders_constraints_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    orgpolicy_folders_constraints_list_execute(builder)
}

/// GET v2/folders/{foldersId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_folders_policies_create_execute()` to send, or `orgpolicy_folders_policies_create` for simplest API.

pub fn orgpolicy_folders_policies_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudOrgpolicyV2Policy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/folders/{}/policies",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/folders/{foldersId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_folders_policies_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_folders_policies_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_folders_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyFoldersPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudOrgpolicyV2Policy,
}

/// GET v2/folders/{foldersId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_folders_policies_create_builder()` + `orgpolicy_folders_policies_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_policies_create(
    client: &SimpleHttpClient,
    args: &OrgpolicyFoldersPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_folders_policies_create_builder(client, &args.parent, &args.body)?;
    orgpolicy_folders_policies_create_execute(builder)
}

/// GET v2/folders/{foldersId}/policies/{policiesId}
/// Deletes a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or organization policy does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_folders_policies_delete_execute()` to send, or `orgpolicy_folders_policies_delete` for simplest API.

pub fn orgpolicy_folders_policies_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/folders/{}/policies/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/policies/{policiesId}
/// Deletes a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or organization policy does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_folders_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_folders_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_folders_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyFoldersPoliciesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<String>,
}

/// GET v2/folders/{foldersId}/policies/{policiesId}
/// Deletes a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or organization policy does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_folders_policies_delete_builder()` + `orgpolicy_folders_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_policies_delete(
    client: &SimpleHttpClient,
    args: &OrgpolicyFoldersPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        orgpolicy_folders_policies_delete_builder(client, &args.name, args.etag.as_deref())?;
    orgpolicy_folders_policies_delete_execute(builder)
}

/// GET v2/folders/{foldersId}/policies/{policiesId}
/// Gets a policy on a resource. If no policy is set on the resource, NOT_FOUND is returned. The entity tag (ETag) can be used with UpdatePolicy() to update a policy during read-modify-write.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_folders_policies_get_execute()` to send, or `orgpolicy_folders_policies_get` for simplest API.

pub fn orgpolicy_folders_policies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/folders/{}/policies/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/policies/{policiesId}
/// Gets a policy on a resource. If no policy is set on the resource, NOT_FOUND is returned. The entity tag (ETag) can be used with UpdatePolicy() to update a policy during read-modify-write.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_folders_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_folders_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_folders_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyFoldersPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/folders/{foldersId}/policies/{policiesId}
/// Gets a policy on a resource. If no policy is set on the resource, NOT_FOUND is returned. The entity tag (ETag) can be used with UpdatePolicy() to update a policy during read-modify-write.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_folders_policies_get_builder()` + `orgpolicy_folders_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_policies_get(
    client: &SimpleHttpClient,
    args: &OrgpolicyFoldersPoliciesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_folders_policies_get_builder(client, &args.name)?;
    orgpolicy_folders_policies_get_execute(builder)
}

/// GET v2/folders/{foldersId}/policies/{policiesId}:getEffectivePolicy
/// Gets the effective policy on a resource. This is the result of merging policies in the resource hierarchy and evaluating conditions. The returned policy will not have an ETag or condition set because it is an evaluated policy across multiple resources. Subtrees of Resource Manager resource hierarchy with 'under:' prefix will not be expanded.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_folders_policies_get_effective_policy_execute()` to send, or `orgpolicy_folders_policies_get_effective_policy` for simplest API.

pub fn orgpolicy_folders_policies_get_effective_policy_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/folders/{}/policies/{}:getEffectivePolicy",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/policies/{policiesId}:getEffectivePolicy
/// Gets the effective policy on a resource. This is the result of merging policies in the resource hierarchy and evaluating conditions. The returned policy will not have an ETag or condition set because it is an evaluated policy across multiple resources. Subtrees of Resource Manager resource hierarchy with 'under:' prefix will not be expanded.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_folders_policies_get_effective_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_folders_policies_get_effective_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_policies_get_effective_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_folders_policies_get_effective_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyFoldersPoliciesGetEffectivePolicyArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/folders/{foldersId}/policies/{policiesId}:getEffectivePolicy
/// Gets the effective policy on a resource. This is the result of merging policies in the resource hierarchy and evaluating conditions. The returned policy will not have an ETag or condition set because it is an evaluated policy across multiple resources. Subtrees of Resource Manager resource hierarchy with 'under:' prefix will not be expanded.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_folders_policies_get_effective_policy_builder()` + `orgpolicy_folders_policies_get_effective_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_policies_get_effective_policy(
    client: &SimpleHttpClient,
    args: &OrgpolicyFoldersPoliciesGetEffectivePolicyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_folders_policies_get_effective_policy_builder(client, &args.name)?;
    orgpolicy_folders_policies_get_effective_policy_execute(builder)
}

/// GET v2/folders/{foldersId}/policies
/// Retrieves all of the policies that exist on a particular resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_folders_policies_list_execute()` to send, or `orgpolicy_folders_policies_list` for simplest API.

pub fn orgpolicy_folders_policies_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/folders/{}/policies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/policies
/// Retrieves all of the policies that exist on a particular resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_folders_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_folders_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2ListPoliciesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_folders_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyFoldersPoliciesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/folders/{foldersId}/policies
/// Retrieves all of the policies that exist on a particular resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_folders_policies_list_builder()` + `orgpolicy_folders_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_policies_list(
    client: &SimpleHttpClient,
    args: &OrgpolicyFoldersPoliciesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_folders_policies_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    orgpolicy_folders_policies_list_execute(builder)
}

/// GET v2/folders/{foldersId}/policies/{policiesId}
/// Updates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or the policy doesn't exist. Returns a google.rpc.Status with google.rpc.Code.ABORTED if the ETag supplied in the request doesn't match the persisted ETag of the policy. Note: the supplied policy will perform a full overwrite of all fields.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_folders_policies_patch_execute()` to send, or `orgpolicy_folders_policies_patch` for simplest API.

pub fn orgpolicy_folders_policies_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudOrgpolicyV2Policy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/folders/{}/policies/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/folders/{foldersId}/policies/{policiesId}
/// Updates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or the policy doesn't exist. Returns a google.rpc.Status with google.rpc.Code.ABORTED if the ETag supplied in the request doesn't match the persisted ETag of the policy. Note: the supplied policy will perform a full overwrite of all fields.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_folders_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_folders_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_folders_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyFoldersPoliciesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudOrgpolicyV2Policy,
}

/// GET v2/folders/{foldersId}/policies/{policiesId}
/// Updates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or the policy doesn't exist. Returns a google.rpc.Status with google.rpc.Code.ABORTED if the ETag supplied in the request doesn't match the persisted ETag of the policy. Note: the supplied policy will perform a full overwrite of all fields.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_folders_policies_patch_builder()` + `orgpolicy_folders_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_folders_policies_patch(
    client: &SimpleHttpClient,
    args: &OrgpolicyFoldersPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_folders_policies_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    orgpolicy_folders_policies_patch_execute(builder)
}

/// GET v2/organizations/{organizationsId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_organizations_constraints_list_execute()` to send, or `orgpolicy_organizations_constraints_list` for simplest API.

pub fn orgpolicy_organizations_constraints_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/organizations/{}/constraints",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_organizations_constraints_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_organizations_constraints_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_constraints_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListConstraintsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2ListConstraintsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_organizations_constraints_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyOrganizationsConstraintsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_organizations_constraints_list_builder()` + `orgpolicy_organizations_constraints_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_constraints_list(
    client: &SimpleHttpClient,
    args: &OrgpolicyOrganizationsConstraintsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListConstraintsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_organizations_constraints_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    orgpolicy_organizations_constraints_list_execute(builder)
}

/// GET v2/organizations/{organizationsId}/customConstraints
/// Creates a custom constraint. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the organization does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the constraint already exists on the given organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_organizations_custom_constraints_create_execute()` to send, or `orgpolicy_organizations_custom_constraints_create` for simplest API.

pub fn orgpolicy_organizations_custom_constraints_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudOrgpolicyV2CustomConstraint,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/organizations/{}/customConstraints",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/organizations/{organizationsId}/customConstraints
/// Creates a custom constraint. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the organization does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the constraint already exists on the given organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_organizations_custom_constraints_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_organizations_custom_constraints_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_custom_constraints_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2CustomConstraint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2CustomConstraint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_organizations_custom_constraints_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyOrganizationsCustomConstraintsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudOrgpolicyV2CustomConstraint,
}

/// GET v2/organizations/{organizationsId}/customConstraints
/// Creates a custom constraint. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the organization does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the constraint already exists on the given organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_organizations_custom_constraints_create_builder()` + `orgpolicy_organizations_custom_constraints_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_custom_constraints_create(
    client: &SimpleHttpClient,
    args: &OrgpolicyOrganizationsCustomConstraintsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2CustomConstraint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_organizations_custom_constraints_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    orgpolicy_organizations_custom_constraints_create_execute(builder)
}

/// GET v2/organizations/{organizationsId}/customConstraints/{customConstraintsId}
/// Deletes a custom constraint. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_organizations_custom_constraints_delete_execute()` to send, or `orgpolicy_organizations_custom_constraints_delete` for simplest API.

pub fn orgpolicy_organizations_custom_constraints_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/organizations/{}/customConstraints/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/customConstraints/{customConstraintsId}
/// Deletes a custom constraint. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_organizations_custom_constraints_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_organizations_custom_constraints_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_custom_constraints_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_organizations_custom_constraints_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyOrganizationsCustomConstraintsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/customConstraints/{customConstraintsId}
/// Deletes a custom constraint. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_organizations_custom_constraints_delete_builder()` + `orgpolicy_organizations_custom_constraints_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_custom_constraints_delete(
    client: &SimpleHttpClient,
    args: &OrgpolicyOrganizationsCustomConstraintsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_organizations_custom_constraints_delete_builder(client, &args.name)?;
    orgpolicy_organizations_custom_constraints_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/customConstraints/{customConstraintsId}
/// Gets a custom or managed constraint. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the custom or managed constraint does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_organizations_custom_constraints_get_execute()` to send, or `orgpolicy_organizations_custom_constraints_get` for simplest API.

pub fn orgpolicy_organizations_custom_constraints_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/organizations/{}/customConstraints/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/customConstraints/{customConstraintsId}
/// Gets a custom or managed constraint. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the custom or managed constraint does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_organizations_custom_constraints_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_organizations_custom_constraints_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_custom_constraints_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2CustomConstraint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2CustomConstraint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_organizations_custom_constraints_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyOrganizationsCustomConstraintsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/customConstraints/{customConstraintsId}
/// Gets a custom or managed constraint. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the custom or managed constraint does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_organizations_custom_constraints_get_builder()` + `orgpolicy_organizations_custom_constraints_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_custom_constraints_get(
    client: &SimpleHttpClient,
    args: &OrgpolicyOrganizationsCustomConstraintsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2CustomConstraint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_organizations_custom_constraints_get_builder(client, &args.name)?;
    orgpolicy_organizations_custom_constraints_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/customConstraints
/// Retrieves all of the custom constraints that exist on a particular organization resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_organizations_custom_constraints_list_execute()` to send, or `orgpolicy_organizations_custom_constraints_list` for simplest API.

pub fn orgpolicy_organizations_custom_constraints_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/organizations/{}/customConstraints",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/customConstraints
/// Retrieves all of the custom constraints that exist on a particular organization resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_organizations_custom_constraints_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_organizations_custom_constraints_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_custom_constraints_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListCustomConstraintsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2ListCustomConstraintsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_organizations_custom_constraints_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyOrganizationsCustomConstraintsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/customConstraints
/// Retrieves all of the custom constraints that exist on a particular organization resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_organizations_custom_constraints_list_builder()` + `orgpolicy_organizations_custom_constraints_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_custom_constraints_list(
    client: &SimpleHttpClient,
    args: &OrgpolicyOrganizationsCustomConstraintsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListCustomConstraintsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_organizations_custom_constraints_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    orgpolicy_organizations_custom_constraints_list_execute(builder)
}

/// GET v2/organizations/{organizationsId}/customConstraints/{customConstraintsId}
/// Updates a custom constraint. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Note: the supplied policy will perform a full overwrite of all fields.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_organizations_custom_constraints_patch_execute()` to send, or `orgpolicy_organizations_custom_constraints_patch` for simplest API.

pub fn orgpolicy_organizations_custom_constraints_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudOrgpolicyV2CustomConstraint,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/organizations/{}/customConstraints/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/organizations/{organizationsId}/customConstraints/{customConstraintsId}
/// Updates a custom constraint. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Note: the supplied policy will perform a full overwrite of all fields.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_organizations_custom_constraints_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_organizations_custom_constraints_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_custom_constraints_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2CustomConstraint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2CustomConstraint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_organizations_custom_constraints_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyOrganizationsCustomConstraintsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudOrgpolicyV2CustomConstraint,
}

/// GET v2/organizations/{organizationsId}/customConstraints/{customConstraintsId}
/// Updates a custom constraint. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Note: the supplied policy will perform a full overwrite of all fields.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_organizations_custom_constraints_patch_builder()` + `orgpolicy_organizations_custom_constraints_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_custom_constraints_patch(
    client: &SimpleHttpClient,
    args: &OrgpolicyOrganizationsCustomConstraintsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2CustomConstraint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        orgpolicy_organizations_custom_constraints_patch_builder(client, &args.name, &args.body)?;
    orgpolicy_organizations_custom_constraints_patch_execute(builder)
}

/// GET v2/organizations/{organizationsId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_organizations_policies_create_execute()` to send, or `orgpolicy_organizations_policies_create` for simplest API.

pub fn orgpolicy_organizations_policies_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudOrgpolicyV2Policy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/organizations/{}/policies",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/organizations/{organizationsId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_organizations_policies_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_organizations_policies_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_organizations_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyOrganizationsPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudOrgpolicyV2Policy,
}

/// GET v2/organizations/{organizationsId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_organizations_policies_create_builder()` + `orgpolicy_organizations_policies_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_policies_create(
    client: &SimpleHttpClient,
    args: &OrgpolicyOrganizationsPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        orgpolicy_organizations_policies_create_builder(client, &args.parent, &args.body)?;
    orgpolicy_organizations_policies_create_execute(builder)
}

/// GET v2/organizations/{organizationsId}/policies/{policiesId}
/// Deletes a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or organization policy does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_organizations_policies_delete_execute()` to send, or `orgpolicy_organizations_policies_delete` for simplest API.

pub fn orgpolicy_organizations_policies_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/organizations/{}/policies/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/policies/{policiesId}
/// Deletes a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or organization policy does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_organizations_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_organizations_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_organizations_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyOrganizationsPoliciesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<String>,
}

/// GET v2/organizations/{organizationsId}/policies/{policiesId}
/// Deletes a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or organization policy does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_organizations_policies_delete_builder()` + `orgpolicy_organizations_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_policies_delete(
    client: &SimpleHttpClient,
    args: &OrgpolicyOrganizationsPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        orgpolicy_organizations_policies_delete_builder(client, &args.name, args.etag.as_deref())?;
    orgpolicy_organizations_policies_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/policies/{policiesId}
/// Gets a policy on a resource. If no policy is set on the resource, NOT_FOUND is returned. The entity tag (ETag) can be used with UpdatePolicy() to update a policy during read-modify-write.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_organizations_policies_get_execute()` to send, or `orgpolicy_organizations_policies_get` for simplest API.

pub fn orgpolicy_organizations_policies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/organizations/{}/policies/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/policies/{policiesId}
/// Gets a policy on a resource. If no policy is set on the resource, NOT_FOUND is returned. The entity tag (ETag) can be used with UpdatePolicy() to update a policy during read-modify-write.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_organizations_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_organizations_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_organizations_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyOrganizationsPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/policies/{policiesId}
/// Gets a policy on a resource. If no policy is set on the resource, NOT_FOUND is returned. The entity tag (ETag) can be used with UpdatePolicy() to update a policy during read-modify-write.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_organizations_policies_get_builder()` + `orgpolicy_organizations_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_policies_get(
    client: &SimpleHttpClient,
    args: &OrgpolicyOrganizationsPoliciesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_organizations_policies_get_builder(client, &args.name)?;
    orgpolicy_organizations_policies_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/policies/{policiesId}:getEffectivePolicy
/// Gets the effective policy on a resource. This is the result of merging policies in the resource hierarchy and evaluating conditions. The returned policy will not have an ETag or condition set because it is an evaluated policy across multiple resources. Subtrees of Resource Manager resource hierarchy with 'under:' prefix will not be expanded.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_organizations_policies_get_effective_policy_execute()` to send, or `orgpolicy_organizations_policies_get_effective_policy` for simplest API.

pub fn orgpolicy_organizations_policies_get_effective_policy_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/organizations/{}/policies/{}:getEffectivePolicy",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/policies/{policiesId}:getEffectivePolicy
/// Gets the effective policy on a resource. This is the result of merging policies in the resource hierarchy and evaluating conditions. The returned policy will not have an ETag or condition set because it is an evaluated policy across multiple resources. Subtrees of Resource Manager resource hierarchy with 'under:' prefix will not be expanded.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_organizations_policies_get_effective_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_organizations_policies_get_effective_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_policies_get_effective_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_organizations_policies_get_effective_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyOrganizationsPoliciesGetEffectivePolicyArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/policies/{policiesId}:getEffectivePolicy
/// Gets the effective policy on a resource. This is the result of merging policies in the resource hierarchy and evaluating conditions. The returned policy will not have an ETag or condition set because it is an evaluated policy across multiple resources. Subtrees of Resource Manager resource hierarchy with 'under:' prefix will not be expanded.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_organizations_policies_get_effective_policy_builder()` + `orgpolicy_organizations_policies_get_effective_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_policies_get_effective_policy(
    client: &SimpleHttpClient,
    args: &OrgpolicyOrganizationsPoliciesGetEffectivePolicyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        orgpolicy_organizations_policies_get_effective_policy_builder(client, &args.name)?;
    orgpolicy_organizations_policies_get_effective_policy_execute(builder)
}

/// GET v2/organizations/{organizationsId}/policies
/// Retrieves all of the policies that exist on a particular resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_organizations_policies_list_execute()` to send, or `orgpolicy_organizations_policies_list` for simplest API.

pub fn orgpolicy_organizations_policies_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/organizations/{}/policies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/policies
/// Retrieves all of the policies that exist on a particular resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_organizations_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_organizations_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2ListPoliciesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_organizations_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyOrganizationsPoliciesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/policies
/// Retrieves all of the policies that exist on a particular resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_organizations_policies_list_builder()` + `orgpolicy_organizations_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_policies_list(
    client: &SimpleHttpClient,
    args: &OrgpolicyOrganizationsPoliciesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_organizations_policies_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    orgpolicy_organizations_policies_list_execute(builder)
}

/// GET v2/organizations/{organizationsId}/policies/{policiesId}
/// Updates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or the policy doesn't exist. Returns a google.rpc.Status with google.rpc.Code.ABORTED if the ETag supplied in the request doesn't match the persisted ETag of the policy. Note: the supplied policy will perform a full overwrite of all fields.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_organizations_policies_patch_execute()` to send, or `orgpolicy_organizations_policies_patch` for simplest API.

pub fn orgpolicy_organizations_policies_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudOrgpolicyV2Policy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/organizations/{}/policies/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/organizations/{organizationsId}/policies/{policiesId}
/// Updates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or the policy doesn't exist. Returns a google.rpc.Status with google.rpc.Code.ABORTED if the ETag supplied in the request doesn't match the persisted ETag of the policy. Note: the supplied policy will perform a full overwrite of all fields.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_organizations_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_organizations_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_organizations_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyOrganizationsPoliciesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudOrgpolicyV2Policy,
}

/// GET v2/organizations/{organizationsId}/policies/{policiesId}
/// Updates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or the policy doesn't exist. Returns a google.rpc.Status with google.rpc.Code.ABORTED if the ETag supplied in the request doesn't match the persisted ETag of the policy. Note: the supplied policy will perform a full overwrite of all fields.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_organizations_policies_patch_builder()` + `orgpolicy_organizations_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_organizations_policies_patch(
    client: &SimpleHttpClient,
    args: &OrgpolicyOrganizationsPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_organizations_policies_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    orgpolicy_organizations_policies_patch_execute(builder)
}

/// GET v2/projects/{projectsId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_projects_constraints_list_execute()` to send, or `orgpolicy_projects_constraints_list` for simplest API.

pub fn orgpolicy_projects_constraints_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/projects/{}/constraints",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_projects_constraints_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_projects_constraints_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_constraints_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListConstraintsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2ListConstraintsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_projects_constraints_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyProjectsConstraintsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/constraints
/// Lists constraints that could be applied on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_projects_constraints_list_builder()` + `orgpolicy_projects_constraints_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_constraints_list(
    client: &SimpleHttpClient,
    args: &OrgpolicyProjectsConstraintsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListConstraintsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_projects_constraints_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    orgpolicy_projects_constraints_list_execute(builder)
}

/// GET v2/projects/{projectsId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_projects_policies_create_execute()` to send, or `orgpolicy_projects_policies_create` for simplest API.

pub fn orgpolicy_projects_policies_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudOrgpolicyV2Policy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/projects/{}/policies",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/projects/{projectsId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_projects_policies_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_projects_policies_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_projects_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyProjectsPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudOrgpolicyV2Policy,
}

/// GET v2/projects/{projectsId}/policies
/// Creates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint does not exist. Returns a google.rpc.Status with google.rpc.Code.ALREADY_EXISTS if the policy already exists on the given Google Cloud resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_projects_policies_create_builder()` + `orgpolicy_projects_policies_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_policies_create(
    client: &SimpleHttpClient,
    args: &OrgpolicyProjectsPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_projects_policies_create_builder(client, &args.parent, &args.body)?;
    orgpolicy_projects_policies_create_execute(builder)
}

/// GET v2/projects/{projectsId}/policies/{policiesId}
/// Deletes a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or organization policy does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_projects_policies_delete_execute()` to send, or `orgpolicy_projects_policies_delete` for simplest API.

pub fn orgpolicy_projects_policies_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/projects/{}/policies/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/policies/{policiesId}
/// Deletes a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or organization policy does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_projects_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_projects_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_projects_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyProjectsPoliciesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<String>,
}

/// GET v2/projects/{projectsId}/policies/{policiesId}
/// Deletes a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or organization policy does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_projects_policies_delete_builder()` + `orgpolicy_projects_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_policies_delete(
    client: &SimpleHttpClient,
    args: &OrgpolicyProjectsPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        orgpolicy_projects_policies_delete_builder(client, &args.name, args.etag.as_deref())?;
    orgpolicy_projects_policies_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/policies/{policiesId}
/// Gets a policy on a resource. If no policy is set on the resource, NOT_FOUND is returned. The entity tag (ETag) can be used with UpdatePolicy() to update a policy during read-modify-write.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_projects_policies_get_execute()` to send, or `orgpolicy_projects_policies_get` for simplest API.

pub fn orgpolicy_projects_policies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/projects/{}/policies/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/policies/{policiesId}
/// Gets a policy on a resource. If no policy is set on the resource, NOT_FOUND is returned. The entity tag (ETag) can be used with UpdatePolicy() to update a policy during read-modify-write.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_projects_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_projects_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_projects_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyProjectsPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/policies/{policiesId}
/// Gets a policy on a resource. If no policy is set on the resource, NOT_FOUND is returned. The entity tag (ETag) can be used with UpdatePolicy() to update a policy during read-modify-write.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_projects_policies_get_builder()` + `orgpolicy_projects_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_policies_get(
    client: &SimpleHttpClient,
    args: &OrgpolicyProjectsPoliciesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_projects_policies_get_builder(client, &args.name)?;
    orgpolicy_projects_policies_get_execute(builder)
}

/// GET v2/projects/{projectsId}/policies/{policiesId}:getEffectivePolicy
/// Gets the effective policy on a resource. This is the result of merging policies in the resource hierarchy and evaluating conditions. The returned policy will not have an ETag or condition set because it is an evaluated policy across multiple resources. Subtrees of Resource Manager resource hierarchy with 'under:' prefix will not be expanded.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_projects_policies_get_effective_policy_execute()` to send, or `orgpolicy_projects_policies_get_effective_policy` for simplest API.

pub fn orgpolicy_projects_policies_get_effective_policy_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/projects/{}/policies/{}:getEffectivePolicy",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/policies/{policiesId}:getEffectivePolicy
/// Gets the effective policy on a resource. This is the result of merging policies in the resource hierarchy and evaluating conditions. The returned policy will not have an ETag or condition set because it is an evaluated policy across multiple resources. Subtrees of Resource Manager resource hierarchy with 'under:' prefix will not be expanded.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_projects_policies_get_effective_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_projects_policies_get_effective_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_policies_get_effective_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_projects_policies_get_effective_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyProjectsPoliciesGetEffectivePolicyArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/policies/{policiesId}:getEffectivePolicy
/// Gets the effective policy on a resource. This is the result of merging policies in the resource hierarchy and evaluating conditions. The returned policy will not have an ETag or condition set because it is an evaluated policy across multiple resources. Subtrees of Resource Manager resource hierarchy with 'under:' prefix will not be expanded.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_projects_policies_get_effective_policy_builder()` + `orgpolicy_projects_policies_get_effective_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_policies_get_effective_policy(
    client: &SimpleHttpClient,
    args: &OrgpolicyProjectsPoliciesGetEffectivePolicyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_projects_policies_get_effective_policy_builder(client, &args.name)?;
    orgpolicy_projects_policies_get_effective_policy_execute(builder)
}

/// GET v2/projects/{projectsId}/policies
/// Retrieves all of the policies that exist on a particular resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_projects_policies_list_execute()` to send, or `orgpolicy_projects_policies_list` for simplest API.

pub fn orgpolicy_projects_policies_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/projects/{}/policies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/policies
/// Retrieves all of the policies that exist on a particular resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_projects_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_projects_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2ListPoliciesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_projects_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyProjectsPoliciesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/policies
/// Retrieves all of the policies that exist on a particular resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_projects_policies_list_builder()` + `orgpolicy_projects_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_policies_list(
    client: &SimpleHttpClient,
    args: &OrgpolicyProjectsPoliciesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2ListPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_projects_policies_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    orgpolicy_projects_policies_list_execute(builder)
}

/// GET v2/projects/{projectsId}/policies/{policiesId}
/// Updates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or the policy doesn't exist. Returns a google.rpc.Status with google.rpc.Code.ABORTED if the ETag supplied in the request doesn't match the persisted ETag of the policy. Note: the supplied policy will perform a full overwrite of all fields.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `orgpolicy_projects_policies_patch_execute()` to send, or `orgpolicy_projects_policies_patch` for simplest API.

pub fn orgpolicy_projects_policies_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudOrgpolicyV2Policy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://orgpolicy.googleapis.com/v2/projects/{}/policies/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/projects/{projectsId}/policies/{policiesId}
/// Updates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or the policy doesn't exist. Returns a google.rpc.Status with google.rpc.Code.ABORTED if the ETag supplied in the request doesn't match the persisted ETag of the policy. Note: the supplied policy will perform a full overwrite of all fields.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `orgpolicy_projects_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from orgpolicy_projects_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudOrgpolicyV2Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`orgpolicy_projects_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OrgpolicyProjectsPoliciesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudOrgpolicyV2Policy,
}

/// GET v2/projects/{projectsId}/policies/{policiesId}
/// Updates a policy. Returns a google.rpc.Status with google.rpc.Code.NOT_FOUND if the constraint or the policy doesn't exist. Returns a google.rpc.Status with google.rpc.Code.ABORTED if the ETag supplied in the request doesn't match the persisted ETag of the policy. Note: the supplied policy will perform a full overwrite of all fields.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `orgpolicy_projects_policies_patch_builder()` + `orgpolicy_projects_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn orgpolicy_projects_policies_patch(
    client: &SimpleHttpClient,
    args: &OrgpolicyProjectsPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudOrgpolicyV2Policy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = orgpolicy_projects_policies_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    orgpolicy_projects_policies_patch_execute(builder)
}
