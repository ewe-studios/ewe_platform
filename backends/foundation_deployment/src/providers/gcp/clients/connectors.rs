//! Auto-generated API clients for gcp/connectors.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v2/{+name}:checkReadiness
/// Reports readiness status of the connector. Similar logic to GetStatus but modified for kubernetes health check to understand.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_check_readiness_execute()` to send, or `connectors_projects_locations_connections_check_readiness` for simplest API.

pub fn connectors_projects_locations_connections_check_readiness_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:checkReadiness", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:checkReadiness
/// Reports readiness status of the connector. Similar logic to GetStatus but modified for kubernetes health check to understand.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_check_readiness_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_check_readiness_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_check_readiness_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckReadinessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckReadinessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:checkReadiness
/// Reports readiness status of the connector. Similar logic to GetStatus but modified for kubernetes health check to understand.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_check_readiness_builder()` + `connectors_projects_locations_connections_check_readiness_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_check_readiness(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckReadinessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_check_readiness_builder(client, name)?;
    connectors_projects_locations_connections_check_readiness_execute(builder)
}

/// GET v2/{+name}:checkStatus
/// Reports the status of the connection. Note that when the connection is in a state that is not `ACTIVE`, the implementation of this RPC method must return a Status with the corresponding State instead of returning a `gRPC` status code that is not "`OK`", which indicates that ConnectionStatus itself, not the connection, failed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_check_status_execute()` to send, or `connectors_projects_locations_connections_check_status` for simplest API.

pub fn connectors_projects_locations_connections_check_status_builder(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:checkStatus", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "executionConfig_headers",
            executionConfig_headers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}:checkStatus
/// Reports the status of the connection. Note that when the connection is in a state that is not `ACTIVE`, the implementation of this RPC method must return a Status with the corresponding State instead of returning a `gRPC` status code that is not "`OK`", which indicates that ConnectionStatus itself, not the connection, failed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_check_status_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_check_status_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_check_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:checkStatus
/// Reports the status of the connection. Note that when the connection is in a state that is not `ACTIVE`, the implementation of this RPC method must return a Status with the corresponding State instead of returning a `gRPC` status code that is not "`OK`", which indicates that ConnectionStatus itself, not the connection, failed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_check_status_builder()` + `connectors_projects_locations_connections_check_status_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_check_status(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_check_status_builder(
        client,
        name,
        executionConfig_headers,
    )?;
    connectors_projects_locations_connections_check_status_execute(builder)
}

/// GET v2/{+name}:exchangeAuthCode
/// ExchangeAuthCode exchanges the OAuth authorization code (and other necessary data) for an access token (and associated credentials).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_exchange_auth_code_execute()` to send, or `connectors_projects_locations_connections_exchange_auth_code` for simplest API.

pub fn connectors_projects_locations_connections_exchange_auth_code_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ExchangeAuthCodeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:exchangeAuthCode", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:exchangeAuthCode
/// ExchangeAuthCode exchanges the OAuth authorization code (and other necessary data) for an access token (and associated credentials).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_exchange_auth_code_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_exchange_auth_code_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_exchange_auth_code_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExchangeAuthCodeResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExchangeAuthCodeResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:exchangeAuthCode
/// ExchangeAuthCode exchanges the OAuth authorization code (and other necessary data) for an access token (and associated credentials).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_exchange_auth_code_builder()` + `connectors_projects_locations_connections_exchange_auth_code_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_exchange_auth_code(
    client: &SimpleHttpClient,
    name: &str,
    body: &ExchangeAuthCodeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExchangeAuthCodeResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        connectors_projects_locations_connections_exchange_auth_code_builder(client, name, body)?;
    connectors_projects_locations_connections_exchange_auth_code_execute(builder)
}

/// GET v2/{+connection}:executeSqlQuery
/// Executes a SQL statement specified in the body of the request. An example of this SQL statement in the case of Salesforce connector would be 'select * from Account a, Order o where a.Id = o.AccountId'.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_execute_sql_query_execute()` to send, or `connectors_projects_locations_connections_execute_sql_query` for simplest API.

pub fn connectors_projects_locations_connections_execute_sql_query_builder(
    client: &SimpleHttpClient,
    connection: &str,
    body: &ExecuteSqlQueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+connection}:executeSqlQuery",
        connection,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+connection}:executeSqlQuery
/// Executes a SQL statement specified in the body of the request. An example of this SQL statement in the case of Salesforce connector would be 'select * from Account a, Order o where a.Id = o.AccountId'.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_execute_sql_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_execute_sql_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_execute_sql_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecuteSqlQueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExecuteSqlQueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+connection}:executeSqlQuery
/// Executes a SQL statement specified in the body of the request. An example of this SQL statement in the case of Salesforce connector would be 'select * from Account a, Order o where a.Id = o.AccountId'.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_execute_sql_query_builder()` + `connectors_projects_locations_connections_execute_sql_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_execute_sql_query(
    client: &SimpleHttpClient,
    connection: &str,
    body: &ExecuteSqlQueryRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecuteSqlQueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_execute_sql_query_builder(
        client, connection, body,
    )?;
    connectors_projects_locations_connections_execute_sql_query_execute(builder)
}

/// GET v2/{+name}:generateConnectionToolspecOverride
/// Generate toolspec override for the given list of `toolNames`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_generate_connection_toolspec_override_execute()` to send, or `connectors_projects_locations_connections_generate_connection_toolspec_override` for simplest API.

pub fn connectors_projects_locations_connections_generate_connection_toolspec_override_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GenerateCustomToolspecRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+name}:generateConnectionToolspecOverride",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:generateConnectionToolspecOverride
/// Generate toolspec override for the given list of `toolNames`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_generate_connection_toolspec_override_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_generate_connection_toolspec_override_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_generate_connection_toolspec_override_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateCustomToolspecResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateCustomToolspecResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:generateConnectionToolspecOverride
/// Generate toolspec override for the given list of `toolNames`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_generate_connection_toolspec_override_builder()` + `connectors_projects_locations_connections_generate_connection_toolspec_override_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_generate_connection_toolspec_override(
    client: &SimpleHttpClient,
    name: &str,
    body: &GenerateCustomToolspecRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateCustomToolspecResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        connectors_projects_locations_connections_generate_connection_toolspec_override_builder(
            client, name, body,
        )?;
    connectors_projects_locations_connections_generate_connection_toolspec_override_execute(builder)
}

/// GET v2/{+name}:listCustomToolNames
/// Lists custom tool names.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_list_custom_tool_names_execute()` to send, or `connectors_projects_locations_connections_list_custom_tool_names` for simplest API.

pub fn connectors_projects_locations_connections_list_custom_tool_names_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+name}:listCustomToolNames",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:listCustomToolNames
/// Lists custom tool names.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_list_custom_tool_names_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_list_custom_tool_names_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_list_custom_tool_names_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCustomToolNamesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCustomToolNamesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:listCustomToolNames
/// Lists custom tool names.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_list_custom_tool_names_builder()` + `connectors_projects_locations_connections_list_custom_tool_names_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_list_custom_tool_names(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCustomToolNamesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        connectors_projects_locations_connections_list_custom_tool_names_builder(client, name)?;
    connectors_projects_locations_connections_list_custom_tool_names_execute(builder)
}

/// GET v2/{+name}:refreshAccessToken
/// RefreshAccessToken exchanges the OAuth refresh token (and other necessary data) for a new access token (and new associated credentials).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_refresh_access_token_execute()` to send, or `connectors_projects_locations_connections_refresh_access_token` for simplest API.

pub fn connectors_projects_locations_connections_refresh_access_token_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RefreshAccessTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:refreshAccessToken", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:refreshAccessToken
/// RefreshAccessToken exchanges the OAuth refresh token (and other necessary data) for a new access token (and new associated credentials).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_refresh_access_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_refresh_access_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_refresh_access_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RefreshAccessTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RefreshAccessTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:refreshAccessToken
/// RefreshAccessToken exchanges the OAuth refresh token (and other necessary data) for a new access token (and new associated credentials).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_refresh_access_token_builder()` + `connectors_projects_locations_connections_refresh_access_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_refresh_access_token(
    client: &SimpleHttpClient,
    name: &str,
    body: &RefreshAccessTokenRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RefreshAccessTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        connectors_projects_locations_connections_refresh_access_token_builder(client, name, body)?;
    connectors_projects_locations_connections_refresh_access_token_execute(builder)
}

/// GET v2/{+parent}/tools
/// Lists all available tools with POST.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_tools_execute()` to send, or `connectors_projects_locations_connections_tools` for simplest API.

pub fn connectors_projects_locations_connections_tools_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ListToolsPostRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/tools", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/tools
/// Lists all available tools with POST.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_tools_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_tools_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_tools_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListToolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListToolsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/tools
/// Lists all available tools with POST.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_tools_builder()` + `connectors_projects_locations_connections_tools_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_tools(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ListToolsPostRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListToolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_tools_builder(client, parent, body)?;
    connectors_projects_locations_connections_tools_execute(builder)
}

/// GET v2/{+name}:execute
/// Executes an action with the name specified in the request. The input parameters for executing the action are passed through the body of the ExecuteAction request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_actions_execute_execute()` to send, or `connectors_projects_locations_connections_actions_execute` for simplest API.

pub fn connectors_projects_locations_connections_actions_execute_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ExecuteActionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:execute", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:execute
/// Executes an action with the name specified in the request. The input parameters for executing the action are passed through the body of the ExecuteAction request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_actions_execute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_actions_execute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_actions_execute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecuteActionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExecuteActionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:execute
/// Executes an action with the name specified in the request. The input parameters for executing the action are passed through the body of the ExecuteAction request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_actions_execute_builder()` + `connectors_projects_locations_connections_actions_execute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_actions_execute(
    client: &SimpleHttpClient,
    name: &str,
    body: &ExecuteActionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecuteActionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        connectors_projects_locations_connections_actions_execute_builder(client, name, body)?;
    connectors_projects_locations_connections_actions_execute_execute(builder)
}

/// GET v2/{+name}
/// Gets the schema of the given action.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_actions_get_execute()` to send, or `connectors_projects_locations_connections_actions_get` for simplest API.

pub fn connectors_projects_locations_connections_actions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "executionConfig_headers",
            executionConfig_headers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}
/// Gets the schema of the given action.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_actions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_actions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_actions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Action>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Action = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the schema of the given action.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_actions_get_builder()` + `connectors_projects_locations_connections_actions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_actions_get(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Action>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_actions_get_builder(
        client,
        name,
        executionConfig_headers,
        view,
    )?;
    connectors_projects_locations_connections_actions_get_execute(builder)
}

/// GET v2/{+parent}/actions
/// Gets the schema of all the actions supported by the connector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_actions_list_execute()` to send, or `connectors_projects_locations_connections_actions_list` for simplest API.

pub fn connectors_projects_locations_connections_actions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    executionConfig_headers: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/actions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "executionConfig_headers",
            executionConfig_headers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/actions
/// Gets the schema of all the actions supported by the connector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_actions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_actions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_actions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListActionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListActionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/actions
/// Gets the schema of all the actions supported by the connector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_actions_list_builder()` + `connectors_projects_locations_connections_actions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_actions_list(
    client: &SimpleHttpClient,
    parent: &str,
    executionConfig_headers: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListActionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_actions_list_builder(
        client,
        parent,
        executionConfig_headers,
        pageSize,
        pageToken,
        view,
    )?;
    connectors_projects_locations_connections_actions_list_execute(builder)
}

/// GET v2/{+name}
/// Gets metadata of given entity type
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_get_execute()` to send, or `connectors_projects_locations_connections_entity_types_get` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    contextMetadata: Option<&str>,
    executionConfig_headers: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "contextMetadata",
            contextMetadata
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "executionConfig_headers",
            executionConfig_headers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}
/// Gets metadata of given entity type
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_entity_types_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_entity_types_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntityType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets metadata of given entity type
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_get_builder()` + `connectors_projects_locations_connections_entity_types_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_get(
    client: &SimpleHttpClient,
    name: &str,
    contextMetadata: Option<&str>,
    executionConfig_headers: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_get_builder(
        client,
        name,
        contextMetadata,
        executionConfig_headers,
        view,
    )?;
    connectors_projects_locations_connections_entity_types_get_execute(builder)
}

/// GET v2/{+parent}/entityTypes
/// Lists metadata related to all entity types present in the external system.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_list_execute()` to send, or `connectors_projects_locations_connections_entity_types_list` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    executionConfig_headers: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/entityTypes", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "executionConfig_headers",
            executionConfig_headers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/entityTypes
/// Lists metadata related to all entity types present in the external system.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_entity_types_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_entity_types_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEntityTypesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEntityTypesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/entityTypes
/// Lists metadata related to all entity types present in the external system.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_list_builder()` + `connectors_projects_locations_connections_entity_types_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_list(
    client: &SimpleHttpClient,
    parent: &str,
    executionConfig_headers: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEntityTypesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_list_builder(
        client,
        parent,
        executionConfig_headers,
        pageSize,
        pageToken,
        view,
    )?;
    connectors_projects_locations_connections_entity_types_list_execute(builder)
}

/// GET v2/{+parent}/entities
/// Creates a new entity row of the specified entity type in the external system. The field values for creating the row are contained in the body of the request. The response message contains a Entity message object returned as a response by the external system.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_entities_create_execute()` to send, or `connectors_projects_locations_connections_entity_types_entities_create` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_entities_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    executionConfig_headers: Option<&str>,
    body: &Entity,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/entities", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "executionConfig_headers",
            executionConfig_headers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/entities
/// Creates a new entity row of the specified entity type in the external system. The field values for creating the row are contained in the body of the request. The response message contains a Entity message object returned as a response by the external system.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_entity_types_entities_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entity>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Entity = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/entities
/// Creates a new entity row of the specified entity type in the external system. The field values for creating the row are contained in the body of the request. The response message contains a Entity message object returned as a response by the external system.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_create_builder()` + `connectors_projects_locations_connections_entity_types_entities_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_create(
    client: &SimpleHttpClient,
    parent: &str,
    executionConfig_headers: Option<&str>,
    body: &Entity,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entity>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_entities_create_builder(
        client,
        parent,
        executionConfig_headers,
        body,
    )?;
    connectors_projects_locations_connections_entity_types_entities_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes an existing entity row matching the entity type and entity id specified in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_entities_delete_execute()` to send, or `connectors_projects_locations_connections_entity_types_entities_delete` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_entities_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "executionConfig_headers",
            executionConfig_headers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}
/// Deletes an existing entity row matching the entity type and entity id specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_entity_types_entities_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an existing entity row matching the entity type and entity id specified in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_delete_builder()` + `connectors_projects_locations_connections_entity_types_entities_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_delete(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_entities_delete_builder(
        client,
        name,
        executionConfig_headers,
    )?;
    connectors_projects_locations_connections_entity_types_entities_delete_execute(builder)
}

/// GET v2/{+entityType}/entities:deleteEntitiesWithConditions
/// Deletes entities based on conditions specified in the request and not on entity id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_execute()` to send, or `connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_builder(
    client: &SimpleHttpClient,
    entityType: &str,
    conditions: Option<&str>,
    executionConfig_headers: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+entityType}/entities:deleteEntitiesWithConditions",
        entityType,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "conditions",
            conditions
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "executionConfig_headers",
            executionConfig_headers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+entityType}/entities:deleteEntitiesWithConditions
/// Deletes entities based on conditions specified in the request and not on entity id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+entityType}/entities:deleteEntitiesWithConditions
/// Deletes entities based on conditions specified in the request and not on entity id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_builder()` + `connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions(
    client: &SimpleHttpClient,
    entityType: &str,
    conditions: Option<&str>,
    executionConfig_headers: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_builder(client, entityType, conditions, executionConfig_headers)?;
    connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_execute(builder)
}

/// GET v2/{+name}
/// Gets a single entity row matching the entity type and entity id specified in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_entities_get_execute()` to send, or `connectors_projects_locations_connections_entity_types_entities_get` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_entities_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "executionConfig_headers",
            executionConfig_headers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}
/// Gets a single entity row matching the entity type and entity id specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_entity_types_entities_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entity>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Entity = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a single entity row matching the entity type and entity id specified in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_get_builder()` + `connectors_projects_locations_connections_entity_types_entities_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_get(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entity>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_entities_get_builder(
        client,
        name,
        executionConfig_headers,
    )?;
    connectors_projects_locations_connections_entity_types_entities_get_execute(builder)
}

/// GET v2/{+parent}/entities
/// Lists entity rows of a particular entity type contained in the request. Note: 1. Currently, only max of one 'sort_by' column is supported. 2. If no 'sort_by' column is provided, the primary key of the table is used. If zero or more than one primary key is available, we default to the unpaginated list entities logic which only returns the first page. 3. The values of the 'sort_by' columns must uniquely identify an entity row, otherwise undefined behaviors may be observed during pagination. 4. Since transactions are not supported, any updates, inserts or deletes during pagination can lead to stale data being returned or other unexpected behaviors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_entities_list_execute()` to send, or `connectors_projects_locations_connections_entity_types_entities_list` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_entities_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    conditions: Option<&str>,
    executionConfig_headers: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    sortBy: Option<&str>,
    sortOrder: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/entities", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "conditions",
            conditions
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "executionConfig_headers",
            executionConfig_headers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sortBy",
            sortBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sortOrder",
            sortOrder
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/entities
/// Lists entity rows of a particular entity type contained in the request. Note: 1. Currently, only max of one 'sort_by' column is supported. 2. If no 'sort_by' column is provided, the primary key of the table is used. If zero or more than one primary key is available, we default to the unpaginated list entities logic which only returns the first page. 3. The values of the 'sort_by' columns must uniquely identify an entity row, otherwise undefined behaviors may be observed during pagination. 4. Since transactions are not supported, any updates, inserts or deletes during pagination can lead to stale data being returned or other unexpected behaviors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_entity_types_entities_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEntitiesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEntitiesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/entities
/// Lists entity rows of a particular entity type contained in the request. Note: 1. Currently, only max of one 'sort_by' column is supported. 2. If no 'sort_by' column is provided, the primary key of the table is used. If zero or more than one primary key is available, we default to the unpaginated list entities logic which only returns the first page. 3. The values of the 'sort_by' columns must uniquely identify an entity row, otherwise undefined behaviors may be observed during pagination. 4. Since transactions are not supported, any updates, inserts or deletes during pagination can lead to stale data being returned or other unexpected behaviors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_list_builder()` + `connectors_projects_locations_connections_entity_types_entities_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_list(
    client: &SimpleHttpClient,
    parent: &str,
    conditions: Option<&str>,
    executionConfig_headers: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    sortBy: Option<&str>,
    sortOrder: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEntitiesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_entities_list_builder(
        client,
        parent,
        conditions,
        executionConfig_headers,
        pageSize,
        pageToken,
        sortBy,
        sortOrder,
    )?;
    connectors_projects_locations_connections_entity_types_entities_list_execute(builder)
}

/// GET v2/{+name}
/// Updates an existing entity row matching the entity type and entity id specified in the request. The fields in the entity row that need to be modified are contained in the body of the request. All unspecified fields are left unchanged. The response message contains a Entity message object returned as a response by the external system.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_entities_patch_execute()` to send, or `connectors_projects_locations_connections_entity_types_entities_patch` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_entities_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
    body: &Entity,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "executionConfig_headers",
            executionConfig_headers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates an existing entity row matching the entity type and entity id specified in the request. The fields in the entity row that need to be modified are contained in the body of the request. All unspecified fields are left unchanged. The response message contains a Entity message object returned as a response by the external system.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_entity_types_entities_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entity>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Entity = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates an existing entity row matching the entity type and entity id specified in the request. The fields in the entity row that need to be modified are contained in the body of the request. All unspecified fields are left unchanged. The response message contains a Entity message object returned as a response by the external system.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_patch_builder()` + `connectors_projects_locations_connections_entity_types_entities_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_patch(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
    body: &Entity,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entity>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_entities_patch_builder(
        client,
        name,
        executionConfig_headers,
        body,
    )?;
    connectors_projects_locations_connections_entity_types_entities_patch_execute(builder)
}

/// GET v2/{+entityType}/entities:updateEntitiesWithConditions
/// Updates entities based on conditions specified in the request and not on entity id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_execute()` to send, or `connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_builder(
    client: &SimpleHttpClient,
    entityType: &str,
    conditions: Option<&str>,
    executionConfig_headers: Option<&str>,
    body: &Entity,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+entityType}/entities:updateEntitiesWithConditions",
        entityType,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "conditions",
            conditions
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "executionConfig_headers",
            executionConfig_headers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+entityType}/entities:updateEntitiesWithConditions
/// Updates entities based on conditions specified in the request and not on entity id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateEntitiesWithConditionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateEntitiesWithConditionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+entityType}/entities:updateEntitiesWithConditions
/// Updates entities based on conditions specified in the request and not on entity id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_builder()` + `connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions(
    client: &SimpleHttpClient,
    entityType: &str,
    conditions: Option<&str>,
    executionConfig_headers: Option<&str>,
    body: &Entity,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateEntitiesWithConditionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_builder(client, entityType, conditions, executionConfig_headers, body)?;
    connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_execute(builder)
}

/// GET v2/{+name}
/// Gets a specific resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_resources_get_execute()` to send, or `connectors_projects_locations_connections_resources_get` for simplest API.

pub fn connectors_projects_locations_connections_resources_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "executionConfig_headers",
            executionConfig_headers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}
/// Gets a specific resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_resources_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_resources_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_resources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetResourceResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetResourceResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a specific resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_resources_get_builder()` + `connectors_projects_locations_connections_resources_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_resources_get(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetResourceResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_resources_get_builder(
        client,
        name,
        executionConfig_headers,
    )?;
    connectors_projects_locations_connections_resources_get_execute(builder)
}

/// GET v2/{+name}
/// Gets a specific resource with POST.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_resources_get_resource_post_execute()` to send, or `connectors_projects_locations_connections_resources_get_resource_post` for simplest API.

pub fn connectors_projects_locations_connections_resources_get_resource_post_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GetResourcePostRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Gets a specific resource with POST.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_resources_get_resource_post_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_resources_get_resource_post_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_resources_get_resource_post_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetResourceResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetResourceResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a specific resource with POST.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_resources_get_resource_post_builder()` + `connectors_projects_locations_connections_resources_get_resource_post_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_resources_get_resource_post(
    client: &SimpleHttpClient,
    name: &str,
    body: &GetResourcePostRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetResourceResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_resources_get_resource_post_builder(
        client, name, body,
    )?;
    connectors_projects_locations_connections_resources_get_resource_post_execute(builder)
}

/// GET v2/{+parent}/resources
/// Lists all available resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_resources_list_execute()` to send, or `connectors_projects_locations_connections_resources_list` for simplest API.

pub fn connectors_projects_locations_connections_resources_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    executionConfig_headers: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/resources", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "executionConfig_headers",
            executionConfig_headers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/resources
/// Lists all available resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_resources_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_resources_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_resources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListResourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListResourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/resources
/// Lists all available resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_resources_list_builder()` + `connectors_projects_locations_connections_resources_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_resources_list(
    client: &SimpleHttpClient,
    parent: &str,
    executionConfig_headers: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListResourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_resources_list_builder(
        client,
        parent,
        executionConfig_headers,
        pageSize,
        pageToken,
    )?;
    connectors_projects_locations_connections_resources_list_execute(builder)
}

/// GET v2/{+name}:execute
/// Executes a specific tool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_tools_execute_execute()` to send, or `connectors_projects_locations_connections_tools_execute` for simplest API.

pub fn connectors_projects_locations_connections_tools_execute_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ExecuteToolRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:execute", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:execute
/// Executes a specific tool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_tools_execute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_tools_execute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_tools_execute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecuteToolResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExecuteToolResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:execute
/// Executes a specific tool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_tools_execute_builder()` + `connectors_projects_locations_connections_tools_execute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_tools_execute(
    client: &SimpleHttpClient,
    name: &str,
    body: &ExecuteToolRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecuteToolResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        connectors_projects_locations_connections_tools_execute_builder(client, name, body)?;
    connectors_projects_locations_connections_tools_execute_execute(builder)
}

/// GET v2/{+parent}/tools
/// Lists all available tools.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_tools_list_execute()` to send, or `connectors_projects_locations_connections_tools_list` for simplest API.

pub fn connectors_projects_locations_connections_tools_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    executionConfig_headers: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/tools", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "executionConfig_headers",
            executionConfig_headers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/tools
/// Lists all available tools.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `connectors_projects_locations_connections_tools_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from connectors_projects_locations_connections_tools_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_tools_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListToolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListToolsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/tools
/// Lists all available tools.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_tools_list_builder()` + `connectors_projects_locations_connections_tools_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_tools_list(
    client: &SimpleHttpClient,
    parent: &str,
    executionConfig_headers: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListToolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_tools_list_builder(
        client,
        parent,
        executionConfig_headers,
        pageSize,
        pageToken,
    )?;
    connectors_projects_locations_connections_tools_list_execute(builder)
}
