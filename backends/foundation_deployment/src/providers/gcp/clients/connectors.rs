//! Auto-generated API clients for gcp/connectors.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:checkReadiness
/// Reports readiness status of the connector. Similar logic to GetStatus but modified for kubernetes health check to understand.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_check_readiness_execute()` to send, or `connectors_projects_locations_connections_check_readiness` for simplest API.

pub fn connectors_projects_locations_connections_check_readiness_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}:checkReadiness",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:checkReadiness
/// Reports readiness status of the connector. Similar logic to GetStatus but modified for kubernetes health check to understand.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_check_readiness_execute()` or `connectors_projects_locations_connections_check_readiness`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_check_readiness_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_check_readiness_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<CheckReadinessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckReadinessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:checkReadiness
/// Reports readiness status of the connector. Similar logic to GetStatus but modified for kubernetes health check to understand.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_check_readiness_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_check_readiness_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_check_readiness()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_check_readiness_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_check_readiness_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckReadinessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_check_readiness_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_check_readiness`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsCheckReadinessArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:checkReadiness
/// Reports readiness status of the connector. Similar logic to GetStatus but modified for kubernetes health check to understand.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_check_readiness_builder()` + `connectors_projects_locations_connections_check_readiness_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_check_readiness_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_check_readiness(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsCheckReadinessArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckReadinessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        connectors_projects_locations_connections_check_readiness_builder(client, &args.name)?;
    connectors_projects_locations_connections_check_readiness_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:checkStatus
/// Reports the status of the connection. Note that when the connection is in a state that is not `ACTIVE`, the implementation of this RPC method must return a Status with the corresponding State instead of returning a `gRPC` status code that is not "`OK`", which indicates that ConnectionStatus itself, not the connection, failed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_check_status_execute()` to send, or `connectors_projects_locations_connections_check_status` for simplest API.

pub fn connectors_projects_locations_connections_check_status_builder(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}:checkStatus",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = executionConfig_headers {
        query_parts.push(format!("executionConfig_headers={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:checkStatus
/// Reports the status of the connection. Note that when the connection is in a state that is not `ACTIVE`, the implementation of this RPC method must return a Status with the corresponding State instead of returning a `gRPC` status code that is not "`OK`", which indicates that ConnectionStatus itself, not the connection, failed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_check_status_execute()` or `connectors_projects_locations_connections_check_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_check_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_check_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<CheckStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:checkStatus
/// Reports the status of the connection. Note that when the connection is in a state that is not `ACTIVE`, the implementation of this RPC method must return a Status with the corresponding State instead of returning a `gRPC` status code that is not "`OK`", which indicates that ConnectionStatus itself, not the connection, failed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_check_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_check_status_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_check_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_check_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_check_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_check_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_check_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsCheckStatusArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: executionConfig_headers
    pub executionConfig_headers: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:checkStatus
/// Reports the status of the connection. Note that when the connection is in a state that is not `ACTIVE`, the implementation of this RPC method must return a Status with the corresponding State instead of returning a `gRPC` status code that is not "`OK`", which indicates that ConnectionStatus itself, not the connection, failed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_check_status_builder()` + `connectors_projects_locations_connections_check_status_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_check_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_check_status(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsCheckStatusArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_check_status_builder(
        client,
        &args.name,
        args.executionConfig_headers.as_deref(),
    )?;
    connectors_projects_locations_connections_check_status_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:exchangeAuthCode
/// ExchangeAuthCode exchanges the OAuth authorization code (and other necessary data) for an access token (and associated credentials).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_exchange_auth_code_execute()` to send, or `connectors_projects_locations_connections_exchange_auth_code` for simplest API.

pub fn connectors_projects_locations_connections_exchange_auth_code_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ExchangeAuthCodeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}:exchangeAuthCode",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:exchangeAuthCode
/// ExchangeAuthCode exchanges the OAuth authorization code (and other necessary data) for an access token (and associated credentials).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_exchange_auth_code_execute()` or `connectors_projects_locations_connections_exchange_auth_code`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_exchange_auth_code_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_exchange_auth_code_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ExchangeAuthCodeResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExchangeAuthCodeResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:exchangeAuthCode
/// ExchangeAuthCode exchanges the OAuth authorization code (and other necessary data) for an access token (and associated credentials).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_exchange_auth_code_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_exchange_auth_code_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_exchange_auth_code()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_exchange_auth_code_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_exchange_auth_code_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExchangeAuthCodeResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_exchange_auth_code_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_exchange_auth_code`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsExchangeAuthCodeArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ExchangeAuthCodeRequest,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:exchangeAuthCode
/// ExchangeAuthCode exchanges the OAuth authorization code (and other necessary data) for an access token (and associated credentials).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_exchange_auth_code_builder()` + `connectors_projects_locations_connections_exchange_auth_code_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_exchange_auth_code_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_exchange_auth_code(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsExchangeAuthCodeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExchangeAuthCodeResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_exchange_auth_code_builder(
        client, &args.name, &args.body,
    )?;
    connectors_projects_locations_connections_exchange_auth_code_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:executeSqlQuery
/// Executes a SQL statement specified in the body of the request. An example of this SQL statement in the case of Salesforce connector would be 'select * from Account a, Order o where a.Id = o.AccountId'.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_execute_sql_query_execute()` to send, or `connectors_projects_locations_connections_execute_sql_query` for simplest API.

pub fn connectors_projects_locations_connections_execute_sql_query_builder(
    client: &SimpleHttpClient,
    connection: &str,
    body: &ExecuteSqlQueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}:executeSqlQuery",
        connection,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:executeSqlQuery
/// Executes a SQL statement specified in the body of the request. An example of this SQL statement in the case of Salesforce connector would be 'select * from Account a, Order o where a.Id = o.AccountId'.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_execute_sql_query_execute()` or `connectors_projects_locations_connections_execute_sql_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_execute_sql_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_execute_sql_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ExecuteSqlQueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExecuteSqlQueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:executeSqlQuery
/// Executes a SQL statement specified in the body of the request. An example of this SQL statement in the case of Salesforce connector would be 'select * from Account a, Order o where a.Id = o.AccountId'.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_execute_sql_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_execute_sql_query_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_execute_sql_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_execute_sql_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_execute_sql_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecuteSqlQueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_execute_sql_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_execute_sql_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsExecuteSqlQueryArgs {
    /// Path parameter: connection
    pub connection: String,
    /// Request body.
    pub body: ExecuteSqlQueryRequest,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:executeSqlQuery
/// Executes a SQL statement specified in the body of the request. An example of this SQL statement in the case of Salesforce connector would be 'select * from Account a, Order o where a.Id = o.AccountId'.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_execute_sql_query_builder()` + `connectors_projects_locations_connections_execute_sql_query_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_execute_sql_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_execute_sql_query(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsExecuteSqlQueryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecuteSqlQueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_execute_sql_query_builder(
        client,
        &args.connection,
        &args.body,
    )?;
    connectors_projects_locations_connections_execute_sql_query_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:generateConnectionToolspecOverride
/// Generate toolspec override for the given list of `toolNames`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_generate_connection_toolspec_override_execute()` to send, or `connectors_projects_locations_connections_generate_connection_toolspec_override` for simplest API.

pub fn connectors_projects_locations_connections_generate_connection_toolspec_override_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GenerateCustomToolspecRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}:generateConnectionToolspecOverride",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:generateConnectionToolspecOverride
/// Generate toolspec override for the given list of `toolNames`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_generate_connection_toolspec_override_execute()` or `connectors_projects_locations_connections_generate_connection_toolspec_override`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_generate_connection_toolspec_override_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_generate_connection_toolspec_override_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GenerateCustomToolspecResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateCustomToolspecResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:generateConnectionToolspecOverride
/// Generate toolspec override for the given list of `toolNames`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_generate_connection_toolspec_override_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_generate_connection_toolspec_override_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_generate_connection_toolspec_override()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_generate_connection_toolspec_override_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_generate_connection_toolspec_override_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateCustomToolspecResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        connectors_projects_locations_connections_generate_connection_toolspec_override_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_generate_connection_toolspec_override`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsGenerateConnectionToolspecOverrideArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GenerateCustomToolspecRequest,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:generateConnectionToolspecOverride
/// Generate toolspec override for the given list of `toolNames`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_generate_connection_toolspec_override_builder()` + `connectors_projects_locations_connections_generate_connection_toolspec_override_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_generate_connection_toolspec_override_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_generate_connection_toolspec_override(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsGenerateConnectionToolspecOverrideArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateCustomToolspecResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        connectors_projects_locations_connections_generate_connection_toolspec_override_builder(
            client, &args.name, &args.body,
        )?;
    connectors_projects_locations_connections_generate_connection_toolspec_override_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:listCustomToolNames
/// Lists custom tool names.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_list_custom_tool_names_execute()` to send, or `connectors_projects_locations_connections_list_custom_tool_names` for simplest API.

pub fn connectors_projects_locations_connections_list_custom_tool_names_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}:listCustomToolNames",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:listCustomToolNames
/// Lists custom tool names.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_list_custom_tool_names_execute()` or `connectors_projects_locations_connections_list_custom_tool_names`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_list_custom_tool_names_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_list_custom_tool_names_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListCustomToolNamesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCustomToolNamesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:listCustomToolNames
/// Lists custom tool names.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_list_custom_tool_names_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_list_custom_tool_names_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_list_custom_tool_names()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_list_custom_tool_names_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_list_custom_tool_names_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCustomToolNamesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_list_custom_tool_names_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_list_custom_tool_names`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsListCustomToolNamesArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:listCustomToolNames
/// Lists custom tool names.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_list_custom_tool_names_builder()` + `connectors_projects_locations_connections_list_custom_tool_names_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_list_custom_tool_names_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_list_custom_tool_names(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsListCustomToolNamesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCustomToolNamesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_list_custom_tool_names_builder(
        client, &args.name,
    )?;
    connectors_projects_locations_connections_list_custom_tool_names_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:refreshAccessToken
/// RefreshAccessToken exchanges the OAuth refresh token (and other necessary data) for a new access token (and new associated credentials).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_refresh_access_token_execute()` to send, or `connectors_projects_locations_connections_refresh_access_token` for simplest API.

pub fn connectors_projects_locations_connections_refresh_access_token_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RefreshAccessTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}:refreshAccessToken",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:refreshAccessToken
/// RefreshAccessToken exchanges the OAuth refresh token (and other necessary data) for a new access token (and new associated credentials).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_refresh_access_token_execute()` or `connectors_projects_locations_connections_refresh_access_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_refresh_access_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_refresh_access_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<RefreshAccessTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RefreshAccessTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:refreshAccessToken
/// RefreshAccessToken exchanges the OAuth refresh token (and other necessary data) for a new access token (and new associated credentials).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_refresh_access_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_refresh_access_token_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_refresh_access_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_refresh_access_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_refresh_access_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RefreshAccessTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_refresh_access_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_refresh_access_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsRefreshAccessTokenArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: RefreshAccessTokenRequest,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:refreshAccessToken
/// RefreshAccessToken exchanges the OAuth refresh token (and other necessary data) for a new access token (and new associated credentials).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_refresh_access_token_builder()` + `connectors_projects_locations_connections_refresh_access_token_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_refresh_access_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_refresh_access_token(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsRefreshAccessTokenArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RefreshAccessTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_refresh_access_token_builder(
        client, &args.name, &args.body,
    )?;
    connectors_projects_locations_connections_refresh_access_token_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/tools
/// Lists all available tools with POST.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_tools_execute()` to send, or `connectors_projects_locations_connections_tools` for simplest API.

pub fn connectors_projects_locations_connections_tools_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ListToolsPostRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/tools",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/tools
/// Lists all available tools with POST.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_tools_execute()` or `connectors_projects_locations_connections_tools`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_tools_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_tools_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListToolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListToolsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/tools
/// Lists all available tools with POST.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_tools_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_tools_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_tools()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_tools_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_tools_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListToolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_tools_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_tools`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsToolsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: ListToolsPostRequest,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/tools
/// Lists all available tools with POST.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_tools_builder()` + `connectors_projects_locations_connections_tools_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_tools_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_tools(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsToolsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListToolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        connectors_projects_locations_connections_tools_builder(client, &args.parent, &args.body)?;
    connectors_projects_locations_connections_tools_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/actions/{actionsId}:execute
/// Executes an action with the name specified in the request. The input parameters for executing the action are passed through the body of the ExecuteAction request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_actions_execute_execute()` to send, or `connectors_projects_locations_connections_actions_execute` for simplest API.

pub fn connectors_projects_locations_connections_actions_execute_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ExecuteActionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/actions/{}:execute",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/actions/{actionsId}:execute
/// Executes an action with the name specified in the request. The input parameters for executing the action are passed through the body of the ExecuteAction request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_actions_execute_execute()` or `connectors_projects_locations_connections_actions_execute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_actions_execute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_actions_execute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ExecuteActionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExecuteActionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/actions/{actionsId}:execute
/// Executes an action with the name specified in the request. The input parameters for executing the action are passed through the body of the ExecuteAction request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_actions_execute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_actions_execute_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_actions_execute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_actions_execute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_actions_execute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecuteActionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_actions_execute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_actions_execute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsActionsExecuteArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ExecuteActionRequest,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/actions/{actionsId}:execute
/// Executes an action with the name specified in the request. The input parameters for executing the action are passed through the body of the ExecuteAction request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_actions_execute_builder()` + `connectors_projects_locations_connections_actions_execute_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_actions_execute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_actions_execute(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsActionsExecuteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecuteActionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_actions_execute_builder(
        client, &args.name, &args.body,
    )?;
    connectors_projects_locations_connections_actions_execute_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/actions/{actionsId}
/// Gets the schema of the given action.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_actions_get_execute()` to send, or `connectors_projects_locations_connections_actions_get` for simplest API.

pub fn connectors_projects_locations_connections_actions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/actions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = executionConfig_headers {
        query_parts.push(format!("executionConfig_headers={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/actions/{actionsId}
/// Gets the schema of the given action.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_actions_get_execute()` or `connectors_projects_locations_connections_actions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_actions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_actions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Action>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Action = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/actions/{actionsId}
/// Gets the schema of the given action.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_actions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_actions_get_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_actions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_actions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_actions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Action>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_actions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_actions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsActionsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: executionConfig_headers
    pub executionConfig_headers: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/actions/{actionsId}
/// Gets the schema of the given action.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_actions_get_builder()` + `connectors_projects_locations_connections_actions_get_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_actions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_actions_get(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsActionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Action>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_actions_get_builder(
        client,
        &args.name,
        args.executionConfig_headers.as_deref(),
        args.view.as_deref(),
    )?;
    connectors_projects_locations_connections_actions_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/actions
/// Gets the schema of all the actions supported by the connector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_actions_list_execute()` to send, or `connectors_projects_locations_connections_actions_list` for simplest API.

pub fn connectors_projects_locations_connections_actions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    executionConfig_headers: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/actions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = executionConfig_headers {
        query_parts.push(format!("executionConfig_headers={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/actions
/// Gets the schema of all the actions supported by the connector.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_actions_list_execute()` or `connectors_projects_locations_connections_actions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_actions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_actions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListActionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListActionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/actions
/// Gets the schema of all the actions supported by the connector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_actions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_actions_list_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_actions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_actions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_actions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListActionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_actions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_actions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsActionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: executionConfig_headers
    pub executionConfig_headers: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/actions
/// Gets the schema of all the actions supported by the connector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_actions_list_builder()` + `connectors_projects_locations_connections_actions_list_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_actions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_actions_list(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsActionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListActionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_actions_list_builder(
        client,
        &args.parent,
        args.executionConfig_headers.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.view.as_deref(),
    )?;
    connectors_projects_locations_connections_actions_list_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}
/// Gets metadata of given entity type
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_get_execute()` to send, or `connectors_projects_locations_connections_entity_types_get` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    contextMetadata: Option<&str>,
    executionConfig_headers: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/entityTypes/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = contextMetadata {
        query_parts.push(format!("contextMetadata={}", val));
    }
    if let Some(val) = executionConfig_headers {
        query_parts.push(format!("executionConfig_headers={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}
/// Gets metadata of given entity type
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_entity_types_get_execute()` or `connectors_projects_locations_connections_entity_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<EntityType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntityType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}
/// Gets metadata of given entity type
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_entity_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_get_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_entity_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_entity_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_entity_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_entity_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: contextMetadata
    pub contextMetadata: Option<String>,
    /// Query parameter: executionConfig_headers
    pub executionConfig_headers: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}
/// Gets metadata of given entity type
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_get_builder()` + `connectors_projects_locations_connections_entity_types_get_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_get(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_get_builder(
        client,
        &args.name,
        args.contextMetadata.as_deref(),
        args.executionConfig_headers.as_deref(),
        args.view.as_deref(),
    )?;
    connectors_projects_locations_connections_entity_types_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes
/// Lists metadata related to all entity types present in the external system.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_list_execute()` to send, or `connectors_projects_locations_connections_entity_types_list` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    executionConfig_headers: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/entityTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = executionConfig_headers {
        query_parts.push(format!("executionConfig_headers={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes
/// Lists metadata related to all entity types present in the external system.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_entity_types_list_execute()` or `connectors_projects_locations_connections_entity_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListEntityTypesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEntityTypesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes
/// Lists metadata related to all entity types present in the external system.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_entity_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_list_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_entity_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_entity_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEntityTypesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_entity_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_entity_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: executionConfig_headers
    pub executionConfig_headers: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes
/// Lists metadata related to all entity types present in the external system.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_list_builder()` + `connectors_projects_locations_connections_entity_types_list_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_list(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEntityTypesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_list_builder(
        client,
        &args.parent,
        args.executionConfig_headers.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.view.as_deref(),
    )?;
    connectors_projects_locations_connections_entity_types_list_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities
/// Creates a new entity row of the specified entity type in the external system. The field values for creating the row are contained in the body of the request. The response message contains a Entity message object returned as a response by the external system.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_entities_create_execute()` to send, or `connectors_projects_locations_connections_entity_types_entities_create` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_entities_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    executionConfig_headers: Option<&str>,
    body: &Entity,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/entityTypes/{}/entities",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = executionConfig_headers {
        query_parts.push(format!("executionConfig_headers={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities
/// Creates a new entity row of the specified entity type in the external system. The field values for creating the row are contained in the body of the request. The response message contains a Entity message object returned as a response by the external system.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_entity_types_entities_create_execute()` or `connectors_projects_locations_connections_entity_types_entities_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_entities_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Entity>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Entity = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities
/// Creates a new entity row of the specified entity type in the external system. The field values for creating the row are contained in the body of the request. The response message contains a Entity message object returned as a response by the external system.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_entity_types_entities_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_entities_create_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_entity_types_entities_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_entities_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_entity_types_entities_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entity>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        connectors_projects_locations_connections_entity_types_entities_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_entity_types_entities_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: executionConfig_headers
    pub executionConfig_headers: Option<String>,
    /// Request body.
    pub body: Entity,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities
/// Creates a new entity row of the specified entity type in the external system. The field values for creating the row are contained in the body of the request. The response message contains a Entity message object returned as a response by the external system.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_create_builder()` + `connectors_projects_locations_connections_entity_types_entities_create_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_entities_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_create(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entity>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_entities_create_builder(
        client,
        &args.parent,
        args.executionConfig_headers.as_deref(),
        &args.body,
    )?;
    connectors_projects_locations_connections_entity_types_entities_create_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities/{entitiesId}
/// Deletes an existing entity row matching the entity type and entity id specified in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_entities_delete_execute()` to send, or `connectors_projects_locations_connections_entity_types_entities_delete` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_entities_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/entityTypes/{}/entities/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = executionConfig_headers {
        query_parts.push(format!("executionConfig_headers={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities/{entitiesId}
/// Deletes an existing entity row matching the entity type and entity id specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_entity_types_entities_delete_execute()` or `connectors_projects_locations_connections_entity_types_entities_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_entities_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities/{entitiesId}
/// Deletes an existing entity row matching the entity type and entity id specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_entity_types_entities_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_entities_delete_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_entity_types_entities_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_entities_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_entity_types_entities_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        connectors_projects_locations_connections_entity_types_entities_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_entity_types_entities_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: executionConfig_headers
    pub executionConfig_headers: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities/{entitiesId}
/// Deletes an existing entity row matching the entity type and entity id specified in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_delete_builder()` + `connectors_projects_locations_connections_entity_types_entities_delete_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_entities_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_delete(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_entities_delete_builder(
        client,
        &args.name,
        args.executionConfig_headers.as_deref(),
    )?;
    connectors_projects_locations_connections_entity_types_entities_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities:deleteEntitiesWithConditions
/// Deletes entities based on conditions specified in the request and not on entity id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_execute()` to send, or `connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_builder(
    client: &SimpleHttpClient,
    entityType: &str,
    conditions: Option<&str>,
    executionConfig_headers: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/entityTypes/{}/entities:deleteEntitiesWithConditions",
        entityType,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = conditions {
        query_parts.push(format!("conditions={}", val));
    }
    if let Some(val) = executionConfig_headers {
        query_parts.push(format!("executionConfig_headers={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities:deleteEntitiesWithConditions
/// Deletes entities based on conditions specified in the request and not on entity id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_execute()` or `connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities:deleteEntitiesWithConditions
/// Deletes entities based on conditions specified in the request and not on entity id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesDeleteEntitiesWithConditionsArgs
{
    /// Path parameter: entityType
    pub entityType: String,
    /// Query parameter: conditions
    pub conditions: Option<String>,
    /// Query parameter: executionConfig_headers
    pub executionConfig_headers: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities:deleteEntitiesWithConditions
/// Deletes entities based on conditions specified in the request and not on entity id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_builder()` + `connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesDeleteEntitiesWithConditionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_builder(client, &args.entityType, args.conditions.as_deref(), args.executionConfig_headers.as_deref())?;
    connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities/{entitiesId}
/// Gets a single entity row matching the entity type and entity id specified in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_entities_get_execute()` to send, or `connectors_projects_locations_connections_entity_types_entities_get` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_entities_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/entityTypes/{}/entities/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = executionConfig_headers {
        query_parts.push(format!("executionConfig_headers={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities/{entitiesId}
/// Gets a single entity row matching the entity type and entity id specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_entity_types_entities_get_execute()` or `connectors_projects_locations_connections_entity_types_entities_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_entities_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Entity>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Entity = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities/{entitiesId}
/// Gets a single entity row matching the entity type and entity id specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_entity_types_entities_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_entities_get_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_entity_types_entities_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_entities_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_entity_types_entities_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entity>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_entity_types_entities_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_entity_types_entities_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: executionConfig_headers
    pub executionConfig_headers: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities/{entitiesId}
/// Gets a single entity row matching the entity type and entity id specified in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_get_builder()` + `connectors_projects_locations_connections_entity_types_entities_get_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_entities_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_get(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entity>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_entities_get_builder(
        client,
        &args.name,
        args.executionConfig_headers.as_deref(),
    )?;
    connectors_projects_locations_connections_entity_types_entities_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities
/// Lists entity rows of a particular entity type contained in the request. Note: 1. Currently, only max of one 'sort_by' column is supported. 2. If no 'sort_by' column is provided, the primary key of the table is used. If zero or more than one primary key is available, we default to the unpaginated list entities logic which only returns the first page. 3. The values of the 'sort_by' columns must uniquely identify an entity row, otherwise undefined behaviors may be observed during pagination. 4. Since transactions are not supported, any updates, inserts or deletes during pagination can lead to stale data being returned or other unexpected behaviors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_entities_list_execute()` to send, or `connectors_projects_locations_connections_entity_types_entities_list` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_entities_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    conditions: Option<&str>,
    executionConfig_headers: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    sortBy: Option<&str>,
    sortOrder: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/entityTypes/{}/entities",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = conditions {
        query_parts.push(format!("conditions={}", val));
    }
    if let Some(val) = executionConfig_headers {
        query_parts.push(format!("executionConfig_headers={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = sortBy {
        query_parts.push(format!("sortBy={}", val));
    }
    if let Some(val) = sortOrder {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities
/// Lists entity rows of a particular entity type contained in the request. Note: 1. Currently, only max of one 'sort_by' column is supported. 2. If no 'sort_by' column is provided, the primary key of the table is used. If zero or more than one primary key is available, we default to the unpaginated list entities logic which only returns the first page. 3. The values of the 'sort_by' columns must uniquely identify an entity row, otherwise undefined behaviors may be observed during pagination. 4. Since transactions are not supported, any updates, inserts or deletes during pagination can lead to stale data being returned or other unexpected behaviors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_entity_types_entities_list_execute()` or `connectors_projects_locations_connections_entity_types_entities_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_entities_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListEntitiesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEntitiesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities
/// Lists entity rows of a particular entity type contained in the request. Note: 1. Currently, only max of one 'sort_by' column is supported. 2. If no 'sort_by' column is provided, the primary key of the table is used. If zero or more than one primary key is available, we default to the unpaginated list entities logic which only returns the first page. 3. The values of the 'sort_by' columns must uniquely identify an entity row, otherwise undefined behaviors may be observed during pagination. 4. Since transactions are not supported, any updates, inserts or deletes during pagination can lead to stale data being returned or other unexpected behaviors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_entity_types_entities_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_entities_list_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_entity_types_entities_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_entities_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_entity_types_entities_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEntitiesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_entity_types_entities_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_entity_types_entities_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: conditions
    pub conditions: Option<String>,
    /// Query parameter: executionConfig_headers
    pub executionConfig_headers: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: sortBy
    pub sortBy: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities
/// Lists entity rows of a particular entity type contained in the request. Note: 1. Currently, only max of one 'sort_by' column is supported. 2. If no 'sort_by' column is provided, the primary key of the table is used. If zero or more than one primary key is available, we default to the unpaginated list entities logic which only returns the first page. 3. The values of the 'sort_by' columns must uniquely identify an entity row, otherwise undefined behaviors may be observed during pagination. 4. Since transactions are not supported, any updates, inserts or deletes during pagination can lead to stale data being returned or other unexpected behaviors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_list_builder()` + `connectors_projects_locations_connections_entity_types_entities_list_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_entities_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_list(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEntitiesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_entities_list_builder(
        client,
        &args.parent,
        args.conditions.as_deref(),
        args.executionConfig_headers.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.sortBy.as_deref(),
        args.sortOrder.as_deref(),
    )?;
    connectors_projects_locations_connections_entity_types_entities_list_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities/{entitiesId}
/// Updates an existing entity row matching the entity type and entity id specified in the request. The fields in the entity row that need to be modified are contained in the body of the request. All unspecified fields are left unchanged. The response message contains a Entity message object returned as a response by the external system.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_entities_patch_execute()` to send, or `connectors_projects_locations_connections_entity_types_entities_patch` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_entities_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
    body: &Entity,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/entityTypes/{}/entities/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = executionConfig_headers {
        query_parts.push(format!("executionConfig_headers={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities/{entitiesId}
/// Updates an existing entity row matching the entity type and entity id specified in the request. The fields in the entity row that need to be modified are contained in the body of the request. All unspecified fields are left unchanged. The response message contains a Entity message object returned as a response by the external system.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_entity_types_entities_patch_execute()` or `connectors_projects_locations_connections_entity_types_entities_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_entities_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Entity>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Entity = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities/{entitiesId}
/// Updates an existing entity row matching the entity type and entity id specified in the request. The fields in the entity row that need to be modified are contained in the body of the request. All unspecified fields are left unchanged. The response message contains a Entity message object returned as a response by the external system.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_entity_types_entities_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_entities_patch_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_entity_types_entities_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_entities_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_entity_types_entities_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entity>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_entity_types_entities_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_entity_types_entities_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: executionConfig_headers
    pub executionConfig_headers: Option<String>,
    /// Request body.
    pub body: Entity,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities/{entitiesId}
/// Updates an existing entity row matching the entity type and entity id specified in the request. The fields in the entity row that need to be modified are contained in the body of the request. All unspecified fields are left unchanged. The response message contains a Entity message object returned as a response by the external system.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_patch_builder()` + `connectors_projects_locations_connections_entity_types_entities_patch_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_entities_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_patch(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entity>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_entities_patch_builder(
        client,
        &args.name,
        args.executionConfig_headers.as_deref(),
        &args.body,
    )?;
    connectors_projects_locations_connections_entity_types_entities_patch_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities:updateEntitiesWithConditions
/// Updates entities based on conditions specified in the request and not on entity id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_execute()` to send, or `connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions` for simplest API.

pub fn connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_builder(
    client: &SimpleHttpClient,
    entityType: &str,
    conditions: Option<&str>,
    executionConfig_headers: Option<&str>,
    body: &Entity,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/entityTypes/{}/entities:updateEntitiesWithConditions",
        entityType,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = conditions {
        query_parts.push(format!("conditions={}", val));
    }
    if let Some(val) = executionConfig_headers {
        query_parts.push(format!("executionConfig_headers={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities:updateEntitiesWithConditions
/// Updates entities based on conditions specified in the request and not on entity id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_execute()` or `connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<UpdateEntitiesWithConditionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateEntitiesWithConditionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities:updateEntitiesWithConditions
/// Updates entities based on conditions specified in the request and not on entity id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateEntitiesWithConditionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesUpdateEntitiesWithConditionsArgs
{
    /// Path parameter: entityType
    pub entityType: String,
    /// Query parameter: conditions
    pub conditions: Option<String>,
    /// Query parameter: executionConfig_headers
    pub executionConfig_headers: Option<String>,
    /// Request body.
    pub body: Entity,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/entityTypes/{entityTypesId}/entities:updateEntitiesWithConditions
/// Updates entities based on conditions specified in the request and not on entity id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_builder()` + `connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesUpdateEntitiesWithConditionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateEntitiesWithConditionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_builder(client, &args.entityType, args.conditions.as_deref(), args.executionConfig_headers.as_deref(), &args.body)?;
    connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/resources/{resourcesId}
/// Gets a specific resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_resources_get_execute()` to send, or `connectors_projects_locations_connections_resources_get` for simplest API.

pub fn connectors_projects_locations_connections_resources_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    executionConfig_headers: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/resources/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = executionConfig_headers {
        query_parts.push(format!("executionConfig_headers={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/resources/{resourcesId}
/// Gets a specific resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_resources_get_execute()` or `connectors_projects_locations_connections_resources_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_resources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_resources_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GetResourceResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetResourceResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/resources/{resourcesId}
/// Gets a specific resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_resources_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_resources_get_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_resources_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_resources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_resources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetResourceResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_resources_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_resources_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsResourcesGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: executionConfig_headers
    pub executionConfig_headers: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/resources/{resourcesId}
/// Gets a specific resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_resources_get_builder()` + `connectors_projects_locations_connections_resources_get_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_resources_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_resources_get(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsResourcesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetResourceResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_resources_get_builder(
        client,
        &args.name,
        args.executionConfig_headers.as_deref(),
    )?;
    connectors_projects_locations_connections_resources_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/resources/{resourcesId}
/// Gets a specific resource with POST.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_resources_get_resource_post_execute()` to send, or `connectors_projects_locations_connections_resources_get_resource_post` for simplest API.

pub fn connectors_projects_locations_connections_resources_get_resource_post_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GetResourcePostRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/resources/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/resources/{resourcesId}
/// Gets a specific resource with POST.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_resources_get_resource_post_execute()` or `connectors_projects_locations_connections_resources_get_resource_post`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_resources_get_resource_post_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_resources_get_resource_post_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GetResourceResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetResourceResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/resources/{resourcesId}
/// Gets a specific resource with POST.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_resources_get_resource_post_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_resources_get_resource_post_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_resources_get_resource_post()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_resources_get_resource_post_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_resources_get_resource_post_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetResourceResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_resources_get_resource_post_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_resources_get_resource_post`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsResourcesGetResourcePostArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GetResourcePostRequest,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/resources/{resourcesId}
/// Gets a specific resource with POST.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_resources_get_resource_post_builder()` + `connectors_projects_locations_connections_resources_get_resource_post_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_resources_get_resource_post_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_resources_get_resource_post(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsResourcesGetResourcePostArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetResourceResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_resources_get_resource_post_builder(
        client, &args.name, &args.body,
    )?;
    connectors_projects_locations_connections_resources_get_resource_post_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/resources
/// Lists all available resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_resources_list_execute()` to send, or `connectors_projects_locations_connections_resources_list` for simplest API.

pub fn connectors_projects_locations_connections_resources_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    executionConfig_headers: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/resources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = executionConfig_headers {
        query_parts.push(format!("executionConfig_headers={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/resources
/// Lists all available resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_resources_list_execute()` or `connectors_projects_locations_connections_resources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_resources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_resources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListResourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListResourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/resources
/// Lists all available resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_resources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_resources_list_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_resources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_resources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_resources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListResourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_resources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_resources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsResourcesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: executionConfig_headers
    pub executionConfig_headers: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/resources
/// Lists all available resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_resources_list_builder()` + `connectors_projects_locations_connections_resources_list_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_resources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_resources_list(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsResourcesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListResourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_resources_list_builder(
        client,
        &args.parent,
        args.executionConfig_headers.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    connectors_projects_locations_connections_resources_list_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/tools/{toolsId}:execute
/// Executes a specific tool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_tools_execute_execute()` to send, or `connectors_projects_locations_connections_tools_execute` for simplest API.

pub fn connectors_projects_locations_connections_tools_execute_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ExecuteToolRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/tools/{}:execute",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/tools/{toolsId}:execute
/// Executes a specific tool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_tools_execute_execute()` or `connectors_projects_locations_connections_tools_execute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_tools_execute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_tools_execute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ExecuteToolResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExecuteToolResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/tools/{toolsId}:execute
/// Executes a specific tool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_tools_execute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_tools_execute_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_tools_execute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_tools_execute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_tools_execute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecuteToolResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_tools_execute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_tools_execute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsToolsExecuteArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ExecuteToolRequest,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/tools/{toolsId}:execute
/// Executes a specific tool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_tools_execute_builder()` + `connectors_projects_locations_connections_tools_execute_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_tools_execute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_tools_execute(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsToolsExecuteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecuteToolResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_tools_execute_builder(
        client, &args.name, &args.body,
    )?;
    connectors_projects_locations_connections_tools_execute_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/tools
/// Lists all available tools.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `connectors_projects_locations_connections_tools_list_execute()` to send, or `connectors_projects_locations_connections_tools_list` for simplest API.

pub fn connectors_projects_locations_connections_tools_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    executionConfig_headers: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://connectors.googleapis.com/v2/projects/{}/locations/{}/connections/{}/tools",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = executionConfig_headers {
        query_parts.push(format!("executionConfig_headers={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/tools
/// Lists all available tools.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `connectors_projects_locations_connections_tools_list_execute()` or `connectors_projects_locations_connections_tools_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_tools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_tools_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListToolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListToolsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/tools
/// Lists all available tools.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `connectors_projects_locations_connections_tools_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `connectors_projects_locations_connections_tools_list_task()`.
/// For the simplest API, use `connectors_projects_locations_connections_tools_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `connectors_projects_locations_connections_tools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn connectors_projects_locations_connections_tools_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListToolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = connectors_projects_locations_connections_tools_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`connectors_projects_locations_connections_tools_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsToolsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: executionConfig_headers
    pub executionConfig_headers: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/tools
/// Lists all available tools.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `connectors_projects_locations_connections_tools_list_builder()` + `connectors_projects_locations_connections_tools_list_execute()`.
/// For task-level control, use `connectors_projects_locations_connections_tools_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn connectors_projects_locations_connections_tools_list(
    client: &SimpleHttpClient,
    args: &ConnectorsProjectsLocationsConnectionsToolsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListToolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = connectors_projects_locations_connections_tools_list_builder(
        client,
        &args.parent,
        args.executionConfig_headers.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    connectors_projects_locations_connections_tools_list_execute(builder)
}
