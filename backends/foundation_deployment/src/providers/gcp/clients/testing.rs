//! Auto-generated API clients for gcp/testing.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/applicationDetailService/getApkDetails
/// Gets the details of an Android application APK.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `testing_application_detail_service_get_apk_details_execute()` to send, or `testing_application_detail_service_get_apk_details` for simplest API.

pub fn testing_application_detail_service_get_apk_details_builder(
    client: &SimpleHttpClient,
    bundleLocation_gcsPath: Option<&str>,
    body: &FileReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/applicationDetailService/getApkDetails",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "bundleLocation_gcsPath",
            bundleLocation_gcsPath
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/applicationDetailService/getApkDetails
/// Gets the details of an Android application APK.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `testing_application_detail_service_get_apk_details_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from testing_application_detail_service_get_apk_details_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_application_detail_service_get_apk_details_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetApkDetailsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetApkDetailsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/applicationDetailService/getApkDetails
/// Gets the details of an Android application APK.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `testing_application_detail_service_get_apk_details_builder()` + `testing_application_detail_service_get_apk_details_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_application_detail_service_get_apk_details(
    client: &SimpleHttpClient,
    bundleLocation_gcsPath: Option<&str>,
    body: &FileReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetApkDetailsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = testing_application_detail_service_get_apk_details_builder(
        client,
        bundleLocation_gcsPath,
        body,
    )?;
    testing_application_detail_service_get_apk_details_execute(builder)
}

/// GET v1/{+name}:cancel
/// POST /v1/`projects/{project_id}/`deviceSessions`/{device_session_id}`:cancel Changes the DeviceSession to state FINISHED and terminates all connections. Canceled sessions are not deleted and can be retrieved or listed by the user until they expire based on the 28 day deletion policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `testing_projects_device_sessions_cancel_execute()` to send, or `testing_projects_device_sessions_cancel` for simplest API.

pub fn testing_projects_device_sessions_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelDeviceSessionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:cancel
/// POST /v1/`projects/{project_id}/`deviceSessions`/{device_session_id}`:cancel Changes the DeviceSession to state FINISHED and terminates all connections. Canceled sessions are not deleted and can be retrieved or listed by the user until they expire based on the 28 day deletion policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `testing_projects_device_sessions_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from testing_projects_device_sessions_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_device_sessions_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// POST /v1/`projects/{project_id}/`deviceSessions`/{device_session_id}`:cancel Changes the DeviceSession to state FINISHED and terminates all connections. Canceled sessions are not deleted and can be retrieved or listed by the user until they expire based on the 28 day deletion policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `testing_projects_device_sessions_cancel_builder()` + `testing_projects_device_sessions_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_device_sessions_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelDeviceSessionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = testing_projects_device_sessions_cancel_builder(client, name, body)?;
    testing_projects_device_sessions_cancel_execute(builder)
}

/// GET v1/{+parent}/deviceSessions
/// POST /v1/`projects/{project_id}/`deviceSessions``
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `testing_projects_device_sessions_create_execute()` to send, or `testing_projects_device_sessions_create` for simplest API.

pub fn testing_projects_device_sessions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &DeviceSession,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/deviceSessions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/deviceSessions
/// POST /v1/`projects/{project_id}/`deviceSessions``
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `testing_projects_device_sessions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from testing_projects_device_sessions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_device_sessions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeviceSession = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/deviceSessions
/// POST /v1/`projects/{project_id}/`deviceSessions``
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `testing_projects_device_sessions_create_builder()` + `testing_projects_device_sessions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_device_sessions_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &DeviceSession,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = testing_projects_device_sessions_create_builder(client, parent, body)?;
    testing_projects_device_sessions_create_execute(builder)
}

/// GET v1/{+name}
/// GET /v1/`projects/{project_id}/`deviceSessions`/{device_session_id}` Return a DeviceSession, which documents the allocation status and whether the device is allocated. Clients making requests from this API must poll GetDeviceSession.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `testing_projects_device_sessions_get_execute()` to send, or `testing_projects_device_sessions_get` for simplest API.

pub fn testing_projects_device_sessions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// GET /v1/`projects/{project_id}/`deviceSessions`/{device_session_id}` Return a DeviceSession, which documents the allocation status and whether the device is allocated. Clients making requests from this API must poll GetDeviceSession.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `testing_projects_device_sessions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from testing_projects_device_sessions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_device_sessions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeviceSession = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// GET /v1/`projects/{project_id}/`deviceSessions`/{device_session_id}` Return a DeviceSession, which documents the allocation status and whether the device is allocated. Clients making requests from this API must poll GetDeviceSession.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `testing_projects_device_sessions_get_builder()` + `testing_projects_device_sessions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_device_sessions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = testing_projects_device_sessions_get_builder(client, name)?;
    testing_projects_device_sessions_get_execute(builder)
}

/// GET v1/{+parent}/deviceSessions
/// GET /v1/`projects/{project_id}/`deviceSessions`` Lists device Sessions owned by the project user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `testing_projects_device_sessions_list_execute()` to send, or `testing_projects_device_sessions_list` for simplest API.

pub fn testing_projects_device_sessions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/deviceSessions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/deviceSessions
/// GET /v1/`projects/{project_id}/`deviceSessions`` Lists device Sessions owned by the project user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `testing_projects_device_sessions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from testing_projects_device_sessions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_device_sessions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDeviceSessionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDeviceSessionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/deviceSessions
/// GET /v1/`projects/{project_id}/`deviceSessions`` Lists device Sessions owned by the project user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `testing_projects_device_sessions_list_builder()` + `testing_projects_device_sessions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_device_sessions_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDeviceSessionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        testing_projects_device_sessions_list_builder(client, parent, filter, pageSize, pageToken)?;
    testing_projects_device_sessions_list_execute(builder)
}

/// GET v1/{+name}
/// PATCH /v1/`projects/{`projectId`}/`deviceSessions`/`deviceSessionId`}`:`updateDeviceSession` Updates the current device session to the fields described by the update_mask.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `testing_projects_device_sessions_patch_execute()` to send, or `testing_projects_device_sessions_patch` for simplest API.

pub fn testing_projects_device_sessions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &DeviceSession,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// PATCH /v1/`projects/{`projectId`}/`deviceSessions`/`deviceSessionId`}`:`updateDeviceSession` Updates the current device session to the fields described by the update_mask.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `testing_projects_device_sessions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from testing_projects_device_sessions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_device_sessions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeviceSession = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// PATCH /v1/`projects/{`projectId`}/`deviceSessions`/`deviceSessionId`}`:`updateDeviceSession` Updates the current device session to the fields described by the update_mask.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `testing_projects_device_sessions_patch_builder()` + `testing_projects_device_sessions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_device_sessions_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &DeviceSession,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = testing_projects_device_sessions_patch_builder(client, name, updateMask, body)?;
    testing_projects_device_sessions_patch_execute(builder)
}

/// GET v1/projects/{projectId}/testMatrices/{testMatrixId}:cancel
/// Cancels unfinished test executions in a test matrix. This call returns immediately and cancellation proceeds asynchronously. If the matrix is already final, this operation will have no effect. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Test Matrix does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `testing_projects_test_matrices_cancel_execute()` to send, or `testing_projects_test_matrices_cancel` for simplest API.

pub fn testing_projects_test_matrices_cancel_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    testMatrixId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/testMatrices/{}:cancel",
        projectId, testMatrixId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/testMatrices/{testMatrixId}:cancel
/// Cancels unfinished test executions in a test matrix. This call returns immediately and cancellation proceeds asynchronously. If the matrix is already final, this operation will have no effect. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Test Matrix does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `testing_projects_test_matrices_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from testing_projects_test_matrices_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_test_matrices_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CancelTestMatrixResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CancelTestMatrixResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/testMatrices/{testMatrixId}:cancel
/// Cancels unfinished test executions in a test matrix. This call returns immediately and cancellation proceeds asynchronously. If the matrix is already final, this operation will have no effect. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Test Matrix does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `testing_projects_test_matrices_cancel_builder()` + `testing_projects_test_matrices_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_test_matrices_cancel(
    client: &SimpleHttpClient,
    projectId: &str,
    testMatrixId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CancelTestMatrixResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = testing_projects_test_matrices_cancel_builder(client, projectId, testMatrixId)?;
    testing_projects_test_matrices_cancel_execute(builder)
}

/// GET v1/projects/{projectId}/testMatrices
/// Creates and runs a matrix of tests according to the given specifications. Unsupported environments will be returned in the state UNSUPPORTED. A test matrix is limited to use at most 2000 devices in parallel. The returned matrix will not yet contain the executions that will be created for this matrix. Execution creation happens later on and will require a call to GetTestMatrix. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed or if the matrix tries to use too many simultaneous devices.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `testing_projects_test_matrices_create_execute()` to send, or `testing_projects_test_matrices_create` for simplest API.

pub fn testing_projects_test_matrices_create_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    requestId: Option<&str>,
    body: &TestMatrix,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/testMatrices",
        projectId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/testMatrices
/// Creates and runs a matrix of tests according to the given specifications. Unsupported environments will be returned in the state UNSUPPORTED. A test matrix is limited to use at most 2000 devices in parallel. The returned matrix will not yet contain the executions that will be created for this matrix. Execution creation happens later on and will require a call to GetTestMatrix. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed or if the matrix tries to use too many simultaneous devices.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `testing_projects_test_matrices_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from testing_projects_test_matrices_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_test_matrices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestMatrix>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestMatrix = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/testMatrices
/// Creates and runs a matrix of tests according to the given specifications. Unsupported environments will be returned in the state UNSUPPORTED. A test matrix is limited to use at most 2000 devices in parallel. The returned matrix will not yet contain the executions that will be created for this matrix. Execution creation happens later on and will require a call to GetTestMatrix. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed or if the matrix tries to use too many simultaneous devices.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `testing_projects_test_matrices_create_builder()` + `testing_projects_test_matrices_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_test_matrices_create(
    client: &SimpleHttpClient,
    projectId: &str,
    requestId: Option<&str>,
    body: &TestMatrix,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestMatrix>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        testing_projects_test_matrices_create_builder(client, projectId, requestId, body)?;
    testing_projects_test_matrices_create_execute(builder)
}

/// GET v1/projects/{projectId}/testMatrices/{testMatrixId}
/// Checks the status of a test matrix and the executions once they are created. The test matrix will contain the list of test executions to run if and only if the `resultStorage`.`toolResultsExecution` fields have been populated. Note: Flaky test executions may be added to the matrix at a later stage. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Test Matrix does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `testing_projects_test_matrices_get_execute()` to send, or `testing_projects_test_matrices_get` for simplest API.

pub fn testing_projects_test_matrices_get_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    testMatrixId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/testMatrices/{}",
        projectId, testMatrixId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/testMatrices/{testMatrixId}
/// Checks the status of a test matrix and the executions once they are created. The test matrix will contain the list of test executions to run if and only if the `resultStorage`.`toolResultsExecution` fields have been populated. Note: Flaky test executions may be added to the matrix at a later stage. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Test Matrix does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `testing_projects_test_matrices_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from testing_projects_test_matrices_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_test_matrices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestMatrix>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestMatrix = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/testMatrices/{testMatrixId}
/// Checks the status of a test matrix and the executions once they are created. The test matrix will contain the list of test executions to run if and only if the `resultStorage`.`toolResultsExecution` fields have been populated. Note: Flaky test executions may be added to the matrix at a later stage. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Test Matrix does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `testing_projects_test_matrices_get_builder()` + `testing_projects_test_matrices_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_test_matrices_get(
    client: &SimpleHttpClient,
    projectId: &str,
    testMatrixId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestMatrix>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = testing_projects_test_matrices_get_builder(client, projectId, testMatrixId)?;
    testing_projects_test_matrices_get_execute(builder)
}

/// GET v1/testEnvironmentCatalog/{environmentType}
/// Gets the catalog of supported test environments. May return any of the following canonical error codes: - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the environment type does not exist - INTERNAL - if an internal error occurred
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `testing_test_environment_catalog_get_execute()` to send, or `testing_test_environment_catalog_get` for simplest API.

pub fn testing_test_environment_catalog_get_builder(
    client: &SimpleHttpClient,
    environmentType: &str,
    includeViewableModels: Option<bool>,
    projectId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/testEnvironmentCatalog/{}",
        environmentType,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "includeViewableModels",
            includeViewableModels
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "projectId",
            projectId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/testEnvironmentCatalog/{environmentType}
/// Gets the catalog of supported test environments. May return any of the following canonical error codes: - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the environment type does not exist - INTERNAL - if an internal error occurred
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `testing_test_environment_catalog_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from testing_test_environment_catalog_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_test_environment_catalog_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestEnvironmentCatalog>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestEnvironmentCatalog = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/testEnvironmentCatalog/{environmentType}
/// Gets the catalog of supported test environments. May return any of the following canonical error codes: - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the environment type does not exist - INTERNAL - if an internal error occurred
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `testing_test_environment_catalog_get_builder()` + `testing_test_environment_catalog_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_test_environment_catalog_get(
    client: &SimpleHttpClient,
    environmentType: &str,
    includeViewableModels: Option<bool>,
    projectId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestEnvironmentCatalog>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = testing_test_environment_catalog_get_builder(
        client,
        environmentType,
        includeViewableModels,
        projectId,
    )?;
    testing_test_environment_catalog_get_execute(builder)
}
