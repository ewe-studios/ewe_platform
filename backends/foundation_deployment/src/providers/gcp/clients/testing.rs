//! Auto-generated API clients for gcp/testing.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/applicationDetailService/getApkDetails
/// Gets the details of an Android application APK.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `testing_application_detail_service_get_apk_details_execute()` to send, or `testing_application_detail_service_get_apk_details` for simplest API.

pub fn testing_application_detail_service_get_apk_details_builder(
    client: &SimpleHttpClient,
    bundleLocation_gcsPath: Option<String>,
    body: &FileReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://testing.googleapis.com/v1/applicationDetailService/getApkDetails",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bundleLocation_gcsPath {
        query_parts.push(format!("bundleLocation.gcsPath={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/applicationDetailService/getApkDetails
/// Gets the details of an Android application APK.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `testing_application_detail_service_get_apk_details_execute()` or `testing_application_detail_service_get_apk_details`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `testing_application_detail_service_get_apk_details_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_application_detail_service_get_apk_details_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetApkDetailsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetApkDetailsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/applicationDetailService/getApkDetails
/// Gets the details of an Android application APK.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `testing_application_detail_service_get_apk_details_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `testing_application_detail_service_get_apk_details_task()`.
/// For the simplest API, use `testing_application_detail_service_get_apk_details()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `testing_application_detail_service_get_apk_details_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn testing_application_detail_service_get_apk_details_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetApkDetailsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = testing_application_detail_service_get_apk_details_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`testing_application_detail_service_get_apk_details`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TestingApplicationDetailServiceGetApkDetailsArgs {
    /// Query parameter: bundleLocation_gcsPath
    pub bundleLocation_gcsPath: Option<String>,
    /// Request body.
    pub body: FileReference,
}

/// GET v1/applicationDetailService/getApkDetails
/// Gets the details of an Android application APK.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `testing_application_detail_service_get_apk_details_builder()` + `testing_application_detail_service_get_apk_details_execute()`.
/// For task-level control, use `testing_application_detail_service_get_apk_details_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_application_detail_service_get_apk_details(
    client: &SimpleHttpClient,
    args: &TestingApplicationDetailServiceGetApkDetailsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetApkDetailsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = testing_application_detail_service_get_apk_details_builder(
        client,
        args.bundleLocation_gcsPath.clone(),
        &args.body,
    )?;
    testing_application_detail_service_get_apk_details_execute(builder)
}

/// GET v1/projects/{projectsId}/deviceSessions
/// POST /v1/`projects/{project_id}/`deviceSessions``
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `testing_projects_device_sessions_create_execute()` to send, or `testing_projects_device_sessions_create` for simplest API.

pub fn testing_projects_device_sessions_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &DeviceSession,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://testing.googleapis.com/v1/projects/{}/deviceSessions",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/deviceSessions
/// POST /v1/`projects/{project_id}/`deviceSessions``
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `testing_projects_device_sessions_create_execute()` or `testing_projects_device_sessions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `testing_projects_device_sessions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_device_sessions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeviceSession>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeviceSession = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/deviceSessions
/// POST /v1/`projects/{project_id}/`deviceSessions``
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `testing_projects_device_sessions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `testing_projects_device_sessions_create_task()`.
/// For the simplest API, use `testing_projects_device_sessions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `testing_projects_device_sessions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn testing_projects_device_sessions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = testing_projects_device_sessions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`testing_projects_device_sessions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TestingProjectsDeviceSessionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: DeviceSession,
}

/// GET v1/projects/{projectsId}/deviceSessions
/// POST /v1/`projects/{project_id}/`deviceSessions``
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `testing_projects_device_sessions_create_builder()` + `testing_projects_device_sessions_create_execute()`.
/// For task-level control, use `testing_projects_device_sessions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_device_sessions_create(
    client: &SimpleHttpClient,
    args: &TestingProjectsDeviceSessionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        testing_projects_device_sessions_create_builder(client, args.parent.clone(), &args.body)?;
    testing_projects_device_sessions_create_execute(builder)
}

/// GET v1/projects/{projectId}/testMatrices/{testMatrixId}:cancel
/// Cancels unfinished test executions in a test matrix. This call returns immediately and cancellation proceeds asynchronously. If the matrix is already final, this operation will have no effect. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Test Matrix does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `testing_projects_test_matrices_cancel_execute()` to send, or `testing_projects_test_matrices_cancel` for simplest API.

pub fn testing_projects_test_matrices_cancel_builder(
    client: &SimpleHttpClient,
    projectId: String,
    testMatrixId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://testing.googleapis.com/v1/projects/{}/testMatrices/{}:cancel",
        projectId.as_str(),
        testMatrixId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectId}/testMatrices/{testMatrixId}:cancel
/// Cancels unfinished test executions in a test matrix. This call returns immediately and cancellation proceeds asynchronously. If the matrix is already final, this operation will have no effect. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Test Matrix does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `testing_projects_test_matrices_cancel_execute()` or `testing_projects_test_matrices_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `testing_projects_test_matrices_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_test_matrices_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CancelTestMatrixResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CancelTestMatrixResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/testMatrices/{testMatrixId}:cancel
/// Cancels unfinished test executions in a test matrix. This call returns immediately and cancellation proceeds asynchronously. If the matrix is already final, this operation will have no effect. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Test Matrix does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `testing_projects_test_matrices_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `testing_projects_test_matrices_cancel_task()`.
/// For the simplest API, use `testing_projects_test_matrices_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `testing_projects_test_matrices_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn testing_projects_test_matrices_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CancelTestMatrixResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = testing_projects_test_matrices_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`testing_projects_test_matrices_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TestingProjectsTestMatricesCancelArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: testMatrixId
    pub testMatrixId: String,
}

/// GET v1/projects/{projectId}/testMatrices/{testMatrixId}:cancel
/// Cancels unfinished test executions in a test matrix. This call returns immediately and cancellation proceeds asynchronously. If the matrix is already final, this operation will have no effect. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Test Matrix does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `testing_projects_test_matrices_cancel_builder()` + `testing_projects_test_matrices_cancel_execute()`.
/// For task-level control, use `testing_projects_test_matrices_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_test_matrices_cancel(
    client: &SimpleHttpClient,
    args: &TestingProjectsTestMatricesCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CancelTestMatrixResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = testing_projects_test_matrices_cancel_builder(
        client,
        args.projectId.clone(),
        args.testMatrixId.clone(),
    )?;
    testing_projects_test_matrices_cancel_execute(builder)
}

/// GET v1/projects/{projectId}/testMatrices
/// Creates and runs a matrix of tests according to the given specifications. Unsupported environments will be returned in the state UNSUPPORTED. A test matrix is limited to use at most 2000 devices in parallel. The returned matrix will not yet contain the executions that will be created for this matrix. Execution creation happens later on and will require a call to GetTestMatrix. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed or if the matrix tries to use too many simultaneous devices.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `testing_projects_test_matrices_create_execute()` to send, or `testing_projects_test_matrices_create` for simplest API.

pub fn testing_projects_test_matrices_create_builder(
    client: &SimpleHttpClient,
    projectId: String,
    requestId: Option<String>,
    body: &TestMatrix,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://testing.googleapis.com/v1/projects/{}/testMatrices",
        projectId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/testMatrices
/// Creates and runs a matrix of tests according to the given specifications. Unsupported environments will be returned in the state UNSUPPORTED. A test matrix is limited to use at most 2000 devices in parallel. The returned matrix will not yet contain the executions that will be created for this matrix. Execution creation happens later on and will require a call to GetTestMatrix. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed or if the matrix tries to use too many simultaneous devices.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `testing_projects_test_matrices_create_execute()` or `testing_projects_test_matrices_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `testing_projects_test_matrices_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_test_matrices_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestMatrix>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestMatrix = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/testMatrices
/// Creates and runs a matrix of tests according to the given specifications. Unsupported environments will be returned in the state UNSUPPORTED. A test matrix is limited to use at most 2000 devices in parallel. The returned matrix will not yet contain the executions that will be created for this matrix. Execution creation happens later on and will require a call to GetTestMatrix. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed or if the matrix tries to use too many simultaneous devices.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `testing_projects_test_matrices_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `testing_projects_test_matrices_create_task()`.
/// For the simplest API, use `testing_projects_test_matrices_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `testing_projects_test_matrices_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn testing_projects_test_matrices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestMatrix>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = testing_projects_test_matrices_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`testing_projects_test_matrices_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TestingProjectsTestMatricesCreateArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: TestMatrix,
}

/// GET v1/projects/{projectId}/testMatrices
/// Creates and runs a matrix of tests according to the given specifications. Unsupported environments will be returned in the state UNSUPPORTED. A test matrix is limited to use at most 2000 devices in parallel. The returned matrix will not yet contain the executions that will be created for this matrix. Execution creation happens later on and will require a call to GetTestMatrix. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed or if the matrix tries to use too many simultaneous devices.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `testing_projects_test_matrices_create_builder()` + `testing_projects_test_matrices_create_execute()`.
/// For task-level control, use `testing_projects_test_matrices_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_test_matrices_create(
    client: &SimpleHttpClient,
    args: &TestingProjectsTestMatricesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestMatrix>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = testing_projects_test_matrices_create_builder(
        client,
        args.projectId.clone(),
        args.requestId.clone(),
        &args.body,
    )?;
    testing_projects_test_matrices_create_execute(builder)
}

/// GET v1/projects/{projectId}/testMatrices/{testMatrixId}
/// Checks the status of a test matrix and the executions once they are created. The test matrix will contain the list of test executions to run if and only if the `resultStorage`.`toolResultsExecution` fields have been populated. Note: Flaky test executions may be added to the matrix at a later stage. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Test Matrix does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `testing_projects_test_matrices_get_execute()` to send, or `testing_projects_test_matrices_get` for simplest API.

pub fn testing_projects_test_matrices_get_builder(
    client: &SimpleHttpClient,
    projectId: String,
    testMatrixId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://testing.googleapis.com/v1/projects/{}/testMatrices/{}",
        projectId.as_str(),
        testMatrixId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectId}/testMatrices/{testMatrixId}
/// Checks the status of a test matrix and the executions once they are created. The test matrix will contain the list of test executions to run if and only if the `resultStorage`.`toolResultsExecution` fields have been populated. Note: Flaky test executions may be added to the matrix at a later stage. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Test Matrix does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `testing_projects_test_matrices_get_execute()` or `testing_projects_test_matrices_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `testing_projects_test_matrices_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_test_matrices_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestMatrix>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestMatrix = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/testMatrices/{testMatrixId}
/// Checks the status of a test matrix and the executions once they are created. The test matrix will contain the list of test executions to run if and only if the `resultStorage`.`toolResultsExecution` fields have been populated. Note: Flaky test executions may be added to the matrix at a later stage. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Test Matrix does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `testing_projects_test_matrices_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `testing_projects_test_matrices_get_task()`.
/// For the simplest API, use `testing_projects_test_matrices_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `testing_projects_test_matrices_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn testing_projects_test_matrices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestMatrix>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = testing_projects_test_matrices_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`testing_projects_test_matrices_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TestingProjectsTestMatricesGetArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: testMatrixId
    pub testMatrixId: String,
}

/// GET v1/projects/{projectId}/testMatrices/{testMatrixId}
/// Checks the status of a test matrix and the executions once they are created. The test matrix will contain the list of test executions to run if and only if the `resultStorage`.`toolResultsExecution` fields have been populated. Note: Flaky test executions may be added to the matrix at a later stage. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Test Matrix does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `testing_projects_test_matrices_get_builder()` + `testing_projects_test_matrices_get_execute()`.
/// For task-level control, use `testing_projects_test_matrices_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_projects_test_matrices_get(
    client: &SimpleHttpClient,
    args: &TestingProjectsTestMatricesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestMatrix>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = testing_projects_test_matrices_get_builder(
        client,
        args.projectId.clone(),
        args.testMatrixId.clone(),
    )?;
    testing_projects_test_matrices_get_execute(builder)
}

/// GET v1/testEnvironmentCatalog/{environmentType}
/// Gets the catalog of supported test environments. May return any of the following canonical error codes: - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the environment type does not exist - INTERNAL - if an internal error occurred
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `testing_test_environment_catalog_get_execute()` to send, or `testing_test_environment_catalog_get` for simplest API.

pub fn testing_test_environment_catalog_get_builder(
    client: &SimpleHttpClient,
    environmentType: String,
    includeViewableModels: Option<bool>,
    projectId: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://testing.googleapis.com/v1/testEnvironmentCatalog/{}",
        environmentType.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = includeViewableModels {
        query_parts.push(format!("includeViewableModels={}", val));
    }
    if let Some(val) = projectId {
        query_parts.push(format!("projectId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/testEnvironmentCatalog/{environmentType}
/// Gets the catalog of supported test environments. May return any of the following canonical error codes: - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the environment type does not exist - INTERNAL - if an internal error occurred
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `testing_test_environment_catalog_get_execute()` or `testing_test_environment_catalog_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `testing_test_environment_catalog_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_test_environment_catalog_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestEnvironmentCatalog>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestEnvironmentCatalog = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/testEnvironmentCatalog/{environmentType}
/// Gets the catalog of supported test environments. May return any of the following canonical error codes: - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the environment type does not exist - INTERNAL - if an internal error occurred
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `testing_test_environment_catalog_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `testing_test_environment_catalog_get_task()`.
/// For the simplest API, use `testing_test_environment_catalog_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `testing_test_environment_catalog_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn testing_test_environment_catalog_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestEnvironmentCatalog>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = testing_test_environment_catalog_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`testing_test_environment_catalog_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TestingTestEnvironmentCatalogGetArgs {
    /// Path parameter: environmentType
    pub environmentType: String,
    /// Query parameter: includeViewableModels
    pub includeViewableModels: Option<bool>,
    /// Query parameter: projectId
    pub projectId: Option<String>,
}

/// GET v1/testEnvironmentCatalog/{environmentType}
/// Gets the catalog of supported test environments. May return any of the following canonical error codes: - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the environment type does not exist - INTERNAL - if an internal error occurred
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `testing_test_environment_catalog_get_builder()` + `testing_test_environment_catalog_get_execute()`.
/// For task-level control, use `testing_test_environment_catalog_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn testing_test_environment_catalog_get(
    client: &SimpleHttpClient,
    args: &TestingTestEnvironmentCatalogGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestEnvironmentCatalog>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = testing_test_environment_catalog_get_builder(
        client,
        args.environmentType.clone(),
        args.includeViewableModels.clone(),
        args.projectId.clone(),
    )?;
    testing_test_environment_catalog_get_execute(builder)
}
