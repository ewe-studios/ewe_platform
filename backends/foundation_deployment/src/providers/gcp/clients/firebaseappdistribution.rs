//! Auto-generated API clients for gcp/firebaseappdistribution.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/groups
/// Create a group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappdistribution_projects_groups_create_execute()` to send, or `firebaseappdistribution_projects_groups_create` for simplest API.

pub fn firebaseappdistribution_projects_groups_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    groupId: &Option<String>,
    body: &GoogleFirebaseAppdistroV1Group,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://firebaseappdistribution.googleapis.com/v1/projects/{}/groups",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = groupId.as_ref() {
        query_parts.push(format!("groupId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/groups
/// Create a group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaseappdistribution_projects_groups_create_execute()` or `firebaseappdistribution_projects_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappdistribution_projects_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappdistribution_projects_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirebaseAppdistroV1Group>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppdistroV1Group = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/groups
/// Create a group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaseappdistribution_projects_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaseappdistribution_projects_groups_create_task()`.
/// For the simplest API, use `firebaseappdistribution_projects_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappdistribution_projects_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaseappdistribution_projects_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppdistroV1Group>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebaseappdistribution_projects_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaseappdistribution_projects_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseappdistributionProjectsGroupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: groupId
    pub groupId: Option<String>,
    /// Request body.
    pub body: GoogleFirebaseAppdistroV1Group,
}

/// GET v1/projects/{projectsId}/groups
/// Create a group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappdistribution_projects_groups_create_builder()` + `firebaseappdistribution_projects_groups_create_execute()`.
/// For task-level control, use `firebaseappdistribution_projects_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappdistribution_projects_groups_create(
    client: &SimpleHttpClient,
    args: &FirebaseappdistributionProjectsGroupsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppdistroV1Group>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappdistribution_projects_groups_create_builder(
        client,
        &args.parent,
        &args.groupId,
        &args.body,
    )?;
    firebaseappdistribution_projects_groups_create_execute(builder)
}

/// GET v1/projects/{projectsId}/testers:batchAdd
/// Batch adds testers. This call adds testers for the specified emails if they don't already exist. Returns all testers specified in the request, including newly created and previously existing testers. This action is idempotent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappdistribution_projects_testers_batch_add_execute()` to send, or `firebaseappdistribution_projects_testers_batch_add` for simplest API.

pub fn firebaseappdistribution_projects_testers_batch_add_builder(
    client: &SimpleHttpClient,
    project: &String,
    body: &GoogleFirebaseAppdistroV1BatchAddTestersRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://firebaseappdistribution.googleapis.com/v1/projects/{}/testers:batchAdd",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/testers:batchAdd
/// Batch adds testers. This call adds testers for the specified emails if they don't already exist. Returns all testers specified in the request, including newly created and previously existing testers. This action is idempotent.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaseappdistribution_projects_testers_batch_add_execute()` or `firebaseappdistribution_projects_testers_batch_add`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappdistribution_projects_testers_batch_add_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappdistribution_projects_testers_batch_add_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirebaseAppdistroV1BatchAddTestersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppdistroV1BatchAddTestersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/testers:batchAdd
/// Batch adds testers. This call adds testers for the specified emails if they don't already exist. Returns all testers specified in the request, including newly created and previously existing testers. This action is idempotent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaseappdistribution_projects_testers_batch_add_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaseappdistribution_projects_testers_batch_add_task()`.
/// For the simplest API, use `firebaseappdistribution_projects_testers_batch_add()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappdistribution_projects_testers_batch_add_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaseappdistribution_projects_testers_batch_add_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppdistroV1BatchAddTestersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebaseappdistribution_projects_testers_batch_add_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaseappdistribution_projects_testers_batch_add`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseappdistributionProjectsTestersBatchAddArgs {
    /// Path parameter: project
    pub project: String,
    /// Request body.
    pub body: GoogleFirebaseAppdistroV1BatchAddTestersRequest,
}

/// GET v1/projects/{projectsId}/testers:batchAdd
/// Batch adds testers. This call adds testers for the specified emails if they don't already exist. Returns all testers specified in the request, including newly created and previously existing testers. This action is idempotent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappdistribution_projects_testers_batch_add_builder()` + `firebaseappdistribution_projects_testers_batch_add_execute()`.
/// For task-level control, use `firebaseappdistribution_projects_testers_batch_add_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappdistribution_projects_testers_batch_add(
    client: &SimpleHttpClient,
    args: &FirebaseappdistributionProjectsTestersBatchAddArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppdistroV1BatchAddTestersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappdistribution_projects_testers_batch_add_builder(
        client,
        &args.project,
        &args.body,
    )?;
    firebaseappdistribution_projects_testers_batch_add_execute(builder)
}

/// GET v1/projects/{projectsId}/testers:batchRemove
/// Batch removes testers. If found, this call deletes testers for the specified emails. Returns all deleted testers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappdistribution_projects_testers_batch_remove_execute()` to send, or `firebaseappdistribution_projects_testers_batch_remove` for simplest API.

pub fn firebaseappdistribution_projects_testers_batch_remove_builder(
    client: &SimpleHttpClient,
    project: &String,
    body: &GoogleFirebaseAppdistroV1BatchRemoveTestersRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firebaseappdistribution.googleapis.com/v1/projects/{}/testers:batchRemove",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/testers:batchRemove
/// Batch removes testers. If found, this call deletes testers for the specified emails. Returns all deleted testers.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaseappdistribution_projects_testers_batch_remove_execute()` or `firebaseappdistribution_projects_testers_batch_remove`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappdistribution_projects_testers_batch_remove_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappdistribution_projects_testers_batch_remove_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleFirebaseAppdistroV1BatchRemoveTestersResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppdistroV1BatchRemoveTestersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/testers:batchRemove
/// Batch removes testers. If found, this call deletes testers for the specified emails. Returns all deleted testers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaseappdistribution_projects_testers_batch_remove_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaseappdistribution_projects_testers_batch_remove_task()`.
/// For the simplest API, use `firebaseappdistribution_projects_testers_batch_remove()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappdistribution_projects_testers_batch_remove_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaseappdistribution_projects_testers_batch_remove_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppdistroV1BatchRemoveTestersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebaseappdistribution_projects_testers_batch_remove_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaseappdistribution_projects_testers_batch_remove`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseappdistributionProjectsTestersBatchRemoveArgs {
    /// Path parameter: project
    pub project: String,
    /// Request body.
    pub body: GoogleFirebaseAppdistroV1BatchRemoveTestersRequest,
}

/// GET v1/projects/{projectsId}/testers:batchRemove
/// Batch removes testers. If found, this call deletes testers for the specified emails. Returns all deleted testers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappdistribution_projects_testers_batch_remove_builder()` + `firebaseappdistribution_projects_testers_batch_remove_execute()`.
/// For task-level control, use `firebaseappdistribution_projects_testers_batch_remove_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappdistribution_projects_testers_batch_remove(
    client: &SimpleHttpClient,
    args: &FirebaseappdistributionProjectsTestersBatchRemoveArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppdistroV1BatchRemoveTestersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappdistribution_projects_testers_batch_remove_builder(
        client,
        &args.project,
        &args.body,
    )?;
    firebaseappdistribution_projects_testers_batch_remove_execute(builder)
}

/// GET v1/projects/{projectsId}/testers
/// Lists testers and their resource ids.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappdistribution_projects_testers_list_execute()` to send, or `firebaseappdistribution_projects_testers_list` for simplest API.

pub fn firebaseappdistribution_projects_testers_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://firebaseappdistribution.googleapis.com/v1/projects/{}/testers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/testers
/// Lists testers and their resource ids.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaseappdistribution_projects_testers_list_execute()` or `firebaseappdistribution_projects_testers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappdistribution_projects_testers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappdistribution_projects_testers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirebaseAppdistroV1ListTestersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppdistroV1ListTestersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/testers
/// Lists testers and their resource ids.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaseappdistribution_projects_testers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaseappdistribution_projects_testers_list_task()`.
/// For the simplest API, use `firebaseappdistribution_projects_testers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappdistribution_projects_testers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaseappdistribution_projects_testers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppdistroV1ListTestersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebaseappdistribution_projects_testers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaseappdistribution_projects_testers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseappdistributionProjectsTestersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/testers
/// Lists testers and their resource ids.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappdistribution_projects_testers_list_builder()` + `firebaseappdistribution_projects_testers_list_execute()`.
/// For task-level control, use `firebaseappdistribution_projects_testers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappdistribution_projects_testers_list(
    client: &SimpleHttpClient,
    args: &FirebaseappdistributionProjectsTestersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppdistroV1ListTestersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappdistribution_projects_testers_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    firebaseappdistribution_projects_testers_list_execute(builder)
}
