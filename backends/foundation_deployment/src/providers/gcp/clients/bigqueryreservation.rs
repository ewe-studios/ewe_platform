//! Auto-generated API clients for gcp/bigqueryreservation.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}/biReservation
/// Retrieves a BI reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_get_bi_reservation_execute()` to send, or `bigqueryreservation_projects_locations_get_bi_reservation` for simplest API.

pub fn bigqueryreservation_projects_locations_get_bi_reservation_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/biReservation",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/biReservation
/// Retrieves a BI reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_get_bi_reservation_execute()` or `bigqueryreservation_projects_locations_get_bi_reservation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_get_bi_reservation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_get_bi_reservation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BiReservation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BiReservation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/biReservation
/// Retrieves a BI reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_get_bi_reservation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_get_bi_reservation_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_get_bi_reservation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_get_bi_reservation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_get_bi_reservation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BiReservation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_get_bi_reservation_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_get_bi_reservation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsGetBiReservationArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/biReservation
/// Retrieves a BI reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_get_bi_reservation_builder()` + `bigqueryreservation_projects_locations_get_bi_reservation_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_get_bi_reservation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_get_bi_reservation(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsGetBiReservationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BiReservation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        bigqueryreservation_projects_locations_get_bi_reservation_builder(client, &args.name)?;
    bigqueryreservation_projects_locations_get_bi_reservation_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:searchAllAssignments
/// Looks up assignments for a specified resource for a particular region. If the request is about a project: 1. Assignments created on the project will be returned if they exist. 2. Otherwise assignments created on the closest ancestor will be returned. 3. Assignments for different JobTypes will all be returned. The same logic applies if the request is about a folder. If the request is about an organization, then assignments created on the organization will be returned (organization doesn''t have ancestors). Comparing to ListAssignments, there are some behavior differences: 1. permission on the assignee will be verified in this API. 2. Hierarchy lookup (project-&gt;folder-&gt;organization) happens in this API. 3. Parent here is projects/*/locations/*, instead of projects/*/locations/*reservations/*.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_search_all_assignments_execute()` to send, or `bigqueryreservation_projects_locations_search_all_assignments` for simplest API.

pub fn bigqueryreservation_projects_locations_search_all_assignments_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    query: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}:searchAllAssignments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:searchAllAssignments
/// Looks up assignments for a specified resource for a particular region. If the request is about a project: 1. Assignments created on the project will be returned if they exist. 2. Otherwise assignments created on the closest ancestor will be returned. 3. Assignments for different JobTypes will all be returned. The same logic applies if the request is about a folder. If the request is about an organization, then assignments created on the organization will be returned (organization doesn''t have ancestors). Comparing to ListAssignments, there are some behavior differences: 1. permission on the assignee will be verified in this API. 2. Hierarchy lookup (project-&gt;folder-&gt;organization) happens in this API. 3. Parent here is projects/*/locations/*, instead of projects/*/locations/*reservations/*.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_search_all_assignments_execute()` or `bigqueryreservation_projects_locations_search_all_assignments`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_search_all_assignments_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_search_all_assignments_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchAllAssignmentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchAllAssignmentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:searchAllAssignments
/// Looks up assignments for a specified resource for a particular region. If the request is about a project: 1. Assignments created on the project will be returned if they exist. 2. Otherwise assignments created on the closest ancestor will be returned. 3. Assignments for different JobTypes will all be returned. The same logic applies if the request is about a folder. If the request is about an organization, then assignments created on the organization will be returned (organization doesn''t have ancestors). Comparing to ListAssignments, there are some behavior differences: 1. permission on the assignee will be verified in this API. 2. Hierarchy lookup (project-&gt;folder-&gt;organization) happens in this API. 3. Parent here is projects/*/locations/*, instead of projects/*/locations/*reservations/*.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_search_all_assignments_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_search_all_assignments_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_search_all_assignments()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_search_all_assignments_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_search_all_assignments_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchAllAssignmentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_search_all_assignments_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_search_all_assignments`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsSearchAllAssignmentsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:searchAllAssignments
/// Looks up assignments for a specified resource for a particular region. If the request is about a project: 1. Assignments created on the project will be returned if they exist. 2. Otherwise assignments created on the closest ancestor will be returned. 3. Assignments for different JobTypes will all be returned. The same logic applies if the request is about a folder. If the request is about an organization, then assignments created on the organization will be returned (organization doesn''t have ancestors). Comparing to ListAssignments, there are some behavior differences: 1. permission on the assignee will be verified in this API. 2. Hierarchy lookup (project-&gt;folder-&gt;organization) happens in this API. 3. Parent here is projects/*/locations/*, instead of projects/*/locations/*reservations/*.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_search_all_assignments_builder()` + `bigqueryreservation_projects_locations_search_all_assignments_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_search_all_assignments_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_search_all_assignments(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsSearchAllAssignmentsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchAllAssignmentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_search_all_assignments_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.query,
    )?;
    bigqueryreservation_projects_locations_search_all_assignments_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:searchAssignments
/// Deprecated: Looks up assignments for a specified resource for a particular region. If the request is about a project: 1. Assignments created on the project will be returned if they exist. 2. Otherwise assignments created on the closest ancestor will be returned. 3. Assignments for different JobTypes will all be returned. The same logic applies if the request is about a folder. If the request is about an organization, then assignments created on the organization will be returned (organization doesn''t have ancestors). Comparing to ListAssignments, there are some behavior differences: 1. permission on the assignee will be verified in this API. 2. Hierarchy lookup (project-&gt;folder-&gt;organization) happens in this API. 3. Parent here is projects/*/locations/*, instead of projects/*/locations/*reservations/*. **Note** "-" cannot be used for projects nor locations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_search_assignments_execute()` to send, or `bigqueryreservation_projects_locations_search_assignments` for simplest API.

pub fn bigqueryreservation_projects_locations_search_assignments_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    query: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}:searchAssignments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:searchAssignments
/// Deprecated: Looks up assignments for a specified resource for a particular region. If the request is about a project: 1. Assignments created on the project will be returned if they exist. 2. Otherwise assignments created on the closest ancestor will be returned. 3. Assignments for different JobTypes will all be returned. The same logic applies if the request is about a folder. If the request is about an organization, then assignments created on the organization will be returned (organization doesn''t have ancestors). Comparing to ListAssignments, there are some behavior differences: 1. permission on the assignee will be verified in this API. 2. Hierarchy lookup (project-&gt;folder-&gt;organization) happens in this API. 3. Parent here is projects/*/locations/*, instead of projects/*/locations/*reservations/*. **Note** "-" cannot be used for projects nor locations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_search_assignments_execute()` or `bigqueryreservation_projects_locations_search_assignments`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_search_assignments_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_search_assignments_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchAssignmentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchAssignmentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:searchAssignments
/// Deprecated: Looks up assignments for a specified resource for a particular region. If the request is about a project: 1. Assignments created on the project will be returned if they exist. 2. Otherwise assignments created on the closest ancestor will be returned. 3. Assignments for different JobTypes will all be returned. The same logic applies if the request is about a folder. If the request is about an organization, then assignments created on the organization will be returned (organization doesn''t have ancestors). Comparing to ListAssignments, there are some behavior differences: 1. permission on the assignee will be verified in this API. 2. Hierarchy lookup (project-&gt;folder-&gt;organization) happens in this API. 3. Parent here is projects/*/locations/*, instead of projects/*/locations/*reservations/*. **Note** "-" cannot be used for projects nor locations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_search_assignments_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_search_assignments_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_search_assignments()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_search_assignments_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_search_assignments_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchAssignmentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_search_assignments_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_search_assignments`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsSearchAssignmentsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:searchAssignments
/// Deprecated: Looks up assignments for a specified resource for a particular region. If the request is about a project: 1. Assignments created on the project will be returned if they exist. 2. Otherwise assignments created on the closest ancestor will be returned. 3. Assignments for different JobTypes will all be returned. The same logic applies if the request is about a folder. If the request is about an organization, then assignments created on the organization will be returned (organization doesn''t have ancestors). Comparing to ListAssignments, there are some behavior differences: 1. permission on the assignee will be verified in this API. 2. Hierarchy lookup (project-&gt;folder-&gt;organization) happens in this API. 3. Parent here is projects/*/locations/*, instead of projects/*/locations/*reservations/*. **Note** "-" cannot be used for projects nor locations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_search_assignments_builder()` + `bigqueryreservation_projects_locations_search_assignments_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_search_assignments_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_search_assignments(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsSearchAssignmentsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchAssignmentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_search_assignments_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.query,
    )?;
    bigqueryreservation_projects_locations_search_assignments_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/biReservation
/// Updates a BI reservation. Only fields specified in the field_mask are updated. A singleton BI reservation always exists with default size 0. In order to reserve BI capacity it needs to be updated to an amount greater than 0. In order to release BI capacity reservation size must be set to 0.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_update_bi_reservation_execute()` to send, or `bigqueryreservation_projects_locations_update_bi_reservation` for simplest API.

pub fn bigqueryreservation_projects_locations_update_bi_reservation_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/biReservation",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/biReservation
/// Updates a BI reservation. Only fields specified in the field_mask are updated. A singleton BI reservation always exists with default size 0. In order to reserve BI capacity it needs to be updated to an amount greater than 0. In order to release BI capacity reservation size must be set to 0.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_update_bi_reservation_execute()` or `bigqueryreservation_projects_locations_update_bi_reservation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_update_bi_reservation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_update_bi_reservation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BiReservation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BiReservation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/biReservation
/// Updates a BI reservation. Only fields specified in the field_mask are updated. A singleton BI reservation always exists with default size 0. In order to reserve BI capacity it needs to be updated to an amount greater than 0. In order to release BI capacity reservation size must be set to 0.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_update_bi_reservation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_update_bi_reservation_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_update_bi_reservation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_update_bi_reservation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_update_bi_reservation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BiReservation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_update_bi_reservation_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_update_bi_reservation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsUpdateBiReservationArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/biReservation
/// Updates a BI reservation. Only fields specified in the field_mask are updated. A singleton BI reservation always exists with default size 0. In order to reserve BI capacity it needs to be updated to an amount greater than 0. In order to release BI capacity reservation size must be set to 0.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_update_bi_reservation_builder()` + `bigqueryreservation_projects_locations_update_bi_reservation_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_update_bi_reservation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_update_bi_reservation(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsUpdateBiReservationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BiReservation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_update_bi_reservation_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    bigqueryreservation_projects_locations_update_bi_reservation_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments
/// Creates a new capacity commitment resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_capacity_commitments_create_execute()` to send, or `bigqueryreservation_projects_locations_capacity_commitments_create` for simplest API.

pub fn bigqueryreservation_projects_locations_capacity_commitments_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    capacityCommitmentId: &Option<String>,
    enforceSingleAdminProjectPerOrg: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/capacityCommitments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = capacityCommitmentId.as_ref() {
        query_parts.push(format!("capacityCommitmentId={}", val));
    }
    if let Some(val) = enforceSingleAdminProjectPerOrg.as_ref() {
        query_parts.push(format!("enforceSingleAdminProjectPerOrg={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments
/// Creates a new capacity commitment resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_capacity_commitments_create_execute()` or `bigqueryreservation_projects_locations_capacity_commitments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_capacity_commitments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_capacity_commitments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CapacityCommitment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CapacityCommitment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments
/// Creates a new capacity commitment resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_capacity_commitments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_capacity_commitments_create_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_capacity_commitments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_capacity_commitments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_capacity_commitments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CapacityCommitment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_capacity_commitments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_capacity_commitments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsCapacityCommitmentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: capacityCommitmentId
    pub capacityCommitmentId: Option<String>,
    /// Query parameter: enforceSingleAdminProjectPerOrg
    pub enforceSingleAdminProjectPerOrg: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments
/// Creates a new capacity commitment resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_capacity_commitments_create_builder()` + `bigqueryreservation_projects_locations_capacity_commitments_create_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_capacity_commitments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_capacity_commitments_create(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsCapacityCommitmentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CapacityCommitment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_capacity_commitments_create_builder(
        client,
        &args.parent,
        &args.capacityCommitmentId,
        &args.enforceSingleAdminProjectPerOrg,
    )?;
    bigqueryreservation_projects_locations_capacity_commitments_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}
/// Deletes a capacity commitment. Attempting to delete capacity commitment before its commitment_end_time will fail with the error code google.rpc.Code.FAILED_PRECONDITION.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_capacity_commitments_delete_execute()` to send, or `bigqueryreservation_projects_locations_capacity_commitments_delete` for simplest API.

pub fn bigqueryreservation_projects_locations_capacity_commitments_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    force: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}
/// Deletes a capacity commitment. Attempting to delete capacity commitment before its commitment_end_time will fail with the error code google.rpc.Code.FAILED_PRECONDITION.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_capacity_commitments_delete_execute()` or `bigqueryreservation_projects_locations_capacity_commitments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_capacity_commitments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_capacity_commitments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}
/// Deletes a capacity commitment. Attempting to delete capacity commitment before its commitment_end_time will fail with the error code google.rpc.Code.FAILED_PRECONDITION.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_capacity_commitments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_capacity_commitments_delete_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_capacity_commitments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_capacity_commitments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_capacity_commitments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_capacity_commitments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_capacity_commitments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsCapacityCommitmentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}
/// Deletes a capacity commitment. Attempting to delete capacity commitment before its commitment_end_time will fail with the error code google.rpc.Code.FAILED_PRECONDITION.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_capacity_commitments_delete_builder()` + `bigqueryreservation_projects_locations_capacity_commitments_delete_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_capacity_commitments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_capacity_commitments_delete(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsCapacityCommitmentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_capacity_commitments_delete_builder(
        client,
        &args.name,
        &args.force,
    )?;
    bigqueryreservation_projects_locations_capacity_commitments_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}
/// Returns information about the capacity commitment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_capacity_commitments_get_execute()` to send, or `bigqueryreservation_projects_locations_capacity_commitments_get` for simplest API.

pub fn bigqueryreservation_projects_locations_capacity_commitments_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}
/// Returns information about the capacity commitment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_capacity_commitments_get_execute()` or `bigqueryreservation_projects_locations_capacity_commitments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_capacity_commitments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_capacity_commitments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CapacityCommitment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CapacityCommitment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}
/// Returns information about the capacity commitment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_capacity_commitments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_capacity_commitments_get_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_capacity_commitments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_capacity_commitments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_capacity_commitments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CapacityCommitment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_capacity_commitments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_capacity_commitments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsCapacityCommitmentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}
/// Returns information about the capacity commitment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_capacity_commitments_get_builder()` + `bigqueryreservation_projects_locations_capacity_commitments_get_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_capacity_commitments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_capacity_commitments_get(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsCapacityCommitmentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CapacityCommitment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_capacity_commitments_get_builder(
        client, &args.name,
    )?;
    bigqueryreservation_projects_locations_capacity_commitments_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments
/// Lists all the capacity commitments for the admin project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_capacity_commitments_list_execute()` to send, or `bigqueryreservation_projects_locations_capacity_commitments_list` for simplest API.

pub fn bigqueryreservation_projects_locations_capacity_commitments_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/capacityCommitments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments
/// Lists all the capacity commitments for the admin project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_capacity_commitments_list_execute()` or `bigqueryreservation_projects_locations_capacity_commitments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_capacity_commitments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_capacity_commitments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCapacityCommitmentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCapacityCommitmentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments
/// Lists all the capacity commitments for the admin project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_capacity_commitments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_capacity_commitments_list_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_capacity_commitments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_capacity_commitments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_capacity_commitments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCapacityCommitmentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_capacity_commitments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_capacity_commitments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsCapacityCommitmentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments
/// Lists all the capacity commitments for the admin project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_capacity_commitments_list_builder()` + `bigqueryreservation_projects_locations_capacity_commitments_list_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_capacity_commitments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_capacity_commitments_list(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsCapacityCommitmentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCapacityCommitmentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_capacity_commitments_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    bigqueryreservation_projects_locations_capacity_commitments_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments:merge
/// Merges capacity commitments of the same plan into a single commitment. The resulting capacity commitment has the greater commitment_end_time out of the to-be-merged capacity commitments. Attempting to merge capacity commitments of different plan will fail with the error code google.rpc.Code.FAILED_PRECONDITION.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_capacity_commitments_merge_execute()` to send, or `bigqueryreservation_projects_locations_capacity_commitments_merge` for simplest API.

pub fn bigqueryreservation_projects_locations_capacity_commitments_merge_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/capacityCommitments:merge",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments:merge
/// Merges capacity commitments of the same plan into a single commitment. The resulting capacity commitment has the greater commitment_end_time out of the to-be-merged capacity commitments. Attempting to merge capacity commitments of different plan will fail with the error code google.rpc.Code.FAILED_PRECONDITION.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_capacity_commitments_merge_execute()` or `bigqueryreservation_projects_locations_capacity_commitments_merge`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_capacity_commitments_merge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_capacity_commitments_merge_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CapacityCommitment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CapacityCommitment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments:merge
/// Merges capacity commitments of the same plan into a single commitment. The resulting capacity commitment has the greater commitment_end_time out of the to-be-merged capacity commitments. Attempting to merge capacity commitments of different plan will fail with the error code google.rpc.Code.FAILED_PRECONDITION.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_capacity_commitments_merge_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_capacity_commitments_merge_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_capacity_commitments_merge()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_capacity_commitments_merge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_capacity_commitments_merge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CapacityCommitment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_capacity_commitments_merge_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_capacity_commitments_merge`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsCapacityCommitmentsMergeArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments:merge
/// Merges capacity commitments of the same plan into a single commitment. The resulting capacity commitment has the greater commitment_end_time out of the to-be-merged capacity commitments. Attempting to merge capacity commitments of different plan will fail with the error code google.rpc.Code.FAILED_PRECONDITION.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_capacity_commitments_merge_builder()` + `bigqueryreservation_projects_locations_capacity_commitments_merge_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_capacity_commitments_merge_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_capacity_commitments_merge(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsCapacityCommitmentsMergeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CapacityCommitment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_capacity_commitments_merge_builder(
        client,
        &args.parent,
    )?;
    bigqueryreservation_projects_locations_capacity_commitments_merge_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}
/// Updates an existing capacity commitment. Only plan and renewal_plan fields can be updated. Plan can only be changed to a plan of a longer commitment period. Attempting to change to a plan with shorter commitment period will fail with the error code google.rpc.Code.FAILED_PRECONDITION.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_capacity_commitments_patch_execute()` to send, or `bigqueryreservation_projects_locations_capacity_commitments_patch` for simplest API.

pub fn bigqueryreservation_projects_locations_capacity_commitments_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}
/// Updates an existing capacity commitment. Only plan and renewal_plan fields can be updated. Plan can only be changed to a plan of a longer commitment period. Attempting to change to a plan with shorter commitment period will fail with the error code google.rpc.Code.FAILED_PRECONDITION.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_capacity_commitments_patch_execute()` or `bigqueryreservation_projects_locations_capacity_commitments_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_capacity_commitments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_capacity_commitments_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CapacityCommitment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CapacityCommitment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}
/// Updates an existing capacity commitment. Only plan and renewal_plan fields can be updated. Plan can only be changed to a plan of a longer commitment period. Attempting to change to a plan with shorter commitment period will fail with the error code google.rpc.Code.FAILED_PRECONDITION.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_capacity_commitments_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_capacity_commitments_patch_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_capacity_commitments_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_capacity_commitments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_capacity_commitments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CapacityCommitment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_capacity_commitments_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_capacity_commitments_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsCapacityCommitmentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}
/// Updates an existing capacity commitment. Only plan and renewal_plan fields can be updated. Plan can only be changed to a plan of a longer commitment period. Attempting to change to a plan with shorter commitment period will fail with the error code google.rpc.Code.FAILED_PRECONDITION.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_capacity_commitments_patch_builder()` + `bigqueryreservation_projects_locations_capacity_commitments_patch_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_capacity_commitments_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_capacity_commitments_patch(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsCapacityCommitmentsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CapacityCommitment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_capacity_commitments_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    bigqueryreservation_projects_locations_capacity_commitments_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}:split
/// Splits capacity commitment to two commitments of the same plan and commitment_end_time. A common use case is to enable downgrading commitments. For example, in order to downgrade from 10000 slots to 8000, you might split a 10000 capacity commitment into commitments of 2000 and 8000. Then, you delete the first one after the commitment end time passes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_capacity_commitments_split_execute()` to send, or `bigqueryreservation_projects_locations_capacity_commitments_split` for simplest API.

pub fn bigqueryreservation_projects_locations_capacity_commitments_split_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}:split",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}:split
/// Splits capacity commitment to two commitments of the same plan and commitment_end_time. A common use case is to enable downgrading commitments. For example, in order to downgrade from 10000 slots to 8000, you might split a 10000 capacity commitment into commitments of 2000 and 8000. Then, you delete the first one after the commitment end time passes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_capacity_commitments_split_execute()` or `bigqueryreservation_projects_locations_capacity_commitments_split`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_capacity_commitments_split_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_capacity_commitments_split_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SplitCapacityCommitmentResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SplitCapacityCommitmentResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}:split
/// Splits capacity commitment to two commitments of the same plan and commitment_end_time. A common use case is to enable downgrading commitments. For example, in order to downgrade from 10000 slots to 8000, you might split a 10000 capacity commitment into commitments of 2000 and 8000. Then, you delete the first one after the commitment end time passes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_capacity_commitments_split_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_capacity_commitments_split_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_capacity_commitments_split()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_capacity_commitments_split_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_capacity_commitments_split_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SplitCapacityCommitmentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_capacity_commitments_split_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_capacity_commitments_split`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsCapacityCommitmentsSplitArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/capacityCommitments/{capacityCommitmentsId}:split
/// Splits capacity commitment to two commitments of the same plan and commitment_end_time. A common use case is to enable downgrading commitments. For example, in order to downgrade from 10000 slots to 8000, you might split a 10000 capacity commitment into commitments of 2000 and 8000. Then, you delete the first one after the commitment end time passes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_capacity_commitments_split_builder()` + `bigqueryreservation_projects_locations_capacity_commitments_split_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_capacity_commitments_split_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_capacity_commitments_split(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsCapacityCommitmentsSplitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SplitCapacityCommitmentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_capacity_commitments_split_builder(
        client, &args.name,
    )?;
    bigqueryreservation_projects_locations_capacity_commitments_split_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservationGroups
/// Creates a new reservation group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservation_groups_create_execute()` to send, or `bigqueryreservation_projects_locations_reservation_groups_create` for simplest API.

pub fn bigqueryreservation_projects_locations_reservation_groups_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    reservationGroupId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservationGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = reservationGroupId.as_ref() {
        query_parts.push(format!("reservationGroupId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservationGroups
/// Creates a new reservation group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservation_groups_create_execute()` or `bigqueryreservation_projects_locations_reservation_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservation_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservation_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservationGroups
/// Creates a new reservation group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservation_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservation_groups_create_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservation_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservation_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservation_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReservationGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_reservation_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservation_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationGroupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: reservationGroupId
    pub reservationGroupId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservationGroups
/// Creates a new reservation group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservation_groups_create_builder()` + `bigqueryreservation_projects_locations_reservation_groups_create_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservation_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservation_groups_create(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationGroupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReservationGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_reservation_groups_create_builder(
        client,
        &args.parent,
        &args.reservationGroupId,
    )?;
    bigqueryreservation_projects_locations_reservation_groups_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/reservationGroups/{reservationGroupsId}
/// Deletes a reservation. Returns google.rpc.Code.FAILED_PRECONDITION when reservation has assignments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservation_groups_delete_execute()` to send, or `bigqueryreservation_projects_locations_reservation_groups_delete` for simplest API.

pub fn bigqueryreservation_projects_locations_reservation_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservationGroups/{reservationGroupsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/reservationGroups/{reservationGroupsId}
/// Deletes a reservation. Returns google.rpc.Code.FAILED_PRECONDITION when reservation has assignments.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservation_groups_delete_execute()` or `bigqueryreservation_projects_locations_reservation_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservation_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservation_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/reservationGroups/{reservationGroupsId}
/// Deletes a reservation. Returns google.rpc.Code.FAILED_PRECONDITION when reservation has assignments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservation_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservation_groups_delete_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservation_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservation_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservation_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_reservation_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservation_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationGroupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/reservationGroups/{reservationGroupsId}
/// Deletes a reservation. Returns google.rpc.Code.FAILED_PRECONDITION when reservation has assignments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservation_groups_delete_builder()` + `bigqueryreservation_projects_locations_reservation_groups_delete_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservation_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservation_groups_delete(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_reservation_groups_delete_builder(
        client, &args.name,
    )?;
    bigqueryreservation_projects_locations_reservation_groups_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservationGroups/{reservationGroupsId}
/// Returns information about the reservation group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservation_groups_get_execute()` to send, or `bigqueryreservation_projects_locations_reservation_groups_get` for simplest API.

pub fn bigqueryreservation_projects_locations_reservation_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservationGroups/{reservationGroupsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservationGroups/{reservationGroupsId}
/// Returns information about the reservation group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservation_groups_get_execute()` or `bigqueryreservation_projects_locations_reservation_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservation_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservation_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservationGroups/{reservationGroupsId}
/// Returns information about the reservation group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservation_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservation_groups_get_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservation_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservation_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservation_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReservationGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_reservation_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservation_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationGroupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservationGroups/{reservationGroupsId}
/// Returns information about the reservation group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservation_groups_get_builder()` + `bigqueryreservation_projects_locations_reservation_groups_get_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservation_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservation_groups_get(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReservationGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        bigqueryreservation_projects_locations_reservation_groups_get_builder(client, &args.name)?;
    bigqueryreservation_projects_locations_reservation_groups_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservationGroups
/// Lists all the reservation groups for the project in the specified location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservation_groups_list_execute()` to send, or `bigqueryreservation_projects_locations_reservation_groups_list` for simplest API.

pub fn bigqueryreservation_projects_locations_reservation_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservationGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservationGroups
/// Lists all the reservation groups for the project in the specified location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservation_groups_list_execute()` or `bigqueryreservation_projects_locations_reservation_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservation_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservation_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReservationGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReservationGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservationGroups
/// Lists all the reservation groups for the project in the specified location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservation_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservation_groups_list_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservation_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservation_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservation_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListReservationGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_reservation_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservation_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationGroupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservationGroups
/// Lists all the reservation groups for the project in the specified location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservation_groups_list_builder()` + `bigqueryreservation_projects_locations_reservation_groups_list_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservation_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservation_groups_list(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListReservationGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_reservation_groups_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    bigqueryreservation_projects_locations_reservation_groups_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations
/// Creates a new reservation resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservations_create_execute()` to send, or `bigqueryreservation_projects_locations_reservations_create` for simplest API.

pub fn bigqueryreservation_projects_locations_reservations_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    reservationId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = reservationId.as_ref() {
        query_parts.push(format!("reservationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations
/// Creates a new reservation resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservations_create_execute()` or `bigqueryreservation_projects_locations_reservations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Reservation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Reservation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations
/// Creates a new reservation resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_create_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_reservations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: reservationId
    pub reservationId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations
/// Creates a new reservation resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservations_create_builder()` + `bigqueryreservation_projects_locations_reservations_create_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_create(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_reservations_create_builder(
        client,
        &args.parent,
        &args.reservationId,
    )?;
    bigqueryreservation_projects_locations_reservations_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Deletes a reservation. Returns google.rpc.Code.FAILED_PRECONDITION when reservation has assignments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservations_delete_execute()` to send, or `bigqueryreservation_projects_locations_reservations_delete` for simplest API.

pub fn bigqueryreservation_projects_locations_reservations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservations/{reservationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Deletes a reservation. Returns google.rpc.Code.FAILED_PRECONDITION when reservation has assignments.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservations_delete_execute()` or `bigqueryreservation_projects_locations_reservations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Deletes a reservation. Returns google.rpc.Code.FAILED_PRECONDITION when reservation has assignments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_delete_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_reservations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Deletes a reservation. Returns google.rpc.Code.FAILED_PRECONDITION when reservation has assignments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservations_delete_builder()` + `bigqueryreservation_projects_locations_reservations_delete_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_delete(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigqueryreservation_projects_locations_reservations_delete_builder(client, &args.name)?;
    bigqueryreservation_projects_locations_reservations_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}:failoverReservation
/// Fail over a reservation to the secondary location. The operation should be done in the current secondary location, which will be promoted to the new primary location for the reservation. Attempting to failover a reservation in the current primary location will fail with the error code google.rpc.Code.FAILED_PRECONDITION.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservations_failover_reservation_execute()` to send, or `bigqueryreservation_projects_locations_reservations_failover_reservation` for simplest API.

pub fn bigqueryreservation_projects_locations_reservations_failover_reservation_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservations/{reservationsId}:failoverReservation",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}:failoverReservation
/// Fail over a reservation to the secondary location. The operation should be done in the current secondary location, which will be promoted to the new primary location for the reservation. Attempting to failover a reservation in the current primary location will fail with the error code google.rpc.Code.FAILED_PRECONDITION.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservations_failover_reservation_execute()` or `bigqueryreservation_projects_locations_reservations_failover_reservation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_failover_reservation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_failover_reservation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Reservation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Reservation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}:failoverReservation
/// Fail over a reservation to the secondary location. The operation should be done in the current secondary location, which will be promoted to the new primary location for the reservation. Attempting to failover a reservation in the current primary location will fail with the error code google.rpc.Code.FAILED_PRECONDITION.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservations_failover_reservation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_failover_reservation_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservations_failover_reservation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_failover_reservation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservations_failover_reservation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        bigqueryreservation_projects_locations_reservations_failover_reservation_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservations_failover_reservation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationsFailoverReservationArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}:failoverReservation
/// Fail over a reservation to the secondary location. The operation should be done in the current secondary location, which will be promoted to the new primary location for the reservation. Attempting to failover a reservation in the current primary location will fail with the error code google.rpc.Code.FAILED_PRECONDITION.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservations_failover_reservation_builder()` + `bigqueryreservation_projects_locations_reservations_failover_reservation_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_failover_reservation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_failover_reservation(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationsFailoverReservationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_reservations_failover_reservation_builder(
        client, &args.name,
    )?;
    bigqueryreservation_projects_locations_reservations_failover_reservation_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Returns information about the reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservations_get_execute()` to send, or `bigqueryreservation_projects_locations_reservations_get` for simplest API.

pub fn bigqueryreservation_projects_locations_reservations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservations/{reservationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Returns information about the reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservations_get_execute()` or `bigqueryreservation_projects_locations_reservations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Reservation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Reservation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Returns information about the reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_get_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_reservations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Returns information about the reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservations_get_builder()` + `bigqueryreservation_projects_locations_reservations_get_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_get(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigqueryreservation_projects_locations_reservations_get_builder(client, &args.name)?;
    bigqueryreservation_projects_locations_reservations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}:getIamPolicy
/// Gets the access control policy for a resource. May return: * ANOT_FOUND error if the resource doesn''t exist or you don''t have the permission to view it. * An empty policy if the resource exists but doesn''t have a set policy. Supported resources are: - Reservations - ReservationAssignments To call this method, you must have the following Google IAM permissions: - bigqueryreservation.reservations.`getIamPolicy` to get policies on reservations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservations_get_iam_policy_execute()` to send, or `bigqueryreservation_projects_locations_reservations_get_iam_policy` for simplest API.

pub fn bigqueryreservation_projects_locations_reservations_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservations/{reservationsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}:getIamPolicy
/// Gets the access control policy for a resource. May return: * ANOT_FOUND error if the resource doesn''t exist or you don''t have the permission to view it. * An empty policy if the resource exists but doesn''t have a set policy. Supported resources are: - Reservations - ReservationAssignments To call this method, you must have the following Google IAM permissions: - bigqueryreservation.reservations.`getIamPolicy` to get policies on reservations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservations_get_iam_policy_execute()` or `bigqueryreservation_projects_locations_reservations_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}:getIamPolicy
/// Gets the access control policy for a resource. May return: * ANOT_FOUND error if the resource doesn''t exist or you don''t have the permission to view it. * An empty policy if the resource exists but doesn''t have a set policy. Supported resources are: - Reservations - ReservationAssignments To call this method, you must have the following Google IAM permissions: - bigqueryreservation.reservations.`getIamPolicy` to get policies on reservations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservations_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_get_iam_policy_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservations_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservations_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_reservations_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservations_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}:getIamPolicy
/// Gets the access control policy for a resource. May return: * ANOT_FOUND error if the resource doesn''t exist or you don''t have the permission to view it. * An empty policy if the resource exists but doesn''t have a set policy. Supported resources are: - Reservations - ReservationAssignments To call this method, you must have the following Google IAM permissions: - bigqueryreservation.reservations.`getIamPolicy` to get policies on reservations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservations_get_iam_policy_builder()` + `bigqueryreservation_projects_locations_reservations_get_iam_policy_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_get_iam_policy(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_reservations_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    bigqueryreservation_projects_locations_reservations_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations
/// Lists all the reservations for the project in the specified location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservations_list_execute()` to send, or `bigqueryreservation_projects_locations_reservations_list` for simplest API.

pub fn bigqueryreservation_projects_locations_reservations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations
/// Lists all the reservations for the project in the specified location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservations_list_execute()` or `bigqueryreservation_projects_locations_reservations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReservationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReservationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations
/// Lists all the reservations for the project in the specified location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_list_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReservationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_reservations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations
/// Lists all the reservations for the project in the specified location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservations_list_builder()` + `bigqueryreservation_projects_locations_reservations_list_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_list(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReservationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_reservations_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    bigqueryreservation_projects_locations_reservations_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Updates an existing reservation resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservations_patch_execute()` to send, or `bigqueryreservation_projects_locations_reservations_patch` for simplest API.

pub fn bigqueryreservation_projects_locations_reservations_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservations/{reservationsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Updates an existing reservation resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservations_patch_execute()` or `bigqueryreservation_projects_locations_reservations_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Reservation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Reservation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Updates an existing reservation resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservations_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_patch_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservations_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_reservations_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservations_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Updates an existing reservation resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservations_patch_builder()` + `bigqueryreservation_projects_locations_reservations_patch_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_patch(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_reservations_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    bigqueryreservation_projects_locations_reservations_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}:setIamPolicy
/// Sets an access control policy for a resource. Replaces any existing policy. Supported resources are: - Reservations To call this method, you must have the following Google IAM permissions: - bigqueryreservation.reservations.`setIamPolicy` to set policies on reservations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservations_set_iam_policy_execute()` to send, or `bigqueryreservation_projects_locations_reservations_set_iam_policy` for simplest API.

pub fn bigqueryreservation_projects_locations_reservations_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservations/{reservationsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}:setIamPolicy
/// Sets an access control policy for a resource. Replaces any existing policy. Supported resources are: - Reservations To call this method, you must have the following Google IAM permissions: - bigqueryreservation.reservations.`setIamPolicy` to set policies on reservations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservations_set_iam_policy_execute()` or `bigqueryreservation_projects_locations_reservations_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}:setIamPolicy
/// Sets an access control policy for a resource. Replaces any existing policy. Supported resources are: - Reservations To call this method, you must have the following Google IAM permissions: - bigqueryreservation.reservations.`setIamPolicy` to set policies on reservations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservations_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_set_iam_policy_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservations_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservations_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_reservations_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservations_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}:setIamPolicy
/// Sets an access control policy for a resource. Replaces any existing policy. Supported resources are: - Reservations To call this method, you must have the following Google IAM permissions: - bigqueryreservation.reservations.`setIamPolicy` to set policies on reservations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservations_set_iam_policy_builder()` + `bigqueryreservation_projects_locations_reservations_set_iam_policy_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_set_iam_policy(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_reservations_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    bigqueryreservation_projects_locations_reservations_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}:testIamPermissions
/// Gets your permissions on a resource. Returns an empty set of permissions if the resource doesn''t exist. Supported resources are: - Reservations No Google IAM permissions are required to call this method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservations_test_iam_permissions_execute()` to send, or `bigqueryreservation_projects_locations_reservations_test_iam_permissions` for simplest API.

pub fn bigqueryreservation_projects_locations_reservations_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservations/{reservationsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}:testIamPermissions
/// Gets your permissions on a resource. Returns an empty set of permissions if the resource doesn''t exist. Supported resources are: - Reservations No Google IAM permissions are required to call this method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservations_test_iam_permissions_execute()` or `bigqueryreservation_projects_locations_reservations_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}:testIamPermissions
/// Gets your permissions on a resource. Returns an empty set of permissions if the resource doesn''t exist. Supported resources are: - Reservations No Google IAM permissions are required to call this method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservations_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_test_iam_permissions_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservations_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservations_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        bigqueryreservation_projects_locations_reservations_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservations_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}:testIamPermissions
/// Gets your permissions on a resource. Returns an empty set of permissions if the resource doesn''t exist. Supported resources are: - Reservations No Google IAM permissions are required to call this method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservations_test_iam_permissions_builder()` + `bigqueryreservation_projects_locations_reservations_test_iam_permissions_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_reservations_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    bigqueryreservation_projects_locations_reservations_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments
/// Creates an assignment object which allows the given project to submit jobs of a certain type using slots from the specified reservation. Currently a resource (project, folder, organization) can only have one assignment per each (job_type, location) combination, and that reservation will be used for all jobs of the matching type. Different assignments can be created on different levels of the projects, folders or organization hierarchy. During query execution, the assignment is looked up at the project, folder and organization levels in that order. The first assignment found is applied to the query. When creating assignments, it does not matter if other assignments exist at higher levels. Example: * The organization organizationA contains two projects, project1 and project2. * Assignments for all three entities (organizationA, project1, and project2) could all be created and mapped to the same or different reservations. "None" assignments represent an absence of the assignment. Projects assigned to None use on-demand pricing. To create a "None" assignment, use "none" as a reservation_id in the parent. Example parent: `projects/myproject/locations/US/reservations/none`. Returns google.rpc.Code.PERMISSION_DENIED if user does not have ''bigquery.admin'' permissions on the project using the reservation and the project that owns this reservation. Returns google.rpc.Code.INVALID_ARGUMENT when location of the assignment does not match location of the reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservations_assignments_create_execute()` to send, or `bigqueryreservation_projects_locations_reservations_assignments_create` for simplest API.

pub fn bigqueryreservation_projects_locations_reservations_assignments_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    assignmentId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservations/{reservationsId}/assignments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = assignmentId.as_ref() {
        query_parts.push(format!("assignmentId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments
/// Creates an assignment object which allows the given project to submit jobs of a certain type using slots from the specified reservation. Currently a resource (project, folder, organization) can only have one assignment per each (job_type, location) combination, and that reservation will be used for all jobs of the matching type. Different assignments can be created on different levels of the projects, folders or organization hierarchy. During query execution, the assignment is looked up at the project, folder and organization levels in that order. The first assignment found is applied to the query. When creating assignments, it does not matter if other assignments exist at higher levels. Example: * The organization organizationA contains two projects, project1 and project2. * Assignments for all three entities (organizationA, project1, and project2) could all be created and mapped to the same or different reservations. "None" assignments represent an absence of the assignment. Projects assigned to None use on-demand pricing. To create a "None" assignment, use "none" as a reservation_id in the parent. Example parent: `projects/myproject/locations/US/reservations/none`. Returns google.rpc.Code.PERMISSION_DENIED if user does not have ''bigquery.admin'' permissions on the project using the reservation and the project that owns this reservation. Returns google.rpc.Code.INVALID_ARGUMENT when location of the assignment does not match location of the reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservations_assignments_create_execute()` or `bigqueryreservation_projects_locations_reservations_assignments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_assignments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_assignments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Assignment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Assignment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments
/// Creates an assignment object which allows the given project to submit jobs of a certain type using slots from the specified reservation. Currently a resource (project, folder, organization) can only have one assignment per each (job_type, location) combination, and that reservation will be used for all jobs of the matching type. Different assignments can be created on different levels of the projects, folders or organization hierarchy. During query execution, the assignment is looked up at the project, folder and organization levels in that order. The first assignment found is applied to the query. When creating assignments, it does not matter if other assignments exist at higher levels. Example: * The organization organizationA contains two projects, project1 and project2. * Assignments for all three entities (organizationA, project1, and project2) could all be created and mapped to the same or different reservations. "None" assignments represent an absence of the assignment. Projects assigned to None use on-demand pricing. To create a "None" assignment, use "none" as a reservation_id in the parent. Example parent: `projects/myproject/locations/US/reservations/none`. Returns google.rpc.Code.PERMISSION_DENIED if user does not have ''bigquery.admin'' permissions on the project using the reservation and the project that owns this reservation. Returns google.rpc.Code.INVALID_ARGUMENT when location of the assignment does not match location of the reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservations_assignments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_assignments_create_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservations_assignments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_assignments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservations_assignments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Assignment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        bigqueryreservation_projects_locations_reservations_assignments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservations_assignments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationsAssignmentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: assignmentId
    pub assignmentId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments
/// Creates an assignment object which allows the given project to submit jobs of a certain type using slots from the specified reservation. Currently a resource (project, folder, organization) can only have one assignment per each (job_type, location) combination, and that reservation will be used for all jobs of the matching type. Different assignments can be created on different levels of the projects, folders or organization hierarchy. During query execution, the assignment is looked up at the project, folder and organization levels in that order. The first assignment found is applied to the query. When creating assignments, it does not matter if other assignments exist at higher levels. Example: * The organization organizationA contains two projects, project1 and project2. * Assignments for all three entities (organizationA, project1, and project2) could all be created and mapped to the same or different reservations. "None" assignments represent an absence of the assignment. Projects assigned to None use on-demand pricing. To create a "None" assignment, use "none" as a reservation_id in the parent. Example parent: `projects/myproject/locations/US/reservations/none`. Returns google.rpc.Code.PERMISSION_DENIED if user does not have ''bigquery.admin'' permissions on the project using the reservation and the project that owns this reservation. Returns google.rpc.Code.INVALID_ARGUMENT when location of the assignment does not match location of the reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservations_assignments_create_builder()` + `bigqueryreservation_projects_locations_reservations_assignments_create_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_assignments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_assignments_create(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationsAssignmentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Assignment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_reservations_assignments_create_builder(
        client,
        &args.parent,
        &args.assignmentId,
    )?;
    bigqueryreservation_projects_locations_reservations_assignments_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}
/// Deletes a assignment. No expansion will happen. Example: * Organization organizationA contains two projects, project1 and project2. * Reservation res1 exists and was created previously. * CreateAssignment was used previously to define the following associations between entities and reservations:  and  In this example, deletion of the  assignment won''t affect the other assignment . After said deletion, queries from project1 will still use res1 while queries from project2 will switch to use on-demand mode.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservations_assignments_delete_execute()` to send, or `bigqueryreservation_projects_locations_reservations_assignments_delete` for simplest API.

pub fn bigqueryreservation_projects_locations_reservations_assignments_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}
/// Deletes a assignment. No expansion will happen. Example: * Organization organizationA contains two projects, project1 and project2. * Reservation res1 exists and was created previously. * CreateAssignment was used previously to define the following associations between entities and reservations:  and  In this example, deletion of the  assignment won''t affect the other assignment . After said deletion, queries from project1 will still use res1 while queries from project2 will switch to use on-demand mode.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservations_assignments_delete_execute()` or `bigqueryreservation_projects_locations_reservations_assignments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_assignments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_assignments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}
/// Deletes a assignment. No expansion will happen. Example: * Organization organizationA contains two projects, project1 and project2. * Reservation res1 exists and was created previously. * CreateAssignment was used previously to define the following associations between entities and reservations:  and  In this example, deletion of the  assignment won''t affect the other assignment . After said deletion, queries from project1 will still use res1 while queries from project2 will switch to use on-demand mode.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservations_assignments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_assignments_delete_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservations_assignments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_assignments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservations_assignments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        bigqueryreservation_projects_locations_reservations_assignments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservations_assignments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationsAssignmentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}
/// Deletes a assignment. No expansion will happen. Example: * Organization organizationA contains two projects, project1 and project2. * Reservation res1 exists and was created previously. * CreateAssignment was used previously to define the following associations between entities and reservations:  and  In this example, deletion of the  assignment won''t affect the other assignment . After said deletion, queries from project1 will still use res1 while queries from project2 will switch to use on-demand mode.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservations_assignments_delete_builder()` + `bigqueryreservation_projects_locations_reservations_assignments_delete_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_assignments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_assignments_delete(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationsAssignmentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_reservations_assignments_delete_builder(
        client, &args.name,
    )?;
    bigqueryreservation_projects_locations_reservations_assignments_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:getIamPolicy
/// Gets the access control policy for a resource. May return: * ANOT_FOUND error if the resource doesn''t exist or you don''t have the permission to view it. * An empty policy if the resource exists but doesn''t have a set policy. Supported resources are: - Reservations - ReservationAssignments To call this method, you must have the following Google IAM permissions: - bigqueryreservation.reservations.`getIamPolicy` to get policies on reservations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy_execute()` to send, or `bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy` for simplest API.

pub fn bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:getIamPolicy
/// Gets the access control policy for a resource. May return: * ANOT_FOUND error if the resource doesn''t exist or you don''t have the permission to view it. * An empty policy if the resource exists but doesn''t have a set policy. Supported resources are: - Reservations - ReservationAssignments To call this method, you must have the following Google IAM permissions: - bigqueryreservation.reservations.`getIamPolicy` to get policies on reservations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy_execute()` or `bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:getIamPolicy
/// Gets the access control policy for a resource. May return: * ANOT_FOUND error if the resource doesn''t exist or you don''t have the permission to view it. * An empty policy if the resource exists but doesn''t have a set policy. Supported resources are: - Reservations - ReservationAssignments To call this method, you must have the following Google IAM permissions: - bigqueryreservation.reservations.`getIamPolicy` to get policies on reservations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationsAssignmentsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:getIamPolicy
/// Gets the access control policy for a resource. May return: * ANOT_FOUND error if the resource doesn''t exist or you don''t have the permission to view it. * An empty policy if the resource exists but doesn''t have a set policy. Supported resources are: - Reservations - ReservationAssignments To call this method, you must have the following Google IAM permissions: - bigqueryreservation.reservations.`getIamPolicy` to get policies on reservations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy_builder()` + `bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationsAssignmentsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy_builder(
            client,
            &args.resource,
            &args.options_requestedPolicyVersion,
        )?;
    bigqueryreservation_projects_locations_reservations_assignments_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments
/// Lists assignments. Only explicitly created assignments will be returned. Example: * Organization organizationA contains two projects, project1 and project2. * Reservation res1 exists and was created previously. * CreateAssignment was used previously to define the following associations between entities and reservations:  and  In this example, ListAssignments will just return the above two assignments for reservation res1, and no `expansion/merge` will happen. The wildcard "-" can be used for reservations in the request. In that case all assignments belongs to the specified project and location will be listed. **Note** "-" cannot be used for projects nor locations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservations_assignments_list_execute()` to send, or `bigqueryreservation_projects_locations_reservations_assignments_list` for simplest API.

pub fn bigqueryreservation_projects_locations_reservations_assignments_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservations/{reservationsId}/assignments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments
/// Lists assignments. Only explicitly created assignments will be returned. Example: * Organization organizationA contains two projects, project1 and project2. * Reservation res1 exists and was created previously. * CreateAssignment was used previously to define the following associations between entities and reservations:  and  In this example, ListAssignments will just return the above two assignments for reservation res1, and no `expansion/merge` will happen. The wildcard "-" can be used for reservations in the request. In that case all assignments belongs to the specified project and location will be listed. **Note** "-" cannot be used for projects nor locations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservations_assignments_list_execute()` or `bigqueryreservation_projects_locations_reservations_assignments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_assignments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_assignments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAssignmentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAssignmentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments
/// Lists assignments. Only explicitly created assignments will be returned. Example: * Organization organizationA contains two projects, project1 and project2. * Reservation res1 exists and was created previously. * CreateAssignment was used previously to define the following associations between entities and reservations:  and  In this example, ListAssignments will just return the above two assignments for reservation res1, and no `expansion/merge` will happen. The wildcard "-" can be used for reservations in the request. In that case all assignments belongs to the specified project and location will be listed. **Note** "-" cannot be used for projects nor locations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservations_assignments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_assignments_list_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservations_assignments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_assignments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservations_assignments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssignmentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_reservations_assignments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservations_assignments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationsAssignmentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments
/// Lists assignments. Only explicitly created assignments will be returned. Example: * Organization organizationA contains two projects, project1 and project2. * Reservation res1 exists and was created previously. * CreateAssignment was used previously to define the following associations between entities and reservations:  and  In this example, ListAssignments will just return the above two assignments for reservation res1, and no `expansion/merge` will happen. The wildcard "-" can be used for reservations in the request. In that case all assignments belongs to the specified project and location will be listed. **Note** "-" cannot be used for projects nor locations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservations_assignments_list_builder()` + `bigqueryreservation_projects_locations_reservations_assignments_list_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_assignments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_assignments_list(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationsAssignmentsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssignmentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_reservations_assignments_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    bigqueryreservation_projects_locations_reservations_assignments_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:move
/// Moves an assignment under a new reservation. This differs from removing an existing assignment and recreating a new one by providing a transactional change that ensures an assignee always has an associated reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservations_assignments_move_execute()` to send, or `bigqueryreservation_projects_locations_reservations_assignments_move` for simplest API.

pub fn bigqueryreservation_projects_locations_reservations_assignments_move_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:move",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:move
/// Moves an assignment under a new reservation. This differs from removing an existing assignment and recreating a new one by providing a transactional change that ensures an assignee always has an associated reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservations_assignments_move_execute()` or `bigqueryreservation_projects_locations_reservations_assignments_move`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_assignments_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_assignments_move_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Assignment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Assignment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:move
/// Moves an assignment under a new reservation. This differs from removing an existing assignment and recreating a new one by providing a transactional change that ensures an assignee always has an associated reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservations_assignments_move_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_assignments_move_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservations_assignments_move()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_assignments_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservations_assignments_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Assignment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_reservations_assignments_move_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservations_assignments_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationsAssignmentsMoveArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:move
/// Moves an assignment under a new reservation. This differs from removing an existing assignment and recreating a new one by providing a transactional change that ensures an assignee always has an associated reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservations_assignments_move_builder()` + `bigqueryreservation_projects_locations_reservations_assignments_move_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_assignments_move_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_assignments_move(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationsAssignmentsMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Assignment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_reservations_assignments_move_builder(
        client, &args.name,
    )?;
    bigqueryreservation_projects_locations_reservations_assignments_move_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}
/// Updates an existing assignment. Only the priority field can be updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservations_assignments_patch_execute()` to send, or `bigqueryreservation_projects_locations_reservations_assignments_patch` for simplest API.

pub fn bigqueryreservation_projects_locations_reservations_assignments_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}
/// Updates an existing assignment. Only the priority field can be updated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservations_assignments_patch_execute()` or `bigqueryreservation_projects_locations_reservations_assignments_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_assignments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_assignments_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Assignment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Assignment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}
/// Updates an existing assignment. Only the priority field can be updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservations_assignments_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_assignments_patch_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservations_assignments_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_assignments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservations_assignments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Assignment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_reservations_assignments_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservations_assignments_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationsAssignmentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}
/// Updates an existing assignment. Only the priority field can be updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservations_assignments_patch_builder()` + `bigqueryreservation_projects_locations_reservations_assignments_patch_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_assignments_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_assignments_patch(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationsAssignmentsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Assignment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_reservations_assignments_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    bigqueryreservation_projects_locations_reservations_assignments_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:setIamPolicy
/// Sets an access control policy for a resource. Replaces any existing policy. Supported resources are: - Reservations To call this method, you must have the following Google IAM permissions: - bigqueryreservation.reservations.`setIamPolicy` to set policies on reservations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy_execute()` to send, or `bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy` for simplest API.

pub fn bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:setIamPolicy
/// Sets an access control policy for a resource. Replaces any existing policy. Supported resources are: - Reservations To call this method, you must have the following Google IAM permissions: - bigqueryreservation.reservations.`setIamPolicy` to set policies on reservations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy_execute()` or `bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:setIamPolicy
/// Sets an access control policy for a resource. Replaces any existing policy. Supported resources are: - Reservations To call this method, you must have the following Google IAM permissions: - bigqueryreservation.reservations.`setIamPolicy` to set policies on reservations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationsAssignmentsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:setIamPolicy
/// Sets an access control policy for a resource. Replaces any existing policy. Supported resources are: - Reservations To call this method, you must have the following Google IAM permissions: - bigqueryreservation.reservations.`setIamPolicy` to set policies on reservations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy_builder()` + `bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationsAssignmentsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy_builder(
            client,
            &args.resource,
        )?;
    bigqueryreservation_projects_locations_reservations_assignments_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:testIamPermissions
/// Gets your permissions on a resource. Returns an empty set of permissions if the resource doesn''t exist. Supported resources are: - Reservations No Google IAM permissions are required to call this method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions_execute()` to send, or `bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions` for simplest API.

pub fn bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryreservation.googleapis.com/v1/projects/{}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:testIamPermissions
/// Gets your permissions on a resource. Returns an empty set of permissions if the resource doesn''t exist. Supported resources are: - Reservations No Google IAM permissions are required to call this method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions_execute()` or `bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:testIamPermissions
/// Gets your permissions on a resource. Returns an empty set of permissions if the resource doesn''t exist. Supported resources are: - Reservations No Google IAM permissions are required to call this method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions_task()`.
/// For the simplest API, use `bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryreservationProjectsLocationsReservationsAssignmentsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/assignments/{assignmentsId}:testIamPermissions
/// Gets your permissions on a resource. Returns an empty set of permissions if the resource doesn''t exist. Supported resources are: - Reservations No Google IAM permissions are required to call this method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions_builder()` + `bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions_execute()`.
/// For task-level control, use `bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &BigqueryreservationProjectsLocationsReservationsAssignmentsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions_builder(client, &args.resource)?;
    bigqueryreservation_projects_locations_reservations_assignments_test_iam_permissions_execute(
        builder,
    )
}

// =============================================================================
// ResourceIdentifier implementation for BiReservation
// =============================================================================

/// ResourceIdentifier implementation for BiReservation with BigqueryreservationProjectsLocationsGetBiReservationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsGetBiReservationArgs>
    for BiReservation
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsGetBiReservationArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::BiReservation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::BiReservation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SearchAllAssignmentsResponse
// =============================================================================

/// ResourceIdentifier implementation for SearchAllAssignmentsResponse with BigqueryreservationProjectsLocationsSearchAllAssignmentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsSearchAllAssignmentsArgs>
    for SearchAllAssignmentsResponse
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsSearchAllAssignmentsArgs,
    ) -> String {
        format!(
            "gcp::bigqueryreservation::SearchAllAssignmentsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::SearchAllAssignmentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SearchAssignmentsResponse
// =============================================================================

/// ResourceIdentifier implementation for SearchAssignmentsResponse with BigqueryreservationProjectsLocationsSearchAssignmentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsSearchAssignmentsArgs>
    for SearchAssignmentsResponse
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsSearchAssignmentsArgs,
    ) -> String {
        format!(
            "gcp::bigqueryreservation::SearchAssignmentsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::SearchAssignmentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BiReservation
// =============================================================================

/// ResourceIdentifier implementation for BiReservation with BigqueryreservationProjectsLocationsUpdateBiReservationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsUpdateBiReservationArgs>
    for BiReservation
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsUpdateBiReservationArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::BiReservation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::BiReservation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CapacityCommitment
// =============================================================================

/// ResourceIdentifier implementation for CapacityCommitment with BigqueryreservationProjectsLocationsCapacityCommitmentsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsCapacityCommitmentsCreateArgs>
    for CapacityCommitment
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsCapacityCommitmentsCreateArgs,
    ) -> String {
        format!(
            "gcp::bigqueryreservation::CapacityCommitment/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::CapacityCommitment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BigqueryreservationProjectsLocationsCapacityCommitmentsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsCapacityCommitmentsDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsCapacityCommitmentsDeleteArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CapacityCommitment
// =============================================================================

/// ResourceIdentifier implementation for CapacityCommitment with BigqueryreservationProjectsLocationsCapacityCommitmentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsCapacityCommitmentsGetArgs>
    for CapacityCommitment
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsCapacityCommitmentsGetArgs,
    ) -> String {
        format!(
            "gcp::bigqueryreservation::CapacityCommitment/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::CapacityCommitment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCapacityCommitmentsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCapacityCommitmentsResponse with BigqueryreservationProjectsLocationsCapacityCommitmentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsCapacityCommitmentsListArgs>
    for ListCapacityCommitmentsResponse
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsCapacityCommitmentsListArgs,
    ) -> String {
        format!(
            "gcp::bigqueryreservation::ListCapacityCommitmentsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::ListCapacityCommitmentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CapacityCommitment
// =============================================================================

/// ResourceIdentifier implementation for CapacityCommitment with BigqueryreservationProjectsLocationsCapacityCommitmentsMergeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsCapacityCommitmentsMergeArgs>
    for CapacityCommitment
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsCapacityCommitmentsMergeArgs,
    ) -> String {
        format!(
            "gcp::bigqueryreservation::CapacityCommitment/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::CapacityCommitment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CapacityCommitment
// =============================================================================

/// ResourceIdentifier implementation for CapacityCommitment with BigqueryreservationProjectsLocationsCapacityCommitmentsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsCapacityCommitmentsPatchArgs>
    for CapacityCommitment
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsCapacityCommitmentsPatchArgs,
    ) -> String {
        format!(
            "gcp::bigqueryreservation::CapacityCommitment/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::CapacityCommitment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SplitCapacityCommitmentResponse
// =============================================================================

/// ResourceIdentifier implementation for SplitCapacityCommitmentResponse with BigqueryreservationProjectsLocationsCapacityCommitmentsSplitArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsCapacityCommitmentsSplitArgs>
    for SplitCapacityCommitmentResponse
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsCapacityCommitmentsSplitArgs,
    ) -> String {
        format!(
            "gcp::bigqueryreservation::SplitCapacityCommitmentResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::SplitCapacityCommitmentResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReservationGroup
// =============================================================================

/// ResourceIdentifier implementation for ReservationGroup with BigqueryreservationProjectsLocationsReservationGroupsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationGroupsCreateArgs>
    for ReservationGroup
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationGroupsCreateArgs,
    ) -> String {
        format!(
            "gcp::bigqueryreservation::ReservationGroup/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::ReservationGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BigqueryreservationProjectsLocationsReservationGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationGroupsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationGroupsDeleteArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReservationGroup
// =============================================================================

/// ResourceIdentifier implementation for ReservationGroup with BigqueryreservationProjectsLocationsReservationGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationGroupsGetArgs>
    for ReservationGroup
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationGroupsGetArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::ReservationGroup/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::ReservationGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListReservationGroupsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListReservationGroupsResponse with BigqueryreservationProjectsLocationsReservationGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationGroupsListArgs>
    for ListReservationGroupsResponse
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationGroupsListArgs,
    ) -> String {
        format!(
            "gcp::bigqueryreservation::ListReservationGroupsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::ListReservationGroupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Reservation
// =============================================================================

/// ResourceIdentifier implementation for Reservation with BigqueryreservationProjectsLocationsReservationsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationsCreateArgs>
    for Reservation
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationsCreateArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::Reservation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::Reservation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BigqueryreservationProjectsLocationsReservationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationsDeleteArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Reservation
// =============================================================================

/// ResourceIdentifier implementation for Reservation with BigqueryreservationProjectsLocationsReservationsFailoverReservationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationsFailoverReservationArgs>
    for Reservation
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationsFailoverReservationArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::Reservation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::Reservation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Reservation
// =============================================================================

/// ResourceIdentifier implementation for Reservation with BigqueryreservationProjectsLocationsReservationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationsGetArgs> for Reservation {
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationsGetArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::Reservation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::Reservation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with BigqueryreservationProjectsLocationsReservationsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationsGetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListReservationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListReservationsResponse with BigqueryreservationProjectsLocationsReservationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationsListArgs>
    for ListReservationsResponse
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationsListArgs,
    ) -> String {
        format!(
            "gcp::bigqueryreservation::ListReservationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::ListReservationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Reservation
// =============================================================================

/// ResourceIdentifier implementation for Reservation with BigqueryreservationProjectsLocationsReservationsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationsPatchArgs> for Reservation {
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationsPatchArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::Reservation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::Reservation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with BigqueryreservationProjectsLocationsReservationsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationsSetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with BigqueryreservationProjectsLocationsReservationsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::bigqueryreservation::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Assignment
// =============================================================================

/// ResourceIdentifier implementation for Assignment with BigqueryreservationProjectsLocationsReservationsAssignmentsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationsAssignmentsCreateArgs>
    for Assignment
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationsAssignmentsCreateArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::Assignment/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::Assignment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BigqueryreservationProjectsLocationsReservationsAssignmentsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationsAssignmentsDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationsAssignmentsDeleteArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with BigqueryreservationProjectsLocationsReservationsAssignmentsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationsAssignmentsGetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationsAssignmentsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAssignmentsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAssignmentsResponse with BigqueryreservationProjectsLocationsReservationsAssignmentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationsAssignmentsListArgs>
    for ListAssignmentsResponse
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationsAssignmentsListArgs,
    ) -> String {
        format!(
            "gcp::bigqueryreservation::ListAssignmentsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::ListAssignmentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Assignment
// =============================================================================

/// ResourceIdentifier implementation for Assignment with BigqueryreservationProjectsLocationsReservationsAssignmentsMoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationsAssignmentsMoveArgs>
    for Assignment
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationsAssignmentsMoveArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::Assignment/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::Assignment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Assignment
// =============================================================================

/// ResourceIdentifier implementation for Assignment with BigqueryreservationProjectsLocationsReservationsAssignmentsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationsAssignmentsPatchArgs>
    for Assignment
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationsAssignmentsPatchArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::Assignment/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::Assignment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with BigqueryreservationProjectsLocationsReservationsAssignmentsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryreservationProjectsLocationsReservationsAssignmentsSetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationsAssignmentsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::bigqueryreservation::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with BigqueryreservationProjectsLocationsReservationsAssignmentsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        BigqueryreservationProjectsLocationsReservationsAssignmentsTestIamPermissionsArgs,
    > for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &BigqueryreservationProjectsLocationsReservationsAssignmentsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::bigqueryreservation::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryreservation::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
