//! Auto-generated API clients for gcp/smartdevicemanagement.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+name}:executeCommand
/// Executes a command to device managed by the enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `smartdevicemanagement_enterprises_devices_execute_command_execute()` to send, or `smartdevicemanagement_enterprises_devices_execute_command` for simplest API.

pub fn smartdevicemanagement_enterprises_devices_execute_command_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleHomeEnterpriseSdmV1ExecuteDeviceCommandRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:executeCommand", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:executeCommand
/// Executes a command to device managed by the enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `smartdevicemanagement_enterprises_devices_execute_command_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from smartdevicemanagement_enterprises_devices_execute_command_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn smartdevicemanagement_enterprises_devices_execute_command_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleHomeEnterpriseSdmV1ExecuteDeviceCommandResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleHomeEnterpriseSdmV1ExecuteDeviceCommandResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:executeCommand
/// Executes a command to device managed by the enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `smartdevicemanagement_enterprises_devices_execute_command_builder()` + `smartdevicemanagement_enterprises_devices_execute_command_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn smartdevicemanagement_enterprises_devices_execute_command(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleHomeEnterpriseSdmV1ExecuteDeviceCommandRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleHomeEnterpriseSdmV1ExecuteDeviceCommandResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        smartdevicemanagement_enterprises_devices_execute_command_builder(client, name, body)?;
    smartdevicemanagement_enterprises_devices_execute_command_execute(builder)
}

/// GET v1/{+name}
/// Gets a device managed by the enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `smartdevicemanagement_enterprises_devices_get_execute()` to send, or `smartdevicemanagement_enterprises_devices_get` for simplest API.

pub fn smartdevicemanagement_enterprises_devices_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a device managed by the enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `smartdevicemanagement_enterprises_devices_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from smartdevicemanagement_enterprises_devices_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn smartdevicemanagement_enterprises_devices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleHomeEnterpriseSdmV1Device>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleHomeEnterpriseSdmV1Device = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a device managed by the enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `smartdevicemanagement_enterprises_devices_get_builder()` + `smartdevicemanagement_enterprises_devices_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn smartdevicemanagement_enterprises_devices_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleHomeEnterpriseSdmV1Device>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = smartdevicemanagement_enterprises_devices_get_builder(client, name)?;
    smartdevicemanagement_enterprises_devices_get_execute(builder)
}

/// GET v1/{+parent}/devices
/// Lists devices managed by the enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `smartdevicemanagement_enterprises_devices_list_execute()` to send, or `smartdevicemanagement_enterprises_devices_list` for simplest API.

pub fn smartdevicemanagement_enterprises_devices_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/devices", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/devices
/// Lists devices managed by the enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `smartdevicemanagement_enterprises_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from smartdevicemanagement_enterprises_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn smartdevicemanagement_enterprises_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleHomeEnterpriseSdmV1ListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleHomeEnterpriseSdmV1ListDevicesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/devices
/// Lists devices managed by the enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `smartdevicemanagement_enterprises_devices_list_builder()` + `smartdevicemanagement_enterprises_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn smartdevicemanagement_enterprises_devices_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleHomeEnterpriseSdmV1ListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = smartdevicemanagement_enterprises_devices_list_builder(client, parent, filter)?;
    smartdevicemanagement_enterprises_devices_list_execute(builder)
}

/// GET v1/{+name}
/// Gets a structure managed by the enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `smartdevicemanagement_enterprises_structures_get_execute()` to send, or `smartdevicemanagement_enterprises_structures_get` for simplest API.

pub fn smartdevicemanagement_enterprises_structures_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a structure managed by the enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `smartdevicemanagement_enterprises_structures_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from smartdevicemanagement_enterprises_structures_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn smartdevicemanagement_enterprises_structures_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleHomeEnterpriseSdmV1Structure>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleHomeEnterpriseSdmV1Structure = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a structure managed by the enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `smartdevicemanagement_enterprises_structures_get_builder()` + `smartdevicemanagement_enterprises_structures_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn smartdevicemanagement_enterprises_structures_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleHomeEnterpriseSdmV1Structure>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = smartdevicemanagement_enterprises_structures_get_builder(client, name)?;
    smartdevicemanagement_enterprises_structures_get_execute(builder)
}

/// GET v1/{+parent}/structures
/// Lists structures managed by the enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `smartdevicemanagement_enterprises_structures_list_execute()` to send, or `smartdevicemanagement_enterprises_structures_list` for simplest API.

pub fn smartdevicemanagement_enterprises_structures_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/structures", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/structures
/// Lists structures managed by the enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `smartdevicemanagement_enterprises_structures_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from smartdevicemanagement_enterprises_structures_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn smartdevicemanagement_enterprises_structures_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleHomeEnterpriseSdmV1ListStructuresResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleHomeEnterpriseSdmV1ListStructuresResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/structures
/// Lists structures managed by the enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `smartdevicemanagement_enterprises_structures_list_builder()` + `smartdevicemanagement_enterprises_structures_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn smartdevicemanagement_enterprises_structures_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleHomeEnterpriseSdmV1ListStructuresResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        smartdevicemanagement_enterprises_structures_list_builder(client, parent, filter)?;
    smartdevicemanagement_enterprises_structures_list_execute(builder)
}

/// GET v1/{+name}
/// Gets a room managed by the enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `smartdevicemanagement_enterprises_structures_rooms_get_execute()` to send, or `smartdevicemanagement_enterprises_structures_rooms_get` for simplest API.

pub fn smartdevicemanagement_enterprises_structures_rooms_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a room managed by the enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `smartdevicemanagement_enterprises_structures_rooms_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from smartdevicemanagement_enterprises_structures_rooms_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn smartdevicemanagement_enterprises_structures_rooms_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleHomeEnterpriseSdmV1Room>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleHomeEnterpriseSdmV1Room = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a room managed by the enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `smartdevicemanagement_enterprises_structures_rooms_get_builder()` + `smartdevicemanagement_enterprises_structures_rooms_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn smartdevicemanagement_enterprises_structures_rooms_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleHomeEnterpriseSdmV1Room>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = smartdevicemanagement_enterprises_structures_rooms_get_builder(client, name)?;
    smartdevicemanagement_enterprises_structures_rooms_get_execute(builder)
}

/// GET v1/{+parent}/rooms
/// Lists rooms managed by the enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `smartdevicemanagement_enterprises_structures_rooms_list_execute()` to send, or `smartdevicemanagement_enterprises_structures_rooms_list` for simplest API.

pub fn smartdevicemanagement_enterprises_structures_rooms_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/rooms", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/rooms
/// Lists rooms managed by the enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `smartdevicemanagement_enterprises_structures_rooms_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from smartdevicemanagement_enterprises_structures_rooms_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn smartdevicemanagement_enterprises_structures_rooms_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleHomeEnterpriseSdmV1ListRoomsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleHomeEnterpriseSdmV1ListRoomsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/rooms
/// Lists rooms managed by the enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `smartdevicemanagement_enterprises_structures_rooms_list_builder()` + `smartdevicemanagement_enterprises_structures_rooms_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn smartdevicemanagement_enterprises_structures_rooms_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleHomeEnterpriseSdmV1ListRoomsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = smartdevicemanagement_enterprises_structures_rooms_list_builder(client, parent)?;
    smartdevicemanagement_enterprises_structures_rooms_list_execute(builder)
}
