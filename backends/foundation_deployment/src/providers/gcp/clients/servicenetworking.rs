//! Auto-generated API clients for Google Cloud Platform - servicenetworking.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_operations_cancel_execute()` to send, or `servicenetworking_operations_cancel` for simplest API.

pub fn servicenetworking_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_operations_cancel_builder()` + `servicenetworking_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = servicenetworking_operations_cancel_builder(client, name, body)?;
    servicenetworking_operations_cancel_execute(builder)
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_operations_delete_execute()` to send, or `servicenetworking_operations_delete` for simplest API.

pub fn servicenetworking_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_operations_delete_builder()` + `servicenetworking_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_operations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = servicenetworking_operations_delete_builder(client, name)?;
    servicenetworking_operations_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_operations_get_execute()` to send, or `servicenetworking_operations_get` for simplest API.

pub fn servicenetworking_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_operations_get_builder()` + `servicenetworking_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = servicenetworking_operations_get_builder(client, name)?;
    servicenetworking_operations_get_execute(builder)
}

/// GET v1/{+name}
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_operations_list_execute()` to send, or `servicenetworking_operations_list` for simplest API.

pub fn servicenetworking_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_operations_list_builder()` + `servicenetworking_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = servicenetworking_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    servicenetworking_operations_list_execute(builder)
}

/// GET v1/{+parent}:addSubnetwork
/// For service producers, provisions a new subnet in a peered service's shared VPC network in the requested region and with the requested size that's expressed as a CIDR range (number of leading bits of `ipV4` network mask). The method checks against the assigned allocated ranges to find a non-conflicting IP address range. The method will reuse a subnet if subsequent calls contain the same subnet name, region, and prefix length. This method will make producer's tenant project to be a shared VPC service project as needed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_add_subnetwork_execute()` to send, or `servicenetworking_services_add_subnetwork` for simplest API.

pub fn servicenetworking_services_add_subnetwork_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AddSubnetworkRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}:addSubnetwork", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}:addSubnetwork
/// For service producers, provisions a new subnet in a peered service's shared VPC network in the requested region and with the requested size that's expressed as a CIDR range (number of leading bits of `ipV4` network mask). The method checks against the assigned allocated ranges to find a non-conflicting IP address range. The method will reuse a subnet if subsequent calls contain the same subnet name, region, and prefix length. This method will make producer's tenant project to be a shared VPC service project as needed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_add_subnetwork_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_add_subnetwork_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_add_subnetwork_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}:addSubnetwork
/// For service producers, provisions a new subnet in a peered service's shared VPC network in the requested region and with the requested size that's expressed as a CIDR range (number of leading bits of `ipV4` network mask). The method checks against the assigned allocated ranges to find a non-conflicting IP address range. The method will reuse a subnet if subsequent calls contain the same subnet name, region, and prefix length. This method will make producer's tenant project to be a shared VPC service project as needed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_add_subnetwork_builder()` + `servicenetworking_services_add_subnetwork_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_add_subnetwork(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AddSubnetworkRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = servicenetworking_services_add_subnetwork_builder(client, parent, body)?;
    servicenetworking_services_add_subnetwork_execute(builder)
}

/// GET v1/{+parent}:disableVpcServiceControls
/// Disables VPC service controls for a connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_disable_vpc_service_controls_execute()` to send, or `servicenetworking_services_disable_vpc_service_controls` for simplest API.

pub fn servicenetworking_services_disable_vpc_service_controls_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &DisableVpcServiceControlsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}:disableVpcServiceControls",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}:disableVpcServiceControls
/// Disables VPC service controls for a connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_disable_vpc_service_controls_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_disable_vpc_service_controls_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_disable_vpc_service_controls_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}:disableVpcServiceControls
/// Disables VPC service controls for a connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_disable_vpc_service_controls_builder()` + `servicenetworking_services_disable_vpc_service_controls_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_disable_vpc_service_controls(
    client: &SimpleHttpClient,
    parent: &str,
    body: &DisableVpcServiceControlsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        servicenetworking_services_disable_vpc_service_controls_builder(client, parent, body)?;
    servicenetworking_services_disable_vpc_service_controls_execute(builder)
}

/// GET v1/{+parent}:enableVpcServiceControls
/// Enables VPC service controls for a connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_enable_vpc_service_controls_execute()` to send, or `servicenetworking_services_enable_vpc_service_controls` for simplest API.

pub fn servicenetworking_services_enable_vpc_service_controls_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EnableVpcServiceControlsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}:enableVpcServiceControls",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}:enableVpcServiceControls
/// Enables VPC service controls for a connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_enable_vpc_service_controls_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_enable_vpc_service_controls_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_enable_vpc_service_controls_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}:enableVpcServiceControls
/// Enables VPC service controls for a connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_enable_vpc_service_controls_builder()` + `servicenetworking_services_enable_vpc_service_controls_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_enable_vpc_service_controls(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EnableVpcServiceControlsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        servicenetworking_services_enable_vpc_service_controls_builder(client, parent, body)?;
    servicenetworking_services_enable_vpc_service_controls_execute(builder)
}

/// GET v1/{+parent}:searchRange
/// Service producers can use this method to find a currently unused range within consumer allocated ranges. This returned range is not reserved, and not guaranteed to remain unused. It will validate previously provided allocated ranges, find non-conflicting sub-range of requested size (expressed in number of leading bits of ipv4 network mask, as in CIDR range notation).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_search_range_execute()` to send, or `servicenetworking_services_search_range` for simplest API.

pub fn servicenetworking_services_search_range_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SearchRangeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}:searchRange", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}:searchRange
/// Service producers can use this method to find a currently unused range within consumer allocated ranges. This returned range is not reserved, and not guaranteed to remain unused. It will validate previously provided allocated ranges, find non-conflicting sub-range of requested size (expressed in number of leading bits of ipv4 network mask, as in CIDR range notation).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_search_range_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_search_range_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_search_range_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}:searchRange
/// Service producers can use this method to find a currently unused range within consumer allocated ranges. This returned range is not reserved, and not guaranteed to remain unused. It will validate previously provided allocated ranges, find non-conflicting sub-range of requested size (expressed in number of leading bits of ipv4 network mask, as in CIDR range notation).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_search_range_builder()` + `servicenetworking_services_search_range_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_search_range(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SearchRangeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = servicenetworking_services_search_range_builder(client, parent, body)?;
    servicenetworking_services_search_range_execute(builder)
}

/// GET v1/{+parent}:validate
/// Service producers use this method to validate if the consumer provided network, project and requested range are valid. This allows them to use a fail-fast mechanism for consumer requests, and not have to wait for AddSubnetwork operation completion to determine if user request is invalid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_validate_execute()` to send, or `servicenetworking_services_validate` for simplest API.

pub fn servicenetworking_services_validate_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ValidateConsumerConfigRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}:validate", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}:validate
/// Service producers use this method to validate if the consumer provided network, project and requested range are valid. This allows them to use a fail-fast mechanism for consumer requests, and not have to wait for AddSubnetwork operation completion to determine if user request is invalid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_validate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_validate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_validate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateConsumerConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValidateConsumerConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}:validate
/// Service producers use this method to validate if the consumer provided network, project and requested range are valid. This allows them to use a fail-fast mechanism for consumer requests, and not have to wait for AddSubnetwork operation completion to determine if user request is invalid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_validate_builder()` + `servicenetworking_services_validate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_validate(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ValidateConsumerConfigRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateConsumerConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = servicenetworking_services_validate_builder(client, parent, body)?;
    servicenetworking_services_validate_execute(builder)
}

/// GET v1/{+parent}/connections
/// Creates a private connection that establishes a VPC Network Peering connection to a VPC network in the service producer's organization. The administrator of the service consumer's VPC network invokes this method. The administrator must assign one or more allocated IP ranges for provisioning subnetworks in the service producer's VPC network. This connection is used for all supported services in the service producer's organization, so it only needs to be invoked once.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_connections_create_execute()` to send, or `servicenetworking_services_connections_create` for simplest API.

pub fn servicenetworking_services_connections_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Connection,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/connections", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/connections
/// Creates a private connection that establishes a VPC Network Peering connection to a VPC network in the service producer's organization. The administrator of the service consumer's VPC network invokes this method. The administrator must assign one or more allocated IP ranges for provisioning subnetworks in the service producer's VPC network. This connection is used for all supported services in the service producer's organization, so it only needs to be invoked once.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_connections_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_connections_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_connections_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/connections
/// Creates a private connection that establishes a VPC Network Peering connection to a VPC network in the service producer's organization. The administrator of the service consumer's VPC network invokes this method. The administrator must assign one or more allocated IP ranges for provisioning subnetworks in the service producer's VPC network. This connection is used for all supported services in the service producer's organization, so it only needs to be invoked once.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_connections_create_builder()` + `servicenetworking_services_connections_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_connections_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Connection,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = servicenetworking_services_connections_create_builder(client, parent, body)?;
    servicenetworking_services_connections_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a private service access connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_connections_delete_connection_execute()` to send, or `servicenetworking_services_connections_delete_connection` for simplest API.

pub fn servicenetworking_services_connections_delete_connection_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &DeleteConnectionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Deletes a private service access connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_connections_delete_connection_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_connections_delete_connection_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_connections_delete_connection_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a private service access connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_connections_delete_connection_builder()` + `servicenetworking_services_connections_delete_connection_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_connections_delete_connection(
    client: &SimpleHttpClient,
    name: &str,
    body: &DeleteConnectionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        servicenetworking_services_connections_delete_connection_builder(client, name, body)?;
    servicenetworking_services_connections_delete_connection_execute(builder)
}

/// GET v1/{+parent}/connections
/// List the private connections that are configured in a service consumer's VPC network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_connections_list_execute()` to send, or `servicenetworking_services_connections_list` for simplest API.

pub fn servicenetworking_services_connections_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    network: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/connections", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "network",
            network
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/connections
/// List the private connections that are configured in a service consumer's VPC network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_connections_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_connections_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_connections_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListConnectionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListConnectionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/connections
/// List the private connections that are configured in a service consumer's VPC network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_connections_list_builder()` + `servicenetworking_services_connections_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_connections_list(
    client: &SimpleHttpClient,
    parent: &str,
    network: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListConnectionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = servicenetworking_services_connections_list_builder(client, parent, network)?;
    servicenetworking_services_connections_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the allocated ranges that are assigned to a connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_connections_patch_execute()` to send, or `servicenetworking_services_connections_patch` for simplest API.

pub fn servicenetworking_services_connections_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
    updateMask: Option<&str>,
    body: &Connection,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "force",
            force
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the allocated ranges that are assigned to a connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_connections_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_connections_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_connections_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the allocated ranges that are assigned to a connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_connections_patch_builder()` + `servicenetworking_services_connections_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_connections_patch(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
    updateMask: Option<&str>,
    body: &Connection,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = servicenetworking_services_connections_patch_builder(
        client, name, force, updateMask, body,
    )?;
    servicenetworking_services_connections_patch_execute(builder)
}

/// GET v1/{+parent}/dnsRecordSets:add
/// Service producers can use this method to add DNS record sets to private DNS zones in the shared producer host project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_dns_record_sets_add_execute()` to send, or `servicenetworking_services_dns_record_sets_add` for simplest API.

pub fn servicenetworking_services_dns_record_sets_add_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AddDnsRecordSetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/dnsRecordSets:add",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/dnsRecordSets:add
/// Service producers can use this method to add DNS record sets to private DNS zones in the shared producer host project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_dns_record_sets_add_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_dns_record_sets_add_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_dns_record_sets_add_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/dnsRecordSets:add
/// Service producers can use this method to add DNS record sets to private DNS zones in the shared producer host project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_dns_record_sets_add_builder()` + `servicenetworking_services_dns_record_sets_add_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_dns_record_sets_add(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AddDnsRecordSetRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = servicenetworking_services_dns_record_sets_add_builder(client, parent, body)?;
    servicenetworking_services_dns_record_sets_add_execute(builder)
}

/// GET v1/{+parent}/dnsRecordSets:get
/// Producers can use this method to retrieve information about the DNS record set added to the private zone inside the shared tenant host project associated with a consumer network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_dns_record_sets_get_execute()` to send, or `servicenetworking_services_dns_record_sets_get` for simplest API.

pub fn servicenetworking_services_dns_record_sets_get_builder(
    client: &SimpleHttpClient,
    parent: &str,
    consumerNetwork: Option<&str>,
    domain: Option<&str>,
    type_rs: Option<&str>,
    zone: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/dnsRecordSets:get",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "consumerNetwork",
            consumerNetwork
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "domain",
            domain
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "type",
            type_rs
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "zone",
            zone.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/dnsRecordSets:get
/// Producers can use this method to retrieve information about the DNS record set added to the private zone inside the shared tenant host project associated with a consumer network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_dns_record_sets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_dns_record_sets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_dns_record_sets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DnsRecordSet>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DnsRecordSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/dnsRecordSets:get
/// Producers can use this method to retrieve information about the DNS record set added to the private zone inside the shared tenant host project associated with a consumer network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_dns_record_sets_get_builder()` + `servicenetworking_services_dns_record_sets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_dns_record_sets_get(
    client: &SimpleHttpClient,
    parent: &str,
    consumerNetwork: Option<&str>,
    domain: Option<&str>,
    type_rs: Option<&str>,
    zone: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DnsRecordSet>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = servicenetworking_services_dns_record_sets_get_builder(
        client,
        parent,
        consumerNetwork,
        domain,
        type_rs,
        zone,
    )?;
    servicenetworking_services_dns_record_sets_get_execute(builder)
}

/// GET v1/{+parent}/dnsRecordSets:list
/// Producers can use this method to retrieve a list of available DNS RecordSets available inside the private zone on the tenant host project accessible from their network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_dns_record_sets_list_execute()` to send, or `servicenetworking_services_dns_record_sets_list` for simplest API.

pub fn servicenetworking_services_dns_record_sets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    consumerNetwork: Option<&str>,
    zone: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/dnsRecordSets:list",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "consumerNetwork",
            consumerNetwork
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "zone",
            zone.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/dnsRecordSets:list
/// Producers can use this method to retrieve a list of available DNS RecordSets available inside the private zone on the tenant host project accessible from their network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_dns_record_sets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_dns_record_sets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_dns_record_sets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDnsRecordSetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDnsRecordSetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/dnsRecordSets:list
/// Producers can use this method to retrieve a list of available DNS RecordSets available inside the private zone on the tenant host project accessible from their network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_dns_record_sets_list_builder()` + `servicenetworking_services_dns_record_sets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_dns_record_sets_list(
    client: &SimpleHttpClient,
    parent: &str,
    consumerNetwork: Option<&str>,
    zone: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDnsRecordSetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = servicenetworking_services_dns_record_sets_list_builder(
        client,
        parent,
        consumerNetwork,
        zone,
    )?;
    servicenetworking_services_dns_record_sets_list_execute(builder)
}

/// GET v1/{+parent}/dnsRecordSets:remove
/// Service producers can use this method to remove DNS record sets from private DNS zones in the shared producer host project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_dns_record_sets_remove_execute()` to send, or `servicenetworking_services_dns_record_sets_remove` for simplest API.

pub fn servicenetworking_services_dns_record_sets_remove_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &RemoveDnsRecordSetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/dnsRecordSets:remove",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/dnsRecordSets:remove
/// Service producers can use this method to remove DNS record sets from private DNS zones in the shared producer host project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_dns_record_sets_remove_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_dns_record_sets_remove_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_dns_record_sets_remove_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/dnsRecordSets:remove
/// Service producers can use this method to remove DNS record sets from private DNS zones in the shared producer host project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_dns_record_sets_remove_builder()` + `servicenetworking_services_dns_record_sets_remove_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_dns_record_sets_remove(
    client: &SimpleHttpClient,
    parent: &str,
    body: &RemoveDnsRecordSetRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = servicenetworking_services_dns_record_sets_remove_builder(client, parent, body)?;
    servicenetworking_services_dns_record_sets_remove_execute(builder)
}

/// GET v1/{+parent}/dnsRecordSets:update
/// Service producers can use this method to update DNS record sets from private DNS zones in the shared producer host project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_dns_record_sets_update_execute()` to send, or `servicenetworking_services_dns_record_sets_update` for simplest API.

pub fn servicenetworking_services_dns_record_sets_update_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &UpdateDnsRecordSetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/dnsRecordSets:update",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/dnsRecordSets:update
/// Service producers can use this method to update DNS record sets from private DNS zones in the shared producer host project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_dns_record_sets_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_dns_record_sets_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_dns_record_sets_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/dnsRecordSets:update
/// Service producers can use this method to update DNS record sets from private DNS zones in the shared producer host project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_dns_record_sets_update_builder()` + `servicenetworking_services_dns_record_sets_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_dns_record_sets_update(
    client: &SimpleHttpClient,
    parent: &str,
    body: &UpdateDnsRecordSetRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = servicenetworking_services_dns_record_sets_update_builder(client, parent, body)?;
    servicenetworking_services_dns_record_sets_update_execute(builder)
}

/// GET v1/{+parent}/dnsZones:add
/// Service producers can use this method to add private DNS zones in the shared producer host project and matching peering zones in the consumer project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_dns_zones_add_execute()` to send, or `servicenetworking_services_dns_zones_add` for simplest API.

pub fn servicenetworking_services_dns_zones_add_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AddDnsZoneRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/dnsZones:add", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/dnsZones:add
/// Service producers can use this method to add private DNS zones in the shared producer host project and matching peering zones in the consumer project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_dns_zones_add_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_dns_zones_add_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_dns_zones_add_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/dnsZones:add
/// Service producers can use this method to add private DNS zones in the shared producer host project and matching peering zones in the consumer project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_dns_zones_add_builder()` + `servicenetworking_services_dns_zones_add_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_dns_zones_add(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AddDnsZoneRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = servicenetworking_services_dns_zones_add_builder(client, parent, body)?;
    servicenetworking_services_dns_zones_add_execute(builder)
}

/// GET v1/{+parent}/dnsZones:remove
/// Service producers can use this method to remove private DNS zones in the shared producer host project and matching peering zones in the consumer project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_dns_zones_remove_execute()` to send, or `servicenetworking_services_dns_zones_remove` for simplest API.

pub fn servicenetworking_services_dns_zones_remove_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &RemoveDnsZoneRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/dnsZones:remove",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/dnsZones:remove
/// Service producers can use this method to remove private DNS zones in the shared producer host project and matching peering zones in the consumer project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_dns_zones_remove_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_dns_zones_remove_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_dns_zones_remove_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/dnsZones:remove
/// Service producers can use this method to remove private DNS zones in the shared producer host project and matching peering zones in the consumer project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_dns_zones_remove_builder()` + `servicenetworking_services_dns_zones_remove_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_dns_zones_remove(
    client: &SimpleHttpClient,
    parent: &str,
    body: &RemoveDnsZoneRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = servicenetworking_services_dns_zones_remove_builder(client, parent, body)?;
    servicenetworking_services_dns_zones_remove_execute(builder)
}

/// GET v1/{+name}
/// Service producers use this method to get the configuration of their connection including the `import/export` of custom routes and subnetwork routes with public IP.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_projects_global_networks_get_execute()` to send, or `servicenetworking_services_projects_global_networks_get` for simplest API.

pub fn servicenetworking_services_projects_global_networks_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    includeUsedIpRanges: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "includeUsedIpRanges",
            includeUsedIpRanges
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Service producers use this method to get the configuration of their connection including the `import/export` of custom routes and subnetwork routes with public IP.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_projects_global_networks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_projects_global_networks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_projects_global_networks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConsumerConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConsumerConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Service producers use this method to get the configuration of their connection including the `import/export` of custom routes and subnetwork routes with public IP.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_projects_global_networks_get_builder()` + `servicenetworking_services_projects_global_networks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_projects_global_networks_get(
    client: &SimpleHttpClient,
    name: &str,
    includeUsedIpRanges: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConsumerConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = servicenetworking_services_projects_global_networks_get_builder(
        client,
        name,
        includeUsedIpRanges,
    )?;
    servicenetworking_services_projects_global_networks_get_execute(builder)
}

/// GET v1/{+name}/vpcServiceControls
/// Consumers use this method to find out the state of VPC Service Controls. The controls could be enabled or disabled for a connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_projects_global_networks_get_vpc_service_controls_execute()` to send, or `servicenetworking_services_projects_global_networks_get_vpc_service_controls` for simplest API.

pub fn servicenetworking_services_projects_global_networks_get_vpc_service_controls_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}/vpcServiceControls", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/vpcServiceControls
/// Consumers use this method to find out the state of VPC Service Controls. The controls could be enabled or disabled for a connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_projects_global_networks_get_vpc_service_controls_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_projects_global_networks_get_vpc_service_controls_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_projects_global_networks_get_vpc_service_controls_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpcServiceControls>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VpcServiceControls = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/vpcServiceControls
/// Consumers use this method to find out the state of VPC Service Controls. The controls could be enabled or disabled for a connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_projects_global_networks_get_vpc_service_controls_builder()` + `servicenetworking_services_projects_global_networks_get_vpc_service_controls_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_projects_global_networks_get_vpc_service_controls(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpcServiceControls>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        servicenetworking_services_projects_global_networks_get_vpc_service_controls_builder(
            client, name,
        )?;
    servicenetworking_services_projects_global_networks_get_vpc_service_controls_execute(builder)
}

/// GET v1/{+parent}:updateConsumerConfig
/// Service producers use this method to update the configuration of their connection including the `import/export` of custom routes and subnetwork routes with public IP.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_projects_global_networks_update_consumer_config_execute()` to send, or `servicenetworking_services_projects_global_networks_update_consumer_config` for simplest API.

pub fn servicenetworking_services_projects_global_networks_update_consumer_config_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &UpdateConsumerConfigRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}:updateConsumerConfig",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}:updateConsumerConfig
/// Service producers use this method to update the configuration of their connection including the `import/export` of custom routes and subnetwork routes with public IP.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_projects_global_networks_update_consumer_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_projects_global_networks_update_consumer_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_projects_global_networks_update_consumer_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}:updateConsumerConfig
/// Service producers use this method to update the configuration of their connection including the `import/export` of custom routes and subnetwork routes with public IP.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_projects_global_networks_update_consumer_config_builder()` + `servicenetworking_services_projects_global_networks_update_consumer_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_projects_global_networks_update_consumer_config(
    client: &SimpleHttpClient,
    parent: &str,
    body: &UpdateConsumerConfigRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        servicenetworking_services_projects_global_networks_update_consumer_config_builder(
            client, parent, body,
        )?;
    servicenetworking_services_projects_global_networks_update_consumer_config_execute(builder)
}

/// GET v1/{+name}
/// Service producers can use this method to retrieve a DNS zone in the shared producer host project and the matching peering zones in consumer project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_projects_global_networks_dns_zones_get_execute()` to send, or `servicenetworking_services_projects_global_networks_dns_zones_get` for simplest API.

pub fn servicenetworking_services_projects_global_networks_dns_zones_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Service producers can use this method to retrieve a DNS zone in the shared producer host project and the matching peering zones in consumer project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_projects_global_networks_dns_zones_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_projects_global_networks_dns_zones_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_projects_global_networks_dns_zones_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetDnsZoneResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetDnsZoneResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Service producers can use this method to retrieve a DNS zone in the shared producer host project and the matching peering zones in consumer project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_projects_global_networks_dns_zones_get_builder()` + `servicenetworking_services_projects_global_networks_dns_zones_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_projects_global_networks_dns_zones_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetDnsZoneResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        servicenetworking_services_projects_global_networks_dns_zones_get_builder(client, name)?;
    servicenetworking_services_projects_global_networks_dns_zones_get_execute(builder)
}

/// GET v1/{+parent}/dnsZones:list
/// * Service producers can use this method to retrieve a list of available DNS zones in the shared producer host project and the matching peering zones in the consumer project. *
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_projects_global_networks_dns_zones_list_execute()` to send, or `servicenetworking_services_projects_global_networks_dns_zones_list` for simplest API.

pub fn servicenetworking_services_projects_global_networks_dns_zones_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/dnsZones:list", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/dnsZones:list
/// * Service producers can use this method to retrieve a list of available DNS zones in the shared producer host project and the matching peering zones in the consumer project. *
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_projects_global_networks_dns_zones_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_projects_global_networks_dns_zones_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_projects_global_networks_dns_zones_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDnsZonesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDnsZonesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/dnsZones:list
/// * Service producers can use this method to retrieve a list of available DNS zones in the shared producer host project and the matching peering zones in the consumer project. *
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_projects_global_networks_dns_zones_list_builder()` + `servicenetworking_services_projects_global_networks_dns_zones_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_projects_global_networks_dns_zones_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDnsZonesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        servicenetworking_services_projects_global_networks_dns_zones_list_builder(client, parent)?;
    servicenetworking_services_projects_global_networks_dns_zones_list_execute(builder)
}

/// GET v1/{+parent}/peeredDnsDomains
/// Creates a peered DNS domain which sends requests for records in given namespace originating in the service producer VPC network to the consumer VPC network to be resolved.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_projects_global_networks_peered_dns_domains_create_execute()` to send, or `servicenetworking_services_projects_global_networks_peered_dns_domains_create` for simplest API.

pub fn servicenetworking_services_projects_global_networks_peered_dns_domains_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &PeeredDnsDomain,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/peeredDnsDomains",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/peeredDnsDomains
/// Creates a peered DNS domain which sends requests for records in given namespace originating in the service producer VPC network to the consumer VPC network to be resolved.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_projects_global_networks_peered_dns_domains_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_projects_global_networks_peered_dns_domains_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_projects_global_networks_peered_dns_domains_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/peeredDnsDomains
/// Creates a peered DNS domain which sends requests for records in given namespace originating in the service producer VPC network to the consumer VPC network to be resolved.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_projects_global_networks_peered_dns_domains_create_builder()` + `servicenetworking_services_projects_global_networks_peered_dns_domains_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_projects_global_networks_peered_dns_domains_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &PeeredDnsDomain,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        servicenetworking_services_projects_global_networks_peered_dns_domains_create_builder(
            client, parent, body,
        )?;
    servicenetworking_services_projects_global_networks_peered_dns_domains_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a peered DNS domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_projects_global_networks_peered_dns_domains_delete_execute()` to send, or `servicenetworking_services_projects_global_networks_peered_dns_domains_delete` for simplest API.

pub fn servicenetworking_services_projects_global_networks_peered_dns_domains_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a peered DNS domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_projects_global_networks_peered_dns_domains_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_projects_global_networks_peered_dns_domains_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_projects_global_networks_peered_dns_domains_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a peered DNS domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_projects_global_networks_peered_dns_domains_delete_builder()` + `servicenetworking_services_projects_global_networks_peered_dns_domains_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_projects_global_networks_peered_dns_domains_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        servicenetworking_services_projects_global_networks_peered_dns_domains_delete_builder(
            client, name,
        )?;
    servicenetworking_services_projects_global_networks_peered_dns_domains_delete_execute(builder)
}

/// GET v1/{+parent}/peeredDnsDomains
/// Lists peered DNS domains for a connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_projects_global_networks_peered_dns_domains_list_execute()` to send, or `servicenetworking_services_projects_global_networks_peered_dns_domains_list` for simplest API.

pub fn servicenetworking_services_projects_global_networks_peered_dns_domains_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/peeredDnsDomains",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/peeredDnsDomains
/// Lists peered DNS domains for a connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_projects_global_networks_peered_dns_domains_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_projects_global_networks_peered_dns_domains_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_projects_global_networks_peered_dns_domains_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPeeredDnsDomainsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPeeredDnsDomainsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/peeredDnsDomains
/// Lists peered DNS domains for a connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_projects_global_networks_peered_dns_domains_list_builder()` + `servicenetworking_services_projects_global_networks_peered_dns_domains_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_projects_global_networks_peered_dns_domains_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPeeredDnsDomainsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        servicenetworking_services_projects_global_networks_peered_dns_domains_list_builder(
            client, parent,
        )?;
    servicenetworking_services_projects_global_networks_peered_dns_domains_list_execute(builder)
}

/// GET v1/{+parent}/roles:add
/// Service producers can use this method to add roles in the shared VPC host project. Each role is bound to the provided member. Each role must be selected from within an allowlisted set of roles. Each role is applied at only the granularity specified in the allowlist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicenetworking_services_roles_add_execute()` to send, or `servicenetworking_services_roles_add` for simplest API.

pub fn servicenetworking_services_roles_add_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AddRolesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/roles:add", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/roles:add
/// Service producers can use this method to add roles in the shared VPC host project. Each role is bound to the provided member. Each role must be selected from within an allowlisted set of roles. Each role is applied at only the granularity specified in the allowlist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `servicenetworking_services_roles_add_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from servicenetworking_services_roles_add_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_roles_add_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/roles:add
/// Service producers can use this method to add roles in the shared VPC host project. Each role is bound to the provided member. Each role must be selected from within an allowlisted set of roles. Each role is applied at only the granularity specified in the allowlist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicenetworking_services_roles_add_builder()` + `servicenetworking_services_roles_add_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicenetworking_services_roles_add(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AddRolesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = servicenetworking_services_roles_add_builder(client, parent, body)?;
    servicenetworking_services_roles_add_execute(builder)
}
