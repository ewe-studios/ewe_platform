//! Auto-generated API clients for gcp/retail.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}:completeQuery
/// Completes the specified prefix with keyword suggestions. This feature is only available for users who have Retail Search enabled. Enable Retail Search on Cloud Console before using this feature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_complete_query_execute()` to send, or `retail_projects_locations_catalogs_complete_query` for simplest API.

pub fn retail_projects_locations_catalogs_complete_query_builder<R>(
    client: &SimpleHttpClient<R>,
    catalog: &String,
    dataset: &Option<String>,
    deviceType: &Option<String>,
    enableAttributeSuggestions: &Option<String>,
    entity: &Option<String>,
    languageCodes: &Option<String>,
    maxSuggestions: &Option<String>,
    query: &Option<String>,
    visitorId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}:completeQuery",
        catalog,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dataset.as_ref() {
        query_parts.push(format!("dataset={}", val));
    }
    if let Some(val) = deviceType.as_ref() {
        query_parts.push(format!("deviceType={}", val));
    }
    if let Some(val) = enableAttributeSuggestions.as_ref() {
        query_parts.push(format!("enableAttributeSuggestions={}", val));
    }
    if let Some(val) = entity.as_ref() {
        query_parts.push(format!("entity={}", val));
    }
    if let Some(val) = languageCodes.as_ref() {
        query_parts.push(format!("languageCodes={}", val));
    }
    if let Some(val) = maxSuggestions.as_ref() {
        query_parts.push(format!("maxSuggestions={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }
    if let Some(val) = visitorId.as_ref() {
        query_parts.push(format!("visitorId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}:completeQuery
/// Completes the specified prefix with keyword suggestions. This feature is only available for users who have Retail Search enabled. Enable Retail Search on Cloud Console before using this feature.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_complete_query_execute()` or `retail_projects_locations_catalogs_complete_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_complete_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_complete_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2CompleteQueryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2CompleteQueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}:completeQuery
/// Completes the specified prefix with keyword suggestions. This feature is only available for users who have Retail Search enabled. Enable Retail Search on Cloud Console before using this feature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_complete_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_complete_query_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_complete_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_complete_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_complete_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2CompleteQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_complete_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_complete_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsCompleteQueryArgs {
    /// Path parameter: catalog
    pub catalog: String,
    /// Query parameter: dataset
    pub dataset: Option<String>,
    /// Query parameter: deviceType
    pub deviceType: Option<String>,
    /// Query parameter: enableAttributeSuggestions
    pub enableAttributeSuggestions: Option<String>,
    /// Query parameter: entity
    pub entity: Option<String>,
    /// Query parameter: languageCodes
    pub languageCodes: Option<String>,
    /// Query parameter: maxSuggestions
    pub maxSuggestions: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
    /// Query parameter: visitorId
    pub visitorId: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}:completeQuery
/// Completes the specified prefix with keyword suggestions. This feature is only available for users who have Retail Search enabled. Enable Retail Search on Cloud Console before using this feature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_complete_query_builder()` + `retail_projects_locations_catalogs_complete_query_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_complete_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_complete_query(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsCompleteQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2CompleteQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_complete_query_builder(
        client,
        &args.catalog,
        &args.dataset,
        &args.deviceType,
        &args.enableAttributeSuggestions,
        &args.entity,
        &args.languageCodes,
        &args.maxSuggestions,
        &args.query,
        &args.visitorId,
    )?;
    retail_projects_locations_catalogs_complete_query_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}:exportAnalyticsMetrics
/// Exports analytics metrics. Operation.response is of type ExportAnalyticsMetricsResponse. Operation.metadata is of type ExportMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_export_analytics_metrics_execute()` to send, or `retail_projects_locations_catalogs_export_analytics_metrics` for simplest API.

pub fn retail_projects_locations_catalogs_export_analytics_metrics_builder<R>(
    client: &SimpleHttpClient<R>,
    catalog: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}:exportAnalyticsMetrics",
        catalog,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}:exportAnalyticsMetrics
/// Exports analytics metrics. Operation.response is of type ExportAnalyticsMetricsResponse. Operation.metadata is of type ExportMetadata.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_export_analytics_metrics_execute()` or `retail_projects_locations_catalogs_export_analytics_metrics`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_export_analytics_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_export_analytics_metrics_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}:exportAnalyticsMetrics
/// Exports analytics metrics. Operation.response is of type ExportAnalyticsMetricsResponse. Operation.metadata is of type ExportMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_export_analytics_metrics_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_export_analytics_metrics_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_export_analytics_metrics()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_export_analytics_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_export_analytics_metrics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_export_analytics_metrics_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_export_analytics_metrics`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsExportAnalyticsMetricsArgs {
    /// Path parameter: catalog
    pub catalog: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}:exportAnalyticsMetrics
/// Exports analytics metrics. Operation.response is of type ExportAnalyticsMetricsResponse. Operation.metadata is of type ExportMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_export_analytics_metrics_builder()` + `retail_projects_locations_catalogs_export_analytics_metrics_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_export_analytics_metrics_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_export_analytics_metrics(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsExportAnalyticsMetricsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_export_analytics_metrics_builder(client, &args.catalog)?;
    retail_projects_locations_catalogs_export_analytics_metrics_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig
/// Gets an AttributesConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_get_attributes_config_execute()` to send, or `retail_projects_locations_catalogs_get_attributes_config` for simplest API.

pub fn retail_projects_locations_catalogs_get_attributes_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig
/// Gets an AttributesConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_get_attributes_config_execute()` or `retail_projects_locations_catalogs_get_attributes_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_get_attributes_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_get_attributes_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2AttributesConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2AttributesConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig
/// Gets an AttributesConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_get_attributes_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_get_attributes_config_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_get_attributes_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_get_attributes_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_get_attributes_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2AttributesConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_get_attributes_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_get_attributes_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsGetAttributesConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig
/// Gets an AttributesConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_get_attributes_config_builder()` + `retail_projects_locations_catalogs_get_attributes_config_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_get_attributes_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_get_attributes_config(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsGetAttributesConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2AttributesConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_get_attributes_config_builder(client, &args.name)?;
    retail_projects_locations_catalogs_get_attributes_config_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/completionConfig
/// Gets a CompletionConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_get_completion_config_execute()` to send, or `retail_projects_locations_catalogs_get_completion_config` for simplest API.

pub fn retail_projects_locations_catalogs_get_completion_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/completionConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/completionConfig
/// Gets a CompletionConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_get_completion_config_execute()` or `retail_projects_locations_catalogs_get_completion_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_get_completion_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_get_completion_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2CompletionConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2CompletionConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/completionConfig
/// Gets a CompletionConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_get_completion_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_get_completion_config_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_get_completion_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_get_completion_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_get_completion_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2CompletionConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_get_completion_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_get_completion_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsGetCompletionConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/completionConfig
/// Gets a CompletionConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_get_completion_config_builder()` + `retail_projects_locations_catalogs_get_completion_config_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_get_completion_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_get_completion_config(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsGetCompletionConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2CompletionConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_get_completion_config_builder(client, &args.name)?;
    retail_projects_locations_catalogs_get_completion_config_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/conversationalSearchCustomizationConfig
/// Returns the conversational search customization config for a given catalog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_get_conversational_search_customization_config_execute()` to send, or `retail_projects_locations_catalogs_get_conversational_search_customization_config` for simplest API.

pub fn retail_projects_locations_catalogs_get_conversational_search_customization_config_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/conversationalSearchCustomizationConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/conversationalSearchCustomizationConfig
/// Returns the conversational search customization config for a given catalog.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_get_conversational_search_customization_config_execute()` or `retail_projects_locations_catalogs_get_conversational_search_customization_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_get_conversational_search_customization_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_get_conversational_search_customization_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2ConversationalSearchCustomizationConfig>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ConversationalSearchCustomizationConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/conversationalSearchCustomizationConfig
/// Returns the conversational search customization config for a given catalog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_get_conversational_search_customization_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_get_conversational_search_customization_config_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_get_conversational_search_customization_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_get_conversational_search_customization_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_get_conversational_search_customization_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRetailV2ConversationalSearchCustomizationConfig>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        retail_projects_locations_catalogs_get_conversational_search_customization_config_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_get_conversational_search_customization_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsGetConversationalSearchCustomizationConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/conversationalSearchCustomizationConfig
/// Returns the conversational search customization config for a given catalog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_get_conversational_search_customization_config_builder()` + `retail_projects_locations_catalogs_get_conversational_search_customization_config_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_get_conversational_search_customization_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_get_conversational_search_customization_config(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsGetConversationalSearchCustomizationConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRetailV2ConversationalSearchCustomizationConfig>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_get_conversational_search_customization_config_builder(
            client, &args.name,
        )?;
    retail_projects_locations_catalogs_get_conversational_search_customization_config_execute(
        builder,
    )
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}:getDefaultBranch
/// Get which branch is currently default branch set by CatalogService.SetDefaultBranch method under a specified parent catalog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_get_default_branch_execute()` to send, or `retail_projects_locations_catalogs_get_default_branch` for simplest API.

pub fn retail_projects_locations_catalogs_get_default_branch_builder<R>(
    client: &SimpleHttpClient<R>,
    catalog: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}:getDefaultBranch",
        catalog,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}:getDefaultBranch
/// Get which branch is currently default branch set by CatalogService.SetDefaultBranch method under a specified parent catalog.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_get_default_branch_execute()` or `retail_projects_locations_catalogs_get_default_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_get_default_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_get_default_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2GetDefaultBranchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2GetDefaultBranchResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}:getDefaultBranch
/// Get which branch is currently default branch set by CatalogService.SetDefaultBranch method under a specified parent catalog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_get_default_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_get_default_branch_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_get_default_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_get_default_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_get_default_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2GetDefaultBranchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_get_default_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_get_default_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsGetDefaultBranchArgs {
    /// Path parameter: catalog
    pub catalog: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}:getDefaultBranch
/// Get which branch is currently default branch set by CatalogService.SetDefaultBranch method under a specified parent catalog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_get_default_branch_builder()` + `retail_projects_locations_catalogs_get_default_branch_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_get_default_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_get_default_branch(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsGetDefaultBranchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2GetDefaultBranchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_get_default_branch_builder(client, &args.catalog)?;
    retail_projects_locations_catalogs_get_default_branch_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestionFeature
/// Manages overal generative question feature state -- enables toggling feature on and off.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_get_generative_question_feature_execute()` to send, or `retail_projects_locations_catalogs_get_generative_question_feature` for simplest API.

pub fn retail_projects_locations_catalogs_get_generative_question_feature_builder<R>(
    client: &SimpleHttpClient<R>,
    catalog: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestionFeature",
        catalog,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestionFeature
/// Manages overal generative question feature state -- enables toggling feature on and off.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_get_generative_question_feature_execute()` or `retail_projects_locations_catalogs_get_generative_question_feature`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_get_generative_question_feature_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_get_generative_question_feature_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2GenerativeQuestionsFeatureConfig>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2GenerativeQuestionsFeatureConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestionFeature
/// Manages overal generative question feature state -- enables toggling feature on and off.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_get_generative_question_feature_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_get_generative_question_feature_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_get_generative_question_feature()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_get_generative_question_feature_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_get_generative_question_feature_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2GenerativeQuestionsFeatureConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_get_generative_question_feature_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_get_generative_question_feature`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsGetGenerativeQuestionFeatureArgs {
    /// Path parameter: catalog
    pub catalog: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestionFeature
/// Manages overal generative question feature state -- enables toggling feature on and off.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_get_generative_question_feature_builder()` + `retail_projects_locations_catalogs_get_generative_question_feature_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_get_generative_question_feature_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_get_generative_question_feature(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsGetGenerativeQuestionFeatureArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2GenerativeQuestionsFeatureConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_get_generative_question_feature_builder(
        client,
        &args.catalog,
    )?;
    retail_projects_locations_catalogs_get_generative_question_feature_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs
/// Lists all the Catalogs associated with the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_list_execute()` to send, or `retail_projects_locations_catalogs_list` for simplest API.

pub fn retail_projects_locations_catalogs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs
/// Lists all the Catalogs associated with the project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_list_execute()` or `retail_projects_locations_catalogs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2ListCatalogsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ListCatalogsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs
/// Lists all the Catalogs associated with the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_list_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ListCatalogsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs
/// Lists all the Catalogs associated with the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_list_builder()` + `retail_projects_locations_catalogs_list_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_list(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ListCatalogsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    retail_projects_locations_catalogs_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}
/// Updates the Catalogs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_patch_execute()` to send, or `retail_projects_locations_catalogs_patch` for simplest API.

pub fn retail_projects_locations_catalogs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}
/// Updates the Catalogs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_patch_execute()` or `retail_projects_locations_catalogs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Catalog>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Catalog = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}
/// Updates the Catalogs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_patch_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2Catalog>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}
/// Updates the Catalogs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_patch_builder()` + `retail_projects_locations_catalogs_patch_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_patch(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2Catalog>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_patch_builder(client, &args.name, &args.updateMask)?;
    retail_projects_locations_catalogs_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}:setDefaultBranch
/// Set a specified branch id as default branch. API methods such as SearchService.Search, ProductService.GetProduct, ProductService.ListProducts will treat requests using "default_branch" to the actual branch id set as default. For example, if projects/*/locations/*/catalogs/*/`branches/1` is set as default, setting SearchRequest.branch to projects/*/locations/*/catalogs/*/`branches/default_branch` is equivalent to setting SearchRequest.branch to projects/*/locations/*/catalogs/*/`branches/1`. Using multiple branches can be useful when developers would like to have a staging branch to test and verify for future usage. When it becomes ready, developers switch on the staging branch using this API while keeping using projects/*/locations/*/catalogs/*/`branches/default_branch` as SearchRequest.branch to route the traffic to this staging branch. CAUTION: If you have live `predict/search` traffic, switching the default branch could potentially cause outages if the ID space of the new branch is very different from the old one. More specifically: * PredictionService will only return product IDs from branch {`newBranch`}. * SearchService will only return product IDs from branch {`newBranch`} (if branch is not explicitly set). * UserEventService will only join events with products from branch {`newBranch`}.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_set_default_branch_execute()` to send, or `retail_projects_locations_catalogs_set_default_branch` for simplest API.

pub fn retail_projects_locations_catalogs_set_default_branch_builder<R>(
    client: &SimpleHttpClient<R>,
    catalog: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}:setDefaultBranch",
        catalog,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}:setDefaultBranch
/// Set a specified branch id as default branch. API methods such as SearchService.Search, ProductService.GetProduct, ProductService.ListProducts will treat requests using "default_branch" to the actual branch id set as default. For example, if projects/*/locations/*/catalogs/*/`branches/1` is set as default, setting SearchRequest.branch to projects/*/locations/*/catalogs/*/`branches/default_branch` is equivalent to setting SearchRequest.branch to projects/*/locations/*/catalogs/*/`branches/1`. Using multiple branches can be useful when developers would like to have a staging branch to test and verify for future usage. When it becomes ready, developers switch on the staging branch using this API while keeping using projects/*/locations/*/catalogs/*/`branches/default_branch` as SearchRequest.branch to route the traffic to this staging branch. CAUTION: If you have live `predict/search` traffic, switching the default branch could potentially cause outages if the ID space of the new branch is very different from the old one. More specifically: * PredictionService will only return product IDs from branch {`newBranch`}. * SearchService will only return product IDs from branch {`newBranch`} (if branch is not explicitly set). * UserEventService will only join events with products from branch {`newBranch`}.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_set_default_branch_execute()` or `retail_projects_locations_catalogs_set_default_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_set_default_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_set_default_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}:setDefaultBranch
/// Set a specified branch id as default branch. API methods such as SearchService.Search, ProductService.GetProduct, ProductService.ListProducts will treat requests using "default_branch" to the actual branch id set as default. For example, if projects/*/locations/*/catalogs/*/`branches/1` is set as default, setting SearchRequest.branch to projects/*/locations/*/catalogs/*/`branches/default_branch` is equivalent to setting SearchRequest.branch to projects/*/locations/*/catalogs/*/`branches/1`. Using multiple branches can be useful when developers would like to have a staging branch to test and verify for future usage. When it becomes ready, developers switch on the staging branch using this API while keeping using projects/*/locations/*/catalogs/*/`branches/default_branch` as SearchRequest.branch to route the traffic to this staging branch. CAUTION: If you have live `predict/search` traffic, switching the default branch could potentially cause outages if the ID space of the new branch is very different from the old one. More specifically: * PredictionService will only return product IDs from branch {`newBranch`}. * SearchService will only return product IDs from branch {`newBranch`} (if branch is not explicitly set). * UserEventService will only join events with products from branch {`newBranch`}.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_set_default_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_set_default_branch_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_set_default_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_set_default_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_set_default_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_set_default_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_set_default_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsSetDefaultBranchArgs {
    /// Path parameter: catalog
    pub catalog: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}:setDefaultBranch
/// Set a specified branch id as default branch. API methods such as SearchService.Search, ProductService.GetProduct, ProductService.ListProducts will treat requests using "default_branch" to the actual branch id set as default. For example, if projects/*/locations/*/catalogs/*/`branches/1` is set as default, setting SearchRequest.branch to projects/*/locations/*/catalogs/*/`branches/default_branch` is equivalent to setting SearchRequest.branch to projects/*/locations/*/catalogs/*/`branches/1`. Using multiple branches can be useful when developers would like to have a staging branch to test and verify for future usage. When it becomes ready, developers switch on the staging branch using this API while keeping using projects/*/locations/*/catalogs/*/`branches/default_branch` as SearchRequest.branch to route the traffic to this staging branch. CAUTION: If you have live `predict/search` traffic, switching the default branch could potentially cause outages if the ID space of the new branch is very different from the old one. More specifically: * PredictionService will only return product IDs from branch {`newBranch`}. * SearchService will only return product IDs from branch {`newBranch`} (if branch is not explicitly set). * UserEventService will only join events with products from branch {`newBranch`}.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_set_default_branch_builder()` + `retail_projects_locations_catalogs_set_default_branch_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_set_default_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_set_default_branch(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsSetDefaultBranchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_set_default_branch_builder(client, &args.catalog)?;
    retail_projects_locations_catalogs_set_default_branch_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig
/// Updates the AttributesConfig. The catalog attributes in the request will be updated in the catalog, or inserted if they do not exist. Existing catalog attributes not included in the request will remain unchanged. Attributes that are assigned to products, but do not exist at the catalog level, are always included in the response. The product attribute is assigned default values for missing catalog attribute fields, e.g., searchable and dynamic facetable options.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_update_attributes_config_execute()` to send, or `retail_projects_locations_catalogs_update_attributes_config` for simplest API.

pub fn retail_projects_locations_catalogs_update_attributes_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig
/// Updates the AttributesConfig. The catalog attributes in the request will be updated in the catalog, or inserted if they do not exist. Existing catalog attributes not included in the request will remain unchanged. Attributes that are assigned to products, but do not exist at the catalog level, are always included in the response. The product attribute is assigned default values for missing catalog attribute fields, e.g., searchable and dynamic facetable options.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_update_attributes_config_execute()` or `retail_projects_locations_catalogs_update_attributes_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_update_attributes_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_update_attributes_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2AttributesConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2AttributesConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig
/// Updates the AttributesConfig. The catalog attributes in the request will be updated in the catalog, or inserted if they do not exist. Existing catalog attributes not included in the request will remain unchanged. Attributes that are assigned to products, but do not exist at the catalog level, are always included in the response. The product attribute is assigned default values for missing catalog attribute fields, e.g., searchable and dynamic facetable options.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_update_attributes_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_update_attributes_config_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_update_attributes_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_update_attributes_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_update_attributes_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2AttributesConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_update_attributes_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_update_attributes_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUpdateAttributesConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig
/// Updates the AttributesConfig. The catalog attributes in the request will be updated in the catalog, or inserted if they do not exist. Existing catalog attributes not included in the request will remain unchanged. Attributes that are assigned to products, but do not exist at the catalog level, are always included in the response. The product attribute is assigned default values for missing catalog attribute fields, e.g., searchable and dynamic facetable options.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_update_attributes_config_builder()` + `retail_projects_locations_catalogs_update_attributes_config_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_update_attributes_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_update_attributes_config(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsUpdateAttributesConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2AttributesConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_update_attributes_config_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    retail_projects_locations_catalogs_update_attributes_config_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/completionConfig
/// Updates the CompletionConfigs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_update_completion_config_execute()` to send, or `retail_projects_locations_catalogs_update_completion_config` for simplest API.

pub fn retail_projects_locations_catalogs_update_completion_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/completionConfig",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/completionConfig
/// Updates the CompletionConfigs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_update_completion_config_execute()` or `retail_projects_locations_catalogs_update_completion_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_update_completion_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_update_completion_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2CompletionConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2CompletionConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/completionConfig
/// Updates the CompletionConfigs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_update_completion_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_update_completion_config_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_update_completion_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_update_completion_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_update_completion_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2CompletionConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_update_completion_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_update_completion_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUpdateCompletionConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/completionConfig
/// Updates the CompletionConfigs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_update_completion_config_builder()` + `retail_projects_locations_catalogs_update_completion_config_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_update_completion_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_update_completion_config(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsUpdateCompletionConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2CompletionConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_update_completion_config_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    retail_projects_locations_catalogs_update_completion_config_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/conversationalSearchCustomizationConfig
/// Updates the conversational search customization config for a given catalog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_update_conversational_search_customization_config_execute()` to send, or `retail_projects_locations_catalogs_update_conversational_search_customization_config` for simplest API.

pub fn retail_projects_locations_catalogs_update_conversational_search_customization_config_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    catalog: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/conversationalSearchCustomizationConfig",
        catalog,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/conversationalSearchCustomizationConfig
/// Updates the conversational search customization config for a given catalog.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_update_conversational_search_customization_config_execute()` or `retail_projects_locations_catalogs_update_conversational_search_customization_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_update_conversational_search_customization_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_update_conversational_search_customization_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2ConversationalSearchCustomizationConfig>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ConversationalSearchCustomizationConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/conversationalSearchCustomizationConfig
/// Updates the conversational search customization config for a given catalog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_update_conversational_search_customization_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_update_conversational_search_customization_config_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_update_conversational_search_customization_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_update_conversational_search_customization_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_update_conversational_search_customization_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRetailV2ConversationalSearchCustomizationConfig>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        retail_projects_locations_catalogs_update_conversational_search_customization_config_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_update_conversational_search_customization_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUpdateConversationalSearchCustomizationConfigArgs {
    /// Path parameter: catalog
    pub catalog: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/conversationalSearchCustomizationConfig
/// Updates the conversational search customization config for a given catalog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_update_conversational_search_customization_config_builder()` + `retail_projects_locations_catalogs_update_conversational_search_customization_config_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_update_conversational_search_customization_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_update_conversational_search_customization_config(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsUpdateConversationalSearchCustomizationConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRetailV2ConversationalSearchCustomizationConfig>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_update_conversational_search_customization_config_builder(client, &args.catalog, &args.updateMask)?;
    retail_projects_locations_catalogs_update_conversational_search_customization_config_execute(
        builder,
    )
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestion
/// Allows management of individual questions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_update_generative_question_execute()` to send, or `retail_projects_locations_catalogs_update_generative_question` for simplest API.

pub fn retail_projects_locations_catalogs_update_generative_question_builder<R>(
    client: &SimpleHttpClient<R>,
    catalog: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestion",
        catalog,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestion
/// Allows management of individual questions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_update_generative_question_execute()` or `retail_projects_locations_catalogs_update_generative_question`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_update_generative_question_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_update_generative_question_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2GenerativeQuestionConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2GenerativeQuestionConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestion
/// Allows management of individual questions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_update_generative_question_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_update_generative_question_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_update_generative_question()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_update_generative_question_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_update_generative_question_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2GenerativeQuestionConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_update_generative_question_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_update_generative_question`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUpdateGenerativeQuestionArgs {
    /// Path parameter: catalog
    pub catalog: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestion
/// Allows management of individual questions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_update_generative_question_builder()` + `retail_projects_locations_catalogs_update_generative_question_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_update_generative_question_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_update_generative_question(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsUpdateGenerativeQuestionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2GenerativeQuestionConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_update_generative_question_builder(
        client,
        &args.catalog,
        &args.updateMask,
    )?;
    retail_projects_locations_catalogs_update_generative_question_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestionFeature
/// Manages overal generative question feature state -- enables toggling feature on and off.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_update_generative_question_feature_execute()` to send, or `retail_projects_locations_catalogs_update_generative_question_feature` for simplest API.

pub fn retail_projects_locations_catalogs_update_generative_question_feature_builder<R>(
    client: &SimpleHttpClient<R>,
    catalog: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestionFeature",
        catalog,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestionFeature
/// Manages overal generative question feature state -- enables toggling feature on and off.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_update_generative_question_feature_execute()` or `retail_projects_locations_catalogs_update_generative_question_feature`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_update_generative_question_feature_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_update_generative_question_feature_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2GenerativeQuestionsFeatureConfig>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2GenerativeQuestionsFeatureConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestionFeature
/// Manages overal generative question feature state -- enables toggling feature on and off.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_update_generative_question_feature_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_update_generative_question_feature_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_update_generative_question_feature()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_update_generative_question_feature_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_update_generative_question_feature_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2GenerativeQuestionsFeatureConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_update_generative_question_feature_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_update_generative_question_feature`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUpdateGenerativeQuestionFeatureArgs {
    /// Path parameter: catalog
    pub catalog: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestionFeature
/// Manages overal generative question feature state -- enables toggling feature on and off.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_update_generative_question_feature_builder()` + `retail_projects_locations_catalogs_update_generative_question_feature_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_update_generative_question_feature_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_update_generative_question_feature(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsUpdateGenerativeQuestionFeatureArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2GenerativeQuestionsFeatureConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_update_generative_question_feature_builder(
        client,
        &args.catalog,
        &args.updateMask,
    )?;
    retail_projects_locations_catalogs_update_generative_question_feature_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig:addCatalogAttribute
/// Adds the specified CatalogAttribute to the AttributesConfig. If the CatalogAttribute to add already exists, an ALREADY_EXISTS error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_attributes_config_add_catalog_attribute_execute()` to send, or `retail_projects_locations_catalogs_attributes_config_add_catalog_attribute` for simplest API.

pub fn retail_projects_locations_catalogs_attributes_config_add_catalog_attribute_builder<R>(
    client: &SimpleHttpClient<R>,
    attributesConfig: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig:addCatalogAttribute",
        attributesConfig,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig:addCatalogAttribute
/// Adds the specified CatalogAttribute to the AttributesConfig. If the CatalogAttribute to add already exists, an ALREADY_EXISTS error is returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_attributes_config_add_catalog_attribute_execute()` or `retail_projects_locations_catalogs_attributes_config_add_catalog_attribute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_attributes_config_add_catalog_attribute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_attributes_config_add_catalog_attribute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2AttributesConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2AttributesConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig:addCatalogAttribute
/// Adds the specified CatalogAttribute to the AttributesConfig. If the CatalogAttribute to add already exists, an ALREADY_EXISTS error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_attributes_config_add_catalog_attribute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_attributes_config_add_catalog_attribute_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_attributes_config_add_catalog_attribute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_attributes_config_add_catalog_attribute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_attributes_config_add_catalog_attribute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2AttributesConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        retail_projects_locations_catalogs_attributes_config_add_catalog_attribute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_attributes_config_add_catalog_attribute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsAttributesConfigAddCatalogAttributeArgs {
    /// Path parameter: attributesConfig
    pub attributesConfig: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig:addCatalogAttribute
/// Adds the specified CatalogAttribute to the AttributesConfig. If the CatalogAttribute to add already exists, an ALREADY_EXISTS error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_attributes_config_add_catalog_attribute_builder()` + `retail_projects_locations_catalogs_attributes_config_add_catalog_attribute_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_attributes_config_add_catalog_attribute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_attributes_config_add_catalog_attribute(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsAttributesConfigAddCatalogAttributeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2AttributesConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_attributes_config_add_catalog_attribute_builder(
            client,
            &args.attributesConfig,
        )?;
    retail_projects_locations_catalogs_attributes_config_add_catalog_attribute_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig:removeCatalogAttribute
/// Removes the specified CatalogAttribute from the AttributesConfig. If the CatalogAttribute to remove does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute_execute()` to send, or `retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute` for simplest API.

pub fn retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute_builder<R>(
    client: &SimpleHttpClient<R>,
    attributesConfig: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig:removeCatalogAttribute",
        attributesConfig,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig:removeCatalogAttribute
/// Removes the specified CatalogAttribute from the AttributesConfig. If the CatalogAttribute to remove does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute_execute()` or `retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2AttributesConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2AttributesConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig:removeCatalogAttribute
/// Removes the specified CatalogAttribute from the AttributesConfig. If the CatalogAttribute to remove does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2AttributesConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsAttributesConfigRemoveCatalogAttributeArgs {
    /// Path parameter: attributesConfig
    pub attributesConfig: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig:removeCatalogAttribute
/// Removes the specified CatalogAttribute from the AttributesConfig. If the CatalogAttribute to remove does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute_builder()` + `retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsAttributesConfigRemoveCatalogAttributeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2AttributesConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute_builder(
            client,
            &args.attributesConfig,
        )?;
    retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig:replaceCatalogAttribute
/// Replaces the specified CatalogAttribute in the AttributesConfig by updating the catalog attribute with the same CatalogAttribute.key. If the CatalogAttribute to replace does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute_execute()` to send, or `retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute` for simplest API.

pub fn retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute_builder<R>(
    client: &SimpleHttpClient<R>,
    attributesConfig: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig:replaceCatalogAttribute",
        attributesConfig,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig:replaceCatalogAttribute
/// Replaces the specified CatalogAttribute in the AttributesConfig by updating the catalog attribute with the same CatalogAttribute.key. If the CatalogAttribute to replace does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute_execute()` or `retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2AttributesConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2AttributesConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig:replaceCatalogAttribute
/// Replaces the specified CatalogAttribute in the AttributesConfig by updating the catalog attribute with the same CatalogAttribute.key. If the CatalogAttribute to replace does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2AttributesConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsAttributesConfigReplaceCatalogAttributeArgs {
    /// Path parameter: attributesConfig
    pub attributesConfig: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/attributesConfig:replaceCatalogAttribute
/// Replaces the specified CatalogAttribute in the AttributesConfig by updating the catalog attribute with the same CatalogAttribute.key. If the CatalogAttribute to replace does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute_builder()` + `retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsAttributesConfigReplaceCatalogAttributeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2AttributesConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute_builder(
            client,
            &args.attributesConfig,
        )?;
    retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_branches_operations_get_execute()` to send, or `retail_projects_locations_catalogs_branches_operations_get` for simplest API.

pub fn retail_projects_locations_catalogs_branches_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_branches_operations_get_execute()` or `retail_projects_locations_catalogs_branches_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_branches_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_branches_operations_get_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_branches_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_branches_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_branches_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_branches_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_branches_operations_get_builder()` + `retail_projects_locations_catalogs_branches_operations_get_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_branches_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_operations_get(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsBranchesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_branches_operations_get_builder(client, &args.name)?;
    retail_projects_locations_catalogs_branches_operations_get_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:addFulfillmentPlaces
/// We recommend that you use the ProductService.AddLocalInventories method instead of the ProductService.AddFulfillmentPlaces method. ProductService.AddLocalInventories achieves the same results but provides more fine-grained control over ingesting local inventory data. Incrementally adds place IDs to Product.fulfillment_info.place_ids. This process is asynchronous and does not require the Product to exist before updating fulfillment information. If the request is valid, the update will be enqueued and processed downstream. As a consequence, when a response is returned, the added place IDs are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. The returned Operations will be obsolete after 1 day, and GetOperation API will return NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates will not be marked as done until being obsolete.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_branches_products_add_fulfillment_places_execute()` to send, or `retail_projects_locations_catalogs_branches_products_add_fulfillment_places` for simplest API.

pub fn retail_projects_locations_catalogs_branches_products_add_fulfillment_places_builder<R>(
    client: &SimpleHttpClient<R>,
    product: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:addFulfillmentPlaces",
        product,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:addFulfillmentPlaces
/// We recommend that you use the ProductService.AddLocalInventories method instead of the ProductService.AddFulfillmentPlaces method. ProductService.AddLocalInventories achieves the same results but provides more fine-grained control over ingesting local inventory data. Incrementally adds place IDs to Product.fulfillment_info.place_ids. This process is asynchronous and does not require the Product to exist before updating fulfillment information. If the request is valid, the update will be enqueued and processed downstream. As a consequence, when a response is returned, the added place IDs are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. The returned Operations will be obsolete after 1 day, and GetOperation API will return NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates will not be marked as done until being obsolete.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_branches_products_add_fulfillment_places_execute()` or `retail_projects_locations_catalogs_branches_products_add_fulfillment_places`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_add_fulfillment_places_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_add_fulfillment_places_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:addFulfillmentPlaces
/// We recommend that you use the ProductService.AddLocalInventories method instead of the ProductService.AddFulfillmentPlaces method. ProductService.AddLocalInventories achieves the same results but provides more fine-grained control over ingesting local inventory data. Incrementally adds place IDs to Product.fulfillment_info.place_ids. This process is asynchronous and does not require the Product to exist before updating fulfillment information. If the request is valid, the update will be enqueued and processed downstream. As a consequence, when a response is returned, the added place IDs are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. The returned Operations will be obsolete after 1 day, and GetOperation API will return NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates will not be marked as done until being obsolete.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_branches_products_add_fulfillment_places_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_add_fulfillment_places_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_branches_products_add_fulfillment_places()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_add_fulfillment_places_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_branches_products_add_fulfillment_places_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        retail_projects_locations_catalogs_branches_products_add_fulfillment_places_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_branches_products_add_fulfillment_places`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsAddFulfillmentPlacesArgs {
    /// Path parameter: product
    pub product: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:addFulfillmentPlaces
/// We recommend that you use the ProductService.AddLocalInventories method instead of the ProductService.AddFulfillmentPlaces method. ProductService.AddLocalInventories achieves the same results but provides more fine-grained control over ingesting local inventory data. Incrementally adds place IDs to Product.fulfillment_info.place_ids. This process is asynchronous and does not require the Product to exist before updating fulfillment information. If the request is valid, the update will be enqueued and processed downstream. As a consequence, when a response is returned, the added place IDs are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. The returned Operations will be obsolete after 1 day, and GetOperation API will return NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates will not be marked as done until being obsolete.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_branches_products_add_fulfillment_places_builder()` + `retail_projects_locations_catalogs_branches_products_add_fulfillment_places_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_add_fulfillment_places_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_add_fulfillment_places(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsBranchesProductsAddFulfillmentPlacesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_branches_products_add_fulfillment_places_builder(
            client,
            &args.product,
        )?;
    retail_projects_locations_catalogs_branches_products_add_fulfillment_places_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:addLocalInventories
/// Updates local inventory information for a Product at a list of places, while respecting the last update timestamps of each inventory field. This process is asynchronous and does not require the Product to exist before updating inventory information. If the request is valid, the update will be enqueued and processed downstream. As a consequence, when a response is returned, updates are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. Local inventory information can only be modified using this method. ProductService.CreateProduct and ProductService.UpdateProduct has no effect on local inventories. The returned Operations will be obsolete after 1 day, and GetOperation API will return NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates will not be marked as done until being obsolete.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_branches_products_add_local_inventories_execute()` to send, or `retail_projects_locations_catalogs_branches_products_add_local_inventories` for simplest API.

pub fn retail_projects_locations_catalogs_branches_products_add_local_inventories_builder<R>(
    client: &SimpleHttpClient<R>,
    product: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:addLocalInventories",
        product,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:addLocalInventories
/// Updates local inventory information for a Product at a list of places, while respecting the last update timestamps of each inventory field. This process is asynchronous and does not require the Product to exist before updating inventory information. If the request is valid, the update will be enqueued and processed downstream. As a consequence, when a response is returned, updates are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. Local inventory information can only be modified using this method. ProductService.CreateProduct and ProductService.UpdateProduct has no effect on local inventories. The returned Operations will be obsolete after 1 day, and GetOperation API will return NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates will not be marked as done until being obsolete.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_branches_products_add_local_inventories_execute()` or `retail_projects_locations_catalogs_branches_products_add_local_inventories`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_add_local_inventories_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_add_local_inventories_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:addLocalInventories
/// Updates local inventory information for a Product at a list of places, while respecting the last update timestamps of each inventory field. This process is asynchronous and does not require the Product to exist before updating inventory information. If the request is valid, the update will be enqueued and processed downstream. As a consequence, when a response is returned, updates are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. Local inventory information can only be modified using this method. ProductService.CreateProduct and ProductService.UpdateProduct has no effect on local inventories. The returned Operations will be obsolete after 1 day, and GetOperation API will return NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates will not be marked as done until being obsolete.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_branches_products_add_local_inventories_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_add_local_inventories_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_branches_products_add_local_inventories()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_add_local_inventories_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_branches_products_add_local_inventories_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        retail_projects_locations_catalogs_branches_products_add_local_inventories_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_branches_products_add_local_inventories`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsAddLocalInventoriesArgs {
    /// Path parameter: product
    pub product: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:addLocalInventories
/// Updates local inventory information for a Product at a list of places, while respecting the last update timestamps of each inventory field. This process is asynchronous and does not require the Product to exist before updating inventory information. If the request is valid, the update will be enqueued and processed downstream. As a consequence, when a response is returned, updates are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. Local inventory information can only be modified using this method. ProductService.CreateProduct and ProductService.UpdateProduct has no effect on local inventories. The returned Operations will be obsolete after 1 day, and GetOperation API will return NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates will not be marked as done until being obsolete.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_branches_products_add_local_inventories_builder()` + `retail_projects_locations_catalogs_branches_products_add_local_inventories_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_add_local_inventories_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_add_local_inventories(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsBranchesProductsAddLocalInventoriesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_branches_products_add_local_inventories_builder(
            client,
            &args.product,
        )?;
    retail_projects_locations_catalogs_branches_products_add_local_inventories_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products
/// Creates a Product.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_branches_products_create_execute()` to send, or `retail_projects_locations_catalogs_branches_products_create` for simplest API.

pub fn retail_projects_locations_catalogs_branches_products_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    productId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = productId.as_ref() {
        query_parts.push(format!("productId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products
/// Creates a Product.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_branches_products_create_execute()` or `retail_projects_locations_catalogs_branches_products_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Product>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Product = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products
/// Creates a Product.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_branches_products_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_create_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_branches_products_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_branches_products_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2Product>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_branches_products_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_branches_products_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: productId
    pub productId: Option<String>,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products
/// Creates a Product.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_branches_products_create_builder()` + `retail_projects_locations_catalogs_branches_products_create_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_create(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsBranchesProductsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2Product>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_branches_products_create_builder(
        client,
        &args.parent,
        &args.productId,
    )?;
    retail_projects_locations_catalogs_branches_products_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}
/// Deletes a Product.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_branches_products_delete_execute()` to send, or `retail_projects_locations_catalogs_branches_products_delete` for simplest API.

pub fn retail_projects_locations_catalogs_branches_products_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}
/// Deletes a Product.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_branches_products_delete_execute()` or `retail_projects_locations_catalogs_branches_products_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}
/// Deletes a Product.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_branches_products_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_delete_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_branches_products_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_branches_products_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_branches_products_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_branches_products_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}
/// Deletes a Product.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_branches_products_delete_builder()` + `retail_projects_locations_catalogs_branches_products_delete_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_delete(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsBranchesProductsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_branches_products_delete_builder(client, &args.name)?;
    retail_projects_locations_catalogs_branches_products_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}
/// Gets a Product.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_branches_products_get_execute()` to send, or `retail_projects_locations_catalogs_branches_products_get` for simplest API.

pub fn retail_projects_locations_catalogs_branches_products_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}
/// Gets a Product.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_branches_products_get_execute()` or `retail_projects_locations_catalogs_branches_products_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Product>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Product = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}
/// Gets a Product.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_branches_products_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_get_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_branches_products_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_branches_products_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2Product>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_branches_products_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_branches_products_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}
/// Gets a Product.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_branches_products_get_builder()` + `retail_projects_locations_catalogs_branches_products_get_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_get(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsBranchesProductsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2Product>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_branches_products_get_builder(client, &args.name)?;
    retail_projects_locations_catalogs_branches_products_get_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products:import
/// Bulk import of multiple Products. Request processing may be synchronous. Non-existing items are created. Note that it is possible for a subset of the Products to be successfully updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_branches_products_import_execute()` to send, or `retail_projects_locations_catalogs_branches_products_import` for simplest API.

pub fn retail_projects_locations_catalogs_branches_products_import_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products:import",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products:import
/// Bulk import of multiple Products. Request processing may be synchronous. Non-existing items are created. Note that it is possible for a subset of the Products to be successfully updated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_branches_products_import_execute()` or `retail_projects_locations_catalogs_branches_products_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products:import
/// Bulk import of multiple Products. Request processing may be synchronous. Non-existing items are created. Note that it is possible for a subset of the Products to be successfully updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_branches_products_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_import_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_branches_products_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_branches_products_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_branches_products_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_branches_products_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsImportArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products:import
/// Bulk import of multiple Products. Request processing may be synchronous. Non-existing items are created. Note that it is possible for a subset of the Products to be successfully updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_branches_products_import_builder()` + `retail_projects_locations_catalogs_branches_products_import_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_import(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsBranchesProductsImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_branches_products_import_builder(client, &args.parent)?;
    retail_projects_locations_catalogs_branches_products_import_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products
/// Gets a list of Products.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_branches_products_list_execute()` to send, or `retail_projects_locations_catalogs_branches_products_list` for simplest API.

pub fn retail_projects_locations_catalogs_branches_products_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    readMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products
/// Gets a list of Products.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_branches_products_list_execute()` or `retail_projects_locations_catalogs_branches_products_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2ListProductsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ListProductsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products
/// Gets a list of Products.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_branches_products_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_list_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_branches_products_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_branches_products_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ListProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_branches_products_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_branches_products_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products
/// Gets a list of Products.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_branches_products_list_builder()` + `retail_projects_locations_catalogs_branches_products_list_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_list(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsBranchesProductsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ListProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_branches_products_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.readMask,
    )?;
    retail_projects_locations_catalogs_branches_products_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}
/// Updates a Product.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_branches_products_patch_execute()` to send, or `retail_projects_locations_catalogs_branches_products_patch` for simplest API.

pub fn retail_projects_locations_catalogs_branches_products_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}
/// Updates a Product.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_branches_products_patch_execute()` or `retail_projects_locations_catalogs_branches_products_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Product>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Product = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}
/// Updates a Product.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_branches_products_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_patch_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_branches_products_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_branches_products_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2Product>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_branches_products_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_branches_products_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}
/// Updates a Product.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_branches_products_patch_builder()` + `retail_projects_locations_catalogs_branches_products_patch_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_patch(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsBranchesProductsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2Product>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_branches_products_patch_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.updateMask,
    )?;
    retail_projects_locations_catalogs_branches_products_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products:purge
/// Permanently deletes all selected Products under a branch. This process is asynchronous. If the request is valid, the removal will be enqueued and processed offline. Depending on the number of Products, this operation could take hours to complete. Before the operation completes, some Products may still be returned by ProductService.GetProduct or ProductService.ListProducts. Depending on the number of Products, this operation could take hours to complete. To get a sample of Products that would be deleted, set PurgeProductsRequest.force to `false`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_branches_products_purge_execute()` to send, or `retail_projects_locations_catalogs_branches_products_purge` for simplest API.

pub fn retail_projects_locations_catalogs_branches_products_purge_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products:purge",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products:purge
/// Permanently deletes all selected Products under a branch. This process is asynchronous. If the request is valid, the removal will be enqueued and processed offline. Depending on the number of Products, this operation could take hours to complete. Before the operation completes, some Products may still be returned by ProductService.GetProduct or ProductService.ListProducts. Depending on the number of Products, this operation could take hours to complete. To get a sample of Products that would be deleted, set PurgeProductsRequest.force to `false`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_branches_products_purge_execute()` or `retail_projects_locations_catalogs_branches_products_purge`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_purge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_purge_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products:purge
/// Permanently deletes all selected Products under a branch. This process is asynchronous. If the request is valid, the removal will be enqueued and processed offline. Depending on the number of Products, this operation could take hours to complete. Before the operation completes, some Products may still be returned by ProductService.GetProduct or ProductService.ListProducts. Depending on the number of Products, this operation could take hours to complete. To get a sample of Products that would be deleted, set PurgeProductsRequest.force to `false`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_branches_products_purge_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_purge_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_branches_products_purge()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_purge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_branches_products_purge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_branches_products_purge_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_branches_products_purge`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsPurgeArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products:purge
/// Permanently deletes all selected Products under a branch. This process is asynchronous. If the request is valid, the removal will be enqueued and processed offline. Depending on the number of Products, this operation could take hours to complete. Before the operation completes, some Products may still be returned by ProductService.GetProduct or ProductService.ListProducts. Depending on the number of Products, this operation could take hours to complete. To get a sample of Products that would be deleted, set PurgeProductsRequest.force to `false`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_branches_products_purge_builder()` + `retail_projects_locations_catalogs_branches_products_purge_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_purge_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_purge(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsBranchesProductsPurgeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_branches_products_purge_builder(client, &args.parent)?;
    retail_projects_locations_catalogs_branches_products_purge_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:removeFulfillmentPlaces
/// We recommend that you use the ProductService.RemoveLocalInventories method instead of the ProductService.RemoveFulfillmentPlaces method. ProductService.RemoveLocalInventories achieves the same results but provides more fine-grained control over ingesting local inventory data. Incrementally removes place IDs from a Product.fulfillment_info.place_ids. This process is asynchronous and does not require the Product to exist before updating fulfillment information. If the request is valid, the update will be enqueued and processed downstream. As a consequence, when a response is returned, the removed place IDs are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. The returned Operations will be obsolete after 1 day, and GetOperation API will return NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates will not be marked as done until being obsolete.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_branches_products_remove_fulfillment_places_execute()` to send, or `retail_projects_locations_catalogs_branches_products_remove_fulfillment_places` for simplest API.

pub fn retail_projects_locations_catalogs_branches_products_remove_fulfillment_places_builder<R>(
    client: &SimpleHttpClient<R>,
    product: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:removeFulfillmentPlaces",
        product,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:removeFulfillmentPlaces
/// We recommend that you use the ProductService.RemoveLocalInventories method instead of the ProductService.RemoveFulfillmentPlaces method. ProductService.RemoveLocalInventories achieves the same results but provides more fine-grained control over ingesting local inventory data. Incrementally removes place IDs from a Product.fulfillment_info.place_ids. This process is asynchronous and does not require the Product to exist before updating fulfillment information. If the request is valid, the update will be enqueued and processed downstream. As a consequence, when a response is returned, the removed place IDs are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. The returned Operations will be obsolete after 1 day, and GetOperation API will return NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates will not be marked as done until being obsolete.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_branches_products_remove_fulfillment_places_execute()` or `retail_projects_locations_catalogs_branches_products_remove_fulfillment_places`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_remove_fulfillment_places_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_remove_fulfillment_places_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:removeFulfillmentPlaces
/// We recommend that you use the ProductService.RemoveLocalInventories method instead of the ProductService.RemoveFulfillmentPlaces method. ProductService.RemoveLocalInventories achieves the same results but provides more fine-grained control over ingesting local inventory data. Incrementally removes place IDs from a Product.fulfillment_info.place_ids. This process is asynchronous and does not require the Product to exist before updating fulfillment information. If the request is valid, the update will be enqueued and processed downstream. As a consequence, when a response is returned, the removed place IDs are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. The returned Operations will be obsolete after 1 day, and GetOperation API will return NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates will not be marked as done until being obsolete.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_branches_products_remove_fulfillment_places_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_remove_fulfillment_places_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_branches_products_remove_fulfillment_places()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_remove_fulfillment_places_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_branches_products_remove_fulfillment_places_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_branches_products_remove_fulfillment_places_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_branches_products_remove_fulfillment_places`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsRemoveFulfillmentPlacesArgs {
    /// Path parameter: product
    pub product: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:removeFulfillmentPlaces
/// We recommend that you use the ProductService.RemoveLocalInventories method instead of the ProductService.RemoveFulfillmentPlaces method. ProductService.RemoveLocalInventories achieves the same results but provides more fine-grained control over ingesting local inventory data. Incrementally removes place IDs from a Product.fulfillment_info.place_ids. This process is asynchronous and does not require the Product to exist before updating fulfillment information. If the request is valid, the update will be enqueued and processed downstream. As a consequence, when a response is returned, the removed place IDs are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. The returned Operations will be obsolete after 1 day, and GetOperation API will return NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates will not be marked as done until being obsolete.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_branches_products_remove_fulfillment_places_builder()` + `retail_projects_locations_catalogs_branches_products_remove_fulfillment_places_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_remove_fulfillment_places_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_remove_fulfillment_places(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsBranchesProductsRemoveFulfillmentPlacesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_branches_products_remove_fulfillment_places_builder(
            client,
            &args.product,
        )?;
    retail_projects_locations_catalogs_branches_products_remove_fulfillment_places_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:removeLocalInventories
/// Remove local inventory information for a Product at a list of places at a removal timestamp. This process is asynchronous. If the request is valid, the removal will be enqueued and processed downstream. As a consequence, when a response is returned, removals are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. Local inventory information can only be removed using this method. ProductService.CreateProduct and ProductService.UpdateProduct has no effect on local inventories. The returned Operations will be obsolete after 1 day, and GetOperation API will return NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates will not be marked as done until being obsolete.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_branches_products_remove_local_inventories_execute()` to send, or `retail_projects_locations_catalogs_branches_products_remove_local_inventories` for simplest API.

pub fn retail_projects_locations_catalogs_branches_products_remove_local_inventories_builder<R>(
    client: &SimpleHttpClient<R>,
    product: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:removeLocalInventories",
        product,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:removeLocalInventories
/// Remove local inventory information for a Product at a list of places at a removal timestamp. This process is asynchronous. If the request is valid, the removal will be enqueued and processed downstream. As a consequence, when a response is returned, removals are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. Local inventory information can only be removed using this method. ProductService.CreateProduct and ProductService.UpdateProduct has no effect on local inventories. The returned Operations will be obsolete after 1 day, and GetOperation API will return NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates will not be marked as done until being obsolete.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_branches_products_remove_local_inventories_execute()` or `retail_projects_locations_catalogs_branches_products_remove_local_inventories`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_remove_local_inventories_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_remove_local_inventories_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:removeLocalInventories
/// Remove local inventory information for a Product at a list of places at a removal timestamp. This process is asynchronous. If the request is valid, the removal will be enqueued and processed downstream. As a consequence, when a response is returned, removals are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. Local inventory information can only be removed using this method. ProductService.CreateProduct and ProductService.UpdateProduct has no effect on local inventories. The returned Operations will be obsolete after 1 day, and GetOperation API will return NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates will not be marked as done until being obsolete.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_branches_products_remove_local_inventories_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_remove_local_inventories_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_branches_products_remove_local_inventories()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_remove_local_inventories_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_branches_products_remove_local_inventories_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_branches_products_remove_local_inventories_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_branches_products_remove_local_inventories`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsRemoveLocalInventoriesArgs {
    /// Path parameter: product
    pub product: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:removeLocalInventories
/// Remove local inventory information for a Product at a list of places at a removal timestamp. This process is asynchronous. If the request is valid, the removal will be enqueued and processed downstream. As a consequence, when a response is returned, removals are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. Local inventory information can only be removed using this method. ProductService.CreateProduct and ProductService.UpdateProduct has no effect on local inventories. The returned Operations will be obsolete after 1 day, and GetOperation API will return NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates will not be marked as done until being obsolete.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_branches_products_remove_local_inventories_builder()` + `retail_projects_locations_catalogs_branches_products_remove_local_inventories_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_remove_local_inventories_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_remove_local_inventories(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsBranchesProductsRemoveLocalInventoriesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_branches_products_remove_local_inventories_builder(
            client,
            &args.product,
        )?;
    retail_projects_locations_catalogs_branches_products_remove_local_inventories_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:setInventory
/// Updates inventory information for a Product while respecting the last update timestamps of each inventory field. This process is asynchronous and does not require the Product to exist before updating fulfillment information. If the request is valid, the update is enqueued and processed downstream. As a consequence, when a response is returned, updates are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. When inventory is updated with ProductService.CreateProduct and ProductService.UpdateProduct, the specified inventory field value(s) overwrite any existing value(s) while ignoring the last update time for this field. Furthermore, the last update times for the specified inventory fields are overwritten by the times of the ProductService.CreateProduct or ProductService.UpdateProduct request. If no inventory fields are set in CreateProductRequest.product, then any pre-existing inventory information for this product is used. If no inventory fields are set in SetInventoryRequest.set_mask, then any existing inventory information is preserved. Pre-existing inventory information can only be updated with ProductService.SetInventory, ProductService.AddFulfillmentPlaces, and ProductService.RemoveFulfillmentPlaces. The returned Operations is obsolete after one day, and the GetOperation API returns NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates are not marked as done until they are obsolete.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_branches_products_set_inventory_execute()` to send, or `retail_projects_locations_catalogs_branches_products_set_inventory` for simplest API.

pub fn retail_projects_locations_catalogs_branches_products_set_inventory_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:setInventory",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:setInventory
/// Updates inventory information for a Product while respecting the last update timestamps of each inventory field. This process is asynchronous and does not require the Product to exist before updating fulfillment information. If the request is valid, the update is enqueued and processed downstream. As a consequence, when a response is returned, updates are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. When inventory is updated with ProductService.CreateProduct and ProductService.UpdateProduct, the specified inventory field value(s) overwrite any existing value(s) while ignoring the last update time for this field. Furthermore, the last update times for the specified inventory fields are overwritten by the times of the ProductService.CreateProduct or ProductService.UpdateProduct request. If no inventory fields are set in CreateProductRequest.product, then any pre-existing inventory information for this product is used. If no inventory fields are set in SetInventoryRequest.set_mask, then any existing inventory information is preserved. Pre-existing inventory information can only be updated with ProductService.SetInventory, ProductService.AddFulfillmentPlaces, and ProductService.RemoveFulfillmentPlaces. The returned Operations is obsolete after one day, and the GetOperation API returns NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates are not marked as done until they are obsolete.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_branches_products_set_inventory_execute()` or `retail_projects_locations_catalogs_branches_products_set_inventory`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_set_inventory_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_set_inventory_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:setInventory
/// Updates inventory information for a Product while respecting the last update timestamps of each inventory field. This process is asynchronous and does not require the Product to exist before updating fulfillment information. If the request is valid, the update is enqueued and processed downstream. As a consequence, when a response is returned, updates are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. When inventory is updated with ProductService.CreateProduct and ProductService.UpdateProduct, the specified inventory field value(s) overwrite any existing value(s) while ignoring the last update time for this field. Furthermore, the last update times for the specified inventory fields are overwritten by the times of the ProductService.CreateProduct or ProductService.UpdateProduct request. If no inventory fields are set in CreateProductRequest.product, then any pre-existing inventory information for this product is used. If no inventory fields are set in SetInventoryRequest.set_mask, then any existing inventory information is preserved. Pre-existing inventory information can only be updated with ProductService.SetInventory, ProductService.AddFulfillmentPlaces, and ProductService.RemoveFulfillmentPlaces. The returned Operations is obsolete after one day, and the GetOperation API returns NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates are not marked as done until they are obsolete.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_branches_products_set_inventory_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_set_inventory_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_branches_products_set_inventory()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_branches_products_set_inventory_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_branches_products_set_inventory_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_branches_products_set_inventory_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_branches_products_set_inventory`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsSetInventoryArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/branches/{branchesId}/products/{productsId}:setInventory
/// Updates inventory information for a Product while respecting the last update timestamps of each inventory field. This process is asynchronous and does not require the Product to exist before updating fulfillment information. If the request is valid, the update is enqueued and processed downstream. As a consequence, when a response is returned, updates are not immediately manifested in the Product queried by ProductService.GetProduct or ProductService.ListProducts. When inventory is updated with ProductService.CreateProduct and ProductService.UpdateProduct, the specified inventory field value(s) overwrite any existing value(s) while ignoring the last update time for this field. Furthermore, the last update times for the specified inventory fields are overwritten by the times of the ProductService.CreateProduct or ProductService.UpdateProduct request. If no inventory fields are set in CreateProductRequest.product, then any pre-existing inventory information for this product is used. If no inventory fields are set in SetInventoryRequest.set_mask, then any existing inventory information is preserved. Pre-existing inventory information can only be updated with ProductService.SetInventory, ProductService.AddFulfillmentPlaces, and ProductService.RemoveFulfillmentPlaces. The returned Operations is obsolete after one day, and the GetOperation API returns NOT_FOUND afterwards. If conflicting updates are issued, the Operations associated with the stale updates are not marked as done until they are obsolete.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_branches_products_set_inventory_builder()` + `retail_projects_locations_catalogs_branches_products_set_inventory_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_branches_products_set_inventory_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_branches_products_set_inventory(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsBranchesProductsSetInventoryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_branches_products_set_inventory_builder(
        client, &args.name,
    )?;
    retail_projects_locations_catalogs_branches_products_set_inventory_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/completionData:import
/// Bulk import of processed completion dataset. Request processing is asynchronous. Partial updating is not supported. The operation is successfully finished only after the imported suggestions are indexed successfully and ready for serving. The process takes hours. This feature is only available for users who have Retail Search enabled. Enable Retail Search on Cloud Console before using this feature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_completion_data_import_execute()` to send, or `retail_projects_locations_catalogs_completion_data_import` for simplest API.

pub fn retail_projects_locations_catalogs_completion_data_import_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/completionData:import",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/completionData:import
/// Bulk import of processed completion dataset. Request processing is asynchronous. Partial updating is not supported. The operation is successfully finished only after the imported suggestions are indexed successfully and ready for serving. The process takes hours. This feature is only available for users who have Retail Search enabled. Enable Retail Search on Cloud Console before using this feature.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_completion_data_import_execute()` or `retail_projects_locations_catalogs_completion_data_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_completion_data_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_completion_data_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/completionData:import
/// Bulk import of processed completion dataset. Request processing is asynchronous. Partial updating is not supported. The operation is successfully finished only after the imported suggestions are indexed successfully and ready for serving. The process takes hours. This feature is only available for users who have Retail Search enabled. Enable Retail Search on Cloud Console before using this feature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_completion_data_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_completion_data_import_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_completion_data_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_completion_data_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_completion_data_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_completion_data_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_completion_data_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsCompletionDataImportArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/completionData:import
/// Bulk import of processed completion dataset. Request processing is asynchronous. Partial updating is not supported. The operation is successfully finished only after the imported suggestions are indexed successfully and ready for serving. The process takes hours. This feature is only available for users who have Retail Search enabled. Enable Retail Search on Cloud Console before using this feature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_completion_data_import_builder()` + `retail_projects_locations_catalogs_completion_data_import_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_completion_data_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_completion_data_import(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsCompletionDataImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_completion_data_import_builder(client, &args.parent)?;
    retail_projects_locations_catalogs_completion_data_import_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls
/// Creates a Control. If the Control to create already exists, an ALREADY_EXISTS error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_controls_create_execute()` to send, or `retail_projects_locations_catalogs_controls_create` for simplest API.

pub fn retail_projects_locations_catalogs_controls_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    controlId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/controls",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = controlId.as_ref() {
        query_parts.push(format!("controlId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls
/// Creates a Control. If the Control to create already exists, an ALREADY_EXISTS error is returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_controls_create_execute()` or `retail_projects_locations_catalogs_controls_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_controls_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_controls_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Control>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Control = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls
/// Creates a Control. If the Control to create already exists, an ALREADY_EXISTS error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_controls_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_controls_create_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_controls_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_controls_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_controls_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_controls_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_controls_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsControlsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: controlId
    pub controlId: Option<String>,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls
/// Creates a Control. If the Control to create already exists, an ALREADY_EXISTS error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_controls_create_builder()` + `retail_projects_locations_catalogs_controls_create_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_controls_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_controls_create(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsControlsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_controls_create_builder(
        client,
        &args.parent,
        &args.controlId,
    )?;
    retail_projects_locations_catalogs_controls_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls/{controlsId}
/// Deletes a Control. If the Control to delete does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_controls_delete_execute()` to send, or `retail_projects_locations_catalogs_controls_delete` for simplest API.

pub fn retail_projects_locations_catalogs_controls_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/controls/{controlsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls/{controlsId}
/// Deletes a Control. If the Control to delete does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_controls_delete_execute()` or `retail_projects_locations_catalogs_controls_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_controls_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_controls_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls/{controlsId}
/// Deletes a Control. If the Control to delete does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_controls_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_controls_delete_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_controls_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_controls_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_controls_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_controls_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_controls_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsControlsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls/{controlsId}
/// Deletes a Control. If the Control to delete does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_controls_delete_builder()` + `retail_projects_locations_catalogs_controls_delete_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_controls_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_controls_delete(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsControlsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_controls_delete_builder(client, &args.name)?;
    retail_projects_locations_catalogs_controls_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls/{controlsId}
/// Gets a Control.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_controls_get_execute()` to send, or `retail_projects_locations_catalogs_controls_get` for simplest API.

pub fn retail_projects_locations_catalogs_controls_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/controls/{controlsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls/{controlsId}
/// Gets a Control.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_controls_get_execute()` or `retail_projects_locations_catalogs_controls_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_controls_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_controls_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Control>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Control = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls/{controlsId}
/// Gets a Control.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_controls_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_controls_get_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_controls_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_controls_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_controls_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_controls_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_controls_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsControlsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls/{controlsId}
/// Gets a Control.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_controls_get_builder()` + `retail_projects_locations_catalogs_controls_get_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_controls_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_controls_get(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsControlsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_controls_get_builder(client, &args.name)?;
    retail_projects_locations_catalogs_controls_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls
/// Lists all Controls by their parent Catalog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_controls_list_execute()` to send, or `retail_projects_locations_catalogs_controls_list` for simplest API.

pub fn retail_projects_locations_catalogs_controls_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/controls",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls
/// Lists all Controls by their parent Catalog.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_controls_list_execute()` or `retail_projects_locations_catalogs_controls_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_controls_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_controls_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2ListControlsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ListControlsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls
/// Lists all Controls by their parent Catalog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_controls_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_controls_list_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_controls_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_controls_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_controls_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ListControlsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_controls_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_controls_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsControlsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls
/// Lists all Controls by their parent Catalog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_controls_list_builder()` + `retail_projects_locations_catalogs_controls_list_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_controls_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_controls_list(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsControlsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ListControlsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_controls_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    retail_projects_locations_catalogs_controls_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls/{controlsId}
/// Updates a Control. Control cannot be set to a different oneof field, if so an INVALID_ARGUMENT is returned. If the Control to update does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_controls_patch_execute()` to send, or `retail_projects_locations_catalogs_controls_patch` for simplest API.

pub fn retail_projects_locations_catalogs_controls_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/controls/{controlsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls/{controlsId}
/// Updates a Control. Control cannot be set to a different oneof field, if so an INVALID_ARGUMENT is returned. If the Control to update does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_controls_patch_execute()` or `retail_projects_locations_catalogs_controls_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_controls_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_controls_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Control>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Control = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls/{controlsId}
/// Updates a Control. Control cannot be set to a different oneof field, if so an INVALID_ARGUMENT is returned. If the Control to update does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_controls_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_controls_patch_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_controls_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_controls_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_controls_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_controls_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_controls_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsControlsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/controls/{controlsId}
/// Updates a Control. Control cannot be set to a different oneof field, if so an INVALID_ARGUMENT is returned. If the Control to update does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_controls_patch_builder()` + `retail_projects_locations_catalogs_controls_patch_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_controls_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_controls_patch(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsControlsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_controls_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    retail_projects_locations_catalogs_controls_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestion:batchUpdate
/// Allows management of multiple questions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_generative_question_batch_update_execute()` to send, or `retail_projects_locations_catalogs_generative_question_batch_update` for simplest API.

pub fn retail_projects_locations_catalogs_generative_question_batch_update_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestion:batchUpdate",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestion:batchUpdate
/// Allows management of multiple questions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_generative_question_batch_update_execute()` or `retail_projects_locations_catalogs_generative_question_batch_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_generative_question_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_generative_question_batch_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2BatchUpdateGenerativeQuestionConfigsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2BatchUpdateGenerativeQuestionConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestion:batchUpdate
/// Allows management of multiple questions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_generative_question_batch_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_generative_question_batch_update_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_generative_question_batch_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_generative_question_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_generative_question_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRetailV2BatchUpdateGenerativeQuestionConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_generative_question_batch_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_generative_question_batch_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsGenerativeQuestionBatchUpdateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestion:batchUpdate
/// Allows management of multiple questions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_generative_question_batch_update_builder()` + `retail_projects_locations_catalogs_generative_question_batch_update_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_generative_question_batch_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_generative_question_batch_update(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsGenerativeQuestionBatchUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRetailV2BatchUpdateGenerativeQuestionConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_generative_question_batch_update_builder(
        client,
        &args.parent,
    )?;
    retail_projects_locations_catalogs_generative_question_batch_update_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestions
/// Returns all questions for a given catalog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_generative_questions_list_execute()` to send, or `retail_projects_locations_catalogs_generative_questions_list` for simplest API.

pub fn retail_projects_locations_catalogs_generative_questions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestions",
        parent,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestions
/// Returns all questions for a given catalog.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_generative_questions_list_execute()` or `retail_projects_locations_catalogs_generative_questions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_generative_questions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_generative_questions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2ListGenerativeQuestionConfigsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ListGenerativeQuestionConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestions
/// Returns all questions for a given catalog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_generative_questions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_generative_questions_list_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_generative_questions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_generative_questions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_generative_questions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRetailV2ListGenerativeQuestionConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_generative_questions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_generative_questions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsGenerativeQuestionsListArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/generativeQuestions
/// Returns all questions for a given catalog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_generative_questions_list_builder()` + `retail_projects_locations_catalogs_generative_questions_list_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_generative_questions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_generative_questions_list(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsGenerativeQuestionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRetailV2ListGenerativeQuestionConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_generative_questions_list_builder(client, &args.parent)?;
    retail_projects_locations_catalogs_generative_questions_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models
/// Creates a new model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_models_create_execute()` to send, or `retail_projects_locations_catalogs_models_create` for simplest API.

pub fn retail_projects_locations_catalogs_models_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dryRun: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/models",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dryRun.as_ref() {
        query_parts.push(format!("dryRun={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models
/// Creates a new model.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_models_create_execute()` or `retail_projects_locations_catalogs_models_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_models_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_models_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models
/// Creates a new model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_models_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_models_create_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_models_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_models_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_models_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_models_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_models_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsModelsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: dryRun
    pub dryRun: Option<String>,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models
/// Creates a new model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_models_create_builder()` + `retail_projects_locations_catalogs_models_create_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_models_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_models_create(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsModelsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_models_create_builder(
        client,
        &args.parent,
        &args.dryRun,
    )?;
    retail_projects_locations_catalogs_models_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}
/// Deletes an existing model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_models_delete_execute()` to send, or `retail_projects_locations_catalogs_models_delete` for simplest API.

pub fn retail_projects_locations_catalogs_models_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}
/// Deletes an existing model.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_models_delete_execute()` or `retail_projects_locations_catalogs_models_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_models_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_models_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}
/// Deletes an existing model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_models_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_models_delete_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_models_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_models_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_models_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_models_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_models_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsModelsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}
/// Deletes an existing model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_models_delete_builder()` + `retail_projects_locations_catalogs_models_delete_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_models_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_models_delete(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsModelsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_models_delete_builder(client, &args.name)?;
    retail_projects_locations_catalogs_models_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}
/// Gets a model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_models_get_execute()` to send, or `retail_projects_locations_catalogs_models_get` for simplest API.

pub fn retail_projects_locations_catalogs_models_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}
/// Gets a model.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_models_get_execute()` or `retail_projects_locations_catalogs_models_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_models_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_models_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Model>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Model = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}
/// Gets a model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_models_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_models_get_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_models_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_models_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_models_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudRetailV2Model>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_models_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_models_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsModelsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}
/// Gets a model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_models_get_builder()` + `retail_projects_locations_catalogs_models_get_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_models_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_models_get(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsModelsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudRetailV2Model>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_models_get_builder(client, &args.name)?;
    retail_projects_locations_catalogs_models_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models
/// Lists all the models linked to this event store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_models_list_execute()` to send, or `retail_projects_locations_catalogs_models_list` for simplest API.

pub fn retail_projects_locations_catalogs_models_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/models",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models
/// Lists all the models linked to this event store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_models_list_execute()` or `retail_projects_locations_catalogs_models_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_models_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_models_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2ListModelsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ListModelsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models
/// Lists all the models linked to this event store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_models_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_models_list_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_models_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_models_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_models_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ListModelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_models_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_models_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsModelsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models
/// Lists all the models linked to this event store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_models_list_builder()` + `retail_projects_locations_catalogs_models_list_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_models_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_models_list(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsModelsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ListModelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_models_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    retail_projects_locations_catalogs_models_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}
/// Update of model metadata. Only fields that currently can be updated are: filtering_option and periodic_tuning_state. If other values are provided, this API method ignores them.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_models_patch_execute()` to send, or `retail_projects_locations_catalogs_models_patch` for simplest API.

pub fn retail_projects_locations_catalogs_models_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}
/// Update of model metadata. Only fields that currently can be updated are: filtering_option and periodic_tuning_state. If other values are provided, this API method ignores them.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_models_patch_execute()` or `retail_projects_locations_catalogs_models_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_models_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_models_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Model>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Model = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}
/// Update of model metadata. Only fields that currently can be updated are: filtering_option and periodic_tuning_state. If other values are provided, this API method ignores them.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_models_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_models_patch_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_models_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_models_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_models_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudRetailV2Model>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_models_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_models_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsModelsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}
/// Update of model metadata. Only fields that currently can be updated are: filtering_option and periodic_tuning_state. If other values are provided, this API method ignores them.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_models_patch_builder()` + `retail_projects_locations_catalogs_models_patch_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_models_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_models_patch(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsModelsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudRetailV2Model>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_models_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    retail_projects_locations_catalogs_models_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}:pause
/// Pauses the training of an existing model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_models_pause_execute()` to send, or `retail_projects_locations_catalogs_models_pause` for simplest API.

pub fn retail_projects_locations_catalogs_models_pause_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}:pause",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}:pause
/// Pauses the training of an existing model.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_models_pause_execute()` or `retail_projects_locations_catalogs_models_pause`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_models_pause_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_models_pause_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Model>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Model = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}:pause
/// Pauses the training of an existing model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_models_pause_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_models_pause_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_models_pause()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_models_pause_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_models_pause_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudRetailV2Model>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_models_pause_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_models_pause`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsModelsPauseArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}:pause
/// Pauses the training of an existing model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_models_pause_builder()` + `retail_projects_locations_catalogs_models_pause_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_models_pause_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_models_pause(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsModelsPauseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudRetailV2Model>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_models_pause_builder(client, &args.name)?;
    retail_projects_locations_catalogs_models_pause_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}:resume
/// Resumes the training of an existing model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_models_resume_execute()` to send, or `retail_projects_locations_catalogs_models_resume` for simplest API.

pub fn retail_projects_locations_catalogs_models_resume_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}:resume",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}:resume
/// Resumes the training of an existing model.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_models_resume_execute()` or `retail_projects_locations_catalogs_models_resume`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_models_resume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_models_resume_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Model>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Model = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}:resume
/// Resumes the training of an existing model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_models_resume_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_models_resume_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_models_resume()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_models_resume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_models_resume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudRetailV2Model>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_models_resume_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_models_resume`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsModelsResumeArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}:resume
/// Resumes the training of an existing model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_models_resume_builder()` + `retail_projects_locations_catalogs_models_resume_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_models_resume_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_models_resume(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsModelsResumeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudRetailV2Model>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_models_resume_builder(client, &args.name)?;
    retail_projects_locations_catalogs_models_resume_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}:tune
/// Tunes an existing model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_models_tune_execute()` to send, or `retail_projects_locations_catalogs_models_tune` for simplest API.

pub fn retail_projects_locations_catalogs_models_tune_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}:tune",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}:tune
/// Tunes an existing model.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_models_tune_execute()` or `retail_projects_locations_catalogs_models_tune`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_models_tune_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_models_tune_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}:tune
/// Tunes an existing model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_models_tune_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_models_tune_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_models_tune()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_models_tune_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_models_tune_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_models_tune_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_models_tune`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsModelsTuneArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/models/{modelsId}:tune
/// Tunes an existing model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_models_tune_builder()` + `retail_projects_locations_catalogs_models_tune_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_models_tune_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_models_tune(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsModelsTuneArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_models_tune_builder(client, &args.name)?;
    retail_projects_locations_catalogs_models_tune_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_operations_get_execute()` to send, or `retail_projects_locations_catalogs_operations_get` for simplest API.

pub fn retail_projects_locations_catalogs_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_operations_get_execute()` or `retail_projects_locations_catalogs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_operations_get_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_operations_get_builder()` + `retail_projects_locations_catalogs_operations_get_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_operations_get(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_operations_get_builder(client, &args.name)?;
    retail_projects_locations_catalogs_operations_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_operations_list_execute()` to send, or `retail_projects_locations_catalogs_operations_list` for simplest API.

pub fn retail_projects_locations_catalogs_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_operations_list_execute()` or `retail_projects_locations_catalogs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_operations_list_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_operations_list_builder()` + `retail_projects_locations_catalogs_operations_list_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_operations_list(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    retail_projects_locations_catalogs_operations_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/placements/{placementsId}:conversationalSearch
/// Performs a conversational search. This feature is only available for users who have Conversational Search enabled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_placements_conversational_search_execute()` to send, or `retail_projects_locations_catalogs_placements_conversational_search` for simplest API.

pub fn retail_projects_locations_catalogs_placements_conversational_search_builder<R>(
    client: &SimpleHttpClient<R>,
    placement: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/placements/{placementsId}:conversationalSearch",
        placement,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/placements/{placementsId}:conversationalSearch
/// Performs a conversational search. This feature is only available for users who have Conversational Search enabled.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_placements_conversational_search_execute()` or `retail_projects_locations_catalogs_placements_conversational_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_placements_conversational_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_placements_conversational_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2ConversationalSearchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ConversationalSearchResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/placements/{placementsId}:conversationalSearch
/// Performs a conversational search. This feature is only available for users who have Conversational Search enabled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_placements_conversational_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_placements_conversational_search_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_placements_conversational_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_placements_conversational_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_placements_conversational_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ConversationalSearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_placements_conversational_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_placements_conversational_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsPlacementsConversationalSearchArgs {
    /// Path parameter: placement
    pub placement: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/placements/{placementsId}:conversationalSearch
/// Performs a conversational search. This feature is only available for users who have Conversational Search enabled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_placements_conversational_search_builder()` + `retail_projects_locations_catalogs_placements_conversational_search_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_placements_conversational_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_placements_conversational_search(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsPlacementsConversationalSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ConversationalSearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_placements_conversational_search_builder(
        client,
        &args.placement,
    )?;
    retail_projects_locations_catalogs_placements_conversational_search_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/placements/{placementsId}:predict
/// Makes a recommendation prediction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_placements_predict_execute()` to send, or `retail_projects_locations_catalogs_placements_predict` for simplest API.

pub fn retail_projects_locations_catalogs_placements_predict_builder<R>(
    client: &SimpleHttpClient<R>,
    placement: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/placements/{placementsId}:predict",
        placement,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/placements/{placementsId}:predict
/// Makes a recommendation prediction.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_placements_predict_execute()` or `retail_projects_locations_catalogs_placements_predict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_placements_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_placements_predict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2PredictResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2PredictResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/placements/{placementsId}:predict
/// Makes a recommendation prediction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_placements_predict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_placements_predict_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_placements_predict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_placements_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_placements_predict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2PredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_placements_predict_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_placements_predict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsPlacementsPredictArgs {
    /// Path parameter: placement
    pub placement: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/placements/{placementsId}:predict
/// Makes a recommendation prediction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_placements_predict_builder()` + `retail_projects_locations_catalogs_placements_predict_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_placements_predict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_placements_predict(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsPlacementsPredictArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2PredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_placements_predict_builder(client, &args.placement)?;
    retail_projects_locations_catalogs_placements_predict_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/placements/{placementsId}:search
/// Performs a search. This feature is only available for users who have Retail Search enabled. Enable Retail Search on Cloud Console before using this feature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_placements_search_execute()` to send, or `retail_projects_locations_catalogs_placements_search` for simplest API.

pub fn retail_projects_locations_catalogs_placements_search_builder<R>(
    client: &SimpleHttpClient<R>,
    placement: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/placements/{placementsId}:search",
        placement,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/placements/{placementsId}:search
/// Performs a search. This feature is only available for users who have Retail Search enabled. Enable Retail Search on Cloud Console before using this feature.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_placements_search_execute()` or `retail_projects_locations_catalogs_placements_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_placements_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_placements_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2SearchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2SearchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/placements/{placementsId}:search
/// Performs a search. This feature is only available for users who have Retail Search enabled. Enable Retail Search on Cloud Console before using this feature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_placements_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_placements_search_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_placements_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_placements_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_placements_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2SearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_placements_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_placements_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsPlacementsSearchArgs {
    /// Path parameter: placement
    pub placement: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/placements/{placementsId}:search
/// Performs a search. This feature is only available for users who have Retail Search enabled. Enable Retail Search on Cloud Console before using this feature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_placements_search_builder()` + `retail_projects_locations_catalogs_placements_search_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_placements_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_placements_search(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsPlacementsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2SearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_placements_search_builder(client, &args.placement)?;
    retail_projects_locations_catalogs_placements_search_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:addControl
/// Enables a Control on the specified ServingConfig. The control is added in the last position of the list of controls it belongs to (e.g. if it''s a facet spec control it will be applied in the last position of `servingConfig`.`facetSpecIds`) Returns a ALREADY_EXISTS error if the control has already been applied. Returns a FAILED_PRECONDITION error if the addition could exceed maximum number of control allowed for that type of control.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_serving_configs_add_control_execute()` to send, or `retail_projects_locations_catalogs_serving_configs_add_control` for simplest API.

pub fn retail_projects_locations_catalogs_serving_configs_add_control_builder<R>(
    client: &SimpleHttpClient<R>,
    servingConfig: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:addControl",
        servingConfig,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:addControl
/// Enables a Control on the specified ServingConfig. The control is added in the last position of the list of controls it belongs to (e.g. if it''s a facet spec control it will be applied in the last position of `servingConfig`.`facetSpecIds`) Returns a ALREADY_EXISTS error if the control has already been applied. Returns a FAILED_PRECONDITION error if the addition could exceed maximum number of control allowed for that type of control.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_serving_configs_add_control_execute()` or `retail_projects_locations_catalogs_serving_configs_add_control`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_add_control_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_add_control_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ServingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:addControl
/// Enables a Control on the specified ServingConfig. The control is added in the last position of the list of controls it belongs to (e.g. if it''s a facet spec control it will be applied in the last position of `servingConfig`.`facetSpecIds`) Returns a ALREADY_EXISTS error if the control has already been applied. Returns a FAILED_PRECONDITION error if the addition could exceed maximum number of control allowed for that type of control.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_serving_configs_add_control_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_add_control_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_serving_configs_add_control()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_add_control_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_serving_configs_add_control_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_serving_configs_add_control_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_serving_configs_add_control`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsAddControlArgs {
    /// Path parameter: servingConfig
    pub servingConfig: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:addControl
/// Enables a Control on the specified ServingConfig. The control is added in the last position of the list of controls it belongs to (e.g. if it''s a facet spec control it will be applied in the last position of `servingConfig`.`facetSpecIds`) Returns a ALREADY_EXISTS error if the control has already been applied. Returns a FAILED_PRECONDITION error if the addition could exceed maximum number of control allowed for that type of control.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_serving_configs_add_control_builder()` + `retail_projects_locations_catalogs_serving_configs_add_control_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_add_control_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_add_control(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsServingConfigsAddControlArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_serving_configs_add_control_builder(
        client,
        &args.servingConfig,
    )?;
    retail_projects_locations_catalogs_serving_configs_add_control_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:conversationalSearch
/// Performs a conversational search. This feature is only available for users who have Conversational Search enabled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_serving_configs_conversational_search_execute()` to send, or `retail_projects_locations_catalogs_serving_configs_conversational_search` for simplest API.

pub fn retail_projects_locations_catalogs_serving_configs_conversational_search_builder<R>(
    client: &SimpleHttpClient<R>,
    placement: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:conversationalSearch",
        placement,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:conversationalSearch
/// Performs a conversational search. This feature is only available for users who have Conversational Search enabled.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_serving_configs_conversational_search_execute()` or `retail_projects_locations_catalogs_serving_configs_conversational_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_conversational_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_conversational_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2ConversationalSearchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ConversationalSearchResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:conversationalSearch
/// Performs a conversational search. This feature is only available for users who have Conversational Search enabled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_serving_configs_conversational_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_conversational_search_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_serving_configs_conversational_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_conversational_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_serving_configs_conversational_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ConversationalSearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        retail_projects_locations_catalogs_serving_configs_conversational_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_serving_configs_conversational_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsConversationalSearchArgs {
    /// Path parameter: placement
    pub placement: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:conversationalSearch
/// Performs a conversational search. This feature is only available for users who have Conversational Search enabled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_serving_configs_conversational_search_builder()` + `retail_projects_locations_catalogs_serving_configs_conversational_search_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_conversational_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_conversational_search(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsServingConfigsConversationalSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ConversationalSearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_serving_configs_conversational_search_builder(
        client,
        &args.placement,
    )?;
    retail_projects_locations_catalogs_serving_configs_conversational_search_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs
/// Creates a ServingConfig. A maximum of 100 ServingConfigs are allowed in a Catalog, otherwise a FAILED_PRECONDITION error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_serving_configs_create_execute()` to send, or `retail_projects_locations_catalogs_serving_configs_create` for simplest API.

pub fn retail_projects_locations_catalogs_serving_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    servingConfigId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = servingConfigId.as_ref() {
        query_parts.push(format!("servingConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs
/// Creates a ServingConfig. A maximum of 100 ServingConfigs are allowed in a Catalog, otherwise a FAILED_PRECONDITION error is returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_serving_configs_create_execute()` or `retail_projects_locations_catalogs_serving_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ServingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs
/// Creates a ServingConfig. A maximum of 100 ServingConfigs are allowed in a Catalog, otherwise a FAILED_PRECONDITION error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_serving_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_create_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_serving_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_serving_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_serving_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_serving_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: servingConfigId
    pub servingConfigId: Option<String>,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs
/// Creates a ServingConfig. A maximum of 100 ServingConfigs are allowed in a Catalog, otherwise a FAILED_PRECONDITION error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_serving_configs_create_builder()` + `retail_projects_locations_catalogs_serving_configs_create_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_create(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsServingConfigsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_serving_configs_create_builder(
        client,
        &args.parent,
        &args.servingConfigId,
    )?;
    retail_projects_locations_catalogs_serving_configs_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}
/// Deletes a ServingConfig. Returns a NotFound error if the ServingConfig does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_serving_configs_delete_execute()` to send, or `retail_projects_locations_catalogs_serving_configs_delete` for simplest API.

pub fn retail_projects_locations_catalogs_serving_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}
/// Deletes a ServingConfig. Returns a NotFound error if the ServingConfig does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_serving_configs_delete_execute()` or `retail_projects_locations_catalogs_serving_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}
/// Deletes a ServingConfig. Returns a NotFound error if the ServingConfig does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_serving_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_delete_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_serving_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_serving_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_serving_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_serving_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}
/// Deletes a ServingConfig. Returns a NotFound error if the ServingConfig does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_serving_configs_delete_builder()` + `retail_projects_locations_catalogs_serving_configs_delete_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_delete(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsServingConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_serving_configs_delete_builder(client, &args.name)?;
    retail_projects_locations_catalogs_serving_configs_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}
/// Gets a ServingConfig. Returns a NotFound error if the ServingConfig does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_serving_configs_get_execute()` to send, or `retail_projects_locations_catalogs_serving_configs_get` for simplest API.

pub fn retail_projects_locations_catalogs_serving_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}
/// Gets a ServingConfig. Returns a NotFound error if the ServingConfig does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_serving_configs_get_execute()` or `retail_projects_locations_catalogs_serving_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ServingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}
/// Gets a ServingConfig. Returns a NotFound error if the ServingConfig does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_serving_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_get_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_serving_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_serving_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_serving_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_serving_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}
/// Gets a ServingConfig. Returns a NotFound error if the ServingConfig does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_serving_configs_get_builder()` + `retail_projects_locations_catalogs_serving_configs_get_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_get(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsServingConfigsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_serving_configs_get_builder(client, &args.name)?;
    retail_projects_locations_catalogs_serving_configs_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs
/// Lists all ServingConfigs linked to this catalog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_serving_configs_list_execute()` to send, or `retail_projects_locations_catalogs_serving_configs_list` for simplest API.

pub fn retail_projects_locations_catalogs_serving_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs
/// Lists all ServingConfigs linked to this catalog.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_serving_configs_list_execute()` or `retail_projects_locations_catalogs_serving_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2ListServingConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ListServingConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs
/// Lists all ServingConfigs linked to this catalog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_serving_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_list_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_serving_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_serving_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ListServingConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_serving_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_serving_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs
/// Lists all ServingConfigs linked to this catalog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_serving_configs_list_builder()` + `retail_projects_locations_catalogs_serving_configs_list_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_list(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsServingConfigsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ListServingConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_serving_configs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    retail_projects_locations_catalogs_serving_configs_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}
/// Updates a ServingConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_serving_configs_patch_execute()` to send, or `retail_projects_locations_catalogs_serving_configs_patch` for simplest API.

pub fn retail_projects_locations_catalogs_serving_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}
/// Updates a ServingConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_serving_configs_patch_execute()` or `retail_projects_locations_catalogs_serving_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ServingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}
/// Updates a ServingConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_serving_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_patch_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_serving_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_serving_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_serving_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_serving_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}
/// Updates a ServingConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_serving_configs_patch_builder()` + `retail_projects_locations_catalogs_serving_configs_patch_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_patch(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsServingConfigsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_serving_configs_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    retail_projects_locations_catalogs_serving_configs_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:predict
/// Makes a recommendation prediction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_serving_configs_predict_execute()` to send, or `retail_projects_locations_catalogs_serving_configs_predict` for simplest API.

pub fn retail_projects_locations_catalogs_serving_configs_predict_builder<R>(
    client: &SimpleHttpClient<R>,
    placement: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:predict",
        placement,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:predict
/// Makes a recommendation prediction.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_serving_configs_predict_execute()` or `retail_projects_locations_catalogs_serving_configs_predict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_predict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2PredictResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2PredictResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:predict
/// Makes a recommendation prediction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_serving_configs_predict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_predict_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_serving_configs_predict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_serving_configs_predict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2PredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_serving_configs_predict_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_serving_configs_predict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsPredictArgs {
    /// Path parameter: placement
    pub placement: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:predict
/// Makes a recommendation prediction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_serving_configs_predict_builder()` + `retail_projects_locations_catalogs_serving_configs_predict_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_predict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_predict(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsServingConfigsPredictArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2PredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_serving_configs_predict_builder(
        client,
        &args.placement,
    )?;
    retail_projects_locations_catalogs_serving_configs_predict_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:removeControl
/// Disables a Control on the specified ServingConfig. The control is removed from the ServingConfig. Returns a NOT_FOUND error if the Control is not enabled for the ServingConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_serving_configs_remove_control_execute()` to send, or `retail_projects_locations_catalogs_serving_configs_remove_control` for simplest API.

pub fn retail_projects_locations_catalogs_serving_configs_remove_control_builder<R>(
    client: &SimpleHttpClient<R>,
    servingConfig: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:removeControl",
        servingConfig,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:removeControl
/// Disables a Control on the specified ServingConfig. The control is removed from the ServingConfig. Returns a NOT_FOUND error if the Control is not enabled for the ServingConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_serving_configs_remove_control_execute()` or `retail_projects_locations_catalogs_serving_configs_remove_control`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_remove_control_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_remove_control_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ServingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:removeControl
/// Disables a Control on the specified ServingConfig. The control is removed from the ServingConfig. Returns a NOT_FOUND error if the Control is not enabled for the ServingConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_serving_configs_remove_control_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_remove_control_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_serving_configs_remove_control()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_remove_control_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_serving_configs_remove_control_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_serving_configs_remove_control_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_serving_configs_remove_control`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsRemoveControlArgs {
    /// Path parameter: servingConfig
    pub servingConfig: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:removeControl
/// Disables a Control on the specified ServingConfig. The control is removed from the ServingConfig. Returns a NOT_FOUND error if the Control is not enabled for the ServingConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_serving_configs_remove_control_builder()` + `retail_projects_locations_catalogs_serving_configs_remove_control_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_remove_control_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_remove_control(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsServingConfigsRemoveControlArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_serving_configs_remove_control_builder(
        client,
        &args.servingConfig,
    )?;
    retail_projects_locations_catalogs_serving_configs_remove_control_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:search
/// Performs a search. This feature is only available for users who have Retail Search enabled. Enable Retail Search on Cloud Console before using this feature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_serving_configs_search_execute()` to send, or `retail_projects_locations_catalogs_serving_configs_search` for simplest API.

pub fn retail_projects_locations_catalogs_serving_configs_search_builder<R>(
    client: &SimpleHttpClient<R>,
    placement: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:search",
        placement,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:search
/// Performs a search. This feature is only available for users who have Retail Search enabled. Enable Retail Search on Cloud Console before using this feature.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_serving_configs_search_execute()` or `retail_projects_locations_catalogs_serving_configs_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2SearchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2SearchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:search
/// Performs a search. This feature is only available for users who have Retail Search enabled. Enable Retail Search on Cloud Console before using this feature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_serving_configs_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_search_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_serving_configs_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_serving_configs_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_serving_configs_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2SearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_serving_configs_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_serving_configs_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsSearchArgs {
    /// Path parameter: placement
    pub placement: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/servingConfigs/{servingConfigsId}:search
/// Performs a search. This feature is only available for users who have Retail Search enabled. Enable Retail Search on Cloud Console before using this feature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_serving_configs_search_builder()` + `retail_projects_locations_catalogs_serving_configs_search_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_serving_configs_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_serving_configs_search(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsServingConfigsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2SearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_serving_configs_search_builder(client, &args.placement)?;
    retail_projects_locations_catalogs_serving_configs_search_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:collect
/// Writes a single user event from the browser. For larger user event payload over 16 KB, the POST method should be used instead, otherwise a 400 Bad Request error is returned. This method is used only by the Retail API JavaScript pixel and Google Tag Manager. Users should not call this method directly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_user_events_collect_execute()` to send, or `retail_projects_locations_catalogs_user_events_collect` for simplest API.

pub fn retail_projects_locations_catalogs_user_events_collect_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:collect",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:collect
/// Writes a single user event from the browser. For larger user event payload over 16 KB, the POST method should be used instead, otherwise a 400 Bad Request error is returned. This method is used only by the Retail API JavaScript pixel and Google Tag Manager. Users should not call this method directly.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_user_events_collect_execute()` or `retail_projects_locations_catalogs_user_events_collect`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_user_events_collect_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_user_events_collect_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleApiHttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:collect
/// Writes a single user event from the browser. For larger user event payload over 16 KB, the POST method should be used instead, otherwise a 400 Bad Request error is returned. This method is used only by the Retail API JavaScript pixel and Google Tag Manager. Users should not call this method directly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_user_events_collect_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_user_events_collect_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_user_events_collect()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_user_events_collect_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_user_events_collect_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_user_events_collect_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_user_events_collect`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUserEventsCollectArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:collect
/// Writes a single user event from the browser. For larger user event payload over 16 KB, the POST method should be used instead, otherwise a 400 Bad Request error is returned. This method is used only by the Retail API JavaScript pixel and Google Tag Manager. Users should not call this method directly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_user_events_collect_builder()` + `retail_projects_locations_catalogs_user_events_collect_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_user_events_collect_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_user_events_collect(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsUserEventsCollectArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_user_events_collect_builder(client, &args.parent)?;
    retail_projects_locations_catalogs_user_events_collect_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:import
/// Bulk import of User events. Request processing might be synchronous. Events that already exist are skipped. Use this method for backfilling historical user events. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully inserted. Operation.metadata is of type ImportMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_user_events_import_execute()` to send, or `retail_projects_locations_catalogs_user_events_import` for simplest API.

pub fn retail_projects_locations_catalogs_user_events_import_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:import",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:import
/// Bulk import of User events. Request processing might be synchronous. Events that already exist are skipped. Use this method for backfilling historical user events. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully inserted. Operation.metadata is of type ImportMetadata.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_user_events_import_execute()` or `retail_projects_locations_catalogs_user_events_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_user_events_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_user_events_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:import
/// Bulk import of User events. Request processing might be synchronous. Events that already exist are skipped. Use this method for backfilling historical user events. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully inserted. Operation.metadata is of type ImportMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_user_events_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_user_events_import_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_user_events_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_user_events_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_user_events_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_user_events_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_user_events_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUserEventsImportArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:import
/// Bulk import of User events. Request processing might be synchronous. Events that already exist are skipped. Use this method for backfilling historical user events. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully inserted. Operation.metadata is of type ImportMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_user_events_import_builder()` + `retail_projects_locations_catalogs_user_events_import_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_user_events_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_user_events_import(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsUserEventsImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_user_events_import_builder(client, &args.parent)?;
    retail_projects_locations_catalogs_user_events_import_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:purge
/// Deletes permanently all user events specified by the filter provided. Depending on the number of events specified by the filter, this operation could take hours or days to complete. To test a filter, use the list command first.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_user_events_purge_execute()` to send, or `retail_projects_locations_catalogs_user_events_purge` for simplest API.

pub fn retail_projects_locations_catalogs_user_events_purge_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:purge",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:purge
/// Deletes permanently all user events specified by the filter provided. Depending on the number of events specified by the filter, this operation could take hours or days to complete. To test a filter, use the list command first.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_user_events_purge_execute()` or `retail_projects_locations_catalogs_user_events_purge`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_user_events_purge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_user_events_purge_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:purge
/// Deletes permanently all user events specified by the filter provided. Depending on the number of events specified by the filter, this operation could take hours or days to complete. To test a filter, use the list command first.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_user_events_purge_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_user_events_purge_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_user_events_purge()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_user_events_purge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_user_events_purge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_user_events_purge_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_user_events_purge`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUserEventsPurgeArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:purge
/// Deletes permanently all user events specified by the filter provided. Depending on the number of events specified by the filter, this operation could take hours or days to complete. To test a filter, use the list command first.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_user_events_purge_builder()` + `retail_projects_locations_catalogs_user_events_purge_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_user_events_purge_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_user_events_purge(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsUserEventsPurgeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_user_events_purge_builder(client, &args.parent)?;
    retail_projects_locations_catalogs_user_events_purge_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:rejoin
/// Starts a user-event rejoin operation with latest product catalog. Events are not annotated with detailed product information for products that are missing from the catalog when the user event is ingested. These events are stored as unjoined events with limited usage on training and serving. You can use this method to start a join operation on specified events with the latest version of product catalog. You can also use this method to correct events joined with the wrong product catalog. A rejoin operation can take hours or days to complete.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_user_events_rejoin_execute()` to send, or `retail_projects_locations_catalogs_user_events_rejoin` for simplest API.

pub fn retail_projects_locations_catalogs_user_events_rejoin_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:rejoin",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:rejoin
/// Starts a user-event rejoin operation with latest product catalog. Events are not annotated with detailed product information for products that are missing from the catalog when the user event is ingested. These events are stored as unjoined events with limited usage on training and serving. You can use this method to start a join operation on specified events with the latest version of product catalog. You can also use this method to correct events joined with the wrong product catalog. A rejoin operation can take hours or days to complete.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_user_events_rejoin_execute()` or `retail_projects_locations_catalogs_user_events_rejoin`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_user_events_rejoin_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_user_events_rejoin_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:rejoin
/// Starts a user-event rejoin operation with latest product catalog. Events are not annotated with detailed product information for products that are missing from the catalog when the user event is ingested. These events are stored as unjoined events with limited usage on training and serving. You can use this method to start a join operation on specified events with the latest version of product catalog. You can also use this method to correct events joined with the wrong product catalog. A rejoin operation can take hours or days to complete.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_user_events_rejoin_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_user_events_rejoin_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_user_events_rejoin()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_user_events_rejoin_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_user_events_rejoin_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_user_events_rejoin_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_user_events_rejoin`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUserEventsRejoinArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:rejoin
/// Starts a user-event rejoin operation with latest product catalog. Events are not annotated with detailed product information for products that are missing from the catalog when the user event is ingested. These events are stored as unjoined events with limited usage on training and serving. You can use this method to start a join operation on specified events with the latest version of product catalog. You can also use this method to correct events joined with the wrong product catalog. A rejoin operation can take hours or days to complete.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_user_events_rejoin_builder()` + `retail_projects_locations_catalogs_user_events_rejoin_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_user_events_rejoin_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_user_events_rejoin(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsUserEventsRejoinArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        retail_projects_locations_catalogs_user_events_rejoin_builder(client, &args.parent)?;
    retail_projects_locations_catalogs_user_events_rejoin_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:write
/// Writes a single user event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_catalogs_user_events_write_execute()` to send, or `retail_projects_locations_catalogs_user_events_write` for simplest API.

pub fn retail_projects_locations_catalogs_user_events_write_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    writeAsync: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:write",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = writeAsync.as_ref() {
        query_parts.push(format!("writeAsync={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:write
/// Writes a single user event.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_catalogs_user_events_write_execute()` or `retail_projects_locations_catalogs_user_events_write`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_user_events_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_user_events_write_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2UserEvent>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2UserEvent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:write
/// Writes a single user event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_catalogs_user_events_write_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_catalogs_user_events_write_task()`.
/// For the simplest API, use `retail_projects_locations_catalogs_user_events_write()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_catalogs_user_events_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_catalogs_user_events_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2UserEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_catalogs_user_events_write_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_catalogs_user_events_write`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUserEventsWriteArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: writeAsync
    pub writeAsync: Option<String>,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/userEvents:write
/// Writes a single user event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_catalogs_user_events_write_builder()` + `retail_projects_locations_catalogs_user_events_write_execute()`.
/// For task-level control, use `retail_projects_locations_catalogs_user_events_write_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_catalogs_user_events_write(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsCatalogsUserEventsWriteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRetailV2UserEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_catalogs_user_events_write_builder(
        client,
        &args.parent,
        &args.writeAsync,
    )?;
    retail_projects_locations_catalogs_user_events_write_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_operations_get_execute()` to send, or `retail_projects_locations_operations_get` for simplest API.

pub fn retail_projects_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_operations_get_execute()` or `retail_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_operations_get_task()`.
/// For the simplest API, use `retail_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_operations_get_builder()` + `retail_projects_locations_operations_get_execute()`.
/// For task-level control, use `retail_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_operations_get_builder(client, &args.name)?;
    retail_projects_locations_operations_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_locations_operations_list_execute()` to send, or `retail_projects_locations_operations_list` for simplest API.

pub fn retail_projects_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_locations_operations_list_execute()` or `retail_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_locations_operations_list_task()`.
/// For the simplest API, use `retail_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_locations_operations_list_builder()` + `retail_projects_locations_operations_list_execute()`.
/// For task-level control, use `retail_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &RetailProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    retail_projects_locations_operations_list_execute(builder)
}

/// GET v2/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_operations_get_execute()` to send, or `retail_projects_operations_get` for simplest API.

pub fn retail_projects_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_operations_get_execute()` or `retail_projects_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_operations_get_task()`.
/// For the simplest API, use `retail_projects_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_operations_get_builder()` + `retail_projects_operations_get_execute()`.
/// For task-level control, use `retail_projects_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_operations_get(
    client: &SimpleHttpClient,
    args: &RetailProjectsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_operations_get_builder(client, &args.name)?;
    retail_projects_operations_get_execute(builder)
}

/// GET v2/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `retail_projects_operations_list_execute()` to send, or `retail_projects_operations_list` for simplest API.

pub fn retail_projects_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/projects/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `retail_projects_operations_list_execute()` or `retail_projects_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `retail_projects_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `retail_projects_operations_list_task()`.
/// For the simplest API, use `retail_projects_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `retail_projects_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn retail_projects_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = retail_projects_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`retail_projects_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RetailProjectsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v2/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `retail_projects_operations_list_builder()` + `retail_projects_operations_list_execute()`.
/// For task-level control, use `retail_projects_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn retail_projects_operations_list(
    client: &SimpleHttpClient,
    args: &RetailProjectsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = retail_projects_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    retail_projects_operations_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2CompleteQueryResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2CompleteQueryResponse with RetailProjectsLocationsCatalogsCompleteQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsCompleteQueryArgs>
    for GoogleCloudRetailV2CompleteQueryResponse
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsCompleteQueryArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2CompleteQueryResponse/{}",
            input.catalog
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2CompleteQueryResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsLocationsCatalogsExportAnalyticsMetricsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsExportAnalyticsMetricsArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsExportAnalyticsMetricsArgs,
    ) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.catalog)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2AttributesConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2AttributesConfig with RetailProjectsLocationsCatalogsGetAttributesConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsGetAttributesConfigArgs>
    for GoogleCloudRetailV2AttributesConfig
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsGetAttributesConfigArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2AttributesConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2AttributesConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2CompletionConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2CompletionConfig with RetailProjectsLocationsCatalogsGetCompletionConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsGetCompletionConfigArgs>
    for GoogleCloudRetailV2CompletionConfig
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsGetCompletionConfigArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2CompletionConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2CompletionConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2ConversationalSearchCustomizationConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2ConversationalSearchCustomizationConfig with RetailProjectsLocationsCatalogsGetConversationalSearchCustomizationConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        RetailProjectsLocationsCatalogsGetConversationalSearchCustomizationConfigArgs,
    > for GoogleCloudRetailV2ConversationalSearchCustomizationConfig
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsGetConversationalSearchCustomizationConfigArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2ConversationalSearchCustomizationConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2ConversationalSearchCustomizationConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2GetDefaultBranchResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2GetDefaultBranchResponse with RetailProjectsLocationsCatalogsGetDefaultBranchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsGetDefaultBranchArgs>
    for GoogleCloudRetailV2GetDefaultBranchResponse
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsGetDefaultBranchArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2GetDefaultBranchResponse/{}",
            input.catalog
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2GetDefaultBranchResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2GenerativeQuestionsFeatureConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2GenerativeQuestionsFeatureConfig with RetailProjectsLocationsCatalogsGetGenerativeQuestionFeatureArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsGetGenerativeQuestionFeatureArgs>
    for GoogleCloudRetailV2GenerativeQuestionsFeatureConfig
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsGetGenerativeQuestionFeatureArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2GenerativeQuestionsFeatureConfig/{}",
            input.catalog
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2GenerativeQuestionsFeatureConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2ListCatalogsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2ListCatalogsResponse with RetailProjectsLocationsCatalogsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsListArgs>
    for GoogleCloudRetailV2ListCatalogsResponse
{
    fn generate_resource_id(&self, input: &RetailProjectsLocationsCatalogsListArgs) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2ListCatalogsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2ListCatalogsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2Catalog
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2Catalog with RetailProjectsLocationsCatalogsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsPatchArgs> for GoogleCloudRetailV2Catalog {
    fn generate_resource_id(&self, input: &RetailProjectsLocationsCatalogsPatchArgs) -> String {
        format!("gcp::retail::GoogleCloudRetailV2Catalog/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2Catalog"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with RetailProjectsLocationsCatalogsSetDefaultBranchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsSetDefaultBranchArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsSetDefaultBranchArgs,
    ) -> String {
        format!("gcp::retail::GoogleProtobufEmpty/{}", input.catalog)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2AttributesConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2AttributesConfig with RetailProjectsLocationsCatalogsUpdateAttributesConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsUpdateAttributesConfigArgs>
    for GoogleCloudRetailV2AttributesConfig
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsUpdateAttributesConfigArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2AttributesConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2AttributesConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2CompletionConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2CompletionConfig with RetailProjectsLocationsCatalogsUpdateCompletionConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsUpdateCompletionConfigArgs>
    for GoogleCloudRetailV2CompletionConfig
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsUpdateCompletionConfigArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2CompletionConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2CompletionConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2ConversationalSearchCustomizationConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2ConversationalSearchCustomizationConfig with RetailProjectsLocationsCatalogsUpdateConversationalSearchCustomizationConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        RetailProjectsLocationsCatalogsUpdateConversationalSearchCustomizationConfigArgs,
    > for GoogleCloudRetailV2ConversationalSearchCustomizationConfig
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsUpdateConversationalSearchCustomizationConfigArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2ConversationalSearchCustomizationConfig/{}",
            input.catalog
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2ConversationalSearchCustomizationConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2GenerativeQuestionConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2GenerativeQuestionConfig with RetailProjectsLocationsCatalogsUpdateGenerativeQuestionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsUpdateGenerativeQuestionArgs>
    for GoogleCloudRetailV2GenerativeQuestionConfig
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsUpdateGenerativeQuestionArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2GenerativeQuestionConfig/{}",
            input.catalog
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2GenerativeQuestionConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2GenerativeQuestionsFeatureConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2GenerativeQuestionsFeatureConfig with RetailProjectsLocationsCatalogsUpdateGenerativeQuestionFeatureArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsUpdateGenerativeQuestionFeatureArgs>
    for GoogleCloudRetailV2GenerativeQuestionsFeatureConfig
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsUpdateGenerativeQuestionFeatureArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2GenerativeQuestionsFeatureConfig/{}",
            input.catalog
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2GenerativeQuestionsFeatureConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2AttributesConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2AttributesConfig with RetailProjectsLocationsCatalogsAttributesConfigAddCatalogAttributeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsAttributesConfigAddCatalogAttributeArgs>
    for GoogleCloudRetailV2AttributesConfig
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsAttributesConfigAddCatalogAttributeArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2AttributesConfig/{}",
            input.attributesConfig
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2AttributesConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2AttributesConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2AttributesConfig with RetailProjectsLocationsCatalogsAttributesConfigRemoveCatalogAttributeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsAttributesConfigRemoveCatalogAttributeArgs>
    for GoogleCloudRetailV2AttributesConfig
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsAttributesConfigRemoveCatalogAttributeArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2AttributesConfig/{}",
            input.attributesConfig
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2AttributesConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2AttributesConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2AttributesConfig with RetailProjectsLocationsCatalogsAttributesConfigReplaceCatalogAttributeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsAttributesConfigReplaceCatalogAttributeArgs>
    for GoogleCloudRetailV2AttributesConfig
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsAttributesConfigReplaceCatalogAttributeArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2AttributesConfig/{}",
            input.attributesConfig
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2AttributesConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsLocationsCatalogsBranchesOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsBranchesOperationsGetArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsBranchesOperationsGetArgs,
    ) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsLocationsCatalogsBranchesProductsAddFulfillmentPlacesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsBranchesProductsAddFulfillmentPlacesArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsBranchesProductsAddFulfillmentPlacesArgs,
    ) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.product)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsLocationsCatalogsBranchesProductsAddLocalInventoriesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsBranchesProductsAddLocalInventoriesArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsBranchesProductsAddLocalInventoriesArgs,
    ) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.product)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2Product
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2Product with RetailProjectsLocationsCatalogsBranchesProductsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsBranchesProductsCreateArgs>
    for GoogleCloudRetailV2Product
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsBranchesProductsCreateArgs,
    ) -> String {
        format!("gcp::retail::GoogleCloudRetailV2Product/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2Product"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with RetailProjectsLocationsCatalogsBranchesProductsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsBranchesProductsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsBranchesProductsDeleteArgs,
    ) -> String {
        format!("gcp::retail::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2Product
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2Product with RetailProjectsLocationsCatalogsBranchesProductsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsBranchesProductsGetArgs>
    for GoogleCloudRetailV2Product
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsBranchesProductsGetArgs,
    ) -> String {
        format!("gcp::retail::GoogleCloudRetailV2Product/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2Product"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsLocationsCatalogsBranchesProductsImportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsBranchesProductsImportArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsBranchesProductsImportArgs,
    ) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2ListProductsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2ListProductsResponse with RetailProjectsLocationsCatalogsBranchesProductsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsBranchesProductsListArgs>
    for GoogleCloudRetailV2ListProductsResponse
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsBranchesProductsListArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2ListProductsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2ListProductsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2Product
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2Product with RetailProjectsLocationsCatalogsBranchesProductsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsBranchesProductsPatchArgs>
    for GoogleCloudRetailV2Product
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsBranchesProductsPatchArgs,
    ) -> String {
        format!("gcp::retail::GoogleCloudRetailV2Product/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2Product"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsLocationsCatalogsBranchesProductsPurgeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsBranchesProductsPurgeArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsBranchesProductsPurgeArgs,
    ) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsLocationsCatalogsBranchesProductsRemoveFulfillmentPlacesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsBranchesProductsRemoveFulfillmentPlacesArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsBranchesProductsRemoveFulfillmentPlacesArgs,
    ) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.product)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsLocationsCatalogsBranchesProductsRemoveLocalInventoriesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsBranchesProductsRemoveLocalInventoriesArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsBranchesProductsRemoveLocalInventoriesArgs,
    ) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.product)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsLocationsCatalogsBranchesProductsSetInventoryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsBranchesProductsSetInventoryArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsBranchesProductsSetInventoryArgs,
    ) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsLocationsCatalogsCompletionDataImportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsCompletionDataImportArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsCompletionDataImportArgs,
    ) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2Control
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2Control with RetailProjectsLocationsCatalogsControlsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsControlsCreateArgs>
    for GoogleCloudRetailV2Control
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsControlsCreateArgs,
    ) -> String {
        format!("gcp::retail::GoogleCloudRetailV2Control/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2Control"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with RetailProjectsLocationsCatalogsControlsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsControlsDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsControlsDeleteArgs,
    ) -> String {
        format!("gcp::retail::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2Control
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2Control with RetailProjectsLocationsCatalogsControlsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsControlsGetArgs>
    for GoogleCloudRetailV2Control
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsControlsGetArgs,
    ) -> String {
        format!("gcp::retail::GoogleCloudRetailV2Control/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2Control"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2ListControlsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2ListControlsResponse with RetailProjectsLocationsCatalogsControlsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsControlsListArgs>
    for GoogleCloudRetailV2ListControlsResponse
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsControlsListArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2ListControlsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2ListControlsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2Control
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2Control with RetailProjectsLocationsCatalogsControlsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsControlsPatchArgs>
    for GoogleCloudRetailV2Control
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsControlsPatchArgs,
    ) -> String {
        format!("gcp::retail::GoogleCloudRetailV2Control/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2Control"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2BatchUpdateGenerativeQuestionConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2BatchUpdateGenerativeQuestionConfigsResponse with RetailProjectsLocationsCatalogsGenerativeQuestionBatchUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsGenerativeQuestionBatchUpdateArgs>
    for GoogleCloudRetailV2BatchUpdateGenerativeQuestionConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsGenerativeQuestionBatchUpdateArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2BatchUpdateGenerativeQuestionConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2BatchUpdateGenerativeQuestionConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2ListGenerativeQuestionConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2ListGenerativeQuestionConfigsResponse with RetailProjectsLocationsCatalogsGenerativeQuestionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsGenerativeQuestionsListArgs>
    for GoogleCloudRetailV2ListGenerativeQuestionConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsGenerativeQuestionsListArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2ListGenerativeQuestionConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2ListGenerativeQuestionConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsLocationsCatalogsModelsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsModelsCreateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsModelsCreateArgs,
    ) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with RetailProjectsLocationsCatalogsModelsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsModelsDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsModelsDeleteArgs,
    ) -> String {
        format!("gcp::retail::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2Model
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2Model with RetailProjectsLocationsCatalogsModelsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsModelsGetArgs> for GoogleCloudRetailV2Model {
    fn generate_resource_id(&self, input: &RetailProjectsLocationsCatalogsModelsGetArgs) -> String {
        format!("gcp::retail::GoogleCloudRetailV2Model/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2Model"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2ListModelsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2ListModelsResponse with RetailProjectsLocationsCatalogsModelsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsModelsListArgs>
    for GoogleCloudRetailV2ListModelsResponse
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsModelsListArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2ListModelsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2ListModelsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2Model
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2Model with RetailProjectsLocationsCatalogsModelsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsModelsPatchArgs>
    for GoogleCloudRetailV2Model
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsModelsPatchArgs,
    ) -> String {
        format!("gcp::retail::GoogleCloudRetailV2Model/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2Model"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2Model
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2Model with RetailProjectsLocationsCatalogsModelsPauseArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsModelsPauseArgs>
    for GoogleCloudRetailV2Model
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsModelsPauseArgs,
    ) -> String {
        format!("gcp::retail::GoogleCloudRetailV2Model/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2Model"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2Model
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2Model with RetailProjectsLocationsCatalogsModelsResumeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsModelsResumeArgs>
    for GoogleCloudRetailV2Model
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsModelsResumeArgs,
    ) -> String {
        format!("gcp::retail::GoogleCloudRetailV2Model/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2Model"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsLocationsCatalogsModelsTuneArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsModelsTuneArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsModelsTuneArgs,
    ) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsLocationsCatalogsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsOperationsGetArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsOperationsGetArgs,
    ) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse with RetailProjectsLocationsCatalogsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsOperationsListArgs>
    for GoogleLongrunningListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsOperationsListArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleLongrunningListOperationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2ConversationalSearchResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2ConversationalSearchResponse with RetailProjectsLocationsCatalogsPlacementsConversationalSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsPlacementsConversationalSearchArgs>
    for GoogleCloudRetailV2ConversationalSearchResponse
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsPlacementsConversationalSearchArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2ConversationalSearchResponse/{}",
            input.placement
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2ConversationalSearchResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2PredictResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2PredictResponse with RetailProjectsLocationsCatalogsPlacementsPredictArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsPlacementsPredictArgs>
    for GoogleCloudRetailV2PredictResponse
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsPlacementsPredictArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2PredictResponse/{}",
            input.placement
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2PredictResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2SearchResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2SearchResponse with RetailProjectsLocationsCatalogsPlacementsSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsPlacementsSearchArgs>
    for GoogleCloudRetailV2SearchResponse
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsPlacementsSearchArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2SearchResponse/{}",
            input.placement
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2SearchResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2ServingConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2ServingConfig with RetailProjectsLocationsCatalogsServingConfigsAddControlArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsServingConfigsAddControlArgs>
    for GoogleCloudRetailV2ServingConfig
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsServingConfigsAddControlArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2ServingConfig/{}",
            input.servingConfig
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2ServingConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2ConversationalSearchResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2ConversationalSearchResponse with RetailProjectsLocationsCatalogsServingConfigsConversationalSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsServingConfigsConversationalSearchArgs>
    for GoogleCloudRetailV2ConversationalSearchResponse
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsServingConfigsConversationalSearchArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2ConversationalSearchResponse/{}",
            input.placement
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2ConversationalSearchResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2ServingConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2ServingConfig with RetailProjectsLocationsCatalogsServingConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsServingConfigsCreateArgs>
    for GoogleCloudRetailV2ServingConfig
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsServingConfigsCreateArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2ServingConfig/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2ServingConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with RetailProjectsLocationsCatalogsServingConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsServingConfigsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsServingConfigsDeleteArgs,
    ) -> String {
        format!("gcp::retail::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2ServingConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2ServingConfig with RetailProjectsLocationsCatalogsServingConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsServingConfigsGetArgs>
    for GoogleCloudRetailV2ServingConfig
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsServingConfigsGetArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2ServingConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2ServingConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2ListServingConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2ListServingConfigsResponse with RetailProjectsLocationsCatalogsServingConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsServingConfigsListArgs>
    for GoogleCloudRetailV2ListServingConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsServingConfigsListArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2ListServingConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2ListServingConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2ServingConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2ServingConfig with RetailProjectsLocationsCatalogsServingConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsServingConfigsPatchArgs>
    for GoogleCloudRetailV2ServingConfig
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsServingConfigsPatchArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2ServingConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2ServingConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2PredictResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2PredictResponse with RetailProjectsLocationsCatalogsServingConfigsPredictArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsServingConfigsPredictArgs>
    for GoogleCloudRetailV2PredictResponse
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsServingConfigsPredictArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2PredictResponse/{}",
            input.placement
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2PredictResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2ServingConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2ServingConfig with RetailProjectsLocationsCatalogsServingConfigsRemoveControlArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsServingConfigsRemoveControlArgs>
    for GoogleCloudRetailV2ServingConfig
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsServingConfigsRemoveControlArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2ServingConfig/{}",
            input.servingConfig
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2ServingConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2SearchResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2SearchResponse with RetailProjectsLocationsCatalogsServingConfigsSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsServingConfigsSearchArgs>
    for GoogleCloudRetailV2SearchResponse
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsServingConfigsSearchArgs,
    ) -> String {
        format!(
            "gcp::retail::GoogleCloudRetailV2SearchResponse/{}",
            input.placement
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2SearchResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleApiHttpBody
// =============================================================================

/// ResourceIdentifier implementation for GoogleApiHttpBody with RetailProjectsLocationsCatalogsUserEventsCollectArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsUserEventsCollectArgs>
    for GoogleApiHttpBody
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsUserEventsCollectArgs,
    ) -> String {
        format!("gcp::retail::GoogleApiHttpBody/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleApiHttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsLocationsCatalogsUserEventsImportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsUserEventsImportArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsUserEventsImportArgs,
    ) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsLocationsCatalogsUserEventsPurgeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsUserEventsPurgeArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsUserEventsPurgeArgs,
    ) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsLocationsCatalogsUserEventsRejoinArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsUserEventsRejoinArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsUserEventsRejoinArgs,
    ) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRetailV2UserEvent
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRetailV2UserEvent with RetailProjectsLocationsCatalogsUserEventsWriteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsCatalogsUserEventsWriteArgs>
    for GoogleCloudRetailV2UserEvent
{
    fn generate_resource_id(
        &self,
        input: &RetailProjectsLocationsCatalogsUserEventsWriteArgs,
    ) -> String {
        format!("gcp::retail::GoogleCloudRetailV2UserEvent/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleCloudRetailV2UserEvent"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsOperationsGetArgs> for GoogleLongrunningOperation {
    fn generate_resource_id(&self, input: &RetailProjectsLocationsOperationsGetArgs) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse with RetailProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsLocationsOperationsListArgs>
    for GoogleLongrunningListOperationsResponse
{
    fn generate_resource_id(&self, input: &RetailProjectsLocationsOperationsListArgs) -> String {
        format!(
            "gcp::retail::GoogleLongrunningListOperationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RetailProjectsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsOperationsGetArgs> for GoogleLongrunningOperation {
    fn generate_resource_id(&self, input: &RetailProjectsOperationsGetArgs) -> String {
        format!("gcp::retail::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse with RetailProjectsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RetailProjectsOperationsListArgs>
    for GoogleLongrunningListOperationsResponse
{
    fn generate_resource_id(&self, input: &RetailProjectsOperationsListArgs) -> String {
        format!(
            "gcp::retail::GoogleLongrunningListOperationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::retail::GoogleLongrunningListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
