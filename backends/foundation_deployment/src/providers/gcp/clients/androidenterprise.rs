//! Auto-generated API clients for gcp/androidenterprise.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/forceReportUpload
/// Uploads a report containing any changes in app states on the device since the last report was generated. You can call this method up to 3 times every 24 hours for a given device. If you exceed the quota, then the Google Play EMM API returns HTTP 429 Too Many Requests.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_devices_force_report_upload_execute()` to send, or `androidenterprise_devices_force_report_upload` for simplest API.

pub fn androidenterprise_devices_force_report_upload_builder(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/devices/{}/forceReportUpload",
        deviceId,
        enterpriseId,
        userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/forceReportUpload
/// Uploads a report containing any changes in app states on the device since the last report was generated. You can call this method up to 3 times every 24 hours for a given device. If you exceed the quota, then the Google Play EMM API returns HTTP 429 Too Many Requests.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_devices_force_report_upload_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_devices_force_report_upload_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_force_report_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/forceReportUpload
/// Uploads a report containing any changes in app states on the device since the last report was generated. You can call this method up to 3 times every 24 hours for a given device. If you exceed the quota, then the Google Play EMM API returns HTTP 429 Too Many Requests.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_devices_force_report_upload_builder()` + `androidenterprise_devices_force_report_upload_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_force_report_upload(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_devices_force_report_upload_builder(
        client,
        deviceId,
        enterpriseId,
        userId,
    )?;
    androidenterprise_devices_force_report_upload_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}
/// Retrieves the details of a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_devices_get_execute()` to send, or `androidenterprise_devices_get` for simplest API.

pub fn androidenterprise_devices_get_builder(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/devices/{}",
        deviceId, enterpriseId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}
/// Retrieves the details of a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_devices_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_devices_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Device>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Device = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}
/// Retrieves the details of a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_devices_get_builder()` + `androidenterprise_devices_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_get(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Device>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_devices_get_builder(client, deviceId, enterpriseId, userId)?;
    androidenterprise_devices_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/state
/// Checks if a device can access Google apps and services for a user. Returns whether access is "enabled" or "disabled". A "disabled" state prevents the user's Managed Google Account on the device from successfully authenticating with Google. This blocks access to most Google applications and services, including Google Play, as the device cannot prove its entitlement to access them. New devices default to "disabled". Important: Enforcement of this state depends on the following conditions: * The user must be a managed google account. * The enterprise must be a managed google domain. * Third-party Android mobile management must be active in the Google Admin Console for the user's Organizational Unit. If these conditions aren't met, access may still be possible even in a "disabled" state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_devices_get_state_execute()` to send, or `androidenterprise_devices_get_state` for simplest API.

pub fn androidenterprise_devices_get_state_builder(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/devices/{}/state",
        deviceId, enterpriseId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/state
/// Checks if a device can access Google apps and services for a user. Returns whether access is "enabled" or "disabled". A "disabled" state prevents the user's Managed Google Account on the device from successfully authenticating with Google. This blocks access to most Google applications and services, including Google Play, as the device cannot prove its entitlement to access them. New devices default to "disabled". Important: Enforcement of this state depends on the following conditions: * The user must be a managed google account. * The enterprise must be a managed google domain. * Third-party Android mobile management must be active in the Google Admin Console for the user's Organizational Unit. If these conditions aren't met, access may still be possible even in a "disabled" state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_devices_get_state_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_devices_get_state_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_get_state_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceState>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeviceState = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/state
/// Checks if a device can access Google apps and services for a user. Returns whether access is "enabled" or "disabled". A "disabled" state prevents the user's Managed Google Account on the device from successfully authenticating with Google. This blocks access to most Google applications and services, including Google Play, as the device cannot prove its entitlement to access them. New devices default to "disabled". Important: Enforcement of this state depends on the following conditions: * The user must be a managed google account. * The enterprise must be a managed google domain. * Third-party Android mobile management must be active in the Google Admin Console for the user's Organizational Unit. If these conditions aren't met, access may still be possible even in a "disabled" state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_devices_get_state_builder()` + `androidenterprise_devices_get_state_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_get_state(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceState>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_devices_get_state_builder(client, deviceId, enterpriseId, userId)?;
    androidenterprise_devices_get_state_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices
/// Retrieves the IDs of all of a user's devices.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_devices_list_execute()` to send, or `androidenterprise_devices_list` for simplest API.

pub fn androidenterprise_devices_list_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/devices",
        enterpriseId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices
/// Retrieves the IDs of all of a user's devices.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DevicesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DevicesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices
/// Retrieves the IDs of all of a user's devices.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_devices_list_builder()` + `androidenterprise_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_list(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DevicesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_devices_list_builder(client, enterpriseId, userId)?;
    androidenterprise_devices_list_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/state
/// Sets whether a device's access to Google services (including Google Play) is enabled or disabled for the specified user. Setting the state to "enabled" allows the Google Account to access Google services, while "disabled" blocks access by preventing OAuth token issuance. Preconditions for Enforcement: 1. This setting is only effective for Google-managed users. 2. The enterprise must be linked to a Google Managed Domain. 3. Enforcement requires third-party Android mobile management to be enabled within the Google Admin Console for the user's Organizational Unit. If these preconditions are not met, changes to this state may be ignored.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_devices_set_state_execute()` to send, or `androidenterprise_devices_set_state` for simplest API.

pub fn androidenterprise_devices_set_state_builder(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    userId: &str,
    body: &DeviceState,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/devices/{}/state",
        deviceId, enterpriseId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/state
/// Sets whether a device's access to Google services (including Google Play) is enabled or disabled for the specified user. Setting the state to "enabled" allows the Google Account to access Google services, while "disabled" blocks access by preventing OAuth token issuance. Preconditions for Enforcement: 1. This setting is only effective for Google-managed users. 2. The enterprise must be linked to a Google Managed Domain. 3. Enforcement requires third-party Android mobile management to be enabled within the Google Admin Console for the user's Organizational Unit. If these preconditions are not met, changes to this state may be ignored.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_devices_set_state_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_devices_set_state_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_set_state_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceState>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeviceState = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/state
/// Sets whether a device's access to Google services (including Google Play) is enabled or disabled for the specified user. Setting the state to "enabled" allows the Google Account to access Google services, while "disabled" blocks access by preventing OAuth token issuance. Preconditions for Enforcement: 1. This setting is only effective for Google-managed users. 2. The enterprise must be linked to a Google Managed Domain. 3. Enforcement requires third-party Android mobile management to be enabled within the Google Admin Console for the user's Organizational Unit. If these preconditions are not met, changes to this state may be ignored.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_devices_set_state_builder()` + `androidenterprise_devices_set_state_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_set_state(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    userId: &str,
    body: &DeviceState,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceState>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_devices_set_state_builder(client, deviceId, enterpriseId, userId, body)?;
    androidenterprise_devices_set_state_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}
/// Updates the device policy. To ensure the policy is properly enforced, you need to prevent unmanaged accounts from accessing Google Play by setting the allowed_accounts in the managed configuration for the Google Play package. See restrict accounts in Google Play. When provisioning a new device, you should set the device policy using this method before adding the managed Google Play Account to the device, otherwise the policy will not be applied for a short period of time after adding the account to the device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_devices_update_execute()` to send, or `androidenterprise_devices_update` for simplest API.

pub fn androidenterprise_devices_update_builder(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    userId: &str,
    updateMask: Option<&str>,
    body: &Device,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/devices/{}",
        deviceId, enterpriseId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}
/// Updates the device policy. To ensure the policy is properly enforced, you need to prevent unmanaged accounts from accessing Google Play by setting the allowed_accounts in the managed configuration for the Google Play package. See restrict accounts in Google Play. When provisioning a new device, you should set the device policy using this method before adding the managed Google Play Account to the device, otherwise the policy will not be applied for a short period of time after adding the account to the device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_devices_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_devices_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Device>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Device = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}
/// Updates the device policy. To ensure the policy is properly enforced, you need to prevent unmanaged accounts from accessing Google Play by setting the allowed_accounts in the managed configuration for the Google Play package. See restrict accounts in Google Play. When provisioning a new device, you should set the device policy using this method before adding the managed Google Play Account to the device, otherwise the policy will not be applied for a short period of time after adding the account to the device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_devices_update_builder()` + `androidenterprise_devices_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_update(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    userId: &str,
    updateMask: Option<&str>,
    body: &Device,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Device>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_devices_update_builder(
        client,
        deviceId,
        enterpriseId,
        userId,
        updateMask,
        body,
    )?;
    androidenterprise_devices_update_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/enrollmentTokens
/// Returns a token for device enrollment. The DPC can encode this token within the QR/NFC/zero-touch enrollment payload or fetch it before calling the on-device API to authenticate the user. The token can be generated for each device or reused across multiple devices.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enrollment_tokens_create_execute()` to send, or `androidenterprise_enrollment_tokens_create` for simplest API.

pub fn androidenterprise_enrollment_tokens_create_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    body: &EnrollmentToken,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/enrollmentTokens",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/enrollmentTokens
/// Returns a token for device enrollment. The DPC can encode this token within the QR/NFC/zero-touch enrollment payload or fetch it before calling the on-device API to authenticate the user. The token can be generated for each device or reused across multiple devices.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_enrollment_tokens_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_enrollment_tokens_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enrollment_tokens_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnrollmentToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EnrollmentToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/enrollmentTokens
/// Returns a token for device enrollment. The DPC can encode this token within the QR/NFC/zero-touch enrollment payload or fetch it before calling the on-device API to authenticate the user. The token can be generated for each device or reused across multiple devices.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enrollment_tokens_create_builder()` + `androidenterprise_enrollment_tokens_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enrollment_tokens_create(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    body: &EnrollmentToken,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnrollmentToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_enrollment_tokens_create_builder(client, enterpriseId, body)?;
    androidenterprise_enrollment_tokens_create_execute(builder)
}

/// GET androidenterprise/v1/enterprises/acknowledgeNotificationSet
/// Acknowledges notifications that were received from Enterprises.PullNotificationSet to prevent subsequent calls from returning the same notifications.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_acknowledge_notification_set_execute()` to send, or `androidenterprise_enterprises_acknowledge_notification_set` for simplest API.

pub fn androidenterprise_enterprises_acknowledge_notification_set_builder(
    client: &SimpleHttpClient,
    notificationSetId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/acknowledgeNotificationSet",
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "notificationSetId",
            notificationSetId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidenterprise/v1/enterprises/acknowledgeNotificationSet
/// Acknowledges notifications that were received from Enterprises.PullNotificationSet to prevent subsequent calls from returning the same notifications.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_enterprises_acknowledge_notification_set_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_enterprises_acknowledge_notification_set_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_acknowledge_notification_set_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/acknowledgeNotificationSet
/// Acknowledges notifications that were received from Enterprises.PullNotificationSet to prevent subsequent calls from returning the same notifications.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_acknowledge_notification_set_builder()` + `androidenterprise_enterprises_acknowledge_notification_set_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_acknowledge_notification_set(
    client: &SimpleHttpClient,
    notificationSetId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_acknowledge_notification_set_builder(
        client,
        notificationSetId,
    )?;
    androidenterprise_enterprises_acknowledge_notification_set_execute(builder)
}

/// GET androidenterprise/v1/enterprises/completeSignup
/// Completes the signup flow, by specifying the Completion token and Enterprise token. This request must not be called multiple times for a given Enterprise Token.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_complete_signup_execute()` to send, or `androidenterprise_enterprises_complete_signup` for simplest API.

pub fn androidenterprise_enterprises_complete_signup_builder(
    client: &SimpleHttpClient,
    completionToken: Option<&str>,
    enterpriseToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comandroidenterprise/v1/enterprises/completeSignup",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "completionToken",
            completionToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "enterpriseToken",
            enterpriseToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidenterprise/v1/enterprises/completeSignup
/// Completes the signup flow, by specifying the Completion token and Enterprise token. This request must not be called multiple times for a given Enterprise Token.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_enterprises_complete_signup_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_enterprises_complete_signup_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_complete_signup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Enterprise = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/completeSignup
/// Completes the signup flow, by specifying the Completion token and Enterprise token. This request must not be called multiple times for a given Enterprise Token.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_complete_signup_builder()` + `androidenterprise_enterprises_complete_signup_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_complete_signup(
    client: &SimpleHttpClient,
    completionToken: Option<&str>,
    enterpriseToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_complete_signup_builder(
        client,
        completionToken,
        enterpriseToken,
    )?;
    androidenterprise_enterprises_complete_signup_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/createWebToken
/// Returns a unique token to access an embeddable UI. To generate a web UI, pass the generated token into the managed Google Play javascript API. Each token may only be used to start one UI session. See the JavaScript API documentation for further information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_create_web_token_execute()` to send, or `androidenterprise_enterprises_create_web_token` for simplest API.

pub fn androidenterprise_enterprises_create_web_token_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    body: &AdministratorWebTokenSpec,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/createWebToken",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/createWebToken
/// Returns a unique token to access an embeddable UI. To generate a web UI, pass the generated token into the managed Google Play javascript API. Each token may only be used to start one UI session. See the JavaScript API documentation for further information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_enterprises_create_web_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_enterprises_create_web_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_create_web_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdministratorWebToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdministratorWebToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/createWebToken
/// Returns a unique token to access an embeddable UI. To generate a web UI, pass the generated token into the managed Google Play javascript API. Each token may only be used to start one UI session. See the JavaScript API documentation for further information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_create_web_token_builder()` + `androidenterprise_enterprises_create_web_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_create_web_token(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    body: &AdministratorWebTokenSpec,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdministratorWebToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_enterprises_create_web_token_builder(client, enterpriseId, body)?;
    androidenterprise_enterprises_create_web_token_execute(builder)
}

/// GET androidenterprise/v1/enterprises/enroll
/// Enrolls an enterprise with the calling EMM.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_enroll_execute()` to send, or `androidenterprise_enterprises_enroll` for simplest API.

pub fn androidenterprise_enterprises_enroll_builder(
    client: &SimpleHttpClient,
    token: Option<&str>,
    body: &Enterprise,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comandroidenterprise/v1/enterprises/enroll",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "token",
            token
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/enroll
/// Enrolls an enterprise with the calling EMM.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_enterprises_enroll_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_enterprises_enroll_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_enroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Enterprise = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/enroll
/// Enrolls an enterprise with the calling EMM.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_enroll_builder()` + `androidenterprise_enterprises_enroll_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_enroll(
    client: &SimpleHttpClient,
    token: Option<&str>,
    body: &Enterprise,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_enroll_builder(client, token, body)?;
    androidenterprise_enterprises_enroll_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/generateEnterpriseUpgradeUrl
/// Generates an enterprise upgrade URL to upgrade an existing managed Google Play Accounts enterprise to a managed Google domain. See the guide to upgrading an enterprise for more details.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_generate_enterprise_upgrade_url_execute()` to send, or `androidenterprise_enterprises_generate_enterprise_upgrade_url` for simplest API.

pub fn androidenterprise_enterprises_generate_enterprise_upgrade_url_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    adminEmail: Option<&str>,
    allowedDomains: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/generateEnterpriseUpgradeUrl",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "adminEmail",
            adminEmail
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "allowedDomains",
            allowedDomains
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/generateEnterpriseUpgradeUrl
/// Generates an enterprise upgrade URL to upgrade an existing managed Google Play Accounts enterprise to a managed Google domain. See the guide to upgrading an enterprise for more details.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_enterprises_generate_enterprise_upgrade_url_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_enterprises_generate_enterprise_upgrade_url_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_generate_enterprise_upgrade_url_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateEnterpriseUpgradeUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateEnterpriseUpgradeUrlResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/generateEnterpriseUpgradeUrl
/// Generates an enterprise upgrade URL to upgrade an existing managed Google Play Accounts enterprise to a managed Google domain. See the guide to upgrading an enterprise for more details.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_generate_enterprise_upgrade_url_builder()` + `androidenterprise_enterprises_generate_enterprise_upgrade_url_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_generate_enterprise_upgrade_url(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    adminEmail: Option<&str>,
    allowedDomains: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateEnterpriseUpgradeUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_generate_enterprise_upgrade_url_builder(
        client,
        enterpriseId,
        adminEmail,
        allowedDomains,
    )?;
    androidenterprise_enterprises_generate_enterprise_upgrade_url_execute(builder)
}

/// GET androidenterprise/v1/enterprises/signupUrl
/// Generates a sign-up URL.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_generate_signup_url_execute()` to send, or `androidenterprise_enterprises_generate_signup_url` for simplest API.

pub fn androidenterprise_enterprises_generate_signup_url_builder(
    client: &SimpleHttpClient,
    adminEmail: Option<&str>,
    allowedDomains: Option<&str>,
    callbackUrl: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comandroidenterprise/v1/enterprises/signupUrl",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "adminEmail",
            adminEmail
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "allowedDomains",
            allowedDomains
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "callbackUrl",
            callbackUrl
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidenterprise/v1/enterprises/signupUrl
/// Generates a sign-up URL.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_enterprises_generate_signup_url_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_enterprises_generate_signup_url_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_generate_signup_url_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SignupInfo>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SignupInfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/signupUrl
/// Generates a sign-up URL.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_generate_signup_url_builder()` + `androidenterprise_enterprises_generate_signup_url_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_generate_signup_url(
    client: &SimpleHttpClient,
    adminEmail: Option<&str>,
    allowedDomains: Option<&str>,
    callbackUrl: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SignupInfo>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_generate_signup_url_builder(
        client,
        adminEmail,
        allowedDomains,
        callbackUrl,
    )?;
    androidenterprise_enterprises_generate_signup_url_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}
/// Retrieves the name and domain of an enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_get_execute()` to send, or `androidenterprise_enterprises_get` for simplest API.

pub fn androidenterprise_enterprises_get_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}
/// Retrieves the name and domain of an enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_enterprises_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_enterprises_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Enterprise = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}
/// Retrieves the name and domain of an enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_get_builder()` + `androidenterprise_enterprises_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_get(
    client: &SimpleHttpClient,
    enterpriseId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_get_builder(client, enterpriseId)?;
    androidenterprise_enterprises_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccount
/// Returns a service account and credentials. The service account can be bound to the enterprise by calling `setAccount`. The service account is unique to this enterprise and EMM, and will be deleted if the enterprise is unbound. The credentials contain private key data and are not stored server-side. This method can only be called after calling Enterprises.Enroll or Enterprises.CompleteSignup, and before Enterprises.SetAccount; at other times it will return an error. Subsequent calls after the first will generate a new, unique set of credentials, and invalidate the previously generated credentials. Once the service account is bound to the enterprise, it can be managed using the `serviceAccountKeys` resource. *Note:* After you create a key, you might need to wait for 60 seconds or more before you perform another operation with the key. If you try to perform an operation with the key immediately after you create the key, and you receive an error, you can retry the request with exponential backoff .
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_get_service_account_execute()` to send, or `androidenterprise_enterprises_get_service_account` for simplest API.

pub fn androidenterprise_enterprises_get_service_account_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    keyType: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/serviceAccount",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "keyType",
            keyType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccount
/// Returns a service account and credentials. The service account can be bound to the enterprise by calling `setAccount`. The service account is unique to this enterprise and EMM, and will be deleted if the enterprise is unbound. The credentials contain private key data and are not stored server-side. This method can only be called after calling Enterprises.Enroll or Enterprises.CompleteSignup, and before Enterprises.SetAccount; at other times it will return an error. Subsequent calls after the first will generate a new, unique set of credentials, and invalidate the previously generated credentials. Once the service account is bound to the enterprise, it can be managed using the `serviceAccountKeys` resource. *Note:* After you create a key, you might need to wait for 60 seconds or more before you perform another operation with the key. If you try to perform an operation with the key immediately after you create the key, and you receive an error, you can retry the request with exponential backoff .
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_enterprises_get_service_account_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_enterprises_get_service_account_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_get_service_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceAccount>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccount
/// Returns a service account and credentials. The service account can be bound to the enterprise by calling `setAccount`. The service account is unique to this enterprise and EMM, and will be deleted if the enterprise is unbound. The credentials contain private key data and are not stored server-side. This method can only be called after calling Enterprises.Enroll or Enterprises.CompleteSignup, and before Enterprises.SetAccount; at other times it will return an error. Subsequent calls after the first will generate a new, unique set of credentials, and invalidate the previously generated credentials. Once the service account is bound to the enterprise, it can be managed using the `serviceAccountKeys` resource. *Note:* After you create a key, you might need to wait for 60 seconds or more before you perform another operation with the key. If you try to perform an operation with the key immediately after you create the key, and you receive an error, you can retry the request with exponential backoff .
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_get_service_account_builder()` + `androidenterprise_enterprises_get_service_account_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_get_service_account(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    keyType: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceAccount>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_enterprises_get_service_account_builder(client, enterpriseId, keyType)?;
    androidenterprise_enterprises_get_service_account_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout
/// Returns the store layout for the enterprise. If the store layout has not been set, returns "basic" as the store layout type and no homepage.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_get_store_layout_execute()` to send, or `androidenterprise_enterprises_get_store_layout` for simplest API.

pub fn androidenterprise_enterprises_get_store_layout_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/storeLayout",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout
/// Returns the store layout for the enterprise. If the store layout has not been set, returns "basic" as the store layout type and no homepage.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_enterprises_get_store_layout_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_enterprises_get_store_layout_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_get_store_layout_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreLayout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoreLayout = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout
/// Returns the store layout for the enterprise. If the store layout has not been set, returns "basic" as the store layout type and no homepage.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_get_store_layout_builder()` + `androidenterprise_enterprises_get_store_layout_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_get_store_layout(
    client: &SimpleHttpClient,
    enterpriseId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreLayout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_get_store_layout_builder(client, enterpriseId)?;
    androidenterprise_enterprises_get_store_layout_execute(builder)
}

/// GET androidenterprise/v1/enterprises
/// Looks up an enterprise by domain name. This is only supported for enterprises created via the Google-initiated creation flow. Lookup of the id is not needed for enterprises created via the EMM-initiated flow since the EMM learns the enterprise ID in the callback specified in the Enterprises.`generateSignupUrl` call.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_list_execute()` to send, or `androidenterprise_enterprises_list` for simplest API.

pub fn androidenterprise_enterprises_list_builder(
    client: &SimpleHttpClient,
    domain: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comandroidenterprise/v1/enterprises",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "domain",
            domain
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidenterprise/v1/enterprises
/// Looks up an enterprise by domain name. This is only supported for enterprises created via the Google-initiated creation flow. Lookup of the id is not needed for enterprises created via the EMM-initiated flow since the EMM learns the enterprise ID in the callback specified in the Enterprises.`generateSignupUrl` call.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_enterprises_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_enterprises_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnterprisesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EnterprisesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises
/// Looks up an enterprise by domain name. This is only supported for enterprises created via the Google-initiated creation flow. Lookup of the id is not needed for enterprises created via the EMM-initiated flow since the EMM learns the enterprise ID in the callback specified in the Enterprises.`generateSignupUrl` call.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_list_builder()` + `androidenterprise_enterprises_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_list(
    client: &SimpleHttpClient,
    domain: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnterprisesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_list_builder(client, domain)?;
    androidenterprise_enterprises_list_execute(builder)
}

/// GET androidenterprise/v1/enterprises/pullNotificationSet
/// Pulls and returns a notification set for the enterprises associated with the service account authenticated for the request. The notification set may be empty if no notification are pending. A notification set returned needs to be acknowledged within 20 seconds by calling Enterprises.AcknowledgeNotificationSet, unless the notification set is empty. Notifications that are not acknowledged within the 20 seconds will eventually be included again in the response to another PullNotificationSet request, and those that are never acknowledged will ultimately be deleted according to the Google Cloud Platform P`ub/Sub` system policy. Multiple requests might be performed concurrently to retrieve notifications, in which case the pending notifications (if any) will be split among each caller, if any are pending. If no notifications are present, an empty notification list is returned. Subsequent requests may return more notifications once they become available.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_pull_notification_set_execute()` to send, or `androidenterprise_enterprises_pull_notification_set` for simplest API.

pub fn androidenterprise_enterprises_pull_notification_set_builder(
    client: &SimpleHttpClient,
    requestMode: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url =
        format!("https://api.example.comandroidenterprise/v1/enterprises/pullNotificationSet",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestMode",
            requestMode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidenterprise/v1/enterprises/pullNotificationSet
/// Pulls and returns a notification set for the enterprises associated with the service account authenticated for the request. The notification set may be empty if no notification are pending. A notification set returned needs to be acknowledged within 20 seconds by calling Enterprises.AcknowledgeNotificationSet, unless the notification set is empty. Notifications that are not acknowledged within the 20 seconds will eventually be included again in the response to another PullNotificationSet request, and those that are never acknowledged will ultimately be deleted according to the Google Cloud Platform P`ub/Sub` system policy. Multiple requests might be performed concurrently to retrieve notifications, in which case the pending notifications (if any) will be split among each caller, if any are pending. If no notifications are present, an empty notification list is returned. Subsequent requests may return more notifications once they become available.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_enterprises_pull_notification_set_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_enterprises_pull_notification_set_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_pull_notification_set_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationSet>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/pullNotificationSet
/// Pulls and returns a notification set for the enterprises associated with the service account authenticated for the request. The notification set may be empty if no notification are pending. A notification set returned needs to be acknowledged within 20 seconds by calling Enterprises.AcknowledgeNotificationSet, unless the notification set is empty. Notifications that are not acknowledged within the 20 seconds will eventually be included again in the response to another PullNotificationSet request, and those that are never acknowledged will ultimately be deleted according to the Google Cloud Platform P`ub/Sub` system policy. Multiple requests might be performed concurrently to retrieve notifications, in which case the pending notifications (if any) will be split among each caller, if any are pending. If no notifications are present, an empty notification list is returned. Subsequent requests may return more notifications once they become available.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_pull_notification_set_builder()` + `androidenterprise_enterprises_pull_notification_set_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_pull_notification_set(
    client: &SimpleHttpClient,
    requestMode: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationSet>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_pull_notification_set_builder(client, requestMode)?;
    androidenterprise_enterprises_pull_notification_set_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/sendTestPushNotification
/// Sends a test notification to validate the EMM integration with the Google Cloud P`ub/Sub` service for this enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_send_test_push_notification_execute()` to send, or `androidenterprise_enterprises_send_test_push_notification` for simplest API.

pub fn androidenterprise_enterprises_send_test_push_notification_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/sendTestPushNotification",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/sendTestPushNotification
/// Sends a test notification to validate the EMM integration with the Google Cloud P`ub/Sub` service for this enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_enterprises_send_test_push_notification_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_enterprises_send_test_push_notification_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_send_test_push_notification_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EnterprisesSendTestPushNotificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EnterprisesSendTestPushNotificationResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/sendTestPushNotification
/// Sends a test notification to validate the EMM integration with the Google Cloud P`ub/Sub` service for this enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_send_test_push_notification_builder()` + `androidenterprise_enterprises_send_test_push_notification_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_send_test_push_notification(
    client: &SimpleHttpClient,
    enterpriseId: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EnterprisesSendTestPushNotificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_enterprises_send_test_push_notification_builder(client, enterpriseId)?;
    androidenterprise_enterprises_send_test_push_notification_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/account
/// Sets the account that will be used to authenticate to the API as the enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_set_account_execute()` to send, or `androidenterprise_enterprises_set_account` for simplest API.

pub fn androidenterprise_enterprises_set_account_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    body: &EnterpriseAccount,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/account",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/account
/// Sets the account that will be used to authenticate to the API as the enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_enterprises_set_account_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_enterprises_set_account_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_set_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnterpriseAccount>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EnterpriseAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/account
/// Sets the account that will be used to authenticate to the API as the enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_set_account_builder()` + `androidenterprise_enterprises_set_account_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_set_account(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    body: &EnterpriseAccount,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnterpriseAccount>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_set_account_builder(client, enterpriseId, body)?;
    androidenterprise_enterprises_set_account_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout
/// Sets the store layout for the enterprise. By default, `storeLayoutType` is set to "basic" and the basic store layout is enabled. The basic layout only contains apps approved by the admin, and that have been added to the available product set for a user (using the `setAvailableProductSet` call). Apps on the page are sorted in order of their product ID value. If you create a custom store layout (by setting `storeLayoutType` = "custom" and setting a homepage), the basic store layout is disabled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_set_store_layout_execute()` to send, or `androidenterprise_enterprises_set_store_layout` for simplest API.

pub fn androidenterprise_enterprises_set_store_layout_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    body: &StoreLayout,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/storeLayout",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout
/// Sets the store layout for the enterprise. By default, `storeLayoutType` is set to "basic" and the basic store layout is enabled. The basic layout only contains apps approved by the admin, and that have been added to the available product set for a user (using the `setAvailableProductSet` call). Apps on the page are sorted in order of their product ID value. If you create a custom store layout (by setting `storeLayoutType` = "custom" and setting a homepage), the basic store layout is disabled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_enterprises_set_store_layout_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_enterprises_set_store_layout_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_set_store_layout_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreLayout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoreLayout = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout
/// Sets the store layout for the enterprise. By default, `storeLayoutType` is set to "basic" and the basic store layout is enabled. The basic layout only contains apps approved by the admin, and that have been added to the available product set for a user (using the `setAvailableProductSet` call). Apps on the page are sorted in order of their product ID value. If you create a custom store layout (by setting `storeLayoutType` = "custom" and setting a homepage), the basic store layout is disabled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_set_store_layout_builder()` + `androidenterprise_enterprises_set_store_layout_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_set_store_layout(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    body: &StoreLayout,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreLayout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_enterprises_set_store_layout_builder(client, enterpriseId, body)?;
    androidenterprise_enterprises_set_store_layout_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/unenroll
/// Unenrolls an enterprise from the calling EMM.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_unenroll_execute()` to send, or `androidenterprise_enterprises_unenroll` for simplest API.

pub fn androidenterprise_enterprises_unenroll_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/unenroll",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/unenroll
/// Unenrolls an enterprise from the calling EMM.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_enterprises_unenroll_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_enterprises_unenroll_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_unenroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/unenroll
/// Unenrolls an enterprise from the calling EMM.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_unenroll_builder()` + `androidenterprise_enterprises_unenroll_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_unenroll(
    client: &SimpleHttpClient,
    enterpriseId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_unenroll_builder(client, enterpriseId)?;
    androidenterprise_enterprises_unenroll_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Removes an entitlement to an app for a user. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_entitlements_delete_execute()` to send, or `androidenterprise_entitlements_delete` for simplest API.

pub fn androidenterprise_entitlements_delete_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    entitlementId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/entitlements/{}",
        enterpriseId, entitlementId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Removes an entitlement to an app for a user. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_entitlements_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_entitlements_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_entitlements_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Removes an entitlement to an app for a user. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_entitlements_delete_builder()` + `androidenterprise_entitlements_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_entitlements_delete(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    entitlementId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_entitlements_delete_builder(client, enterpriseId, entitlementId, userId)?;
    androidenterprise_entitlements_delete_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Retrieves details of an entitlement. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_entitlements_get_execute()` to send, or `androidenterprise_entitlements_get` for simplest API.

pub fn androidenterprise_entitlements_get_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    entitlementId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/entitlements/{}",
        enterpriseId, entitlementId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Retrieves details of an entitlement. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_entitlements_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_entitlements_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_entitlements_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entitlement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Entitlement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Retrieves details of an entitlement. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_entitlements_get_builder()` + `androidenterprise_entitlements_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_entitlements_get(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    entitlementId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entitlement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_entitlements_get_builder(client, enterpriseId, entitlementId, userId)?;
    androidenterprise_entitlements_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements
/// Lists all entitlements for the specified user. Only the ID is set. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_entitlements_list_execute()` to send, or `androidenterprise_entitlements_list` for simplest API.

pub fn androidenterprise_entitlements_list_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/entitlements",
        enterpriseId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements
/// Lists all entitlements for the specified user. Only the ID is set. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_entitlements_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_entitlements_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_entitlements_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntitlementsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntitlementsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements
/// Lists all entitlements for the specified user. Only the ID is set. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_entitlements_list_builder()` + `androidenterprise_entitlements_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_entitlements_list(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntitlementsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_entitlements_list_builder(client, enterpriseId, userId)?;
    androidenterprise_entitlements_list_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Adds or updates an entitlement to an app for a user. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_entitlements_update_execute()` to send, or `androidenterprise_entitlements_update` for simplest API.

pub fn androidenterprise_entitlements_update_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    entitlementId: &str,
    userId: &str,
    install: Option<bool>,
    body: &Entitlement,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/entitlements/{}",
        enterpriseId, entitlementId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "install",
            install
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Adds or updates an entitlement to an app for a user. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_entitlements_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_entitlements_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_entitlements_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entitlement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Entitlement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Adds or updates an entitlement to an app for a user. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_entitlements_update_builder()` + `androidenterprise_entitlements_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_entitlements_update(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    entitlementId: &str,
    userId: &str,
    install: Option<bool>,
    body: &Entitlement,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entitlement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_entitlements_update_builder(
        client,
        enterpriseId,
        entitlementId,
        userId,
        install,
        body,
    )?;
    androidenterprise_entitlements_update_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses/{groupLicenseId}
/// Retrieves details of an enterprise's group license for a product. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_grouplicenses_get_execute()` to send, or `androidenterprise_grouplicenses_get` for simplest API.

pub fn androidenterprise_grouplicenses_get_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    groupLicenseId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/groupLicenses/{}",
        enterpriseId, groupLicenseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses/{groupLicenseId}
/// Retrieves details of an enterprise's group license for a product. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_grouplicenses_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_grouplicenses_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_grouplicenses_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupLicense>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupLicense = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses/{groupLicenseId}
/// Retrieves details of an enterprise's group license for a product. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_grouplicenses_get_builder()` + `androidenterprise_grouplicenses_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_grouplicenses_get(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    groupLicenseId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupLicense>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_grouplicenses_get_builder(client, enterpriseId, groupLicenseId)?;
    androidenterprise_grouplicenses_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses
/// Retrieves IDs of all products for which the enterprise has a group license. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_grouplicenses_list_execute()` to send, or `androidenterprise_grouplicenses_list` for simplest API.

pub fn androidenterprise_grouplicenses_list_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/groupLicenses",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses
/// Retrieves IDs of all products for which the enterprise has a group license. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_grouplicenses_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_grouplicenses_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_grouplicenses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupLicensesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupLicensesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses
/// Retrieves IDs of all products for which the enterprise has a group license. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_grouplicenses_list_builder()` + `androidenterprise_grouplicenses_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_grouplicenses_list(
    client: &SimpleHttpClient,
    enterpriseId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupLicensesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_grouplicenses_list_builder(client, enterpriseId)?;
    androidenterprise_grouplicenses_list_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses/{groupLicenseId}/users
/// Retrieves the IDs of the users who have been granted entitlements under the license. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_grouplicenseusers_list_execute()` to send, or `androidenterprise_grouplicenseusers_list` for simplest API.

pub fn androidenterprise_grouplicenseusers_list_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    groupLicenseId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/groupLicenses/{}/users",
        enterpriseId, groupLicenseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses/{groupLicenseId}/users
/// Retrieves the IDs of the users who have been granted entitlements under the license. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_grouplicenseusers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_grouplicenseusers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_grouplicenseusers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GroupLicenseUsersListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupLicenseUsersListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses/{groupLicenseId}/users
/// Retrieves the IDs of the users who have been granted entitlements under the license. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_grouplicenseusers_list_builder()` + `androidenterprise_grouplicenseusers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_grouplicenseusers_list(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    groupLicenseId: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GroupLicenseUsersListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_grouplicenseusers_list_builder(client, enterpriseId, groupLicenseId)?;
    androidenterprise_grouplicenseusers_list_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Requests to remove an app from a device. A call to get or list will still show the app as installed on the device until it is actually removed. A successful response indicates that a removal request has been sent to the device. The call will be considered successful even if the app is not present on the device (e.g. it was never installed, or was removed by the user).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_installs_delete_execute()` to send, or `androidenterprise_installs_delete` for simplest API.

pub fn androidenterprise_installs_delete_builder(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    installId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/devices/{}/installs/{}",
        deviceId,
        enterpriseId,
        installId,
        userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Requests to remove an app from a device. A call to get or list will still show the app as installed on the device until it is actually removed. A successful response indicates that a removal request has been sent to the device. The call will be considered successful even if the app is not present on the device (e.g. it was never installed, or was removed by the user).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_installs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_installs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_installs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Requests to remove an app from a device. A call to get or list will still show the app as installed on the device until it is actually removed. A successful response indicates that a removal request has been sent to the device. The call will be considered successful even if the app is not present on the device (e.g. it was never installed, or was removed by the user).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_installs_delete_builder()` + `androidenterprise_installs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_installs_delete(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    installId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_installs_delete_builder(
        client,
        deviceId,
        enterpriseId,
        installId,
        userId,
    )?;
    androidenterprise_installs_delete_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Retrieves details of an installation of an app on a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_installs_get_execute()` to send, or `androidenterprise_installs_get` for simplest API.

pub fn androidenterprise_installs_get_builder(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    installId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/devices/{}/installs/{}",
        deviceId,
        enterpriseId,
        installId,
        userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Retrieves details of an installation of an app on a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_installs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_installs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_installs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Install>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Install = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Retrieves details of an installation of an app on a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_installs_get_builder()` + `androidenterprise_installs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_installs_get(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    installId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Install>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_installs_get_builder(client, deviceId, enterpriseId, installId, userId)?;
    androidenterprise_installs_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs
/// Retrieves the details of all apps installed on the specified device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_installs_list_execute()` to send, or `androidenterprise_installs_list` for simplest API.

pub fn androidenterprise_installs_list_builder(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/devices/{}/installs",
        deviceId, enterpriseId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs
/// Retrieves the details of all apps installed on the specified device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_installs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_installs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_installs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstallsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstallsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs
/// Retrieves the details of all apps installed on the specified device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_installs_list_builder()` + `androidenterprise_installs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_installs_list(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstallsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_installs_list_builder(client, deviceId, enterpriseId, userId)?;
    androidenterprise_installs_list_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Requests to install the latest version of an app to a device. If the app is already installed, then it is updated to the latest version if necessary.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_installs_update_execute()` to send, or `androidenterprise_installs_update` for simplest API.

pub fn androidenterprise_installs_update_builder(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    installId: &str,
    userId: &str,
    body: &Install,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/devices/{}/installs/{}",
        deviceId,
        enterpriseId,
        installId,
        userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Requests to install the latest version of an app to a device. If the app is already installed, then it is updated to the latest version if necessary.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_installs_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_installs_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_installs_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Install>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Install = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Requests to install the latest version of an app to a device. If the app is already installed, then it is updated to the latest version if necessary.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_installs_update_builder()` + `androidenterprise_installs_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_installs_update(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    installId: &str,
    userId: &str,
    body: &Install,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Install>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_installs_update_builder(
        client,
        deviceId,
        enterpriseId,
        installId,
        userId,
        body,
    )?;
    androidenterprise_installs_update_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Removes a per-device managed configuration for an app for the specified device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationsfordevice_delete_execute()` to send, or `androidenterprise_managedconfigurationsfordevice_delete` for simplest API.

pub fn androidenterprise_managedconfigurationsfordevice_delete_builder(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    managedConfigurationForDeviceId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/devices/{}/managedConfigurationsForDevice/{}",
        deviceId,
        enterpriseId,
        managedConfigurationForDeviceId,
        userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Removes a per-device managed configuration for an app for the specified device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_managedconfigurationsfordevice_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_managedconfigurationsfordevice_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsfordevice_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Removes a per-device managed configuration for an app for the specified device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationsfordevice_delete_builder()` + `androidenterprise_managedconfigurationsfordevice_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsfordevice_delete(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    managedConfigurationForDeviceId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_managedconfigurationsfordevice_delete_builder(
        client,
        deviceId,
        enterpriseId,
        managedConfigurationForDeviceId,
        userId,
    )?;
    androidenterprise_managedconfigurationsfordevice_delete_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Retrieves details of a per-device managed configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationsfordevice_get_execute()` to send, or `androidenterprise_managedconfigurationsfordevice_get` for simplest API.

pub fn androidenterprise_managedconfigurationsfordevice_get_builder(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    managedConfigurationForDeviceId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/devices/{}/managedConfigurationsForDevice/{}",
        deviceId,
        enterpriseId,
        managedConfigurationForDeviceId,
        userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Retrieves details of a per-device managed configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_managedconfigurationsfordevice_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_managedconfigurationsfordevice_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsfordevice_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Retrieves details of a per-device managed configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationsfordevice_get_builder()` + `androidenterprise_managedconfigurationsfordevice_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsfordevice_get(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    managedConfigurationForDeviceId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_managedconfigurationsfordevice_get_builder(
        client,
        deviceId,
        enterpriseId,
        managedConfigurationForDeviceId,
        userId,
    )?;
    androidenterprise_managedconfigurationsfordevice_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice
/// Lists all the per-device managed configurations for the specified device. Only the ID is set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationsfordevice_list_execute()` to send, or `androidenterprise_managedconfigurationsfordevice_list` for simplest API.

pub fn androidenterprise_managedconfigurationsfordevice_list_builder(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/devices/{}/managedConfigurationsForDevice",
        deviceId,
        enterpriseId,
        userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice
/// Lists all the per-device managed configurations for the specified device. Only the ID is set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_managedconfigurationsfordevice_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_managedconfigurationsfordevice_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsfordevice_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ManagedConfigurationsForDeviceListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedConfigurationsForDeviceListResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice
/// Lists all the per-device managed configurations for the specified device. Only the ID is set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationsfordevice_list_builder()` + `androidenterprise_managedconfigurationsfordevice_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsfordevice_list(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ManagedConfigurationsForDeviceListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_managedconfigurationsfordevice_list_builder(
        client,
        deviceId,
        enterpriseId,
        userId,
    )?;
    androidenterprise_managedconfigurationsfordevice_list_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Adds or updates a per-device managed configuration for an app for the specified device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationsfordevice_update_execute()` to send, or `androidenterprise_managedconfigurationsfordevice_update` for simplest API.

pub fn androidenterprise_managedconfigurationsfordevice_update_builder(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    managedConfigurationForDeviceId: &str,
    userId: &str,
    body: &ManagedConfiguration,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/devices/{}/managedConfigurationsForDevice/{}",
        deviceId,
        enterpriseId,
        managedConfigurationForDeviceId,
        userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Adds or updates a per-device managed configuration for an app for the specified device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_managedconfigurationsfordevice_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_managedconfigurationsfordevice_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsfordevice_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Adds or updates a per-device managed configuration for an app for the specified device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationsfordevice_update_builder()` + `androidenterprise_managedconfigurationsfordevice_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsfordevice_update(
    client: &SimpleHttpClient,
    deviceId: &str,
    enterpriseId: &str,
    managedConfigurationForDeviceId: &str,
    userId: &str,
    body: &ManagedConfiguration,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_managedconfigurationsfordevice_update_builder(
        client,
        deviceId,
        enterpriseId,
        managedConfigurationForDeviceId,
        userId,
        body,
    )?;
    androidenterprise_managedconfigurationsfordevice_update_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Removes a per-user managed configuration for an app for the specified user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationsforuser_delete_execute()` to send, or `androidenterprise_managedconfigurationsforuser_delete` for simplest API.

pub fn androidenterprise_managedconfigurationsforuser_delete_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    managedConfigurationForUserId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/managedConfigurationsForUser/{}",
        enterpriseId,
        managedConfigurationForUserId,
        userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Removes a per-user managed configuration for an app for the specified user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_managedconfigurationsforuser_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_managedconfigurationsforuser_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsforuser_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Removes a per-user managed configuration for an app for the specified user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationsforuser_delete_builder()` + `androidenterprise_managedconfigurationsforuser_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsforuser_delete(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    managedConfigurationForUserId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_managedconfigurationsforuser_delete_builder(
        client,
        enterpriseId,
        managedConfigurationForUserId,
        userId,
    )?;
    androidenterprise_managedconfigurationsforuser_delete_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Retrieves details of a per-user managed configuration for an app for the specified user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationsforuser_get_execute()` to send, or `androidenterprise_managedconfigurationsforuser_get` for simplest API.

pub fn androidenterprise_managedconfigurationsforuser_get_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    managedConfigurationForUserId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/managedConfigurationsForUser/{}",
        enterpriseId,
        managedConfigurationForUserId,
        userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Retrieves details of a per-user managed configuration for an app for the specified user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_managedconfigurationsforuser_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_managedconfigurationsforuser_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsforuser_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Retrieves details of a per-user managed configuration for an app for the specified user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationsforuser_get_builder()` + `androidenterprise_managedconfigurationsforuser_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsforuser_get(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    managedConfigurationForUserId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_managedconfigurationsforuser_get_builder(
        client,
        enterpriseId,
        managedConfigurationForUserId,
        userId,
    )?;
    androidenterprise_managedconfigurationsforuser_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser
/// Lists all the per-user managed configurations for the specified user. Only the ID is set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationsforuser_list_execute()` to send, or `androidenterprise_managedconfigurationsforuser_list` for simplest API.

pub fn androidenterprise_managedconfigurationsforuser_list_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/managedConfigurationsForUser",
        enterpriseId,
        userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser
/// Lists all the per-user managed configurations for the specified user. Only the ID is set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_managedconfigurationsforuser_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_managedconfigurationsforuser_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsforuser_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ManagedConfigurationsForUserListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedConfigurationsForUserListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser
/// Lists all the per-user managed configurations for the specified user. Only the ID is set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationsforuser_list_builder()` + `androidenterprise_managedconfigurationsforuser_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsforuser_list(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ManagedConfigurationsForUserListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_managedconfigurationsforuser_list_builder(client, enterpriseId, userId)?;
    androidenterprise_managedconfigurationsforuser_list_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Adds or updates the managed configuration settings for an app for the specified user. If you support the Managed configurations iframe, you can apply managed configurations to a user by specifying an `mcmId` and its associated configuration variables (if any) in the request. Alternatively, all EMMs can apply managed configurations by passing a list of managed properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationsforuser_update_execute()` to send, or `androidenterprise_managedconfigurationsforuser_update` for simplest API.

pub fn androidenterprise_managedconfigurationsforuser_update_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    managedConfigurationForUserId: &str,
    userId: &str,
    body: &ManagedConfiguration,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/managedConfigurationsForUser/{}",
        enterpriseId,
        managedConfigurationForUserId,
        userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Adds or updates the managed configuration settings for an app for the specified user. If you support the Managed configurations iframe, you can apply managed configurations to a user by specifying an `mcmId` and its associated configuration variables (if any) in the request. Alternatively, all EMMs can apply managed configurations by passing a list of managed properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_managedconfigurationsforuser_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_managedconfigurationsforuser_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsforuser_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Adds or updates the managed configuration settings for an app for the specified user. If you support the Managed configurations iframe, you can apply managed configurations to a user by specifying an `mcmId` and its associated configuration variables (if any) in the request. Alternatively, all EMMs can apply managed configurations by passing a list of managed properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationsforuser_update_builder()` + `androidenterprise_managedconfigurationsforuser_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsforuser_update(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    managedConfigurationForUserId: &str,
    userId: &str,
    body: &ManagedConfiguration,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_managedconfigurationsforuser_update_builder(
        client,
        enterpriseId,
        managedConfigurationForUserId,
        userId,
        body,
    )?;
    androidenterprise_managedconfigurationsforuser_update_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/managedConfigurationsSettings
/// Lists all the managed configurations settings for the specified app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationssettings_list_execute()` to send, or `androidenterprise_managedconfigurationssettings_list` for simplest API.

pub fn androidenterprise_managedconfigurationssettings_list_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    productId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/products/{}/managedConfigurationsSettings",
        enterpriseId,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/managedConfigurationsSettings
/// Lists all the managed configurations settings for the specified app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_managedconfigurationssettings_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_managedconfigurationssettings_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationssettings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ManagedConfigurationsSettingsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedConfigurationsSettingsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/managedConfigurationsSettings
/// Lists all the managed configurations settings for the specified app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationssettings_list_builder()` + `androidenterprise_managedconfigurationssettings_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationssettings_list(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    productId: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ManagedConfigurationsSettingsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_managedconfigurationssettings_list_builder(
        client,
        enterpriseId,
        productId,
    )?;
    androidenterprise_managedconfigurationssettings_list_execute(builder)
}

/// GET androidenterprise/v1/permissions/{permissionId}
/// Retrieves details of an Android app permission for display to an enterprise admin.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_permissions_get_execute()` to send, or `androidenterprise_permissions_get` for simplest API.

pub fn androidenterprise_permissions_get_builder(
    client: &SimpleHttpClient,
    permissionId: &str,
    language: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/permissions/{}",
        permissionId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "language",
            language
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidenterprise/v1/permissions/{permissionId}
/// Retrieves details of an Android app permission for display to an enterprise admin.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_permissions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_permissions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_permissions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Permission>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Permission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/permissions/{permissionId}
/// Retrieves details of an Android app permission for display to an enterprise admin.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_permissions_get_builder()` + `androidenterprise_permissions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_permissions_get(
    client: &SimpleHttpClient,
    permissionId: &str,
    language: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Permission>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_permissions_get_builder(client, permissionId, language)?;
    androidenterprise_permissions_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/approve
/// Approves the specified product and the relevant app permissions, if any. The maximum number of products that you can approve per enterprise customer is 1,000. To learn how to use managed Google Play to design and create a store layout to display approved products to your users, see Store Layout Design. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_products_approve_execute()` to send, or `androidenterprise_products_approve` for simplest API.

pub fn androidenterprise_products_approve_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    productId: &str,
    body: &ProductsApproveRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/products/{}/approve",
        enterpriseId, productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/approve
/// Approves the specified product and the relevant app permissions, if any. The maximum number of products that you can approve per enterprise customer is 1,000. To learn how to use managed Google Play to design and create a store layout to display approved products to your users, see Store Layout Design. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_products_approve_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_products_approve_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_approve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/approve
/// Approves the specified product and the relevant app permissions, if any. The maximum number of products that you can approve per enterprise customer is 1,000. To learn how to use managed Google Play to design and create a store layout to display approved products to your users, see Store Layout Design. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_products_approve_builder()` + `androidenterprise_products_approve_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_approve(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    productId: &str,
    body: &ProductsApproveRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_products_approve_builder(client, enterpriseId, productId, body)?;
    androidenterprise_products_approve_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/generateApprovalUrl
/// Generates a URL that can be rendered in an iframe to display the permissions (if any) of a product. An enterprise admin must view these permissions and accept them on behalf of their organization in order to approve that product. Admins should accept the displayed permissions by interacting with a separate UI element in the EMM console, which in turn should trigger the use of this URL as the `approvalUrlInfo`.`approvalUrl` property in a Products.approve call to approve the product. This URL can only be used to display permissions for up to 1 day. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_products_generate_approval_url_execute()` to send, or `androidenterprise_products_generate_approval_url` for simplest API.

pub fn androidenterprise_products_generate_approval_url_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    productId: &str,
    languageCode: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/products/{}/generateApprovalUrl",
        enterpriseId,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "languageCode",
            languageCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/generateApprovalUrl
/// Generates a URL that can be rendered in an iframe to display the permissions (if any) of a product. An enterprise admin must view these permissions and accept them on behalf of their organization in order to approve that product. Admins should accept the displayed permissions by interacting with a separate UI element in the EMM console, which in turn should trigger the use of this URL as the `approvalUrlInfo`.`approvalUrl` property in a Products.approve call to approve the product. This URL can only be used to display permissions for up to 1 day. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_products_generate_approval_url_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_products_generate_approval_url_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_generate_approval_url_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ProductsGenerateApprovalUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductsGenerateApprovalUrlResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/generateApprovalUrl
/// Generates a URL that can be rendered in an iframe to display the permissions (if any) of a product. An enterprise admin must view these permissions and accept them on behalf of their organization in order to approve that product. Admins should accept the displayed permissions by interacting with a separate UI element in the EMM console, which in turn should trigger the use of this URL as the `approvalUrlInfo`.`approvalUrl` property in a Products.approve call to approve the product. This URL can only be used to display permissions for up to 1 day. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_products_generate_approval_url_builder()` + `androidenterprise_products_generate_approval_url_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_generate_approval_url(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    productId: &str,
    languageCode: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ProductsGenerateApprovalUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_products_generate_approval_url_builder(
        client,
        enterpriseId,
        productId,
        languageCode,
    )?;
    androidenterprise_products_generate_approval_url_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}
/// Retrieves details of a product for display to an enterprise admin.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_products_get_execute()` to send, or `androidenterprise_products_get` for simplest API.

pub fn androidenterprise_products_get_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    productId: &str,
    language: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/products/{}",
        enterpriseId, productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "language",
            language
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}
/// Retrieves details of a product for display to an enterprise admin.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_products_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_products_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Product = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}
/// Retrieves details of a product for display to an enterprise admin.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_products_get_builder()` + `androidenterprise_products_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_get(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    productId: &str,
    language: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_products_get_builder(client, enterpriseId, productId, language)?;
    androidenterprise_products_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/appRestrictionsSchema
/// Retrieves the schema that defines the configurable properties for this product. All products have a schema, but this schema may be empty if no managed configurations have been defined. This schema can be used to populate a UI that allows an admin to configure the product. To apply a managed configuration based on the schema obtained using this API, see Managed Configurations through Play.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_products_get_app_restrictions_schema_execute()` to send, or `androidenterprise_products_get_app_restrictions_schema` for simplest API.

pub fn androidenterprise_products_get_app_restrictions_schema_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    productId: &str,
    language: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/products/{}/appRestrictionsSchema",
        enterpriseId,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "language",
            language
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/appRestrictionsSchema
/// Retrieves the schema that defines the configurable properties for this product. All products have a schema, but this schema may be empty if no managed configurations have been defined. This schema can be used to populate a UI that allows an admin to configure the product. To apply a managed configuration based on the schema obtained using this API, see Managed Configurations through Play.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_products_get_app_restrictions_schema_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_products_get_app_restrictions_schema_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_get_app_restrictions_schema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppRestrictionsSchema>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppRestrictionsSchema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/appRestrictionsSchema
/// Retrieves the schema that defines the configurable properties for this product. All products have a schema, but this schema may be empty if no managed configurations have been defined. This schema can be used to populate a UI that allows an admin to configure the product. To apply a managed configuration based on the schema obtained using this API, see Managed Configurations through Play.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_products_get_app_restrictions_schema_builder()` + `androidenterprise_products_get_app_restrictions_schema_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_get_app_restrictions_schema(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    productId: &str,
    language: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppRestrictionsSchema>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_products_get_app_restrictions_schema_builder(
        client,
        enterpriseId,
        productId,
        language,
    )?;
    androidenterprise_products_get_app_restrictions_schema_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/permissions
/// Retrieves the Android app permissions required by this app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_products_get_permissions_execute()` to send, or `androidenterprise_products_get_permissions` for simplest API.

pub fn androidenterprise_products_get_permissions_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    productId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/products/{}/permissions",
        enterpriseId, productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/permissions
/// Retrieves the Android app permissions required by this app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_products_get_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_products_get_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_get_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductPermissions>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductPermissions = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/permissions
/// Retrieves the Android app permissions required by this app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_products_get_permissions_builder()` + `androidenterprise_products_get_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_get_permissions(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    productId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductPermissions>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_products_get_permissions_builder(client, enterpriseId, productId)?;
    androidenterprise_products_get_permissions_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products
/// Finds approved products that match a query, or all approved products if there is no query. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_products_list_execute()` to send, or `androidenterprise_products_list` for simplest API.

pub fn androidenterprise_products_list_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    approved: Option<bool>,
    language: Option<&str>,
    maxResults: Option<i32>,
    query: Option<&str>,
    token: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/products",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "approved",
            approved
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "language",
            language
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "query",
            query
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "token",
            token
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products
/// Finds approved products that match a query, or all approved products if there is no query. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_products_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_products_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products
/// Finds approved products that match a query, or all approved products if there is no query. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_products_list_builder()` + `androidenterprise_products_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_list(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    approved: Option<bool>,
    language: Option<&str>,
    maxResults: Option<i32>,
    query: Option<&str>,
    token: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_products_list_builder(
        client,
        enterpriseId,
        approved,
        language,
        maxResults,
        query,
        token,
    )?;
    androidenterprise_products_list_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/unapprove
/// Unapproves the specified product (and the relevant app permissions, if any) **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_products_unapprove_execute()` to send, or `androidenterprise_products_unapprove` for simplest API.

pub fn androidenterprise_products_unapprove_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    productId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/products/{}/unapprove",
        enterpriseId, productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/unapprove
/// Unapproves the specified product (and the relevant app permissions, if any) **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_products_unapprove_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_products_unapprove_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_unapprove_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/unapprove
/// Unapproves the specified product (and the relevant app permissions, if any) **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_products_unapprove_builder()` + `androidenterprise_products_unapprove_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_unapprove(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    productId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_products_unapprove_builder(client, enterpriseId, productId)?;
    androidenterprise_products_unapprove_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys/{keyId}
/// Removes and invalidates the specified credentials for the service account associated with this enterprise. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_serviceaccountkeys_delete_execute()` to send, or `androidenterprise_serviceaccountkeys_delete` for simplest API.

pub fn androidenterprise_serviceaccountkeys_delete_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    keyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/serviceAccountKeys/{}",
        enterpriseId, keyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys/{keyId}
/// Removes and invalidates the specified credentials for the service account associated with this enterprise. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_serviceaccountkeys_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_serviceaccountkeys_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_serviceaccountkeys_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys/{keyId}
/// Removes and invalidates the specified credentials for the service account associated with this enterprise. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_serviceaccountkeys_delete_builder()` + `androidenterprise_serviceaccountkeys_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_serviceaccountkeys_delete(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    keyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_serviceaccountkeys_delete_builder(client, enterpriseId, keyId)?;
    androidenterprise_serviceaccountkeys_delete_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys
/// Generates new credentials for the service account associated with this enterprise. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount. Only the type of the key should be populated in the resource to be inserted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_serviceaccountkeys_insert_execute()` to send, or `androidenterprise_serviceaccountkeys_insert` for simplest API.

pub fn androidenterprise_serviceaccountkeys_insert_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    body: &ServiceAccountKey,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/serviceAccountKeys",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys
/// Generates new credentials for the service account associated with this enterprise. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount. Only the type of the key should be populated in the resource to be inserted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_serviceaccountkeys_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_serviceaccountkeys_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_serviceaccountkeys_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceAccountKey>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceAccountKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys
/// Generates new credentials for the service account associated with this enterprise. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount. Only the type of the key should be populated in the resource to be inserted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_serviceaccountkeys_insert_builder()` + `androidenterprise_serviceaccountkeys_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_serviceaccountkeys_insert(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    body: &ServiceAccountKey,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceAccountKey>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_serviceaccountkeys_insert_builder(client, enterpriseId, body)?;
    androidenterprise_serviceaccountkeys_insert_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys
/// Lists all active credentials for the service account associated with this enterprise. Only the ID and key type are returned. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_serviceaccountkeys_list_execute()` to send, or `androidenterprise_serviceaccountkeys_list` for simplest API.

pub fn androidenterprise_serviceaccountkeys_list_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/serviceAccountKeys",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys
/// Lists all active credentials for the service account associated with this enterprise. Only the ID and key type are returned. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_serviceaccountkeys_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_serviceaccountkeys_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_serviceaccountkeys_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ServiceAccountKeysListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceAccountKeysListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys
/// Lists all active credentials for the service account associated with this enterprise. Only the ID and key type are returned. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_serviceaccountkeys_list_builder()` + `androidenterprise_serviceaccountkeys_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_serviceaccountkeys_list(
    client: &SimpleHttpClient,
    enterpriseId: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ServiceAccountKeysListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_serviceaccountkeys_list_builder(client, enterpriseId)?;
    androidenterprise_serviceaccountkeys_list_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Deletes a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutclusters_delete_execute()` to send, or `androidenterprise_storelayoutclusters_delete` for simplest API.

pub fn androidenterprise_storelayoutclusters_delete_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    enterpriseId: &str,
    pageId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/storeLayout/pages/{}/clusters/{}",
        clusterId,
        enterpriseId,
        pageId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Deletes a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_storelayoutclusters_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_storelayoutclusters_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Deletes a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutclusters_delete_builder()` + `androidenterprise_storelayoutclusters_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_delete(
    client: &SimpleHttpClient,
    clusterId: &str,
    enterpriseId: &str,
    pageId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_storelayoutclusters_delete_builder(
        client,
        clusterId,
        enterpriseId,
        pageId,
    )?;
    androidenterprise_storelayoutclusters_delete_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Retrieves details of a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutclusters_get_execute()` to send, or `androidenterprise_storelayoutclusters_get` for simplest API.

pub fn androidenterprise_storelayoutclusters_get_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    enterpriseId: &str,
    pageId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/storeLayout/pages/{}/clusters/{}",
        clusterId,
        enterpriseId,
        pageId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Retrieves details of a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_storelayoutclusters_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_storelayoutclusters_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoreCluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Retrieves details of a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutclusters_get_builder()` + `androidenterprise_storelayoutclusters_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_get(
    client: &SimpleHttpClient,
    clusterId: &str,
    enterpriseId: &str,
    pageId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_storelayoutclusters_get_builder(client, clusterId, enterpriseId, pageId)?;
    androidenterprise_storelayoutclusters_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters
/// Inserts a new cluster in a page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutclusters_insert_execute()` to send, or `androidenterprise_storelayoutclusters_insert` for simplest API.

pub fn androidenterprise_storelayoutclusters_insert_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    pageId: &str,
    body: &StoreCluster,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/storeLayout/pages/{}/clusters",
        enterpriseId, pageId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters
/// Inserts a new cluster in a page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_storelayoutclusters_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_storelayoutclusters_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoreCluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters
/// Inserts a new cluster in a page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutclusters_insert_builder()` + `androidenterprise_storelayoutclusters_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_insert(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    pageId: &str,
    body: &StoreCluster,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_storelayoutclusters_insert_builder(client, enterpriseId, pageId, body)?;
    androidenterprise_storelayoutclusters_insert_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters
/// Retrieves the details of all clusters on the specified page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutclusters_list_execute()` to send, or `androidenterprise_storelayoutclusters_list` for simplest API.

pub fn androidenterprise_storelayoutclusters_list_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    pageId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/storeLayout/pages/{}/clusters",
        enterpriseId, pageId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters
/// Retrieves the details of all clusters on the specified page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_storelayoutclusters_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_storelayoutclusters_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StoreLayoutClustersListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoreLayoutClustersListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters
/// Retrieves the details of all clusters on the specified page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutclusters_list_builder()` + `androidenterprise_storelayoutclusters_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_list(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    pageId: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StoreLayoutClustersListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_storelayoutclusters_list_builder(client, enterpriseId, pageId)?;
    androidenterprise_storelayoutclusters_list_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Updates a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutclusters_update_execute()` to send, or `androidenterprise_storelayoutclusters_update` for simplest API.

pub fn androidenterprise_storelayoutclusters_update_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    enterpriseId: &str,
    pageId: &str,
    body: &StoreCluster,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/storeLayout/pages/{}/clusters/{}",
        clusterId,
        enterpriseId,
        pageId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Updates a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_storelayoutclusters_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_storelayoutclusters_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoreCluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Updates a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutclusters_update_builder()` + `androidenterprise_storelayoutclusters_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_update(
    client: &SimpleHttpClient,
    clusterId: &str,
    enterpriseId: &str,
    pageId: &str,
    body: &StoreCluster,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_storelayoutclusters_update_builder(
        client,
        clusterId,
        enterpriseId,
        pageId,
        body,
    )?;
    androidenterprise_storelayoutclusters_update_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Deletes a store page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutpages_delete_execute()` to send, or `androidenterprise_storelayoutpages_delete` for simplest API.

pub fn androidenterprise_storelayoutpages_delete_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    pageId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/storeLayout/pages/{}",
        enterpriseId, pageId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Deletes a store page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_storelayoutpages_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_storelayoutpages_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Deletes a store page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutpages_delete_builder()` + `androidenterprise_storelayoutpages_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_delete(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    pageId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_storelayoutpages_delete_builder(client, enterpriseId, pageId)?;
    androidenterprise_storelayoutpages_delete_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Retrieves details of a store page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutpages_get_execute()` to send, or `androidenterprise_storelayoutpages_get` for simplest API.

pub fn androidenterprise_storelayoutpages_get_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    pageId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/storeLayout/pages/{}",
        enterpriseId, pageId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Retrieves details of a store page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_storelayoutpages_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_storelayoutpages_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StorePage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StorePage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Retrieves details of a store page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutpages_get_builder()` + `androidenterprise_storelayoutpages_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_get(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    pageId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StorePage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_storelayoutpages_get_builder(client, enterpriseId, pageId)?;
    androidenterprise_storelayoutpages_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages
/// Inserts a new store page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutpages_insert_execute()` to send, or `androidenterprise_storelayoutpages_insert` for simplest API.

pub fn androidenterprise_storelayoutpages_insert_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    body: &StorePage,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/storeLayout/pages",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages
/// Inserts a new store page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_storelayoutpages_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_storelayoutpages_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StorePage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StorePage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages
/// Inserts a new store page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutpages_insert_builder()` + `androidenterprise_storelayoutpages_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_insert(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    body: &StorePage,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StorePage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_storelayoutpages_insert_builder(client, enterpriseId, body)?;
    androidenterprise_storelayoutpages_insert_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages
/// Retrieves the details of all pages in the store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutpages_list_execute()` to send, or `androidenterprise_storelayoutpages_list` for simplest API.

pub fn androidenterprise_storelayoutpages_list_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/storeLayout/pages",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages
/// Retrieves the details of all pages in the store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_storelayoutpages_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_storelayoutpages_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StoreLayoutPagesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoreLayoutPagesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages
/// Retrieves the details of all pages in the store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutpages_list_builder()` + `androidenterprise_storelayoutpages_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_list(
    client: &SimpleHttpClient,
    enterpriseId: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StoreLayoutPagesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_storelayoutpages_list_builder(client, enterpriseId)?;
    androidenterprise_storelayoutpages_list_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Updates the content of a store page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutpages_update_execute()` to send, or `androidenterprise_storelayoutpages_update` for simplest API.

pub fn androidenterprise_storelayoutpages_update_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    pageId: &str,
    body: &StorePage,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/storeLayout/pages/{}",
        enterpriseId, pageId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Updates the content of a store page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_storelayoutpages_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_storelayoutpages_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StorePage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StorePage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Updates the content of a store page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutpages_update_builder()` + `androidenterprise_storelayoutpages_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_update(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    pageId: &str,
    body: &StorePage,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StorePage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_storelayoutpages_update_builder(client, enterpriseId, pageId, body)?;
    androidenterprise_storelayoutpages_update_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Deleted an EMM-managed user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_delete_execute()` to send, or `androidenterprise_users_delete` for simplest API.

pub fn androidenterprise_users_delete_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}",
        enterpriseId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Deleted an EMM-managed user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_users_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_users_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Deleted an EMM-managed user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_delete_builder()` + `androidenterprise_users_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_delete(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_users_delete_builder(client, enterpriseId, userId)?;
    androidenterprise_users_delete_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/authenticationToken
/// Generates an authentication token which the device policy client can use to provision the given EMM-managed user account on a device. The generated token is single-use and expires after a few minutes. You can provision a maximum of 10 devices per user. This call only works with EMM-managed accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_generate_authentication_token_execute()` to send, or `androidenterprise_users_generate_authentication_token` for simplest API.

pub fn androidenterprise_users_generate_authentication_token_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/authenticationToken",
        enterpriseId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/authenticationToken
/// Generates an authentication token which the device policy client can use to provision the given EMM-managed user account on a device. The generated token is single-use and expires after a few minutes. You can provision a maximum of 10 devices per user. This call only works with EMM-managed accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_users_generate_authentication_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_users_generate_authentication_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_generate_authentication_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthenticationToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuthenticationToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/authenticationToken
/// Generates an authentication token which the device policy client can use to provision the given EMM-managed user account on a device. The generated token is single-use and expires after a few minutes. You can provision a maximum of 10 devices per user. This call only works with EMM-managed accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_generate_authentication_token_builder()` + `androidenterprise_users_generate_authentication_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_generate_authentication_token(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthenticationToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_users_generate_authentication_token_builder(
        client,
        enterpriseId,
        userId,
    )?;
    androidenterprise_users_generate_authentication_token_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Retrieves a user's details.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_get_execute()` to send, or `androidenterprise_users_get` for simplest API.

pub fn androidenterprise_users_get_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}",
        enterpriseId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Retrieves a user's details.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_users_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_users_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: User = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Retrieves a user's details.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_get_builder()` + `androidenterprise_users_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_get(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_users_get_builder(client, enterpriseId, userId)?;
    androidenterprise_users_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/availableProductSet
/// Retrieves the set of products a user is entitled to access. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_get_available_product_set_execute()` to send, or `androidenterprise_users_get_available_product_set` for simplest API.

pub fn androidenterprise_users_get_available_product_set_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/availableProductSet",
        enterpriseId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/availableProductSet
/// Retrieves the set of products a user is entitled to access. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_users_get_available_product_set_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_users_get_available_product_set_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_get_available_product_set_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/availableProductSet
/// Retrieves the set of products a user is entitled to access. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_get_available_product_set_builder()` + `androidenterprise_users_get_available_product_set_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_get_available_product_set(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_users_get_available_product_set_builder(client, enterpriseId, userId)?;
    androidenterprise_users_get_available_product_set_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users
/// Creates a new EMM-managed user. The Users resource passed in the body of the request should include an `accountIdentifier` and an `accountType`. If a corresponding user already exists with the same account identifier, the user will be updated with the resource. In this case only the `displayName` field can be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_insert_execute()` to send, or `androidenterprise_users_insert` for simplest API.

pub fn androidenterprise_users_insert_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    body: &User,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users
/// Creates a new EMM-managed user. The Users resource passed in the body of the request should include an `accountIdentifier` and an `accountType`. If a corresponding user already exists with the same account identifier, the user will be updated with the resource. In this case only the `displayName` field can be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_users_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_users_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: User = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users
/// Creates a new EMM-managed user. The Users resource passed in the body of the request should include an `accountIdentifier` and an `accountType`. If a corresponding user already exists with the same account identifier, the user will be updated with the resource. In this case only the `displayName` field can be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_insert_builder()` + `androidenterprise_users_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_insert(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    body: &User,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_users_insert_builder(client, enterpriseId, body)?;
    androidenterprise_users_insert_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users
/// Looks up a user by primary email address. This is only supported for Google-managed users. Lookup of the id is not needed for EMM-managed users because the id is already returned in the result of the Users.insert call.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_list_execute()` to send, or `androidenterprise_users_list` for simplest API.

pub fn androidenterprise_users_list_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    email: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "email",
            email
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users
/// Looks up a user by primary email address. This is only supported for Google-managed users. Lookup of the id is not needed for EMM-managed users because the id is already returned in the result of the Users.insert call.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_users_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_users_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsersListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UsersListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users
/// Looks up a user by primary email address. This is only supported for Google-managed users. Lookup of the id is not needed for EMM-managed users because the id is already returned in the result of the Users.insert call.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_list_builder()` + `androidenterprise_users_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_list(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    email: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsersListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_users_list_builder(client, enterpriseId, email)?;
    androidenterprise_users_list_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/deviceAccess
/// Revokes access to all devices currently provisioned to the user. The user will no longer be able to use the managed Play store on any of their managed devices. This call only works with EMM-managed accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_revoke_device_access_execute()` to send, or `androidenterprise_users_revoke_device_access` for simplest API.

pub fn androidenterprise_users_revoke_device_access_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/deviceAccess",
        enterpriseId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/deviceAccess
/// Revokes access to all devices currently provisioned to the user. The user will no longer be able to use the managed Play store on any of their managed devices. This call only works with EMM-managed accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_users_revoke_device_access_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_users_revoke_device_access_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_revoke_device_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/deviceAccess
/// Revokes access to all devices currently provisioned to the user. The user will no longer be able to use the managed Play store on any of their managed devices. This call only works with EMM-managed accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_revoke_device_access_builder()` + `androidenterprise_users_revoke_device_access_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_revoke_device_access(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_users_revoke_device_access_builder(client, enterpriseId, userId)?;
    androidenterprise_users_revoke_device_access_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/availableProductSet
/// Modifies the set of products that a user is entitled to access (referred to as *whitelisted* products). Only products that are approved or products that were previously approved (products with revoked approval) can be whitelisted. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_set_available_product_set_execute()` to send, or `androidenterprise_users_set_available_product_set` for simplest API.

pub fn androidenterprise_users_set_available_product_set_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
    body: &ProductSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}/availableProductSet",
        enterpriseId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/availableProductSet
/// Modifies the set of products that a user is entitled to access (referred to as *whitelisted* products). Only products that are approved or products that were previously approved (products with revoked approval) can be whitelisted. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_users_set_available_product_set_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_users_set_available_product_set_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_set_available_product_set_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/availableProductSet
/// Modifies the set of products that a user is entitled to access (referred to as *whitelisted* products). Only products that are approved or products that were previously approved (products with revoked approval) can be whitelisted. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_set_available_product_set_builder()` + `androidenterprise_users_set_available_product_set_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_set_available_product_set(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
    body: &ProductSet,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_users_set_available_product_set_builder(
        client,
        enterpriseId,
        userId,
        body,
    )?;
    androidenterprise_users_set_available_product_set_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Updates the details of an EMM-managed user. Can be used with EMM-managed users only (not Google managed users). Pass the new details in the Users resource in the request body. Only the `displayName` field can be changed. Other fields must either be unset or have the currently active value.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_update_execute()` to send, or `androidenterprise_users_update` for simplest API.

pub fn androidenterprise_users_update_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
    body: &User,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/users/{}",
        enterpriseId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Updates the details of an EMM-managed user. Can be used with EMM-managed users only (not Google managed users). Pass the new details in the Users resource in the request body. Only the `displayName` field can be changed. Other fields must either be unset or have the currently active value.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_users_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_users_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: User = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Updates the details of an EMM-managed user. Can be used with EMM-managed users only (not Google managed users). Pass the new details in the Users resource in the request body. Only the `displayName` field can be changed. Other fields must either be unset or have the currently active value.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_update_builder()` + `androidenterprise_users_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_update(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    userId: &str,
    body: &User,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_users_update_builder(client, enterpriseId, userId, body)?;
    androidenterprise_users_update_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Deletes an existing web app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_webapps_delete_execute()` to send, or `androidenterprise_webapps_delete` for simplest API.

pub fn androidenterprise_webapps_delete_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    webAppId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/webApps/{}",
        enterpriseId, webAppId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Deletes an existing web app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_webapps_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_webapps_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Deletes an existing web app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_webapps_delete_builder()` + `androidenterprise_webapps_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_delete(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    webAppId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_webapps_delete_builder(client, enterpriseId, webAppId)?;
    androidenterprise_webapps_delete_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Gets an existing web app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_webapps_get_execute()` to send, or `androidenterprise_webapps_get` for simplest API.

pub fn androidenterprise_webapps_get_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    webAppId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/webApps/{}",
        enterpriseId, webAppId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Gets an existing web app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_webapps_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_webapps_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Gets an existing web app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_webapps_get_builder()` + `androidenterprise_webapps_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_get(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    webAppId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_webapps_get_builder(client, enterpriseId, webAppId)?;
    androidenterprise_webapps_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps
/// Creates a new web app for the enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_webapps_insert_execute()` to send, or `androidenterprise_webapps_insert` for simplest API.

pub fn androidenterprise_webapps_insert_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    body: &WebApp,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/webApps",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps
/// Creates a new web app for the enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_webapps_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_webapps_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps
/// Creates a new web app for the enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_webapps_insert_builder()` + `androidenterprise_webapps_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_insert(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    body: &WebApp,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_webapps_insert_builder(client, enterpriseId, body)?;
    androidenterprise_webapps_insert_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps
/// Retrieves the details of all web apps for a given enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_webapps_list_execute()` to send, or `androidenterprise_webapps_list` for simplest API.

pub fn androidenterprise_webapps_list_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/webApps",
        enterpriseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps
/// Retrieves the details of all web apps for a given enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_webapps_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_webapps_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebAppsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebAppsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps
/// Retrieves the details of all web apps for a given enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_webapps_list_builder()` + `androidenterprise_webapps_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_list(
    client: &SimpleHttpClient,
    enterpriseId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebAppsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_webapps_list_builder(client, enterpriseId)?;
    androidenterprise_webapps_list_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Updates an existing web app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_webapps_update_execute()` to send, or `androidenterprise_webapps_update` for simplest API.

pub fn androidenterprise_webapps_update_builder(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    webAppId: &str,
    body: &WebApp,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidenterprise/v1/enterprises/{}/webApps/{}",
        enterpriseId, webAppId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Updates an existing web app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidenterprise_webapps_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidenterprise_webapps_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Updates an existing web app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_webapps_update_builder()` + `androidenterprise_webapps_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_update(
    client: &SimpleHttpClient,
    enterpriseId: &str,
    webAppId: &str,
    body: &WebApp,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_webapps_update_builder(client, enterpriseId, webAppId, body)?;
    androidenterprise_webapps_update_execute(builder)
}
