//! Auto-generated API clients for gcp/androidenterprise.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/forceReportUpload
/// Uploads a report containing any changes in app states on the device since the last report was generated. You can call this method up to 3 times every 24 hours for a given device. If you exceed the quota, then the Google Play EMM API returns HTTP 429 Too Many Requests.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_devices_force_report_upload_execute()` to send, or `androidenterprise_devices_force_report_upload` for simplest API.

pub fn androidenterprise_devices_force_report_upload_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    deviceId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/devices/{}/forceReportUpload",
        enterpriseId,
        userId,
        deviceId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/forceReportUpload
/// Uploads a report containing any changes in app states on the device since the last report was generated. You can call this method up to 3 times every 24 hours for a given device. If you exceed the quota, then the Google Play EMM API returns HTTP 429 Too Many Requests.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_devices_force_report_upload_execute()` or `androidenterprise_devices_force_report_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_devices_force_report_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_force_report_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/forceReportUpload
/// Uploads a report containing any changes in app states on the device since the last report was generated. You can call this method up to 3 times every 24 hours for a given device. If you exceed the quota, then the Google Play EMM API returns HTTP 429 Too Many Requests.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_devices_force_report_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_devices_force_report_upload_task()`.
/// For the simplest API, use `androidenterprise_devices_force_report_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_devices_force_report_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_devices_force_report_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_devices_force_report_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_devices_force_report_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseDevicesForceReportUploadArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: deviceId
    pub deviceId: String,
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/forceReportUpload
/// Uploads a report containing any changes in app states on the device since the last report was generated. You can call this method up to 3 times every 24 hours for a given device. If you exceed the quota, then the Google Play EMM API returns HTTP 429 Too Many Requests.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_devices_force_report_upload_builder()` + `androidenterprise_devices_force_report_upload_execute()`.
/// For task-level control, use `androidenterprise_devices_force_report_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_force_report_upload(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseDevicesForceReportUploadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_devices_force_report_upload_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.deviceId,
    )?;
    androidenterprise_devices_force_report_upload_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}
/// Retrieves the details of a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_devices_get_execute()` to send, or `androidenterprise_devices_get` for simplest API.

pub fn androidenterprise_devices_get_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    deviceId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/devices/{}",
        enterpriseId,
        userId,
        deviceId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}
/// Retrieves the details of a device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_devices_get_execute()` or `androidenterprise_devices_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_devices_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Device>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Device = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}
/// Retrieves the details of a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_devices_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_devices_get_task()`.
/// For the simplest API, use `androidenterprise_devices_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_devices_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_devices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Device>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_devices_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_devices_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseDevicesGetArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: deviceId
    pub deviceId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}
/// Retrieves the details of a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_devices_get_builder()` + `androidenterprise_devices_get_execute()`.
/// For task-level control, use `androidenterprise_devices_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_get(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseDevicesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Device>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_devices_get_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.deviceId,
    )?;
    androidenterprise_devices_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/state
/// Checks if a device can access Google apps and services for a user. Returns whether access is "enabled" or "disabled". A "disabled" state prevents the user''s Managed Google Account on the device from successfully authenticating with Google. This blocks access to most Google applications and services, including Google Play, as the device cannot prove its entitlement to access them. New devices default to "disabled". Important: Enforcement of this state depends on the following conditions: * The user must be a managed google account. * The enterprise must be a managed google domain. * Third-party Android mobile management must be active in the Google Admin Console for the user''s Organizational Unit. If these conditions aren''t met, access may still be possible even in a "disabled" state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_devices_get_state_execute()` to send, or `androidenterprise_devices_get_state` for simplest API.

pub fn androidenterprise_devices_get_state_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    deviceId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/devices/{}/state",
        enterpriseId,
        userId,
        deviceId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/state
/// Checks if a device can access Google apps and services for a user. Returns whether access is "enabled" or "disabled". A "disabled" state prevents the user''s Managed Google Account on the device from successfully authenticating with Google. This blocks access to most Google applications and services, including Google Play, as the device cannot prove its entitlement to access them. New devices default to "disabled". Important: Enforcement of this state depends on the following conditions: * The user must be a managed google account. * The enterprise must be a managed google domain. * Third-party Android mobile management must be active in the Google Admin Console for the user''s Organizational Unit. If these conditions aren''t met, access may still be possible even in a "disabled" state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_devices_get_state_execute()` or `androidenterprise_devices_get_state`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_devices_get_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_get_state_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeviceState>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeviceState = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/state
/// Checks if a device can access Google apps and services for a user. Returns whether access is "enabled" or "disabled". A "disabled" state prevents the user''s Managed Google Account on the device from successfully authenticating with Google. This blocks access to most Google applications and services, including Google Play, as the device cannot prove its entitlement to access them. New devices default to "disabled". Important: Enforcement of this state depends on the following conditions: * The user must be a managed google account. * The enterprise must be a managed google domain. * Third-party Android mobile management must be active in the Google Admin Console for the user''s Organizational Unit. If these conditions aren''t met, access may still be possible even in a "disabled" state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_devices_get_state_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_devices_get_state_task()`.
/// For the simplest API, use `androidenterprise_devices_get_state()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_devices_get_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_devices_get_state_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceState>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_devices_get_state_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_devices_get_state`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseDevicesGetStateArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: deviceId
    pub deviceId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/state
/// Checks if a device can access Google apps and services for a user. Returns whether access is "enabled" or "disabled". A "disabled" state prevents the user''s Managed Google Account on the device from successfully authenticating with Google. This blocks access to most Google applications and services, including Google Play, as the device cannot prove its entitlement to access them. New devices default to "disabled". Important: Enforcement of this state depends on the following conditions: * The user must be a managed google account. * The enterprise must be a managed google domain. * Third-party Android mobile management must be active in the Google Admin Console for the user''s Organizational Unit. If these conditions aren''t met, access may still be possible even in a "disabled" state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_devices_get_state_builder()` + `androidenterprise_devices_get_state_execute()`.
/// For task-level control, use `androidenterprise_devices_get_state_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_get_state(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseDevicesGetStateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceState>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_devices_get_state_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.deviceId,
    )?;
    androidenterprise_devices_get_state_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices
/// Retrieves the IDs of all of a user''s devices.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_devices_list_execute()` to send, or `androidenterprise_devices_list` for simplest API.

pub fn androidenterprise_devices_list_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/devices",
        enterpriseId,
        userId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices
/// Retrieves the IDs of all of a user''s devices.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_devices_list_execute()` or `androidenterprise_devices_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DevicesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DevicesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices
/// Retrieves the IDs of all of a user''s devices.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_devices_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_devices_list_task()`.
/// For the simplest API, use `androidenterprise_devices_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DevicesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_devices_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_devices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseDevicesListArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices
/// Retrieves the IDs of all of a user''s devices.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_devices_list_builder()` + `androidenterprise_devices_list_execute()`.
/// For task-level control, use `androidenterprise_devices_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_list(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseDevicesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DevicesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_devices_list_builder(client, &args.enterpriseId, &args.userId)?;
    androidenterprise_devices_list_execute(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/state
/// Sets whether a device''s access to Google services (including Google Play) is enabled or disabled for the specified user. Setting the state to "enabled" allows the Google Account to access Google services, while "disabled" blocks access by preventing OAuth token issuance. Preconditions for Enforcement: 1. This setting is only effective for Google-managed users. 2. The enterprise must be linked to a Google Managed Domain. 3. Enforcement requires third-party Android mobile management to be enabled within the Google Admin Console for the user''s Organizational Unit. If these preconditions are not met, changes to this state may be ignored.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_devices_set_state_execute()` to send, or `androidenterprise_devices_set_state` for simplest API.

pub fn androidenterprise_devices_set_state_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    deviceId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/devices/{}/state",
        enterpriseId,
        userId,
        deviceId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/state
/// Sets whether a device''s access to Google services (including Google Play) is enabled or disabled for the specified user. Setting the state to "enabled" allows the Google Account to access Google services, while "disabled" blocks access by preventing OAuth token issuance. Preconditions for Enforcement: 1. This setting is only effective for Google-managed users. 2. The enterprise must be linked to a Google Managed Domain. 3. Enforcement requires third-party Android mobile management to be enabled within the Google Admin Console for the user''s Organizational Unit. If these preconditions are not met, changes to this state may be ignored.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_devices_set_state_execute()` or `androidenterprise_devices_set_state`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_devices_set_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_set_state_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeviceState>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeviceState = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/state
/// Sets whether a device''s access to Google services (including Google Play) is enabled or disabled for the specified user. Setting the state to "enabled" allows the Google Account to access Google services, while "disabled" blocks access by preventing OAuth token issuance. Preconditions for Enforcement: 1. This setting is only effective for Google-managed users. 2. The enterprise must be linked to a Google Managed Domain. 3. Enforcement requires third-party Android mobile management to be enabled within the Google Admin Console for the user''s Organizational Unit. If these preconditions are not met, changes to this state may be ignored.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_devices_set_state_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_devices_set_state_task()`.
/// For the simplest API, use `androidenterprise_devices_set_state()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_devices_set_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_devices_set_state_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceState>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_devices_set_state_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_devices_set_state`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseDevicesSetStateArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: deviceId
    pub deviceId: String,
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/state
/// Sets whether a device''s access to Google services (including Google Play) is enabled or disabled for the specified user. Setting the state to "enabled" allows the Google Account to access Google services, while "disabled" blocks access by preventing OAuth token issuance. Preconditions for Enforcement: 1. This setting is only effective for Google-managed users. 2. The enterprise must be linked to a Google Managed Domain. 3. Enforcement requires third-party Android mobile management to be enabled within the Google Admin Console for the user''s Organizational Unit. If these preconditions are not met, changes to this state may be ignored.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_devices_set_state_builder()` + `androidenterprise_devices_set_state_execute()`.
/// For task-level control, use `androidenterprise_devices_set_state_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_set_state(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseDevicesSetStateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceState>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_devices_set_state_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.deviceId,
    )?;
    androidenterprise_devices_set_state_execute(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}
/// Updates the device policy. To ensure the policy is properly enforced, you need to prevent unmanaged accounts from accessing Google Play by setting the allowed_accounts in the managed configuration for the Google Play package. See restrict accounts in Google Play. When provisioning a new device, you should set the device policy using this method before adding the managed Google Play Account to the device, otherwise the policy will not be applied for a short period of time after adding the account to the device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_devices_update_execute()` to send, or `androidenterprise_devices_update` for simplest API.

pub fn androidenterprise_devices_update_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    deviceId: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/devices/{}",
        enterpriseId,
        userId,
        deviceId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}
/// Updates the device policy. To ensure the policy is properly enforced, you need to prevent unmanaged accounts from accessing Google Play by setting the allowed_accounts in the managed configuration for the Google Play package. See restrict accounts in Google Play. When provisioning a new device, you should set the device policy using this method before adding the managed Google Play Account to the device, otherwise the policy will not be applied for a short period of time after adding the account to the device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_devices_update_execute()` or `androidenterprise_devices_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_devices_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Device>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Device = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}
/// Updates the device policy. To ensure the policy is properly enforced, you need to prevent unmanaged accounts from accessing Google Play by setting the allowed_accounts in the managed configuration for the Google Play package. See restrict accounts in Google Play. When provisioning a new device, you should set the device policy using this method before adding the managed Google Play Account to the device, otherwise the policy will not be applied for a short period of time after adding the account to the device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_devices_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_devices_update_task()`.
/// For the simplest API, use `androidenterprise_devices_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_devices_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_devices_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Device>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_devices_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_devices_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseDevicesUpdateArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: deviceId
    pub deviceId: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}
/// Updates the device policy. To ensure the policy is properly enforced, you need to prevent unmanaged accounts from accessing Google Play by setting the allowed_accounts in the managed configuration for the Google Play package. See restrict accounts in Google Play. When provisioning a new device, you should set the device policy using this method before adding the managed Google Play Account to the device, otherwise the policy will not be applied for a short period of time after adding the account to the device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_devices_update_builder()` + `androidenterprise_devices_update_execute()`.
/// For task-level control, use `androidenterprise_devices_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_devices_update(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseDevicesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Device>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_devices_update_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.deviceId,
        &args.updateMask,
    )?;
    androidenterprise_devices_update_execute(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/enrollmentTokens
/// Returns a token for device enrollment. The DPC can encode this token within the QR/NFC/zero-touch enrollment payload or fetch it before calling the on-device API to authenticate the user. The token can be generated for each device or reused across multiple devices.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enrollment_tokens_create_execute()` to send, or `androidenterprise_enrollment_tokens_create` for simplest API.

pub fn androidenterprise_enrollment_tokens_create_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/enrollmentTokens",
        enterpriseId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/enrollmentTokens
/// Returns a token for device enrollment. The DPC can encode this token within the QR/NFC/zero-touch enrollment payload or fetch it before calling the on-device API to authenticate the user. The token can be generated for each device or reused across multiple devices.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_enrollment_tokens_create_execute()` or `androidenterprise_enrollment_tokens_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enrollment_tokens_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enrollment_tokens_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EnrollmentToken>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EnrollmentToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/enrollmentTokens
/// Returns a token for device enrollment. The DPC can encode this token within the QR/NFC/zero-touch enrollment payload or fetch it before calling the on-device API to authenticate the user. The token can be generated for each device or reused across multiple devices.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_enrollment_tokens_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_enrollment_tokens_create_task()`.
/// For the simplest API, use `androidenterprise_enrollment_tokens_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enrollment_tokens_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_enrollment_tokens_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnrollmentToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_enrollment_tokens_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_enrollment_tokens_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEnrollmentTokensCreateArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/enrollmentTokens
/// Returns a token for device enrollment. The DPC can encode this token within the QR/NFC/zero-touch enrollment payload or fetch it before calling the on-device API to authenticate the user. The token can be generated for each device or reused across multiple devices.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enrollment_tokens_create_builder()` + `androidenterprise_enrollment_tokens_create_execute()`.
/// For task-level control, use `androidenterprise_enrollment_tokens_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enrollment_tokens_create(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEnrollmentTokensCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnrollmentToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_enrollment_tokens_create_builder(client, &args.enterpriseId)?;
    androidenterprise_enrollment_tokens_create_execute(builder)
}

/// POST androidenterprise/v1/enterprises/acknowledgeNotificationSet
/// Acknowledges notifications that were received from Enterprises.PullNotificationSet to prevent subsequent calls from returning the same notifications.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_acknowledge_notification_set_execute()` to send, or `androidenterprise_enterprises_acknowledge_notification_set` for simplest API.

pub fn androidenterprise_enterprises_acknowledge_notification_set_builder<R>(
    client: &SimpleHttpClient<R>,
    notificationSetId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/acknowledgeNotificationSet",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = notificationSetId.as_ref() {
        query_parts.push(format!("notificationSetId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/acknowledgeNotificationSet
/// Acknowledges notifications that were received from Enterprises.PullNotificationSet to prevent subsequent calls from returning the same notifications.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_enterprises_acknowledge_notification_set_execute()` or `androidenterprise_enterprises_acknowledge_notification_set`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_acknowledge_notification_set_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_acknowledge_notification_set_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/acknowledgeNotificationSet
/// Acknowledges notifications that were received from Enterprises.PullNotificationSet to prevent subsequent calls from returning the same notifications.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_enterprises_acknowledge_notification_set_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_enterprises_acknowledge_notification_set_task()`.
/// For the simplest API, use `androidenterprise_enterprises_acknowledge_notification_set()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_acknowledge_notification_set_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_enterprises_acknowledge_notification_set_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_enterprises_acknowledge_notification_set_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_enterprises_acknowledge_notification_set`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEnterprisesAcknowledgeNotificationSetArgs {
    /// Query parameter: notificationSetId
    pub notificationSetId: Option<String>,
}

/// POST androidenterprise/v1/enterprises/acknowledgeNotificationSet
/// Acknowledges notifications that were received from Enterprises.PullNotificationSet to prevent subsequent calls from returning the same notifications.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_acknowledge_notification_set_builder()` + `androidenterprise_enterprises_acknowledge_notification_set_execute()`.
/// For task-level control, use `androidenterprise_enterprises_acknowledge_notification_set_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_acknowledge_notification_set(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEnterprisesAcknowledgeNotificationSetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_acknowledge_notification_set_builder(
        client,
        &args.notificationSetId,
    )?;
    androidenterprise_enterprises_acknowledge_notification_set_execute(builder)
}

/// POST androidenterprise/v1/enterprises/completeSignup
/// Completes the signup flow, by specifying the Completion token and Enterprise token. This request must not be called multiple times for a given Enterprise Token.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_complete_signup_execute()` to send, or `androidenterprise_enterprises_complete_signup` for simplest API.

pub fn androidenterprise_enterprises_complete_signup_builder<R>(
    client: &SimpleHttpClient<R>,
    completionToken: &Option<String>,
    enterpriseToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/completeSignup",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = completionToken.as_ref() {
        query_parts.push(format!("completionToken={}", val));
    }
    if let Some(val) = enterpriseToken.as_ref() {
        query_parts.push(format!("enterpriseToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/completeSignup
/// Completes the signup flow, by specifying the Completion token and Enterprise token. This request must not be called multiple times for a given Enterprise Token.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_enterprises_complete_signup_execute()` or `androidenterprise_enterprises_complete_signup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_complete_signup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_complete_signup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Enterprise>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Enterprise = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/completeSignup
/// Completes the signup flow, by specifying the Completion token and Enterprise token. This request must not be called multiple times for a given Enterprise Token.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_enterprises_complete_signup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_enterprises_complete_signup_task()`.
/// For the simplest API, use `androidenterprise_enterprises_complete_signup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_complete_signup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_enterprises_complete_signup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_enterprises_complete_signup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_enterprises_complete_signup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEnterprisesCompleteSignupArgs {
    /// Query parameter: completionToken
    pub completionToken: Option<String>,
    /// Query parameter: enterpriseToken
    pub enterpriseToken: Option<String>,
}

/// POST androidenterprise/v1/enterprises/completeSignup
/// Completes the signup flow, by specifying the Completion token and Enterprise token. This request must not be called multiple times for a given Enterprise Token.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_complete_signup_builder()` + `androidenterprise_enterprises_complete_signup_execute()`.
/// For task-level control, use `androidenterprise_enterprises_complete_signup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_complete_signup(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEnterprisesCompleteSignupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_complete_signup_builder(
        client,
        &args.completionToken,
        &args.enterpriseToken,
    )?;
    androidenterprise_enterprises_complete_signup_execute(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/createWebToken
/// Returns a unique token to access an embeddable UI. To generate a web UI, pass the generated token into the managed Google Play javascript API. Each token may only be used to start one UI session. See the JavaScript API documentation for further information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_create_web_token_execute()` to send, or `androidenterprise_enterprises_create_web_token` for simplest API.

pub fn androidenterprise_enterprises_create_web_token_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/createWebToken",
        enterpriseId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/createWebToken
/// Returns a unique token to access an embeddable UI. To generate a web UI, pass the generated token into the managed Google Play javascript API. Each token may only be used to start one UI session. See the JavaScript API documentation for further information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_enterprises_create_web_token_execute()` or `androidenterprise_enterprises_create_web_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_create_web_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_create_web_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdministratorWebToken>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdministratorWebToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/createWebToken
/// Returns a unique token to access an embeddable UI. To generate a web UI, pass the generated token into the managed Google Play javascript API. Each token may only be used to start one UI session. See the JavaScript API documentation for further information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_enterprises_create_web_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_enterprises_create_web_token_task()`.
/// For the simplest API, use `androidenterprise_enterprises_create_web_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_create_web_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_enterprises_create_web_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdministratorWebToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_enterprises_create_web_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_enterprises_create_web_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEnterprisesCreateWebTokenArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/createWebToken
/// Returns a unique token to access an embeddable UI. To generate a web UI, pass the generated token into the managed Google Play javascript API. Each token may only be used to start one UI session. See the JavaScript API documentation for further information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_create_web_token_builder()` + `androidenterprise_enterprises_create_web_token_execute()`.
/// For task-level control, use `androidenterprise_enterprises_create_web_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_create_web_token(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEnterprisesCreateWebTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdministratorWebToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_enterprises_create_web_token_builder(client, &args.enterpriseId)?;
    androidenterprise_enterprises_create_web_token_execute(builder)
}

/// POST androidenterprise/v1/enterprises/enroll
/// Enrolls an enterprise with the calling EMM.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_enroll_execute()` to send, or `androidenterprise_enterprises_enroll` for simplest API.

pub fn androidenterprise_enterprises_enroll_builder<R>(
    client: &SimpleHttpClient<R>,
    token: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/enroll",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = token.as_ref() {
        query_parts.push(format!("token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/enroll
/// Enrolls an enterprise with the calling EMM.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_enterprises_enroll_execute()` or `androidenterprise_enterprises_enroll`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_enroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_enroll_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Enterprise>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Enterprise = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/enroll
/// Enrolls an enterprise with the calling EMM.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_enterprises_enroll_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_enterprises_enroll_task()`.
/// For the simplest API, use `androidenterprise_enterprises_enroll()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_enroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_enterprises_enroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_enterprises_enroll_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_enterprises_enroll`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEnterprisesEnrollArgs {
    /// Query parameter: token
    pub token: Option<String>,
}

/// POST androidenterprise/v1/enterprises/enroll
/// Enrolls an enterprise with the calling EMM.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_enroll_builder()` + `androidenterprise_enterprises_enroll_execute()`.
/// For task-level control, use `androidenterprise_enterprises_enroll_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_enroll(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEnterprisesEnrollArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_enroll_builder(client, &args.token)?;
    androidenterprise_enterprises_enroll_execute(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/generateEnterpriseUpgradeUrl
/// Generates an enterprise upgrade URL to upgrade an existing managed Google Play Accounts enterprise to a managed Google domain. See the guide to upgrading an enterprise for more details.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_generate_enterprise_upgrade_url_execute()` to send, or `androidenterprise_enterprises_generate_enterprise_upgrade_url` for simplest API.

pub fn androidenterprise_enterprises_generate_enterprise_upgrade_url_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    adminEmail: &Option<String>,
    allowedDomains: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/generateEnterpriseUpgradeUrl",
        enterpriseId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = adminEmail.as_ref() {
        query_parts.push(format!("adminEmail={}", val));
    }
    if let Some(val) = allowedDomains.as_ref() {
        query_parts.push(format!("allowedDomains={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/generateEnterpriseUpgradeUrl
/// Generates an enterprise upgrade URL to upgrade an existing managed Google Play Accounts enterprise to a managed Google domain. See the guide to upgrading an enterprise for more details.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_enterprises_generate_enterprise_upgrade_url_execute()` or `androidenterprise_enterprises_generate_enterprise_upgrade_url`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_generate_enterprise_upgrade_url_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_generate_enterprise_upgrade_url_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenerateEnterpriseUpgradeUrlResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateEnterpriseUpgradeUrlResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/generateEnterpriseUpgradeUrl
/// Generates an enterprise upgrade URL to upgrade an existing managed Google Play Accounts enterprise to a managed Google domain. See the guide to upgrading an enterprise for more details.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_enterprises_generate_enterprise_upgrade_url_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_enterprises_generate_enterprise_upgrade_url_task()`.
/// For the simplest API, use `androidenterprise_enterprises_generate_enterprise_upgrade_url()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_generate_enterprise_upgrade_url_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_enterprises_generate_enterprise_upgrade_url_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateEnterpriseUpgradeUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_enterprises_generate_enterprise_upgrade_url_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_enterprises_generate_enterprise_upgrade_url`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEnterprisesGenerateEnterpriseUpgradeUrlArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Query parameter: adminEmail
    pub adminEmail: Option<String>,
    /// Query parameter: allowedDomains
    pub allowedDomains: Option<String>,
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/generateEnterpriseUpgradeUrl
/// Generates an enterprise upgrade URL to upgrade an existing managed Google Play Accounts enterprise to a managed Google domain. See the guide to upgrading an enterprise for more details.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_generate_enterprise_upgrade_url_builder()` + `androidenterprise_enterprises_generate_enterprise_upgrade_url_execute()`.
/// For task-level control, use `androidenterprise_enterprises_generate_enterprise_upgrade_url_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_generate_enterprise_upgrade_url(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEnterprisesGenerateEnterpriseUpgradeUrlArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateEnterpriseUpgradeUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_generate_enterprise_upgrade_url_builder(
        client,
        &args.enterpriseId,
        &args.adminEmail,
        &args.allowedDomains,
    )?;
    androidenterprise_enterprises_generate_enterprise_upgrade_url_execute(builder)
}

/// POST androidenterprise/v1/enterprises/signupUrl
/// Generates a sign-up URL.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_generate_signup_url_execute()` to send, or `androidenterprise_enterprises_generate_signup_url` for simplest API.

pub fn androidenterprise_enterprises_generate_signup_url_builder<R>(
    client: &SimpleHttpClient<R>,
    adminEmail: &Option<String>,
    allowedDomains: &Option<String>,
    callbackUrl: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/signupUrl",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = adminEmail.as_ref() {
        query_parts.push(format!("adminEmail={}", val));
    }
    if let Some(val) = allowedDomains.as_ref() {
        query_parts.push(format!("allowedDomains={}", val));
    }
    if let Some(val) = callbackUrl.as_ref() {
        query_parts.push(format!("callbackUrl={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/signupUrl
/// Generates a sign-up URL.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_enterprises_generate_signup_url_execute()` or `androidenterprise_enterprises_generate_signup_url`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_generate_signup_url_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_generate_signup_url_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SignupInfo>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SignupInfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/signupUrl
/// Generates a sign-up URL.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_enterprises_generate_signup_url_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_enterprises_generate_signup_url_task()`.
/// For the simplest API, use `androidenterprise_enterprises_generate_signup_url()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_generate_signup_url_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_enterprises_generate_signup_url_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SignupInfo>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_enterprises_generate_signup_url_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_enterprises_generate_signup_url`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEnterprisesGenerateSignupUrlArgs {
    /// Query parameter: adminEmail
    pub adminEmail: Option<String>,
    /// Query parameter: allowedDomains
    pub allowedDomains: Option<String>,
    /// Query parameter: callbackUrl
    pub callbackUrl: Option<String>,
}

/// POST androidenterprise/v1/enterprises/signupUrl
/// Generates a sign-up URL.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_generate_signup_url_builder()` + `androidenterprise_enterprises_generate_signup_url_execute()`.
/// For task-level control, use `androidenterprise_enterprises_generate_signup_url_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_generate_signup_url(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEnterprisesGenerateSignupUrlArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SignupInfo>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_generate_signup_url_builder(
        client,
        &args.adminEmail,
        &args.allowedDomains,
        &args.callbackUrl,
    )?;
    androidenterprise_enterprises_generate_signup_url_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}
/// Retrieves the name and domain of an enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_get_execute()` to send, or `androidenterprise_enterprises_get` for simplest API.

pub fn androidenterprise_enterprises_get_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}",
        enterpriseId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}
/// Retrieves the name and domain of an enterprise.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_enterprises_get_execute()` or `androidenterprise_enterprises_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Enterprise>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Enterprise = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}
/// Retrieves the name and domain of an enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_enterprises_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_enterprises_get_task()`.
/// For the simplest API, use `androidenterprise_enterprises_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_enterprises_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_enterprises_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_enterprises_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEnterprisesGetArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}
/// Retrieves the name and domain of an enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_get_builder()` + `androidenterprise_enterprises_get_execute()`.
/// For task-level control, use `androidenterprise_enterprises_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_get(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEnterprisesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_get_builder(client, &args.enterpriseId)?;
    androidenterprise_enterprises_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccount
/// Returns a service account and credentials. The service account can be bound to the enterprise by calling `setAccount`. The service account is unique to this enterprise and EMM, and will be deleted if the enterprise is unbound. The credentials contain private key data and are not stored server-side. This method can only be called after calling Enterprises.Enroll or Enterprises.CompleteSignup, and before Enterprises.SetAccount; at other times it will return an error. Subsequent calls after the first will generate a new, unique set of credentials, and invalidate the previously generated credentials. Once the service account is bound to the enterprise, it can be managed using the `serviceAccountKeys` resource. *Note:* After you create a key, you might need to wait for 60 seconds or more before you perform another operation with the key. If you try to perform an operation with the key immediately after you create the key, and you receive an error, you can retry the request with exponential backoff .
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_get_service_account_execute()` to send, or `androidenterprise_enterprises_get_service_account` for simplest API.

pub fn androidenterprise_enterprises_get_service_account_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    keyType: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/serviceAccount",
        enterpriseId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = keyType.as_ref() {
        query_parts.push(format!("keyType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccount
/// Returns a service account and credentials. The service account can be bound to the enterprise by calling `setAccount`. The service account is unique to this enterprise and EMM, and will be deleted if the enterprise is unbound. The credentials contain private key data and are not stored server-side. This method can only be called after calling Enterprises.Enroll or Enterprises.CompleteSignup, and before Enterprises.SetAccount; at other times it will return an error. Subsequent calls after the first will generate a new, unique set of credentials, and invalidate the previously generated credentials. Once the service account is bound to the enterprise, it can be managed using the `serviceAccountKeys` resource. *Note:* After you create a key, you might need to wait for 60 seconds or more before you perform another operation with the key. If you try to perform an operation with the key immediately after you create the key, and you receive an error, you can retry the request with exponential backoff .
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_enterprises_get_service_account_execute()` or `androidenterprise_enterprises_get_service_account`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_get_service_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_get_service_account_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceAccount>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccount
/// Returns a service account and credentials. The service account can be bound to the enterprise by calling `setAccount`. The service account is unique to this enterprise and EMM, and will be deleted if the enterprise is unbound. The credentials contain private key data and are not stored server-side. This method can only be called after calling Enterprises.Enroll or Enterprises.CompleteSignup, and before Enterprises.SetAccount; at other times it will return an error. Subsequent calls after the first will generate a new, unique set of credentials, and invalidate the previously generated credentials. Once the service account is bound to the enterprise, it can be managed using the `serviceAccountKeys` resource. *Note:* After you create a key, you might need to wait for 60 seconds or more before you perform another operation with the key. If you try to perform an operation with the key immediately after you create the key, and you receive an error, you can retry the request with exponential backoff .
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_enterprises_get_service_account_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_enterprises_get_service_account_task()`.
/// For the simplest API, use `androidenterprise_enterprises_get_service_account()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_get_service_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_enterprises_get_service_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceAccount>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_enterprises_get_service_account_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_enterprises_get_service_account`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEnterprisesGetServiceAccountArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Query parameter: keyType
    pub keyType: Option<String>,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccount
/// Returns a service account and credentials. The service account can be bound to the enterprise by calling `setAccount`. The service account is unique to this enterprise and EMM, and will be deleted if the enterprise is unbound. The credentials contain private key data and are not stored server-side. This method can only be called after calling Enterprises.Enroll or Enterprises.CompleteSignup, and before Enterprises.SetAccount; at other times it will return an error. Subsequent calls after the first will generate a new, unique set of credentials, and invalidate the previously generated credentials. Once the service account is bound to the enterprise, it can be managed using the `serviceAccountKeys` resource. *Note:* After you create a key, you might need to wait for 60 seconds or more before you perform another operation with the key. If you try to perform an operation with the key immediately after you create the key, and you receive an error, you can retry the request with exponential backoff .
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_get_service_account_builder()` + `androidenterprise_enterprises_get_service_account_execute()`.
/// For task-level control, use `androidenterprise_enterprises_get_service_account_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_get_service_account(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEnterprisesGetServiceAccountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceAccount>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_get_service_account_builder(
        client,
        &args.enterpriseId,
        &args.keyType,
    )?;
    androidenterprise_enterprises_get_service_account_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout
/// Returns the store layout for the enterprise. If the store layout has not been set, returns "basic" as the store layout type and no homepage.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_get_store_layout_execute()` to send, or `androidenterprise_enterprises_get_store_layout` for simplest API.

pub fn androidenterprise_enterprises_get_store_layout_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/storeLayout",
        enterpriseId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout
/// Returns the store layout for the enterprise. If the store layout has not been set, returns "basic" as the store layout type and no homepage.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_enterprises_get_store_layout_execute()` or `androidenterprise_enterprises_get_store_layout`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_get_store_layout_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_get_store_layout_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoreLayout>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoreLayout = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout
/// Returns the store layout for the enterprise. If the store layout has not been set, returns "basic" as the store layout type and no homepage.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_enterprises_get_store_layout_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_enterprises_get_store_layout_task()`.
/// For the simplest API, use `androidenterprise_enterprises_get_store_layout()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_get_store_layout_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_enterprises_get_store_layout_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreLayout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_enterprises_get_store_layout_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_enterprises_get_store_layout`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEnterprisesGetStoreLayoutArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout
/// Returns the store layout for the enterprise. If the store layout has not been set, returns "basic" as the store layout type and no homepage.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_get_store_layout_builder()` + `androidenterprise_enterprises_get_store_layout_execute()`.
/// For task-level control, use `androidenterprise_enterprises_get_store_layout_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_get_store_layout(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEnterprisesGetStoreLayoutArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreLayout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_enterprises_get_store_layout_builder(client, &args.enterpriseId)?;
    androidenterprise_enterprises_get_store_layout_execute(builder)
}

/// GET androidenterprise/v1/enterprises
/// Looks up an enterprise by domain name. This is only supported for enterprises created via the Google-initiated creation flow. Lookup of the id is not needed for enterprises created via the EMM-initiated flow since the EMM learns the enterprise ID in the callback specified in the Enterprises.`generateSignupUrl` call.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_list_execute()` to send, or `androidenterprise_enterprises_list` for simplest API.

pub fn androidenterprise_enterprises_list_builder<R>(
    client: &SimpleHttpClient<R>,
    domain: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = domain.as_ref() {
        query_parts.push(format!("domain={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises
/// Looks up an enterprise by domain name. This is only supported for enterprises created via the Google-initiated creation flow. Lookup of the id is not needed for enterprises created via the EMM-initiated flow since the EMM learns the enterprise ID in the callback specified in the Enterprises.`generateSignupUrl` call.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_enterprises_list_execute()` or `androidenterprise_enterprises_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EnterprisesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EnterprisesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises
/// Looks up an enterprise by domain name. This is only supported for enterprises created via the Google-initiated creation flow. Lookup of the id is not needed for enterprises created via the EMM-initiated flow since the EMM learns the enterprise ID in the callback specified in the Enterprises.`generateSignupUrl` call.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_enterprises_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_enterprises_list_task()`.
/// For the simplest API, use `androidenterprise_enterprises_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_enterprises_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnterprisesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_enterprises_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_enterprises_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEnterprisesListArgs {
    /// Query parameter: domain
    pub domain: Option<String>,
}

/// GET androidenterprise/v1/enterprises
/// Looks up an enterprise by domain name. This is only supported for enterprises created via the Google-initiated creation flow. Lookup of the id is not needed for enterprises created via the EMM-initiated flow since the EMM learns the enterprise ID in the callback specified in the Enterprises.`generateSignupUrl` call.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_list_builder()` + `androidenterprise_enterprises_list_execute()`.
/// For task-level control, use `androidenterprise_enterprises_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_list(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEnterprisesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnterprisesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_list_builder(client, &args.domain)?;
    androidenterprise_enterprises_list_execute(builder)
}

/// POST androidenterprise/v1/enterprises/pullNotificationSet
/// Pulls and returns a notification set for the enterprises associated with the service account authenticated for the request. The notification set may be empty if no notification are pending. A notification set returned needs to be acknowledged within 20 seconds by calling Enterprises.AcknowledgeNotificationSet, unless the notification set is empty. Notifications that are not acknowledged within the 20 seconds will eventually be included again in the response to another PullNotificationSet request, and those that are never acknowledged will ultimately be deleted according to the Google Cloud Platform P`ub/Sub` system policy. Multiple requests might be performed concurrently to retrieve notifications, in which case the pending notifications (if any) will be split among each caller, if any are pending. If no notifications are present, an empty notification list is returned. Subsequent requests may return more notifications once they become available.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_pull_notification_set_execute()` to send, or `androidenterprise_enterprises_pull_notification_set` for simplest API.

pub fn androidenterprise_enterprises_pull_notification_set_builder<R>(
    client: &SimpleHttpClient<R>,
    requestMode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/pullNotificationSet",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestMode.as_ref() {
        query_parts.push(format!("requestMode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/pullNotificationSet
/// Pulls and returns a notification set for the enterprises associated with the service account authenticated for the request. The notification set may be empty if no notification are pending. A notification set returned needs to be acknowledged within 20 seconds by calling Enterprises.AcknowledgeNotificationSet, unless the notification set is empty. Notifications that are not acknowledged within the 20 seconds will eventually be included again in the response to another PullNotificationSet request, and those that are never acknowledged will ultimately be deleted according to the Google Cloud Platform P`ub/Sub` system policy. Multiple requests might be performed concurrently to retrieve notifications, in which case the pending notifications (if any) will be split among each caller, if any are pending. If no notifications are present, an empty notification list is returned. Subsequent requests may return more notifications once they become available.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_enterprises_pull_notification_set_execute()` or `androidenterprise_enterprises_pull_notification_set`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_pull_notification_set_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_pull_notification_set_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationSet>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/pullNotificationSet
/// Pulls and returns a notification set for the enterprises associated with the service account authenticated for the request. The notification set may be empty if no notification are pending. A notification set returned needs to be acknowledged within 20 seconds by calling Enterprises.AcknowledgeNotificationSet, unless the notification set is empty. Notifications that are not acknowledged within the 20 seconds will eventually be included again in the response to another PullNotificationSet request, and those that are never acknowledged will ultimately be deleted according to the Google Cloud Platform P`ub/Sub` system policy. Multiple requests might be performed concurrently to retrieve notifications, in which case the pending notifications (if any) will be split among each caller, if any are pending. If no notifications are present, an empty notification list is returned. Subsequent requests may return more notifications once they become available.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_enterprises_pull_notification_set_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_enterprises_pull_notification_set_task()`.
/// For the simplest API, use `androidenterprise_enterprises_pull_notification_set()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_pull_notification_set_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_enterprises_pull_notification_set_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationSet>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_enterprises_pull_notification_set_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_enterprises_pull_notification_set`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEnterprisesPullNotificationSetArgs {
    /// Query parameter: requestMode
    pub requestMode: Option<String>,
}

/// POST androidenterprise/v1/enterprises/pullNotificationSet
/// Pulls and returns a notification set for the enterprises associated with the service account authenticated for the request. The notification set may be empty if no notification are pending. A notification set returned needs to be acknowledged within 20 seconds by calling Enterprises.AcknowledgeNotificationSet, unless the notification set is empty. Notifications that are not acknowledged within the 20 seconds will eventually be included again in the response to another PullNotificationSet request, and those that are never acknowledged will ultimately be deleted according to the Google Cloud Platform P`ub/Sub` system policy. Multiple requests might be performed concurrently to retrieve notifications, in which case the pending notifications (if any) will be split among each caller, if any are pending. If no notifications are present, an empty notification list is returned. Subsequent requests may return more notifications once they become available.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_pull_notification_set_builder()` + `androidenterprise_enterprises_pull_notification_set_execute()`.
/// For task-level control, use `androidenterprise_enterprises_pull_notification_set_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_pull_notification_set(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEnterprisesPullNotificationSetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationSet>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_enterprises_pull_notification_set_builder(client, &args.requestMode)?;
    androidenterprise_enterprises_pull_notification_set_execute(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/sendTestPushNotification
/// Sends a test notification to validate the EMM integration with the Google Cloud P`ub/Sub` service for this enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_send_test_push_notification_execute()` to send, or `androidenterprise_enterprises_send_test_push_notification` for simplest API.

pub fn androidenterprise_enterprises_send_test_push_notification_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/sendTestPushNotification",
        enterpriseId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/sendTestPushNotification
/// Sends a test notification to validate the EMM integration with the Google Cloud P`ub/Sub` service for this enterprise.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_enterprises_send_test_push_notification_execute()` or `androidenterprise_enterprises_send_test_push_notification`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_send_test_push_notification_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_send_test_push_notification_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EnterprisesSendTestPushNotificationResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EnterprisesSendTestPushNotificationResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/sendTestPushNotification
/// Sends a test notification to validate the EMM integration with the Google Cloud P`ub/Sub` service for this enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_enterprises_send_test_push_notification_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_enterprises_send_test_push_notification_task()`.
/// For the simplest API, use `androidenterprise_enterprises_send_test_push_notification()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_send_test_push_notification_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_enterprises_send_test_push_notification_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EnterprisesSendTestPushNotificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_enterprises_send_test_push_notification_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_enterprises_send_test_push_notification`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEnterprisesSendTestPushNotificationArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/sendTestPushNotification
/// Sends a test notification to validate the EMM integration with the Google Cloud P`ub/Sub` service for this enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_send_test_push_notification_builder()` + `androidenterprise_enterprises_send_test_push_notification_execute()`.
/// For task-level control, use `androidenterprise_enterprises_send_test_push_notification_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_send_test_push_notification(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEnterprisesSendTestPushNotificationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EnterprisesSendTestPushNotificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_send_test_push_notification_builder(
        client,
        &args.enterpriseId,
    )?;
    androidenterprise_enterprises_send_test_push_notification_execute(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/account
/// Sets the account that will be used to authenticate to the API as the enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_set_account_execute()` to send, or `androidenterprise_enterprises_set_account` for simplest API.

pub fn androidenterprise_enterprises_set_account_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/account",
        enterpriseId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/account
/// Sets the account that will be used to authenticate to the API as the enterprise.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_enterprises_set_account_execute()` or `androidenterprise_enterprises_set_account`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_set_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_set_account_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EnterpriseAccount>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EnterpriseAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/account
/// Sets the account that will be used to authenticate to the API as the enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_enterprises_set_account_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_enterprises_set_account_task()`.
/// For the simplest API, use `androidenterprise_enterprises_set_account()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_set_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_enterprises_set_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnterpriseAccount>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_enterprises_set_account_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_enterprises_set_account`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEnterprisesSetAccountArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/account
/// Sets the account that will be used to authenticate to the API as the enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_set_account_builder()` + `androidenterprise_enterprises_set_account_execute()`.
/// For task-level control, use `androidenterprise_enterprises_set_account_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_set_account(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEnterprisesSetAccountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnterpriseAccount>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_set_account_builder(client, &args.enterpriseId)?;
    androidenterprise_enterprises_set_account_execute(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/storeLayout
/// Sets the store layout for the enterprise. By default, `storeLayoutType` is set to "basic" and the basic store layout is enabled. The basic layout only contains apps approved by the admin, and that have been added to the available product set for a user (using the `setAvailableProductSet` call). Apps on the page are sorted in order of their product ID value. If you create a custom store layout (by setting `storeLayoutType` = "custom" and setting a homepage), the basic store layout is disabled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_set_store_layout_execute()` to send, or `androidenterprise_enterprises_set_store_layout` for simplest API.

pub fn androidenterprise_enterprises_set_store_layout_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/storeLayout",
        enterpriseId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/storeLayout
/// Sets the store layout for the enterprise. By default, `storeLayoutType` is set to "basic" and the basic store layout is enabled. The basic layout only contains apps approved by the admin, and that have been added to the available product set for a user (using the `setAvailableProductSet` call). Apps on the page are sorted in order of their product ID value. If you create a custom store layout (by setting `storeLayoutType` = "custom" and setting a homepage), the basic store layout is disabled.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_enterprises_set_store_layout_execute()` or `androidenterprise_enterprises_set_store_layout`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_set_store_layout_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_set_store_layout_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoreLayout>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoreLayout = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/storeLayout
/// Sets the store layout for the enterprise. By default, `storeLayoutType` is set to "basic" and the basic store layout is enabled. The basic layout only contains apps approved by the admin, and that have been added to the available product set for a user (using the `setAvailableProductSet` call). Apps on the page are sorted in order of their product ID value. If you create a custom store layout (by setting `storeLayoutType` = "custom" and setting a homepage), the basic store layout is disabled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_enterprises_set_store_layout_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_enterprises_set_store_layout_task()`.
/// For the simplest API, use `androidenterprise_enterprises_set_store_layout()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_set_store_layout_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_enterprises_set_store_layout_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreLayout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_enterprises_set_store_layout_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_enterprises_set_store_layout`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEnterprisesSetStoreLayoutArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/storeLayout
/// Sets the store layout for the enterprise. By default, `storeLayoutType` is set to "basic" and the basic store layout is enabled. The basic layout only contains apps approved by the admin, and that have been added to the available product set for a user (using the `setAvailableProductSet` call). Apps on the page are sorted in order of their product ID value. If you create a custom store layout (by setting `storeLayoutType` = "custom" and setting a homepage), the basic store layout is disabled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_set_store_layout_builder()` + `androidenterprise_enterprises_set_store_layout_execute()`.
/// For task-level control, use `androidenterprise_enterprises_set_store_layout_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_set_store_layout(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEnterprisesSetStoreLayoutArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreLayout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_enterprises_set_store_layout_builder(client, &args.enterpriseId)?;
    androidenterprise_enterprises_set_store_layout_execute(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/unenroll
/// Unenrolls an enterprise from the calling EMM.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_enterprises_unenroll_execute()` to send, or `androidenterprise_enterprises_unenroll` for simplest API.

pub fn androidenterprise_enterprises_unenroll_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/unenroll",
        enterpriseId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/unenroll
/// Unenrolls an enterprise from the calling EMM.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_enterprises_unenroll_execute()` or `androidenterprise_enterprises_unenroll`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_unenroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_unenroll_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/unenroll
/// Unenrolls an enterprise from the calling EMM.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_enterprises_unenroll_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_enterprises_unenroll_task()`.
/// For the simplest API, use `androidenterprise_enterprises_unenroll()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_enterprises_unenroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_enterprises_unenroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_enterprises_unenroll_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_enterprises_unenroll`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEnterprisesUnenrollArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/unenroll
/// Unenrolls an enterprise from the calling EMM.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_enterprises_unenroll_builder()` + `androidenterprise_enterprises_unenroll_execute()`.
/// For task-level control, use `androidenterprise_enterprises_unenroll_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_enterprises_unenroll(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEnterprisesUnenrollArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_enterprises_unenroll_builder(client, &args.enterpriseId)?;
    androidenterprise_enterprises_unenroll_execute(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Removes an entitlement to an app for a user. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_entitlements_delete_execute()` to send, or `androidenterprise_entitlements_delete` for simplest API.

pub fn androidenterprise_entitlements_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    entitlementId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/entitlements/{}",
        enterpriseId,
        userId,
        entitlementId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Removes an entitlement to an app for a user. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_entitlements_delete_execute()` or `androidenterprise_entitlements_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_entitlements_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_entitlements_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Removes an entitlement to an app for a user. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_entitlements_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_entitlements_delete_task()`.
/// For the simplest API, use `androidenterprise_entitlements_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_entitlements_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_entitlements_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_entitlements_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_entitlements_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEntitlementsDeleteArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: entitlementId
    pub entitlementId: String,
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Removes an entitlement to an app for a user. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_entitlements_delete_builder()` + `androidenterprise_entitlements_delete_execute()`.
/// For task-level control, use `androidenterprise_entitlements_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_entitlements_delete(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEntitlementsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_entitlements_delete_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.entitlementId,
    )?;
    androidenterprise_entitlements_delete_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Retrieves details of an entitlement. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_entitlements_get_execute()` to send, or `androidenterprise_entitlements_get` for simplest API.

pub fn androidenterprise_entitlements_get_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    entitlementId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/entitlements/{}",
        enterpriseId,
        userId,
        entitlementId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Retrieves details of an entitlement. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_entitlements_get_execute()` or `androidenterprise_entitlements_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_entitlements_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_entitlements_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Entitlement>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Entitlement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Retrieves details of an entitlement. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_entitlements_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_entitlements_get_task()`.
/// For the simplest API, use `androidenterprise_entitlements_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_entitlements_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_entitlements_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entitlement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_entitlements_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_entitlements_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEntitlementsGetArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: entitlementId
    pub entitlementId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Retrieves details of an entitlement. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_entitlements_get_builder()` + `androidenterprise_entitlements_get_execute()`.
/// For task-level control, use `androidenterprise_entitlements_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_entitlements_get(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEntitlementsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entitlement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_entitlements_get_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.entitlementId,
    )?;
    androidenterprise_entitlements_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements
/// Lists all entitlements for the specified user. Only the ID is set. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_entitlements_list_execute()` to send, or `androidenterprise_entitlements_list` for simplest API.

pub fn androidenterprise_entitlements_list_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/entitlements",
        enterpriseId,
        userId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements
/// Lists all entitlements for the specified user. Only the ID is set. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_entitlements_list_execute()` or `androidenterprise_entitlements_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_entitlements_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_entitlements_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntitlementsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntitlementsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements
/// Lists all entitlements for the specified user. Only the ID is set. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_entitlements_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_entitlements_list_task()`.
/// For the simplest API, use `androidenterprise_entitlements_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_entitlements_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_entitlements_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntitlementsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_entitlements_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_entitlements_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEntitlementsListArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements
/// Lists all entitlements for the specified user. Only the ID is set. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_entitlements_list_builder()` + `androidenterprise_entitlements_list_execute()`.
/// For task-level control, use `androidenterprise_entitlements_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_entitlements_list(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEntitlementsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntitlementsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_entitlements_list_builder(client, &args.enterpriseId, &args.userId)?;
    androidenterprise_entitlements_list_execute(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Adds or updates an entitlement to an app for a user. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_entitlements_update_execute()` to send, or `androidenterprise_entitlements_update` for simplest API.

pub fn androidenterprise_entitlements_update_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    entitlementId: &String,
    install: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/entitlements/{}",
        enterpriseId,
        userId,
        entitlementId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = install.as_ref() {
        query_parts.push(format!("install={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Adds or updates an entitlement to an app for a user. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_entitlements_update_execute()` or `androidenterprise_entitlements_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_entitlements_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_entitlements_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Entitlement>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Entitlement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Adds or updates an entitlement to an app for a user. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_entitlements_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_entitlements_update_task()`.
/// For the simplest API, use `androidenterprise_entitlements_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_entitlements_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_entitlements_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entitlement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_entitlements_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_entitlements_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseEntitlementsUpdateArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: entitlementId
    pub entitlementId: String,
    /// Query parameter: install
    pub install: Option<String>,
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/entitlements/{entitlementId}
/// Adds or updates an entitlement to an app for a user. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_entitlements_update_builder()` + `androidenterprise_entitlements_update_execute()`.
/// For task-level control, use `androidenterprise_entitlements_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_entitlements_update(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseEntitlementsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entitlement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_entitlements_update_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.entitlementId,
        &args.install,
    )?;
    androidenterprise_entitlements_update_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses/{groupLicenseId}
/// Retrieves details of an enterprise''s group license for a product. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_grouplicenses_get_execute()` to send, or `androidenterprise_grouplicenses_get` for simplest API.

pub fn androidenterprise_grouplicenses_get_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    groupLicenseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/groupLicenses/{}",
        enterpriseId,
        groupLicenseId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses/{groupLicenseId}
/// Retrieves details of an enterprise''s group license for a product. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_grouplicenses_get_execute()` or `androidenterprise_grouplicenses_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_grouplicenses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_grouplicenses_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GroupLicense>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupLicense = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses/{groupLicenseId}
/// Retrieves details of an enterprise''s group license for a product. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_grouplicenses_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_grouplicenses_get_task()`.
/// For the simplest API, use `androidenterprise_grouplicenses_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_grouplicenses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_grouplicenses_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupLicense>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_grouplicenses_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_grouplicenses_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseGrouplicensesGetArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: groupLicenseId
    pub groupLicenseId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses/{groupLicenseId}
/// Retrieves details of an enterprise''s group license for a product. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_grouplicenses_get_builder()` + `androidenterprise_grouplicenses_get_execute()`.
/// For task-level control, use `androidenterprise_grouplicenses_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_grouplicenses_get(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseGrouplicensesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupLicense>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_grouplicenses_get_builder(
        client,
        &args.enterpriseId,
        &args.groupLicenseId,
    )?;
    androidenterprise_grouplicenses_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses
/// Retrieves IDs of all products for which the enterprise has a group license. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_grouplicenses_list_execute()` to send, or `androidenterprise_grouplicenses_list` for simplest API.

pub fn androidenterprise_grouplicenses_list_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/groupLicenses",
        enterpriseId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses
/// Retrieves IDs of all products for which the enterprise has a group license. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_grouplicenses_list_execute()` or `androidenterprise_grouplicenses_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_grouplicenses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_grouplicenses_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GroupLicensesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupLicensesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses
/// Retrieves IDs of all products for which the enterprise has a group license. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_grouplicenses_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_grouplicenses_list_task()`.
/// For the simplest API, use `androidenterprise_grouplicenses_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_grouplicenses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_grouplicenses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupLicensesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_grouplicenses_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_grouplicenses_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseGrouplicensesListArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses
/// Retrieves IDs of all products for which the enterprise has a group license. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_grouplicenses_list_builder()` + `androidenterprise_grouplicenses_list_execute()`.
/// For task-level control, use `androidenterprise_grouplicenses_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_grouplicenses_list(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseGrouplicensesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupLicensesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_grouplicenses_list_builder(client, &args.enterpriseId)?;
    androidenterprise_grouplicenses_list_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses/{groupLicenseId}/users
/// Retrieves the IDs of the users who have been granted entitlements under the license. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_grouplicenseusers_list_execute()` to send, or `androidenterprise_grouplicenseusers_list` for simplest API.

pub fn androidenterprise_grouplicenseusers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    groupLicenseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/groupLicenses/{}/users",
        enterpriseId,
        groupLicenseId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses/{groupLicenseId}/users
/// Retrieves the IDs of the users who have been granted entitlements under the license. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_grouplicenseusers_list_execute()` or `androidenterprise_grouplicenseusers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_grouplicenseusers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_grouplicenseusers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GroupLicenseUsersListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupLicenseUsersListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses/{groupLicenseId}/users
/// Retrieves the IDs of the users who have been granted entitlements under the license. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_grouplicenseusers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_grouplicenseusers_list_task()`.
/// For the simplest API, use `androidenterprise_grouplicenseusers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_grouplicenseusers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_grouplicenseusers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GroupLicenseUsersListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_grouplicenseusers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_grouplicenseusers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseGrouplicenseusersListArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: groupLicenseId
    pub groupLicenseId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/groupLicenses/{groupLicenseId}/users
/// Retrieves the IDs of the users who have been granted entitlements under the license. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_grouplicenseusers_list_builder()` + `androidenterprise_grouplicenseusers_list_execute()`.
/// For task-level control, use `androidenterprise_grouplicenseusers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_grouplicenseusers_list(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseGrouplicenseusersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GroupLicenseUsersListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_grouplicenseusers_list_builder(
        client,
        &args.enterpriseId,
        &args.groupLicenseId,
    )?;
    androidenterprise_grouplicenseusers_list_execute(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Requests to remove an app from a device. A call to get or list will still show the app as installed on the device until it is actually removed. A successful response indicates that a removal request has been sent to the device. The call will be considered successful even if the app is not present on the device (e.g. it was never installed, or was removed by the user).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_installs_delete_execute()` to send, or `androidenterprise_installs_delete` for simplest API.

pub fn androidenterprise_installs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    deviceId: &String,
    installId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/devices/{}/installs/{}",
        enterpriseId,
        userId,
        deviceId,
        installId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Requests to remove an app from a device. A call to get or list will still show the app as installed on the device until it is actually removed. A successful response indicates that a removal request has been sent to the device. The call will be considered successful even if the app is not present on the device (e.g. it was never installed, or was removed by the user).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_installs_delete_execute()` or `androidenterprise_installs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_installs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_installs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Requests to remove an app from a device. A call to get or list will still show the app as installed on the device until it is actually removed. A successful response indicates that a removal request has been sent to the device. The call will be considered successful even if the app is not present on the device (e.g. it was never installed, or was removed by the user).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_installs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_installs_delete_task()`.
/// For the simplest API, use `androidenterprise_installs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_installs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_installs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_installs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_installs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseInstallsDeleteArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: deviceId
    pub deviceId: String,
    /// Path parameter: installId
    pub installId: String,
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Requests to remove an app from a device. A call to get or list will still show the app as installed on the device until it is actually removed. A successful response indicates that a removal request has been sent to the device. The call will be considered successful even if the app is not present on the device (e.g. it was never installed, or was removed by the user).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_installs_delete_builder()` + `androidenterprise_installs_delete_execute()`.
/// For task-level control, use `androidenterprise_installs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_installs_delete(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseInstallsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_installs_delete_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.deviceId,
        &args.installId,
    )?;
    androidenterprise_installs_delete_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Retrieves details of an installation of an app on a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_installs_get_execute()` to send, or `androidenterprise_installs_get` for simplest API.

pub fn androidenterprise_installs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    deviceId: &String,
    installId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/devices/{}/installs/{}",
        enterpriseId,
        userId,
        deviceId,
        installId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Retrieves details of an installation of an app on a device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_installs_get_execute()` or `androidenterprise_installs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_installs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_installs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Install>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Install = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Retrieves details of an installation of an app on a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_installs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_installs_get_task()`.
/// For the simplest API, use `androidenterprise_installs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_installs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_installs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Install>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_installs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_installs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseInstallsGetArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: deviceId
    pub deviceId: String,
    /// Path parameter: installId
    pub installId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Retrieves details of an installation of an app on a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_installs_get_builder()` + `androidenterprise_installs_get_execute()`.
/// For task-level control, use `androidenterprise_installs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_installs_get(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseInstallsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Install>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_installs_get_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.deviceId,
        &args.installId,
    )?;
    androidenterprise_installs_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs
/// Retrieves the details of all apps installed on the specified device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_installs_list_execute()` to send, or `androidenterprise_installs_list` for simplest API.

pub fn androidenterprise_installs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    deviceId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/devices/{}/installs",
        enterpriseId,
        userId,
        deviceId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs
/// Retrieves the details of all apps installed on the specified device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_installs_list_execute()` or `androidenterprise_installs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_installs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_installs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstallsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstallsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs
/// Retrieves the details of all apps installed on the specified device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_installs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_installs_list_task()`.
/// For the simplest API, use `androidenterprise_installs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_installs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_installs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstallsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_installs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_installs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseInstallsListArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: deviceId
    pub deviceId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs
/// Retrieves the details of all apps installed on the specified device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_installs_list_builder()` + `androidenterprise_installs_list_execute()`.
/// For task-level control, use `androidenterprise_installs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_installs_list(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseInstallsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstallsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_installs_list_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.deviceId,
    )?;
    androidenterprise_installs_list_execute(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Requests to install the latest version of an app to a device. If the app is already installed, then it is updated to the latest version if necessary.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_installs_update_execute()` to send, or `androidenterprise_installs_update` for simplest API.

pub fn androidenterprise_installs_update_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    deviceId: &String,
    installId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/devices/{}/installs/{}",
        enterpriseId,
        userId,
        deviceId,
        installId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Requests to install the latest version of an app to a device. If the app is already installed, then it is updated to the latest version if necessary.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_installs_update_execute()` or `androidenterprise_installs_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_installs_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_installs_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Install>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Install = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Requests to install the latest version of an app to a device. If the app is already installed, then it is updated to the latest version if necessary.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_installs_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_installs_update_task()`.
/// For the simplest API, use `androidenterprise_installs_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_installs_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_installs_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Install>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_installs_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_installs_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseInstallsUpdateArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: deviceId
    pub deviceId: String,
    /// Path parameter: installId
    pub installId: String,
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/installs/{installId}
/// Requests to install the latest version of an app to a device. If the app is already installed, then it is updated to the latest version if necessary.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_installs_update_builder()` + `androidenterprise_installs_update_execute()`.
/// For task-level control, use `androidenterprise_installs_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_installs_update(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseInstallsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Install>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_installs_update_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.deviceId,
        &args.installId,
    )?;
    androidenterprise_installs_update_execute(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Removes a per-device managed configuration for an app for the specified device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationsfordevice_delete_execute()` to send, or `androidenterprise_managedconfigurationsfordevice_delete` for simplest API.

pub fn androidenterprise_managedconfigurationsfordevice_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    deviceId: &String,
    managedConfigurationForDeviceId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/devices/{}/managedConfigurationsForDevice/{}",
        enterpriseId,
        userId,
        deviceId,
        managedConfigurationForDeviceId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Removes a per-device managed configuration for an app for the specified device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_managedconfigurationsfordevice_delete_execute()` or `androidenterprise_managedconfigurationsfordevice_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationsfordevice_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsfordevice_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Removes a per-device managed configuration for an app for the specified device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_managedconfigurationsfordevice_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_managedconfigurationsfordevice_delete_task()`.
/// For the simplest API, use `androidenterprise_managedconfigurationsfordevice_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationsfordevice_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_managedconfigurationsfordevice_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_managedconfigurationsfordevice_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_managedconfigurationsfordevice_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseManagedconfigurationsfordeviceDeleteArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: deviceId
    pub deviceId: String,
    /// Path parameter: managedConfigurationForDeviceId
    pub managedConfigurationForDeviceId: String,
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Removes a per-device managed configuration for an app for the specified device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationsfordevice_delete_builder()` + `androidenterprise_managedconfigurationsfordevice_delete_execute()`.
/// For task-level control, use `androidenterprise_managedconfigurationsfordevice_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsfordevice_delete(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseManagedconfigurationsfordeviceDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_managedconfigurationsfordevice_delete_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.deviceId,
        &args.managedConfigurationForDeviceId,
    )?;
    androidenterprise_managedconfigurationsfordevice_delete_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Retrieves details of a per-device managed configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationsfordevice_get_execute()` to send, or `androidenterprise_managedconfigurationsfordevice_get` for simplest API.

pub fn androidenterprise_managedconfigurationsfordevice_get_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    deviceId: &String,
    managedConfigurationForDeviceId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/devices/{}/managedConfigurationsForDevice/{}",
        enterpriseId,
        userId,
        deviceId,
        managedConfigurationForDeviceId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Retrieves details of a per-device managed configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_managedconfigurationsfordevice_get_execute()` or `androidenterprise_managedconfigurationsfordevice_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationsfordevice_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsfordevice_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ManagedConfiguration>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Retrieves details of a per-device managed configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_managedconfigurationsfordevice_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_managedconfigurationsfordevice_get_task()`.
/// For the simplest API, use `androidenterprise_managedconfigurationsfordevice_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationsfordevice_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_managedconfigurationsfordevice_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_managedconfigurationsfordevice_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_managedconfigurationsfordevice_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseManagedconfigurationsfordeviceGetArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: deviceId
    pub deviceId: String,
    /// Path parameter: managedConfigurationForDeviceId
    pub managedConfigurationForDeviceId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Retrieves details of a per-device managed configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationsfordevice_get_builder()` + `androidenterprise_managedconfigurationsfordevice_get_execute()`.
/// For task-level control, use `androidenterprise_managedconfigurationsfordevice_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsfordevice_get(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseManagedconfigurationsfordeviceGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_managedconfigurationsfordevice_get_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.deviceId,
        &args.managedConfigurationForDeviceId,
    )?;
    androidenterprise_managedconfigurationsfordevice_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice
/// Lists all the per-device managed configurations for the specified device. Only the ID is set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationsfordevice_list_execute()` to send, or `androidenterprise_managedconfigurationsfordevice_list` for simplest API.

pub fn androidenterprise_managedconfigurationsfordevice_list_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    deviceId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/devices/{}/managedConfigurationsForDevice",
        enterpriseId,
        userId,
        deviceId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice
/// Lists all the per-device managed configurations for the specified device. Only the ID is set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_managedconfigurationsfordevice_list_execute()` or `androidenterprise_managedconfigurationsfordevice_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationsfordevice_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsfordevice_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ManagedConfigurationsForDeviceListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedConfigurationsForDeviceListResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice
/// Lists all the per-device managed configurations for the specified device. Only the ID is set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_managedconfigurationsfordevice_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_managedconfigurationsfordevice_list_task()`.
/// For the simplest API, use `androidenterprise_managedconfigurationsfordevice_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationsfordevice_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_managedconfigurationsfordevice_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ManagedConfigurationsForDeviceListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_managedconfigurationsfordevice_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_managedconfigurationsfordevice_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseManagedconfigurationsfordeviceListArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: deviceId
    pub deviceId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice
/// Lists all the per-device managed configurations for the specified device. Only the ID is set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationsfordevice_list_builder()` + `androidenterprise_managedconfigurationsfordevice_list_execute()`.
/// For task-level control, use `androidenterprise_managedconfigurationsfordevice_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsfordevice_list(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseManagedconfigurationsfordeviceListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ManagedConfigurationsForDeviceListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_managedconfigurationsfordevice_list_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.deviceId,
    )?;
    androidenterprise_managedconfigurationsfordevice_list_execute(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Adds or updates a per-device managed configuration for an app for the specified device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationsfordevice_update_execute()` to send, or `androidenterprise_managedconfigurationsfordevice_update` for simplest API.

pub fn androidenterprise_managedconfigurationsfordevice_update_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    deviceId: &String,
    managedConfigurationForDeviceId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/devices/{}/managedConfigurationsForDevice/{}",
        enterpriseId,
        userId,
        deviceId,
        managedConfigurationForDeviceId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Adds or updates a per-device managed configuration for an app for the specified device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_managedconfigurationsfordevice_update_execute()` or `androidenterprise_managedconfigurationsfordevice_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationsfordevice_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsfordevice_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ManagedConfiguration>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Adds or updates a per-device managed configuration for an app for the specified device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_managedconfigurationsfordevice_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_managedconfigurationsfordevice_update_task()`.
/// For the simplest API, use `androidenterprise_managedconfigurationsfordevice_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationsfordevice_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_managedconfigurationsfordevice_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_managedconfigurationsfordevice_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_managedconfigurationsfordevice_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseManagedconfigurationsfordeviceUpdateArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: deviceId
    pub deviceId: String,
    /// Path parameter: managedConfigurationForDeviceId
    pub managedConfigurationForDeviceId: String,
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}/managedConfigurationsForDevice/{managedConfigurationForDeviceId}
/// Adds or updates a per-device managed configuration for an app for the specified device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationsfordevice_update_builder()` + `androidenterprise_managedconfigurationsfordevice_update_execute()`.
/// For task-level control, use `androidenterprise_managedconfigurationsfordevice_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsfordevice_update(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseManagedconfigurationsfordeviceUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_managedconfigurationsfordevice_update_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.deviceId,
        &args.managedConfigurationForDeviceId,
    )?;
    androidenterprise_managedconfigurationsfordevice_update_execute(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Removes a per-user managed configuration for an app for the specified user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationsforuser_delete_execute()` to send, or `androidenterprise_managedconfigurationsforuser_delete` for simplest API.

pub fn androidenterprise_managedconfigurationsforuser_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    managedConfigurationForUserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/managedConfigurationsForUser/{}",
        enterpriseId,
        userId,
        managedConfigurationForUserId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Removes a per-user managed configuration for an app for the specified user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_managedconfigurationsforuser_delete_execute()` or `androidenterprise_managedconfigurationsforuser_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationsforuser_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsforuser_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Removes a per-user managed configuration for an app for the specified user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_managedconfigurationsforuser_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_managedconfigurationsforuser_delete_task()`.
/// For the simplest API, use `androidenterprise_managedconfigurationsforuser_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationsforuser_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_managedconfigurationsforuser_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_managedconfigurationsforuser_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_managedconfigurationsforuser_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseManagedconfigurationsforuserDeleteArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: managedConfigurationForUserId
    pub managedConfigurationForUserId: String,
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Removes a per-user managed configuration for an app for the specified user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationsforuser_delete_builder()` + `androidenterprise_managedconfigurationsforuser_delete_execute()`.
/// For task-level control, use `androidenterprise_managedconfigurationsforuser_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsforuser_delete(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseManagedconfigurationsforuserDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_managedconfigurationsforuser_delete_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.managedConfigurationForUserId,
    )?;
    androidenterprise_managedconfigurationsforuser_delete_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Retrieves details of a per-user managed configuration for an app for the specified user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationsforuser_get_execute()` to send, or `androidenterprise_managedconfigurationsforuser_get` for simplest API.

pub fn androidenterprise_managedconfigurationsforuser_get_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    managedConfigurationForUserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/managedConfigurationsForUser/{}",
        enterpriseId,
        userId,
        managedConfigurationForUserId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Retrieves details of a per-user managed configuration for an app for the specified user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_managedconfigurationsforuser_get_execute()` or `androidenterprise_managedconfigurationsforuser_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationsforuser_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsforuser_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ManagedConfiguration>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Retrieves details of a per-user managed configuration for an app for the specified user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_managedconfigurationsforuser_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_managedconfigurationsforuser_get_task()`.
/// For the simplest API, use `androidenterprise_managedconfigurationsforuser_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationsforuser_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_managedconfigurationsforuser_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_managedconfigurationsforuser_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_managedconfigurationsforuser_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseManagedconfigurationsforuserGetArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: managedConfigurationForUserId
    pub managedConfigurationForUserId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Retrieves details of a per-user managed configuration for an app for the specified user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationsforuser_get_builder()` + `androidenterprise_managedconfigurationsforuser_get_execute()`.
/// For task-level control, use `androidenterprise_managedconfigurationsforuser_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsforuser_get(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseManagedconfigurationsforuserGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_managedconfigurationsforuser_get_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.managedConfigurationForUserId,
    )?;
    androidenterprise_managedconfigurationsforuser_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser
/// Lists all the per-user managed configurations for the specified user. Only the ID is set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationsforuser_list_execute()` to send, or `androidenterprise_managedconfigurationsforuser_list` for simplest API.

pub fn androidenterprise_managedconfigurationsforuser_list_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/managedConfigurationsForUser",
        enterpriseId,
        userId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser
/// Lists all the per-user managed configurations for the specified user. Only the ID is set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_managedconfigurationsforuser_list_execute()` or `androidenterprise_managedconfigurationsforuser_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationsforuser_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsforuser_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ManagedConfigurationsForUserListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedConfigurationsForUserListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser
/// Lists all the per-user managed configurations for the specified user. Only the ID is set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_managedconfigurationsforuser_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_managedconfigurationsforuser_list_task()`.
/// For the simplest API, use `androidenterprise_managedconfigurationsforuser_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationsforuser_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_managedconfigurationsforuser_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ManagedConfigurationsForUserListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_managedconfigurationsforuser_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_managedconfigurationsforuser_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseManagedconfigurationsforuserListArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser
/// Lists all the per-user managed configurations for the specified user. Only the ID is set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationsforuser_list_builder()` + `androidenterprise_managedconfigurationsforuser_list_execute()`.
/// For task-level control, use `androidenterprise_managedconfigurationsforuser_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsforuser_list(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseManagedconfigurationsforuserListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ManagedConfigurationsForUserListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_managedconfigurationsforuser_list_builder(
        client,
        &args.enterpriseId,
        &args.userId,
    )?;
    androidenterprise_managedconfigurationsforuser_list_execute(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Adds or updates the managed configuration settings for an app for the specified user. If you support the Managed configurations iframe, you can apply managed configurations to a user by specifying an `mcmId` and its associated configuration variables (if any) in the request. Alternatively, all EMMs can apply managed configurations by passing a list of managed properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationsforuser_update_execute()` to send, or `androidenterprise_managedconfigurationsforuser_update` for simplest API.

pub fn androidenterprise_managedconfigurationsforuser_update_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
    managedConfigurationForUserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/managedConfigurationsForUser/{}",
        enterpriseId,
        userId,
        managedConfigurationForUserId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Adds or updates the managed configuration settings for an app for the specified user. If you support the Managed configurations iframe, you can apply managed configurations to a user by specifying an `mcmId` and its associated configuration variables (if any) in the request. Alternatively, all EMMs can apply managed configurations by passing a list of managed properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_managedconfigurationsforuser_update_execute()` or `androidenterprise_managedconfigurationsforuser_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationsforuser_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsforuser_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ManagedConfiguration>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Adds or updates the managed configuration settings for an app for the specified user. If you support the Managed configurations iframe, you can apply managed configurations to a user by specifying an `mcmId` and its associated configuration variables (if any) in the request. Alternatively, all EMMs can apply managed configurations by passing a list of managed properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_managedconfigurationsforuser_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_managedconfigurationsforuser_update_task()`.
/// For the simplest API, use `androidenterprise_managedconfigurationsforuser_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationsforuser_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_managedconfigurationsforuser_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_managedconfigurationsforuser_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_managedconfigurationsforuser_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseManagedconfigurationsforuserUpdateArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: managedConfigurationForUserId
    pub managedConfigurationForUserId: String,
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/managedConfigurationsForUser/{managedConfigurationForUserId}
/// Adds or updates the managed configuration settings for an app for the specified user. If you support the Managed configurations iframe, you can apply managed configurations to a user by specifying an `mcmId` and its associated configuration variables (if any) in the request. Alternatively, all EMMs can apply managed configurations by passing a list of managed properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationsforuser_update_builder()` + `androidenterprise_managedconfigurationsforuser_update_execute()`.
/// For task-level control, use `androidenterprise_managedconfigurationsforuser_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationsforuser_update(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseManagedconfigurationsforuserUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_managedconfigurationsforuser_update_builder(
        client,
        &args.enterpriseId,
        &args.userId,
        &args.managedConfigurationForUserId,
    )?;
    androidenterprise_managedconfigurationsforuser_update_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/managedConfigurationsSettings
/// Lists all the managed configurations settings for the specified app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_managedconfigurationssettings_list_execute()` to send, or `androidenterprise_managedconfigurationssettings_list` for simplest API.

pub fn androidenterprise_managedconfigurationssettings_list_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    productId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/products/{}/managedConfigurationsSettings",
        enterpriseId,
        productId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/managedConfigurationsSettings
/// Lists all the managed configurations settings for the specified app.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_managedconfigurationssettings_list_execute()` or `androidenterprise_managedconfigurationssettings_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationssettings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationssettings_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ManagedConfigurationsSettingsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedConfigurationsSettingsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/managedConfigurationsSettings
/// Lists all the managed configurations settings for the specified app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_managedconfigurationssettings_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_managedconfigurationssettings_list_task()`.
/// For the simplest API, use `androidenterprise_managedconfigurationssettings_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_managedconfigurationssettings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_managedconfigurationssettings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ManagedConfigurationsSettingsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_managedconfigurationssettings_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_managedconfigurationssettings_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseManagedconfigurationssettingsListArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: productId
    pub productId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/managedConfigurationsSettings
/// Lists all the managed configurations settings for the specified app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_managedconfigurationssettings_list_builder()` + `androidenterprise_managedconfigurationssettings_list_execute()`.
/// For task-level control, use `androidenterprise_managedconfigurationssettings_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_managedconfigurationssettings_list(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseManagedconfigurationssettingsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ManagedConfigurationsSettingsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_managedconfigurationssettings_list_builder(
        client,
        &args.enterpriseId,
        &args.productId,
    )?;
    androidenterprise_managedconfigurationssettings_list_execute(builder)
}

/// GET androidenterprise/v1/permissions/{permissionId}
/// Retrieves details of an Android app permission for display to an enterprise admin.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_permissions_get_execute()` to send, or `androidenterprise_permissions_get` for simplest API.

pub fn androidenterprise_permissions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    permissionId: &String,
    language: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/permissions/{}",
        permissionId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language.as_ref() {
        query_parts.push(format!("language={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/permissions/{permissionId}
/// Retrieves details of an Android app permission for display to an enterprise admin.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_permissions_get_execute()` or `androidenterprise_permissions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_permissions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_permissions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Permission>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Permission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/permissions/{permissionId}
/// Retrieves details of an Android app permission for display to an enterprise admin.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_permissions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_permissions_get_task()`.
/// For the simplest API, use `androidenterprise_permissions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_permissions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_permissions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Permission>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_permissions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_permissions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterprisePermissionsGetArgs {
    /// Path parameter: permissionId
    pub permissionId: String,
    /// Query parameter: language
    pub language: Option<String>,
}

/// GET androidenterprise/v1/permissions/{permissionId}
/// Retrieves details of an Android app permission for display to an enterprise admin.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_permissions_get_builder()` + `androidenterprise_permissions_get_execute()`.
/// For task-level control, use `androidenterprise_permissions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_permissions_get(
    client: &SimpleHttpClient,
    args: &AndroidenterprisePermissionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Permission>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_permissions_get_builder(client, &args.permissionId, &args.language)?;
    androidenterprise_permissions_get_execute(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/approve
/// Approves the specified product and the relevant app permissions, if any. The maximum number of products that you can approve per enterprise customer is 1,000. To learn how to use managed Google Play to design and create a store layout to display approved products to your users, see Store Layout Design. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_products_approve_execute()` to send, or `androidenterprise_products_approve` for simplest API.

pub fn androidenterprise_products_approve_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    productId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/products/{}/approve",
        enterpriseId,
        productId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/approve
/// Approves the specified product and the relevant app permissions, if any. The maximum number of products that you can approve per enterprise customer is 1,000. To learn how to use managed Google Play to design and create a store layout to display approved products to your users, see Store Layout Design. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_products_approve_execute()` or `androidenterprise_products_approve`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_products_approve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_approve_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/approve
/// Approves the specified product and the relevant app permissions, if any. The maximum number of products that you can approve per enterprise customer is 1,000. To learn how to use managed Google Play to design and create a store layout to display approved products to your users, see Store Layout Design. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_products_approve_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_products_approve_task()`.
/// For the simplest API, use `androidenterprise_products_approve()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_products_approve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_products_approve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_products_approve_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_products_approve`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseProductsApproveArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: productId
    pub productId: String,
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/approve
/// Approves the specified product and the relevant app permissions, if any. The maximum number of products that you can approve per enterprise customer is 1,000. To learn how to use managed Google Play to design and create a store layout to display approved products to your users, see Store Layout Design. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_products_approve_builder()` + `androidenterprise_products_approve_execute()`.
/// For task-level control, use `androidenterprise_products_approve_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_approve(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseProductsApproveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_products_approve_builder(client, &args.enterpriseId, &args.productId)?;
    androidenterprise_products_approve_execute(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/generateApprovalUrl
/// Generates a URL that can be rendered in an iframe to display the permissions (if any) of a product. An enterprise admin must view these permissions and accept them on behalf of their organization in order to approve that product. Admins should accept the displayed permissions by interacting with a separate UI element in the EMM console, which in turn should trigger the use of this URL as the `approvalUrlInfo`.`approvalUrl` property in a Products.approve call to approve the product. This URL can only be used to display permissions for up to 1 day. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_products_generate_approval_url_execute()` to send, or `androidenterprise_products_generate_approval_url` for simplest API.

pub fn androidenterprise_products_generate_approval_url_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    productId: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/products/{}/generateApprovalUrl",
        enterpriseId,
        productId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/generateApprovalUrl
/// Generates a URL that can be rendered in an iframe to display the permissions (if any) of a product. An enterprise admin must view these permissions and accept them on behalf of their organization in order to approve that product. Admins should accept the displayed permissions by interacting with a separate UI element in the EMM console, which in turn should trigger the use of this URL as the `approvalUrlInfo`.`approvalUrl` property in a Products.approve call to approve the product. This URL can only be used to display permissions for up to 1 day. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_products_generate_approval_url_execute()` or `androidenterprise_products_generate_approval_url`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_products_generate_approval_url_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_generate_approval_url_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductsGenerateApprovalUrlResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductsGenerateApprovalUrlResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/generateApprovalUrl
/// Generates a URL that can be rendered in an iframe to display the permissions (if any) of a product. An enterprise admin must view these permissions and accept them on behalf of their organization in order to approve that product. Admins should accept the displayed permissions by interacting with a separate UI element in the EMM console, which in turn should trigger the use of this URL as the `approvalUrlInfo`.`approvalUrl` property in a Products.approve call to approve the product. This URL can only be used to display permissions for up to 1 day. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_products_generate_approval_url_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_products_generate_approval_url_task()`.
/// For the simplest API, use `androidenterprise_products_generate_approval_url()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_products_generate_approval_url_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_products_generate_approval_url_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ProductsGenerateApprovalUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_products_generate_approval_url_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_products_generate_approval_url`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseProductsGenerateApprovalUrlArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: productId
    pub productId: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/generateApprovalUrl
/// Generates a URL that can be rendered in an iframe to display the permissions (if any) of a product. An enterprise admin must view these permissions and accept them on behalf of their organization in order to approve that product. Admins should accept the displayed permissions by interacting with a separate UI element in the EMM console, which in turn should trigger the use of this URL as the `approvalUrlInfo`.`approvalUrl` property in a Products.approve call to approve the product. This URL can only be used to display permissions for up to 1 day. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_products_generate_approval_url_builder()` + `androidenterprise_products_generate_approval_url_execute()`.
/// For task-level control, use `androidenterprise_products_generate_approval_url_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_generate_approval_url(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseProductsGenerateApprovalUrlArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ProductsGenerateApprovalUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_products_generate_approval_url_builder(
        client,
        &args.enterpriseId,
        &args.productId,
        &args.languageCode,
    )?;
    androidenterprise_products_generate_approval_url_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}
/// Retrieves details of a product for display to an enterprise admin.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_products_get_execute()` to send, or `androidenterprise_products_get` for simplest API.

pub fn androidenterprise_products_get_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    productId: &String,
    language: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/products/{}",
        enterpriseId, productId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language.as_ref() {
        query_parts.push(format!("language={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}
/// Retrieves details of a product for display to an enterprise admin.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_products_get_execute()` or `androidenterprise_products_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_products_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Product>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Product = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}
/// Retrieves details of a product for display to an enterprise admin.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_products_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_products_get_task()`.
/// For the simplest API, use `androidenterprise_products_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_products_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_products_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_products_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_products_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseProductsGetArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: productId
    pub productId: String,
    /// Query parameter: language
    pub language: Option<String>,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}
/// Retrieves details of a product for display to an enterprise admin.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_products_get_builder()` + `androidenterprise_products_get_execute()`.
/// For task-level control, use `androidenterprise_products_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_get(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseProductsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_products_get_builder(
        client,
        &args.enterpriseId,
        &args.productId,
        &args.language,
    )?;
    androidenterprise_products_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/appRestrictionsSchema
/// Retrieves the schema that defines the configurable properties for this product. All products have a schema, but this schema may be empty if no managed configurations have been defined. This schema can be used to populate a UI that allows an admin to configure the product. To apply a managed configuration based on the schema obtained using this API, see Managed Configurations through Play.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_products_get_app_restrictions_schema_execute()` to send, or `androidenterprise_products_get_app_restrictions_schema` for simplest API.

pub fn androidenterprise_products_get_app_restrictions_schema_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    productId: &String,
    language: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/products/{}/appRestrictionsSchema",
        enterpriseId,
        productId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language.as_ref() {
        query_parts.push(format!("language={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/appRestrictionsSchema
/// Retrieves the schema that defines the configurable properties for this product. All products have a schema, but this schema may be empty if no managed configurations have been defined. This schema can be used to populate a UI that allows an admin to configure the product. To apply a managed configuration based on the schema obtained using this API, see Managed Configurations through Play.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_products_get_app_restrictions_schema_execute()` or `androidenterprise_products_get_app_restrictions_schema`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_products_get_app_restrictions_schema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_get_app_restrictions_schema_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AppRestrictionsSchema>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppRestrictionsSchema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/appRestrictionsSchema
/// Retrieves the schema that defines the configurable properties for this product. All products have a schema, but this schema may be empty if no managed configurations have been defined. This schema can be used to populate a UI that allows an admin to configure the product. To apply a managed configuration based on the schema obtained using this API, see Managed Configurations through Play.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_products_get_app_restrictions_schema_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_products_get_app_restrictions_schema_task()`.
/// For the simplest API, use `androidenterprise_products_get_app_restrictions_schema()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_products_get_app_restrictions_schema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_products_get_app_restrictions_schema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppRestrictionsSchema>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_products_get_app_restrictions_schema_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_products_get_app_restrictions_schema`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseProductsGetAppRestrictionsSchemaArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: productId
    pub productId: String,
    /// Query parameter: language
    pub language: Option<String>,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/appRestrictionsSchema
/// Retrieves the schema that defines the configurable properties for this product. All products have a schema, but this schema may be empty if no managed configurations have been defined. This schema can be used to populate a UI that allows an admin to configure the product. To apply a managed configuration based on the schema obtained using this API, see Managed Configurations through Play.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_products_get_app_restrictions_schema_builder()` + `androidenterprise_products_get_app_restrictions_schema_execute()`.
/// For task-level control, use `androidenterprise_products_get_app_restrictions_schema_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_get_app_restrictions_schema(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseProductsGetAppRestrictionsSchemaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppRestrictionsSchema>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_products_get_app_restrictions_schema_builder(
        client,
        &args.enterpriseId,
        &args.productId,
        &args.language,
    )?;
    androidenterprise_products_get_app_restrictions_schema_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/permissions
/// Retrieves the Android app permissions required by this app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_products_get_permissions_execute()` to send, or `androidenterprise_products_get_permissions` for simplest API.

pub fn androidenterprise_products_get_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    productId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/products/{}/permissions",
        enterpriseId,
        productId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/permissions
/// Retrieves the Android app permissions required by this app.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_products_get_permissions_execute()` or `androidenterprise_products_get_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_products_get_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_get_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductPermissions>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductPermissions = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/permissions
/// Retrieves the Android app permissions required by this app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_products_get_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_products_get_permissions_task()`.
/// For the simplest API, use `androidenterprise_products_get_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_products_get_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_products_get_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductPermissions>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_products_get_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_products_get_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseProductsGetPermissionsArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: productId
    pub productId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/permissions
/// Retrieves the Android app permissions required by this app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_products_get_permissions_builder()` + `androidenterprise_products_get_permissions_execute()`.
/// For task-level control, use `androidenterprise_products_get_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_get_permissions(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseProductsGetPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductPermissions>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_products_get_permissions_builder(
        client,
        &args.enterpriseId,
        &args.productId,
    )?;
    androidenterprise_products_get_permissions_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products
/// Finds approved products that match a query, or all approved products if there is no query. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_products_list_execute()` to send, or `androidenterprise_products_list` for simplest API.

pub fn androidenterprise_products_list_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    approved: &Option<String>,
    language: &Option<String>,
    maxResults: &Option<String>,
    query: &Option<String>,
    token: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/products",
        enterpriseId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = approved.as_ref() {
        query_parts.push(format!("approved={}", val));
    }
    if let Some(val) = language.as_ref() {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }
    if let Some(val) = token.as_ref() {
        query_parts.push(format!("token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products
/// Finds approved products that match a query, or all approved products if there is no query. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_products_list_execute()` or `androidenterprise_products_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_products_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products
/// Finds approved products that match a query, or all approved products if there is no query. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_products_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_products_list_task()`.
/// For the simplest API, use `androidenterprise_products_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_products_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_products_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_products_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_products_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseProductsListArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Query parameter: approved
    pub approved: Option<String>,
    /// Query parameter: language
    pub language: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
    /// Query parameter: token
    pub token: Option<String>,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/products
/// Finds approved products that match a query, or all approved products if there is no query. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_products_list_builder()` + `androidenterprise_products_list_execute()`.
/// For task-level control, use `androidenterprise_products_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_list(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseProductsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_products_list_builder(
        client,
        &args.enterpriseId,
        &args.approved,
        &args.language,
        &args.maxResults,
        &args.query,
        &args.token,
    )?;
    androidenterprise_products_list_execute(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/unapprove
/// Unapproves the specified product (and the relevant app permissions, if any) **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_products_unapprove_execute()` to send, or `androidenterprise_products_unapprove` for simplest API.

pub fn androidenterprise_products_unapprove_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    productId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/products/{}/unapprove",
        enterpriseId,
        productId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/unapprove
/// Unapproves the specified product (and the relevant app permissions, if any) **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_products_unapprove_execute()` or `androidenterprise_products_unapprove`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_products_unapprove_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_unapprove_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/unapprove
/// Unapproves the specified product (and the relevant app permissions, if any) **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_products_unapprove_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_products_unapprove_task()`.
/// For the simplest API, use `androidenterprise_products_unapprove()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_products_unapprove_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_products_unapprove_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_products_unapprove_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_products_unapprove`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseProductsUnapproveArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: productId
    pub productId: String,
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/products/{productId}/unapprove
/// Unapproves the specified product (and the relevant app permissions, if any) **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_products_unapprove_builder()` + `androidenterprise_products_unapprove_execute()`.
/// For task-level control, use `androidenterprise_products_unapprove_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_products_unapprove(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseProductsUnapproveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_products_unapprove_builder(client, &args.enterpriseId, &args.productId)?;
    androidenterprise_products_unapprove_execute(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys/{keyId}
/// Removes and invalidates the specified credentials for the service account associated with this enterprise. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_serviceaccountkeys_delete_execute()` to send, or `androidenterprise_serviceaccountkeys_delete` for simplest API.

pub fn androidenterprise_serviceaccountkeys_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    keyId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/serviceAccountKeys/{}",
        enterpriseId,
        keyId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys/{keyId}
/// Removes and invalidates the specified credentials for the service account associated with this enterprise. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_serviceaccountkeys_delete_execute()` or `androidenterprise_serviceaccountkeys_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_serviceaccountkeys_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_serviceaccountkeys_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys/{keyId}
/// Removes and invalidates the specified credentials for the service account associated with this enterprise. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_serviceaccountkeys_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_serviceaccountkeys_delete_task()`.
/// For the simplest API, use `androidenterprise_serviceaccountkeys_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_serviceaccountkeys_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_serviceaccountkeys_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_serviceaccountkeys_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_serviceaccountkeys_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseServiceaccountkeysDeleteArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: keyId
    pub keyId: String,
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys/{keyId}
/// Removes and invalidates the specified credentials for the service account associated with this enterprise. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_serviceaccountkeys_delete_builder()` + `androidenterprise_serviceaccountkeys_delete_execute()`.
/// For task-level control, use `androidenterprise_serviceaccountkeys_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_serviceaccountkeys_delete(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseServiceaccountkeysDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_serviceaccountkeys_delete_builder(
        client,
        &args.enterpriseId,
        &args.keyId,
    )?;
    androidenterprise_serviceaccountkeys_delete_execute(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys
/// Generates new credentials for the service account associated with this enterprise. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount. Only the type of the key should be populated in the resource to be inserted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_serviceaccountkeys_insert_execute()` to send, or `androidenterprise_serviceaccountkeys_insert` for simplest API.

pub fn androidenterprise_serviceaccountkeys_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/serviceAccountKeys",
        enterpriseId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys
/// Generates new credentials for the service account associated with this enterprise. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount. Only the type of the key should be populated in the resource to be inserted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_serviceaccountkeys_insert_execute()` or `androidenterprise_serviceaccountkeys_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_serviceaccountkeys_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_serviceaccountkeys_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceAccountKey>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceAccountKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys
/// Generates new credentials for the service account associated with this enterprise. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount. Only the type of the key should be populated in the resource to be inserted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_serviceaccountkeys_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_serviceaccountkeys_insert_task()`.
/// For the simplest API, use `androidenterprise_serviceaccountkeys_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_serviceaccountkeys_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_serviceaccountkeys_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceAccountKey>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_serviceaccountkeys_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_serviceaccountkeys_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseServiceaccountkeysInsertArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys
/// Generates new credentials for the service account associated with this enterprise. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount. Only the type of the key should be populated in the resource to be inserted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_serviceaccountkeys_insert_builder()` + `androidenterprise_serviceaccountkeys_insert_execute()`.
/// For task-level control, use `androidenterprise_serviceaccountkeys_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_serviceaccountkeys_insert(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseServiceaccountkeysInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceAccountKey>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_serviceaccountkeys_insert_builder(client, &args.enterpriseId)?;
    androidenterprise_serviceaccountkeys_insert_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys
/// Lists all active credentials for the service account associated with this enterprise. Only the ID and key type are returned. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_serviceaccountkeys_list_execute()` to send, or `androidenterprise_serviceaccountkeys_list` for simplest API.

pub fn androidenterprise_serviceaccountkeys_list_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/serviceAccountKeys",
        enterpriseId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys
/// Lists all active credentials for the service account associated with this enterprise. Only the ID and key type are returned. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_serviceaccountkeys_list_execute()` or `androidenterprise_serviceaccountkeys_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_serviceaccountkeys_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_serviceaccountkeys_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceAccountKeysListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceAccountKeysListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys
/// Lists all active credentials for the service account associated with this enterprise. Only the ID and key type are returned. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_serviceaccountkeys_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_serviceaccountkeys_list_task()`.
/// For the simplest API, use `androidenterprise_serviceaccountkeys_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_serviceaccountkeys_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_serviceaccountkeys_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ServiceAccountKeysListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_serviceaccountkeys_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_serviceaccountkeys_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseServiceaccountkeysListArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/serviceAccountKeys
/// Lists all active credentials for the service account associated with this enterprise. Only the ID and key type are returned. The calling service account must have been retrieved by calling Enterprises.GetServiceAccount and must have been set as the enterprise service account by calling Enterprises.SetAccount.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_serviceaccountkeys_list_builder()` + `androidenterprise_serviceaccountkeys_list_execute()`.
/// For task-level control, use `androidenterprise_serviceaccountkeys_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_serviceaccountkeys_list(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseServiceaccountkeysListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ServiceAccountKeysListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_serviceaccountkeys_list_builder(client, &args.enterpriseId)?;
    androidenterprise_serviceaccountkeys_list_execute(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Deletes a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutclusters_delete_execute()` to send, or `androidenterprise_storelayoutclusters_delete` for simplest API.

pub fn androidenterprise_storelayoutclusters_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    pageId: &String,
    clusterId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/storeLayout/pages/{}/clusters/{}",
        enterpriseId,
        pageId,
        clusterId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Deletes a cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_storelayoutclusters_delete_execute()` or `androidenterprise_storelayoutclusters_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutclusters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Deletes a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_storelayoutclusters_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_storelayoutclusters_delete_task()`.
/// For the simplest API, use `androidenterprise_storelayoutclusters_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutclusters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_storelayoutclusters_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_storelayoutclusters_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_storelayoutclusters_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseStorelayoutclustersDeleteArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: pageId
    pub pageId: String,
    /// Path parameter: clusterId
    pub clusterId: String,
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Deletes a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutclusters_delete_builder()` + `androidenterprise_storelayoutclusters_delete_execute()`.
/// For task-level control, use `androidenterprise_storelayoutclusters_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_delete(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseStorelayoutclustersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_storelayoutclusters_delete_builder(
        client,
        &args.enterpriseId,
        &args.pageId,
        &args.clusterId,
    )?;
    androidenterprise_storelayoutclusters_delete_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Retrieves details of a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutclusters_get_execute()` to send, or `androidenterprise_storelayoutclusters_get` for simplest API.

pub fn androidenterprise_storelayoutclusters_get_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    pageId: &String,
    clusterId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/storeLayout/pages/{}/clusters/{}",
        enterpriseId,
        pageId,
        clusterId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Retrieves details of a cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_storelayoutclusters_get_execute()` or `androidenterprise_storelayoutclusters_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutclusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoreCluster>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoreCluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Retrieves details of a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_storelayoutclusters_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_storelayoutclusters_get_task()`.
/// For the simplest API, use `androidenterprise_storelayoutclusters_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutclusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_storelayoutclusters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_storelayoutclusters_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_storelayoutclusters_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseStorelayoutclustersGetArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: pageId
    pub pageId: String,
    /// Path parameter: clusterId
    pub clusterId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Retrieves details of a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutclusters_get_builder()` + `androidenterprise_storelayoutclusters_get_execute()`.
/// For task-level control, use `androidenterprise_storelayoutclusters_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_get(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseStorelayoutclustersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_storelayoutclusters_get_builder(
        client,
        &args.enterpriseId,
        &args.pageId,
        &args.clusterId,
    )?;
    androidenterprise_storelayoutclusters_get_execute(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters
/// Inserts a new cluster in a page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutclusters_insert_execute()` to send, or `androidenterprise_storelayoutclusters_insert` for simplest API.

pub fn androidenterprise_storelayoutclusters_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    pageId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/storeLayout/pages/{}/clusters",
        enterpriseId,
        pageId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters
/// Inserts a new cluster in a page.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_storelayoutclusters_insert_execute()` or `androidenterprise_storelayoutclusters_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutclusters_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoreCluster>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoreCluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters
/// Inserts a new cluster in a page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_storelayoutclusters_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_storelayoutclusters_insert_task()`.
/// For the simplest API, use `androidenterprise_storelayoutclusters_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutclusters_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_storelayoutclusters_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_storelayoutclusters_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_storelayoutclusters_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseStorelayoutclustersInsertArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: pageId
    pub pageId: String,
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters
/// Inserts a new cluster in a page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutclusters_insert_builder()` + `androidenterprise_storelayoutclusters_insert_execute()`.
/// For task-level control, use `androidenterprise_storelayoutclusters_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_insert(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseStorelayoutclustersInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_storelayoutclusters_insert_builder(
        client,
        &args.enterpriseId,
        &args.pageId,
    )?;
    androidenterprise_storelayoutclusters_insert_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters
/// Retrieves the details of all clusters on the specified page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutclusters_list_execute()` to send, or `androidenterprise_storelayoutclusters_list` for simplest API.

pub fn androidenterprise_storelayoutclusters_list_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    pageId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/storeLayout/pages/{}/clusters",
        enterpriseId,
        pageId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters
/// Retrieves the details of all clusters on the specified page.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_storelayoutclusters_list_execute()` or `androidenterprise_storelayoutclusters_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutclusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoreLayoutClustersListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoreLayoutClustersListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters
/// Retrieves the details of all clusters on the specified page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_storelayoutclusters_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_storelayoutclusters_list_task()`.
/// For the simplest API, use `androidenterprise_storelayoutclusters_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutclusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_storelayoutclusters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StoreLayoutClustersListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_storelayoutclusters_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_storelayoutclusters_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseStorelayoutclustersListArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: pageId
    pub pageId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters
/// Retrieves the details of all clusters on the specified page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutclusters_list_builder()` + `androidenterprise_storelayoutclusters_list_execute()`.
/// For task-level control, use `androidenterprise_storelayoutclusters_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_list(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseStorelayoutclustersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StoreLayoutClustersListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_storelayoutclusters_list_builder(
        client,
        &args.enterpriseId,
        &args.pageId,
    )?;
    androidenterprise_storelayoutclusters_list_execute(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Updates a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutclusters_update_execute()` to send, or `androidenterprise_storelayoutclusters_update` for simplest API.

pub fn androidenterprise_storelayoutclusters_update_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    pageId: &String,
    clusterId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/storeLayout/pages/{}/clusters/{}",
        enterpriseId,
        pageId,
        clusterId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Updates a cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_storelayoutclusters_update_execute()` or `androidenterprise_storelayoutclusters_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutclusters_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoreCluster>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoreCluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Updates a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_storelayoutclusters_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_storelayoutclusters_update_task()`.
/// For the simplest API, use `androidenterprise_storelayoutclusters_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutclusters_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_storelayoutclusters_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_storelayoutclusters_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_storelayoutclusters_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseStorelayoutclustersUpdateArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: pageId
    pub pageId: String,
    /// Path parameter: clusterId
    pub clusterId: String,
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}/clusters/{clusterId}
/// Updates a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutclusters_update_builder()` + `androidenterprise_storelayoutclusters_update_execute()`.
/// For task-level control, use `androidenterprise_storelayoutclusters_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutclusters_update(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseStorelayoutclustersUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoreCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_storelayoutclusters_update_builder(
        client,
        &args.enterpriseId,
        &args.pageId,
        &args.clusterId,
    )?;
    androidenterprise_storelayoutclusters_update_execute(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Deletes a store page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutpages_delete_execute()` to send, or `androidenterprise_storelayoutpages_delete` for simplest API.

pub fn androidenterprise_storelayoutpages_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    pageId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/storeLayout/pages/{}",
        enterpriseId,
        pageId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Deletes a store page.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_storelayoutpages_delete_execute()` or `androidenterprise_storelayoutpages_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutpages_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Deletes a store page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_storelayoutpages_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_storelayoutpages_delete_task()`.
/// For the simplest API, use `androidenterprise_storelayoutpages_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutpages_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_storelayoutpages_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_storelayoutpages_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_storelayoutpages_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseStorelayoutpagesDeleteArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: pageId
    pub pageId: String,
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Deletes a store page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutpages_delete_builder()` + `androidenterprise_storelayoutpages_delete_execute()`.
/// For task-level control, use `androidenterprise_storelayoutpages_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_delete(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseStorelayoutpagesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_storelayoutpages_delete_builder(
        client,
        &args.enterpriseId,
        &args.pageId,
    )?;
    androidenterprise_storelayoutpages_delete_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Retrieves details of a store page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutpages_get_execute()` to send, or `androidenterprise_storelayoutpages_get` for simplest API.

pub fn androidenterprise_storelayoutpages_get_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    pageId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/storeLayout/pages/{}",
        enterpriseId,
        pageId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Retrieves details of a store page.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_storelayoutpages_get_execute()` or `androidenterprise_storelayoutpages_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutpages_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StorePage>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StorePage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Retrieves details of a store page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_storelayoutpages_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_storelayoutpages_get_task()`.
/// For the simplest API, use `androidenterprise_storelayoutpages_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutpages_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_storelayoutpages_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StorePage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_storelayoutpages_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_storelayoutpages_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseStorelayoutpagesGetArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: pageId
    pub pageId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Retrieves details of a store page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutpages_get_builder()` + `androidenterprise_storelayoutpages_get_execute()`.
/// For task-level control, use `androidenterprise_storelayoutpages_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_get(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseStorelayoutpagesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StorePage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_storelayoutpages_get_builder(client, &args.enterpriseId, &args.pageId)?;
    androidenterprise_storelayoutpages_get_execute(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages
/// Inserts a new store page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutpages_insert_execute()` to send, or `androidenterprise_storelayoutpages_insert` for simplest API.

pub fn androidenterprise_storelayoutpages_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/storeLayout/pages",
        enterpriseId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages
/// Inserts a new store page.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_storelayoutpages_insert_execute()` or `androidenterprise_storelayoutpages_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutpages_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StorePage>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StorePage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages
/// Inserts a new store page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_storelayoutpages_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_storelayoutpages_insert_task()`.
/// For the simplest API, use `androidenterprise_storelayoutpages_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutpages_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_storelayoutpages_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StorePage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_storelayoutpages_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_storelayoutpages_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseStorelayoutpagesInsertArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages
/// Inserts a new store page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutpages_insert_builder()` + `androidenterprise_storelayoutpages_insert_execute()`.
/// For task-level control, use `androidenterprise_storelayoutpages_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_insert(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseStorelayoutpagesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StorePage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_storelayoutpages_insert_builder(client, &args.enterpriseId)?;
    androidenterprise_storelayoutpages_insert_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages
/// Retrieves the details of all pages in the store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutpages_list_execute()` to send, or `androidenterprise_storelayoutpages_list` for simplest API.

pub fn androidenterprise_storelayoutpages_list_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/storeLayout/pages",
        enterpriseId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages
/// Retrieves the details of all pages in the store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_storelayoutpages_list_execute()` or `androidenterprise_storelayoutpages_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutpages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoreLayoutPagesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoreLayoutPagesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages
/// Retrieves the details of all pages in the store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_storelayoutpages_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_storelayoutpages_list_task()`.
/// For the simplest API, use `androidenterprise_storelayoutpages_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutpages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_storelayoutpages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StoreLayoutPagesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_storelayoutpages_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_storelayoutpages_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseStorelayoutpagesListArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages
/// Retrieves the details of all pages in the store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutpages_list_builder()` + `androidenterprise_storelayoutpages_list_execute()`.
/// For task-level control, use `androidenterprise_storelayoutpages_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_list(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseStorelayoutpagesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StoreLayoutPagesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_storelayoutpages_list_builder(client, &args.enterpriseId)?;
    androidenterprise_storelayoutpages_list_execute(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Updates the content of a store page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_storelayoutpages_update_execute()` to send, or `androidenterprise_storelayoutpages_update` for simplest API.

pub fn androidenterprise_storelayoutpages_update_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    pageId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/storeLayout/pages/{}",
        enterpriseId,
        pageId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Updates the content of a store page.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_storelayoutpages_update_execute()` or `androidenterprise_storelayoutpages_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutpages_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StorePage>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StorePage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Updates the content of a store page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_storelayoutpages_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_storelayoutpages_update_task()`.
/// For the simplest API, use `androidenterprise_storelayoutpages_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_storelayoutpages_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_storelayoutpages_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StorePage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_storelayoutpages_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_storelayoutpages_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseStorelayoutpagesUpdateArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: pageId
    pub pageId: String,
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/storeLayout/pages/{pageId}
/// Updates the content of a store page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_storelayoutpages_update_builder()` + `androidenterprise_storelayoutpages_update_execute()`.
/// For task-level control, use `androidenterprise_storelayoutpages_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_storelayoutpages_update(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseStorelayoutpagesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StorePage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_storelayoutpages_update_builder(
        client,
        &args.enterpriseId,
        &args.pageId,
    )?;
    androidenterprise_storelayoutpages_update_execute(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Deleted an EMM-managed user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_delete_execute()` to send, or `androidenterprise_users_delete` for simplest API.

pub fn androidenterprise_users_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}",
        enterpriseId, userId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Deleted an EMM-managed user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_users_delete_execute()` or `androidenterprise_users_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Deleted an EMM-managed user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_users_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_users_delete_task()`.
/// For the simplest API, use `androidenterprise_users_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_users_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_users_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_users_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseUsersDeleteArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Deleted an EMM-managed user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_delete_builder()` + `androidenterprise_users_delete_execute()`.
/// For task-level control, use `androidenterprise_users_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_delete(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseUsersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_users_delete_builder(client, &args.enterpriseId, &args.userId)?;
    androidenterprise_users_delete_execute(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/authenticationToken
/// Generates an authentication token which the device policy client can use to provision the given EMM-managed user account on a device. The generated token is single-use and expires after a few minutes. You can provision a maximum of 10 devices per user. This call only works with EMM-managed accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_generate_authentication_token_execute()` to send, or `androidenterprise_users_generate_authentication_token` for simplest API.

pub fn androidenterprise_users_generate_authentication_token_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/authenticationToken",
        enterpriseId,
        userId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/authenticationToken
/// Generates an authentication token which the device policy client can use to provision the given EMM-managed user account on a device. The generated token is single-use and expires after a few minutes. You can provision a maximum of 10 devices per user. This call only works with EMM-managed accounts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_users_generate_authentication_token_execute()` or `androidenterprise_users_generate_authentication_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_generate_authentication_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_generate_authentication_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AuthenticationToken>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuthenticationToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/authenticationToken
/// Generates an authentication token which the device policy client can use to provision the given EMM-managed user account on a device. The generated token is single-use and expires after a few minutes. You can provision a maximum of 10 devices per user. This call only works with EMM-managed accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_users_generate_authentication_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_users_generate_authentication_token_task()`.
/// For the simplest API, use `androidenterprise_users_generate_authentication_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_generate_authentication_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_users_generate_authentication_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthenticationToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_users_generate_authentication_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_users_generate_authentication_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseUsersGenerateAuthenticationTokenArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/authenticationToken
/// Generates an authentication token which the device policy client can use to provision the given EMM-managed user account on a device. The generated token is single-use and expires after a few minutes. You can provision a maximum of 10 devices per user. This call only works with EMM-managed accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_generate_authentication_token_builder()` + `androidenterprise_users_generate_authentication_token_execute()`.
/// For task-level control, use `androidenterprise_users_generate_authentication_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_generate_authentication_token(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseUsersGenerateAuthenticationTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthenticationToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_users_generate_authentication_token_builder(
        client,
        &args.enterpriseId,
        &args.userId,
    )?;
    androidenterprise_users_generate_authentication_token_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Retrieves a user''s details.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_get_execute()` to send, or `androidenterprise_users_get` for simplest API.

pub fn androidenterprise_users_get_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}",
        enterpriseId, userId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Retrieves a user''s details.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_users_get_execute()` or `androidenterprise_users_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<User>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: User = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Retrieves a user''s details.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_users_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_users_get_task()`.
/// For the simplest API, use `androidenterprise_users_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_users_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_users_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_users_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseUsersGetArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Retrieves a user''s details.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_get_builder()` + `androidenterprise_users_get_execute()`.
/// For task-level control, use `androidenterprise_users_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_get(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseUsersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_users_get_builder(client, &args.enterpriseId, &args.userId)?;
    androidenterprise_users_get_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/availableProductSet
/// Retrieves the set of products a user is entitled to access. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_get_available_product_set_execute()` to send, or `androidenterprise_users_get_available_product_set` for simplest API.

pub fn androidenterprise_users_get_available_product_set_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/availableProductSet",
        enterpriseId,
        userId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/availableProductSet
/// Retrieves the set of products a user is entitled to access. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_users_get_available_product_set_execute()` or `androidenterprise_users_get_available_product_set`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_get_available_product_set_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_get_available_product_set_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductSet>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/availableProductSet
/// Retrieves the set of products a user is entitled to access. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_users_get_available_product_set_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_users_get_available_product_set_task()`.
/// For the simplest API, use `androidenterprise_users_get_available_product_set()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_get_available_product_set_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_users_get_available_product_set_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_users_get_available_product_set_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_users_get_available_product_set`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseUsersGetAvailableProductSetArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/availableProductSet
/// Retrieves the set of products a user is entitled to access. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_get_available_product_set_builder()` + `androidenterprise_users_get_available_product_set_execute()`.
/// For task-level control, use `androidenterprise_users_get_available_product_set_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_get_available_product_set(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseUsersGetAvailableProductSetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_users_get_available_product_set_builder(
        client,
        &args.enterpriseId,
        &args.userId,
    )?;
    androidenterprise_users_get_available_product_set_execute(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/users
/// Creates a new EMM-managed user. The Users resource passed in the body of the request should include an `accountIdentifier` and an `accountType`. If a corresponding user already exists with the same account identifier, the user will be updated with the resource. In this case only the `displayName` field can be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_insert_execute()` to send, or `androidenterprise_users_insert` for simplest API.

pub fn androidenterprise_users_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users",
        enterpriseId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/users
/// Creates a new EMM-managed user. The Users resource passed in the body of the request should include an `accountIdentifier` and an `accountType`. If a corresponding user already exists with the same account identifier, the user will be updated with the resource. In this case only the `displayName` field can be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_users_insert_execute()` or `androidenterprise_users_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<User>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: User = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/users
/// Creates a new EMM-managed user. The Users resource passed in the body of the request should include an `accountIdentifier` and an `accountType`. If a corresponding user already exists with the same account identifier, the user will be updated with the resource. In this case only the `displayName` field can be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_users_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_users_insert_task()`.
/// For the simplest API, use `androidenterprise_users_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_users_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_users_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_users_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseUsersInsertArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/users
/// Creates a new EMM-managed user. The Users resource passed in the body of the request should include an `accountIdentifier` and an `accountType`. If a corresponding user already exists with the same account identifier, the user will be updated with the resource. In this case only the `displayName` field can be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_insert_builder()` + `androidenterprise_users_insert_execute()`.
/// For task-level control, use `androidenterprise_users_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_insert(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseUsersInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_users_insert_builder(client, &args.enterpriseId)?;
    androidenterprise_users_insert_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users
/// Looks up a user by primary email address. This is only supported for Google-managed users. Lookup of the id is not needed for EMM-managed users because the id is already returned in the result of the Users.insert call.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_list_execute()` to send, or `androidenterprise_users_list` for simplest API.

pub fn androidenterprise_users_list_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    email: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users",
        enterpriseId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = email.as_ref() {
        query_parts.push(format!("email={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users
/// Looks up a user by primary email address. This is only supported for Google-managed users. Lookup of the id is not needed for EMM-managed users because the id is already returned in the result of the Users.insert call.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_users_list_execute()` or `androidenterprise_users_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UsersListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UsersListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users
/// Looks up a user by primary email address. This is only supported for Google-managed users. Lookup of the id is not needed for EMM-managed users because the id is already returned in the result of the Users.insert call.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_users_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_users_list_task()`.
/// For the simplest API, use `androidenterprise_users_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_users_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsersListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_users_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_users_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseUsersListArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Query parameter: email
    pub email: Option<String>,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users
/// Looks up a user by primary email address. This is only supported for Google-managed users. Lookup of the id is not needed for EMM-managed users because the id is already returned in the result of the Users.insert call.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_list_builder()` + `androidenterprise_users_list_execute()`.
/// For task-level control, use `androidenterprise_users_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_list(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseUsersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsersListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_users_list_builder(client, &args.enterpriseId, &args.email)?;
    androidenterprise_users_list_execute(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/deviceAccess
/// Revokes access to all devices currently provisioned to the user. The user will no longer be able to use the managed Play store on any of their managed devices. This call only works with EMM-managed accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_revoke_device_access_execute()` to send, or `androidenterprise_users_revoke_device_access` for simplest API.

pub fn androidenterprise_users_revoke_device_access_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/deviceAccess",
        enterpriseId,
        userId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/deviceAccess
/// Revokes access to all devices currently provisioned to the user. The user will no longer be able to use the managed Play store on any of their managed devices. This call only works with EMM-managed accounts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_users_revoke_device_access_execute()` or `androidenterprise_users_revoke_device_access`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_revoke_device_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_revoke_device_access_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/deviceAccess
/// Revokes access to all devices currently provisioned to the user. The user will no longer be able to use the managed Play store on any of their managed devices. This call only works with EMM-managed accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_users_revoke_device_access_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_users_revoke_device_access_task()`.
/// For the simplest API, use `androidenterprise_users_revoke_device_access()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_revoke_device_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_users_revoke_device_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_users_revoke_device_access_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_users_revoke_device_access`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseUsersRevokeDeviceAccessArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/deviceAccess
/// Revokes access to all devices currently provisioned to the user. The user will no longer be able to use the managed Play store on any of their managed devices. This call only works with EMM-managed accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_revoke_device_access_builder()` + `androidenterprise_users_revoke_device_access_execute()`.
/// For task-level control, use `androidenterprise_users_revoke_device_access_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_revoke_device_access(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseUsersRevokeDeviceAccessArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_users_revoke_device_access_builder(
        client,
        &args.enterpriseId,
        &args.userId,
    )?;
    androidenterprise_users_revoke_device_access_execute(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/availableProductSet
/// Modifies the set of products that a user is entitled to access (referred to as *whitelisted* products). Only products that are approved or products that were previously approved (products with revoked approval) can be whitelisted. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_set_available_product_set_execute()` to send, or `androidenterprise_users_set_available_product_set` for simplest API.

pub fn androidenterprise_users_set_available_product_set_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/availableProductSet",
        enterpriseId,
        userId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/availableProductSet
/// Modifies the set of products that a user is entitled to access (referred to as *whitelisted* products). Only products that are approved or products that were previously approved (products with revoked approval) can be whitelisted. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_users_set_available_product_set_execute()` or `androidenterprise_users_set_available_product_set`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_set_available_product_set_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_set_available_product_set_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductSet>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/availableProductSet
/// Modifies the set of products that a user is entitled to access (referred to as *whitelisted* products). Only products that are approved or products that were previously approved (products with revoked approval) can be whitelisted. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_users_set_available_product_set_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_users_set_available_product_set_task()`.
/// For the simplest API, use `androidenterprise_users_set_available_product_set()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_set_available_product_set_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_users_set_available_product_set_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_users_set_available_product_set_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_users_set_available_product_set`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseUsersSetAvailableProductSetArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/availableProductSet
/// Modifies the set of products that a user is entitled to access (referred to as *whitelisted* products). Only products that are approved or products that were previously approved (products with revoked approval) can be whitelisted. **Note:** This item has been deprecated. New integrations cannot use this method and can refer to our new recommendations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_set_available_product_set_builder()` + `androidenterprise_users_set_available_product_set_execute()`.
/// For task-level control, use `androidenterprise_users_set_available_product_set_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_set_available_product_set(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseUsersSetAvailableProductSetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_users_set_available_product_set_builder(
        client,
        &args.enterpriseId,
        &args.userId,
    )?;
    androidenterprise_users_set_available_product_set_execute(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Updates the details of an EMM-managed user. Can be used with EMM-managed users only (not Google managed users). Pass the new details in the Users resource in the request body. Only the `displayName` field can be changed. Other fields must either be unset or have the currently active value.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_users_update_execute()` to send, or `androidenterprise_users_update` for simplest API.

pub fn androidenterprise_users_update_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    userId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}",
        enterpriseId, userId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Updates the details of an EMM-managed user. Can be used with EMM-managed users only (not Google managed users). Pass the new details in the Users resource in the request body. Only the `displayName` field can be changed. Other fields must either be unset or have the currently active value.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_users_update_execute()` or `androidenterprise_users_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<User>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: User = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Updates the details of an EMM-managed user. Can be used with EMM-managed users only (not Google managed users). Pass the new details in the Users resource in the request body. Only the `displayName` field can be changed. Other fields must either be unset or have the currently active value.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_users_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_users_update_task()`.
/// For the simplest API, use `androidenterprise_users_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_users_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_users_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_users_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_users_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseUsersUpdateArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: userId
    pub userId: String,
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}
/// Updates the details of an EMM-managed user. Can be used with EMM-managed users only (not Google managed users). Pass the new details in the Users resource in the request body. Only the `displayName` field can be changed. Other fields must either be unset or have the currently active value.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_users_update_builder()` + `androidenterprise_users_update_execute()`.
/// For task-level control, use `androidenterprise_users_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_users_update(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseUsersUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_users_update_builder(client, &args.enterpriseId, &args.userId)?;
    androidenterprise_users_update_execute(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Deletes an existing web app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_webapps_delete_execute()` to send, or `androidenterprise_webapps_delete` for simplest API.

pub fn androidenterprise_webapps_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    webAppId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/webApps/{}",
        enterpriseId, webAppId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Deletes an existing web app.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_webapps_delete_execute()` or `androidenterprise_webapps_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_webapps_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Deletes an existing web app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_webapps_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_webapps_delete_task()`.
/// For the simplest API, use `androidenterprise_webapps_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_webapps_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_webapps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_webapps_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_webapps_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseWebappsDeleteArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: webAppId
    pub webAppId: String,
}

/// DELETE androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Deletes an existing web app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_webapps_delete_builder()` + `androidenterprise_webapps_delete_execute()`.
/// For task-level control, use `androidenterprise_webapps_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_delete(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseWebappsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_webapps_delete_builder(client, &args.enterpriseId, &args.webAppId)?;
    androidenterprise_webapps_delete_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Gets an existing web app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_webapps_get_execute()` to send, or `androidenterprise_webapps_get` for simplest API.

pub fn androidenterprise_webapps_get_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    webAppId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/webApps/{}",
        enterpriseId, webAppId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Gets an existing web app.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_webapps_get_execute()` or `androidenterprise_webapps_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_webapps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WebApp>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Gets an existing web app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_webapps_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_webapps_get_task()`.
/// For the simplest API, use `androidenterprise_webapps_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_webapps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_webapps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_webapps_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_webapps_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseWebappsGetArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: webAppId
    pub webAppId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Gets an existing web app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_webapps_get_builder()` + `androidenterprise_webapps_get_execute()`.
/// For task-level control, use `androidenterprise_webapps_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_get(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseWebappsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_webapps_get_builder(client, &args.enterpriseId, &args.webAppId)?;
    androidenterprise_webapps_get_execute(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/webApps
/// Creates a new web app for the enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_webapps_insert_execute()` to send, or `androidenterprise_webapps_insert` for simplest API.

pub fn androidenterprise_webapps_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/webApps",
        enterpriseId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/webApps
/// Creates a new web app for the enterprise.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_webapps_insert_execute()` or `androidenterprise_webapps_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_webapps_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WebApp>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/webApps
/// Creates a new web app for the enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_webapps_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_webapps_insert_task()`.
/// For the simplest API, use `androidenterprise_webapps_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_webapps_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_webapps_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_webapps_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_webapps_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseWebappsInsertArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
}

/// POST androidenterprise/v1/enterprises/{enterpriseId}/webApps
/// Creates a new web app for the enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_webapps_insert_builder()` + `androidenterprise_webapps_insert_execute()`.
/// For task-level control, use `androidenterprise_webapps_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_insert(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseWebappsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidenterprise_webapps_insert_builder(client, &args.enterpriseId)?;
    androidenterprise_webapps_insert_execute(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps
/// Retrieves the details of all web apps for a given enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_webapps_list_execute()` to send, or `androidenterprise_webapps_list` for simplest API.

pub fn androidenterprise_webapps_list_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/webApps",
        enterpriseId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps
/// Retrieves the details of all web apps for a given enterprise.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_webapps_list_execute()` or `androidenterprise_webapps_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_webapps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WebAppsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebAppsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps
/// Retrieves the details of all web apps for a given enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_webapps_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_webapps_list_task()`.
/// For the simplest API, use `androidenterprise_webapps_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_webapps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_webapps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebAppsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidenterprise_webapps_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_webapps_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseWebappsListArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
}

/// GET androidenterprise/v1/enterprises/{enterpriseId}/webApps
/// Retrieves the details of all web apps for a given enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_webapps_list_builder()` + `androidenterprise_webapps_list_execute()`.
/// For task-level control, use `androidenterprise_webapps_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_list(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseWebappsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebAppsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidenterprise_webapps_list_builder(client, &args.enterpriseId)?;
    androidenterprise_webapps_list_execute(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Updates an existing web app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidenterprise_webapps_update_execute()` to send, or `androidenterprise_webapps_update` for simplest API.

pub fn androidenterprise_webapps_update_builder<R>(
    client: &SimpleHttpClient<R>,
    enterpriseId: &String,
    webAppId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/webApps/{}",
        enterpriseId, webAppId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Updates an existing web app.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidenterprise_webapps_update_execute()` or `androidenterprise_webapps_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_webapps_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WebApp>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Updates an existing web app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidenterprise_webapps_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidenterprise_webapps_update_task()`.
/// For the simplest API, use `androidenterprise_webapps_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidenterprise_webapps_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidenterprise_webapps_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidenterprise_webapps_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidenterprise_webapps_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseWebappsUpdateArgs {
    /// Path parameter: enterpriseId
    pub enterpriseId: String,
    /// Path parameter: webAppId
    pub webAppId: String,
}

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/webApps/{webAppId}
/// Updates an existing web app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidenterprise_webapps_update_builder()` + `androidenterprise_webapps_update_execute()`.
/// For task-level control, use `androidenterprise_webapps_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidenterprise_webapps_update(
    client: &SimpleHttpClient,
    args: &AndroidenterpriseWebappsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidenterprise_webapps_update_builder(client, &args.enterpriseId, &args.webAppId)?;
    androidenterprise_webapps_update_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Device
// =============================================================================

/// ResourceIdentifier implementation for Device with AndroidenterpriseDevicesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseDevicesGetArgs> for Device {
    fn generate_resource_id(&self, input: &AndroidenterpriseDevicesGetArgs) -> String {
        format!(
            "gcp::androidenterprise::Device/{}/{}/{}",
            input.enterpriseId, input.userId, input.deviceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::Device"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DeviceState
// =============================================================================

/// ResourceIdentifier implementation for DeviceState with AndroidenterpriseDevicesGetStateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseDevicesGetStateArgs> for DeviceState {
    fn generate_resource_id(&self, input: &AndroidenterpriseDevicesGetStateArgs) -> String {
        format!(
            "gcp::androidenterprise::DeviceState/{}/{}/{}",
            input.enterpriseId, input.userId, input.deviceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::DeviceState"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DevicesListResponse
// =============================================================================

/// ResourceIdentifier implementation for DevicesListResponse with AndroidenterpriseDevicesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseDevicesListArgs> for DevicesListResponse {
    fn generate_resource_id(&self, input: &AndroidenterpriseDevicesListArgs) -> String {
        format!(
            "gcp::androidenterprise::DevicesListResponse/{}/{}",
            input.enterpriseId, input.userId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::DevicesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DeviceState
// =============================================================================

/// ResourceIdentifier implementation for DeviceState with AndroidenterpriseDevicesSetStateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseDevicesSetStateArgs> for DeviceState {
    fn generate_resource_id(&self, input: &AndroidenterpriseDevicesSetStateArgs) -> String {
        format!(
            "gcp::androidenterprise::DeviceState/{}/{}/{}",
            input.enterpriseId, input.userId, input.deviceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::DeviceState"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Device
// =============================================================================

/// ResourceIdentifier implementation for Device with AndroidenterpriseDevicesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseDevicesUpdateArgs> for Device {
    fn generate_resource_id(&self, input: &AndroidenterpriseDevicesUpdateArgs) -> String {
        format!(
            "gcp::androidenterprise::Device/{}/{}/{}",
            input.enterpriseId, input.userId, input.deviceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::Device"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EnrollmentToken
// =============================================================================

/// ResourceIdentifier implementation for EnrollmentToken with AndroidenterpriseEnrollmentTokensCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseEnrollmentTokensCreateArgs> for EnrollmentToken {
    fn generate_resource_id(&self, input: &AndroidenterpriseEnrollmentTokensCreateArgs) -> String {
        format!(
            "gcp::androidenterprise::EnrollmentToken/{}",
            input.enterpriseId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::EnrollmentToken"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Enterprise
// =============================================================================

/// ResourceIdentifier implementation for Enterprise with AndroidenterpriseEnterprisesCompleteSignupArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseEnterprisesCompleteSignupArgs> for Enterprise {
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseEnterprisesCompleteSignupArgs,
    ) -> String {
        "gcp::androidenterprise::Enterprise".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::Enterprise"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdministratorWebToken
// =============================================================================

/// ResourceIdentifier implementation for AdministratorWebToken with AndroidenterpriseEnterprisesCreateWebTokenArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseEnterprisesCreateWebTokenArgs> for AdministratorWebToken {
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseEnterprisesCreateWebTokenArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::AdministratorWebToken/{}",
            input.enterpriseId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::AdministratorWebToken"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Enterprise
// =============================================================================

/// ResourceIdentifier implementation for Enterprise with AndroidenterpriseEnterprisesEnrollArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseEnterprisesEnrollArgs> for Enterprise {
    fn generate_resource_id(&self, input: &AndroidenterpriseEnterprisesEnrollArgs) -> String {
        "gcp::androidenterprise::Enterprise".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::Enterprise"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GenerateEnterpriseUpgradeUrlResponse
// =============================================================================

/// ResourceIdentifier implementation for GenerateEnterpriseUpgradeUrlResponse with AndroidenterpriseEnterprisesGenerateEnterpriseUpgradeUrlArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseEnterprisesGenerateEnterpriseUpgradeUrlArgs>
    for GenerateEnterpriseUpgradeUrlResponse
{
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseEnterprisesGenerateEnterpriseUpgradeUrlArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::GenerateEnterpriseUpgradeUrlResponse/{}",
            input.enterpriseId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::GenerateEnterpriseUpgradeUrlResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SignupInfo
// =============================================================================

/// ResourceIdentifier implementation for SignupInfo with AndroidenterpriseEnterprisesGenerateSignupUrlArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseEnterprisesGenerateSignupUrlArgs> for SignupInfo {
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseEnterprisesGenerateSignupUrlArgs,
    ) -> String {
        "gcp::androidenterprise::SignupInfo".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::SignupInfo"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Enterprise
// =============================================================================

/// ResourceIdentifier implementation for Enterprise with AndroidenterpriseEnterprisesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseEnterprisesGetArgs> for Enterprise {
    fn generate_resource_id(&self, input: &AndroidenterpriseEnterprisesGetArgs) -> String {
        format!("gcp::androidenterprise::Enterprise/{}", input.enterpriseId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::Enterprise"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ServiceAccount
// =============================================================================

/// ResourceIdentifier implementation for ServiceAccount with AndroidenterpriseEnterprisesGetServiceAccountArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseEnterprisesGetServiceAccountArgs> for ServiceAccount {
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseEnterprisesGetServiceAccountArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::ServiceAccount/{}",
            input.enterpriseId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::ServiceAccount"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StoreLayout
// =============================================================================

/// ResourceIdentifier implementation for StoreLayout with AndroidenterpriseEnterprisesGetStoreLayoutArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseEnterprisesGetStoreLayoutArgs> for StoreLayout {
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseEnterprisesGetStoreLayoutArgs,
    ) -> String {
        format!("gcp::androidenterprise::StoreLayout/{}", input.enterpriseId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::StoreLayout"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EnterprisesListResponse
// =============================================================================

/// ResourceIdentifier implementation for EnterprisesListResponse with AndroidenterpriseEnterprisesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseEnterprisesListArgs> for EnterprisesListResponse {
    fn generate_resource_id(&self, input: &AndroidenterpriseEnterprisesListArgs) -> String {
        "gcp::androidenterprise::EnterprisesListResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::EnterprisesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationSet
// =============================================================================

/// ResourceIdentifier implementation for NotificationSet with AndroidenterpriseEnterprisesPullNotificationSetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseEnterprisesPullNotificationSetArgs> for NotificationSet {
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseEnterprisesPullNotificationSetArgs,
    ) -> String {
        "gcp::androidenterprise::NotificationSet".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::NotificationSet"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EnterprisesSendTestPushNotificationResponse
// =============================================================================

/// ResourceIdentifier implementation for EnterprisesSendTestPushNotificationResponse with AndroidenterpriseEnterprisesSendTestPushNotificationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseEnterprisesSendTestPushNotificationArgs>
    for EnterprisesSendTestPushNotificationResponse
{
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseEnterprisesSendTestPushNotificationArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::EnterprisesSendTestPushNotificationResponse/{}",
            input.enterpriseId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::EnterprisesSendTestPushNotificationResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EnterpriseAccount
// =============================================================================

/// ResourceIdentifier implementation for EnterpriseAccount with AndroidenterpriseEnterprisesSetAccountArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseEnterprisesSetAccountArgs> for EnterpriseAccount {
    fn generate_resource_id(&self, input: &AndroidenterpriseEnterprisesSetAccountArgs) -> String {
        format!(
            "gcp::androidenterprise::EnterpriseAccount/{}",
            input.enterpriseId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::EnterpriseAccount"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StoreLayout
// =============================================================================

/// ResourceIdentifier implementation for StoreLayout with AndroidenterpriseEnterprisesSetStoreLayoutArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseEnterprisesSetStoreLayoutArgs> for StoreLayout {
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseEnterprisesSetStoreLayoutArgs,
    ) -> String {
        format!("gcp::androidenterprise::StoreLayout/{}", input.enterpriseId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::StoreLayout"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Entitlement
// =============================================================================

/// ResourceIdentifier implementation for Entitlement with AndroidenterpriseEntitlementsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseEntitlementsGetArgs> for Entitlement {
    fn generate_resource_id(&self, input: &AndroidenterpriseEntitlementsGetArgs) -> String {
        format!(
            "gcp::androidenterprise::Entitlement/{}/{}/{}",
            input.enterpriseId, input.userId, input.entitlementId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::Entitlement"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EntitlementsListResponse
// =============================================================================

/// ResourceIdentifier implementation for EntitlementsListResponse with AndroidenterpriseEntitlementsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseEntitlementsListArgs> for EntitlementsListResponse {
    fn generate_resource_id(&self, input: &AndroidenterpriseEntitlementsListArgs) -> String {
        format!(
            "gcp::androidenterprise::EntitlementsListResponse/{}/{}",
            input.enterpriseId, input.userId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::EntitlementsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Entitlement
// =============================================================================

/// ResourceIdentifier implementation for Entitlement with AndroidenterpriseEntitlementsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseEntitlementsUpdateArgs> for Entitlement {
    fn generate_resource_id(&self, input: &AndroidenterpriseEntitlementsUpdateArgs) -> String {
        format!(
            "gcp::androidenterprise::Entitlement/{}/{}/{}",
            input.enterpriseId, input.userId, input.entitlementId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::Entitlement"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GroupLicense
// =============================================================================

/// ResourceIdentifier implementation for GroupLicense with AndroidenterpriseGrouplicensesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseGrouplicensesGetArgs> for GroupLicense {
    fn generate_resource_id(&self, input: &AndroidenterpriseGrouplicensesGetArgs) -> String {
        format!(
            "gcp::androidenterprise::GroupLicense/{}/{}",
            input.enterpriseId, input.groupLicenseId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::GroupLicense"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GroupLicensesListResponse
// =============================================================================

/// ResourceIdentifier implementation for GroupLicensesListResponse with AndroidenterpriseGrouplicensesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseGrouplicensesListArgs> for GroupLicensesListResponse {
    fn generate_resource_id(&self, input: &AndroidenterpriseGrouplicensesListArgs) -> String {
        format!(
            "gcp::androidenterprise::GroupLicensesListResponse/{}",
            input.enterpriseId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::GroupLicensesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GroupLicenseUsersListResponse
// =============================================================================

/// ResourceIdentifier implementation for GroupLicenseUsersListResponse with AndroidenterpriseGrouplicenseusersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseGrouplicenseusersListArgs>
    for GroupLicenseUsersListResponse
{
    fn generate_resource_id(&self, input: &AndroidenterpriseGrouplicenseusersListArgs) -> String {
        format!(
            "gcp::androidenterprise::GroupLicenseUsersListResponse/{}/{}",
            input.enterpriseId, input.groupLicenseId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::GroupLicenseUsersListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Install
// =============================================================================

/// ResourceIdentifier implementation for Install with AndroidenterpriseInstallsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseInstallsGetArgs> for Install {
    fn generate_resource_id(&self, input: &AndroidenterpriseInstallsGetArgs) -> String {
        format!(
            "gcp::androidenterprise::Install/{}/{}/{}/{}",
            input.enterpriseId, input.userId, input.deviceId, input.installId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::Install"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstallsListResponse
// =============================================================================

/// ResourceIdentifier implementation for InstallsListResponse with AndroidenterpriseInstallsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseInstallsListArgs> for InstallsListResponse {
    fn generate_resource_id(&self, input: &AndroidenterpriseInstallsListArgs) -> String {
        format!(
            "gcp::androidenterprise::InstallsListResponse/{}/{}/{}",
            input.enterpriseId, input.userId, input.deviceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::InstallsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Install
// =============================================================================

/// ResourceIdentifier implementation for Install with AndroidenterpriseInstallsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseInstallsUpdateArgs> for Install {
    fn generate_resource_id(&self, input: &AndroidenterpriseInstallsUpdateArgs) -> String {
        format!(
            "gcp::androidenterprise::Install/{}/{}/{}/{}",
            input.enterpriseId, input.userId, input.deviceId, input.installId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::Install"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ManagedConfiguration
// =============================================================================

/// ResourceIdentifier implementation for ManagedConfiguration with AndroidenterpriseManagedconfigurationsfordeviceGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseManagedconfigurationsfordeviceGetArgs>
    for ManagedConfiguration
{
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseManagedconfigurationsfordeviceGetArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::ManagedConfiguration/{}/{}/{}/{}",
            input.enterpriseId, input.userId, input.deviceId, input.managedConfigurationForDeviceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::ManagedConfiguration"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ManagedConfigurationsForDeviceListResponse
// =============================================================================

/// ResourceIdentifier implementation for ManagedConfigurationsForDeviceListResponse with AndroidenterpriseManagedconfigurationsfordeviceListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseManagedconfigurationsfordeviceListArgs>
    for ManagedConfigurationsForDeviceListResponse
{
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseManagedconfigurationsfordeviceListArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::ManagedConfigurationsForDeviceListResponse/{}/{}/{}",
            input.enterpriseId, input.userId, input.deviceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::ManagedConfigurationsForDeviceListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ManagedConfiguration
// =============================================================================

/// ResourceIdentifier implementation for ManagedConfiguration with AndroidenterpriseManagedconfigurationsfordeviceUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseManagedconfigurationsfordeviceUpdateArgs>
    for ManagedConfiguration
{
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseManagedconfigurationsfordeviceUpdateArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::ManagedConfiguration/{}/{}/{}/{}",
            input.enterpriseId, input.userId, input.deviceId, input.managedConfigurationForDeviceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::ManagedConfiguration"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ManagedConfiguration
// =============================================================================

/// ResourceIdentifier implementation for ManagedConfiguration with AndroidenterpriseManagedconfigurationsforuserGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseManagedconfigurationsforuserGetArgs>
    for ManagedConfiguration
{
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseManagedconfigurationsforuserGetArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::ManagedConfiguration/{}/{}/{}",
            input.enterpriseId, input.userId, input.managedConfigurationForUserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::ManagedConfiguration"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ManagedConfigurationsForUserListResponse
// =============================================================================

/// ResourceIdentifier implementation for ManagedConfigurationsForUserListResponse with AndroidenterpriseManagedconfigurationsforuserListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseManagedconfigurationsforuserListArgs>
    for ManagedConfigurationsForUserListResponse
{
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseManagedconfigurationsforuserListArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::ManagedConfigurationsForUserListResponse/{}/{}",
            input.enterpriseId, input.userId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::ManagedConfigurationsForUserListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ManagedConfiguration
// =============================================================================

/// ResourceIdentifier implementation for ManagedConfiguration with AndroidenterpriseManagedconfigurationsforuserUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseManagedconfigurationsforuserUpdateArgs>
    for ManagedConfiguration
{
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseManagedconfigurationsforuserUpdateArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::ManagedConfiguration/{}/{}/{}",
            input.enterpriseId, input.userId, input.managedConfigurationForUserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::ManagedConfiguration"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ManagedConfigurationsSettingsListResponse
// =============================================================================

/// ResourceIdentifier implementation for ManagedConfigurationsSettingsListResponse with AndroidenterpriseManagedconfigurationssettingsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseManagedconfigurationssettingsListArgs>
    for ManagedConfigurationsSettingsListResponse
{
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseManagedconfigurationssettingsListArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::ManagedConfigurationsSettingsListResponse/{}/{}",
            input.enterpriseId, input.productId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::ManagedConfigurationsSettingsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Permission
// =============================================================================

/// ResourceIdentifier implementation for Permission with AndroidenterprisePermissionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterprisePermissionsGetArgs> for Permission {
    fn generate_resource_id(&self, input: &AndroidenterprisePermissionsGetArgs) -> String {
        format!("gcp::androidenterprise::Permission/{}", input.permissionId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::Permission"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ProductsGenerateApprovalUrlResponse
// =============================================================================

/// ResourceIdentifier implementation for ProductsGenerateApprovalUrlResponse with AndroidenterpriseProductsGenerateApprovalUrlArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseProductsGenerateApprovalUrlArgs>
    for ProductsGenerateApprovalUrlResponse
{
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseProductsGenerateApprovalUrlArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::ProductsGenerateApprovalUrlResponse/{}/{}",
            input.enterpriseId, input.productId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::ProductsGenerateApprovalUrlResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Product
// =============================================================================

/// ResourceIdentifier implementation for Product with AndroidenterpriseProductsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseProductsGetArgs> for Product {
    fn generate_resource_id(&self, input: &AndroidenterpriseProductsGetArgs) -> String {
        format!(
            "gcp::androidenterprise::Product/{}/{}",
            input.enterpriseId, input.productId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::Product"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AppRestrictionsSchema
// =============================================================================

/// ResourceIdentifier implementation for AppRestrictionsSchema with AndroidenterpriseProductsGetAppRestrictionsSchemaArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseProductsGetAppRestrictionsSchemaArgs>
    for AppRestrictionsSchema
{
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseProductsGetAppRestrictionsSchemaArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::AppRestrictionsSchema/{}/{}",
            input.enterpriseId, input.productId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::AppRestrictionsSchema"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ProductPermissions
// =============================================================================

/// ResourceIdentifier implementation for ProductPermissions with AndroidenterpriseProductsGetPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseProductsGetPermissionsArgs> for ProductPermissions {
    fn generate_resource_id(&self, input: &AndroidenterpriseProductsGetPermissionsArgs) -> String {
        format!(
            "gcp::androidenterprise::ProductPermissions/{}/{}",
            input.enterpriseId, input.productId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::ProductPermissions"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ProductsListResponse
// =============================================================================

/// ResourceIdentifier implementation for ProductsListResponse with AndroidenterpriseProductsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseProductsListArgs> for ProductsListResponse {
    fn generate_resource_id(&self, input: &AndroidenterpriseProductsListArgs) -> String {
        format!(
            "gcp::androidenterprise::ProductsListResponse/{}",
            input.enterpriseId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::ProductsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ServiceAccountKey
// =============================================================================

/// ResourceIdentifier implementation for ServiceAccountKey with AndroidenterpriseServiceaccountkeysInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseServiceaccountkeysInsertArgs> for ServiceAccountKey {
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseServiceaccountkeysInsertArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::ServiceAccountKey/{}",
            input.enterpriseId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::ServiceAccountKey"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ServiceAccountKeysListResponse
// =============================================================================

/// ResourceIdentifier implementation for ServiceAccountKeysListResponse with AndroidenterpriseServiceaccountkeysListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseServiceaccountkeysListArgs>
    for ServiceAccountKeysListResponse
{
    fn generate_resource_id(&self, input: &AndroidenterpriseServiceaccountkeysListArgs) -> String {
        format!(
            "gcp::androidenterprise::ServiceAccountKeysListResponse/{}",
            input.enterpriseId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::ServiceAccountKeysListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StoreCluster
// =============================================================================

/// ResourceIdentifier implementation for StoreCluster with AndroidenterpriseStorelayoutclustersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseStorelayoutclustersGetArgs> for StoreCluster {
    fn generate_resource_id(&self, input: &AndroidenterpriseStorelayoutclustersGetArgs) -> String {
        format!(
            "gcp::androidenterprise::StoreCluster/{}/{}/{}",
            input.enterpriseId, input.pageId, input.clusterId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::StoreCluster"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StoreCluster
// =============================================================================

/// ResourceIdentifier implementation for StoreCluster with AndroidenterpriseStorelayoutclustersInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseStorelayoutclustersInsertArgs> for StoreCluster {
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseStorelayoutclustersInsertArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::StoreCluster/{}/{}",
            input.enterpriseId, input.pageId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::StoreCluster"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StoreLayoutClustersListResponse
// =============================================================================

/// ResourceIdentifier implementation for StoreLayoutClustersListResponse with AndroidenterpriseStorelayoutclustersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseStorelayoutclustersListArgs>
    for StoreLayoutClustersListResponse
{
    fn generate_resource_id(&self, input: &AndroidenterpriseStorelayoutclustersListArgs) -> String {
        format!(
            "gcp::androidenterprise::StoreLayoutClustersListResponse/{}/{}",
            input.enterpriseId, input.pageId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::StoreLayoutClustersListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StoreCluster
// =============================================================================

/// ResourceIdentifier implementation for StoreCluster with AndroidenterpriseStorelayoutclustersUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseStorelayoutclustersUpdateArgs> for StoreCluster {
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseStorelayoutclustersUpdateArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::StoreCluster/{}/{}/{}",
            input.enterpriseId, input.pageId, input.clusterId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::StoreCluster"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StorePage
// =============================================================================

/// ResourceIdentifier implementation for StorePage with AndroidenterpriseStorelayoutpagesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseStorelayoutpagesGetArgs> for StorePage {
    fn generate_resource_id(&self, input: &AndroidenterpriseStorelayoutpagesGetArgs) -> String {
        format!(
            "gcp::androidenterprise::StorePage/{}/{}",
            input.enterpriseId, input.pageId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::StorePage"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StorePage
// =============================================================================

/// ResourceIdentifier implementation for StorePage with AndroidenterpriseStorelayoutpagesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseStorelayoutpagesInsertArgs> for StorePage {
    fn generate_resource_id(&self, input: &AndroidenterpriseStorelayoutpagesInsertArgs) -> String {
        format!("gcp::androidenterprise::StorePage/{}", input.enterpriseId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::StorePage"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StoreLayoutPagesListResponse
// =============================================================================

/// ResourceIdentifier implementation for StoreLayoutPagesListResponse with AndroidenterpriseStorelayoutpagesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseStorelayoutpagesListArgs>
    for StoreLayoutPagesListResponse
{
    fn generate_resource_id(&self, input: &AndroidenterpriseStorelayoutpagesListArgs) -> String {
        format!(
            "gcp::androidenterprise::StoreLayoutPagesListResponse/{}",
            input.enterpriseId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::StoreLayoutPagesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StorePage
// =============================================================================

/// ResourceIdentifier implementation for StorePage with AndroidenterpriseStorelayoutpagesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseStorelayoutpagesUpdateArgs> for StorePage {
    fn generate_resource_id(&self, input: &AndroidenterpriseStorelayoutpagesUpdateArgs) -> String {
        format!(
            "gcp::androidenterprise::StorePage/{}/{}",
            input.enterpriseId, input.pageId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::StorePage"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AuthenticationToken
// =============================================================================

/// ResourceIdentifier implementation for AuthenticationToken with AndroidenterpriseUsersGenerateAuthenticationTokenArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseUsersGenerateAuthenticationTokenArgs>
    for AuthenticationToken
{
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseUsersGenerateAuthenticationTokenArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::AuthenticationToken/{}/{}",
            input.enterpriseId, input.userId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::AuthenticationToken"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for User
// =============================================================================

/// ResourceIdentifier implementation for User with AndroidenterpriseUsersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseUsersGetArgs> for User {
    fn generate_resource_id(&self, input: &AndroidenterpriseUsersGetArgs) -> String {
        format!(
            "gcp::androidenterprise::User/{}/{}",
            input.enterpriseId, input.userId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::User"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ProductSet
// =============================================================================

/// ResourceIdentifier implementation for ProductSet with AndroidenterpriseUsersGetAvailableProductSetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseUsersGetAvailableProductSetArgs> for ProductSet {
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseUsersGetAvailableProductSetArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::ProductSet/{}/{}",
            input.enterpriseId, input.userId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::ProductSet"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for User
// =============================================================================

/// ResourceIdentifier implementation for User with AndroidenterpriseUsersInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseUsersInsertArgs> for User {
    fn generate_resource_id(&self, input: &AndroidenterpriseUsersInsertArgs) -> String {
        format!("gcp::androidenterprise::User/{}", input.enterpriseId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::User"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UsersListResponse
// =============================================================================

/// ResourceIdentifier implementation for UsersListResponse with AndroidenterpriseUsersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseUsersListArgs> for UsersListResponse {
    fn generate_resource_id(&self, input: &AndroidenterpriseUsersListArgs) -> String {
        format!(
            "gcp::androidenterprise::UsersListResponse/{}",
            input.enterpriseId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::UsersListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ProductSet
// =============================================================================

/// ResourceIdentifier implementation for ProductSet with AndroidenterpriseUsersSetAvailableProductSetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseUsersSetAvailableProductSetArgs> for ProductSet {
    fn generate_resource_id(
        &self,
        input: &AndroidenterpriseUsersSetAvailableProductSetArgs,
    ) -> String {
        format!(
            "gcp::androidenterprise::ProductSet/{}/{}",
            input.enterpriseId, input.userId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::ProductSet"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for User
// =============================================================================

/// ResourceIdentifier implementation for User with AndroidenterpriseUsersUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseUsersUpdateArgs> for User {
    fn generate_resource_id(&self, input: &AndroidenterpriseUsersUpdateArgs) -> String {
        format!(
            "gcp::androidenterprise::User/{}/{}",
            input.enterpriseId, input.userId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::User"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WebApp
// =============================================================================

/// ResourceIdentifier implementation for WebApp with AndroidenterpriseWebappsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseWebappsGetArgs> for WebApp {
    fn generate_resource_id(&self, input: &AndroidenterpriseWebappsGetArgs) -> String {
        format!(
            "gcp::androidenterprise::WebApp/{}/{}",
            input.enterpriseId, input.webAppId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::WebApp"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WebApp
// =============================================================================

/// ResourceIdentifier implementation for WebApp with AndroidenterpriseWebappsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseWebappsInsertArgs> for WebApp {
    fn generate_resource_id(&self, input: &AndroidenterpriseWebappsInsertArgs) -> String {
        format!("gcp::androidenterprise::WebApp/{}", input.enterpriseId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::WebApp"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WebAppsListResponse
// =============================================================================

/// ResourceIdentifier implementation for WebAppsListResponse with AndroidenterpriseWebappsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseWebappsListArgs> for WebAppsListResponse {
    fn generate_resource_id(&self, input: &AndroidenterpriseWebappsListArgs) -> String {
        format!(
            "gcp::androidenterprise::WebAppsListResponse/{}",
            input.enterpriseId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::WebAppsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WebApp
// =============================================================================

/// ResourceIdentifier implementation for WebApp with AndroidenterpriseWebappsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AndroidenterpriseWebappsUpdateArgs> for WebApp {
    fn generate_resource_id(&self, input: &AndroidenterpriseWebappsUpdateArgs) -> String {
        format!(
            "gcp::androidenterprise::WebApp/{}/{}",
            input.enterpriseId, input.webAppId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::androidenterprise::WebApp"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
