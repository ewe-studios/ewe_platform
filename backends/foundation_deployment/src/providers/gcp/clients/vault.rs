//! Auto-generated API clients for gcp/vault.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/matters/{matterId}:addPermissions
/// Adds an account as a matter collaborator.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_add_permissions_execute()` to send, or `vault_matters_add_permissions` for simplest API.

pub fn vault_matters_add_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}:addPermissions",
        matterId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/matters/{matterId}:addPermissions
/// Adds an account as a matter collaborator.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_add_permissions_execute()` or `vault_matters_add_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_add_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_add_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MatterPermission>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MatterPermission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/matters/{matterId}:addPermissions
/// Adds an account as a matter collaborator.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_add_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_add_permissions_task()`.
/// For the simplest API, use `vault_matters_add_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_add_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_add_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MatterPermission>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vault_matters_add_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_add_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersAddPermissionsArgs {
    /// Path parameter: matterId
    pub matterId: String,
}

/// POST v1/matters/{matterId}:addPermissions
/// Adds an account as a matter collaborator.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_add_permissions_builder()` + `vault_matters_add_permissions_execute()`.
/// For task-level control, use `vault_matters_add_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_add_permissions(
    client: &SimpleHttpClient,
    args: &VaultMattersAddPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MatterPermission>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vault_matters_add_permissions_builder(client, &args.matterId)?;
    vault_matters_add_permissions_execute(builder)
}

/// POST v1/matters/{matterId}:close
/// Closes the specified matter. Returns the matter with updated state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_close_execute()` to send, or `vault_matters_close` for simplest API.

pub fn vault_matters_close_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://vault.googleapis.com/v1/matters/{}:close", matterId,);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/matters/{matterId}:close
/// Closes the specified matter. Returns the matter with updated state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_close_execute()` or `vault_matters_close`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_close_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_close_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CloseMatterResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CloseMatterResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/matters/{matterId}:close
/// Closes the specified matter. Returns the matter with updated state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_close_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_close_task()`.
/// For the simplest API, use `vault_matters_close()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_close_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_close_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CloseMatterResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vault_matters_close_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_close`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersCloseArgs {
    /// Path parameter: matterId
    pub matterId: String,
}

/// POST v1/matters/{matterId}:close
/// Closes the specified matter. Returns the matter with updated state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_close_builder()` + `vault_matters_close_execute()`.
/// For task-level control, use `vault_matters_close_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_close(
    client: &SimpleHttpClient,
    args: &VaultMattersCloseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CloseMatterResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vault_matters_close_builder(client, &args.matterId)?;
    vault_matters_close_execute(builder)
}

/// POST v1/matters/{matterId}:count
/// Counts the accounts processed by the specified query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_count_execute()` to send, or `vault_matters_count` for simplest API.

pub fn vault_matters_count_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://vault.googleapis.com/v1/matters/{}:count", matterId,);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/matters/{matterId}:count
/// Counts the accounts processed by the specified query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_count_execute()` or `vault_matters_count`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_count_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_count_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/matters/{matterId}:count
/// Counts the accounts processed by the specified query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_count_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_count_task()`.
/// For the simplest API, use `vault_matters_count()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_count_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_count_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_count_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_count`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersCountArgs {
    /// Path parameter: matterId
    pub matterId: String,
}

/// POST v1/matters/{matterId}:count
/// Counts the accounts processed by the specified query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_count_builder()` + `vault_matters_count_execute()`.
/// For task-level control, use `vault_matters_count_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_count(
    client: &SimpleHttpClient,
    args: &VaultMattersCountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_count_builder(client, &args.matterId)?;
    vault_matters_count_execute(builder)
}

/// POST v1/matters
/// Creates a matter with the given name and description. The initial state is open, and the owner is the method caller. Returns the created matter with default view.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_create_execute()` to send, or `vault_matters_create` for simplest API.

pub fn vault_matters_create_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://vault.googleapis.com/v1/matters",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/matters
/// Creates a matter with the given name and description. The initial state is open, and the owner is the method caller. Returns the created matter with default view.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_create_execute()` or `vault_matters_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Matter>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Matter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/matters
/// Creates a matter with the given name and description. The initial state is open, and the owner is the method caller. Returns the created matter with default view.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_create_task()`.
/// For the simplest API, use `vault_matters_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Matter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/matters
/// Creates a matter with the given name and description. The initial state is open, and the owner is the method caller. Returns the created matter with default view.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_create_builder()` + `vault_matters_create_execute()`.
/// For task-level control, use `vault_matters_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_create(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Matter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_create_builder(client)?;
    vault_matters_create_execute(builder)
}

/// DELETE v1/matters/{matterId}
/// Deletes the specified matter. Returns the matter with updated state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_delete_execute()` to send, or `vault_matters_delete` for simplest API.

pub fn vault_matters_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://vault.googleapis.com/v1/matters/{}", matterId,);

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/matters/{matterId}
/// Deletes the specified matter. Returns the matter with updated state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_delete_execute()` or `vault_matters_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Matter>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Matter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/matters/{matterId}
/// Deletes the specified matter. Returns the matter with updated state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_delete_task()`.
/// For the simplest API, use `vault_matters_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Matter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersDeleteArgs {
    /// Path parameter: matterId
    pub matterId: String,
}

/// DELETE v1/matters/{matterId}
/// Deletes the specified matter. Returns the matter with updated state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_delete_builder()` + `vault_matters_delete_execute()`.
/// For task-level control, use `vault_matters_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_delete(
    client: &SimpleHttpClient,
    args: &VaultMattersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Matter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_delete_builder(client, &args.matterId)?;
    vault_matters_delete_execute(builder)
}

/// GET v1/matters/{matterId}
/// Gets the specified matter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_get_execute()` to send, or `vault_matters_get` for simplest API.

pub fn vault_matters_get_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://vault.googleapis.com/v1/matters/{}", matterId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/matters/{matterId}
/// Gets the specified matter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_get_execute()` or `vault_matters_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Matter>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Matter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}
/// Gets the specified matter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_get_task()`.
/// For the simplest API, use `vault_matters_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Matter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersGetArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v1/matters/{matterId}
/// Gets the specified matter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_get_builder()` + `vault_matters_get_execute()`.
/// For task-level control, use `vault_matters_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_get(
    client: &SimpleHttpClient,
    args: &VaultMattersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Matter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_get_builder(client, &args.matterId, &args.view)?;
    vault_matters_get_execute(builder)
}

/// GET v1/matters
/// Lists matters the requestor has access to.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_list_execute()` to send, or `vault_matters_list` for simplest API.

pub fn vault_matters_list_builder<R>(
    client: &SimpleHttpClient<R>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    state: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://vault.googleapis.com/v1/matters",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = state.as_ref() {
        query_parts.push(format!("state={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/matters
/// Lists matters the requestor has access to.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_list_execute()` or `vault_matters_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMattersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMattersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters
/// Lists matters the requestor has access to.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_list_task()`.
/// For the simplest API, use `vault_matters_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMattersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vault_matters_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: state
    pub state: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v1/matters
/// Lists matters the requestor has access to.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_list_builder()` + `vault_matters_list_execute()`.
/// For task-level control, use `vault_matters_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_list(
    client: &SimpleHttpClient,
    args: &VaultMattersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMattersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vault_matters_list_builder(
        client,
        &args.pageSize,
        &args.pageToken,
        &args.state,
        &args.view,
    )?;
    vault_matters_list_execute(builder)
}

/// POST v1/matters/{matterId}:removePermissions
/// Removes an account as a matter collaborator.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_remove_permissions_execute()` to send, or `vault_matters_remove_permissions` for simplest API.

pub fn vault_matters_remove_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}:removePermissions",
        matterId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/matters/{matterId}:removePermissions
/// Removes an account as a matter collaborator.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_remove_permissions_execute()` or `vault_matters_remove_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_remove_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_remove_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/matters/{matterId}:removePermissions
/// Removes an account as a matter collaborator.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_remove_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_remove_permissions_task()`.
/// For the simplest API, use `vault_matters_remove_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_remove_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_remove_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_remove_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_remove_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersRemovePermissionsArgs {
    /// Path parameter: matterId
    pub matterId: String,
}

/// POST v1/matters/{matterId}:removePermissions
/// Removes an account as a matter collaborator.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_remove_permissions_builder()` + `vault_matters_remove_permissions_execute()`.
/// For task-level control, use `vault_matters_remove_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_remove_permissions(
    client: &SimpleHttpClient,
    args: &VaultMattersRemovePermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_remove_permissions_builder(client, &args.matterId)?;
    vault_matters_remove_permissions_execute(builder)
}

/// POST v1/matters/{matterId}:reopen
/// Reopens the specified matter. Returns the matter with updated state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_reopen_execute()` to send, or `vault_matters_reopen` for simplest API.

pub fn vault_matters_reopen_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}:reopen",
        matterId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/matters/{matterId}:reopen
/// Reopens the specified matter. Returns the matter with updated state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_reopen_execute()` or `vault_matters_reopen`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_reopen_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_reopen_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReopenMatterResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReopenMatterResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/matters/{matterId}:reopen
/// Reopens the specified matter. Returns the matter with updated state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_reopen_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_reopen_task()`.
/// For the simplest API, use `vault_matters_reopen()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_reopen_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_reopen_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReopenMatterResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vault_matters_reopen_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_reopen`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersReopenArgs {
    /// Path parameter: matterId
    pub matterId: String,
}

/// POST v1/matters/{matterId}:reopen
/// Reopens the specified matter. Returns the matter with updated state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_reopen_builder()` + `vault_matters_reopen_execute()`.
/// For task-level control, use `vault_matters_reopen_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_reopen(
    client: &SimpleHttpClient,
    args: &VaultMattersReopenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReopenMatterResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vault_matters_reopen_builder(client, &args.matterId)?;
    vault_matters_reopen_execute(builder)
}

/// POST v1/matters/{matterId}:undelete
/// Undeletes the specified matter. Returns the matter with updated state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_undelete_execute()` to send, or `vault_matters_undelete` for simplest API.

pub fn vault_matters_undelete_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}:undelete",
        matterId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/matters/{matterId}:undelete
/// Undeletes the specified matter. Returns the matter with updated state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_undelete_execute()` or `vault_matters_undelete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_undelete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Matter>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Matter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/matters/{matterId}:undelete
/// Undeletes the specified matter. Returns the matter with updated state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_undelete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_undelete_task()`.
/// For the simplest API, use `vault_matters_undelete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Matter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_undelete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersUndeleteArgs {
    /// Path parameter: matterId
    pub matterId: String,
}

/// POST v1/matters/{matterId}:undelete
/// Undeletes the specified matter. Returns the matter with updated state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_undelete_builder()` + `vault_matters_undelete_execute()`.
/// For task-level control, use `vault_matters_undelete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_undelete(
    client: &SimpleHttpClient,
    args: &VaultMattersUndeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Matter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_undelete_builder(client, &args.matterId)?;
    vault_matters_undelete_execute(builder)
}

/// PUT v1/matters/{matterId}
/// Updates the specified matter. This updates only the name and description of the matter, identified by matter ID. Changes to any other fields are ignored. Returns the default view of the matter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_update_execute()` to send, or `vault_matters_update` for simplest API.

pub fn vault_matters_update_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://vault.googleapis.com/v1/matters/{}", matterId,);

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/matters/{matterId}
/// Updates the specified matter. This updates only the name and description of the matter, identified by matter ID. Changes to any other fields are ignored. Returns the default view of the matter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_update_execute()` or `vault_matters_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Matter>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Matter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/matters/{matterId}
/// Updates the specified matter. This updates only the name and description of the matter, identified by matter ID. Changes to any other fields are ignored. Returns the default view of the matter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_update_task()`.
/// For the simplest API, use `vault_matters_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Matter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersUpdateArgs {
    /// Path parameter: matterId
    pub matterId: String,
}

/// PUT v1/matters/{matterId}
/// Updates the specified matter. This updates only the name and description of the matter, identified by matter ID. Changes to any other fields are ignored. Returns the default view of the matter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_update_builder()` + `vault_matters_update_execute()`.
/// For task-level control, use `vault_matters_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_update(
    client: &SimpleHttpClient,
    args: &VaultMattersUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Matter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_update_builder(client, &args.matterId)?;
    vault_matters_update_execute(builder)
}

/// POST v1/matters/{matterId}/exports
/// Creates an export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_exports_create_execute()` to send, or `vault_matters_exports_create` for simplest API.

pub fn vault_matters_exports_create_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/exports",
        matterId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/matters/{matterId}/exports
/// Creates an export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_exports_create_execute()` or `vault_matters_exports_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_exports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_exports_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Export>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Export = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/matters/{matterId}/exports
/// Creates an export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_exports_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_exports_create_task()`.
/// For the simplest API, use `vault_matters_exports_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_exports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_exports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Export>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_exports_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_exports_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersExportsCreateArgs {
    /// Path parameter: matterId
    pub matterId: String,
}

/// POST v1/matters/{matterId}/exports
/// Creates an export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_exports_create_builder()` + `vault_matters_exports_create_execute()`.
/// For task-level control, use `vault_matters_exports_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_exports_create(
    client: &SimpleHttpClient,
    args: &VaultMattersExportsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Export>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_exports_create_builder(client, &args.matterId)?;
    vault_matters_exports_create_execute(builder)
}

/// DELETE v1/matters/{matterId}/exports/{exportId}
/// Deletes an export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_exports_delete_execute()` to send, or `vault_matters_exports_delete` for simplest API.

pub fn vault_matters_exports_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
    exportId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/exports/{}",
        matterId, exportId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/matters/{matterId}/exports/{exportId}
/// Deletes an export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_exports_delete_execute()` or `vault_matters_exports_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_exports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_exports_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/matters/{matterId}/exports/{exportId}
/// Deletes an export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_exports_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_exports_delete_task()`.
/// For the simplest API, use `vault_matters_exports_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_exports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_exports_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_exports_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_exports_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersExportsDeleteArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: exportId
    pub exportId: String,
}

/// DELETE v1/matters/{matterId}/exports/{exportId}
/// Deletes an export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_exports_delete_builder()` + `vault_matters_exports_delete_execute()`.
/// For task-level control, use `vault_matters_exports_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_exports_delete(
    client: &SimpleHttpClient,
    args: &VaultMattersExportsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_exports_delete_builder(client, &args.matterId, &args.exportId)?;
    vault_matters_exports_delete_execute(builder)
}

/// GET v1/matters/{matterId}/exports/{exportId}
/// Gets an export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_exports_get_execute()` to send, or `vault_matters_exports_get` for simplest API.

pub fn vault_matters_exports_get_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
    exportId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/exports/{}",
        matterId, exportId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/matters/{matterId}/exports/{exportId}
/// Gets an export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_exports_get_execute()` or `vault_matters_exports_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_exports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_exports_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Export>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Export = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}/exports/{exportId}
/// Gets an export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_exports_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_exports_get_task()`.
/// For the simplest API, use `vault_matters_exports_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_exports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_exports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Export>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_exports_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_exports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersExportsGetArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: exportId
    pub exportId: String,
}

/// GET v1/matters/{matterId}/exports/{exportId}
/// Gets an export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_exports_get_builder()` + `vault_matters_exports_get_execute()`.
/// For task-level control, use `vault_matters_exports_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_exports_get(
    client: &SimpleHttpClient,
    args: &VaultMattersExportsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Export>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_exports_get_builder(client, &args.matterId, &args.exportId)?;
    vault_matters_exports_get_execute(builder)
}

/// GET v1/matters/{matterId}/exports
/// Lists details about the exports in the specified matter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_exports_list_execute()` to send, or `vault_matters_exports_list` for simplest API.

pub fn vault_matters_exports_list_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/exports",
        matterId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/matters/{matterId}/exports
/// Lists details about the exports in the specified matter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_exports_list_execute()` or `vault_matters_exports_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_exports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_exports_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListExportsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListExportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}/exports
/// Lists details about the exports in the specified matter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_exports_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_exports_list_task()`.
/// For the simplest API, use `vault_matters_exports_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_exports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_exports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vault_matters_exports_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_exports_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersExportsListArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/matters/{matterId}/exports
/// Lists details about the exports in the specified matter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_exports_list_builder()` + `vault_matters_exports_list_execute()`.
/// For task-level control, use `vault_matters_exports_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_exports_list(
    client: &SimpleHttpClient,
    args: &VaultMattersExportsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vault_matters_exports_list_builder(
        client,
        &args.matterId,
        &args.pageSize,
        &args.pageToken,
    )?;
    vault_matters_exports_list_execute(builder)
}

/// POST v1/matters/{matterId}/holds/{holdId}:addHeldAccounts
/// Adds accounts to a hold. Returns a list of accounts that have been successfully added. Accounts can be added only to an existing account-based hold.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_holds_add_held_accounts_execute()` to send, or `vault_matters_holds_add_held_accounts` for simplest API.

pub fn vault_matters_holds_add_held_accounts_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
    holdId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/holds/{}:addHeldAccounts",
        matterId, holdId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/matters/{matterId}/holds/{holdId}:addHeldAccounts
/// Adds accounts to a hold. Returns a list of accounts that have been successfully added. Accounts can be added only to an existing account-based hold.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_holds_add_held_accounts_execute()` or `vault_matters_holds_add_held_accounts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_add_held_accounts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_add_held_accounts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddHeldAccountsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddHeldAccountsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/matters/{matterId}/holds/{holdId}:addHeldAccounts
/// Adds accounts to a hold. Returns a list of accounts that have been successfully added. Accounts can be added only to an existing account-based hold.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_holds_add_held_accounts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_holds_add_held_accounts_task()`.
/// For the simplest API, use `vault_matters_holds_add_held_accounts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_add_held_accounts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_holds_add_held_accounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddHeldAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vault_matters_holds_add_held_accounts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_holds_add_held_accounts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersHoldsAddHeldAccountsArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: holdId
    pub holdId: String,
}

/// POST v1/matters/{matterId}/holds/{holdId}:addHeldAccounts
/// Adds accounts to a hold. Returns a list of accounts that have been successfully added. Accounts can be added only to an existing account-based hold.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_holds_add_held_accounts_builder()` + `vault_matters_holds_add_held_accounts_execute()`.
/// For task-level control, use `vault_matters_holds_add_held_accounts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_add_held_accounts(
    client: &SimpleHttpClient,
    args: &VaultMattersHoldsAddHeldAccountsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddHeldAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        vault_matters_holds_add_held_accounts_builder(client, &args.matterId, &args.holdId)?;
    vault_matters_holds_add_held_accounts_execute(builder)
}

/// POST v1/matters/{matterId}/holds
/// Creates a hold in the specified matter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_holds_create_execute()` to send, or `vault_matters_holds_create` for simplest API.

pub fn vault_matters_holds_create_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://vault.googleapis.com/v1/matters/{}/holds", matterId,);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/matters/{matterId}/holds
/// Creates a hold in the specified matter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_holds_create_execute()` or `vault_matters_holds_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Hold>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Hold = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/matters/{matterId}/holds
/// Creates a hold in the specified matter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_holds_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_holds_create_task()`.
/// For the simplest API, use `vault_matters_holds_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_holds_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hold>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_holds_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_holds_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersHoldsCreateArgs {
    /// Path parameter: matterId
    pub matterId: String,
}

/// POST v1/matters/{matterId}/holds
/// Creates a hold in the specified matter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_holds_create_builder()` + `vault_matters_holds_create_execute()`.
/// For task-level control, use `vault_matters_holds_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_create(
    client: &SimpleHttpClient,
    args: &VaultMattersHoldsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hold>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_holds_create_builder(client, &args.matterId)?;
    vault_matters_holds_create_execute(builder)
}

/// DELETE v1/matters/{matterId}/holds/{holdId}
/// Removes the specified hold and releases the accounts or organizational unit covered by the hold. If the data is not preserved by another hold or retention rule, it might be purged.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_holds_delete_execute()` to send, or `vault_matters_holds_delete` for simplest API.

pub fn vault_matters_holds_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
    holdId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/holds/{}",
        matterId, holdId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/matters/{matterId}/holds/{holdId}
/// Removes the specified hold and releases the accounts or organizational unit covered by the hold. If the data is not preserved by another hold or retention rule, it might be purged.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_holds_delete_execute()` or `vault_matters_holds_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/matters/{matterId}/holds/{holdId}
/// Removes the specified hold and releases the accounts or organizational unit covered by the hold. If the data is not preserved by another hold or retention rule, it might be purged.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_holds_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_holds_delete_task()`.
/// For the simplest API, use `vault_matters_holds_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_holds_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_holds_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_holds_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersHoldsDeleteArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: holdId
    pub holdId: String,
}

/// DELETE v1/matters/{matterId}/holds/{holdId}
/// Removes the specified hold and releases the accounts or organizational unit covered by the hold. If the data is not preserved by another hold or retention rule, it might be purged.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_holds_delete_builder()` + `vault_matters_holds_delete_execute()`.
/// For task-level control, use `vault_matters_holds_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_delete(
    client: &SimpleHttpClient,
    args: &VaultMattersHoldsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_holds_delete_builder(client, &args.matterId, &args.holdId)?;
    vault_matters_holds_delete_execute(builder)
}

/// GET v1/matters/{matterId}/holds/{holdId}
/// Gets the specified hold.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_holds_get_execute()` to send, or `vault_matters_holds_get` for simplest API.

pub fn vault_matters_holds_get_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
    holdId: &String,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/holds/{}",
        matterId, holdId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/matters/{matterId}/holds/{holdId}
/// Gets the specified hold.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_holds_get_execute()` or `vault_matters_holds_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Hold>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Hold = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}/holds/{holdId}
/// Gets the specified hold.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_holds_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_holds_get_task()`.
/// For the simplest API, use `vault_matters_holds_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_holds_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hold>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_holds_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_holds_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersHoldsGetArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: holdId
    pub holdId: String,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v1/matters/{matterId}/holds/{holdId}
/// Gets the specified hold.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_holds_get_builder()` + `vault_matters_holds_get_execute()`.
/// For task-level control, use `vault_matters_holds_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_get(
    client: &SimpleHttpClient,
    args: &VaultMattersHoldsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hold>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vault_matters_holds_get_builder(client, &args.matterId, &args.holdId, &args.view)?;
    vault_matters_holds_get_execute(builder)
}

/// GET v1/matters/{matterId}/holds
/// Lists the holds in a matter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_holds_list_execute()` to send, or `vault_matters_holds_list` for simplest API.

pub fn vault_matters_holds_list_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://vault.googleapis.com/v1/matters/{}/holds", matterId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/matters/{matterId}/holds
/// Lists the holds in a matter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_holds_list_execute()` or `vault_matters_holds_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListHoldsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListHoldsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}/holds
/// Lists the holds in a matter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_holds_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_holds_list_task()`.
/// For the simplest API, use `vault_matters_holds_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_holds_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListHoldsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vault_matters_holds_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_holds_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersHoldsListArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v1/matters/{matterId}/holds
/// Lists the holds in a matter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_holds_list_builder()` + `vault_matters_holds_list_execute()`.
/// For task-level control, use `vault_matters_holds_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_list(
    client: &SimpleHttpClient,
    args: &VaultMattersHoldsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListHoldsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vault_matters_holds_list_builder(
        client,
        &args.matterId,
        &args.pageSize,
        &args.pageToken,
        &args.view,
    )?;
    vault_matters_holds_list_execute(builder)
}

/// POST v1/matters/{matterId}/holds/{holdId}:removeHeldAccounts
/// Removes the specified accounts from a hold. Returns a list of statuses in the same order as the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_holds_remove_held_accounts_execute()` to send, or `vault_matters_holds_remove_held_accounts` for simplest API.

pub fn vault_matters_holds_remove_held_accounts_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
    holdId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/holds/{}:removeHeldAccounts",
        matterId, holdId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/matters/{matterId}/holds/{holdId}:removeHeldAccounts
/// Removes the specified accounts from a hold. Returns a list of statuses in the same order as the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_holds_remove_held_accounts_execute()` or `vault_matters_holds_remove_held_accounts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_remove_held_accounts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_remove_held_accounts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemoveHeldAccountsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemoveHeldAccountsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/matters/{matterId}/holds/{holdId}:removeHeldAccounts
/// Removes the specified accounts from a hold. Returns a list of statuses in the same order as the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_holds_remove_held_accounts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_holds_remove_held_accounts_task()`.
/// For the simplest API, use `vault_matters_holds_remove_held_accounts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_remove_held_accounts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_holds_remove_held_accounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RemoveHeldAccountsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = vault_matters_holds_remove_held_accounts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_holds_remove_held_accounts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersHoldsRemoveHeldAccountsArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: holdId
    pub holdId: String,
}

/// POST v1/matters/{matterId}/holds/{holdId}:removeHeldAccounts
/// Removes the specified accounts from a hold. Returns a list of statuses in the same order as the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_holds_remove_held_accounts_builder()` + `vault_matters_holds_remove_held_accounts_execute()`.
/// For task-level control, use `vault_matters_holds_remove_held_accounts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_remove_held_accounts(
    client: &SimpleHttpClient,
    args: &VaultMattersHoldsRemoveHeldAccountsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RemoveHeldAccountsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        vault_matters_holds_remove_held_accounts_builder(client, &args.matterId, &args.holdId)?;
    vault_matters_holds_remove_held_accounts_execute(builder)
}

/// PUT v1/matters/{matterId}/holds/{holdId}
/// Updates the scope (organizational unit or accounts) and query parameters of a hold. You cannot add accounts to a hold that covers an organizational unit, nor can you add organizational units to a hold that covers individual accounts. If you try, the unsupported values are ignored.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_holds_update_execute()` to send, or `vault_matters_holds_update` for simplest API.

pub fn vault_matters_holds_update_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
    holdId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/holds/{}",
        matterId, holdId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/matters/{matterId}/holds/{holdId}
/// Updates the scope (organizational unit or accounts) and query parameters of a hold. You cannot add accounts to a hold that covers an organizational unit, nor can you add organizational units to a hold that covers individual accounts. If you try, the unsupported values are ignored.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_holds_update_execute()` or `vault_matters_holds_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Hold>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Hold = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/matters/{matterId}/holds/{holdId}
/// Updates the scope (organizational unit or accounts) and query parameters of a hold. You cannot add accounts to a hold that covers an organizational unit, nor can you add organizational units to a hold that covers individual accounts. If you try, the unsupported values are ignored.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_holds_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_holds_update_task()`.
/// For the simplest API, use `vault_matters_holds_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_holds_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hold>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_holds_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_holds_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersHoldsUpdateArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: holdId
    pub holdId: String,
}

/// PUT v1/matters/{matterId}/holds/{holdId}
/// Updates the scope (organizational unit or accounts) and query parameters of a hold. You cannot add accounts to a hold that covers an organizational unit, nor can you add organizational units to a hold that covers individual accounts. If you try, the unsupported values are ignored.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_holds_update_builder()` + `vault_matters_holds_update_execute()`.
/// For task-level control, use `vault_matters_holds_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_update(
    client: &SimpleHttpClient,
    args: &VaultMattersHoldsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hold>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_holds_update_builder(client, &args.matterId, &args.holdId)?;
    vault_matters_holds_update_execute(builder)
}

/// POST v1/matters/{matterId}/holds/{holdId}/accounts
/// Adds an account to a hold. Accounts can be added only to a hold that does not have an organizational unit set. If you try to add an account to an organizational unit-based hold, an error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_holds_accounts_create_execute()` to send, or `vault_matters_holds_accounts_create` for simplest API.

pub fn vault_matters_holds_accounts_create_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
    holdId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/holds/{}/accounts",
        matterId, holdId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/matters/{matterId}/holds/{holdId}/accounts
/// Adds an account to a hold. Accounts can be added only to a hold that does not have an organizational unit set. If you try to add an account to an organizational unit-based hold, an error is returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_holds_accounts_create_execute()` or `vault_matters_holds_accounts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_accounts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_accounts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HeldAccount>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HeldAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/matters/{matterId}/holds/{holdId}/accounts
/// Adds an account to a hold. Accounts can be added only to a hold that does not have an organizational unit set. If you try to add an account to an organizational unit-based hold, an error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_holds_accounts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_holds_accounts_create_task()`.
/// For the simplest API, use `vault_matters_holds_accounts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_accounts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_holds_accounts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HeldAccount>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_holds_accounts_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_holds_accounts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersHoldsAccountsCreateArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: holdId
    pub holdId: String,
}

/// POST v1/matters/{matterId}/holds/{holdId}/accounts
/// Adds an account to a hold. Accounts can be added only to a hold that does not have an organizational unit set. If you try to add an account to an organizational unit-based hold, an error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_holds_accounts_create_builder()` + `vault_matters_holds_accounts_create_execute()`.
/// For task-level control, use `vault_matters_holds_accounts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_accounts_create(
    client: &SimpleHttpClient,
    args: &VaultMattersHoldsAccountsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HeldAccount>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vault_matters_holds_accounts_create_builder(client, &args.matterId, &args.holdId)?;
    vault_matters_holds_accounts_create_execute(builder)
}

/// DELETE v1/matters/{matterId}/holds/{holdId}/accounts/{accountId}
/// Removes an account from a hold.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_holds_accounts_delete_execute()` to send, or `vault_matters_holds_accounts_delete` for simplest API.

pub fn vault_matters_holds_accounts_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
    holdId: &String,
    accountId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/holds/{}/accounts/{}",
        matterId, holdId, accountId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/matters/{matterId}/holds/{holdId}/accounts/{accountId}
/// Removes an account from a hold.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_holds_accounts_delete_execute()` or `vault_matters_holds_accounts_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_accounts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_accounts_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/matters/{matterId}/holds/{holdId}/accounts/{accountId}
/// Removes an account from a hold.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_holds_accounts_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_holds_accounts_delete_task()`.
/// For the simplest API, use `vault_matters_holds_accounts_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_accounts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_holds_accounts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_holds_accounts_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_holds_accounts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersHoldsAccountsDeleteArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: holdId
    pub holdId: String,
    /// Path parameter: accountId
    pub accountId: String,
}

/// DELETE v1/matters/{matterId}/holds/{holdId}/accounts/{accountId}
/// Removes an account from a hold.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_holds_accounts_delete_builder()` + `vault_matters_holds_accounts_delete_execute()`.
/// For task-level control, use `vault_matters_holds_accounts_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_accounts_delete(
    client: &SimpleHttpClient,
    args: &VaultMattersHoldsAccountsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_holds_accounts_delete_builder(
        client,
        &args.matterId,
        &args.holdId,
        &args.accountId,
    )?;
    vault_matters_holds_accounts_delete_execute(builder)
}

/// GET v1/matters/{matterId}/holds/{holdId}/accounts
/// Lists the accounts covered by a hold. This can list only individually-specified accounts covered by the hold. If the hold covers an organizational unit, use the [Admin SDK](<https://developers.google.`com/admin-sdk/`>). to list the members of the organizational unit on hold.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_holds_accounts_list_execute()` to send, or `vault_matters_holds_accounts_list` for simplest API.

pub fn vault_matters_holds_accounts_list_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
    holdId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/holds/{}/accounts",
        matterId, holdId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/matters/{matterId}/holds/{holdId}/accounts
/// Lists the accounts covered by a hold. This can list only individually-specified accounts covered by the hold. If the hold covers an organizational unit, use the [Admin SDK](<https://developers.google.`com/admin-sdk/`>). to list the members of the organizational unit on hold.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_holds_accounts_list_execute()` or `vault_matters_holds_accounts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_accounts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_accounts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListHeldAccountsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListHeldAccountsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}/holds/{holdId}/accounts
/// Lists the accounts covered by a hold. This can list only individually-specified accounts covered by the hold. If the hold covers an organizational unit, use the [Admin SDK](<https://developers.google.`com/admin-sdk/`>). to list the members of the organizational unit on hold.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_holds_accounts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_holds_accounts_list_task()`.
/// For the simplest API, use `vault_matters_holds_accounts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_accounts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_holds_accounts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListHeldAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vault_matters_holds_accounts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_holds_accounts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersHoldsAccountsListArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: holdId
    pub holdId: String,
}

/// GET v1/matters/{matterId}/holds/{holdId}/accounts
/// Lists the accounts covered by a hold. This can list only individually-specified accounts covered by the hold. If the hold covers an organizational unit, use the [Admin SDK](<https://developers.google.`com/admin-sdk/`>). to list the members of the organizational unit on hold.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_holds_accounts_list_builder()` + `vault_matters_holds_accounts_list_execute()`.
/// For task-level control, use `vault_matters_holds_accounts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_accounts_list(
    client: &SimpleHttpClient,
    args: &VaultMattersHoldsAccountsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListHeldAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vault_matters_holds_accounts_list_builder(client, &args.matterId, &args.holdId)?;
    vault_matters_holds_accounts_list_execute(builder)
}

/// POST v1/matters/{matterId}/savedQueries
/// Creates a saved query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_saved_queries_create_execute()` to send, or `vault_matters_saved_queries_create` for simplest API.

pub fn vault_matters_saved_queries_create_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/savedQueries",
        matterId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/matters/{matterId}/savedQueries
/// Creates a saved query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_saved_queries_create_execute()` or `vault_matters_saved_queries_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_saved_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_saved_queries_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SavedQuery>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/matters/{matterId}/savedQueries
/// Creates a saved query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_saved_queries_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_saved_queries_create_task()`.
/// For the simplest API, use `vault_matters_saved_queries_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_saved_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_saved_queries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_saved_queries_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_saved_queries_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersSavedQueriesCreateArgs {
    /// Path parameter: matterId
    pub matterId: String,
}

/// POST v1/matters/{matterId}/savedQueries
/// Creates a saved query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_saved_queries_create_builder()` + `vault_matters_saved_queries_create_execute()`.
/// For task-level control, use `vault_matters_saved_queries_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_saved_queries_create(
    client: &SimpleHttpClient,
    args: &VaultMattersSavedQueriesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_saved_queries_create_builder(client, &args.matterId)?;
    vault_matters_saved_queries_create_execute(builder)
}

/// DELETE v1/matters/{matterId}/savedQueries/{savedQueryId}
/// Deletes the specified saved query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_saved_queries_delete_execute()` to send, or `vault_matters_saved_queries_delete` for simplest API.

pub fn vault_matters_saved_queries_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
    savedQueryId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/savedQueries/{}",
        matterId, savedQueryId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/matters/{matterId}/savedQueries/{savedQueryId}
/// Deletes the specified saved query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_saved_queries_delete_execute()` or `vault_matters_saved_queries_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_saved_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_saved_queries_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/matters/{matterId}/savedQueries/{savedQueryId}
/// Deletes the specified saved query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_saved_queries_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_saved_queries_delete_task()`.
/// For the simplest API, use `vault_matters_saved_queries_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_saved_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_saved_queries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_saved_queries_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_saved_queries_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersSavedQueriesDeleteArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: savedQueryId
    pub savedQueryId: String,
}

/// DELETE v1/matters/{matterId}/savedQueries/{savedQueryId}
/// Deletes the specified saved query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_saved_queries_delete_builder()` + `vault_matters_saved_queries_delete_execute()`.
/// For task-level control, use `vault_matters_saved_queries_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_saved_queries_delete(
    client: &SimpleHttpClient,
    args: &VaultMattersSavedQueriesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vault_matters_saved_queries_delete_builder(client, &args.matterId, &args.savedQueryId)?;
    vault_matters_saved_queries_delete_execute(builder)
}

/// GET v1/matters/{matterId}/savedQueries/{savedQueryId}
/// Retrieves the specified saved query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_saved_queries_get_execute()` to send, or `vault_matters_saved_queries_get` for simplest API.

pub fn vault_matters_saved_queries_get_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
    savedQueryId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/savedQueries/{}",
        matterId, savedQueryId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/matters/{matterId}/savedQueries/{savedQueryId}
/// Retrieves the specified saved query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_saved_queries_get_execute()` or `vault_matters_saved_queries_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_saved_queries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_saved_queries_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SavedQuery>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}/savedQueries/{savedQueryId}
/// Retrieves the specified saved query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_saved_queries_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_saved_queries_get_task()`.
/// For the simplest API, use `vault_matters_saved_queries_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_saved_queries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_saved_queries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_saved_queries_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_saved_queries_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersSavedQueriesGetArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: savedQueryId
    pub savedQueryId: String,
}

/// GET v1/matters/{matterId}/savedQueries/{savedQueryId}
/// Retrieves the specified saved query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_saved_queries_get_builder()` + `vault_matters_saved_queries_get_execute()`.
/// For task-level control, use `vault_matters_saved_queries_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_saved_queries_get(
    client: &SimpleHttpClient,
    args: &VaultMattersSavedQueriesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vault_matters_saved_queries_get_builder(client, &args.matterId, &args.savedQueryId)?;
    vault_matters_saved_queries_get_execute(builder)
}

/// GET v1/matters/{matterId}/savedQueries
/// Lists the saved queries in a matter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_saved_queries_list_execute()` to send, or `vault_matters_saved_queries_list` for simplest API.

pub fn vault_matters_saved_queries_list_builder<R>(
    client: &SimpleHttpClient<R>,
    matterId: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/savedQueries",
        matterId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/matters/{matterId}/savedQueries
/// Lists the saved queries in a matter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_saved_queries_list_execute()` or `vault_matters_saved_queries_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_saved_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_saved_queries_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSavedQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}/savedQueries
/// Lists the saved queries in a matter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_saved_queries_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_saved_queries_list_task()`.
/// For the simplest API, use `vault_matters_saved_queries_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_saved_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_saved_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vault_matters_saved_queries_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_saved_queries_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersSavedQueriesListArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/matters/{matterId}/savedQueries
/// Lists the saved queries in a matter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_saved_queries_list_builder()` + `vault_matters_saved_queries_list_execute()`.
/// For task-level control, use `vault_matters_saved_queries_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_saved_queries_list(
    client: &SimpleHttpClient,
    args: &VaultMattersSavedQueriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vault_matters_saved_queries_list_builder(
        client,
        &args.matterId,
        &args.pageSize,
        &args.pageToken,
    )?;
    vault_matters_saved_queries_list_execute(builder)
}

/// POST v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_operations_cancel_execute()` to send, or `vault_operations_cancel` for simplest API.

pub fn vault_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://vault.googleapis.com/v1/operations/{}:cancel", name,);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_operations_cancel_execute()` or `vault_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_operations_cancel_task()`.
/// For the simplest API, use `vault_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_operations_cancel_builder()` + `vault_operations_cancel_execute()`.
/// For task-level control, use `vault_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_operations_cancel(
    client: &SimpleHttpClient,
    args: &VaultOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_operations_cancel_builder(client, &args.name)?;
    vault_operations_cancel_execute(builder)
}

/// DELETE v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_operations_delete_execute()` to send, or `vault_operations_delete` for simplest API.

pub fn vault_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://vault.googleapis.com/v1/operations/{}", name,);

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_operations_delete_execute()` or `vault_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_operations_delete_task()`.
/// For the simplest API, use `vault_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_operations_delete_builder()` + `vault_operations_delete_execute()`.
/// For task-level control, use `vault_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_operations_delete(
    client: &SimpleHttpClient,
    args: &VaultOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_operations_delete_builder(client, &args.name)?;
    vault_operations_delete_execute(builder)
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_operations_get_execute()` to send, or `vault_operations_get` for simplest API.

pub fn vault_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://vault.googleapis.com/v1/operations/{}", name,);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_operations_get_execute()` or `vault_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_operations_get_task()`.
/// For the simplest API, use `vault_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_operations_get_builder()` + `vault_operations_get_execute()`.
/// For task-level control, use `vault_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_operations_get(
    client: &SimpleHttpClient,
    args: &VaultOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_operations_get_builder(client, &args.name)?;
    vault_operations_get_execute(builder)
}

/// GET v1/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_operations_list_execute()` to send, or `vault_operations_list` for simplest API.

pub fn vault_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://vault.googleapis.com/v1/operations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_operations_list_execute()` or `vault_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_operations_list_task()`.
/// For the simplest API, use `vault_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vault_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultOperationsListArgs {
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v1/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_operations_list_builder()` + `vault_operations_list_execute()`.
/// For task-level control, use `vault_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_operations_list(
    client: &SimpleHttpClient,
    args: &VaultOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vault_operations_list_builder(
        client,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    vault_operations_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for MatterPermission
// =============================================================================

/// ResourceIdentifier implementation for MatterPermission with VaultMattersAddPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersAddPermissionsArgs> for MatterPermission {
    fn generate_resource_id(&self, input: &VaultMattersAddPermissionsArgs) -> String {
        format!("gcp::vault::MatterPermission/{}", input.matterId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::MatterPermission"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CloseMatterResponse
// =============================================================================

/// ResourceIdentifier implementation for CloseMatterResponse with VaultMattersCloseArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersCloseArgs> for CloseMatterResponse {
    fn generate_resource_id(&self, input: &VaultMattersCloseArgs) -> String {
        format!("gcp::vault::CloseMatterResponse/{}", input.matterId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::CloseMatterResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VaultMattersCountArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersCountArgs> for Operation {
    fn generate_resource_id(&self, input: &VaultMattersCountArgs) -> String {
        format!("gcp::vault::Operation/{}", input.matterId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Matter
// =============================================================================

/// ResourceIdentifier implementation for Matter with VaultMattersCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersCreateArgs> for Matter {
    fn generate_resource_id(&self, input: &VaultMattersCreateArgs) -> String {
        "gcp::vault::Matter".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Matter"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Matter
// =============================================================================

/// ResourceIdentifier implementation for Matter with VaultMattersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersDeleteArgs> for Matter {
    fn generate_resource_id(&self, input: &VaultMattersDeleteArgs) -> String {
        format!("gcp::vault::Matter/{}", input.matterId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Matter"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Matter
// =============================================================================

/// ResourceIdentifier implementation for Matter with VaultMattersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersGetArgs> for Matter {
    fn generate_resource_id(&self, input: &VaultMattersGetArgs) -> String {
        format!("gcp::vault::Matter/{}", input.matterId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Matter"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMattersResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMattersResponse with VaultMattersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersListArgs> for ListMattersResponse {
    fn generate_resource_id(&self, input: &VaultMattersListArgs) -> String {
        "gcp::vault::ListMattersResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::ListMattersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with VaultMattersRemovePermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersRemovePermissionsArgs> for Empty {
    fn generate_resource_id(&self, input: &VaultMattersRemovePermissionsArgs) -> String {
        format!("gcp::vault::Empty/{}", input.matterId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReopenMatterResponse
// =============================================================================

/// ResourceIdentifier implementation for ReopenMatterResponse with VaultMattersReopenArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersReopenArgs> for ReopenMatterResponse {
    fn generate_resource_id(&self, input: &VaultMattersReopenArgs) -> String {
        format!("gcp::vault::ReopenMatterResponse/{}", input.matterId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::ReopenMatterResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Matter
// =============================================================================

/// ResourceIdentifier implementation for Matter with VaultMattersUndeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersUndeleteArgs> for Matter {
    fn generate_resource_id(&self, input: &VaultMattersUndeleteArgs) -> String {
        format!("gcp::vault::Matter/{}", input.matterId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Matter"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Matter
// =============================================================================

/// ResourceIdentifier implementation for Matter with VaultMattersUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersUpdateArgs> for Matter {
    fn generate_resource_id(&self, input: &VaultMattersUpdateArgs) -> String {
        format!("gcp::vault::Matter/{}", input.matterId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Matter"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Export
// =============================================================================

/// ResourceIdentifier implementation for Export with VaultMattersExportsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersExportsCreateArgs> for Export {
    fn generate_resource_id(&self, input: &VaultMattersExportsCreateArgs) -> String {
        format!("gcp::vault::Export/{}", input.matterId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Export"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with VaultMattersExportsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersExportsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &VaultMattersExportsDeleteArgs) -> String {
        format!("gcp::vault::Empty/{}/{}", input.matterId, input.exportId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Export
// =============================================================================

/// ResourceIdentifier implementation for Export with VaultMattersExportsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersExportsGetArgs> for Export {
    fn generate_resource_id(&self, input: &VaultMattersExportsGetArgs) -> String {
        format!("gcp::vault::Export/{}/{}", input.matterId, input.exportId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Export"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListExportsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListExportsResponse with VaultMattersExportsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersExportsListArgs> for ListExportsResponse {
    fn generate_resource_id(&self, input: &VaultMattersExportsListArgs) -> String {
        format!("gcp::vault::ListExportsResponse/{}", input.matterId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::ListExportsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AddHeldAccountsResponse
// =============================================================================

/// ResourceIdentifier implementation for AddHeldAccountsResponse with VaultMattersHoldsAddHeldAccountsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersHoldsAddHeldAccountsArgs> for AddHeldAccountsResponse {
    fn generate_resource_id(&self, input: &VaultMattersHoldsAddHeldAccountsArgs) -> String {
        format!(
            "gcp::vault::AddHeldAccountsResponse/{}/{}",
            input.matterId, input.holdId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::AddHeldAccountsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Hold
// =============================================================================

/// ResourceIdentifier implementation for Hold with VaultMattersHoldsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersHoldsCreateArgs> for Hold {
    fn generate_resource_id(&self, input: &VaultMattersHoldsCreateArgs) -> String {
        format!("gcp::vault::Hold/{}", input.matterId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Hold"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with VaultMattersHoldsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersHoldsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &VaultMattersHoldsDeleteArgs) -> String {
        format!("gcp::vault::Empty/{}/{}", input.matterId, input.holdId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Hold
// =============================================================================

/// ResourceIdentifier implementation for Hold with VaultMattersHoldsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersHoldsGetArgs> for Hold {
    fn generate_resource_id(&self, input: &VaultMattersHoldsGetArgs) -> String {
        format!("gcp::vault::Hold/{}/{}", input.matterId, input.holdId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Hold"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListHoldsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListHoldsResponse with VaultMattersHoldsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersHoldsListArgs> for ListHoldsResponse {
    fn generate_resource_id(&self, input: &VaultMattersHoldsListArgs) -> String {
        format!("gcp::vault::ListHoldsResponse/{}", input.matterId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::ListHoldsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RemoveHeldAccountsResponse
// =============================================================================

/// ResourceIdentifier implementation for RemoveHeldAccountsResponse with VaultMattersHoldsRemoveHeldAccountsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersHoldsRemoveHeldAccountsArgs> for RemoveHeldAccountsResponse {
    fn generate_resource_id(&self, input: &VaultMattersHoldsRemoveHeldAccountsArgs) -> String {
        format!(
            "gcp::vault::RemoveHeldAccountsResponse/{}/{}",
            input.matterId, input.holdId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::RemoveHeldAccountsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Hold
// =============================================================================

/// ResourceIdentifier implementation for Hold with VaultMattersHoldsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersHoldsUpdateArgs> for Hold {
    fn generate_resource_id(&self, input: &VaultMattersHoldsUpdateArgs) -> String {
        format!("gcp::vault::Hold/{}/{}", input.matterId, input.holdId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Hold"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HeldAccount
// =============================================================================

/// ResourceIdentifier implementation for HeldAccount with VaultMattersHoldsAccountsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersHoldsAccountsCreateArgs> for HeldAccount {
    fn generate_resource_id(&self, input: &VaultMattersHoldsAccountsCreateArgs) -> String {
        format!(
            "gcp::vault::HeldAccount/{}/{}",
            input.matterId, input.holdId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::HeldAccount"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with VaultMattersHoldsAccountsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersHoldsAccountsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &VaultMattersHoldsAccountsDeleteArgs) -> String {
        format!(
            "gcp::vault::Empty/{}/{}/{}",
            input.matterId, input.holdId, input.accountId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListHeldAccountsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListHeldAccountsResponse with VaultMattersHoldsAccountsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersHoldsAccountsListArgs> for ListHeldAccountsResponse {
    fn generate_resource_id(&self, input: &VaultMattersHoldsAccountsListArgs) -> String {
        format!(
            "gcp::vault::ListHeldAccountsResponse/{}/{}",
            input.matterId, input.holdId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::ListHeldAccountsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SavedQuery
// =============================================================================

/// ResourceIdentifier implementation for SavedQuery with VaultMattersSavedQueriesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersSavedQueriesCreateArgs> for SavedQuery {
    fn generate_resource_id(&self, input: &VaultMattersSavedQueriesCreateArgs) -> String {
        format!("gcp::vault::SavedQuery/{}", input.matterId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::SavedQuery"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with VaultMattersSavedQueriesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersSavedQueriesDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &VaultMattersSavedQueriesDeleteArgs) -> String {
        format!(
            "gcp::vault::Empty/{}/{}",
            input.matterId, input.savedQueryId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SavedQuery
// =============================================================================

/// ResourceIdentifier implementation for SavedQuery with VaultMattersSavedQueriesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersSavedQueriesGetArgs> for SavedQuery {
    fn generate_resource_id(&self, input: &VaultMattersSavedQueriesGetArgs) -> String {
        format!(
            "gcp::vault::SavedQuery/{}/{}",
            input.matterId, input.savedQueryId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::SavedQuery"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSavedQueriesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSavedQueriesResponse with VaultMattersSavedQueriesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultMattersSavedQueriesListArgs> for ListSavedQueriesResponse {
    fn generate_resource_id(&self, input: &VaultMattersSavedQueriesListArgs) -> String {
        format!("gcp::vault::ListSavedQueriesResponse/{}", input.matterId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::ListSavedQueriesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with VaultOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultOperationsCancelArgs> for Empty {
    fn generate_resource_id(&self, input: &VaultOperationsCancelArgs) -> String {
        format!("gcp::vault::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with VaultOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultOperationsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &VaultOperationsDeleteArgs) -> String {
        format!("gcp::vault::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VaultOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &VaultOperationsGetArgs) -> String {
        format!("gcp::vault::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with VaultOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VaultOperationsListArgs> for ListOperationsResponse {
    fn generate_resource_id(&self, input: &VaultOperationsListArgs) -> String {
        "gcp::vault::ListOperationsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vault::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
