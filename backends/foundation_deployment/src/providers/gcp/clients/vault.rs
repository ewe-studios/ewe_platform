//! Auto-generated API clients for gcp/vault.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/matters/{matterId}:addPermissions
/// Adds an account as a matter collaborator.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_add_permissions_execute()` to send, or `vault_matters_add_permissions` for simplest API.

pub fn vault_matters_add_permissions_builder(
    client: &SimpleHttpClient,
    matterId: String,
    body: &AddMatterPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}:addPermissions",
        matterId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/matters/{matterId}:addPermissions
/// Adds an account as a matter collaborator.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_add_permissions_execute()` or `vault_matters_add_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_add_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_add_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MatterPermission>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MatterPermission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}:addPermissions
/// Adds an account as a matter collaborator.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_add_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_add_permissions_task()`.
/// For the simplest API, use `vault_matters_add_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_add_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_add_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MatterPermission>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vault_matters_add_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_add_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersAddPermissionsArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Request body.
    pub body: AddMatterPermissionsRequest,
}

/// GET v1/matters/{matterId}:addPermissions
/// Adds an account as a matter collaborator.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_add_permissions_builder()` + `vault_matters_add_permissions_execute()`.
/// For task-level control, use `vault_matters_add_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_add_permissions(
    client: &SimpleHttpClient,
    args: &VaultMattersAddPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MatterPermission>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vault_matters_add_permissions_builder(client, args.matterId.clone(), &args.body)?;
    vault_matters_add_permissions_execute(builder)
}

/// GET v1/matters/{matterId}:close
/// Closes the specified matter. Returns the matter with updated state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_close_execute()` to send, or `vault_matters_close` for simplest API.

pub fn vault_matters_close_builder(
    client: &SimpleHttpClient,
    matterId: String,
    body: &CloseMatterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}:close",
        matterId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/matters/{matterId}:close
/// Closes the specified matter. Returns the matter with updated state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_close_execute()` or `vault_matters_close`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_close_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_close_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CloseMatterResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CloseMatterResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}:close
/// Closes the specified matter. Returns the matter with updated state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_close_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_close_task()`.
/// For the simplest API, use `vault_matters_close()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_close_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_close_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CloseMatterResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vault_matters_close_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_close`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersCloseArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Request body.
    pub body: CloseMatterRequest,
}

/// GET v1/matters/{matterId}:close
/// Closes the specified matter. Returns the matter with updated state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_close_builder()` + `vault_matters_close_execute()`.
/// For task-level control, use `vault_matters_close_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_close(
    client: &SimpleHttpClient,
    args: &VaultMattersCloseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CloseMatterResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vault_matters_close_builder(client, args.matterId.clone(), &args.body)?;
    vault_matters_close_execute(builder)
}

/// GET v1/matters/{matterId}:count
/// Counts the accounts processed by the specified query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_count_execute()` to send, or `vault_matters_count` for simplest API.

pub fn vault_matters_count_builder(
    client: &SimpleHttpClient,
    matterId: String,
    body: &CountArtifactsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}:count",
        matterId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/matters/{matterId}:count
/// Counts the accounts processed by the specified query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_count_execute()` or `vault_matters_count`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_count_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_count_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}:count
/// Counts the accounts processed by the specified query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_count_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_count_task()`.
/// For the simplest API, use `vault_matters_count()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_count_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_count_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_count_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_count`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersCountArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Request body.
    pub body: CountArtifactsRequest,
}

/// GET v1/matters/{matterId}:count
/// Counts the accounts processed by the specified query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_count_builder()` + `vault_matters_count_execute()`.
/// For task-level control, use `vault_matters_count_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_count(
    client: &SimpleHttpClient,
    args: &VaultMattersCountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_count_builder(client, args.matterId.clone(), &args.body)?;
    vault_matters_count_execute(builder)
}

/// GET v1/matters
/// Creates a matter with the given name and description. The initial state is open, and the owner is the method caller. Returns the created matter with default view.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_create_execute()` to send, or `vault_matters_create` for simplest API.

pub fn vault_matters_create_builder(
    client: &SimpleHttpClient,
    body: &Matter,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://vault.googleapis.com/v1/matters",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/matters
/// Creates a matter with the given name and description. The initial state is open, and the owner is the method caller. Returns the created matter with default view.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_create_execute()` or `vault_matters_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Matter>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Matter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters
/// Creates a matter with the given name and description. The initial state is open, and the owner is the method caller. Returns the created matter with default view.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_create_task()`.
/// For the simplest API, use `vault_matters_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Matter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersCreateArgs {
    /// Request body.
    pub body: Matter,
}

/// GET v1/matters
/// Creates a matter with the given name and description. The initial state is open, and the owner is the method caller. Returns the created matter with default view.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_create_builder()` + `vault_matters_create_execute()`.
/// For task-level control, use `vault_matters_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_create(
    client: &SimpleHttpClient,
    args: &VaultMattersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Matter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_create_builder(client, &args.body)?;
    vault_matters_create_execute(builder)
}

/// GET v1/matters/{matterId}
/// Deletes the specified matter. Returns the matter with updated state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_delete_execute()` to send, or `vault_matters_delete` for simplest API.

pub fn vault_matters_delete_builder(
    client: &SimpleHttpClient,
    matterId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}",
        matterId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/matters/{matterId}
/// Deletes the specified matter. Returns the matter with updated state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_delete_execute()` or `vault_matters_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Matter>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Matter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}
/// Deletes the specified matter. Returns the matter with updated state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_delete_task()`.
/// For the simplest API, use `vault_matters_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Matter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersDeleteArgs {
    /// Path parameter: matterId
    pub matterId: String,
}

/// GET v1/matters/{matterId}
/// Deletes the specified matter. Returns the matter with updated state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_delete_builder()` + `vault_matters_delete_execute()`.
/// For task-level control, use `vault_matters_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_delete(
    client: &SimpleHttpClient,
    args: &VaultMattersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Matter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_delete_builder(client, args.matterId.clone())?;
    vault_matters_delete_execute(builder)
}

/// GET v1/matters/{matterId}:removePermissions
/// Removes an account as a matter collaborator.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_remove_permissions_execute()` to send, or `vault_matters_remove_permissions` for simplest API.

pub fn vault_matters_remove_permissions_builder(
    client: &SimpleHttpClient,
    matterId: String,
    body: &RemoveMatterPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}:removePermissions",
        matterId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/matters/{matterId}:removePermissions
/// Removes an account as a matter collaborator.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_remove_permissions_execute()` or `vault_matters_remove_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_remove_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_remove_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}:removePermissions
/// Removes an account as a matter collaborator.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_remove_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_remove_permissions_task()`.
/// For the simplest API, use `vault_matters_remove_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_remove_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_remove_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_remove_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_remove_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersRemovePermissionsArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Request body.
    pub body: RemoveMatterPermissionsRequest,
}

/// GET v1/matters/{matterId}:removePermissions
/// Removes an account as a matter collaborator.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_remove_permissions_builder()` + `vault_matters_remove_permissions_execute()`.
/// For task-level control, use `vault_matters_remove_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_remove_permissions(
    client: &SimpleHttpClient,
    args: &VaultMattersRemovePermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vault_matters_remove_permissions_builder(client, args.matterId.clone(), &args.body)?;
    vault_matters_remove_permissions_execute(builder)
}

/// GET v1/matters/{matterId}:reopen
/// Reopens the specified matter. Returns the matter with updated state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_reopen_execute()` to send, or `vault_matters_reopen` for simplest API.

pub fn vault_matters_reopen_builder(
    client: &SimpleHttpClient,
    matterId: String,
    body: &ReopenMatterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}:reopen",
        matterId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/matters/{matterId}:reopen
/// Reopens the specified matter. Returns the matter with updated state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_reopen_execute()` or `vault_matters_reopen`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_reopen_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_reopen_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReopenMatterResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReopenMatterResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}:reopen
/// Reopens the specified matter. Returns the matter with updated state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_reopen_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_reopen_task()`.
/// For the simplest API, use `vault_matters_reopen()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_reopen_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_reopen_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReopenMatterResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vault_matters_reopen_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_reopen`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersReopenArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Request body.
    pub body: ReopenMatterRequest,
}

/// GET v1/matters/{matterId}:reopen
/// Reopens the specified matter. Returns the matter with updated state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_reopen_builder()` + `vault_matters_reopen_execute()`.
/// For task-level control, use `vault_matters_reopen_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_reopen(
    client: &SimpleHttpClient,
    args: &VaultMattersReopenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReopenMatterResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vault_matters_reopen_builder(client, args.matterId.clone(), &args.body)?;
    vault_matters_reopen_execute(builder)
}

/// GET v1/matters/{matterId}:undelete
/// Undeletes the specified matter. Returns the matter with updated state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_undelete_execute()` to send, or `vault_matters_undelete` for simplest API.

pub fn vault_matters_undelete_builder(
    client: &SimpleHttpClient,
    matterId: String,
    body: &UndeleteMatterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}:undelete",
        matterId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/matters/{matterId}:undelete
/// Undeletes the specified matter. Returns the matter with updated state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_undelete_execute()` or `vault_matters_undelete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_undelete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Matter>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Matter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}:undelete
/// Undeletes the specified matter. Returns the matter with updated state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_undelete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_undelete_task()`.
/// For the simplest API, use `vault_matters_undelete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Matter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_undelete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersUndeleteArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Request body.
    pub body: UndeleteMatterRequest,
}

/// GET v1/matters/{matterId}:undelete
/// Undeletes the specified matter. Returns the matter with updated state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_undelete_builder()` + `vault_matters_undelete_execute()`.
/// For task-level control, use `vault_matters_undelete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_undelete(
    client: &SimpleHttpClient,
    args: &VaultMattersUndeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Matter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_undelete_builder(client, args.matterId.clone(), &args.body)?;
    vault_matters_undelete_execute(builder)
}

/// GET v1/matters/{matterId}/exports
/// Creates an export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_exports_create_execute()` to send, or `vault_matters_exports_create` for simplest API.

pub fn vault_matters_exports_create_builder(
    client: &SimpleHttpClient,
    matterId: String,
    body: &Export,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/exports",
        matterId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/matters/{matterId}/exports
/// Creates an export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_exports_create_execute()` or `vault_matters_exports_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_exports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_exports_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Export>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Export = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}/exports
/// Creates an export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_exports_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_exports_create_task()`.
/// For the simplest API, use `vault_matters_exports_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_exports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_exports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Export>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_exports_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_exports_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersExportsCreateArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Request body.
    pub body: Export,
}

/// GET v1/matters/{matterId}/exports
/// Creates an export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_exports_create_builder()` + `vault_matters_exports_create_execute()`.
/// For task-level control, use `vault_matters_exports_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_exports_create(
    client: &SimpleHttpClient,
    args: &VaultMattersExportsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Export>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_exports_create_builder(client, args.matterId.clone(), &args.body)?;
    vault_matters_exports_create_execute(builder)
}

/// GET v1/matters/{matterId}/exports/{exportId}
/// Deletes an export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_exports_delete_execute()` to send, or `vault_matters_exports_delete` for simplest API.

pub fn vault_matters_exports_delete_builder(
    client: &SimpleHttpClient,
    matterId: String,
    exportId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/exports/{}",
        matterId.as_str(),
        exportId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/matters/{matterId}/exports/{exportId}
/// Deletes an export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_exports_delete_execute()` or `vault_matters_exports_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_exports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_exports_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}/exports/{exportId}
/// Deletes an export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_exports_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_exports_delete_task()`.
/// For the simplest API, use `vault_matters_exports_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_exports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_exports_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_exports_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_exports_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersExportsDeleteArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: exportId
    pub exportId: String,
}

/// GET v1/matters/{matterId}/exports/{exportId}
/// Deletes an export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_exports_delete_builder()` + `vault_matters_exports_delete_execute()`.
/// For task-level control, use `vault_matters_exports_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_exports_delete(
    client: &SimpleHttpClient,
    args: &VaultMattersExportsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vault_matters_exports_delete_builder(client, args.matterId.clone(), args.exportId.clone())?;
    vault_matters_exports_delete_execute(builder)
}

/// GET v1/matters/{matterId}/holds/{holdId}:addHeldAccounts
/// Adds accounts to a hold. Returns a list of accounts that have been successfully added. Accounts can be added only to an existing account-based hold.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_holds_add_held_accounts_execute()` to send, or `vault_matters_holds_add_held_accounts` for simplest API.

pub fn vault_matters_holds_add_held_accounts_builder(
    client: &SimpleHttpClient,
    matterId: String,
    holdId: String,
    body: &AddHeldAccountsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/holds/{}:addHeldAccounts",
        matterId.as_str(),
        holdId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/matters/{matterId}/holds/{holdId}:addHeldAccounts
/// Adds accounts to a hold. Returns a list of accounts that have been successfully added. Accounts can be added only to an existing account-based hold.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_holds_add_held_accounts_execute()` or `vault_matters_holds_add_held_accounts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_add_held_accounts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_add_held_accounts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddHeldAccountsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddHeldAccountsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}/holds/{holdId}:addHeldAccounts
/// Adds accounts to a hold. Returns a list of accounts that have been successfully added. Accounts can be added only to an existing account-based hold.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_holds_add_held_accounts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_holds_add_held_accounts_task()`.
/// For the simplest API, use `vault_matters_holds_add_held_accounts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_add_held_accounts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_holds_add_held_accounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddHeldAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vault_matters_holds_add_held_accounts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_holds_add_held_accounts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersHoldsAddHeldAccountsArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: holdId
    pub holdId: String,
    /// Request body.
    pub body: AddHeldAccountsRequest,
}

/// GET v1/matters/{matterId}/holds/{holdId}:addHeldAccounts
/// Adds accounts to a hold. Returns a list of accounts that have been successfully added. Accounts can be added only to an existing account-based hold.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_holds_add_held_accounts_builder()` + `vault_matters_holds_add_held_accounts_execute()`.
/// For task-level control, use `vault_matters_holds_add_held_accounts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_add_held_accounts(
    client: &SimpleHttpClient,
    args: &VaultMattersHoldsAddHeldAccountsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddHeldAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vault_matters_holds_add_held_accounts_builder(
        client,
        args.matterId.clone(),
        args.holdId.clone(),
        &args.body,
    )?;
    vault_matters_holds_add_held_accounts_execute(builder)
}

/// GET v1/matters/{matterId}/holds
/// Creates a hold in the specified matter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_holds_create_execute()` to send, or `vault_matters_holds_create` for simplest API.

pub fn vault_matters_holds_create_builder(
    client: &SimpleHttpClient,
    matterId: String,
    body: &Hold,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/holds",
        matterId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/matters/{matterId}/holds
/// Creates a hold in the specified matter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_holds_create_execute()` or `vault_matters_holds_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Hold>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Hold = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}/holds
/// Creates a hold in the specified matter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_holds_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_holds_create_task()`.
/// For the simplest API, use `vault_matters_holds_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_holds_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hold>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_holds_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_holds_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersHoldsCreateArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Request body.
    pub body: Hold,
}

/// GET v1/matters/{matterId}/holds
/// Creates a hold in the specified matter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_holds_create_builder()` + `vault_matters_holds_create_execute()`.
/// For task-level control, use `vault_matters_holds_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_create(
    client: &SimpleHttpClient,
    args: &VaultMattersHoldsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hold>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_holds_create_builder(client, args.matterId.clone(), &args.body)?;
    vault_matters_holds_create_execute(builder)
}

/// GET v1/matters/{matterId}/holds/{holdId}
/// Removes the specified hold and releases the accounts or organizational unit covered by the hold. If the data is not preserved by another hold or retention rule, it might be purged.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_holds_delete_execute()` to send, or `vault_matters_holds_delete` for simplest API.

pub fn vault_matters_holds_delete_builder(
    client: &SimpleHttpClient,
    matterId: String,
    holdId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/holds/{}",
        matterId.as_str(),
        holdId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/matters/{matterId}/holds/{holdId}
/// Removes the specified hold and releases the accounts or organizational unit covered by the hold. If the data is not preserved by another hold or retention rule, it might be purged.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_holds_delete_execute()` or `vault_matters_holds_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}/holds/{holdId}
/// Removes the specified hold and releases the accounts or organizational unit covered by the hold. If the data is not preserved by another hold or retention rule, it might be purged.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_holds_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_holds_delete_task()`.
/// For the simplest API, use `vault_matters_holds_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_holds_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_holds_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_holds_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersHoldsDeleteArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: holdId
    pub holdId: String,
}

/// GET v1/matters/{matterId}/holds/{holdId}
/// Removes the specified hold and releases the accounts or organizational unit covered by the hold. If the data is not preserved by another hold or retention rule, it might be purged.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_holds_delete_builder()` + `vault_matters_holds_delete_execute()`.
/// For task-level control, use `vault_matters_holds_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_delete(
    client: &SimpleHttpClient,
    args: &VaultMattersHoldsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vault_matters_holds_delete_builder(client, args.matterId.clone(), args.holdId.clone())?;
    vault_matters_holds_delete_execute(builder)
}

/// GET v1/matters/{matterId}/holds/{holdId}:removeHeldAccounts
/// Removes the specified accounts from a hold. Returns a list of statuses in the same order as the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_holds_remove_held_accounts_execute()` to send, or `vault_matters_holds_remove_held_accounts` for simplest API.

pub fn vault_matters_holds_remove_held_accounts_builder(
    client: &SimpleHttpClient,
    matterId: String,
    holdId: String,
    body: &RemoveHeldAccountsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/holds/{}:removeHeldAccounts",
        matterId.as_str(),
        holdId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/matters/{matterId}/holds/{holdId}:removeHeldAccounts
/// Removes the specified accounts from a hold. Returns a list of statuses in the same order as the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_holds_remove_held_accounts_execute()` or `vault_matters_holds_remove_held_accounts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_remove_held_accounts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_remove_held_accounts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemoveHeldAccountsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemoveHeldAccountsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}/holds/{holdId}:removeHeldAccounts
/// Removes the specified accounts from a hold. Returns a list of statuses in the same order as the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_holds_remove_held_accounts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_holds_remove_held_accounts_task()`.
/// For the simplest API, use `vault_matters_holds_remove_held_accounts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_remove_held_accounts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_holds_remove_held_accounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RemoveHeldAccountsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = vault_matters_holds_remove_held_accounts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_holds_remove_held_accounts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersHoldsRemoveHeldAccountsArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: holdId
    pub holdId: String,
    /// Request body.
    pub body: RemoveHeldAccountsRequest,
}

/// GET v1/matters/{matterId}/holds/{holdId}:removeHeldAccounts
/// Removes the specified accounts from a hold. Returns a list of statuses in the same order as the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_holds_remove_held_accounts_builder()` + `vault_matters_holds_remove_held_accounts_execute()`.
/// For task-level control, use `vault_matters_holds_remove_held_accounts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_remove_held_accounts(
    client: &SimpleHttpClient,
    args: &VaultMattersHoldsRemoveHeldAccountsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RemoveHeldAccountsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = vault_matters_holds_remove_held_accounts_builder(
        client,
        args.matterId.clone(),
        args.holdId.clone(),
        &args.body,
    )?;
    vault_matters_holds_remove_held_accounts_execute(builder)
}

/// GET v1/matters/{matterId}/holds/{holdId}/accounts
/// Adds an account to a hold. Accounts can be added only to a hold that does not have an organizational unit set. If you try to add an account to an organizational unit-based hold, an error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_holds_accounts_create_execute()` to send, or `vault_matters_holds_accounts_create` for simplest API.

pub fn vault_matters_holds_accounts_create_builder(
    client: &SimpleHttpClient,
    matterId: String,
    holdId: String,
    body: &HeldAccount,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/holds/{}/accounts",
        matterId.as_str(),
        holdId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/matters/{matterId}/holds/{holdId}/accounts
/// Adds an account to a hold. Accounts can be added only to a hold that does not have an organizational unit set. If you try to add an account to an organizational unit-based hold, an error is returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_holds_accounts_create_execute()` or `vault_matters_holds_accounts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_accounts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_accounts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HeldAccount>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HeldAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}/holds/{holdId}/accounts
/// Adds an account to a hold. Accounts can be added only to a hold that does not have an organizational unit set. If you try to add an account to an organizational unit-based hold, an error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_holds_accounts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_holds_accounts_create_task()`.
/// For the simplest API, use `vault_matters_holds_accounts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_accounts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_holds_accounts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HeldAccount>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_holds_accounts_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_holds_accounts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersHoldsAccountsCreateArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: holdId
    pub holdId: String,
    /// Request body.
    pub body: HeldAccount,
}

/// GET v1/matters/{matterId}/holds/{holdId}/accounts
/// Adds an account to a hold. Accounts can be added only to a hold that does not have an organizational unit set. If you try to add an account to an organizational unit-based hold, an error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_holds_accounts_create_builder()` + `vault_matters_holds_accounts_create_execute()`.
/// For task-level control, use `vault_matters_holds_accounts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_accounts_create(
    client: &SimpleHttpClient,
    args: &VaultMattersHoldsAccountsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HeldAccount>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_holds_accounts_create_builder(
        client,
        args.matterId.clone(),
        args.holdId.clone(),
        &args.body,
    )?;
    vault_matters_holds_accounts_create_execute(builder)
}

/// GET v1/matters/{matterId}/holds/{holdId}/accounts/{accountId}
/// Removes an account from a hold.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_holds_accounts_delete_execute()` to send, or `vault_matters_holds_accounts_delete` for simplest API.

pub fn vault_matters_holds_accounts_delete_builder(
    client: &SimpleHttpClient,
    matterId: String,
    holdId: String,
    accountId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/holds/{}/accounts/{}",
        matterId.as_str(),
        holdId.as_str(),
        accountId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/matters/{matterId}/holds/{holdId}/accounts/{accountId}
/// Removes an account from a hold.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_holds_accounts_delete_execute()` or `vault_matters_holds_accounts_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_accounts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_accounts_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}/holds/{holdId}/accounts/{accountId}
/// Removes an account from a hold.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_holds_accounts_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_holds_accounts_delete_task()`.
/// For the simplest API, use `vault_matters_holds_accounts_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_holds_accounts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_holds_accounts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_holds_accounts_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_holds_accounts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersHoldsAccountsDeleteArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: holdId
    pub holdId: String,
    /// Path parameter: accountId
    pub accountId: String,
}

/// GET v1/matters/{matterId}/holds/{holdId}/accounts/{accountId}
/// Removes an account from a hold.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_holds_accounts_delete_builder()` + `vault_matters_holds_accounts_delete_execute()`.
/// For task-level control, use `vault_matters_holds_accounts_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_holds_accounts_delete(
    client: &SimpleHttpClient,
    args: &VaultMattersHoldsAccountsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_holds_accounts_delete_builder(
        client,
        args.matterId.clone(),
        args.holdId.clone(),
        args.accountId.clone(),
    )?;
    vault_matters_holds_accounts_delete_execute(builder)
}

/// GET v1/matters/{matterId}/savedQueries
/// Creates a saved query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_saved_queries_create_execute()` to send, or `vault_matters_saved_queries_create` for simplest API.

pub fn vault_matters_saved_queries_create_builder(
    client: &SimpleHttpClient,
    matterId: String,
    body: &SavedQuery,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/savedQueries",
        matterId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/matters/{matterId}/savedQueries
/// Creates a saved query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_saved_queries_create_execute()` or `vault_matters_saved_queries_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_saved_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_saved_queries_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SavedQuery>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}/savedQueries
/// Creates a saved query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_saved_queries_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_saved_queries_create_task()`.
/// For the simplest API, use `vault_matters_saved_queries_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_saved_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_saved_queries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_saved_queries_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_saved_queries_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersSavedQueriesCreateArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Request body.
    pub body: SavedQuery,
}

/// GET v1/matters/{matterId}/savedQueries
/// Creates a saved query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_saved_queries_create_builder()` + `vault_matters_saved_queries_create_execute()`.
/// For task-level control, use `vault_matters_saved_queries_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_saved_queries_create(
    client: &SimpleHttpClient,
    args: &VaultMattersSavedQueriesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vault_matters_saved_queries_create_builder(client, args.matterId.clone(), &args.body)?;
    vault_matters_saved_queries_create_execute(builder)
}

/// GET v1/matters/{matterId}/savedQueries/{savedQueryId}
/// Deletes the specified saved query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_matters_saved_queries_delete_execute()` to send, or `vault_matters_saved_queries_delete` for simplest API.

pub fn vault_matters_saved_queries_delete_builder(
    client: &SimpleHttpClient,
    matterId: String,
    savedQueryId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://vault.googleapis.com/v1/matters/{}/savedQueries/{}",
        matterId.as_str(),
        savedQueryId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/matters/{matterId}/savedQueries/{savedQueryId}
/// Deletes the specified saved query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_matters_saved_queries_delete_execute()` or `vault_matters_saved_queries_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_saved_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_saved_queries_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/matters/{matterId}/savedQueries/{savedQueryId}
/// Deletes the specified saved query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_matters_saved_queries_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_matters_saved_queries_delete_task()`.
/// For the simplest API, use `vault_matters_saved_queries_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_matters_saved_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_matters_saved_queries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_matters_saved_queries_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_matters_saved_queries_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultMattersSavedQueriesDeleteArgs {
    /// Path parameter: matterId
    pub matterId: String,
    /// Path parameter: savedQueryId
    pub savedQueryId: String,
}

/// GET v1/matters/{matterId}/savedQueries/{savedQueryId}
/// Deletes the specified saved query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_matters_saved_queries_delete_builder()` + `vault_matters_saved_queries_delete_execute()`.
/// For task-level control, use `vault_matters_saved_queries_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_matters_saved_queries_delete(
    client: &SimpleHttpClient,
    args: &VaultMattersSavedQueriesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_matters_saved_queries_delete_builder(
        client,
        args.matterId.clone(),
        args.savedQueryId.clone(),
    )?;
    vault_matters_saved_queries_delete_execute(builder)
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_operations_cancel_execute()` to send, or `vault_operations_cancel` for simplest API.

pub fn vault_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: String,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://vault.googleapis.com/v1/operations/{}:cancel",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_operations_cancel_execute()` or `vault_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_operations_cancel_task()`.
/// For the simplest API, use `vault_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: CancelOperationRequest,
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_operations_cancel_builder()` + `vault_operations_cancel_execute()`.
/// For task-level control, use `vault_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_operations_cancel(
    client: &SimpleHttpClient,
    args: &VaultOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_operations_cancel_builder(client, args.name.clone(), &args.body)?;
    vault_operations_cancel_execute(builder)
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vault_operations_delete_execute()` to send, or `vault_operations_delete` for simplest API.

pub fn vault_operations_delete_builder(
    client: &SimpleHttpClient,
    name: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://vault.googleapis.com/v1/operations/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vault_operations_delete_execute()` or `vault_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vault_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vault_operations_delete_task()`.
/// For the simplest API, use `vault_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vault_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vault_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vault_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vault_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VaultOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vault_operations_delete_builder()` + `vault_operations_delete_execute()`.
/// For task-level control, use `vault_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vault_operations_delete(
    client: &SimpleHttpClient,
    args: &VaultOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vault_operations_delete_builder(client, args.name.clone())?;
    vault_operations_delete_execute(builder)
}
