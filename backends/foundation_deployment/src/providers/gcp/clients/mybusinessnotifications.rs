//! Auto-generated API clients for gcp/mybusinessnotifications.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/accounts/{accountsId}/notificationSetting
/// Returns the pubsub notification settings for the account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessnotifications_accounts_get_notification_setting_execute()` to send, or `mybusinessnotifications_accounts_get_notification_setting` for simplest API.

pub fn mybusinessnotifications_accounts_get_notification_setting_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://mybusinessnotifications.googleapis.com/v1/accounts/{}/notificationSetting",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accounts/{accountsId}/notificationSetting
/// Returns the pubsub notification settings for the account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessnotifications_accounts_get_notification_setting_execute()` or `mybusinessnotifications_accounts_get_notification_setting`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessnotifications_accounts_get_notification_setting_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessnotifications_accounts_get_notification_setting_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationSetting>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationSetting = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}/notificationSetting
/// Returns the pubsub notification settings for the account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessnotifications_accounts_get_notification_setting_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessnotifications_accounts_get_notification_setting_task()`.
/// For the simplest API, use `mybusinessnotifications_accounts_get_notification_setting()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessnotifications_accounts_get_notification_setting_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessnotifications_accounts_get_notification_setting_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationSetting>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = mybusinessnotifications_accounts_get_notification_setting_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessnotifications_accounts_get_notification_setting`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessnotificationsAccountsGetNotificationSettingArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/accounts/{accountsId}/notificationSetting
/// Returns the pubsub notification settings for the account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessnotifications_accounts_get_notification_setting_builder()` + `mybusinessnotifications_accounts_get_notification_setting_execute()`.
/// For task-level control, use `mybusinessnotifications_accounts_get_notification_setting_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessnotifications_accounts_get_notification_setting(
    client: &SimpleHttpClient,
    args: &MybusinessnotificationsAccountsGetNotificationSettingArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationSetting>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        mybusinessnotifications_accounts_get_notification_setting_builder(client, &args.name)?;
    mybusinessnotifications_accounts_get_notification_setting_execute(builder)
}

/// PATCH v1/accounts/{accountsId}/notificationSetting
/// Sets the pubsub notification setting for the account informing Google which topic to send pubsub notifications for. Use the notification_types field within notification_setting to manipulate the events an account wants to subscribe to. An account will only have one notification setting resource, and only one pubsub topic can be set. To delete the setting, update with an empty notification_types
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessnotifications_accounts_update_notification_setting_execute()` to send, or `mybusinessnotifications_accounts_update_notification_setting` for simplest API.

pub fn mybusinessnotifications_accounts_update_notification_setting_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://mybusinessnotifications.googleapis.com/v1/accounts/{}/notificationSetting",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/accounts/{accountsId}/notificationSetting
/// Sets the pubsub notification setting for the account informing Google which topic to send pubsub notifications for. Use the notification_types field within notification_setting to manipulate the events an account wants to subscribe to. An account will only have one notification setting resource, and only one pubsub topic can be set. To delete the setting, update with an empty notification_types
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessnotifications_accounts_update_notification_setting_execute()` or `mybusinessnotifications_accounts_update_notification_setting`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessnotifications_accounts_update_notification_setting_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessnotifications_accounts_update_notification_setting_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationSetting>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationSetting = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/accounts/{accountsId}/notificationSetting
/// Sets the pubsub notification setting for the account informing Google which topic to send pubsub notifications for. Use the notification_types field within notification_setting to manipulate the events an account wants to subscribe to. An account will only have one notification setting resource, and only one pubsub topic can be set. To delete the setting, update with an empty notification_types
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessnotifications_accounts_update_notification_setting_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessnotifications_accounts_update_notification_setting_task()`.
/// For the simplest API, use `mybusinessnotifications_accounts_update_notification_setting()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessnotifications_accounts_update_notification_setting_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessnotifications_accounts_update_notification_setting_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationSetting>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = mybusinessnotifications_accounts_update_notification_setting_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessnotifications_accounts_update_notification_setting`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessnotificationsAccountsUpdateNotificationSettingArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/accounts/{accountsId}/notificationSetting
/// Sets the pubsub notification setting for the account informing Google which topic to send pubsub notifications for. Use the notification_types field within notification_setting to manipulate the events an account wants to subscribe to. An account will only have one notification setting resource, and only one pubsub topic can be set. To delete the setting, update with an empty notification_types
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessnotifications_accounts_update_notification_setting_builder()` + `mybusinessnotifications_accounts_update_notification_setting_execute()`.
/// For task-level control, use `mybusinessnotifications_accounts_update_notification_setting_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessnotifications_accounts_update_notification_setting(
    client: &SimpleHttpClient,
    args: &MybusinessnotificationsAccountsUpdateNotificationSettingArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationSetting>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessnotifications_accounts_update_notification_setting_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    mybusinessnotifications_accounts_update_notification_setting_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for NotificationSetting
// =============================================================================

/// ResourceIdentifier implementation for NotificationSetting with MybusinessnotificationsAccountsGetNotificationSettingArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MybusinessnotificationsAccountsGetNotificationSettingArgs>
    for NotificationSetting
{
    fn generate_resource_id(
        &self,
        input: &MybusinessnotificationsAccountsGetNotificationSettingArgs,
    ) -> String {
        format!(
            "gcp::mybusinessnotifications::NotificationSetting/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::mybusinessnotifications::NotificationSetting"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationSetting
// =============================================================================

/// ResourceIdentifier implementation for NotificationSetting with MybusinessnotificationsAccountsUpdateNotificationSettingArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MybusinessnotificationsAccountsUpdateNotificationSettingArgs>
    for NotificationSetting
{
    fn generate_resource_id(
        &self,
        input: &MybusinessnotificationsAccountsUpdateNotificationSettingArgs,
    ) -> String {
        format!(
            "gcp::mybusinessnotifications::NotificationSetting/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::mybusinessnotifications::NotificationSetting"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
