//! Auto-generated API clients for gcp/secretmanager.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/secretmanager`

#![cfg(feature = "gcp/secretmanager")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_locations_get_execute()` to send, or `secretmanager_projects_locations_get` for simplest API.

pub fn secretmanager_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/locations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_locations_get_builder()` + `secretmanager_projects_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_locations_get_builder(client, name)?;
    secretmanager_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_locations_list_execute()` to send, or `secretmanager_projects_locations_list` for simplest API.

pub fn secretmanager_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_locations_list_builder()` + `secretmanager_projects_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_list(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_locations_list_builder(
        client,
        name,
        extraLocationTypes,
        filter,
        pageSize,
        pageToken,
    )?;
    secretmanager_projects_locations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}:addVersion
/// Creates a new SecretVersion containing secret data and attaches it to an existing Secret.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_locations_secrets_add_version_execute()` to send, or `secretmanager_projects_locations_secrets_add_version` for simplest API.

pub fn secretmanager_projects_locations_secrets_add_version_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AddSecretVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/locations/{}/secrets/{}:addVersion",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}:addVersion
/// Creates a new SecretVersion containing secret data and attaches it to an existing Secret.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_locations_secrets_add_version_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_locations_secrets_add_version_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_add_version_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecretVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}:addVersion
/// Creates a new SecretVersion containing secret data and attaches it to an existing Secret.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_locations_secrets_add_version_builder()` + `secretmanager_projects_locations_secrets_add_version_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_add_version(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AddSecretVersionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        secretmanager_projects_locations_secrets_add_version_builder(client, parent, body)?;
    secretmanager_projects_locations_secrets_add_version_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets
/// Creates a new Secret containing no SecretVersions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_locations_secrets_create_execute()` to send, or `secretmanager_projects_locations_secrets_create` for simplest API.

pub fn secretmanager_projects_locations_secrets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    secretId: Option<&str>,
    body: &Secret,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/locations/{}/secrets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = secretId {
        query_parts.push(format!("secretId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets
/// Creates a new Secret containing no SecretVersions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_locations_secrets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_locations_secrets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Secret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Secret = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets
/// Creates a new Secret containing no SecretVersions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_locations_secrets_create_builder()` + `secretmanager_projects_locations_secrets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_create(
    client: &SimpleHttpClient,
    parent: &str,
    secretId: Option<&str>,
    body: &Secret,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Secret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        secretmanager_projects_locations_secrets_create_builder(client, parent, secretId, body)?;
    secretmanager_projects_locations_secrets_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}
/// Deletes a Secret.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_locations_secrets_delete_execute()` to send, or `secretmanager_projects_locations_secrets_delete` for simplest API.

pub fn secretmanager_projects_locations_secrets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/locations/{}/secrets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}
/// Deletes a Secret.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_locations_secrets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_locations_secrets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}
/// Deletes a Secret.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_locations_secrets_delete_builder()` + `secretmanager_projects_locations_secrets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_locations_secrets_delete_builder(client, name, etag)?;
    secretmanager_projects_locations_secrets_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}
/// Gets metadata for a given Secret.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_locations_secrets_get_execute()` to send, or `secretmanager_projects_locations_secrets_get` for simplest API.

pub fn secretmanager_projects_locations_secrets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/locations/{}/secrets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}
/// Gets metadata for a given Secret.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_locations_secrets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_locations_secrets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Secret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Secret = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}
/// Gets metadata for a given Secret.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_locations_secrets_get_builder()` + `secretmanager_projects_locations_secrets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Secret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_locations_secrets_get_builder(client, name)?;
    secretmanager_projects_locations_secrets_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}:getIamPolicy
/// Gets the access control policy for a secret. Returns empty policy if the secret exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_locations_secrets_get_iam_policy_execute()` to send, or `secretmanager_projects_locations_secrets_get_iam_policy` for simplest API.

pub fn secretmanager_projects_locations_secrets_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/locations/{}/secrets/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}:getIamPolicy
/// Gets the access control policy for a secret. Returns empty policy if the secret exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_locations_secrets_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_locations_secrets_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}:getIamPolicy
/// Gets the access control policy for a secret. Returns empty policy if the secret exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_locations_secrets_get_iam_policy_builder()` + `secretmanager_projects_locations_secrets_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_locations_secrets_get_iam_policy_builder(
        client,
        resource,
        options_requestedPolicyVersion,
    )?;
    secretmanager_projects_locations_secrets_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets
/// Lists Secrets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_locations_secrets_list_execute()` to send, or `secretmanager_projects_locations_secrets_list` for simplest API.

pub fn secretmanager_projects_locations_secrets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/locations/{}/secrets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets
/// Lists Secrets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_locations_secrets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_locations_secrets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSecretsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSecretsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets
/// Lists Secrets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_locations_secrets_list_builder()` + `secretmanager_projects_locations_secrets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSecretsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_locations_secrets_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    secretmanager_projects_locations_secrets_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}
/// Updates metadata of an existing Secret.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_locations_secrets_patch_execute()` to send, or `secretmanager_projects_locations_secrets_patch` for simplest API.

pub fn secretmanager_projects_locations_secrets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Secret,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/locations/{}/secrets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}
/// Updates metadata of an existing Secret.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_locations_secrets_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_locations_secrets_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Secret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Secret = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}
/// Updates metadata of an existing Secret.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_locations_secrets_patch_builder()` + `secretmanager_projects_locations_secrets_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Secret,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Secret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        secretmanager_projects_locations_secrets_patch_builder(client, name, updateMask, body)?;
    secretmanager_projects_locations_secrets_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}:setIamPolicy
/// Sets the access control policy on the specified secret. Replaces any existing policy. Permissions on SecretVersions are enforced according to the policy set on the associated Secret.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_locations_secrets_set_iam_policy_execute()` to send, or `secretmanager_projects_locations_secrets_set_iam_policy` for simplest API.

pub fn secretmanager_projects_locations_secrets_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/locations/{}/secrets/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}:setIamPolicy
/// Sets the access control policy on the specified secret. Replaces any existing policy. Permissions on SecretVersions are enforced according to the policy set on the associated Secret.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_locations_secrets_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_locations_secrets_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}:setIamPolicy
/// Sets the access control policy on the specified secret. Replaces any existing policy. Permissions on SecretVersions are enforced according to the policy set on the associated Secret.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_locations_secrets_set_iam_policy_builder()` + `secretmanager_projects_locations_secrets_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        secretmanager_projects_locations_secrets_set_iam_policy_builder(client, resource, body)?;
    secretmanager_projects_locations_secrets_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}:testIamPermissions
/// Returns permissions that a caller has for the specified secret. If the secret does not exist, this call returns an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_locations_secrets_test_iam_permissions_execute()` to send, or `secretmanager_projects_locations_secrets_test_iam_permissions` for simplest API.

pub fn secretmanager_projects_locations_secrets_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/locations/{}/secrets/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}:testIamPermissions
/// Returns permissions that a caller has for the specified secret. If the secret does not exist, this call returns an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_locations_secrets_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_locations_secrets_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}:testIamPermissions
/// Returns permissions that a caller has for the specified secret. If the secret does not exist, this call returns an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_locations_secrets_test_iam_permissions_builder()` + `secretmanager_projects_locations_secrets_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_locations_secrets_test_iam_permissions_builder(
        client, resource, body,
    )?;
    secretmanager_projects_locations_secrets_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions/{versionsId}:access
/// Accesses a SecretVersion. This call returns the secret data. projects/*/secrets/*/`versions/latest` is an alias to the most recently created SecretVersion.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_locations_secrets_versions_access_execute()` to send, or `secretmanager_projects_locations_secrets_versions_access` for simplest API.

pub fn secretmanager_projects_locations_secrets_versions_access_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/locations/{}/secrets/{}/versions/{}:access",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions/{versionsId}:access
/// Accesses a SecretVersion. This call returns the secret data. projects/*/secrets/*/`versions/latest` is an alias to the most recently created SecretVersion.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_locations_secrets_versions_access_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_locations_secrets_versions_access_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_versions_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSecretVersionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSecretVersionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions/{versionsId}:access
/// Accesses a SecretVersion. This call returns the secret data. projects/*/secrets/*/`versions/latest` is an alias to the most recently created SecretVersion.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_locations_secrets_versions_access_builder()` + `secretmanager_projects_locations_secrets_versions_access_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_versions_access(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSecretVersionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_locations_secrets_versions_access_builder(client, name)?;
    secretmanager_projects_locations_secrets_versions_access_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions/{versionsId}:destroy
/// Destroys a SecretVersion. Sets the state of the SecretVersion to DESTROYED and irrevocably destroys the secret data.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_locations_secrets_versions_destroy_execute()` to send, or `secretmanager_projects_locations_secrets_versions_destroy` for simplest API.

pub fn secretmanager_projects_locations_secrets_versions_destroy_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &DestroySecretVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/locations/{}/secrets/{}/versions/{}:destroy",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions/{versionsId}:destroy
/// Destroys a SecretVersion. Sets the state of the SecretVersion to DESTROYED and irrevocably destroys the secret data.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_locations_secrets_versions_destroy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_locations_secrets_versions_destroy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_versions_destroy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecretVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions/{versionsId}:destroy
/// Destroys a SecretVersion. Sets the state of the SecretVersion to DESTROYED and irrevocably destroys the secret data.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_locations_secrets_versions_destroy_builder()` + `secretmanager_projects_locations_secrets_versions_destroy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_versions_destroy(
    client: &SimpleHttpClient,
    name: &str,
    body: &DestroySecretVersionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        secretmanager_projects_locations_secrets_versions_destroy_builder(client, name, body)?;
    secretmanager_projects_locations_secrets_versions_destroy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions/{versionsId}:disable
/// Disables a SecretVersion. Sets the state of the SecretVersion to DISABLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_locations_secrets_versions_disable_execute()` to send, or `secretmanager_projects_locations_secrets_versions_disable` for simplest API.

pub fn secretmanager_projects_locations_secrets_versions_disable_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &DisableSecretVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/locations/{}/secrets/{}/versions/{}:disable",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions/{versionsId}:disable
/// Disables a SecretVersion. Sets the state of the SecretVersion to DISABLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_locations_secrets_versions_disable_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_locations_secrets_versions_disable_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_versions_disable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecretVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions/{versionsId}:disable
/// Disables a SecretVersion. Sets the state of the SecretVersion to DISABLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_locations_secrets_versions_disable_builder()` + `secretmanager_projects_locations_secrets_versions_disable_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_versions_disable(
    client: &SimpleHttpClient,
    name: &str,
    body: &DisableSecretVersionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        secretmanager_projects_locations_secrets_versions_disable_builder(client, name, body)?;
    secretmanager_projects_locations_secrets_versions_disable_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions/{versionsId}:enable
/// Enables a SecretVersion. Sets the state of the SecretVersion to ENABLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_locations_secrets_versions_enable_execute()` to send, or `secretmanager_projects_locations_secrets_versions_enable` for simplest API.

pub fn secretmanager_projects_locations_secrets_versions_enable_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &EnableSecretVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/locations/{}/secrets/{}/versions/{}:enable",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions/{versionsId}:enable
/// Enables a SecretVersion. Sets the state of the SecretVersion to ENABLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_locations_secrets_versions_enable_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_locations_secrets_versions_enable_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_versions_enable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecretVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions/{versionsId}:enable
/// Enables a SecretVersion. Sets the state of the SecretVersion to ENABLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_locations_secrets_versions_enable_builder()` + `secretmanager_projects_locations_secrets_versions_enable_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_versions_enable(
    client: &SimpleHttpClient,
    name: &str,
    body: &EnableSecretVersionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        secretmanager_projects_locations_secrets_versions_enable_builder(client, name, body)?;
    secretmanager_projects_locations_secrets_versions_enable_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions/{versionsId}
/// Gets metadata for a SecretVersion. projects/*/secrets/*/`versions/latest` is an alias to the most recently created SecretVersion.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_locations_secrets_versions_get_execute()` to send, or `secretmanager_projects_locations_secrets_versions_get` for simplest API.

pub fn secretmanager_projects_locations_secrets_versions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/locations/{}/secrets/{}/versions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions/{versionsId}
/// Gets metadata for a SecretVersion. projects/*/secrets/*/`versions/latest` is an alias to the most recently created SecretVersion.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_locations_secrets_versions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_locations_secrets_versions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecretVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions/{versionsId}
/// Gets metadata for a SecretVersion. projects/*/secrets/*/`versions/latest` is an alias to the most recently created SecretVersion.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_locations_secrets_versions_get_builder()` + `secretmanager_projects_locations_secrets_versions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_versions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_locations_secrets_versions_get_builder(client, name)?;
    secretmanager_projects_locations_secrets_versions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions
/// Lists SecretVersions. This call does not return secret data.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_locations_secrets_versions_list_execute()` to send, or `secretmanager_projects_locations_secrets_versions_list` for simplest API.

pub fn secretmanager_projects_locations_secrets_versions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/locations/{}/secrets/{}/versions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions
/// Lists SecretVersions. This call does not return secret data.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_locations_secrets_versions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_locations_secrets_versions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecretVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSecretVersionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/secrets/{secretsId}/versions
/// Lists SecretVersions. This call does not return secret data.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_locations_secrets_versions_list_builder()` + `secretmanager_projects_locations_secrets_versions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_locations_secrets_versions_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecretVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_locations_secrets_versions_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    secretmanager_projects_locations_secrets_versions_list_execute(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}:addVersion
/// Creates a new SecretVersion containing secret data and attaches it to an existing Secret.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_secrets_add_version_execute()` to send, or `secretmanager_projects_secrets_add_version` for simplest API.

pub fn secretmanager_projects_secrets_add_version_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AddSecretVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/secrets/{}:addVersion",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}:addVersion
/// Creates a new SecretVersion containing secret data and attaches it to an existing Secret.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_secrets_add_version_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_secrets_add_version_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_add_version_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecretVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}:addVersion
/// Creates a new SecretVersion containing secret data and attaches it to an existing Secret.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_secrets_add_version_builder()` + `secretmanager_projects_secrets_add_version_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_add_version(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AddSecretVersionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_secrets_add_version_builder(client, parent, body)?;
    secretmanager_projects_secrets_add_version_execute(builder)
}

/// GET v1/projects/{projectsId}/secrets
/// Creates a new Secret containing no SecretVersions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_secrets_create_execute()` to send, or `secretmanager_projects_secrets_create` for simplest API.

pub fn secretmanager_projects_secrets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    secretId: Option<&str>,
    body: &Secret,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/secrets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = secretId {
        query_parts.push(format!("secretId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets
/// Creates a new Secret containing no SecretVersions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_secrets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_secrets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Secret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Secret = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets
/// Creates a new Secret containing no SecretVersions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_secrets_create_builder()` + `secretmanager_projects_secrets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_create(
    client: &SimpleHttpClient,
    parent: &str,
    secretId: Option<&str>,
    body: &Secret,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Secret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_secrets_create_builder(client, parent, secretId, body)?;
    secretmanager_projects_secrets_create_execute(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}
/// Deletes a Secret.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_secrets_delete_execute()` to send, or `secretmanager_projects_secrets_delete` for simplest API.

pub fn secretmanager_projects_secrets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/secrets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}
/// Deletes a Secret.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_secrets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_secrets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}
/// Deletes a Secret.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_secrets_delete_builder()` + `secretmanager_projects_secrets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_secrets_delete_builder(client, name, etag)?;
    secretmanager_projects_secrets_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}
/// Gets metadata for a given Secret.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_secrets_get_execute()` to send, or `secretmanager_projects_secrets_get` for simplest API.

pub fn secretmanager_projects_secrets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/secrets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}
/// Gets metadata for a given Secret.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_secrets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_secrets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Secret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Secret = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}
/// Gets metadata for a given Secret.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_secrets_get_builder()` + `secretmanager_projects_secrets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Secret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_secrets_get_builder(client, name)?;
    secretmanager_projects_secrets_get_execute(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}:getIamPolicy
/// Gets the access control policy for a secret. Returns empty policy if the secret exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_secrets_get_iam_policy_execute()` to send, or `secretmanager_projects_secrets_get_iam_policy` for simplest API.

pub fn secretmanager_projects_secrets_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/secrets/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}:getIamPolicy
/// Gets the access control policy for a secret. Returns empty policy if the secret exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_secrets_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_secrets_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}:getIamPolicy
/// Gets the access control policy for a secret. Returns empty policy if the secret exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_secrets_get_iam_policy_builder()` + `secretmanager_projects_secrets_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_secrets_get_iam_policy_builder(
        client,
        resource,
        options_requestedPolicyVersion,
    )?;
    secretmanager_projects_secrets_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/secrets
/// Lists Secrets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_secrets_list_execute()` to send, or `secretmanager_projects_secrets_list` for simplest API.

pub fn secretmanager_projects_secrets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/secrets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/secrets
/// Lists Secrets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_secrets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_secrets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSecretsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSecretsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets
/// Lists Secrets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_secrets_list_builder()` + `secretmanager_projects_secrets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSecretsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        secretmanager_projects_secrets_list_builder(client, parent, filter, pageSize, pageToken)?;
    secretmanager_projects_secrets_list_execute(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}
/// Updates metadata of an existing Secret.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_secrets_patch_execute()` to send, or `secretmanager_projects_secrets_patch` for simplest API.

pub fn secretmanager_projects_secrets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Secret,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/secrets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}
/// Updates metadata of an existing Secret.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_secrets_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_secrets_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Secret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Secret = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}
/// Updates metadata of an existing Secret.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_secrets_patch_builder()` + `secretmanager_projects_secrets_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Secret,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Secret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_secrets_patch_builder(client, name, updateMask, body)?;
    secretmanager_projects_secrets_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}:setIamPolicy
/// Sets the access control policy on the specified secret. Replaces any existing policy. Permissions on SecretVersions are enforced according to the policy set on the associated Secret.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_secrets_set_iam_policy_execute()` to send, or `secretmanager_projects_secrets_set_iam_policy` for simplest API.

pub fn secretmanager_projects_secrets_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/secrets/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}:setIamPolicy
/// Sets the access control policy on the specified secret. Replaces any existing policy. Permissions on SecretVersions are enforced according to the policy set on the associated Secret.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_secrets_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_secrets_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}:setIamPolicy
/// Sets the access control policy on the specified secret. Replaces any existing policy. Permissions on SecretVersions are enforced according to the policy set on the associated Secret.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_secrets_set_iam_policy_builder()` + `secretmanager_projects_secrets_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_secrets_set_iam_policy_builder(client, resource, body)?;
    secretmanager_projects_secrets_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}:testIamPermissions
/// Returns permissions that a caller has for the specified secret. If the secret does not exist, this call returns an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_secrets_test_iam_permissions_execute()` to send, or `secretmanager_projects_secrets_test_iam_permissions` for simplest API.

pub fn secretmanager_projects_secrets_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/secrets/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}:testIamPermissions
/// Returns permissions that a caller has for the specified secret. If the secret does not exist, this call returns an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_secrets_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_secrets_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}:testIamPermissions
/// Returns permissions that a caller has for the specified secret. If the secret does not exist, this call returns an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_secrets_test_iam_permissions_builder()` + `secretmanager_projects_secrets_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        secretmanager_projects_secrets_test_iam_permissions_builder(client, resource, body)?;
    secretmanager_projects_secrets_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions/{versionsId}:access
/// Accesses a SecretVersion. This call returns the secret data. projects/*/secrets/*/`versions/latest` is an alias to the most recently created SecretVersion.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_secrets_versions_access_execute()` to send, or `secretmanager_projects_secrets_versions_access` for simplest API.

pub fn secretmanager_projects_secrets_versions_access_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/secrets/{}/versions/{}:access",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions/{versionsId}:access
/// Accesses a SecretVersion. This call returns the secret data. projects/*/secrets/*/`versions/latest` is an alias to the most recently created SecretVersion.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_secrets_versions_access_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_secrets_versions_access_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_versions_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSecretVersionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSecretVersionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions/{versionsId}:access
/// Accesses a SecretVersion. This call returns the secret data. projects/*/secrets/*/`versions/latest` is an alias to the most recently created SecretVersion.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_secrets_versions_access_builder()` + `secretmanager_projects_secrets_versions_access_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_versions_access(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSecretVersionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_secrets_versions_access_builder(client, name)?;
    secretmanager_projects_secrets_versions_access_execute(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions/{versionsId}:destroy
/// Destroys a SecretVersion. Sets the state of the SecretVersion to DESTROYED and irrevocably destroys the secret data.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_secrets_versions_destroy_execute()` to send, or `secretmanager_projects_secrets_versions_destroy` for simplest API.

pub fn secretmanager_projects_secrets_versions_destroy_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &DestroySecretVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/secrets/{}/versions/{}:destroy",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions/{versionsId}:destroy
/// Destroys a SecretVersion. Sets the state of the SecretVersion to DESTROYED and irrevocably destroys the secret data.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_secrets_versions_destroy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_secrets_versions_destroy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_versions_destroy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecretVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions/{versionsId}:destroy
/// Destroys a SecretVersion. Sets the state of the SecretVersion to DESTROYED and irrevocably destroys the secret data.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_secrets_versions_destroy_builder()` + `secretmanager_projects_secrets_versions_destroy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_versions_destroy(
    client: &SimpleHttpClient,
    name: &str,
    body: &DestroySecretVersionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_secrets_versions_destroy_builder(client, name, body)?;
    secretmanager_projects_secrets_versions_destroy_execute(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions/{versionsId}:disable
/// Disables a SecretVersion. Sets the state of the SecretVersion to DISABLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_secrets_versions_disable_execute()` to send, or `secretmanager_projects_secrets_versions_disable` for simplest API.

pub fn secretmanager_projects_secrets_versions_disable_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &DisableSecretVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/secrets/{}/versions/{}:disable",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions/{versionsId}:disable
/// Disables a SecretVersion. Sets the state of the SecretVersion to DISABLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_secrets_versions_disable_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_secrets_versions_disable_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_versions_disable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecretVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions/{versionsId}:disable
/// Disables a SecretVersion. Sets the state of the SecretVersion to DISABLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_secrets_versions_disable_builder()` + `secretmanager_projects_secrets_versions_disable_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_versions_disable(
    client: &SimpleHttpClient,
    name: &str,
    body: &DisableSecretVersionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_secrets_versions_disable_builder(client, name, body)?;
    secretmanager_projects_secrets_versions_disable_execute(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions/{versionsId}:enable
/// Enables a SecretVersion. Sets the state of the SecretVersion to ENABLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_secrets_versions_enable_execute()` to send, or `secretmanager_projects_secrets_versions_enable` for simplest API.

pub fn secretmanager_projects_secrets_versions_enable_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &EnableSecretVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/secrets/{}/versions/{}:enable",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions/{versionsId}:enable
/// Enables a SecretVersion. Sets the state of the SecretVersion to ENABLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_secrets_versions_enable_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_secrets_versions_enable_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_versions_enable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecretVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions/{versionsId}:enable
/// Enables a SecretVersion. Sets the state of the SecretVersion to ENABLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_secrets_versions_enable_builder()` + `secretmanager_projects_secrets_versions_enable_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_versions_enable(
    client: &SimpleHttpClient,
    name: &str,
    body: &EnableSecretVersionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_secrets_versions_enable_builder(client, name, body)?;
    secretmanager_projects_secrets_versions_enable_execute(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions/{versionsId}
/// Gets metadata for a SecretVersion. projects/*/secrets/*/`versions/latest` is an alias to the most recently created SecretVersion.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_secrets_versions_get_execute()` to send, or `secretmanager_projects_secrets_versions_get` for simplest API.

pub fn secretmanager_projects_secrets_versions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/secrets/{}/versions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions/{versionsId}
/// Gets metadata for a SecretVersion. projects/*/secrets/*/`versions/latest` is an alias to the most recently created SecretVersion.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_secrets_versions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_secrets_versions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecretVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions/{versionsId}
/// Gets metadata for a SecretVersion. projects/*/secrets/*/`versions/latest` is an alias to the most recently created SecretVersion.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_secrets_versions_get_builder()` + `secretmanager_projects_secrets_versions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_versions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_secrets_versions_get_builder(client, name)?;
    secretmanager_projects_secrets_versions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions
/// Lists SecretVersions. This call does not return secret data.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `secretmanager_projects_secrets_versions_list_execute()` to send, or `secretmanager_projects_secrets_versions_list` for simplest API.

pub fn secretmanager_projects_secrets_versions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://secretmanager.googleapis.com/v1/projects/{}/secrets/{}/versions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions
/// Lists SecretVersions. This call does not return secret data.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `secretmanager_projects_secrets_versions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from secretmanager_projects_secrets_versions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecretVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSecretVersionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/secrets/{secretsId}/versions
/// Lists SecretVersions. This call does not return secret data.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `secretmanager_projects_secrets_versions_list_builder()` + `secretmanager_projects_secrets_versions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn secretmanager_projects_secrets_versions_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecretVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = secretmanager_projects_secrets_versions_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    secretmanager_projects_secrets_versions_list_execute(builder)
}
