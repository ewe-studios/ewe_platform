//! Auto-generated API clients for gcp/texttospeech.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `texttospeech_operations_cancel_execute()` to send, or `texttospeech_operations_cancel` for simplest API.

pub fn texttospeech_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &String,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://texttospeech.googleapis.com/v1/operations/{}:cancel",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `texttospeech_operations_cancel_execute()` or `texttospeech_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `texttospeech_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn texttospeech_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `texttospeech_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `texttospeech_operations_cancel_task()`.
/// For the simplest API, use `texttospeech_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `texttospeech_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn texttospeech_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = texttospeech_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`texttospeech_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TexttospeechOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: CancelOperationRequest,
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `texttospeech_operations_cancel_builder()` + `texttospeech_operations_cancel_execute()`.
/// For task-level control, use `texttospeech_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn texttospeech_operations_cancel(
    client: &SimpleHttpClient,
    args: &TexttospeechOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = texttospeech_operations_cancel_builder(client, &args.name, &args.body)?;
    texttospeech_operations_cancel_execute(builder)
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `texttospeech_operations_delete_execute()` to send, or `texttospeech_operations_delete` for simplest API.

pub fn texttospeech_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://texttospeech.googleapis.com/v1/operations/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `texttospeech_operations_delete_execute()` or `texttospeech_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `texttospeech_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn texttospeech_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `texttospeech_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `texttospeech_operations_delete_task()`.
/// For the simplest API, use `texttospeech_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `texttospeech_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn texttospeech_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = texttospeech_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`texttospeech_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TexttospeechOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `texttospeech_operations_delete_builder()` + `texttospeech_operations_delete_execute()`.
/// For task-level control, use `texttospeech_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn texttospeech_operations_delete(
    client: &SimpleHttpClient,
    args: &TexttospeechOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = texttospeech_operations_delete_builder(client, &args.name)?;
    texttospeech_operations_delete_execute(builder)
}

/// GET v1/text:synthesize
/// Synthesizes speech synchronously: receive results after all text input has been processed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `texttospeech_text_synthesize_execute()` to send, or `texttospeech_text_synthesize` for simplest API.

pub fn texttospeech_text_synthesize_builder(
    client: &SimpleHttpClient,
    body: &SynthesizeSpeechRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://texttospeech.googleapis.com/v1/text:synthesize",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/text:synthesize
/// Synthesizes speech synchronously: receive results after all text input has been processed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `texttospeech_text_synthesize_execute()` or `texttospeech_text_synthesize`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `texttospeech_text_synthesize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn texttospeech_text_synthesize_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SynthesizeSpeechResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SynthesizeSpeechResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/text:synthesize
/// Synthesizes speech synchronously: receive results after all text input has been processed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `texttospeech_text_synthesize_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `texttospeech_text_synthesize_task()`.
/// For the simplest API, use `texttospeech_text_synthesize()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `texttospeech_text_synthesize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn texttospeech_text_synthesize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SynthesizeSpeechResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = texttospeech_text_synthesize_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`texttospeech_text_synthesize`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TexttospeechTextSynthesizeArgs {
    /// Request body.
    pub body: SynthesizeSpeechRequest,
}

/// GET v1/text:synthesize
/// Synthesizes speech synchronously: receive results after all text input has been processed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `texttospeech_text_synthesize_builder()` + `texttospeech_text_synthesize_execute()`.
/// For task-level control, use `texttospeech_text_synthesize_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn texttospeech_text_synthesize(
    client: &SimpleHttpClient,
    args: &TexttospeechTextSynthesizeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SynthesizeSpeechResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = texttospeech_text_synthesize_builder(client, &args.body)?;
    texttospeech_text_synthesize_execute(builder)
}

/// GET v1/voices
/// Returns a list of Voice supported for synthesis.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `texttospeech_voices_list_execute()` to send, or `texttospeech_voices_list` for simplest API.

pub fn texttospeech_voices_list_builder(
    client: &SimpleHttpClient,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://texttospeech.googleapis.com/v1/voices",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/voices
/// Returns a list of Voice supported for synthesis.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `texttospeech_voices_list_execute()` or `texttospeech_voices_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `texttospeech_voices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn texttospeech_voices_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListVoicesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVoicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/voices
/// Returns a list of Voice supported for synthesis.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `texttospeech_voices_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `texttospeech_voices_list_task()`.
/// For the simplest API, use `texttospeech_voices_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `texttospeech_voices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn texttospeech_voices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVoicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = texttospeech_voices_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`texttospeech_voices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TexttospeechVoicesListArgs {
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// GET v1/voices
/// Returns a list of Voice supported for synthesis.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `texttospeech_voices_list_builder()` + `texttospeech_voices_list_execute()`.
/// For task-level control, use `texttospeech_voices_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn texttospeech_voices_list(
    client: &SimpleHttpClient,
    args: &TexttospeechVoicesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVoicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = texttospeech_voices_list_builder(client, &args.languageCode)?;
    texttospeech_voices_list_execute(builder)
}
