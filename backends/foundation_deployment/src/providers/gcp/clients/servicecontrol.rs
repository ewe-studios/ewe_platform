//! Auto-generated API clients for gcp/servicecontrol.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v2/services/{serviceName}:check
/// This method provides admission control for services that are integrated with [Service Infrastructure](<https://cloud.google.`com/service-infrastructure`>). It checks whether an operation should be allowed based on the service configuration and relevant policies. It must be called before the operation is executed. For more information, see [Admission Control](<https://cloud.google.`com/service-infrastructure/docs/admission-control`>). NOTE: The admission control has an expected policy propagation delay of 60s. The caller **must** not depend on the most recent policy changes. NOTE: The admission control has a hard limit of 1 referenced resources per call. If an operation refers to more than 1 resources, the caller must call the Check method multiple times. This method requires the servicemanagement.services.check permission on the specified service. For more information, see [Service Control API Access Control](<https://cloud.google.`com/service-infrastructure/docs/service-control/access-control`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicecontrol_services_check_execute()` to send, or `servicecontrol_services_check` for simplest API.

pub fn servicecontrol_services_check_builder<R>(
    client: &SimpleHttpClient<R>,
    serviceName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://servicecontrol.googleapis.com/v2/services/{}:check",
        serviceName,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/services/{serviceName}:check
/// This method provides admission control for services that are integrated with [Service Infrastructure](<https://cloud.google.`com/service-infrastructure`>). It checks whether an operation should be allowed based on the service configuration and relevant policies. It must be called before the operation is executed. For more information, see [Admission Control](<https://cloud.google.`com/service-infrastructure/docs/admission-control`>). NOTE: The admission control has an expected policy propagation delay of 60s. The caller **must** not depend on the most recent policy changes. NOTE: The admission control has a hard limit of 1 referenced resources per call. If an operation refers to more than 1 resources, the caller must call the Check method multiple times. This method requires the servicemanagement.services.check permission on the specified service. For more information, see [Service Control API Access Control](<https://cloud.google.`com/service-infrastructure/docs/service-control/access-control`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `servicecontrol_services_check_execute()` or `servicecontrol_services_check`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `servicecontrol_services_check_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicecontrol_services_check_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CheckResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/services/{serviceName}:check
/// This method provides admission control for services that are integrated with [Service Infrastructure](<https://cloud.google.`com/service-infrastructure`>). It checks whether an operation should be allowed based on the service configuration and relevant policies. It must be called before the operation is executed. For more information, see [Admission Control](<https://cloud.google.`com/service-infrastructure/docs/admission-control`>). NOTE: The admission control has an expected policy propagation delay of 60s. The caller **must** not depend on the most recent policy changes. NOTE: The admission control has a hard limit of 1 referenced resources per call. If an operation refers to more than 1 resources, the caller must call the Check method multiple times. This method requires the servicemanagement.services.check permission on the specified service. For more information, see [Service Control API Access Control](<https://cloud.google.`com/service-infrastructure/docs/service-control/access-control`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `servicecontrol_services_check_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `servicecontrol_services_check_task()`.
/// For the simplest API, use `servicecontrol_services_check()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `servicecontrol_services_check_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn servicecontrol_services_check_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = servicecontrol_services_check_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`servicecontrol_services_check`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ServicecontrolServicesCheckArgs {
    /// Path parameter: serviceName
    pub serviceName: String,
}

/// POST v2/services/{serviceName}:check
/// This method provides admission control for services that are integrated with [Service Infrastructure](<https://cloud.google.`com/service-infrastructure`>). It checks whether an operation should be allowed based on the service configuration and relevant policies. It must be called before the operation is executed. For more information, see [Admission Control](<https://cloud.google.`com/service-infrastructure/docs/admission-control`>). NOTE: The admission control has an expected policy propagation delay of 60s. The caller **must** not depend on the most recent policy changes. NOTE: The admission control has a hard limit of 1 referenced resources per call. If an operation refers to more than 1 resources, the caller must call the Check method multiple times. This method requires the servicemanagement.services.check permission on the specified service. For more information, see [Service Control API Access Control](<https://cloud.google.`com/service-infrastructure/docs/service-control/access-control`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicecontrol_services_check_builder()` + `servicecontrol_services_check_execute()`.
/// For task-level control, use `servicecontrol_services_check_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicecontrol_services_check(
    client: &SimpleHttpClient,
    args: &ServicecontrolServicesCheckArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = servicecontrol_services_check_builder(client, &args.serviceName)?;
    servicecontrol_services_check_execute(builder)
}

/// POST v2/services/{serviceName}:report
/// This method provides telemetry reporting for services that are integrated with [Service Infrastructure](<https://cloud.google.`com/service-infrastructure`>). It reports a list of operations that have occurred on a service. It must be called after the operations have been executed. For more information, see [Telemetry Reporting](<https://cloud.google.`com/service-infrastructure/docs/telemetry-reporting`>). NOTE: The telemetry reporting has a hard limit of 100 operations and 1MB per Report call. This method requires the servicemanagement.services.report permission on the specified service. For more information, see [Service Control API Access Control](<https://cloud.google.`com/service-infrastructure/docs/service-control/access-control`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `servicecontrol_services_report_execute()` to send, or `servicecontrol_services_report` for simplest API.

pub fn servicecontrol_services_report_builder<R>(
    client: &SimpleHttpClient<R>,
    serviceName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://servicecontrol.googleapis.com/v2/services/{}:report",
        serviceName,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/services/{serviceName}:report
/// This method provides telemetry reporting for services that are integrated with [Service Infrastructure](<https://cloud.google.`com/service-infrastructure`>). It reports a list of operations that have occurred on a service. It must be called after the operations have been executed. For more information, see [Telemetry Reporting](<https://cloud.google.`com/service-infrastructure/docs/telemetry-reporting`>). NOTE: The telemetry reporting has a hard limit of 100 operations and 1MB per Report call. This method requires the servicemanagement.services.report permission on the specified service. For more information, see [Service Control API Access Control](<https://cloud.google.`com/service-infrastructure/docs/service-control/access-control`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `servicecontrol_services_report_execute()` or `servicecontrol_services_report`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `servicecontrol_services_report_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicecontrol_services_report_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReportResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReportResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/services/{serviceName}:report
/// This method provides telemetry reporting for services that are integrated with [Service Infrastructure](<https://cloud.google.`com/service-infrastructure`>). It reports a list of operations that have occurred on a service. It must be called after the operations have been executed. For more information, see [Telemetry Reporting](<https://cloud.google.`com/service-infrastructure/docs/telemetry-reporting`>). NOTE: The telemetry reporting has a hard limit of 100 operations and 1MB per Report call. This method requires the servicemanagement.services.report permission on the specified service. For more information, see [Service Control API Access Control](<https://cloud.google.`com/service-infrastructure/docs/service-control/access-control`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `servicecontrol_services_report_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `servicecontrol_services_report_task()`.
/// For the simplest API, use `servicecontrol_services_report()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `servicecontrol_services_report_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn servicecontrol_services_report_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = servicecontrol_services_report_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`servicecontrol_services_report`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ServicecontrolServicesReportArgs {
    /// Path parameter: serviceName
    pub serviceName: String,
}

/// POST v2/services/{serviceName}:report
/// This method provides telemetry reporting for services that are integrated with [Service Infrastructure](<https://cloud.google.`com/service-infrastructure`>). It reports a list of operations that have occurred on a service. It must be called after the operations have been executed. For more information, see [Telemetry Reporting](<https://cloud.google.`com/service-infrastructure/docs/telemetry-reporting`>). NOTE: The telemetry reporting has a hard limit of 100 operations and 1MB per Report call. This method requires the servicemanagement.services.report permission on the specified service. For more information, see [Service Control API Access Control](<https://cloud.google.`com/service-infrastructure/docs/service-control/access-control`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `servicecontrol_services_report_builder()` + `servicecontrol_services_report_execute()`.
/// For task-level control, use `servicecontrol_services_report_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn servicecontrol_services_report(
    client: &SimpleHttpClient,
    args: &ServicecontrolServicesReportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = servicecontrol_services_report_builder(client, &args.serviceName)?;
    servicecontrol_services_report_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for CheckResponse
// =============================================================================

/// ResourceIdentifier implementation for CheckResponse with ServicecontrolServicesCheckArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ServicecontrolServicesCheckArgs> for CheckResponse {
    fn generate_resource_id(&self, input: &ServicecontrolServicesCheckArgs) -> String {
        format!("gcp::servicecontrol::CheckResponse/{}", input.serviceName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::servicecontrol::CheckResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReportResponse
// =============================================================================

/// ResourceIdentifier implementation for ReportResponse with ServicecontrolServicesReportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ServicecontrolServicesReportArgs> for ReportResponse {
    fn generate_resource_id(&self, input: &ServicecontrolServicesReportArgs) -> String {
        format!("gcp::servicecontrol::ReportResponse/{}", input.serviceName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::servicecontrol::ReportResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
