//! Auto-generated API clients for gcp/chromewebstore.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v2/publishers/{publishersId}/items/{itemsId}:upload
/// Upload a new package to an existing item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromewebstore_media_upload_execute()` to send, or `chromewebstore_media_upload` for simplest API.

pub fn chromewebstore_media_upload_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromewebstore.googleapis.com/v2/publishers/{}/items/{itemsId}:upload",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/publishers/{publishersId}/items/{itemsId}:upload
/// Upload a new package to an existing item.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromewebstore_media_upload_execute()` or `chromewebstore_media_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromewebstore_media_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromewebstore_media_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UploadItemPackageResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UploadItemPackageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/publishers/{publishersId}/items/{itemsId}:upload
/// Upload a new package to an existing item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromewebstore_media_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromewebstore_media_upload_task()`.
/// For the simplest API, use `chromewebstore_media_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromewebstore_media_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromewebstore_media_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UploadItemPackageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = chromewebstore_media_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromewebstore_media_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromewebstoreMediaUploadArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/publishers/{publishersId}/items/{itemsId}:upload
/// Upload a new package to an existing item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromewebstore_media_upload_builder()` + `chromewebstore_media_upload_execute()`.
/// For task-level control, use `chromewebstore_media_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromewebstore_media_upload(
    client: &SimpleHttpClient,
    args: &ChromewebstoreMediaUploadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UploadItemPackageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = chromewebstore_media_upload_builder(client, &args.name)?;
    chromewebstore_media_upload_execute(builder)
}

/// POST v2/publishers/{publishersId}/items/{itemsId}:cancelSubmission
/// Cancel the current active submission of an item if present. This can be used to cancel the review of a pending submission.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromewebstore_publishers_items_cancel_submission_execute()` to send, or `chromewebstore_publishers_items_cancel_submission` for simplest API.

pub fn chromewebstore_publishers_items_cancel_submission_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromewebstore.googleapis.com/v2/publishers/{}/items/{itemsId}:cancelSubmission",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/publishers/{publishersId}/items/{itemsId}:cancelSubmission
/// Cancel the current active submission of an item if present. This can be used to cancel the review of a pending submission.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromewebstore_publishers_items_cancel_submission_execute()` or `chromewebstore_publishers_items_cancel_submission`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromewebstore_publishers_items_cancel_submission_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromewebstore_publishers_items_cancel_submission_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CancelSubmissionResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CancelSubmissionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/publishers/{publishersId}/items/{itemsId}:cancelSubmission
/// Cancel the current active submission of an item if present. This can be used to cancel the review of a pending submission.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromewebstore_publishers_items_cancel_submission_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromewebstore_publishers_items_cancel_submission_task()`.
/// For the simplest API, use `chromewebstore_publishers_items_cancel_submission()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromewebstore_publishers_items_cancel_submission_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromewebstore_publishers_items_cancel_submission_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CancelSubmissionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = chromewebstore_publishers_items_cancel_submission_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromewebstore_publishers_items_cancel_submission`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromewebstorePublishersItemsCancelSubmissionArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/publishers/{publishersId}/items/{itemsId}:cancelSubmission
/// Cancel the current active submission of an item if present. This can be used to cancel the review of a pending submission.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromewebstore_publishers_items_cancel_submission_builder()` + `chromewebstore_publishers_items_cancel_submission_execute()`.
/// For task-level control, use `chromewebstore_publishers_items_cancel_submission_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromewebstore_publishers_items_cancel_submission(
    client: &SimpleHttpClient,
    args: &ChromewebstorePublishersItemsCancelSubmissionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CancelSubmissionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = chromewebstore_publishers_items_cancel_submission_builder(client, &args.name)?;
    chromewebstore_publishers_items_cancel_submission_execute(builder)
}

/// GET v2/publishers/{publishersId}/items/{itemsId}:fetchStatus
/// Fetch the status of an item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromewebstore_publishers_items_fetch_status_execute()` to send, or `chromewebstore_publishers_items_fetch_status` for simplest API.

pub fn chromewebstore_publishers_items_fetch_status_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromewebstore.googleapis.com/v2/publishers/{}/items/{itemsId}:fetchStatus",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/publishers/{publishersId}/items/{itemsId}:fetchStatus
/// Fetch the status of an item.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromewebstore_publishers_items_fetch_status_execute()` or `chromewebstore_publishers_items_fetch_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromewebstore_publishers_items_fetch_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromewebstore_publishers_items_fetch_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FetchItemStatusResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchItemStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/publishers/{publishersId}/items/{itemsId}:fetchStatus
/// Fetch the status of an item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromewebstore_publishers_items_fetch_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromewebstore_publishers_items_fetch_status_task()`.
/// For the simplest API, use `chromewebstore_publishers_items_fetch_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromewebstore_publishers_items_fetch_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromewebstore_publishers_items_fetch_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchItemStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = chromewebstore_publishers_items_fetch_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromewebstore_publishers_items_fetch_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromewebstorePublishersItemsFetchStatusArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/publishers/{publishersId}/items/{itemsId}:fetchStatus
/// Fetch the status of an item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromewebstore_publishers_items_fetch_status_builder()` + `chromewebstore_publishers_items_fetch_status_execute()`.
/// For task-level control, use `chromewebstore_publishers_items_fetch_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromewebstore_publishers_items_fetch_status(
    client: &SimpleHttpClient,
    args: &ChromewebstorePublishersItemsFetchStatusArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchItemStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = chromewebstore_publishers_items_fetch_status_builder(client, &args.name)?;
    chromewebstore_publishers_items_fetch_status_execute(builder)
}

/// POST v2/publishers/{publishersId}/items/{itemsId}:publish
/// Submit the item to be published in the store. The item will be submitted for review unless skip_review is set to `true`, or the item is staged from a previous submission with publish_type set to STAGED_PUBLISH.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromewebstore_publishers_items_publish_execute()` to send, or `chromewebstore_publishers_items_publish` for simplest API.

pub fn chromewebstore_publishers_items_publish_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromewebstore.googleapis.com/v2/publishers/{}/items/{itemsId}:publish",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/publishers/{publishersId}/items/{itemsId}:publish
/// Submit the item to be published in the store. The item will be submitted for review unless skip_review is set to `true`, or the item is staged from a previous submission with publish_type set to STAGED_PUBLISH.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromewebstore_publishers_items_publish_execute()` or `chromewebstore_publishers_items_publish`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromewebstore_publishers_items_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromewebstore_publishers_items_publish_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublishItemResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublishItemResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/publishers/{publishersId}/items/{itemsId}:publish
/// Submit the item to be published in the store. The item will be submitted for review unless skip_review is set to `true`, or the item is staged from a previous submission with publish_type set to STAGED_PUBLISH.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromewebstore_publishers_items_publish_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromewebstore_publishers_items_publish_task()`.
/// For the simplest API, use `chromewebstore_publishers_items_publish()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromewebstore_publishers_items_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromewebstore_publishers_items_publish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublishItemResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = chromewebstore_publishers_items_publish_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromewebstore_publishers_items_publish`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromewebstorePublishersItemsPublishArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/publishers/{publishersId}/items/{itemsId}:publish
/// Submit the item to be published in the store. The item will be submitted for review unless skip_review is set to `true`, or the item is staged from a previous submission with publish_type set to STAGED_PUBLISH.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromewebstore_publishers_items_publish_builder()` + `chromewebstore_publishers_items_publish_execute()`.
/// For task-level control, use `chromewebstore_publishers_items_publish_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromewebstore_publishers_items_publish(
    client: &SimpleHttpClient,
    args: &ChromewebstorePublishersItemsPublishArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublishItemResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = chromewebstore_publishers_items_publish_builder(client, &args.name)?;
    chromewebstore_publishers_items_publish_execute(builder)
}

/// POST v2/publishers/{publishersId}/items/{itemsId}:setPublishedDeployPercentage
/// Set a higher target deploy percentage for the item's published revision. This will be updated without the item being submitted for review. This is only available to items with over 10,000 seven-day active users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromewebstore_publishers_items_set_published_deploy_percentage_execute()` to send, or `chromewebstore_publishers_items_set_published_deploy_percentage` for simplest API.

pub fn chromewebstore_publishers_items_set_published_deploy_percentage_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromewebstore.googleapis.com/v2/publishers/{}/items/{itemsId}:setPublishedDeployPercentage",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/publishers/{publishersId}/items/{itemsId}:setPublishedDeployPercentage
/// Set a higher target deploy percentage for the item's published revision. This will be updated without the item being submitted for review. This is only available to items with over 10,000 seven-day active users.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromewebstore_publishers_items_set_published_deploy_percentage_execute()` or `chromewebstore_publishers_items_set_published_deploy_percentage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromewebstore_publishers_items_set_published_deploy_percentage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromewebstore_publishers_items_set_published_deploy_percentage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SetPublishedDeployPercentageResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SetPublishedDeployPercentageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/publishers/{publishersId}/items/{itemsId}:setPublishedDeployPercentage
/// Set a higher target deploy percentage for the item's published revision. This will be updated without the item being submitted for review. This is only available to items with over 10,000 seven-day active users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromewebstore_publishers_items_set_published_deploy_percentage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromewebstore_publishers_items_set_published_deploy_percentage_task()`.
/// For the simplest API, use `chromewebstore_publishers_items_set_published_deploy_percentage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromewebstore_publishers_items_set_published_deploy_percentage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromewebstore_publishers_items_set_published_deploy_percentage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SetPublishedDeployPercentageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromewebstore_publishers_items_set_published_deploy_percentage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromewebstore_publishers_items_set_published_deploy_percentage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromewebstorePublishersItemsSetPublishedDeployPercentageArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/publishers/{publishersId}/items/{itemsId}:setPublishedDeployPercentage
/// Set a higher target deploy percentage for the item's published revision. This will be updated without the item being submitted for review. This is only available to items with over 10,000 seven-day active users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromewebstore_publishers_items_set_published_deploy_percentage_builder()` + `chromewebstore_publishers_items_set_published_deploy_percentage_execute()`.
/// For task-level control, use `chromewebstore_publishers_items_set_published_deploy_percentage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromewebstore_publishers_items_set_published_deploy_percentage(
    client: &SimpleHttpClient,
    args: &ChromewebstorePublishersItemsSetPublishedDeployPercentageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SetPublishedDeployPercentageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromewebstore_publishers_items_set_published_deploy_percentage_builder(
        client, &args.name,
    )?;
    chromewebstore_publishers_items_set_published_deploy_percentage_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for UploadItemPackageResponse
// =============================================================================

/// ResourceIdentifier implementation for UploadItemPackageResponse with ChromewebstoreMediaUploadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromewebstoreMediaUploadArgs> for UploadItemPackageResponse {
    fn generate_resource_id(&self, input: &ChromewebstoreMediaUploadArgs) -> String {
        format!(
            "gcp::chromewebstore::UploadItemPackageResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromewebstore::UploadItemPackageResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CancelSubmissionResponse
// =============================================================================

/// ResourceIdentifier implementation for CancelSubmissionResponse with ChromewebstorePublishersItemsCancelSubmissionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromewebstorePublishersItemsCancelSubmissionArgs>
    for CancelSubmissionResponse
{
    fn generate_resource_id(
        &self,
        input: &ChromewebstorePublishersItemsCancelSubmissionArgs,
    ) -> String {
        format!(
            "gcp::chromewebstore::CancelSubmissionResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromewebstore::CancelSubmissionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FetchItemStatusResponse
// =============================================================================

/// ResourceIdentifier implementation for FetchItemStatusResponse with ChromewebstorePublishersItemsFetchStatusArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromewebstorePublishersItemsFetchStatusArgs> for FetchItemStatusResponse {
    fn generate_resource_id(&self, input: &ChromewebstorePublishersItemsFetchStatusArgs) -> String {
        format!(
            "gcp::chromewebstore::FetchItemStatusResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromewebstore::FetchItemStatusResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PublishItemResponse
// =============================================================================

/// ResourceIdentifier implementation for PublishItemResponse with ChromewebstorePublishersItemsPublishArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromewebstorePublishersItemsPublishArgs> for PublishItemResponse {
    fn generate_resource_id(&self, input: &ChromewebstorePublishersItemsPublishArgs) -> String {
        format!("gcp::chromewebstore::PublishItemResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromewebstore::PublishItemResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SetPublishedDeployPercentageResponse
// =============================================================================

/// ResourceIdentifier implementation for SetPublishedDeployPercentageResponse with ChromewebstorePublishersItemsSetPublishedDeployPercentageArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromewebstorePublishersItemsSetPublishedDeployPercentageArgs>
    for SetPublishedDeployPercentageResponse
{
    fn generate_resource_id(
        &self,
        input: &ChromewebstorePublishersItemsSetPublishedDeployPercentageArgs,
    ) -> String {
        format!(
            "gcp::chromewebstore::SetPublishedDeployPercentageResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromewebstore::SetPublishedDeployPercentageResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
