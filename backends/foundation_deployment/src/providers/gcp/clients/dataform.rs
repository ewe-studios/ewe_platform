//! Auto-generated API clients for gcp/dataform.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_get_execute()` to send, or `dataform_projects_locations_get` for simplest API.

pub fn dataform_projects_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_get_execute()` or `dataform_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_get_task()`.
/// For the simplest API, use `dataform_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_get_builder()` + `dataform_projects_locations_get_execute()`.
/// For task-level control, use `dataform_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_get(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_get_builder(client, &args.name)?;
    dataform_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Get default config for a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_get_config_execute()` to send, or `dataform_projects_locations_get_config` for simplest API.

pub fn dataform_projects_locations_get_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Get default config for a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_get_config_execute()` or `dataform_projects_locations_get_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_get_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Config>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Config = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Get default config for a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_get_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_get_config_task()`.
/// For the simplest API, use `dataform_projects_locations_get_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_get_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Config>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_get_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_get_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsGetConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Get default config for a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_get_config_builder()` + `dataform_projects_locations_get_config_execute()`.
/// For task-level control, use `dataform_projects_locations_get_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_get_config(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsGetConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Config>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_get_config_builder(client, &args.name)?;
    dataform_projects_locations_get_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_list_execute()` to send, or `dataform_projects_locations_list` for simplest API.

pub fn dataform_projects_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_list_execute()` or `dataform_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_list_task()`.
/// For the simplest API, use `dataform_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_list_builder()` + `dataform_projects_locations_list_execute()`.
/// For task-level control, use `dataform_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_list(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    dataform_projects_locations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:queryUserRootContents
/// Returns the contents of a caller''s root folder in a given location. The root folder contains all resources that are created by the user and not contained in any other folder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_query_user_root_contents_execute()` to send, or `dataform_projects_locations_query_user_root_contents` for simplest API.

pub fn dataform_projects_locations_query_user_root_contents_builder<R>(
    client: &SimpleHttpClient<R>,
    location: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}:queryUserRootContents",
        location,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:queryUserRootContents
/// Returns the contents of a caller''s root folder in a given location. The root folder contains all resources that are created by the user and not contained in any other folder.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_query_user_root_contents_execute()` or `dataform_projects_locations_query_user_root_contents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_query_user_root_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_query_user_root_contents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryUserRootContentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryUserRootContentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:queryUserRootContents
/// Returns the contents of a caller''s root folder in a given location. The root folder contains all resources that are created by the user and not contained in any other folder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_query_user_root_contents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_query_user_root_contents_task()`.
/// For the simplest API, use `dataform_projects_locations_query_user_root_contents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_query_user_root_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_query_user_root_contents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryUserRootContentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_query_user_root_contents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_query_user_root_contents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsQueryUserRootContentsArgs {
    /// Path parameter: location
    pub location: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:queryUserRootContents
/// Returns the contents of a caller''s root folder in a given location. The root folder contains all resources that are created by the user and not contained in any other folder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_query_user_root_contents_builder()` + `dataform_projects_locations_query_user_root_contents_execute()`.
/// For task-level control, use `dataform_projects_locations_query_user_root_contents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_query_user_root_contents(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsQueryUserRootContentsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryUserRootContentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_query_user_root_contents_builder(
        client,
        &args.location,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dataform_projects_locations_query_user_root_contents_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/config
/// Update default config for a given project and location. **Note:** *This method does not fully implement [AIP/134](<https://google.aip.`dev/134`>). The wildcard entry (\*) is treated as a bad request, and when the field_mask is omitted, the request is treated as a full update on all modifiable fields.*
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_update_config_execute()` to send, or `dataform_projects_locations_update_config` for simplest API.

pub fn dataform_projects_locations_update_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/config",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/config
/// Update default config for a given project and location. **Note:** *This method does not fully implement [AIP/134](<https://google.aip.`dev/134`>). The wildcard entry (\*) is treated as a bad request, and when the field_mask is omitted, the request is treated as a full update on all modifiable fields.*
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_update_config_execute()` or `dataform_projects_locations_update_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_update_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_update_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Config>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Config = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/config
/// Update default config for a given project and location. **Note:** *This method does not fully implement [AIP/134](<https://google.aip.`dev/134`>). The wildcard entry (\*) is treated as a bad request, and when the field_mask is omitted, the request is treated as a full update on all modifiable fields.*
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_update_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_update_config_task()`.
/// For the simplest API, use `dataform_projects_locations_update_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_update_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_update_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Config>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_update_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_update_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsUpdateConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/config
/// Update default config for a given project and location. **Note:** *This method does not fully implement [AIP/134](<https://google.aip.`dev/134`>). The wildcard entry (\*) is treated as a bad request, and when the field_mask is omitted, the request is treated as a full update on all modifiable fields.*
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_update_config_builder()` + `dataform_projects_locations_update_config_execute()`.
/// For task-level control, use `dataform_projects_locations_update_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_update_config(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsUpdateConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Config>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_update_config_builder(client, &args.name, &args.updateMask)?;
    dataform_projects_locations_update_config_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders
/// Creates a new Folder in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_folders_create_execute()` to send, or `dataform_projects_locations_folders_create` for simplest API.

pub fn dataform_projects_locations_folders_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/folders",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders
/// Creates a new Folder in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_folders_create_execute()` or `dataform_projects_locations_folders_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Folder>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Folder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders
/// Creates a new Folder in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_folders_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_folders_create_task()`.
/// For the simplest API, use `dataform_projects_locations_folders_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_folders_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Folder>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_folders_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_folders_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsFoldersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders
/// Creates a new Folder in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_folders_create_builder()` + `dataform_projects_locations_folders_create_execute()`.
/// For task-level control, use `dataform_projects_locations_folders_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_create(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsFoldersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Folder>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_folders_create_builder(client, &args.parent)?;
    dataform_projects_locations_folders_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}
/// Deletes a single Folder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_folders_delete_execute()` to send, or `dataform_projects_locations_folders_delete` for simplest API.

pub fn dataform_projects_locations_folders_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/folders/{foldersId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}
/// Deletes a single Folder.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_folders_delete_execute()` or `dataform_projects_locations_folders_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}
/// Deletes a single Folder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_folders_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_folders_delete_task()`.
/// For the simplest API, use `dataform_projects_locations_folders_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_folders_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_folders_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_folders_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsFoldersDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}
/// Deletes a single Folder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_folders_delete_builder()` + `dataform_projects_locations_folders_delete_execute()`.
/// For task-level control, use `dataform_projects_locations_folders_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_delete(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsFoldersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_folders_delete_builder(client, &args.name)?;
    dataform_projects_locations_folders_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:deleteTree
/// Deletes a Folder with its contents (Folders, Repositories, Workspaces, ReleaseConfigs, and WorkflowConfigs).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_folders_delete_tree_execute()` to send, or `dataform_projects_locations_folders_delete_tree` for simplest API.

pub fn dataform_projects_locations_folders_delete_tree_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/folders/{foldersId}:deleteTree",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:deleteTree
/// Deletes a Folder with its contents (Folders, Repositories, Workspaces, ReleaseConfigs, and WorkflowConfigs).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_folders_delete_tree_execute()` or `dataform_projects_locations_folders_delete_tree`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_delete_tree_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_delete_tree_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:deleteTree
/// Deletes a Folder with its contents (Folders, Repositories, Workspaces, ReleaseConfigs, and WorkflowConfigs).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_folders_delete_tree_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_folders_delete_tree_task()`.
/// For the simplest API, use `dataform_projects_locations_folders_delete_tree()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_delete_tree_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_folders_delete_tree_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_folders_delete_tree_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_folders_delete_tree`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsFoldersDeleteTreeArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:deleteTree
/// Deletes a Folder with its contents (Folders, Repositories, Workspaces, ReleaseConfigs, and WorkflowConfigs).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_folders_delete_tree_builder()` + `dataform_projects_locations_folders_delete_tree_execute()`.
/// For task-level control, use `dataform_projects_locations_folders_delete_tree_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_delete_tree(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsFoldersDeleteTreeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_folders_delete_tree_builder(client, &args.name)?;
    dataform_projects_locations_folders_delete_tree_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}
/// Fetches a single Folder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_folders_get_execute()` to send, or `dataform_projects_locations_folders_get` for simplest API.

pub fn dataform_projects_locations_folders_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/folders/{foldersId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}
/// Fetches a single Folder.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_folders_get_execute()` or `dataform_projects_locations_folders_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Folder>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Folder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}
/// Fetches a single Folder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_folders_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_folders_get_task()`.
/// For the simplest API, use `dataform_projects_locations_folders_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_folders_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Folder>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_folders_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_folders_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsFoldersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}
/// Fetches a single Folder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_folders_get_builder()` + `dataform_projects_locations_folders_get_execute()`.
/// For task-level control, use `dataform_projects_locations_folders_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_get(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsFoldersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Folder>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_folders_get_builder(client, &args.name)?;
    dataform_projects_locations_folders_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_folders_get_iam_policy_execute()` to send, or `dataform_projects_locations_folders_get_iam_policy` for simplest API.

pub fn dataform_projects_locations_folders_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/folders/{foldersId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_folders_get_iam_policy_execute()` or `dataform_projects_locations_folders_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_folders_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_folders_get_iam_policy_task()`.
/// For the simplest API, use `dataform_projects_locations_folders_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_folders_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_folders_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_folders_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsFoldersGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_folders_get_iam_policy_builder()` + `dataform_projects_locations_folders_get_iam_policy_execute()`.
/// For task-level control, use `dataform_projects_locations_folders_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_get_iam_policy(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsFoldersGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_folders_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    dataform_projects_locations_folders_get_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:move
/// Moves a Folder to a new Folder, TeamFolder, or the root location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_folders_move_execute()` to send, or `dataform_projects_locations_folders_move` for simplest API.

pub fn dataform_projects_locations_folders_move_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/folders/{foldersId}:move",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:move
/// Moves a Folder to a new Folder, TeamFolder, or the root location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_folders_move_execute()` or `dataform_projects_locations_folders_move`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_move_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:move
/// Moves a Folder to a new Folder, TeamFolder, or the root location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_folders_move_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_folders_move_task()`.
/// For the simplest API, use `dataform_projects_locations_folders_move()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_folders_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_folders_move_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_folders_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsFoldersMoveArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:move
/// Moves a Folder to a new Folder, TeamFolder, or the root location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_folders_move_builder()` + `dataform_projects_locations_folders_move_execute()`.
/// For task-level control, use `dataform_projects_locations_folders_move_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_move(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsFoldersMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_folders_move_builder(client, &args.name)?;
    dataform_projects_locations_folders_move_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}
/// Updates a single Folder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_folders_patch_execute()` to send, or `dataform_projects_locations_folders_patch` for simplest API.

pub fn dataform_projects_locations_folders_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/folders/{foldersId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}
/// Updates a single Folder.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_folders_patch_execute()` or `dataform_projects_locations_folders_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Folder>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Folder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}
/// Updates a single Folder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_folders_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_folders_patch_task()`.
/// For the simplest API, use `dataform_projects_locations_folders_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_folders_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Folder>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_folders_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_folders_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsFoldersPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}
/// Updates a single Folder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_folders_patch_builder()` + `dataform_projects_locations_folders_patch_execute()`.
/// For task-level control, use `dataform_projects_locations_folders_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_patch(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsFoldersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Folder>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_folders_patch_builder(client, &args.name, &args.updateMask)?;
    dataform_projects_locations_folders_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:queryFolderContents
/// Returns the contents of a given Folder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_folders_query_folder_contents_execute()` to send, or `dataform_projects_locations_folders_query_folder_contents` for simplest API.

pub fn dataform_projects_locations_folders_query_folder_contents_builder<R>(
    client: &SimpleHttpClient<R>,
    folder: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/folders/{foldersId}:queryFolderContents",
        folder,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:queryFolderContents
/// Returns the contents of a given Folder.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_folders_query_folder_contents_execute()` or `dataform_projects_locations_folders_query_folder_contents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_query_folder_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_query_folder_contents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryFolderContentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryFolderContentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:queryFolderContents
/// Returns the contents of a given Folder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_folders_query_folder_contents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_folders_query_folder_contents_task()`.
/// For the simplest API, use `dataform_projects_locations_folders_query_folder_contents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_query_folder_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_folders_query_folder_contents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryFolderContentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_folders_query_folder_contents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_folders_query_folder_contents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsFoldersQueryFolderContentsArgs {
    /// Path parameter: folder
    pub folder: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:queryFolderContents
/// Returns the contents of a given Folder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_folders_query_folder_contents_builder()` + `dataform_projects_locations_folders_query_folder_contents_execute()`.
/// For task-level control, use `dataform_projects_locations_folders_query_folder_contents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_query_folder_contents(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsFoldersQueryFolderContentsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryFolderContentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_folders_query_folder_contents_builder(
        client,
        &args.folder,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dataform_projects_locations_folders_query_folder_contents_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_folders_set_iam_policy_execute()` to send, or `dataform_projects_locations_folders_set_iam_policy` for simplest API.

pub fn dataform_projects_locations_folders_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/folders/{foldersId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_folders_set_iam_policy_execute()` or `dataform_projects_locations_folders_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_folders_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_folders_set_iam_policy_task()`.
/// For the simplest API, use `dataform_projects_locations_folders_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_folders_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_folders_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_folders_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsFoldersSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_folders_set_iam_policy_builder()` + `dataform_projects_locations_folders_set_iam_policy_execute()`.
/// For task-level control, use `dataform_projects_locations_folders_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_set_iam_policy(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsFoldersSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_folders_set_iam_policy_builder(client, &args.resource)?;
    dataform_projects_locations_folders_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_folders_test_iam_permissions_execute()` to send, or `dataform_projects_locations_folders_test_iam_permissions` for simplest API.

pub fn dataform_projects_locations_folders_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/folders/{foldersId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_folders_test_iam_permissions_execute()` or `dataform_projects_locations_folders_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_folders_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_folders_test_iam_permissions_task()`.
/// For the simplest API, use `dataform_projects_locations_folders_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_folders_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_folders_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_folders_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_folders_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsFoldersTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/folders/{foldersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_folders_test_iam_permissions_builder()` + `dataform_projects_locations_folders_test_iam_permissions_execute()`.
/// For task-level control, use `dataform_projects_locations_folders_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_folders_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsFoldersTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_folders_test_iam_permissions_builder(client, &args.resource)?;
    dataform_projects_locations_folders_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_operations_cancel_execute()` to send, or `dataform_projects_locations_operations_cancel` for simplest API.

pub fn dataform_projects_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_operations_cancel_execute()` or `dataform_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `dataform_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_operations_cancel_builder()` + `dataform_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `dataform_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_operations_cancel_builder(client, &args.name)?;
    dataform_projects_locations_operations_cancel_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_operations_delete_execute()` to send, or `dataform_projects_locations_operations_delete` for simplest API.

pub fn dataform_projects_locations_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_operations_delete_execute()` or `dataform_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_operations_delete_task()`.
/// For the simplest API, use `dataform_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_operations_delete_builder()` + `dataform_projects_locations_operations_delete_execute()`.
/// For task-level control, use `dataform_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_operations_delete_builder(client, &args.name)?;
    dataform_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_operations_get_execute()` to send, or `dataform_projects_locations_operations_get` for simplest API.

pub fn dataform_projects_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_operations_get_execute()` or `dataform_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_operations_get_task()`.
/// For the simplest API, use `dataform_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_operations_get_builder()` + `dataform_projects_locations_operations_get_execute()`.
/// For task-level control, use `dataform_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_operations_get_builder(client, &args.name)?;
    dataform_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_operations_list_execute()` to send, or `dataform_projects_locations_operations_list` for simplest API.

pub fn dataform_projects_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_operations_list_execute()` or `dataform_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_operations_list_task()`.
/// For the simplest API, use `dataform_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_operations_list_builder()` + `dataform_projects_locations_operations_list_execute()`.
/// For task-level control, use `dataform_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    dataform_projects_locations_operations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:commit
/// Applies a Git commit to a Repository. The Repository must not have a value for git_remote_settings.url.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_commit_execute()` to send, or `dataform_projects_locations_repositories_commit` for simplest API.

pub fn dataform_projects_locations_repositories_commit_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}:commit",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:commit
/// Applies a Git commit to a Repository. The Repository must not have a value for git_remote_settings.url.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_commit_execute()` or `dataform_projects_locations_repositories_commit`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_commit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_commit_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommitRepositoryChangesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommitRepositoryChangesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:commit
/// Applies a Git commit to a Repository. The Repository must not have a value for git_remote_settings.url.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_commit_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_commit_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_commit()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_commit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_commit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CommitRepositoryChangesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_commit_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_commit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesCommitArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:commit
/// Applies a Git commit to a Repository. The Repository must not have a value for git_remote_settings.url.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_commit_builder()` + `dataform_projects_locations_repositories_commit_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_commit_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_commit(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesCommitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CommitRepositoryChangesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_commit_builder(client, &args.name)?;
    dataform_projects_locations_repositories_commit_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:computeAccessTokenStatus
/// Computes a Repository''s Git access token status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_compute_access_token_status_execute()` to send, or `dataform_projects_locations_repositories_compute_access_token_status` for simplest API.

pub fn dataform_projects_locations_repositories_compute_access_token_status_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}:computeAccessTokenStatus",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:computeAccessTokenStatus
/// Computes a Repository''s Git access token status.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_compute_access_token_status_execute()` or `dataform_projects_locations_repositories_compute_access_token_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_compute_access_token_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_compute_access_token_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ComputeRepositoryAccessTokenStatusResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ComputeRepositoryAccessTokenStatusResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:computeAccessTokenStatus
/// Computes a Repository''s Git access token status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_compute_access_token_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_compute_access_token_status_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_compute_access_token_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_compute_access_token_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_compute_access_token_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ComputeRepositoryAccessTokenStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_compute_access_token_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_compute_access_token_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesComputeAccessTokenStatusArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:computeAccessTokenStatus
/// Computes a Repository''s Git access token status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_compute_access_token_status_builder()` + `dataform_projects_locations_repositories_compute_access_token_status_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_compute_access_token_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_compute_access_token_status(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesComputeAccessTokenStatusArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ComputeRepositoryAccessTokenStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_compute_access_token_status_builder(
        client, &args.name,
    )?;
    dataform_projects_locations_repositories_compute_access_token_status_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories
/// Creates a new Repository in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_create_execute()` to send, or `dataform_projects_locations_repositories_create` for simplest API.

pub fn dataform_projects_locations_repositories_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    repositoryId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = repositoryId.as_ref() {
        query_parts.push(format!("repositoryId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories
/// Creates a new Repository in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_create_execute()` or `dataform_projects_locations_repositories_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Repository>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Repository = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories
/// Creates a new Repository in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_create_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Repository>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: repositoryId
    pub repositoryId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories
/// Creates a new Repository in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_create_builder()` + `dataform_projects_locations_repositories_create_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_create(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Repository>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_create_builder(
        client,
        &args.parent,
        &args.repositoryId,
    )?;
    dataform_projects_locations_repositories_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}
/// Deletes a single Repository.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_delete_execute()` to send, or `dataform_projects_locations_repositories_delete` for simplest API.

pub fn dataform_projects_locations_repositories_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    force: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}
/// Deletes a single Repository.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_delete_execute()` or `dataform_projects_locations_repositories_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}
/// Deletes a single Repository.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_delete_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}
/// Deletes a single Repository.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_delete_builder()` + `dataform_projects_locations_repositories_delete_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_delete(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_repositories_delete_builder(client, &args.name, &args.force)?;
    dataform_projects_locations_repositories_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:fetchHistory
/// Fetches a Repository''s history of commits. The Repository must not have a value for git_remote_settings.url.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_fetch_history_execute()` to send, or `dataform_projects_locations_repositories_fetch_history` for simplest API.

pub fn dataform_projects_locations_repositories_fetch_history_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}:fetchHistory",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:fetchHistory
/// Fetches a Repository''s history of commits. The Repository must not have a value for git_remote_settings.url.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_fetch_history_execute()` or `dataform_projects_locations_repositories_fetch_history`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_fetch_history_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_fetch_history_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FetchRepositoryHistoryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchRepositoryHistoryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:fetchHistory
/// Fetches a Repository''s history of commits. The Repository must not have a value for git_remote_settings.url.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_fetch_history_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_fetch_history_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_fetch_history()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_fetch_history_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_fetch_history_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchRepositoryHistoryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_fetch_history_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_fetch_history`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesFetchHistoryArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:fetchHistory
/// Fetches a Repository''s history of commits. The Repository must not have a value for git_remote_settings.url.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_fetch_history_builder()` + `dataform_projects_locations_repositories_fetch_history_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_fetch_history_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_fetch_history(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesFetchHistoryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchRepositoryHistoryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_fetch_history_builder(
        client,
        &args.name,
        &args.pageSize,
        &args.pageToken,
    )?;
    dataform_projects_locations_repositories_fetch_history_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:fetchRemoteBranches
/// Fetches a Repository''s remote branches.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_fetch_remote_branches_execute()` to send, or `dataform_projects_locations_repositories_fetch_remote_branches` for simplest API.

pub fn dataform_projects_locations_repositories_fetch_remote_branches_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}:fetchRemoteBranches",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:fetchRemoteBranches
/// Fetches a Repository''s remote branches.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_fetch_remote_branches_execute()` or `dataform_projects_locations_repositories_fetch_remote_branches`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_fetch_remote_branches_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_fetch_remote_branches_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FetchRemoteBranchesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchRemoteBranchesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:fetchRemoteBranches
/// Fetches a Repository''s remote branches.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_fetch_remote_branches_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_fetch_remote_branches_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_fetch_remote_branches()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_fetch_remote_branches_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_fetch_remote_branches_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchRemoteBranchesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_fetch_remote_branches_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_fetch_remote_branches`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesFetchRemoteBranchesArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:fetchRemoteBranches
/// Fetches a Repository''s remote branches.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_fetch_remote_branches_builder()` + `dataform_projects_locations_repositories_fetch_remote_branches_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_fetch_remote_branches_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_fetch_remote_branches(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesFetchRemoteBranchesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchRemoteBranchesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_repositories_fetch_remote_branches_builder(client, &args.name)?;
    dataform_projects_locations_repositories_fetch_remote_branches_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}
/// Fetches a single Repository.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_get_execute()` to send, or `dataform_projects_locations_repositories_get` for simplest API.

pub fn dataform_projects_locations_repositories_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}
/// Fetches a single Repository.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_get_execute()` or `dataform_projects_locations_repositories_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Repository>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Repository = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}
/// Fetches a single Repository.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_get_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Repository>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}
/// Fetches a single Repository.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_get_builder()` + `dataform_projects_locations_repositories_get_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_get(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Repository>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_get_builder(client, &args.name)?;
    dataform_projects_locations_repositories_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_get_iam_policy_execute()` to send, or `dataform_projects_locations_repositories_get_iam_policy` for simplest API.

pub fn dataform_projects_locations_repositories_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_get_iam_policy_execute()` or `dataform_projects_locations_repositories_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_get_iam_policy_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_get_iam_policy_builder()` + `dataform_projects_locations_repositories_get_iam_policy_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_get_iam_policy(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    dataform_projects_locations_repositories_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories
/// Lists Repositories in a given project and location. **Note:** *This method can return repositories not shown in the [Dataform UI](<https://console.cloud.google.`com/bigquery/dataform`>)*.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_list_execute()` to send, or `dataform_projects_locations_repositories_list` for simplest API.

pub fn dataform_projects_locations_repositories_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories
/// Lists Repositories in a given project and location. **Note:** *This method can return repositories not shown in the [Dataform UI](<https://console.cloud.google.`com/bigquery/dataform`>)*.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_list_execute()` or `dataform_projects_locations_repositories_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListRepositoriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRepositoriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories
/// Lists Repositories in a given project and location. **Note:** *This method can return repositories not shown in the [Dataform UI](<https://console.cloud.google.`com/bigquery/dataform`>)*.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_list_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRepositoriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories
/// Lists Repositories in a given project and location. **Note:** *This method can return repositories not shown in the [Dataform UI](<https://console.cloud.google.`com/bigquery/dataform`>)*.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_list_builder()` + `dataform_projects_locations_repositories_list_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_list(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRepositoriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dataform_projects_locations_repositories_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:move
/// Moves a Repository to a new location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_move_execute()` to send, or `dataform_projects_locations_repositories_move` for simplest API.

pub fn dataform_projects_locations_repositories_move_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}:move",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:move
/// Moves a Repository to a new location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_move_execute()` or `dataform_projects_locations_repositories_move`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_move_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:move
/// Moves a Repository to a new location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_move_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_move_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_move()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_move_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesMoveArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:move
/// Moves a Repository to a new location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_move_builder()` + `dataform_projects_locations_repositories_move_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_move_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_move(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_move_builder(client, &args.name)?;
    dataform_projects_locations_repositories_move_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}
/// Updates a single Repository. **Note:** *This method does not fully implement [AIP/134](<https://google.aip.`dev/134`>). The wildcard entry (\*) is treated as a bad request, and when the field_mask is omitted, the request is treated as a full update on all modifiable fields.*
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_patch_execute()` to send, or `dataform_projects_locations_repositories_patch` for simplest API.

pub fn dataform_projects_locations_repositories_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}
/// Updates a single Repository. **Note:** *This method does not fully implement [AIP/134](<https://google.aip.`dev/134`>). The wildcard entry (\*) is treated as a bad request, and when the field_mask is omitted, the request is treated as a full update on all modifiable fields.*
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_patch_execute()` or `dataform_projects_locations_repositories_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Repository>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Repository = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}
/// Updates a single Repository. **Note:** *This method does not fully implement [AIP/134](<https://google.aip.`dev/134`>). The wildcard entry (\*) is treated as a bad request, and when the field_mask is omitted, the request is treated as a full update on all modifiable fields.*
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_patch_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Repository>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}
/// Updates a single Repository. **Note:** *This method does not fully implement [AIP/134](<https://google.aip.`dev/134`>). The wildcard entry (\*) is treated as a bad request, and when the field_mask is omitted, the request is treated as a full update on all modifiable fields.*
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_patch_builder()` + `dataform_projects_locations_repositories_patch_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_patch(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Repository>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    dataform_projects_locations_repositories_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:queryDirectoryContents
/// Returns the contents of a given Repository directory. The Repository must not have a value for git_remote_settings.url.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_query_directory_contents_execute()` to send, or `dataform_projects_locations_repositories_query_directory_contents` for simplest API.

pub fn dataform_projects_locations_repositories_query_directory_contents_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    commitSha: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    path: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}:queryDirectoryContents",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = commitSha.as_ref() {
        query_parts.push(format!("commitSha={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = path.as_ref() {
        query_parts.push(format!("path={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:queryDirectoryContents
/// Returns the contents of a given Repository directory. The Repository must not have a value for git_remote_settings.url.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_query_directory_contents_execute()` or `dataform_projects_locations_repositories_query_directory_contents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_query_directory_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_query_directory_contents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryRepositoryDirectoryContentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryRepositoryDirectoryContentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:queryDirectoryContents
/// Returns the contents of a given Repository directory. The Repository must not have a value for git_remote_settings.url.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_query_directory_contents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_query_directory_contents_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_query_directory_contents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_query_directory_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_query_directory_contents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryRepositoryDirectoryContentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_query_directory_contents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_query_directory_contents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesQueryDirectoryContentsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: commitSha
    pub commitSha: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: path
    pub path: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:queryDirectoryContents
/// Returns the contents of a given Repository directory. The Repository must not have a value for git_remote_settings.url.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_query_directory_contents_builder()` + `dataform_projects_locations_repositories_query_directory_contents_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_query_directory_contents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_query_directory_contents(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesQueryDirectoryContentsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryRepositoryDirectoryContentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_query_directory_contents_builder(
        client,
        &args.name,
        &args.commitSha,
        &args.pageSize,
        &args.pageToken,
        &args.path,
    )?;
    dataform_projects_locations_repositories_query_directory_contents_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:readFile
/// Returns the contents of a file (inside a Repository). The Repository must not have a value for git_remote_settings.url.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_read_file_execute()` to send, or `dataform_projects_locations_repositories_read_file` for simplest API.

pub fn dataform_projects_locations_repositories_read_file_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    commitSha: &Option<Option<String>>,
    path: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}:readFile",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = commitSha.as_ref() {
        query_parts.push(format!("commitSha={}", val));
    }
    if let Some(val) = path.as_ref() {
        query_parts.push(format!("path={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:readFile
/// Returns the contents of a file (inside a Repository). The Repository must not have a value for git_remote_settings.url.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_read_file_execute()` or `dataform_projects_locations_repositories_read_file`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_read_file_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_read_file_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReadRepositoryFileResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReadRepositoryFileResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:readFile
/// Returns the contents of a file (inside a Repository). The Repository must not have a value for git_remote_settings.url.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_read_file_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_read_file_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_read_file()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_read_file_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_read_file_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReadRepositoryFileResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_read_file_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_read_file`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesReadFileArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: commitSha
    pub commitSha: Option<Option<String>>,
    /// Query parameter: path
    pub path: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:readFile
/// Returns the contents of a file (inside a Repository). The Repository must not have a value for git_remote_settings.url.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_read_file_builder()` + `dataform_projects_locations_repositories_read_file_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_read_file_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_read_file(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesReadFileArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReadRepositoryFileResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_read_file_builder(
        client,
        &args.name,
        &args.commitSha,
        &args.path,
    )?;
    dataform_projects_locations_repositories_read_file_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_set_iam_policy_execute()` to send, or `dataform_projects_locations_repositories_set_iam_policy` for simplest API.

pub fn dataform_projects_locations_repositories_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_set_iam_policy_execute()` or `dataform_projects_locations_repositories_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_set_iam_policy_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_set_iam_policy_builder()` + `dataform_projects_locations_repositories_set_iam_policy_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_set_iam_policy(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_repositories_set_iam_policy_builder(client, &args.resource)?;
    dataform_projects_locations_repositories_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_test_iam_permissions_execute()` to send, or `dataform_projects_locations_repositories_test_iam_permissions` for simplest API.

pub fn dataform_projects_locations_repositories_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_test_iam_permissions_execute()` or `dataform_projects_locations_repositories_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_test_iam_permissions_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_test_iam_permissions_builder()` + `dataform_projects_locations_repositories_test_iam_permissions_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    dataform_projects_locations_repositories_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults
/// Creates a new CompilationResult in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_compilation_results_create_execute()` to send, or `dataform_projects_locations_repositories_compilation_results_create` for simplest API.

pub fn dataform_projects_locations_repositories_compilation_results_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults
/// Creates a new CompilationResult in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_compilation_results_create_execute()` or `dataform_projects_locations_repositories_compilation_results_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_compilation_results_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_compilation_results_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CompilationResult>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CompilationResult = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults
/// Creates a new CompilationResult in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_compilation_results_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_compilation_results_create_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_compilation_results_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_compilation_results_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_compilation_results_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CompilationResult>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_compilation_results_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_compilation_results_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesCompilationResultsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults
/// Creates a new CompilationResult in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_compilation_results_create_builder()` + `dataform_projects_locations_repositories_compilation_results_create_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_compilation_results_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_compilation_results_create(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesCompilationResultsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CompilationResult>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_compilation_results_create_builder(
        client,
        &args.parent,
    )?;
    dataform_projects_locations_repositories_compilation_results_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults/{compilationResultsId}
/// Fetches a single CompilationResult.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_compilation_results_get_execute()` to send, or `dataform_projects_locations_repositories_compilation_results_get` for simplest API.

pub fn dataform_projects_locations_repositories_compilation_results_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults/{compilationResultsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults/{compilationResultsId}
/// Fetches a single CompilationResult.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_compilation_results_get_execute()` or `dataform_projects_locations_repositories_compilation_results_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_compilation_results_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_compilation_results_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CompilationResult>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CompilationResult = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults/{compilationResultsId}
/// Fetches a single CompilationResult.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_compilation_results_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_compilation_results_get_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_compilation_results_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_compilation_results_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_compilation_results_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CompilationResult>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_compilation_results_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_compilation_results_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesCompilationResultsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults/{compilationResultsId}
/// Fetches a single CompilationResult.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_compilation_results_get_builder()` + `dataform_projects_locations_repositories_compilation_results_get_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_compilation_results_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_compilation_results_get(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesCompilationResultsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CompilationResult>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_compilation_results_get_builder(
        client, &args.name,
    )?;
    dataform_projects_locations_repositories_compilation_results_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults
/// Lists CompilationResults in a given Repository.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_compilation_results_list_execute()` to send, or `dataform_projects_locations_repositories_compilation_results_list` for simplest API.

pub fn dataform_projects_locations_repositories_compilation_results_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults
/// Lists CompilationResults in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_compilation_results_list_execute()` or `dataform_projects_locations_repositories_compilation_results_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_compilation_results_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_compilation_results_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCompilationResultsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCompilationResultsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults
/// Lists CompilationResults in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_compilation_results_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_compilation_results_list_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_compilation_results_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_compilation_results_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_compilation_results_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCompilationResultsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_compilation_results_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_compilation_results_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesCompilationResultsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults
/// Lists CompilationResults in a given Repository.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_compilation_results_list_builder()` + `dataform_projects_locations_repositories_compilation_results_list_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_compilation_results_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_compilation_results_list(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesCompilationResultsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCompilationResultsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_compilation_results_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dataform_projects_locations_repositories_compilation_results_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults/{compilationResultsId}:query
/// Returns CompilationResultActions in a given CompilationResult.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_compilation_results_query_execute()` to send, or `dataform_projects_locations_repositories_compilation_results_query` for simplest API.

pub fn dataform_projects_locations_repositories_compilation_results_query_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults/{compilationResultsId}:query",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults/{compilationResultsId}:query
/// Returns CompilationResultActions in a given CompilationResult.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_compilation_results_query_execute()` or `dataform_projects_locations_repositories_compilation_results_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_compilation_results_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_compilation_results_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryCompilationResultActionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryCompilationResultActionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults/{compilationResultsId}:query
/// Returns CompilationResultActions in a given CompilationResult.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_compilation_results_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_compilation_results_query_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_compilation_results_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_compilation_results_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_compilation_results_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryCompilationResultActionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_compilation_results_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_compilation_results_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesCompilationResultsQueryArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults/{compilationResultsId}:query
/// Returns CompilationResultActions in a given CompilationResult.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_compilation_results_query_builder()` + `dataform_projects_locations_repositories_compilation_results_query_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_compilation_results_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_compilation_results_query(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesCompilationResultsQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryCompilationResultActionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_compilation_results_query_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    dataform_projects_locations_repositories_compilation_results_query_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs
/// Creates a new ReleaseConfig in a given Repository.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_release_configs_create_execute()` to send, or `dataform_projects_locations_repositories_release_configs_create` for simplest API.

pub fn dataform_projects_locations_repositories_release_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    releaseConfigId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = releaseConfigId.as_ref() {
        query_parts.push(format!("releaseConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs
/// Creates a new ReleaseConfig in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_release_configs_create_execute()` or `dataform_projects_locations_repositories_release_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_release_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_release_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReleaseConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReleaseConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs
/// Creates a new ReleaseConfig in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_release_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_release_configs_create_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_release_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_release_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_release_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReleaseConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_release_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_release_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesReleaseConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: releaseConfigId
    pub releaseConfigId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs
/// Creates a new ReleaseConfig in a given Repository.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_release_configs_create_builder()` + `dataform_projects_locations_repositories_release_configs_create_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_release_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_release_configs_create(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesReleaseConfigsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReleaseConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_release_configs_create_builder(
        client,
        &args.parent,
        &args.releaseConfigId,
    )?;
    dataform_projects_locations_repositories_release_configs_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs/{releaseConfigsId}
/// Deletes a single ReleaseConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_release_configs_delete_execute()` to send, or `dataform_projects_locations_repositories_release_configs_delete` for simplest API.

pub fn dataform_projects_locations_repositories_release_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs/{releaseConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs/{releaseConfigsId}
/// Deletes a single ReleaseConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_release_configs_delete_execute()` or `dataform_projects_locations_repositories_release_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_release_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_release_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs/{releaseConfigsId}
/// Deletes a single ReleaseConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_release_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_release_configs_delete_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_release_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_release_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_release_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_release_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_release_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesReleaseConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs/{releaseConfigsId}
/// Deletes a single ReleaseConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_release_configs_delete_builder()` + `dataform_projects_locations_repositories_release_configs_delete_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_release_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_release_configs_delete(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesReleaseConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_release_configs_delete_builder(
        client, &args.name,
    )?;
    dataform_projects_locations_repositories_release_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs/{releaseConfigsId}
/// Fetches a single ReleaseConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_release_configs_get_execute()` to send, or `dataform_projects_locations_repositories_release_configs_get` for simplest API.

pub fn dataform_projects_locations_repositories_release_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs/{releaseConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs/{releaseConfigsId}
/// Fetches a single ReleaseConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_release_configs_get_execute()` or `dataform_projects_locations_repositories_release_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_release_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_release_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReleaseConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReleaseConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs/{releaseConfigsId}
/// Fetches a single ReleaseConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_release_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_release_configs_get_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_release_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_release_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_release_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReleaseConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_release_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_release_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesReleaseConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs/{releaseConfigsId}
/// Fetches a single ReleaseConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_release_configs_get_builder()` + `dataform_projects_locations_repositories_release_configs_get_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_release_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_release_configs_get(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesReleaseConfigsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReleaseConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_repositories_release_configs_get_builder(client, &args.name)?;
    dataform_projects_locations_repositories_release_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs
/// Lists ReleaseConfigs in a given Repository.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_release_configs_list_execute()` to send, or `dataform_projects_locations_repositories_release_configs_list` for simplest API.

pub fn dataform_projects_locations_repositories_release_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs
/// Lists ReleaseConfigs in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_release_configs_list_execute()` or `dataform_projects_locations_repositories_release_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_release_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_release_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReleaseConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReleaseConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs
/// Lists ReleaseConfigs in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_release_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_release_configs_list_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_release_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_release_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_release_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListReleaseConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_release_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_release_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesReleaseConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs
/// Lists ReleaseConfigs in a given Repository.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_release_configs_list_builder()` + `dataform_projects_locations_repositories_release_configs_list_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_release_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_release_configs_list(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesReleaseConfigsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListReleaseConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_release_configs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    dataform_projects_locations_repositories_release_configs_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs/{releaseConfigsId}
/// Updates a single ReleaseConfig. **Note:** *This method does not fully implement [AIP/134](<https://google.aip.`dev/134`>). The wildcard entry (\*) is treated as a bad request, and when the field_mask is omitted, the request is treated as a full update on all modifiable fields.*
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_release_configs_patch_execute()` to send, or `dataform_projects_locations_repositories_release_configs_patch` for simplest API.

pub fn dataform_projects_locations_repositories_release_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs/{releaseConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs/{releaseConfigsId}
/// Updates a single ReleaseConfig. **Note:** *This method does not fully implement [AIP/134](<https://google.aip.`dev/134`>). The wildcard entry (\*) is treated as a bad request, and when the field_mask is omitted, the request is treated as a full update on all modifiable fields.*
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_release_configs_patch_execute()` or `dataform_projects_locations_repositories_release_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_release_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_release_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReleaseConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReleaseConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs/{releaseConfigsId}
/// Updates a single ReleaseConfig. **Note:** *This method does not fully implement [AIP/134](<https://google.aip.`dev/134`>). The wildcard entry (\*) is treated as a bad request, and when the field_mask is omitted, the request is treated as a full update on all modifiable fields.*
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_release_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_release_configs_patch_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_release_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_release_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_release_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReleaseConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_release_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_release_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesReleaseConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/releaseConfigs/{releaseConfigsId}
/// Updates a single ReleaseConfig. **Note:** *This method does not fully implement [AIP/134](<https://google.aip.`dev/134`>). The wildcard entry (\*) is treated as a bad request, and when the field_mask is omitted, the request is treated as a full update on all modifiable fields.*
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_release_configs_patch_builder()` + `dataform_projects_locations_repositories_release_configs_patch_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_release_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_release_configs_patch(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesReleaseConfigsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReleaseConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_release_configs_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    dataform_projects_locations_repositories_release_configs_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs
/// Creates a new WorkflowConfig in a given Repository.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workflow_configs_create_execute()` to send, or `dataform_projects_locations_repositories_workflow_configs_create` for simplest API.

pub fn dataform_projects_locations_repositories_workflow_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    workflowConfigId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = workflowConfigId.as_ref() {
        query_parts.push(format!("workflowConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs
/// Creates a new WorkflowConfig in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workflow_configs_create_execute()` or `dataform_projects_locations_repositories_workflow_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WorkflowConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkflowConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs
/// Creates a new WorkflowConfig in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workflow_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_configs_create_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workflow_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workflow_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workflow_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workflow_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkflowConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: workflowConfigId
    pub workflowConfigId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs
/// Creates a new WorkflowConfig in a given Repository.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workflow_configs_create_builder()` + `dataform_projects_locations_repositories_workflow_configs_create_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_configs_create(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkflowConfigsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workflow_configs_create_builder(
        client,
        &args.parent,
        &args.workflowConfigId,
    )?;
    dataform_projects_locations_repositories_workflow_configs_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs/{workflowConfigsId}
/// Deletes a single WorkflowConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workflow_configs_delete_execute()` to send, or `dataform_projects_locations_repositories_workflow_configs_delete` for simplest API.

pub fn dataform_projects_locations_repositories_workflow_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs/{workflowConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs/{workflowConfigsId}
/// Deletes a single WorkflowConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workflow_configs_delete_execute()` or `dataform_projects_locations_repositories_workflow_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs/{workflowConfigsId}
/// Deletes a single WorkflowConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workflow_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_configs_delete_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workflow_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workflow_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workflow_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workflow_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkflowConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs/{workflowConfigsId}
/// Deletes a single WorkflowConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workflow_configs_delete_builder()` + `dataform_projects_locations_repositories_workflow_configs_delete_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_configs_delete(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkflowConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workflow_configs_delete_builder(
        client, &args.name,
    )?;
    dataform_projects_locations_repositories_workflow_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs/{workflowConfigsId}
/// Fetches a single WorkflowConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workflow_configs_get_execute()` to send, or `dataform_projects_locations_repositories_workflow_configs_get` for simplest API.

pub fn dataform_projects_locations_repositories_workflow_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs/{workflowConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs/{workflowConfigsId}
/// Fetches a single WorkflowConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workflow_configs_get_execute()` or `dataform_projects_locations_repositories_workflow_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WorkflowConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkflowConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs/{workflowConfigsId}
/// Fetches a single WorkflowConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workflow_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_configs_get_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workflow_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workflow_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workflow_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workflow_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkflowConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs/{workflowConfigsId}
/// Fetches a single WorkflowConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workflow_configs_get_builder()` + `dataform_projects_locations_repositories_workflow_configs_get_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_configs_get(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkflowConfigsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_repositories_workflow_configs_get_builder(client, &args.name)?;
    dataform_projects_locations_repositories_workflow_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs
/// Lists WorkflowConfigs in a given Repository.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workflow_configs_list_execute()` to send, or `dataform_projects_locations_repositories_workflow_configs_list` for simplest API.

pub fn dataform_projects_locations_repositories_workflow_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs
/// Lists WorkflowConfigs in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workflow_configs_list_execute()` or `dataform_projects_locations_repositories_workflow_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListWorkflowConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListWorkflowConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs
/// Lists WorkflowConfigs in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workflow_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_configs_list_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workflow_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workflow_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListWorkflowConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workflow_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workflow_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkflowConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs
/// Lists WorkflowConfigs in a given Repository.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workflow_configs_list_builder()` + `dataform_projects_locations_repositories_workflow_configs_list_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_configs_list(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkflowConfigsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListWorkflowConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workflow_configs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    dataform_projects_locations_repositories_workflow_configs_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs/{workflowConfigsId}
/// Updates a single WorkflowConfig. **Note:** *This method does not fully implement [AIP/134](<https://google.aip.`dev/134`>). The wildcard entry (\*) is treated as a bad request, and when the field_mask is omitted, the request is treated as a full update on all modifiable fields.*
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workflow_configs_patch_execute()` to send, or `dataform_projects_locations_repositories_workflow_configs_patch` for simplest API.

pub fn dataform_projects_locations_repositories_workflow_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs/{workflowConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs/{workflowConfigsId}
/// Updates a single WorkflowConfig. **Note:** *This method does not fully implement [AIP/134](<https://google.aip.`dev/134`>). The wildcard entry (\*) is treated as a bad request, and when the field_mask is omitted, the request is treated as a full update on all modifiable fields.*
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workflow_configs_patch_execute()` or `dataform_projects_locations_repositories_workflow_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WorkflowConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkflowConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs/{workflowConfigsId}
/// Updates a single WorkflowConfig. **Note:** *This method does not fully implement [AIP/134](<https://google.aip.`dev/134`>). The wildcard entry (\*) is treated as a bad request, and when the field_mask is omitted, the request is treated as a full update on all modifiable fields.*
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workflow_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_configs_patch_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workflow_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workflow_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workflow_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workflow_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkflowConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowConfigs/{workflowConfigsId}
/// Updates a single WorkflowConfig. **Note:** *This method does not fully implement [AIP/134](<https://google.aip.`dev/134`>). The wildcard entry (\*) is treated as a bad request, and when the field_mask is omitted, the request is treated as a full update on all modifiable fields.*
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workflow_configs_patch_builder()` + `dataform_projects_locations_repositories_workflow_configs_patch_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_configs_patch(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkflowConfigsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workflow_configs_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    dataform_projects_locations_repositories_workflow_configs_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}:cancel
/// Requests cancellation of a running WorkflowInvocation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workflow_invocations_cancel_execute()` to send, or `dataform_projects_locations_repositories_workflow_invocations_cancel` for simplest API.

pub fn dataform_projects_locations_repositories_workflow_invocations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}:cancel
/// Requests cancellation of a running WorkflowInvocation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workflow_invocations_cancel_execute()` or `dataform_projects_locations_repositories_workflow_invocations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_invocations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_invocations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CancelWorkflowInvocationResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CancelWorkflowInvocationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}:cancel
/// Requests cancellation of a running WorkflowInvocation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workflow_invocations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_invocations_cancel_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workflow_invocations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_invocations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workflow_invocations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CancelWorkflowInvocationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workflow_invocations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workflow_invocations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkflowInvocationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}:cancel
/// Requests cancellation of a running WorkflowInvocation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workflow_invocations_cancel_builder()` + `dataform_projects_locations_repositories_workflow_invocations_cancel_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_invocations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_invocations_cancel(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkflowInvocationsCancelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CancelWorkflowInvocationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workflow_invocations_cancel_builder(
        client, &args.name,
    )?;
    dataform_projects_locations_repositories_workflow_invocations_cancel_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations
/// Creates a new WorkflowInvocation in a given Repository.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workflow_invocations_create_execute()` to send, or `dataform_projects_locations_repositories_workflow_invocations_create` for simplest API.

pub fn dataform_projects_locations_repositories_workflow_invocations_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations
/// Creates a new WorkflowInvocation in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workflow_invocations_create_execute()` or `dataform_projects_locations_repositories_workflow_invocations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_invocations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_invocations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WorkflowInvocation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkflowInvocation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations
/// Creates a new WorkflowInvocation in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workflow_invocations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_invocations_create_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workflow_invocations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_invocations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workflow_invocations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowInvocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workflow_invocations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workflow_invocations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkflowInvocationsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations
/// Creates a new WorkflowInvocation in a given Repository.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workflow_invocations_create_builder()` + `dataform_projects_locations_repositories_workflow_invocations_create_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_invocations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_invocations_create(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkflowInvocationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowInvocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workflow_invocations_create_builder(
        client,
        &args.parent,
    )?;
    dataform_projects_locations_repositories_workflow_invocations_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}
/// Deletes a single WorkflowInvocation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workflow_invocations_delete_execute()` to send, or `dataform_projects_locations_repositories_workflow_invocations_delete` for simplest API.

pub fn dataform_projects_locations_repositories_workflow_invocations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}
/// Deletes a single WorkflowInvocation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workflow_invocations_delete_execute()` or `dataform_projects_locations_repositories_workflow_invocations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_invocations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_invocations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}
/// Deletes a single WorkflowInvocation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workflow_invocations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_invocations_delete_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workflow_invocations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_invocations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workflow_invocations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workflow_invocations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workflow_invocations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkflowInvocationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}
/// Deletes a single WorkflowInvocation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workflow_invocations_delete_builder()` + `dataform_projects_locations_repositories_workflow_invocations_delete_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_invocations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_invocations_delete(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkflowInvocationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workflow_invocations_delete_builder(
        client, &args.name,
    )?;
    dataform_projects_locations_repositories_workflow_invocations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}
/// Fetches a single WorkflowInvocation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workflow_invocations_get_execute()` to send, or `dataform_projects_locations_repositories_workflow_invocations_get` for simplest API.

pub fn dataform_projects_locations_repositories_workflow_invocations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}
/// Fetches a single WorkflowInvocation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workflow_invocations_get_execute()` or `dataform_projects_locations_repositories_workflow_invocations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_invocations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_invocations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WorkflowInvocation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkflowInvocation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}
/// Fetches a single WorkflowInvocation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workflow_invocations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_invocations_get_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workflow_invocations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_invocations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workflow_invocations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowInvocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workflow_invocations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workflow_invocations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkflowInvocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}
/// Fetches a single WorkflowInvocation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workflow_invocations_get_builder()` + `dataform_projects_locations_repositories_workflow_invocations_get_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_invocations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_invocations_get(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkflowInvocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowInvocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workflow_invocations_get_builder(
        client, &args.name,
    )?;
    dataform_projects_locations_repositories_workflow_invocations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations
/// Lists WorkflowInvocations in a given Repository.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workflow_invocations_list_execute()` to send, or `dataform_projects_locations_repositories_workflow_invocations_list` for simplest API.

pub fn dataform_projects_locations_repositories_workflow_invocations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations
/// Lists WorkflowInvocations in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workflow_invocations_list_execute()` or `dataform_projects_locations_repositories_workflow_invocations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_invocations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_invocations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListWorkflowInvocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListWorkflowInvocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations
/// Lists WorkflowInvocations in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workflow_invocations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_invocations_list_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workflow_invocations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_invocations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workflow_invocations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListWorkflowInvocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workflow_invocations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workflow_invocations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkflowInvocationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations
/// Lists WorkflowInvocations in a given Repository.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workflow_invocations_list_builder()` + `dataform_projects_locations_repositories_workflow_invocations_list_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_invocations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_invocations_list(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkflowInvocationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListWorkflowInvocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workflow_invocations_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dataform_projects_locations_repositories_workflow_invocations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}:query
/// Returns WorkflowInvocationActions in a given WorkflowInvocation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workflow_invocations_query_execute()` to send, or `dataform_projects_locations_repositories_workflow_invocations_query` for simplest API.

pub fn dataform_projects_locations_repositories_workflow_invocations_query_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}:query",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}:query
/// Returns WorkflowInvocationActions in a given WorkflowInvocation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workflow_invocations_query_execute()` or `dataform_projects_locations_repositories_workflow_invocations_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_invocations_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_invocations_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryWorkflowInvocationActionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryWorkflowInvocationActionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}:query
/// Returns WorkflowInvocationActions in a given WorkflowInvocation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workflow_invocations_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_invocations_query_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workflow_invocations_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workflow_invocations_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workflow_invocations_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryWorkflowInvocationActionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workflow_invocations_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workflow_invocations_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkflowInvocationsQueryArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workflowInvocations/{workflowInvocationsId}:query
/// Returns WorkflowInvocationActions in a given WorkflowInvocation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workflow_invocations_query_builder()` + `dataform_projects_locations_repositories_workflow_invocations_query_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workflow_invocations_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workflow_invocations_query(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkflowInvocationsQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryWorkflowInvocationActionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workflow_invocations_query_builder(
        client,
        &args.name,
        &args.pageSize,
        &args.pageToken,
    )?;
    dataform_projects_locations_repositories_workflow_invocations_query_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:commit
/// Applies a Git commit for uncommitted files in a Workspace.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_commit_execute()` to send, or `dataform_projects_locations_repositories_workspaces_commit` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_commit_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:commit",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:commit
/// Applies a Git commit for uncommitted files in a Workspace.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_commit_execute()` or `dataform_projects_locations_repositories_workspaces_commit`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_commit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_commit_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommitWorkspaceChangesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommitWorkspaceChangesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:commit
/// Applies a Git commit for uncommitted files in a Workspace.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_commit_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_commit_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_commit()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_commit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_commit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CommitWorkspaceChangesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_commit_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_commit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesCommitArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:commit
/// Applies a Git commit for uncommitted files in a Workspace.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_commit_builder()` + `dataform_projects_locations_repositories_workspaces_commit_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_commit_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_commit(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesCommitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CommitWorkspaceChangesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_repositories_workspaces_commit_builder(client, &args.name)?;
    dataform_projects_locations_repositories_workspaces_commit_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces
/// Creates a new Workspace in a given Repository.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_create_execute()` to send, or `dataform_projects_locations_repositories_workspaces_create` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    workspaceId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = workspaceId.as_ref() {
        query_parts.push(format!("workspaceId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces
/// Creates a new Workspace in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_create_execute()` or `dataform_projects_locations_repositories_workspaces_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Workspace>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Workspace = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces
/// Creates a new Workspace in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_create_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Workspace>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: workspaceId
    pub workspaceId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces
/// Creates a new Workspace in a given Repository.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_create_builder()` + `dataform_projects_locations_repositories_workspaces_create_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_create(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Workspace>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workspaces_create_builder(
        client,
        &args.parent,
        &args.workspaceId,
    )?;
    dataform_projects_locations_repositories_workspaces_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}
/// Deletes a single Workspace.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_delete_execute()` to send, or `dataform_projects_locations_repositories_workspaces_delete` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}
/// Deletes a single Workspace.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_delete_execute()` or `dataform_projects_locations_repositories_workspaces_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}
/// Deletes a single Workspace.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_delete_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}
/// Deletes a single Workspace.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_delete_builder()` + `dataform_projects_locations_repositories_workspaces_delete_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_delete(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_repositories_workspaces_delete_builder(client, &args.name)?;
    dataform_projects_locations_repositories_workspaces_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:fetchFileDiff
/// Fetches Git diff for an uncommitted file in a Workspace.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_fetch_file_diff_execute()` to send, or `dataform_projects_locations_repositories_workspaces_fetch_file_diff` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_fetch_file_diff_builder<R>(
    client: &SimpleHttpClient<R>,
    workspace: &String,
    path: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:fetchFileDiff",
        workspace,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = path.as_ref() {
        query_parts.push(format!("path={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:fetchFileDiff
/// Fetches Git diff for an uncommitted file in a Workspace.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_fetch_file_diff_execute()` or `dataform_projects_locations_repositories_workspaces_fetch_file_diff`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_fetch_file_diff_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_fetch_file_diff_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FetchFileDiffResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchFileDiffResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:fetchFileDiff
/// Fetches Git diff for an uncommitted file in a Workspace.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_fetch_file_diff_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_fetch_file_diff_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_fetch_file_diff()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_fetch_file_diff_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_fetch_file_diff_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchFileDiffResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_fetch_file_diff_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_fetch_file_diff`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesFetchFileDiffArgs {
    /// Path parameter: workspace
    pub workspace: String,
    /// Query parameter: path
    pub path: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:fetchFileDiff
/// Fetches Git diff for an uncommitted file in a Workspace.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_fetch_file_diff_builder()` + `dataform_projects_locations_repositories_workspaces_fetch_file_diff_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_fetch_file_diff_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_fetch_file_diff(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesFetchFileDiffArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchFileDiffResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workspaces_fetch_file_diff_builder(
        client,
        &args.workspace,
        &args.path,
    )?;
    dataform_projects_locations_repositories_workspaces_fetch_file_diff_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:fetchFileGitStatuses
/// Fetches Git statuses for the files in a Workspace.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses_execute()` to send, or `dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:fetchFileGitStatuses",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:fetchFileGitStatuses
/// Fetches Git statuses for the files in a Workspace.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses_execute()` or `dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FetchFileGitStatusesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchFileGitStatusesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:fetchFileGitStatuses
/// Fetches Git statuses for the files in a Workspace.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchFileGitStatusesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesFetchFileGitStatusesArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:fetchFileGitStatuses
/// Fetches Git statuses for the files in a Workspace.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses_builder()` + `dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesFetchFileGitStatusesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchFileGitStatusesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses_builder(
            client, &args.name,
        )?;
    dataform_projects_locations_repositories_workspaces_fetch_file_git_statuses_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:fetchGitAheadBehind
/// Fetches Git `ahead/behind` against a remote branch.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind_execute()` to send, or `dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    remoteBranch: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:fetchGitAheadBehind",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = remoteBranch.as_ref() {
        query_parts.push(format!("remoteBranch={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:fetchGitAheadBehind
/// Fetches Git `ahead/behind` against a remote branch.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind_execute()` or `dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FetchGitAheadBehindResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchGitAheadBehindResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:fetchGitAheadBehind
/// Fetches Git `ahead/behind` against a remote branch.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchGitAheadBehindResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesFetchGitAheadBehindArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: remoteBranch
    pub remoteBranch: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:fetchGitAheadBehind
/// Fetches Git `ahead/behind` against a remote branch.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind_builder()` + `dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesFetchGitAheadBehindArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchGitAheadBehindResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind_builder(
            client,
            &args.name,
            &args.remoteBranch,
        )?;
    dataform_projects_locations_repositories_workspaces_fetch_git_ahead_behind_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}
/// Fetches a single Workspace.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_get_execute()` to send, or `dataform_projects_locations_repositories_workspaces_get` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}
/// Fetches a single Workspace.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_get_execute()` or `dataform_projects_locations_repositories_workspaces_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Workspace>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Workspace = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}
/// Fetches a single Workspace.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_get_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Workspace>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}
/// Fetches a single Workspace.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_get_builder()` + `dataform_projects_locations_repositories_workspaces_get_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_get(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Workspace>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_repositories_workspaces_get_builder(client, &args.name)?;
    dataform_projects_locations_repositories_workspaces_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_get_iam_policy_execute()` to send, or `dataform_projects_locations_repositories_workspaces_get_iam_policy` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_get_iam_policy_execute()` or `dataform_projects_locations_repositories_workspaces_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_get_iam_policy_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_get_iam_policy_builder()` + `dataform_projects_locations_repositories_workspaces_get_iam_policy_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_get_iam_policy(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workspaces_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    dataform_projects_locations_repositories_workspaces_get_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:installNpmPackages
/// Installs dependency NPM packages (inside a Workspace).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_install_npm_packages_execute()` to send, or `dataform_projects_locations_repositories_workspaces_install_npm_packages` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_install_npm_packages_builder<R>(
    client: &SimpleHttpClient<R>,
    workspace: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:installNpmPackages",
        workspace,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:installNpmPackages
/// Installs dependency NPM packages (inside a Workspace).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_install_npm_packages_execute()` or `dataform_projects_locations_repositories_workspaces_install_npm_packages`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_install_npm_packages_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_install_npm_packages_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstallNpmPackagesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstallNpmPackagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:installNpmPackages
/// Installs dependency NPM packages (inside a Workspace).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_install_npm_packages_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_install_npm_packages_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_install_npm_packages()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_install_npm_packages_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_install_npm_packages_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstallNpmPackagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataform_projects_locations_repositories_workspaces_install_npm_packages_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_install_npm_packages`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesInstallNpmPackagesArgs {
    /// Path parameter: workspace
    pub workspace: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:installNpmPackages
/// Installs dependency NPM packages (inside a Workspace).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_install_npm_packages_builder()` + `dataform_projects_locations_repositories_workspaces_install_npm_packages_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_install_npm_packages_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_install_npm_packages(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesInstallNpmPackagesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstallNpmPackagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workspaces_install_npm_packages_builder(
        client,
        &args.workspace,
    )?;
    dataform_projects_locations_repositories_workspaces_install_npm_packages_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces
/// Lists Workspaces in a given Repository.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_list_execute()` to send, or `dataform_projects_locations_repositories_workspaces_list` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces
/// Lists Workspaces in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_list_execute()` or `dataform_projects_locations_repositories_workspaces_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListWorkspacesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListWorkspacesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces
/// Lists Workspaces in a given Repository.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_list_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListWorkspacesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces
/// Lists Workspaces in a given Repository.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_list_builder()` + `dataform_projects_locations_repositories_workspaces_list_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_list(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListWorkspacesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workspaces_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dataform_projects_locations_repositories_workspaces_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:makeDirectory
/// Creates a directory inside a Workspace.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_make_directory_execute()` to send, or `dataform_projects_locations_repositories_workspaces_make_directory` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_make_directory_builder<R>(
    client: &SimpleHttpClient<R>,
    workspace: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:makeDirectory",
        workspace,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:makeDirectory
/// Creates a directory inside a Workspace.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_make_directory_execute()` or `dataform_projects_locations_repositories_workspaces_make_directory`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_make_directory_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_make_directory_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MakeDirectoryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MakeDirectoryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:makeDirectory
/// Creates a directory inside a Workspace.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_make_directory_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_make_directory_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_make_directory()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_make_directory_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_make_directory_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MakeDirectoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_make_directory_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_make_directory`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesMakeDirectoryArgs {
    /// Path parameter: workspace
    pub workspace: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:makeDirectory
/// Creates a directory inside a Workspace.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_make_directory_builder()` + `dataform_projects_locations_repositories_workspaces_make_directory_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_make_directory_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_make_directory(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesMakeDirectoryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MakeDirectoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workspaces_make_directory_builder(
        client,
        &args.workspace,
    )?;
    dataform_projects_locations_repositories_workspaces_make_directory_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:moveDirectory
/// Moves a directory (inside a Workspace), and all of its contents, to a new location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_move_directory_execute()` to send, or `dataform_projects_locations_repositories_workspaces_move_directory` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_move_directory_builder<R>(
    client: &SimpleHttpClient<R>,
    workspace: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:moveDirectory",
        workspace,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:moveDirectory
/// Moves a directory (inside a Workspace), and all of its contents, to a new location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_move_directory_execute()` or `dataform_projects_locations_repositories_workspaces_move_directory`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_move_directory_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_move_directory_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MoveDirectoryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MoveDirectoryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:moveDirectory
/// Moves a directory (inside a Workspace), and all of its contents, to a new location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_move_directory_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_move_directory_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_move_directory()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_move_directory_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_move_directory_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MoveDirectoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_move_directory_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_move_directory`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesMoveDirectoryArgs {
    /// Path parameter: workspace
    pub workspace: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:moveDirectory
/// Moves a directory (inside a Workspace), and all of its contents, to a new location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_move_directory_builder()` + `dataform_projects_locations_repositories_workspaces_move_directory_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_move_directory_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_move_directory(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesMoveDirectoryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MoveDirectoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workspaces_move_directory_builder(
        client,
        &args.workspace,
    )?;
    dataform_projects_locations_repositories_workspaces_move_directory_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:moveFile
/// Moves a file (inside a Workspace) to a new location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_move_file_execute()` to send, or `dataform_projects_locations_repositories_workspaces_move_file` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_move_file_builder<R>(
    client: &SimpleHttpClient<R>,
    workspace: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:moveFile",
        workspace,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:moveFile
/// Moves a file (inside a Workspace) to a new location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_move_file_execute()` or `dataform_projects_locations_repositories_workspaces_move_file`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_move_file_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_move_file_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MoveFileResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MoveFileResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:moveFile
/// Moves a file (inside a Workspace) to a new location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_move_file_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_move_file_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_move_file()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_move_file_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_move_file_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MoveFileResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_move_file_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_move_file`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesMoveFileArgs {
    /// Path parameter: workspace
    pub workspace: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:moveFile
/// Moves a file (inside a Workspace) to a new location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_move_file_builder()` + `dataform_projects_locations_repositories_workspaces_move_file_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_move_file_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_move_file(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesMoveFileArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MoveFileResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workspaces_move_file_builder(
        client,
        &args.workspace,
    )?;
    dataform_projects_locations_repositories_workspaces_move_file_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:pull
/// Pulls Git commits from the Repository''s remote into a Workspace.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_pull_execute()` to send, or `dataform_projects_locations_repositories_workspaces_pull` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_pull_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:pull",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:pull
/// Pulls Git commits from the Repository''s remote into a Workspace.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_pull_execute()` or `dataform_projects_locations_repositories_workspaces_pull`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_pull_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_pull_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PullGitCommitsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PullGitCommitsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:pull
/// Pulls Git commits from the Repository''s remote into a Workspace.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_pull_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_pull_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_pull()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_pull_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_pull_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PullGitCommitsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_pull_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_pull`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesPullArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:pull
/// Pulls Git commits from the Repository''s remote into a Workspace.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_pull_builder()` + `dataform_projects_locations_repositories_workspaces_pull_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_pull_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_pull(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesPullArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PullGitCommitsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_repositories_workspaces_pull_builder(client, &args.name)?;
    dataform_projects_locations_repositories_workspaces_pull_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:push
/// Pushes Git commits from a Workspace to the Repository''s remote.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_push_execute()` to send, or `dataform_projects_locations_repositories_workspaces_push` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_push_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:push",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:push
/// Pushes Git commits from a Workspace to the Repository''s remote.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_push_execute()` or `dataform_projects_locations_repositories_workspaces_push`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_push_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_push_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PushGitCommitsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PushGitCommitsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:push
/// Pushes Git commits from a Workspace to the Repository''s remote.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_push_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_push_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_push()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_push_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_push_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PushGitCommitsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_push_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_push`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesPushArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:push
/// Pushes Git commits from a Workspace to the Repository''s remote.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_push_builder()` + `dataform_projects_locations_repositories_workspaces_push_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_push_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_push(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesPushArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PushGitCommitsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_repositories_workspaces_push_builder(client, &args.name)?;
    dataform_projects_locations_repositories_workspaces_push_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:queryDirectoryContents
/// Returns the contents of a given Workspace directory.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_query_directory_contents_execute()` to send, or `dataform_projects_locations_repositories_workspaces_query_directory_contents` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_query_directory_contents_builder<R>(
    client: &SimpleHttpClient<R>,
    workspace: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    path: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:queryDirectoryContents",
        workspace,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = path.as_ref() {
        query_parts.push(format!("path={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:queryDirectoryContents
/// Returns the contents of a given Workspace directory.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_query_directory_contents_execute()` or `dataform_projects_locations_repositories_workspaces_query_directory_contents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_query_directory_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_query_directory_contents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryDirectoryContentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryDirectoryContentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:queryDirectoryContents
/// Returns the contents of a given Workspace directory.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_query_directory_contents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_query_directory_contents_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_query_directory_contents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_query_directory_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_query_directory_contents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryDirectoryContentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataform_projects_locations_repositories_workspaces_query_directory_contents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_query_directory_contents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesQueryDirectoryContentsArgs {
    /// Path parameter: workspace
    pub workspace: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: path
    pub path: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:queryDirectoryContents
/// Returns the contents of a given Workspace directory.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_query_directory_contents_builder()` + `dataform_projects_locations_repositories_workspaces_query_directory_contents_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_query_directory_contents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_query_directory_contents(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesQueryDirectoryContentsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryDirectoryContentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_repositories_workspaces_query_directory_contents_builder(
            client,
            &args.workspace,
            &args.pageSize,
            &args.pageToken,
            &args.path,
            &args.view,
        )?;
    dataform_projects_locations_repositories_workspaces_query_directory_contents_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:readFile
/// Returns the contents of a file (inside a Workspace).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_read_file_execute()` to send, or `dataform_projects_locations_repositories_workspaces_read_file` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_read_file_builder<R>(
    client: &SimpleHttpClient<R>,
    workspace: &String,
    path: &Option<Option<String>>,
    revision: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:readFile",
        workspace,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = path.as_ref() {
        query_parts.push(format!("path={}", val));
    }
    if let Some(val) = revision.as_ref() {
        query_parts.push(format!("revision={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:readFile
/// Returns the contents of a file (inside a Workspace).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_read_file_execute()` or `dataform_projects_locations_repositories_workspaces_read_file`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_read_file_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_read_file_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReadFileResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReadFileResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:readFile
/// Returns the contents of a file (inside a Workspace).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_read_file_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_read_file_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_read_file()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_read_file_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_read_file_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReadFileResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_read_file_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_read_file`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesReadFileArgs {
    /// Path parameter: workspace
    pub workspace: String,
    /// Query parameter: path
    pub path: Option<Option<String>>,
    /// Query parameter: revision
    pub revision: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:readFile
/// Returns the contents of a file (inside a Workspace).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_read_file_builder()` + `dataform_projects_locations_repositories_workspaces_read_file_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_read_file_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_read_file(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesReadFileArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReadFileResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workspaces_read_file_builder(
        client,
        &args.workspace,
        &args.path,
        &args.revision,
    )?;
    dataform_projects_locations_repositories_workspaces_read_file_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:removeDirectory
/// Deletes a directory (inside a Workspace) and all of its contents.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_remove_directory_execute()` to send, or `dataform_projects_locations_repositories_workspaces_remove_directory` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_remove_directory_builder<R>(
    client: &SimpleHttpClient<R>,
    workspace: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:removeDirectory",
        workspace,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:removeDirectory
/// Deletes a directory (inside a Workspace) and all of its contents.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_remove_directory_execute()` or `dataform_projects_locations_repositories_workspaces_remove_directory`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_remove_directory_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_remove_directory_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemoveDirectoryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemoveDirectoryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:removeDirectory
/// Deletes a directory (inside a Workspace) and all of its contents.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_remove_directory_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_remove_directory_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_remove_directory()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_remove_directory_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_remove_directory_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemoveDirectoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_remove_directory_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_remove_directory`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesRemoveDirectoryArgs {
    /// Path parameter: workspace
    pub workspace: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:removeDirectory
/// Deletes a directory (inside a Workspace) and all of its contents.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_remove_directory_builder()` + `dataform_projects_locations_repositories_workspaces_remove_directory_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_remove_directory_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_remove_directory(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesRemoveDirectoryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemoveDirectoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workspaces_remove_directory_builder(
        client,
        &args.workspace,
    )?;
    dataform_projects_locations_repositories_workspaces_remove_directory_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:removeFile
/// Deletes a file (inside a Workspace).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_remove_file_execute()` to send, or `dataform_projects_locations_repositories_workspaces_remove_file` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_remove_file_builder<R>(
    client: &SimpleHttpClient<R>,
    workspace: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:removeFile",
        workspace,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:removeFile
/// Deletes a file (inside a Workspace).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_remove_file_execute()` or `dataform_projects_locations_repositories_workspaces_remove_file`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_remove_file_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_remove_file_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemoveFileResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemoveFileResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:removeFile
/// Deletes a file (inside a Workspace).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_remove_file_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_remove_file_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_remove_file()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_remove_file_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_remove_file_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemoveFileResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_remove_file_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_remove_file`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesRemoveFileArgs {
    /// Path parameter: workspace
    pub workspace: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:removeFile
/// Deletes a file (inside a Workspace).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_remove_file_builder()` + `dataform_projects_locations_repositories_workspaces_remove_file_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_remove_file_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_remove_file(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesRemoveFileArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemoveFileResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workspaces_remove_file_builder(
        client,
        &args.workspace,
    )?;
    dataform_projects_locations_repositories_workspaces_remove_file_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:reset
/// Performs a Git reset for uncommitted files in a Workspace.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_reset_execute()` to send, or `dataform_projects_locations_repositories_workspaces_reset` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_reset_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:reset",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:reset
/// Performs a Git reset for uncommitted files in a Workspace.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_reset_execute()` or `dataform_projects_locations_repositories_workspaces_reset`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_reset_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ResetWorkspaceChangesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResetWorkspaceChangesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:reset
/// Performs a Git reset for uncommitted files in a Workspace.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_reset_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_reset_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_reset()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResetWorkspaceChangesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_reset_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_reset`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesResetArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:reset
/// Performs a Git reset for uncommitted files in a Workspace.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_reset_builder()` + `dataform_projects_locations_repositories_workspaces_reset_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_reset_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_reset(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesResetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResetWorkspaceChangesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_repositories_workspaces_reset_builder(client, &args.name)?;
    dataform_projects_locations_repositories_workspaces_reset_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:searchFiles
/// Finds the contents of a given Workspace directory by filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_search_files_execute()` to send, or `dataform_projects_locations_repositories_workspaces_search_files` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_search_files_builder<R>(
    client: &SimpleHttpClient<R>,
    workspace: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:searchFiles",
        workspace,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:searchFiles
/// Finds the contents of a given Workspace directory by filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_search_files_execute()` or `dataform_projects_locations_repositories_workspaces_search_files`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_search_files_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_search_files_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchFilesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchFilesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:searchFiles
/// Finds the contents of a given Workspace directory by filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_search_files_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_search_files_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_search_files()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_search_files_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_search_files_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchFilesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_search_files_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_search_files`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesSearchFilesArgs {
    /// Path parameter: workspace
    pub workspace: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:searchFiles
/// Finds the contents of a given Workspace directory by filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_search_files_builder()` + `dataform_projects_locations_repositories_workspaces_search_files_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_search_files_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_search_files(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesSearchFilesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchFilesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workspaces_search_files_builder(
        client,
        &args.workspace,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    dataform_projects_locations_repositories_workspaces_search_files_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_set_iam_policy_execute()` to send, or `dataform_projects_locations_repositories_workspaces_set_iam_policy` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_set_iam_policy_execute()` or `dataform_projects_locations_repositories_workspaces_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_set_iam_policy_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_set_iam_policy_builder()` + `dataform_projects_locations_repositories_workspaces_set_iam_policy_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_set_iam_policy(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workspaces_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    dataform_projects_locations_repositories_workspaces_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_test_iam_permissions_execute()` to send, or `dataform_projects_locations_repositories_workspaces_test_iam_permissions` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_test_iam_permissions_execute()` or `dataform_projects_locations_repositories_workspaces_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_test_iam_permissions_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataform_projects_locations_repositories_workspaces_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_test_iam_permissions_builder()` + `dataform_projects_locations_repositories_workspaces_test_iam_permissions_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workspaces_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    dataform_projects_locations_repositories_workspaces_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:writeFile
/// Writes to a file (inside a Workspace).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_repositories_workspaces_write_file_execute()` to send, or `dataform_projects_locations_repositories_workspaces_write_file` for simplest API.

pub fn dataform_projects_locations_repositories_workspaces_write_file_builder<R>(
    client: &SimpleHttpClient<R>,
    workspace: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:writeFile",
        workspace,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:writeFile
/// Writes to a file (inside a Workspace).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_repositories_workspaces_write_file_execute()` or `dataform_projects_locations_repositories_workspaces_write_file`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_write_file_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_write_file_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WriteFileResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WriteFileResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:writeFile
/// Writes to a file (inside a Workspace).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_repositories_workspaces_write_file_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_write_file_task()`.
/// For the simplest API, use `dataform_projects_locations_repositories_workspaces_write_file()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_repositories_workspaces_write_file_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_repositories_workspaces_write_file_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WriteFileResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_repositories_workspaces_write_file_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_repositories_workspaces_write_file`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesWorkspacesWriteFileArgs {
    /// Path parameter: workspace
    pub workspace: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/workspaces/{workspacesId}:writeFile
/// Writes to a file (inside a Workspace).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_repositories_workspaces_write_file_builder()` + `dataform_projects_locations_repositories_workspaces_write_file_execute()`.
/// For task-level control, use `dataform_projects_locations_repositories_workspaces_write_file_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_repositories_workspaces_write_file(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsRepositoriesWorkspacesWriteFileArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WriteFileResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_repositories_workspaces_write_file_builder(
        client,
        &args.workspace,
    )?;
    dataform_projects_locations_repositories_workspaces_write_file_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/teamFolders
/// Creates a new TeamFolder in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_team_folders_create_execute()` to send, or `dataform_projects_locations_team_folders_create` for simplest API.

pub fn dataform_projects_locations_team_folders_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/teamFolders",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/teamFolders
/// Creates a new TeamFolder in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_team_folders_create_execute()` or `dataform_projects_locations_team_folders_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TeamFolder>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TeamFolder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/teamFolders
/// Creates a new TeamFolder in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_team_folders_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_team_folders_create_task()`.
/// For the simplest API, use `dataform_projects_locations_team_folders_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_team_folders_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TeamFolder>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_team_folders_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_team_folders_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsTeamFoldersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/teamFolders
/// Creates a new TeamFolder in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_team_folders_create_builder()` + `dataform_projects_locations_team_folders_create_execute()`.
/// For task-level control, use `dataform_projects_locations_team_folders_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_create(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsTeamFoldersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TeamFolder>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_team_folders_create_builder(client, &args.parent)?;
    dataform_projects_locations_team_folders_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}
/// Deletes a single TeamFolder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_team_folders_delete_execute()` to send, or `dataform_projects_locations_team_folders_delete` for simplest API.

pub fn dataform_projects_locations_team_folders_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/teamFolders/{teamFoldersId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}
/// Deletes a single TeamFolder.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_team_folders_delete_execute()` or `dataform_projects_locations_team_folders_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}
/// Deletes a single TeamFolder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_team_folders_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_team_folders_delete_task()`.
/// For the simplest API, use `dataform_projects_locations_team_folders_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_team_folders_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_team_folders_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_team_folders_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsTeamFoldersDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}
/// Deletes a single TeamFolder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_team_folders_delete_builder()` + `dataform_projects_locations_team_folders_delete_execute()`.
/// For task-level control, use `dataform_projects_locations_team_folders_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_delete(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsTeamFoldersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_team_folders_delete_builder(client, &args.name)?;
    dataform_projects_locations_team_folders_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:deleteTree
/// Deletes a TeamFolder with its contents (Folders, Repositories, Workspaces, ReleaseConfigs, and WorkflowConfigs).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_team_folders_delete_tree_execute()` to send, or `dataform_projects_locations_team_folders_delete_tree` for simplest API.

pub fn dataform_projects_locations_team_folders_delete_tree_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/teamFolders/{teamFoldersId}:deleteTree",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:deleteTree
/// Deletes a TeamFolder with its contents (Folders, Repositories, Workspaces, ReleaseConfigs, and WorkflowConfigs).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_team_folders_delete_tree_execute()` or `dataform_projects_locations_team_folders_delete_tree`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_delete_tree_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_delete_tree_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:deleteTree
/// Deletes a TeamFolder with its contents (Folders, Repositories, Workspaces, ReleaseConfigs, and WorkflowConfigs).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_team_folders_delete_tree_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_team_folders_delete_tree_task()`.
/// For the simplest API, use `dataform_projects_locations_team_folders_delete_tree()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_delete_tree_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_team_folders_delete_tree_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_team_folders_delete_tree_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_team_folders_delete_tree`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsTeamFoldersDeleteTreeArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:deleteTree
/// Deletes a TeamFolder with its contents (Folders, Repositories, Workspaces, ReleaseConfigs, and WorkflowConfigs).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_team_folders_delete_tree_builder()` + `dataform_projects_locations_team_folders_delete_tree_execute()`.
/// For task-level control, use `dataform_projects_locations_team_folders_delete_tree_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_delete_tree(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsTeamFoldersDeleteTreeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_team_folders_delete_tree_builder(client, &args.name)?;
    dataform_projects_locations_team_folders_delete_tree_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}
/// Fetches a single TeamFolder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_team_folders_get_execute()` to send, or `dataform_projects_locations_team_folders_get` for simplest API.

pub fn dataform_projects_locations_team_folders_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/teamFolders/{teamFoldersId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}
/// Fetches a single TeamFolder.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_team_folders_get_execute()` or `dataform_projects_locations_team_folders_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TeamFolder>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TeamFolder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}
/// Fetches a single TeamFolder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_team_folders_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_team_folders_get_task()`.
/// For the simplest API, use `dataform_projects_locations_team_folders_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_team_folders_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TeamFolder>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_team_folders_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_team_folders_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsTeamFoldersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}
/// Fetches a single TeamFolder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_team_folders_get_builder()` + `dataform_projects_locations_team_folders_get_execute()`.
/// For task-level control, use `dataform_projects_locations_team_folders_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_get(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsTeamFoldersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TeamFolder>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_team_folders_get_builder(client, &args.name)?;
    dataform_projects_locations_team_folders_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_team_folders_get_iam_policy_execute()` to send, or `dataform_projects_locations_team_folders_get_iam_policy` for simplest API.

pub fn dataform_projects_locations_team_folders_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/teamFolders/{teamFoldersId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_team_folders_get_iam_policy_execute()` or `dataform_projects_locations_team_folders_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_team_folders_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_team_folders_get_iam_policy_task()`.
/// For the simplest API, use `dataform_projects_locations_team_folders_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_team_folders_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_team_folders_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_team_folders_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsTeamFoldersGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_team_folders_get_iam_policy_builder()` + `dataform_projects_locations_team_folders_get_iam_policy_execute()`.
/// For task-level control, use `dataform_projects_locations_team_folders_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_get_iam_policy(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsTeamFoldersGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_team_folders_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    dataform_projects_locations_team_folders_get_iam_policy_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}
/// Updates a single TeamFolder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_team_folders_patch_execute()` to send, or `dataform_projects_locations_team_folders_patch` for simplest API.

pub fn dataform_projects_locations_team_folders_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/teamFolders/{teamFoldersId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}
/// Updates a single TeamFolder.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_team_folders_patch_execute()` or `dataform_projects_locations_team_folders_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TeamFolder>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TeamFolder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}
/// Updates a single TeamFolder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_team_folders_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_team_folders_patch_task()`.
/// For the simplest API, use `dataform_projects_locations_team_folders_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_team_folders_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TeamFolder>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_team_folders_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_team_folders_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsTeamFoldersPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}
/// Updates a single TeamFolder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_team_folders_patch_builder()` + `dataform_projects_locations_team_folders_patch_execute()`.
/// For task-level control, use `dataform_projects_locations_team_folders_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_patch(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsTeamFoldersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TeamFolder>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_team_folders_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    dataform_projects_locations_team_folders_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:queryContents
/// Returns the contents of a given TeamFolder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_team_folders_query_contents_execute()` to send, or `dataform_projects_locations_team_folders_query_contents` for simplest API.

pub fn dataform_projects_locations_team_folders_query_contents_builder<R>(
    client: &SimpleHttpClient<R>,
    teamFolder: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/teamFolders/{teamFoldersId}:queryContents",
        teamFolder,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:queryContents
/// Returns the contents of a given TeamFolder.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_team_folders_query_contents_execute()` or `dataform_projects_locations_team_folders_query_contents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_query_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_query_contents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryTeamFolderContentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryTeamFolderContentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:queryContents
/// Returns the contents of a given TeamFolder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_team_folders_query_contents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_team_folders_query_contents_task()`.
/// For the simplest API, use `dataform_projects_locations_team_folders_query_contents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_query_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_team_folders_query_contents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryTeamFolderContentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_team_folders_query_contents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_team_folders_query_contents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsTeamFoldersQueryContentsArgs {
    /// Path parameter: teamFolder
    pub teamFolder: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:queryContents
/// Returns the contents of a given TeamFolder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_team_folders_query_contents_builder()` + `dataform_projects_locations_team_folders_query_contents_execute()`.
/// For task-level control, use `dataform_projects_locations_team_folders_query_contents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_query_contents(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsTeamFoldersQueryContentsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryTeamFolderContentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_team_folders_query_contents_builder(
        client,
        &args.teamFolder,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dataform_projects_locations_team_folders_query_contents_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/teamFolders:search
/// Returns all TeamFolders in a given location that the caller has access to and match the provided filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_team_folders_search_execute()` to send, or `dataform_projects_locations_team_folders_search` for simplest API.

pub fn dataform_projects_locations_team_folders_search_builder<R>(
    client: &SimpleHttpClient<R>,
    location: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/teamFolders:search",
        location,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/teamFolders:search
/// Returns all TeamFolders in a given location that the caller has access to and match the provided filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_team_folders_search_execute()` or `dataform_projects_locations_team_folders_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchTeamFoldersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchTeamFoldersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/teamFolders:search
/// Returns all TeamFolders in a given location that the caller has access to and match the provided filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_team_folders_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_team_folders_search_task()`.
/// For the simplest API, use `dataform_projects_locations_team_folders_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_team_folders_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchTeamFoldersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_team_folders_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_team_folders_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsTeamFoldersSearchArgs {
    /// Path parameter: location
    pub location: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/teamFolders:search
/// Returns all TeamFolders in a given location that the caller has access to and match the provided filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_team_folders_search_builder()` + `dataform_projects_locations_team_folders_search_execute()`.
/// For task-level control, use `dataform_projects_locations_team_folders_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_search(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsTeamFoldersSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchTeamFoldersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_team_folders_search_builder(
        client,
        &args.location,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dataform_projects_locations_team_folders_search_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_team_folders_set_iam_policy_execute()` to send, or `dataform_projects_locations_team_folders_set_iam_policy` for simplest API.

pub fn dataform_projects_locations_team_folders_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/teamFolders/{teamFoldersId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_team_folders_set_iam_policy_execute()` or `dataform_projects_locations_team_folders_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_team_folders_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_team_folders_set_iam_policy_task()`.
/// For the simplest API, use `dataform_projects_locations_team_folders_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_team_folders_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_team_folders_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_team_folders_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsTeamFoldersSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_team_folders_set_iam_policy_builder()` + `dataform_projects_locations_team_folders_set_iam_policy_execute()`.
/// For task-level control, use `dataform_projects_locations_team_folders_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_set_iam_policy(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsTeamFoldersSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataform_projects_locations_team_folders_set_iam_policy_builder(client, &args.resource)?;
    dataform_projects_locations_team_folders_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataform_projects_locations_team_folders_test_iam_permissions_execute()` to send, or `dataform_projects_locations_team_folders_test_iam_permissions` for simplest API.

pub fn dataform_projects_locations_team_folders_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{}/locations/{locationsId}/teamFolders/{teamFoldersId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataform_projects_locations_team_folders_test_iam_permissions_execute()` or `dataform_projects_locations_team_folders_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataform_projects_locations_team_folders_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataform_projects_locations_team_folders_test_iam_permissions_task()`.
/// For the simplest API, use `dataform_projects_locations_team_folders_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataform_projects_locations_team_folders_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataform_projects_locations_team_folders_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataform_projects_locations_team_folders_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataform_projects_locations_team_folders_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsTeamFoldersTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/teamFolders/{teamFoldersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataform_projects_locations_team_folders_test_iam_permissions_builder()` + `dataform_projects_locations_team_folders_test_iam_permissions_execute()`.
/// For task-level control, use `dataform_projects_locations_team_folders_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataform_projects_locations_team_folders_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &DataformProjectsLocationsTeamFoldersTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataform_projects_locations_team_folders_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    dataform_projects_locations_team_folders_test_iam_permissions_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with DataformProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &DataformProjectsLocationsGetArgs) -> String {
        format!("gcp::dataform::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Config
// =============================================================================

/// ResourceIdentifier implementation for Config with DataformProjectsLocationsGetConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsGetConfigArgs> for Config {
    fn generate_resource_id(&self, input: &DataformProjectsLocationsGetConfigArgs) -> String {
        format!("gcp::dataform::Config/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Config"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with DataformProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &DataformProjectsLocationsListArgs) -> String {
        format!("gcp::dataform::ListLocationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for QueryUserRootContentsResponse
// =============================================================================

/// ResourceIdentifier implementation for QueryUserRootContentsResponse with DataformProjectsLocationsQueryUserRootContentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsQueryUserRootContentsArgs>
    for QueryUserRootContentsResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsQueryUserRootContentsArgs,
    ) -> String {
        format!(
            "gcp::dataform::QueryUserRootContentsResponse/{}",
            input.location
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::QueryUserRootContentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Config
// =============================================================================

/// ResourceIdentifier implementation for Config with DataformProjectsLocationsUpdateConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsUpdateConfigArgs> for Config {
    fn generate_resource_id(&self, input: &DataformProjectsLocationsUpdateConfigArgs) -> String {
        format!("gcp::dataform::Config/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Config"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Folder
// =============================================================================

/// ResourceIdentifier implementation for Folder with DataformProjectsLocationsFoldersCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsFoldersCreateArgs> for Folder {
    fn generate_resource_id(&self, input: &DataformProjectsLocationsFoldersCreateArgs) -> String {
        format!("gcp::dataform::Folder/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Folder"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DataformProjectsLocationsFoldersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsFoldersDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &DataformProjectsLocationsFoldersDeleteArgs) -> String {
        format!("gcp::dataform::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with DataformProjectsLocationsFoldersDeleteTreeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsFoldersDeleteTreeArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsFoldersDeleteTreeArgs,
    ) -> String {
        format!("gcp::dataform::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Folder
// =============================================================================

/// ResourceIdentifier implementation for Folder with DataformProjectsLocationsFoldersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsFoldersGetArgs> for Folder {
    fn generate_resource_id(&self, input: &DataformProjectsLocationsFoldersGetArgs) -> String {
        format!("gcp::dataform::Folder/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Folder"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with DataformProjectsLocationsFoldersGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsFoldersGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsFoldersGetIamPolicyArgs,
    ) -> String {
        format!("gcp::dataform::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with DataformProjectsLocationsFoldersMoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsFoldersMoveArgs> for Operation {
    fn generate_resource_id(&self, input: &DataformProjectsLocationsFoldersMoveArgs) -> String {
        format!("gcp::dataform::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Folder
// =============================================================================

/// ResourceIdentifier implementation for Folder with DataformProjectsLocationsFoldersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsFoldersPatchArgs> for Folder {
    fn generate_resource_id(&self, input: &DataformProjectsLocationsFoldersPatchArgs) -> String {
        format!("gcp::dataform::Folder/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Folder"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for QueryFolderContentsResponse
// =============================================================================

/// ResourceIdentifier implementation for QueryFolderContentsResponse with DataformProjectsLocationsFoldersQueryFolderContentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsFoldersQueryFolderContentsArgs>
    for QueryFolderContentsResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsFoldersQueryFolderContentsArgs,
    ) -> String {
        format!(
            "gcp::dataform::QueryFolderContentsResponse/{}",
            input.folder
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::QueryFolderContentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with DataformProjectsLocationsFoldersSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsFoldersSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsFoldersSetIamPolicyArgs,
    ) -> String {
        format!("gcp::dataform::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with DataformProjectsLocationsFoldersTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsFoldersTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsFoldersTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::dataform::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DataformProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::dataform::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DataformProjectsLocationsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsOperationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsOperationsDeleteArgs,
    ) -> String {
        format!("gcp::dataform::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with DataformProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &DataformProjectsLocationsOperationsGetArgs) -> String {
        format!("gcp::dataform::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with DataformProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsOperationsListArgs> for ListOperationsResponse {
    fn generate_resource_id(&self, input: &DataformProjectsLocationsOperationsListArgs) -> String {
        format!("gcp::dataform::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CommitRepositoryChangesResponse
// =============================================================================

/// ResourceIdentifier implementation for CommitRepositoryChangesResponse with DataformProjectsLocationsRepositoriesCommitArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesCommitArgs>
    for CommitRepositoryChangesResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesCommitArgs,
    ) -> String {
        format!(
            "gcp::dataform::CommitRepositoryChangesResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::CommitRepositoryChangesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ComputeRepositoryAccessTokenStatusResponse
// =============================================================================

/// ResourceIdentifier implementation for ComputeRepositoryAccessTokenStatusResponse with DataformProjectsLocationsRepositoriesComputeAccessTokenStatusArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesComputeAccessTokenStatusArgs>
    for ComputeRepositoryAccessTokenStatusResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesComputeAccessTokenStatusArgs,
    ) -> String {
        format!(
            "gcp::dataform::ComputeRepositoryAccessTokenStatusResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::ComputeRepositoryAccessTokenStatusResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Repository
// =============================================================================

/// ResourceIdentifier implementation for Repository with DataformProjectsLocationsRepositoriesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesCreateArgs> for Repository {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesCreateArgs,
    ) -> String {
        format!("gcp::dataform::Repository/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Repository"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DataformProjectsLocationsRepositoriesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesDeleteArgs,
    ) -> String {
        format!("gcp::dataform::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FetchRepositoryHistoryResponse
// =============================================================================

/// ResourceIdentifier implementation for FetchRepositoryHistoryResponse with DataformProjectsLocationsRepositoriesFetchHistoryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesFetchHistoryArgs>
    for FetchRepositoryHistoryResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesFetchHistoryArgs,
    ) -> String {
        format!(
            "gcp::dataform::FetchRepositoryHistoryResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::FetchRepositoryHistoryResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FetchRemoteBranchesResponse
// =============================================================================

/// ResourceIdentifier implementation for FetchRemoteBranchesResponse with DataformProjectsLocationsRepositoriesFetchRemoteBranchesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesFetchRemoteBranchesArgs>
    for FetchRemoteBranchesResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesFetchRemoteBranchesArgs,
    ) -> String {
        format!("gcp::dataform::FetchRemoteBranchesResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::FetchRemoteBranchesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Repository
// =============================================================================

/// ResourceIdentifier implementation for Repository with DataformProjectsLocationsRepositoriesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesGetArgs> for Repository {
    fn generate_resource_id(&self, input: &DataformProjectsLocationsRepositoriesGetArgs) -> String {
        format!("gcp::dataform::Repository/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Repository"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with DataformProjectsLocationsRepositoriesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::dataform::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListRepositoriesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListRepositoriesResponse with DataformProjectsLocationsRepositoriesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesListArgs>
    for ListRepositoriesResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesListArgs,
    ) -> String {
        format!("gcp::dataform::ListRepositoriesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::ListRepositoriesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with DataformProjectsLocationsRepositoriesMoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesMoveArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesMoveArgs,
    ) -> String {
        format!("gcp::dataform::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Repository
// =============================================================================

/// ResourceIdentifier implementation for Repository with DataformProjectsLocationsRepositoriesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesPatchArgs> for Repository {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesPatchArgs,
    ) -> String {
        format!("gcp::dataform::Repository/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Repository"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for QueryRepositoryDirectoryContentsResponse
// =============================================================================

/// ResourceIdentifier implementation for QueryRepositoryDirectoryContentsResponse with DataformProjectsLocationsRepositoriesQueryDirectoryContentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesQueryDirectoryContentsArgs>
    for QueryRepositoryDirectoryContentsResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesQueryDirectoryContentsArgs,
    ) -> String {
        format!(
            "gcp::dataform::QueryRepositoryDirectoryContentsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::QueryRepositoryDirectoryContentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReadRepositoryFileResponse
// =============================================================================

/// ResourceIdentifier implementation for ReadRepositoryFileResponse with DataformProjectsLocationsRepositoriesReadFileArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesReadFileArgs>
    for ReadRepositoryFileResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesReadFileArgs,
    ) -> String {
        format!("gcp::dataform::ReadRepositoryFileResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::ReadRepositoryFileResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with DataformProjectsLocationsRepositoriesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::dataform::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with DataformProjectsLocationsRepositoriesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::dataform::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CompilationResult
// =============================================================================

/// ResourceIdentifier implementation for CompilationResult with DataformProjectsLocationsRepositoriesCompilationResultsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesCompilationResultsCreateArgs>
    for CompilationResult
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesCompilationResultsCreateArgs,
    ) -> String {
        format!("gcp::dataform::CompilationResult/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::CompilationResult"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CompilationResult
// =============================================================================

/// ResourceIdentifier implementation for CompilationResult with DataformProjectsLocationsRepositoriesCompilationResultsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesCompilationResultsGetArgs>
    for CompilationResult
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesCompilationResultsGetArgs,
    ) -> String {
        format!("gcp::dataform::CompilationResult/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::CompilationResult"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCompilationResultsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCompilationResultsResponse with DataformProjectsLocationsRepositoriesCompilationResultsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesCompilationResultsListArgs>
    for ListCompilationResultsResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesCompilationResultsListArgs,
    ) -> String {
        format!(
            "gcp::dataform::ListCompilationResultsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::ListCompilationResultsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for QueryCompilationResultActionsResponse
// =============================================================================

/// ResourceIdentifier implementation for QueryCompilationResultActionsResponse with DataformProjectsLocationsRepositoriesCompilationResultsQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesCompilationResultsQueryArgs>
    for QueryCompilationResultActionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesCompilationResultsQueryArgs,
    ) -> String {
        format!(
            "gcp::dataform::QueryCompilationResultActionsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::QueryCompilationResultActionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReleaseConfig
// =============================================================================

/// ResourceIdentifier implementation for ReleaseConfig with DataformProjectsLocationsRepositoriesReleaseConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesReleaseConfigsCreateArgs>
    for ReleaseConfig
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesReleaseConfigsCreateArgs,
    ) -> String {
        format!("gcp::dataform::ReleaseConfig/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::ReleaseConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DataformProjectsLocationsRepositoriesReleaseConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesReleaseConfigsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesReleaseConfigsDeleteArgs,
    ) -> String {
        format!("gcp::dataform::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReleaseConfig
// =============================================================================

/// ResourceIdentifier implementation for ReleaseConfig with DataformProjectsLocationsRepositoriesReleaseConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesReleaseConfigsGetArgs>
    for ReleaseConfig
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesReleaseConfigsGetArgs,
    ) -> String {
        format!("gcp::dataform::ReleaseConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::ReleaseConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListReleaseConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListReleaseConfigsResponse with DataformProjectsLocationsRepositoriesReleaseConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesReleaseConfigsListArgs>
    for ListReleaseConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesReleaseConfigsListArgs,
    ) -> String {
        format!("gcp::dataform::ListReleaseConfigsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::ListReleaseConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReleaseConfig
// =============================================================================

/// ResourceIdentifier implementation for ReleaseConfig with DataformProjectsLocationsRepositoriesReleaseConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesReleaseConfigsPatchArgs>
    for ReleaseConfig
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesReleaseConfigsPatchArgs,
    ) -> String {
        format!("gcp::dataform::ReleaseConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::ReleaseConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WorkflowConfig
// =============================================================================

/// ResourceIdentifier implementation for WorkflowConfig with DataformProjectsLocationsRepositoriesWorkflowConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkflowConfigsCreateArgs>
    for WorkflowConfig
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkflowConfigsCreateArgs,
    ) -> String {
        format!("gcp::dataform::WorkflowConfig/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::WorkflowConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DataformProjectsLocationsRepositoriesWorkflowConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkflowConfigsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkflowConfigsDeleteArgs,
    ) -> String {
        format!("gcp::dataform::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WorkflowConfig
// =============================================================================

/// ResourceIdentifier implementation for WorkflowConfig with DataformProjectsLocationsRepositoriesWorkflowConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkflowConfigsGetArgs>
    for WorkflowConfig
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkflowConfigsGetArgs,
    ) -> String {
        format!("gcp::dataform::WorkflowConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::WorkflowConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListWorkflowConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListWorkflowConfigsResponse with DataformProjectsLocationsRepositoriesWorkflowConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkflowConfigsListArgs>
    for ListWorkflowConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkflowConfigsListArgs,
    ) -> String {
        format!(
            "gcp::dataform::ListWorkflowConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::ListWorkflowConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WorkflowConfig
// =============================================================================

/// ResourceIdentifier implementation for WorkflowConfig with DataformProjectsLocationsRepositoriesWorkflowConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkflowConfigsPatchArgs>
    for WorkflowConfig
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkflowConfigsPatchArgs,
    ) -> String {
        format!("gcp::dataform::WorkflowConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::WorkflowConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CancelWorkflowInvocationResponse
// =============================================================================

/// ResourceIdentifier implementation for CancelWorkflowInvocationResponse with DataformProjectsLocationsRepositoriesWorkflowInvocationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkflowInvocationsCancelArgs>
    for CancelWorkflowInvocationResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkflowInvocationsCancelArgs,
    ) -> String {
        format!(
            "gcp::dataform::CancelWorkflowInvocationResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::CancelWorkflowInvocationResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WorkflowInvocation
// =============================================================================

/// ResourceIdentifier implementation for WorkflowInvocation with DataformProjectsLocationsRepositoriesWorkflowInvocationsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkflowInvocationsCreateArgs>
    for WorkflowInvocation
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkflowInvocationsCreateArgs,
    ) -> String {
        format!("gcp::dataform::WorkflowInvocation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::WorkflowInvocation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DataformProjectsLocationsRepositoriesWorkflowInvocationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkflowInvocationsDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkflowInvocationsDeleteArgs,
    ) -> String {
        format!("gcp::dataform::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WorkflowInvocation
// =============================================================================

/// ResourceIdentifier implementation for WorkflowInvocation with DataformProjectsLocationsRepositoriesWorkflowInvocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkflowInvocationsGetArgs>
    for WorkflowInvocation
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkflowInvocationsGetArgs,
    ) -> String {
        format!("gcp::dataform::WorkflowInvocation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::WorkflowInvocation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListWorkflowInvocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListWorkflowInvocationsResponse with DataformProjectsLocationsRepositoriesWorkflowInvocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkflowInvocationsListArgs>
    for ListWorkflowInvocationsResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkflowInvocationsListArgs,
    ) -> String {
        format!(
            "gcp::dataform::ListWorkflowInvocationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::ListWorkflowInvocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for QueryWorkflowInvocationActionsResponse
// =============================================================================

/// ResourceIdentifier implementation for QueryWorkflowInvocationActionsResponse with DataformProjectsLocationsRepositoriesWorkflowInvocationsQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkflowInvocationsQueryArgs>
    for QueryWorkflowInvocationActionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkflowInvocationsQueryArgs,
    ) -> String {
        format!(
            "gcp::dataform::QueryWorkflowInvocationActionsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::QueryWorkflowInvocationActionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CommitWorkspaceChangesResponse
// =============================================================================

/// ResourceIdentifier implementation for CommitWorkspaceChangesResponse with DataformProjectsLocationsRepositoriesWorkspacesCommitArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesCommitArgs>
    for CommitWorkspaceChangesResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesCommitArgs,
    ) -> String {
        format!(
            "gcp::dataform::CommitWorkspaceChangesResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::CommitWorkspaceChangesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Workspace
// =============================================================================

/// ResourceIdentifier implementation for Workspace with DataformProjectsLocationsRepositoriesWorkspacesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesCreateArgs> for Workspace {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesCreateArgs,
    ) -> String {
        format!("gcp::dataform::Workspace/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Workspace"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DataformProjectsLocationsRepositoriesWorkspacesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesDeleteArgs,
    ) -> String {
        format!("gcp::dataform::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FetchFileDiffResponse
// =============================================================================

/// ResourceIdentifier implementation for FetchFileDiffResponse with DataformProjectsLocationsRepositoriesWorkspacesFetchFileDiffArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesFetchFileDiffArgs>
    for FetchFileDiffResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesFetchFileDiffArgs,
    ) -> String {
        format!("gcp::dataform::FetchFileDiffResponse/{}", input.workspace)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::FetchFileDiffResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FetchFileGitStatusesResponse
// =============================================================================

/// ResourceIdentifier implementation for FetchFileGitStatusesResponse with DataformProjectsLocationsRepositoriesWorkspacesFetchFileGitStatusesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesFetchFileGitStatusesArgs>
    for FetchFileGitStatusesResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesFetchFileGitStatusesArgs,
    ) -> String {
        format!("gcp::dataform::FetchFileGitStatusesResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::FetchFileGitStatusesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FetchGitAheadBehindResponse
// =============================================================================

/// ResourceIdentifier implementation for FetchGitAheadBehindResponse with DataformProjectsLocationsRepositoriesWorkspacesFetchGitAheadBehindArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesFetchGitAheadBehindArgs>
    for FetchGitAheadBehindResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesFetchGitAheadBehindArgs,
    ) -> String {
        format!("gcp::dataform::FetchGitAheadBehindResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::FetchGitAheadBehindResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Workspace
// =============================================================================

/// ResourceIdentifier implementation for Workspace with DataformProjectsLocationsRepositoriesWorkspacesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesGetArgs> for Workspace {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesGetArgs,
    ) -> String {
        format!("gcp::dataform::Workspace/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Workspace"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with DataformProjectsLocationsRepositoriesWorkspacesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesGetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::dataform::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstallNpmPackagesResponse
// =============================================================================

/// ResourceIdentifier implementation for InstallNpmPackagesResponse with DataformProjectsLocationsRepositoriesWorkspacesInstallNpmPackagesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesInstallNpmPackagesArgs>
    for InstallNpmPackagesResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesInstallNpmPackagesArgs,
    ) -> String {
        format!(
            "gcp::dataform::InstallNpmPackagesResponse/{}",
            input.workspace
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::InstallNpmPackagesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListWorkspacesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListWorkspacesResponse with DataformProjectsLocationsRepositoriesWorkspacesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesListArgs>
    for ListWorkspacesResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesListArgs,
    ) -> String {
        format!("gcp::dataform::ListWorkspacesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::ListWorkspacesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MakeDirectoryResponse
// =============================================================================

/// ResourceIdentifier implementation for MakeDirectoryResponse with DataformProjectsLocationsRepositoriesWorkspacesMakeDirectoryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesMakeDirectoryArgs>
    for MakeDirectoryResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesMakeDirectoryArgs,
    ) -> String {
        format!("gcp::dataform::MakeDirectoryResponse/{}", input.workspace)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::MakeDirectoryResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MoveDirectoryResponse
// =============================================================================

/// ResourceIdentifier implementation for MoveDirectoryResponse with DataformProjectsLocationsRepositoriesWorkspacesMoveDirectoryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesMoveDirectoryArgs>
    for MoveDirectoryResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesMoveDirectoryArgs,
    ) -> String {
        format!("gcp::dataform::MoveDirectoryResponse/{}", input.workspace)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::MoveDirectoryResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MoveFileResponse
// =============================================================================

/// ResourceIdentifier implementation for MoveFileResponse with DataformProjectsLocationsRepositoriesWorkspacesMoveFileArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesMoveFileArgs>
    for MoveFileResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesMoveFileArgs,
    ) -> String {
        format!("gcp::dataform::MoveFileResponse/{}", input.workspace)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::MoveFileResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PullGitCommitsResponse
// =============================================================================

/// ResourceIdentifier implementation for PullGitCommitsResponse with DataformProjectsLocationsRepositoriesWorkspacesPullArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesPullArgs>
    for PullGitCommitsResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesPullArgs,
    ) -> String {
        format!("gcp::dataform::PullGitCommitsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::PullGitCommitsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PushGitCommitsResponse
// =============================================================================

/// ResourceIdentifier implementation for PushGitCommitsResponse with DataformProjectsLocationsRepositoriesWorkspacesPushArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesPushArgs>
    for PushGitCommitsResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesPushArgs,
    ) -> String {
        format!("gcp::dataform::PushGitCommitsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::PushGitCommitsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for QueryDirectoryContentsResponse
// =============================================================================

/// ResourceIdentifier implementation for QueryDirectoryContentsResponse with DataformProjectsLocationsRepositoriesWorkspacesQueryDirectoryContentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesQueryDirectoryContentsArgs>
    for QueryDirectoryContentsResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesQueryDirectoryContentsArgs,
    ) -> String {
        format!(
            "gcp::dataform::QueryDirectoryContentsResponse/{}",
            input.workspace
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::QueryDirectoryContentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReadFileResponse
// =============================================================================

/// ResourceIdentifier implementation for ReadFileResponse with DataformProjectsLocationsRepositoriesWorkspacesReadFileArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesReadFileArgs>
    for ReadFileResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesReadFileArgs,
    ) -> String {
        format!("gcp::dataform::ReadFileResponse/{}", input.workspace)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::ReadFileResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RemoveDirectoryResponse
// =============================================================================

/// ResourceIdentifier implementation for RemoveDirectoryResponse with DataformProjectsLocationsRepositoriesWorkspacesRemoveDirectoryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesRemoveDirectoryArgs>
    for RemoveDirectoryResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesRemoveDirectoryArgs,
    ) -> String {
        format!("gcp::dataform::RemoveDirectoryResponse/{}", input.workspace)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::RemoveDirectoryResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RemoveFileResponse
// =============================================================================

/// ResourceIdentifier implementation for RemoveFileResponse with DataformProjectsLocationsRepositoriesWorkspacesRemoveFileArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesRemoveFileArgs>
    for RemoveFileResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesRemoveFileArgs,
    ) -> String {
        format!("gcp::dataform::RemoveFileResponse/{}", input.workspace)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::RemoveFileResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ResetWorkspaceChangesResponse
// =============================================================================

/// ResourceIdentifier implementation for ResetWorkspaceChangesResponse with DataformProjectsLocationsRepositoriesWorkspacesResetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesResetArgs>
    for ResetWorkspaceChangesResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesResetArgs,
    ) -> String {
        format!(
            "gcp::dataform::ResetWorkspaceChangesResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::ResetWorkspaceChangesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SearchFilesResponse
// =============================================================================

/// ResourceIdentifier implementation for SearchFilesResponse with DataformProjectsLocationsRepositoriesWorkspacesSearchFilesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesSearchFilesArgs>
    for SearchFilesResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesSearchFilesArgs,
    ) -> String {
        format!("gcp::dataform::SearchFilesResponse/{}", input.workspace)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::SearchFilesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with DataformProjectsLocationsRepositoriesWorkspacesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesSetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::dataform::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with DataformProjectsLocationsRepositoriesWorkspacesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::dataform::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WriteFileResponse
// =============================================================================

/// ResourceIdentifier implementation for WriteFileResponse with DataformProjectsLocationsRepositoriesWorkspacesWriteFileArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsRepositoriesWorkspacesWriteFileArgs>
    for WriteFileResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsRepositoriesWorkspacesWriteFileArgs,
    ) -> String {
        format!("gcp::dataform::WriteFileResponse/{}", input.workspace)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::WriteFileResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TeamFolder
// =============================================================================

/// ResourceIdentifier implementation for TeamFolder with DataformProjectsLocationsTeamFoldersCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsTeamFoldersCreateArgs> for TeamFolder {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsTeamFoldersCreateArgs,
    ) -> String {
        format!("gcp::dataform::TeamFolder/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::TeamFolder"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DataformProjectsLocationsTeamFoldersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsTeamFoldersDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsTeamFoldersDeleteArgs,
    ) -> String {
        format!("gcp::dataform::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with DataformProjectsLocationsTeamFoldersDeleteTreeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsTeamFoldersDeleteTreeArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsTeamFoldersDeleteTreeArgs,
    ) -> String {
        format!("gcp::dataform::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TeamFolder
// =============================================================================

/// ResourceIdentifier implementation for TeamFolder with DataformProjectsLocationsTeamFoldersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsTeamFoldersGetArgs> for TeamFolder {
    fn generate_resource_id(&self, input: &DataformProjectsLocationsTeamFoldersGetArgs) -> String {
        format!("gcp::dataform::TeamFolder/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::TeamFolder"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with DataformProjectsLocationsTeamFoldersGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsTeamFoldersGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsTeamFoldersGetIamPolicyArgs,
    ) -> String {
        format!("gcp::dataform::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TeamFolder
// =============================================================================

/// ResourceIdentifier implementation for TeamFolder with DataformProjectsLocationsTeamFoldersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsTeamFoldersPatchArgs> for TeamFolder {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsTeamFoldersPatchArgs,
    ) -> String {
        format!("gcp::dataform::TeamFolder/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::TeamFolder"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for QueryTeamFolderContentsResponse
// =============================================================================

/// ResourceIdentifier implementation for QueryTeamFolderContentsResponse with DataformProjectsLocationsTeamFoldersQueryContentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsTeamFoldersQueryContentsArgs>
    for QueryTeamFolderContentsResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsTeamFoldersQueryContentsArgs,
    ) -> String {
        format!(
            "gcp::dataform::QueryTeamFolderContentsResponse/{}",
            input.teamFolder
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::QueryTeamFolderContentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SearchTeamFoldersResponse
// =============================================================================

/// ResourceIdentifier implementation for SearchTeamFoldersResponse with DataformProjectsLocationsTeamFoldersSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsTeamFoldersSearchArgs>
    for SearchTeamFoldersResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsTeamFoldersSearchArgs,
    ) -> String {
        format!(
            "gcp::dataform::SearchTeamFoldersResponse/{}",
            input.location
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::SearchTeamFoldersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with DataformProjectsLocationsTeamFoldersSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsTeamFoldersSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsTeamFoldersSetIamPolicyArgs,
    ) -> String {
        format!("gcp::dataform::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with DataformProjectsLocationsTeamFoldersTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataformProjectsLocationsTeamFoldersTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DataformProjectsLocationsTeamFoldersTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::dataform::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataform::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
