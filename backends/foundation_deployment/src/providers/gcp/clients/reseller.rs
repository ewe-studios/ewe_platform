//! Auto-generated API clients for gcp/reseller.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET apps/reseller/v1/customers/{customerId}
/// Gets a customer account. Use this operation to see a customer account already in your reseller management, or to see the minimal account information for an existing customer that you do not manage. For more information about the API response for existing customers, see [retrieving a customer account](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#get_customer>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reseller_customers_get_execute()` to send, or `reseller_customers_get` for simplest API.

pub fn reseller_customers_get_builder(
    client: &SimpleHttpClient,
    customerId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://reseller.googleapis.com/apps/reseller/v1/customers/{}",
        customerId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET apps/reseller/v1/customers/{customerId}
/// Gets a customer account. Use this operation to see a customer account already in your reseller management, or to see the minimal account information for an existing customer that you do not manage. For more information about the API response for existing customers, see [retrieving a customer account](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#get_customer>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reseller_customers_get_execute()` or `reseller_customers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_customers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_customers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Customer>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Customer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET apps/reseller/v1/customers/{customerId}
/// Gets a customer account. Use this operation to see a customer account already in your reseller management, or to see the minimal account information for an existing customer that you do not manage. For more information about the API response for existing customers, see [retrieving a customer account](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#get_customer>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reseller_customers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reseller_customers_get_task()`.
/// For the simplest API, use `reseller_customers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_customers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reseller_customers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = reseller_customers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reseller_customers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResellerCustomersGetArgs {
    /// Path parameter: customerId
    pub customerId: String,
}

/// GET apps/reseller/v1/customers/{customerId}
/// Gets a customer account. Use this operation to see a customer account already in your reseller management, or to see the minimal account information for an existing customer that you do not manage. For more information about the API response for existing customers, see [retrieving a customer account](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#get_customer>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reseller_customers_get_builder()` + `reseller_customers_get_execute()`.
/// For task-level control, use `reseller_customers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_customers_get(
    client: &SimpleHttpClient,
    args: &ResellerCustomersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = reseller_customers_get_builder(client, &args.customerId)?;
    reseller_customers_get_execute(builder)
}

/// POST apps/reseller/v1/customers
/// Orders a new customer's account. Before ordering a new customer account, establish whether the customer account already exists using the [customers.get](<https://developers.google.`com/workspace/admin/reseller/v1/reference/customers/get`>) If the customer account exists as a direct Google account or as a resold customer account from another reseller, use the `customerAuthToken`\ as described in [order a resold account for an existing customer](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#create_existing_customer>). For more information about ordering a new customer account, see [order a new customer account](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#create_customer>). After creating a new customer account, you must provision a user as an administrator. The customer's administrator is required to sign in to the Admin console and sign the G Suite via Reseller agreement to activate the account. Resellers are prohibited from signing the G Suite via Reseller agreement on the customer's behalf. For more information, see [order a new customer account](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#tos>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reseller_customers_insert_execute()` to send, or `reseller_customers_insert` for simplest API.

pub fn reseller_customers_insert_builder(
    client: &SimpleHttpClient,
    customerAuthToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://reseller.googleapis.com/apps/reseller/v1/customers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customerAuthToken.as_ref() {
        query_parts.push(format!("customerAuthToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST apps/reseller/v1/customers
/// Orders a new customer's account. Before ordering a new customer account, establish whether the customer account already exists using the [customers.get](<https://developers.google.`com/workspace/admin/reseller/v1/reference/customers/get`>) If the customer account exists as a direct Google account or as a resold customer account from another reseller, use the `customerAuthToken`\ as described in [order a resold account for an existing customer](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#create_existing_customer>). For more information about ordering a new customer account, see [order a new customer account](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#create_customer>). After creating a new customer account, you must provision a user as an administrator. The customer's administrator is required to sign in to the Admin console and sign the G Suite via Reseller agreement to activate the account. Resellers are prohibited from signing the G Suite via Reseller agreement on the customer's behalf. For more information, see [order a new customer account](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#tos>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reseller_customers_insert_execute()` or `reseller_customers_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_customers_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_customers_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Customer>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Customer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST apps/reseller/v1/customers
/// Orders a new customer's account. Before ordering a new customer account, establish whether the customer account already exists using the [customers.get](<https://developers.google.`com/workspace/admin/reseller/v1/reference/customers/get`>) If the customer account exists as a direct Google account or as a resold customer account from another reseller, use the `customerAuthToken`\ as described in [order a resold account for an existing customer](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#create_existing_customer>). For more information about ordering a new customer account, see [order a new customer account](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#create_customer>). After creating a new customer account, you must provision a user as an administrator. The customer's administrator is required to sign in to the Admin console and sign the G Suite via Reseller agreement to activate the account. Resellers are prohibited from signing the G Suite via Reseller agreement on the customer's behalf. For more information, see [order a new customer account](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#tos>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reseller_customers_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reseller_customers_insert_task()`.
/// For the simplest API, use `reseller_customers_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_customers_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reseller_customers_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = reseller_customers_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reseller_customers_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResellerCustomersInsertArgs {
    /// Query parameter: customerAuthToken
    pub customerAuthToken: Option<Option<String>>,
}

/// POST apps/reseller/v1/customers
/// Orders a new customer's account. Before ordering a new customer account, establish whether the customer account already exists using the [customers.get](<https://developers.google.`com/workspace/admin/reseller/v1/reference/customers/get`>) If the customer account exists as a direct Google account or as a resold customer account from another reseller, use the `customerAuthToken`\ as described in [order a resold account for an existing customer](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#create_existing_customer>). For more information about ordering a new customer account, see [order a new customer account](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#create_customer>). After creating a new customer account, you must provision a user as an administrator. The customer's administrator is required to sign in to the Admin console and sign the G Suite via Reseller agreement to activate the account. Resellers are prohibited from signing the G Suite via Reseller agreement on the customer's behalf. For more information, see [order a new customer account](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#tos>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reseller_customers_insert_builder()` + `reseller_customers_insert_execute()`.
/// For task-level control, use `reseller_customers_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_customers_insert(
    client: &SimpleHttpClient,
    args: &ResellerCustomersInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = reseller_customers_insert_builder(client, &args.customerAuthToken)?;
    reseller_customers_insert_execute(builder)
}

/// PATCH apps/reseller/v1/customers/{customerId}
/// Updates a customer account's settings. This method supports patch semantics. You cannot update `customerType` via the Reseller API, but a "team" customer can verify their domain and become `customerType` = "domain". For more information, see [Verify your domain to unlock Essentials features](<https://support.google.`com/a/answer/9122284`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reseller_customers_patch_execute()` to send, or `reseller_customers_patch` for simplest API.

pub fn reseller_customers_patch_builder(
    client: &SimpleHttpClient,
    customerId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://reseller.googleapis.com/apps/reseller/v1/customers/{}",
        customerId,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH apps/reseller/v1/customers/{customerId}
/// Updates a customer account's settings. This method supports patch semantics. You cannot update `customerType` via the Reseller API, but a "team" customer can verify their domain and become `customerType` = "domain". For more information, see [Verify your domain to unlock Essentials features](<https://support.google.`com/a/answer/9122284`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reseller_customers_patch_execute()` or `reseller_customers_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_customers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_customers_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Customer>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Customer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH apps/reseller/v1/customers/{customerId}
/// Updates a customer account's settings. This method supports patch semantics. You cannot update `customerType` via the Reseller API, but a "team" customer can verify their domain and become `customerType` = "domain". For more information, see [Verify your domain to unlock Essentials features](<https://support.google.`com/a/answer/9122284`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reseller_customers_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reseller_customers_patch_task()`.
/// For the simplest API, use `reseller_customers_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_customers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reseller_customers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = reseller_customers_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reseller_customers_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResellerCustomersPatchArgs {
    /// Path parameter: customerId
    pub customerId: String,
}

/// PATCH apps/reseller/v1/customers/{customerId}
/// Updates a customer account's settings. This method supports patch semantics. You cannot update `customerType` via the Reseller API, but a "team" customer can verify their domain and become `customerType` = "domain". For more information, see [Verify your domain to unlock Essentials features](<https://support.google.`com/a/answer/9122284`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reseller_customers_patch_builder()` + `reseller_customers_patch_execute()`.
/// For task-level control, use `reseller_customers_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_customers_patch(
    client: &SimpleHttpClient,
    args: &ResellerCustomersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = reseller_customers_patch_builder(client, &args.customerId)?;
    reseller_customers_patch_execute(builder)
}

/// PUT apps/reseller/v1/customers/{customerId}
/// Updates a customer account's settings. You cannot update `customerType` via the Reseller API, but a "team" customer can verify their domain and become `customerType` = "domain". For more information, see [update a customer's settings](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#update_customer>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reseller_customers_update_execute()` to send, or `reseller_customers_update` for simplest API.

pub fn reseller_customers_update_builder(
    client: &SimpleHttpClient,
    customerId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://reseller.googleapis.com/apps/reseller/v1/customers/{}",
        customerId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT apps/reseller/v1/customers/{customerId}
/// Updates a customer account's settings. You cannot update `customerType` via the Reseller API, but a "team" customer can verify their domain and become `customerType` = "domain". For more information, see [update a customer's settings](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#update_customer>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reseller_customers_update_execute()` or `reseller_customers_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_customers_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_customers_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Customer>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Customer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT apps/reseller/v1/customers/{customerId}
/// Updates a customer account's settings. You cannot update `customerType` via the Reseller API, but a "team" customer can verify their domain and become `customerType` = "domain". For more information, see [update a customer's settings](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#update_customer>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reseller_customers_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reseller_customers_update_task()`.
/// For the simplest API, use `reseller_customers_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_customers_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reseller_customers_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = reseller_customers_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reseller_customers_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResellerCustomersUpdateArgs {
    /// Path parameter: customerId
    pub customerId: String,
}

/// PUT apps/reseller/v1/customers/{customerId}
/// Updates a customer account's settings. You cannot update `customerType` via the Reseller API, but a "team" customer can verify their domain and become `customerType` = "domain". For more information, see [update a customer's settings](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_customers`#update_customer>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reseller_customers_update_builder()` + `reseller_customers_update_execute()`.
/// For task-level control, use `reseller_customers_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_customers_update(
    client: &SimpleHttpClient,
    args: &ResellerCustomersUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = reseller_customers_update_builder(client, &args.customerId)?;
    reseller_customers_update_execute(builder)
}

/// GET apps/reseller/v1/resellernotify/getwatchdetails
/// Returns all the details of the watch corresponding to the reseller.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reseller_resellernotify_getwatchdetails_execute()` to send, or `reseller_resellernotify_getwatchdetails` for simplest API.

pub fn reseller_resellernotify_getwatchdetails_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://reseller.googleapis.com/apps/reseller/v1/resellernotify/getwatchdetails",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET apps/reseller/v1/resellernotify/getwatchdetails
/// Returns all the details of the watch corresponding to the reseller.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reseller_resellernotify_getwatchdetails_execute()` or `reseller_resellernotify_getwatchdetails`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_resellernotify_getwatchdetails_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_resellernotify_getwatchdetails_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ResellernotifyGetwatchdetailsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResellernotifyGetwatchdetailsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET apps/reseller/v1/resellernotify/getwatchdetails
/// Returns all the details of the watch corresponding to the reseller.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reseller_resellernotify_getwatchdetails_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reseller_resellernotify_getwatchdetails_task()`.
/// For the simplest API, use `reseller_resellernotify_getwatchdetails()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_resellernotify_getwatchdetails_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reseller_resellernotify_getwatchdetails_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResellernotifyGetwatchdetailsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = reseller_resellernotify_getwatchdetails_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET apps/reseller/v1/resellernotify/getwatchdetails
/// Returns all the details of the watch corresponding to the reseller.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reseller_resellernotify_getwatchdetails_builder()` + `reseller_resellernotify_getwatchdetails_execute()`.
/// For task-level control, use `reseller_resellernotify_getwatchdetails_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_resellernotify_getwatchdetails(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResellernotifyGetwatchdetailsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = reseller_resellernotify_getwatchdetails_builder(client)?;
    reseller_resellernotify_getwatchdetails_execute(builder)
}

/// POST apps/reseller/v1/resellernotify/register
/// Registers a Reseller for receiving notifications.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reseller_resellernotify_register_execute()` to send, or `reseller_resellernotify_register` for simplest API.

pub fn reseller_resellernotify_register_builder(
    client: &SimpleHttpClient,
    serviceAccountEmailAddress: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://reseller.googleapis.com/apps/reseller/v1/resellernotify/register",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = serviceAccountEmailAddress.as_ref() {
        query_parts.push(format!("serviceAccountEmailAddress={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST apps/reseller/v1/resellernotify/register
/// Registers a Reseller for receiving notifications.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reseller_resellernotify_register_execute()` or `reseller_resellernotify_register`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_resellernotify_register_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_resellernotify_register_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ResellernotifyResource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResellernotifyResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST apps/reseller/v1/resellernotify/register
/// Registers a Reseller for receiving notifications.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reseller_resellernotify_register_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reseller_resellernotify_register_task()`.
/// For the simplest API, use `reseller_resellernotify_register()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_resellernotify_register_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reseller_resellernotify_register_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResellernotifyResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = reseller_resellernotify_register_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reseller_resellernotify_register`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResellerResellernotifyRegisterArgs {
    /// Query parameter: serviceAccountEmailAddress
    pub serviceAccountEmailAddress: Option<Option<String>>,
}

/// POST apps/reseller/v1/resellernotify/register
/// Registers a Reseller for receiving notifications.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reseller_resellernotify_register_builder()` + `reseller_resellernotify_register_execute()`.
/// For task-level control, use `reseller_resellernotify_register_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_resellernotify_register(
    client: &SimpleHttpClient,
    args: &ResellerResellernotifyRegisterArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResellernotifyResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        reseller_resellernotify_register_builder(client, &args.serviceAccountEmailAddress)?;
    reseller_resellernotify_register_execute(builder)
}

/// POST apps/reseller/v1/resellernotify/unregister
/// Unregisters a Reseller for receiving notifications.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reseller_resellernotify_unregister_execute()` to send, or `reseller_resellernotify_unregister` for simplest API.

pub fn reseller_resellernotify_unregister_builder(
    client: &SimpleHttpClient,
    serviceAccountEmailAddress: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://reseller.googleapis.com/apps/reseller/v1/resellernotify/unregister",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = serviceAccountEmailAddress.as_ref() {
        query_parts.push(format!("serviceAccountEmailAddress={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST apps/reseller/v1/resellernotify/unregister
/// Unregisters a Reseller for receiving notifications.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reseller_resellernotify_unregister_execute()` or `reseller_resellernotify_unregister`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_resellernotify_unregister_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_resellernotify_unregister_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ResellernotifyResource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResellernotifyResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST apps/reseller/v1/resellernotify/unregister
/// Unregisters a Reseller for receiving notifications.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reseller_resellernotify_unregister_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reseller_resellernotify_unregister_task()`.
/// For the simplest API, use `reseller_resellernotify_unregister()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_resellernotify_unregister_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reseller_resellernotify_unregister_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResellernotifyResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = reseller_resellernotify_unregister_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reseller_resellernotify_unregister`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResellerResellernotifyUnregisterArgs {
    /// Query parameter: serviceAccountEmailAddress
    pub serviceAccountEmailAddress: Option<Option<String>>,
}

/// POST apps/reseller/v1/resellernotify/unregister
/// Unregisters a Reseller for receiving notifications.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reseller_resellernotify_unregister_builder()` + `reseller_resellernotify_unregister_execute()`.
/// For task-level control, use `reseller_resellernotify_unregister_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_resellernotify_unregister(
    client: &SimpleHttpClient,
    args: &ResellerResellernotifyUnregisterArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResellernotifyResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        reseller_resellernotify_unregister_builder(client, &args.serviceAccountEmailAddress)?;
    reseller_resellernotify_unregister_execute(builder)
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/activate
/// Activates a subscription previously suspended by the reseller. If you did not suspend the customer subscription and it is suspended for any other reason, such as for abuse or a pending ToS acceptance, this call will not reactivate the customer subscription.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reseller_subscriptions_activate_execute()` to send, or `reseller_subscriptions_activate` for simplest API.

pub fn reseller_subscriptions_activate_builder(
    client: &SimpleHttpClient,
    customerId: &String,
    subscriptionId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://reseller.googleapis.com/apps/reseller/v1/customers/{}/subscriptions/{}/activate",
        customerId, subscriptionId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/activate
/// Activates a subscription previously suspended by the reseller. If you did not suspend the customer subscription and it is suspended for any other reason, such as for abuse or a pending ToS acceptance, this call will not reactivate the customer subscription.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reseller_subscriptions_activate_execute()` or `reseller_subscriptions_activate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_activate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/activate
/// Activates a subscription previously suspended by the reseller. If you did not suspend the customer subscription and it is suspended for any other reason, such as for abuse or a pending ToS acceptance, this call will not reactivate the customer subscription.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reseller_subscriptions_activate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reseller_subscriptions_activate_task()`.
/// For the simplest API, use `reseller_subscriptions_activate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reseller_subscriptions_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = reseller_subscriptions_activate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reseller_subscriptions_activate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResellerSubscriptionsActivateArgs {
    /// Path parameter: customerId
    pub customerId: String,
    /// Path parameter: subscriptionId
    pub subscriptionId: String,
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/activate
/// Activates a subscription previously suspended by the reseller. If you did not suspend the customer subscription and it is suspended for any other reason, such as for abuse or a pending ToS acceptance, this call will not reactivate the customer subscription.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reseller_subscriptions_activate_builder()` + `reseller_subscriptions_activate_execute()`.
/// For task-level control, use `reseller_subscriptions_activate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_activate(
    client: &SimpleHttpClient,
    args: &ResellerSubscriptionsActivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        reseller_subscriptions_activate_builder(client, &args.customerId, &args.subscriptionId)?;
    reseller_subscriptions_activate_execute(builder)
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/changePlan
/// Updates a subscription plan. Use this method to update a plan for a 30-day trial or a flexible plan subscription to an annual commitment plan with monthly or yearly payments. How a plan is updated differs depending on the plan and the products. For more information, see the description in [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#update_subscription_plan>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reseller_subscriptions_change_plan_execute()` to send, or `reseller_subscriptions_change_plan` for simplest API.

pub fn reseller_subscriptions_change_plan_builder(
    client: &SimpleHttpClient,
    customerId: &String,
    subscriptionId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://reseller.googleapis.com/apps/reseller/v1/customers/{}/subscriptions/{}/changePlan",
        customerId, subscriptionId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/changePlan
/// Updates a subscription plan. Use this method to update a plan for a 30-day trial or a flexible plan subscription to an annual commitment plan with monthly or yearly payments. How a plan is updated differs depending on the plan and the products. For more information, see the description in [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#update_subscription_plan>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reseller_subscriptions_change_plan_execute()` or `reseller_subscriptions_change_plan`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_change_plan_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_change_plan_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/changePlan
/// Updates a subscription plan. Use this method to update a plan for a 30-day trial or a flexible plan subscription to an annual commitment plan with monthly or yearly payments. How a plan is updated differs depending on the plan and the products. For more information, see the description in [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#update_subscription_plan>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reseller_subscriptions_change_plan_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reseller_subscriptions_change_plan_task()`.
/// For the simplest API, use `reseller_subscriptions_change_plan()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_change_plan_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reseller_subscriptions_change_plan_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = reseller_subscriptions_change_plan_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reseller_subscriptions_change_plan`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResellerSubscriptionsChangePlanArgs {
    /// Path parameter: customerId
    pub customerId: String,
    /// Path parameter: subscriptionId
    pub subscriptionId: String,
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/changePlan
/// Updates a subscription plan. Use this method to update a plan for a 30-day trial or a flexible plan subscription to an annual commitment plan with monthly or yearly payments. How a plan is updated differs depending on the plan and the products. For more information, see the description in [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#update_subscription_plan>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reseller_subscriptions_change_plan_builder()` + `reseller_subscriptions_change_plan_execute()`.
/// For task-level control, use `reseller_subscriptions_change_plan_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_change_plan(
    client: &SimpleHttpClient,
    args: &ResellerSubscriptionsChangePlanArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        reseller_subscriptions_change_plan_builder(client, &args.customerId, &args.subscriptionId)?;
    reseller_subscriptions_change_plan_execute(builder)
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/changeRenewalSettings
/// Updates a user license's renewal settings. This is applicable for accounts with annual commitment plans only. For more information, see the description in [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#update_renewal>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reseller_subscriptions_change_renewal_settings_execute()` to send, or `reseller_subscriptions_change_renewal_settings` for simplest API.

pub fn reseller_subscriptions_change_renewal_settings_builder(
    client: &SimpleHttpClient,
    customerId: &String,
    subscriptionId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://reseller.googleapis.com/apps/reseller/v1/customers/{}/subscriptions/{}/changeRenewalSettings",
        customerId,
        subscriptionId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/changeRenewalSettings
/// Updates a user license's renewal settings. This is applicable for accounts with annual commitment plans only. For more information, see the description in [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#update_renewal>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reseller_subscriptions_change_renewal_settings_execute()` or `reseller_subscriptions_change_renewal_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_change_renewal_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_change_renewal_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/changeRenewalSettings
/// Updates a user license's renewal settings. This is applicable for accounts with annual commitment plans only. For more information, see the description in [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#update_renewal>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reseller_subscriptions_change_renewal_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reseller_subscriptions_change_renewal_settings_task()`.
/// For the simplest API, use `reseller_subscriptions_change_renewal_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_change_renewal_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reseller_subscriptions_change_renewal_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = reseller_subscriptions_change_renewal_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reseller_subscriptions_change_renewal_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResellerSubscriptionsChangeRenewalSettingsArgs {
    /// Path parameter: customerId
    pub customerId: String,
    /// Path parameter: subscriptionId
    pub subscriptionId: String,
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/changeRenewalSettings
/// Updates a user license's renewal settings. This is applicable for accounts with annual commitment plans only. For more information, see the description in [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#update_renewal>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reseller_subscriptions_change_renewal_settings_builder()` + `reseller_subscriptions_change_renewal_settings_execute()`.
/// For task-level control, use `reseller_subscriptions_change_renewal_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_change_renewal_settings(
    client: &SimpleHttpClient,
    args: &ResellerSubscriptionsChangeRenewalSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = reseller_subscriptions_change_renewal_settings_builder(
        client,
        &args.customerId,
        &args.subscriptionId,
    )?;
    reseller_subscriptions_change_renewal_settings_execute(builder)
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/changeSeats
/// Updates a subscription's user license settings. For more information about updating an annual commitment plan or a flexible plan subscription’s licenses, see [Manage Subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#update_subscription_seat>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reseller_subscriptions_change_seats_execute()` to send, or `reseller_subscriptions_change_seats` for simplest API.

pub fn reseller_subscriptions_change_seats_builder(
    client: &SimpleHttpClient,
    customerId: &String,
    subscriptionId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://reseller.googleapis.com/apps/reseller/v1/customers/{}/subscriptions/{}/changeSeats",
        customerId,
        subscriptionId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/changeSeats
/// Updates a subscription's user license settings. For more information about updating an annual commitment plan or a flexible plan subscription’s licenses, see [Manage Subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#update_subscription_seat>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reseller_subscriptions_change_seats_execute()` or `reseller_subscriptions_change_seats`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_change_seats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_change_seats_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/changeSeats
/// Updates a subscription's user license settings. For more information about updating an annual commitment plan or a flexible plan subscription’s licenses, see [Manage Subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#update_subscription_seat>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reseller_subscriptions_change_seats_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reseller_subscriptions_change_seats_task()`.
/// For the simplest API, use `reseller_subscriptions_change_seats()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_change_seats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reseller_subscriptions_change_seats_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = reseller_subscriptions_change_seats_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reseller_subscriptions_change_seats`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResellerSubscriptionsChangeSeatsArgs {
    /// Path parameter: customerId
    pub customerId: String,
    /// Path parameter: subscriptionId
    pub subscriptionId: String,
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/changeSeats
/// Updates a subscription's user license settings. For more information about updating an annual commitment plan or a flexible plan subscription’s licenses, see [Manage Subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#update_subscription_seat>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reseller_subscriptions_change_seats_builder()` + `reseller_subscriptions_change_seats_execute()`.
/// For task-level control, use `reseller_subscriptions_change_seats_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_change_seats(
    client: &SimpleHttpClient,
    args: &ResellerSubscriptionsChangeSeatsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = reseller_subscriptions_change_seats_builder(
        client,
        &args.customerId,
        &args.subscriptionId,
    )?;
    reseller_subscriptions_change_seats_execute(builder)
}

/// DELETE apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}
/// Cancels, suspends, or transfers a subscription to direct.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reseller_subscriptions_delete_execute()` to send, or `reseller_subscriptions_delete` for simplest API.

pub fn reseller_subscriptions_delete_builder(
    client: &SimpleHttpClient,
    customerId: &String,
    subscriptionId: &String,
    deletionType: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://reseller.googleapis.com/apps/reseller/v1/customers/{}/subscriptions/{}",
        customerId, subscriptionId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = deletionType.as_ref() {
        query_parts.push(format!("deletionType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}
/// Cancels, suspends, or transfers a subscription to direct.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reseller_subscriptions_delete_execute()` or `reseller_subscriptions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}
/// Cancels, suspends, or transfers a subscription to direct.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reseller_subscriptions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reseller_subscriptions_delete_task()`.
/// For the simplest API, use `reseller_subscriptions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reseller_subscriptions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = reseller_subscriptions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reseller_subscriptions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResellerSubscriptionsDeleteArgs {
    /// Path parameter: customerId
    pub customerId: String,
    /// Path parameter: subscriptionId
    pub subscriptionId: String,
    /// Query parameter: deletionType
    pub deletionType: Option<Option<String>>,
}

/// DELETE apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}
/// Cancels, suspends, or transfers a subscription to direct.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reseller_subscriptions_delete_builder()` + `reseller_subscriptions_delete_execute()`.
/// For task-level control, use `reseller_subscriptions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_delete(
    client: &SimpleHttpClient,
    args: &ResellerSubscriptionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = reseller_subscriptions_delete_builder(
        client,
        &args.customerId,
        &args.subscriptionId,
        &args.deletionType,
    )?;
    reseller_subscriptions_delete_execute(builder)
}

/// GET apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}
/// Gets a specific subscription. The `subscriptionId` can be found using the [Retrieve all reseller subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#get_all_subscriptions>) method. For more information about retrieving a specific subscription, see the information descrived in [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#get_subscription>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reseller_subscriptions_get_execute()` to send, or `reseller_subscriptions_get` for simplest API.

pub fn reseller_subscriptions_get_builder(
    client: &SimpleHttpClient,
    customerId: &String,
    subscriptionId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://reseller.googleapis.com/apps/reseller/v1/customers/{}/subscriptions/{}",
        customerId, subscriptionId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}
/// Gets a specific subscription. The `subscriptionId` can be found using the [Retrieve all reseller subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#get_all_subscriptions>) method. For more information about retrieving a specific subscription, see the information descrived in [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#get_subscription>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reseller_subscriptions_get_execute()` or `reseller_subscriptions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}
/// Gets a specific subscription. The `subscriptionId` can be found using the [Retrieve all reseller subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#get_all_subscriptions>) method. For more information about retrieving a specific subscription, see the information descrived in [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#get_subscription>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reseller_subscriptions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reseller_subscriptions_get_task()`.
/// For the simplest API, use `reseller_subscriptions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reseller_subscriptions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = reseller_subscriptions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reseller_subscriptions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResellerSubscriptionsGetArgs {
    /// Path parameter: customerId
    pub customerId: String,
    /// Path parameter: subscriptionId
    pub subscriptionId: String,
}

/// GET apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}
/// Gets a specific subscription. The `subscriptionId` can be found using the [Retrieve all reseller subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#get_all_subscriptions>) method. For more information about retrieving a specific subscription, see the information descrived in [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#get_subscription>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reseller_subscriptions_get_builder()` + `reseller_subscriptions_get_execute()`.
/// For task-level control, use `reseller_subscriptions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_get(
    client: &SimpleHttpClient,
    args: &ResellerSubscriptionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        reseller_subscriptions_get_builder(client, &args.customerId, &args.subscriptionId)?;
    reseller_subscriptions_get_execute(builder)
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions
/// Creates or transfer a subscription. Create a subscription for a customer's account that you ordered using the [Order a new customer account](<https://developers.google.`com/workspace/admin/reseller/v1/reference/customers/insert`.html>) method. For more information about creating a subscription for different payment plans, see [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#create_subscription>).\ If you did not order the customer's account using the customer insert method, use the customer's `customerAuthToken` when creating a subscription for that customer. If transferring a G Suite subscription with an associated Google Drive or Google Vault subscription, use the [batch operation](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/batch`.html>) to transfer all of these subscriptions. For more information, see how to [transfer subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#transfer_a_subscription>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reseller_subscriptions_insert_execute()` to send, or `reseller_subscriptions_insert` for simplest API.

pub fn reseller_subscriptions_insert_builder(
    client: &SimpleHttpClient,
    customerId: &String,
    action: &Option<Option<String>>,
    customerAuthToken: &Option<Option<String>>,
    sourceSkuId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://reseller.googleapis.com/apps/reseller/v1/customers/{}/subscriptions",
        customerId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = action.as_ref() {
        query_parts.push(format!("action={}", val));
    }
    if let Some(val) = customerAuthToken.as_ref() {
        query_parts.push(format!("customerAuthToken={}", val));
    }
    if let Some(val) = sourceSkuId.as_ref() {
        query_parts.push(format!("sourceSkuId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions
/// Creates or transfer a subscription. Create a subscription for a customer's account that you ordered using the [Order a new customer account](<https://developers.google.`com/workspace/admin/reseller/v1/reference/customers/insert`.html>) method. For more information about creating a subscription for different payment plans, see [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#create_subscription>).\ If you did not order the customer's account using the customer insert method, use the customer's `customerAuthToken` when creating a subscription for that customer. If transferring a G Suite subscription with an associated Google Drive or Google Vault subscription, use the [batch operation](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/batch`.html>) to transfer all of these subscriptions. For more information, see how to [transfer subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#transfer_a_subscription>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reseller_subscriptions_insert_execute()` or `reseller_subscriptions_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions
/// Creates or transfer a subscription. Create a subscription for a customer's account that you ordered using the [Order a new customer account](<https://developers.google.`com/workspace/admin/reseller/v1/reference/customers/insert`.html>) method. For more information about creating a subscription for different payment plans, see [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#create_subscription>).\ If you did not order the customer's account using the customer insert method, use the customer's `customerAuthToken` when creating a subscription for that customer. If transferring a G Suite subscription with an associated Google Drive or Google Vault subscription, use the [batch operation](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/batch`.html>) to transfer all of these subscriptions. For more information, see how to [transfer subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#transfer_a_subscription>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reseller_subscriptions_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reseller_subscriptions_insert_task()`.
/// For the simplest API, use `reseller_subscriptions_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reseller_subscriptions_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = reseller_subscriptions_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reseller_subscriptions_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResellerSubscriptionsInsertArgs {
    /// Path parameter: customerId
    pub customerId: String,
    /// Query parameter: action
    pub action: Option<Option<String>>,
    /// Query parameter: customerAuthToken
    pub customerAuthToken: Option<Option<String>>,
    /// Query parameter: sourceSkuId
    pub sourceSkuId: Option<Option<String>>,
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions
/// Creates or transfer a subscription. Create a subscription for a customer's account that you ordered using the [Order a new customer account](<https://developers.google.`com/workspace/admin/reseller/v1/reference/customers/insert`.html>) method. For more information about creating a subscription for different payment plans, see [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#create_subscription>).\ If you did not order the customer's account using the customer insert method, use the customer's `customerAuthToken` when creating a subscription for that customer. If transferring a G Suite subscription with an associated Google Drive or Google Vault subscription, use the [batch operation](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/batch`.html>) to transfer all of these subscriptions. For more information, see how to [transfer subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#transfer_a_subscription>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reseller_subscriptions_insert_builder()` + `reseller_subscriptions_insert_execute()`.
/// For task-level control, use `reseller_subscriptions_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_insert(
    client: &SimpleHttpClient,
    args: &ResellerSubscriptionsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = reseller_subscriptions_insert_builder(
        client,
        &args.customerId,
        &args.action,
        &args.customerAuthToken,
        &args.sourceSkuId,
    )?;
    reseller_subscriptions_insert_execute(builder)
}

/// GET apps/reseller/v1/subscriptions
/// Lists of subscriptions managed by the reseller. The list can be all subscriptions, all of a customer's subscriptions, or all of a customer's transferable subscriptions. Optionally, this method can filter the response by a `customerNamePrefix`. For more information, see [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reseller_subscriptions_list_execute()` to send, or `reseller_subscriptions_list` for simplest API.

pub fn reseller_subscriptions_list_builder(
    client: &SimpleHttpClient,
    customerAuthToken: &Option<Option<String>>,
    customerId: &Option<Option<String>>,
    customerNamePrefix: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://reseller.googleapis.com/apps/reseller/v1/subscriptions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customerAuthToken.as_ref() {
        query_parts.push(format!("customerAuthToken={}", val));
    }
    if let Some(val) = customerId.as_ref() {
        query_parts.push(format!("customerId={}", val));
    }
    if let Some(val) = customerNamePrefix.as_ref() {
        query_parts.push(format!("customerNamePrefix={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET apps/reseller/v1/subscriptions
/// Lists of subscriptions managed by the reseller. The list can be all subscriptions, all of a customer's subscriptions, or all of a customer's transferable subscriptions. Optionally, this method can filter the response by a `customerNamePrefix`. For more information, see [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reseller_subscriptions_list_execute()` or `reseller_subscriptions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscriptions>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscriptions = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET apps/reseller/v1/subscriptions
/// Lists of subscriptions managed by the reseller. The list can be all subscriptions, all of a customer's subscriptions, or all of a customer's transferable subscriptions. Optionally, this method can filter the response by a `customerNamePrefix`. For more information, see [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reseller_subscriptions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reseller_subscriptions_list_task()`.
/// For the simplest API, use `reseller_subscriptions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reseller_subscriptions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscriptions>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = reseller_subscriptions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reseller_subscriptions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResellerSubscriptionsListArgs {
    /// Query parameter: customerAuthToken
    pub customerAuthToken: Option<Option<String>>,
    /// Query parameter: customerId
    pub customerId: Option<Option<String>>,
    /// Query parameter: customerNamePrefix
    pub customerNamePrefix: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET apps/reseller/v1/subscriptions
/// Lists of subscriptions managed by the reseller. The list can be all subscriptions, all of a customer's subscriptions, or all of a customer's transferable subscriptions. Optionally, this method can filter the response by a `customerNamePrefix`. For more information, see [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reseller_subscriptions_list_builder()` + `reseller_subscriptions_list_execute()`.
/// For task-level control, use `reseller_subscriptions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_list(
    client: &SimpleHttpClient,
    args: &ResellerSubscriptionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscriptions>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = reseller_subscriptions_list_builder(
        client,
        &args.customerAuthToken,
        &args.customerId,
        &args.customerNamePrefix,
        &args.maxResults,
        &args.pageToken,
    )?;
    reseller_subscriptions_list_execute(builder)
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/startPaidService
/// Immediately move a 30-day free trial subscription to a paid service subscription. This method is only applicable if a payment plan has already been set up for the 30-day trial subscription. For more information, see [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#paid_service>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reseller_subscriptions_start_paid_service_execute()` to send, or `reseller_subscriptions_start_paid_service` for simplest API.

pub fn reseller_subscriptions_start_paid_service_builder(
    client: &SimpleHttpClient,
    customerId: &String,
    subscriptionId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://reseller.googleapis.com/apps/reseller/v1/customers/{}/subscriptions/{}/startPaidService",
        customerId,
        subscriptionId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/startPaidService
/// Immediately move a 30-day free trial subscription to a paid service subscription. This method is only applicable if a payment plan has already been set up for the 30-day trial subscription. For more information, see [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#paid_service>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reseller_subscriptions_start_paid_service_execute()` or `reseller_subscriptions_start_paid_service`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_start_paid_service_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_start_paid_service_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/startPaidService
/// Immediately move a 30-day free trial subscription to a paid service subscription. This method is only applicable if a payment plan has already been set up for the 30-day trial subscription. For more information, see [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#paid_service>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reseller_subscriptions_start_paid_service_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reseller_subscriptions_start_paid_service_task()`.
/// For the simplest API, use `reseller_subscriptions_start_paid_service()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_start_paid_service_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reseller_subscriptions_start_paid_service_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = reseller_subscriptions_start_paid_service_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reseller_subscriptions_start_paid_service`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResellerSubscriptionsStartPaidServiceArgs {
    /// Path parameter: customerId
    pub customerId: String,
    /// Path parameter: subscriptionId
    pub subscriptionId: String,
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/startPaidService
/// Immediately move a 30-day free trial subscription to a paid service subscription. This method is only applicable if a payment plan has already been set up for the 30-day trial subscription. For more information, see [manage subscriptions](<https://developers.google.`com/workspace/admin/reseller/v1/how-tos/manage_subscriptions`#paid_service>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reseller_subscriptions_start_paid_service_builder()` + `reseller_subscriptions_start_paid_service_execute()`.
/// For task-level control, use `reseller_subscriptions_start_paid_service_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_start_paid_service(
    client: &SimpleHttpClient,
    args: &ResellerSubscriptionsStartPaidServiceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = reseller_subscriptions_start_paid_service_builder(
        client,
        &args.customerId,
        &args.subscriptionId,
    )?;
    reseller_subscriptions_start_paid_service_execute(builder)
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/suspend
/// Suspends an active subscription. You can use this method to suspend a paid subscription that is currently in the `ACTIVE` state. * For FLEXIBLE subscriptions, billing is paused. * For ANNUAL_MONTHLY_PAY or ANNUAL_YEARLY_PAY subscriptions: * Suspending the subscription does not change the renewal date that was originally committed to. * A suspended subscription does not renew. If you activate the subscription after the original renewal date, a new annual subscription will be created, starting on the day of activation. We strongly encourage you to suspend subscriptions only for short periods of time as suspensions over 60 days may result in the subscription being cancelled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reseller_subscriptions_suspend_execute()` to send, or `reseller_subscriptions_suspend` for simplest API.

pub fn reseller_subscriptions_suspend_builder(
    client: &SimpleHttpClient,
    customerId: &String,
    subscriptionId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://reseller.googleapis.com/apps/reseller/v1/customers/{}/subscriptions/{}/suspend",
        customerId, subscriptionId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/suspend
/// Suspends an active subscription. You can use this method to suspend a paid subscription that is currently in the `ACTIVE` state. * For FLEXIBLE subscriptions, billing is paused. * For ANNUAL_MONTHLY_PAY or ANNUAL_YEARLY_PAY subscriptions: * Suspending the subscription does not change the renewal date that was originally committed to. * A suspended subscription does not renew. If you activate the subscription after the original renewal date, a new annual subscription will be created, starting on the day of activation. We strongly encourage you to suspend subscriptions only for short periods of time as suspensions over 60 days may result in the subscription being cancelled.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reseller_subscriptions_suspend_execute()` or `reseller_subscriptions_suspend`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_suspend_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_suspend_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/suspend
/// Suspends an active subscription. You can use this method to suspend a paid subscription that is currently in the `ACTIVE` state. * For FLEXIBLE subscriptions, billing is paused. * For ANNUAL_MONTHLY_PAY or ANNUAL_YEARLY_PAY subscriptions: * Suspending the subscription does not change the renewal date that was originally committed to. * A suspended subscription does not renew. If you activate the subscription after the original renewal date, a new annual subscription will be created, starting on the day of activation. We strongly encourage you to suspend subscriptions only for short periods of time as suspensions over 60 days may result in the subscription being cancelled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reseller_subscriptions_suspend_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reseller_subscriptions_suspend_task()`.
/// For the simplest API, use `reseller_subscriptions_suspend()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reseller_subscriptions_suspend_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reseller_subscriptions_suspend_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = reseller_subscriptions_suspend_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reseller_subscriptions_suspend`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResellerSubscriptionsSuspendArgs {
    /// Path parameter: customerId
    pub customerId: String,
    /// Path parameter: subscriptionId
    pub subscriptionId: String,
}

/// POST apps/reseller/v1/customers/{customerId}/subscriptions/{subscriptionId}/suspend
/// Suspends an active subscription. You can use this method to suspend a paid subscription that is currently in the `ACTIVE` state. * For FLEXIBLE subscriptions, billing is paused. * For ANNUAL_MONTHLY_PAY or ANNUAL_YEARLY_PAY subscriptions: * Suspending the subscription does not change the renewal date that was originally committed to. * A suspended subscription does not renew. If you activate the subscription after the original renewal date, a new annual subscription will be created, starting on the day of activation. We strongly encourage you to suspend subscriptions only for short periods of time as suspensions over 60 days may result in the subscription being cancelled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reseller_subscriptions_suspend_builder()` + `reseller_subscriptions_suspend_execute()`.
/// For task-level control, use `reseller_subscriptions_suspend_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reseller_subscriptions_suspend(
    client: &SimpleHttpClient,
    args: &ResellerSubscriptionsSuspendArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        reseller_subscriptions_suspend_builder(client, &args.customerId, &args.subscriptionId)?;
    reseller_subscriptions_suspend_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Customer
// =============================================================================

/// ResourceIdentifier implementation for Customer with ResellerCustomersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ResellerCustomersGetArgs> for Customer {
    fn generate_resource_id(&self, input: &ResellerCustomersGetArgs) -> String {
        format!("gcp::reseller::Customer/{}", input.customerId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::reseller::Customer"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Customer
// =============================================================================

/// ResourceIdentifier implementation for Customer with ResellerCustomersInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ResellerCustomersInsertArgs> for Customer {
    fn generate_resource_id(&self, input: &ResellerCustomersInsertArgs) -> String {
        "gcp::reseller::Customer".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::reseller::Customer"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Customer
// =============================================================================

/// ResourceIdentifier implementation for Customer with ResellerCustomersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ResellerCustomersPatchArgs> for Customer {
    fn generate_resource_id(&self, input: &ResellerCustomersPatchArgs) -> String {
        format!("gcp::reseller::Customer/{}", input.customerId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::reseller::Customer"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Customer
// =============================================================================

/// ResourceIdentifier implementation for Customer with ResellerCustomersUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ResellerCustomersUpdateArgs> for Customer {
    fn generate_resource_id(&self, input: &ResellerCustomersUpdateArgs) -> String {
        format!("gcp::reseller::Customer/{}", input.customerId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::reseller::Customer"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ResellernotifyGetwatchdetailsResponse
// =============================================================================

/// ResourceIdentifier implementation for ResellernotifyGetwatchdetailsResponse with ResellerResellernotifyGetwatchdetailsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ResellerResellernotifyGetwatchdetailsArgs>
    for ResellernotifyGetwatchdetailsResponse
{
    fn generate_resource_id(&self, input: &ResellerResellernotifyGetwatchdetailsArgs) -> String {
        "gcp::reseller::ResellernotifyGetwatchdetailsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::reseller::ResellernotifyGetwatchdetailsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ResellernotifyResource
// =============================================================================

/// ResourceIdentifier implementation for ResellernotifyResource with ResellerResellernotifyRegisterArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ResellerResellernotifyRegisterArgs> for ResellernotifyResource {
    fn generate_resource_id(&self, input: &ResellerResellernotifyRegisterArgs) -> String {
        "gcp::reseller::ResellernotifyResource".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::reseller::ResellernotifyResource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ResellernotifyResource
// =============================================================================

/// ResourceIdentifier implementation for ResellernotifyResource with ResellerResellernotifyUnregisterArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ResellerResellernotifyUnregisterArgs> for ResellernotifyResource {
    fn generate_resource_id(&self, input: &ResellerResellernotifyUnregisterArgs) -> String {
        "gcp::reseller::ResellernotifyResource".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::reseller::ResellernotifyResource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Subscription
// =============================================================================

/// ResourceIdentifier implementation for Subscription with ResellerSubscriptionsActivateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ResellerSubscriptionsActivateArgs> for Subscription {
    fn generate_resource_id(&self, input: &ResellerSubscriptionsActivateArgs) -> String {
        format!(
            "gcp::reseller::Subscription/{}/{}",
            input.customerId, input.subscriptionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::reseller::Subscription"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Subscription
// =============================================================================

/// ResourceIdentifier implementation for Subscription with ResellerSubscriptionsChangePlanArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ResellerSubscriptionsChangePlanArgs> for Subscription {
    fn generate_resource_id(&self, input: &ResellerSubscriptionsChangePlanArgs) -> String {
        format!(
            "gcp::reseller::Subscription/{}/{}",
            input.customerId, input.subscriptionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::reseller::Subscription"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Subscription
// =============================================================================

/// ResourceIdentifier implementation for Subscription with ResellerSubscriptionsChangeRenewalSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ResellerSubscriptionsChangeRenewalSettingsArgs> for Subscription {
    fn generate_resource_id(
        &self,
        input: &ResellerSubscriptionsChangeRenewalSettingsArgs,
    ) -> String {
        format!(
            "gcp::reseller::Subscription/{}/{}",
            input.customerId, input.subscriptionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::reseller::Subscription"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Subscription
// =============================================================================

/// ResourceIdentifier implementation for Subscription with ResellerSubscriptionsChangeSeatsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ResellerSubscriptionsChangeSeatsArgs> for Subscription {
    fn generate_resource_id(&self, input: &ResellerSubscriptionsChangeSeatsArgs) -> String {
        format!(
            "gcp::reseller::Subscription/{}/{}",
            input.customerId, input.subscriptionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::reseller::Subscription"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Subscription
// =============================================================================

/// ResourceIdentifier implementation for Subscription with ResellerSubscriptionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ResellerSubscriptionsGetArgs> for Subscription {
    fn generate_resource_id(&self, input: &ResellerSubscriptionsGetArgs) -> String {
        format!(
            "gcp::reseller::Subscription/{}/{}",
            input.customerId, input.subscriptionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::reseller::Subscription"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Subscription
// =============================================================================

/// ResourceIdentifier implementation for Subscription with ResellerSubscriptionsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ResellerSubscriptionsInsertArgs> for Subscription {
    fn generate_resource_id(&self, input: &ResellerSubscriptionsInsertArgs) -> String {
        format!("gcp::reseller::Subscription/{}", input.customerId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::reseller::Subscription"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Subscriptions
// =============================================================================

/// ResourceIdentifier implementation for Subscriptions with ResellerSubscriptionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ResellerSubscriptionsListArgs> for Subscriptions {
    fn generate_resource_id(&self, input: &ResellerSubscriptionsListArgs) -> String {
        "gcp::reseller::Subscriptions".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::reseller::Subscriptions"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Subscription
// =============================================================================

/// ResourceIdentifier implementation for Subscription with ResellerSubscriptionsStartPaidServiceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ResellerSubscriptionsStartPaidServiceArgs> for Subscription {
    fn generate_resource_id(&self, input: &ResellerSubscriptionsStartPaidServiceArgs) -> String {
        format!(
            "gcp::reseller::Subscription/{}/{}",
            input.customerId, input.subscriptionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::reseller::Subscription"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Subscription
// =============================================================================

/// ResourceIdentifier implementation for Subscription with ResellerSubscriptionsSuspendArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ResellerSubscriptionsSuspendArgs> for Subscription {
    fn generate_resource_id(&self, input: &ResellerSubscriptionsSuspendArgs) -> String {
        format!(
            "gcp::reseller::Subscription/{}/{}",
            input.customerId, input.subscriptionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::reseller::Subscription"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
