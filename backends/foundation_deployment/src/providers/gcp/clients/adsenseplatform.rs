//! Auto-generated API clients for Google Cloud Platform - adsenseplatform.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`
//!

#![cfg(feature = "gcp")]

//! Feature flag: `gcp/adsenseplatform`

#![cfg(feature = "gcp/adsenseplatform")]

use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;

/// GET v1/{+name}:close
/// Closes a sub-account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_close_execute()` to send, or `adsenseplatform_platforms_accounts_close` for simplest API.

pub fn adsenseplatform_platforms_accounts_close_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CloseAccountRequest
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:close",
        name,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:close
/// Closes a sub-account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adsenseplatform_platforms_accounts_close_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adsenseplatform_platforms_accounts_close_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_close_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<CloseAccountResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CloseAccountResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:close
/// Closes a sub-account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_close_builder()` + `adsenseplatform_platforms_accounts_close_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_close(
    client: &SimpleHttpClient,
    name: &str,
    body: &CloseAccountRequest,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<CloseAccountResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = adsenseplatform_platforms_accounts_close_builder(client, name, body)?;
    adsenseplatform_platforms_accounts_close_execute(builder)
}

/// GET v1/{+parent}/accounts
/// Creates a sub-account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_create_execute()` to send, or `adsenseplatform_platforms_accounts_create` for simplest API.

pub fn adsenseplatform_platforms_accounts_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Account
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/accounts",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/accounts
/// Creates a sub-account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adsenseplatform_platforms_accounts_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adsenseplatform_platforms_accounts_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Account>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/accounts
/// Creates a sub-account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_create_builder()` + `adsenseplatform_platforms_accounts_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Account,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Account>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = adsenseplatform_platforms_accounts_create_builder(client, parent, body)?;
    adsenseplatform_platforms_accounts_create_execute(builder)
}

/// GET v1/{+name}
/// Gets information about the selected sub-account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_get_execute()` to send, or `adsenseplatform_platforms_accounts_get` for simplest API.

pub fn adsenseplatform_platforms_accounts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}",
        name,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets information about the selected sub-account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adsenseplatform_platforms_accounts_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adsenseplatform_platforms_accounts_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Account>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets information about the selected sub-account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_get_builder()` + `adsenseplatform_platforms_accounts_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Account>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = adsenseplatform_platforms_accounts_get_builder(client, name)?;
    adsenseplatform_platforms_accounts_get_execute(builder)
}

/// GET v1/{+parent}/accounts
/// Lists a partial view of sub-accounts for a specific parent account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_list_execute()` to send, or `adsenseplatform_platforms_accounts_list` for simplest API.

pub fn adsenseplatform_platforms_accounts_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/accounts",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .query(
            "pageSize",
            pageSize.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )

        .query(
            "pageToken",
            pageToken.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )
}

/// GET v1/{+parent}/accounts
/// Lists a partial view of sub-accounts for a specific parent account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adsenseplatform_platforms_accounts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adsenseplatform_platforms_accounts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ListAccountsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAccountsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/accounts
/// Lists a partial view of sub-accounts for a specific parent account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_list_builder()` + `adsenseplatform_platforms_accounts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ListAccountsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = adsenseplatform_platforms_accounts_list_builder(client, parent, pageSize, pageToken)?;
    adsenseplatform_platforms_accounts_list_execute(builder)
}

/// GET v1/{+parent}/accounts:lookup
/// Looks up information about a sub-account for a specified creation_request_id. If no account exists for the given creation_request_id, returns 404.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_lookup_execute()` to send, or `adsenseplatform_platforms_accounts_lookup` for simplest API.

pub fn adsenseplatform_platforms_accounts_lookup_builder(
    client: &SimpleHttpClient,
    parent: &str,
    creationRequestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/accounts:lookup",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .query(
            "creationRequestId",
            creationRequestId.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )
}

/// GET v1/{+parent}/accounts:lookup
/// Looks up information about a sub-account for a specified creation_request_id. If no account exists for the given creation_request_id, returns 404.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adsenseplatform_platforms_accounts_lookup_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adsenseplatform_platforms_accounts_lookup_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<LookupAccountResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LookupAccountResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/accounts:lookup
/// Looks up information about a sub-account for a specified creation_request_id. If no account exists for the given creation_request_id, returns 404.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_lookup_builder()` + `adsenseplatform_platforms_accounts_lookup_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_lookup(
    client: &SimpleHttpClient,
    parent: &str,
    creationRequestId: Option<&str>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<LookupAccountResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = adsenseplatform_platforms_accounts_lookup_builder(client, parent, creationRequestId)?;
    adsenseplatform_platforms_accounts_lookup_execute(builder)
}

/// GET v1/{+parent}/events
/// Creates an account event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_events_create_execute()` to send, or `adsenseplatform_platforms_accounts_events_create` for simplest API.

pub fn adsenseplatform_platforms_accounts_events_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Event
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/events",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/events
/// Creates an account event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adsenseplatform_platforms_accounts_events_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adsenseplatform_platforms_accounts_events_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_events_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Event>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Event = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/events
/// Creates an account event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_events_create_builder()` + `adsenseplatform_platforms_accounts_events_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_events_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Event,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Event>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = adsenseplatform_platforms_accounts_events_create_builder(client, parent, body)?;
    adsenseplatform_platforms_accounts_events_create_execute(builder)
}

/// GET v1/{+parent}/sites
/// Creates a site for a specified account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_sites_create_execute()` to send, or `adsenseplatform_platforms_accounts_sites_create` for simplest API.

pub fn adsenseplatform_platforms_accounts_sites_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Site
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/sites",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/sites
/// Creates a site for a specified account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adsenseplatform_platforms_accounts_sites_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adsenseplatform_platforms_accounts_sites_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Site>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Site = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sites
/// Creates a site for a specified account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_sites_create_builder()` + `adsenseplatform_platforms_accounts_sites_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Site,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Site>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = adsenseplatform_platforms_accounts_sites_create_builder(client, parent, body)?;
    adsenseplatform_platforms_accounts_sites_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a site from a specified account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_sites_delete_execute()` to send, or `adsenseplatform_platforms_accounts_sites_delete` for simplest API.

pub fn adsenseplatform_platforms_accounts_sites_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}",
        name,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a site from a specified account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adsenseplatform_platforms_accounts_sites_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adsenseplatform_platforms_accounts_sites_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Empty>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a site from a specified account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_sites_delete_builder()` + `adsenseplatform_platforms_accounts_sites_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Empty>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = adsenseplatform_platforms_accounts_sites_delete_builder(client, name)?;
    adsenseplatform_platforms_accounts_sites_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets a site from a specified sub-account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_sites_get_execute()` to send, or `adsenseplatform_platforms_accounts_sites_get` for simplest API.

pub fn adsenseplatform_platforms_accounts_sites_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}",
        name,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a site from a specified sub-account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adsenseplatform_platforms_accounts_sites_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adsenseplatform_platforms_accounts_sites_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Site>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Site = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a site from a specified sub-account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_sites_get_builder()` + `adsenseplatform_platforms_accounts_sites_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Site>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = adsenseplatform_platforms_accounts_sites_get_builder(client, name)?;
    adsenseplatform_platforms_accounts_sites_get_execute(builder)
}

/// GET v1/{+parent}/sites
/// Lists sites for a specific account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_sites_list_execute()` to send, or `adsenseplatform_platforms_accounts_sites_list` for simplest API.

pub fn adsenseplatform_platforms_accounts_sites_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/sites",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .query(
            "pageSize",
            pageSize.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )

        .query(
            "pageToken",
            pageToken.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )
}

/// GET v1/{+parent}/sites
/// Lists sites for a specific account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adsenseplatform_platforms_accounts_sites_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adsenseplatform_platforms_accounts_sites_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ListSitesResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSitesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sites
/// Lists sites for a specific account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_sites_list_builder()` + `adsenseplatform_platforms_accounts_sites_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ListSitesResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = adsenseplatform_platforms_accounts_sites_list_builder(client, parent, pageSize, pageToken)?;
    adsenseplatform_platforms_accounts_sites_list_execute(builder)
}

/// GET v1/{+name}:requestReview
/// Requests the review of a site. The site should be in REQUIRES_REVIEW or NEEDS_ATTENTION state. Note: Make sure you place an [ad tag](<https://developers.google.`com/adsense/platforms/direct/ad-tags`>) on your site before requesting a review.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_sites_request_review_execute()` to send, or `adsenseplatform_platforms_accounts_sites_request_review` for simplest API.

pub fn adsenseplatform_platforms_accounts_sites_request_review_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:requestReview",
        name,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:requestReview
/// Requests the review of a site. The site should be in REQUIRES_REVIEW or NEEDS_ATTENTION state. Note: Make sure you place an [ad tag](<https://developers.google.`com/adsense/platforms/direct/ad-tags`>) on your site before requesting a review.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adsenseplatform_platforms_accounts_sites_request_review_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adsenseplatform_platforms_accounts_sites_request_review_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_request_review_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<RequestSiteReviewResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RequestSiteReviewResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:requestReview
/// Requests the review of a site. The site should be in REQUIRES_REVIEW or NEEDS_ATTENTION state. Note: Make sure you place an [ad tag](<https://developers.google.`com/adsense/platforms/direct/ad-tags`>) on your site before requesting a review.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_sites_request_review_builder()` + `adsenseplatform_platforms_accounts_sites_request_review_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_request_review(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<RequestSiteReviewResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = adsenseplatform_platforms_accounts_sites_request_review_builder(client, name)?;
    adsenseplatform_platforms_accounts_sites_request_review_execute(builder)
}

