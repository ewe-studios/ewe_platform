//! Auto-generated API clients for gcp/adsenseplatform.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/platforms/{platformsId}/accounts/{accountsId}:close
/// Closes a sub-account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_close_execute()` to send, or `adsenseplatform_platforms_accounts_close` for simplest API.

pub fn adsenseplatform_platforms_accounts_close_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CloseAccountRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adsenseplatform.googleapis.com/v1/platforms/{}/accounts/{}:close",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}:close
/// Closes a sub-account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsenseplatform_platforms_accounts_close_execute()` or `adsenseplatform_platforms_accounts_close`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_close_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_close_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<CloseAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CloseAccountResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}:close
/// Closes a sub-account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsenseplatform_platforms_accounts_close_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsenseplatform_platforms_accounts_close_task()`.
/// For the simplest API, use `adsenseplatform_platforms_accounts_close()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_close_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsenseplatform_platforms_accounts_close_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CloseAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adsenseplatform_platforms_accounts_close_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsenseplatform_platforms_accounts_close`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseplatformPlatformsAccountsCloseArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: CloseAccountRequest,
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}:close
/// Closes a sub-account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_close_builder()` + `adsenseplatform_platforms_accounts_close_execute()`.
/// For task-level control, use `adsenseplatform_platforms_accounts_close_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_close(
    client: &SimpleHttpClient,
    args: &AdsenseplatformPlatformsAccountsCloseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CloseAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adsenseplatform_platforms_accounts_close_builder(client, &args.name, &args.body)?;
    adsenseplatform_platforms_accounts_close_execute(builder)
}

/// GET v1/platforms/{platformsId}/accounts
/// Creates a sub-account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_create_execute()` to send, or `adsenseplatform_platforms_accounts_create` for simplest API.

pub fn adsenseplatform_platforms_accounts_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Account,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adsenseplatform.googleapis.com/v1/platforms/{}/accounts",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/platforms/{platformsId}/accounts
/// Creates a sub-account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsenseplatform_platforms_accounts_create_execute()` or `adsenseplatform_platforms_accounts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/platforms/{platformsId}/accounts
/// Creates a sub-account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsenseplatform_platforms_accounts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsenseplatform_platforms_accounts_create_task()`.
/// For the simplest API, use `adsenseplatform_platforms_accounts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsenseplatform_platforms_accounts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adsenseplatform_platforms_accounts_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsenseplatform_platforms_accounts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseplatformPlatformsAccountsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: Account,
}

/// GET v1/platforms/{platformsId}/accounts
/// Creates a sub-account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_create_builder()` + `adsenseplatform_platforms_accounts_create_execute()`.
/// For task-level control, use `adsenseplatform_platforms_accounts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_create(
    client: &SimpleHttpClient,
    args: &AdsenseplatformPlatformsAccountsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        adsenseplatform_platforms_accounts_create_builder(client, &args.parent, &args.body)?;
    adsenseplatform_platforms_accounts_create_execute(builder)
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}
/// Gets information about the selected sub-account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_get_execute()` to send, or `adsenseplatform_platforms_accounts_get` for simplest API.

pub fn adsenseplatform_platforms_accounts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adsenseplatform.googleapis.com/v1/platforms/{}/accounts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}
/// Gets information about the selected sub-account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsenseplatform_platforms_accounts_get_execute()` or `adsenseplatform_platforms_accounts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}
/// Gets information about the selected sub-account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsenseplatform_platforms_accounts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsenseplatform_platforms_accounts_get_task()`.
/// For the simplest API, use `adsenseplatform_platforms_accounts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsenseplatform_platforms_accounts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adsenseplatform_platforms_accounts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsenseplatform_platforms_accounts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseplatformPlatformsAccountsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}
/// Gets information about the selected sub-account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_get_builder()` + `adsenseplatform_platforms_accounts_get_execute()`.
/// For task-level control, use `adsenseplatform_platforms_accounts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_get(
    client: &SimpleHttpClient,
    args: &AdsenseplatformPlatformsAccountsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adsenseplatform_platforms_accounts_get_builder(client, &args.name)?;
    adsenseplatform_platforms_accounts_get_execute(builder)
}

/// GET v1/platforms/{platformsId}/accounts
/// Lists a partial view of sub-accounts for a specific parent account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_list_execute()` to send, or `adsenseplatform_platforms_accounts_list` for simplest API.

pub fn adsenseplatform_platforms_accounts_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adsenseplatform.googleapis.com/v1/platforms/{}/accounts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/platforms/{platformsId}/accounts
/// Lists a partial view of sub-accounts for a specific parent account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsenseplatform_platforms_accounts_list_execute()` or `adsenseplatform_platforms_accounts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAccountsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/platforms/{platformsId}/accounts
/// Lists a partial view of sub-accounts for a specific parent account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsenseplatform_platforms_accounts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsenseplatform_platforms_accounts_list_task()`.
/// For the simplest API, use `adsenseplatform_platforms_accounts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsenseplatform_platforms_accounts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adsenseplatform_platforms_accounts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsenseplatform_platforms_accounts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseplatformPlatformsAccountsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/platforms/{platformsId}/accounts
/// Lists a partial view of sub-accounts for a specific parent account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_list_builder()` + `adsenseplatform_platforms_accounts_list_execute()`.
/// For task-level control, use `adsenseplatform_platforms_accounts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_list(
    client: &SimpleHttpClient,
    args: &AdsenseplatformPlatformsAccountsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adsenseplatform_platforms_accounts_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adsenseplatform_platforms_accounts_list_execute(builder)
}

/// GET v1/platforms/{platformsId}/accounts:lookup
/// Looks up information about a sub-account for a specified creation_request_id. If no account exists for the given creation_request_id, returns 404.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_lookup_execute()` to send, or `adsenseplatform_platforms_accounts_lookup` for simplest API.

pub fn adsenseplatform_platforms_accounts_lookup_builder(
    client: &SimpleHttpClient,
    parent: &str,
    creationRequestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adsenseplatform.googleapis.com/v1/platforms/{}/accounts:lookup",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = creationRequestId {
        query_parts.push(format!("creationRequestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/platforms/{platformsId}/accounts:lookup
/// Looks up information about a sub-account for a specified creation_request_id. If no account exists for the given creation_request_id, returns 404.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsenseplatform_platforms_accounts_lookup_execute()` or `adsenseplatform_platforms_accounts_lookup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_lookup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<LookupAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LookupAccountResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/platforms/{platformsId}/accounts:lookup
/// Looks up information about a sub-account for a specified creation_request_id. If no account exists for the given creation_request_id, returns 404.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsenseplatform_platforms_accounts_lookup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsenseplatform_platforms_accounts_lookup_task()`.
/// For the simplest API, use `adsenseplatform_platforms_accounts_lookup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsenseplatform_platforms_accounts_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LookupAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adsenseplatform_platforms_accounts_lookup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsenseplatform_platforms_accounts_lookup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseplatformPlatformsAccountsLookupArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: creationRequestId
    pub creationRequestId: Option<String>,
}

/// GET v1/platforms/{platformsId}/accounts:lookup
/// Looks up information about a sub-account for a specified creation_request_id. If no account exists for the given creation_request_id, returns 404.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_lookup_builder()` + `adsenseplatform_platforms_accounts_lookup_execute()`.
/// For task-level control, use `adsenseplatform_platforms_accounts_lookup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_lookup(
    client: &SimpleHttpClient,
    args: &AdsenseplatformPlatformsAccountsLookupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LookupAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adsenseplatform_platforms_accounts_lookup_builder(
        client,
        &args.parent,
        args.creationRequestId.as_deref(),
    )?;
    adsenseplatform_platforms_accounts_lookup_execute(builder)
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/events
/// Creates an account event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_events_create_execute()` to send, or `adsenseplatform_platforms_accounts_events_create` for simplest API.

pub fn adsenseplatform_platforms_accounts_events_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Event,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adsenseplatform.googleapis.com/v1/platforms/{}/accounts/{}/events",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/events
/// Creates an account event.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsenseplatform_platforms_accounts_events_create_execute()` or `adsenseplatform_platforms_accounts_events_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_events_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_events_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Event = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/events
/// Creates an account event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsenseplatform_platforms_accounts_events_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsenseplatform_platforms_accounts_events_create_task()`.
/// For the simplest API, use `adsenseplatform_platforms_accounts_events_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_events_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsenseplatform_platforms_accounts_events_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adsenseplatform_platforms_accounts_events_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsenseplatform_platforms_accounts_events_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseplatformPlatformsAccountsEventsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: Event,
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/events
/// Creates an account event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_events_create_builder()` + `adsenseplatform_platforms_accounts_events_create_execute()`.
/// For task-level control, use `adsenseplatform_platforms_accounts_events_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_events_create(
    client: &SimpleHttpClient,
    args: &AdsenseplatformPlatformsAccountsEventsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        adsenseplatform_platforms_accounts_events_create_builder(client, &args.parent, &args.body)?;
    adsenseplatform_platforms_accounts_events_create_execute(builder)
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites
/// Creates a site for a specified account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_sites_create_execute()` to send, or `adsenseplatform_platforms_accounts_sites_create` for simplest API.

pub fn adsenseplatform_platforms_accounts_sites_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Site,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adsenseplatform.googleapis.com/v1/platforms/{}/accounts/{}/sites",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites
/// Creates a site for a specified account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsenseplatform_platforms_accounts_sites_create_execute()` or `adsenseplatform_platforms_accounts_sites_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_sites_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Site = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites
/// Creates a site for a specified account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsenseplatform_platforms_accounts_sites_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsenseplatform_platforms_accounts_sites_create_task()`.
/// For the simplest API, use `adsenseplatform_platforms_accounts_sites_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_sites_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsenseplatform_platforms_accounts_sites_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adsenseplatform_platforms_accounts_sites_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsenseplatform_platforms_accounts_sites_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseplatformPlatformsAccountsSitesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: Site,
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites
/// Creates a site for a specified account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_sites_create_builder()` + `adsenseplatform_platforms_accounts_sites_create_execute()`.
/// For task-level control, use `adsenseplatform_platforms_accounts_sites_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_create(
    client: &SimpleHttpClient,
    args: &AdsenseplatformPlatformsAccountsSitesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        adsenseplatform_platforms_accounts_sites_create_builder(client, &args.parent, &args.body)?;
    adsenseplatform_platforms_accounts_sites_create_execute(builder)
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites/{sitesId}
/// Deletes a site from a specified account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_sites_delete_execute()` to send, or `adsenseplatform_platforms_accounts_sites_delete` for simplest API.

pub fn adsenseplatform_platforms_accounts_sites_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adsenseplatform.googleapis.com/v1/platforms/{}/accounts/{}/sites/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites/{sitesId}
/// Deletes a site from a specified account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsenseplatform_platforms_accounts_sites_delete_execute()` or `adsenseplatform_platforms_accounts_sites_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_sites_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites/{sitesId}
/// Deletes a site from a specified account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsenseplatform_platforms_accounts_sites_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsenseplatform_platforms_accounts_sites_delete_task()`.
/// For the simplest API, use `adsenseplatform_platforms_accounts_sites_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_sites_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsenseplatform_platforms_accounts_sites_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adsenseplatform_platforms_accounts_sites_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsenseplatform_platforms_accounts_sites_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseplatformPlatformsAccountsSitesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites/{sitesId}
/// Deletes a site from a specified account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_sites_delete_builder()` + `adsenseplatform_platforms_accounts_sites_delete_execute()`.
/// For task-level control, use `adsenseplatform_platforms_accounts_sites_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_delete(
    client: &SimpleHttpClient,
    args: &AdsenseplatformPlatformsAccountsSitesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adsenseplatform_platforms_accounts_sites_delete_builder(client, &args.name)?;
    adsenseplatform_platforms_accounts_sites_delete_execute(builder)
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites/{sitesId}
/// Gets a site from a specified sub-account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_sites_get_execute()` to send, or `adsenseplatform_platforms_accounts_sites_get` for simplest API.

pub fn adsenseplatform_platforms_accounts_sites_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adsenseplatform.googleapis.com/v1/platforms/{}/accounts/{}/sites/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites/{sitesId}
/// Gets a site from a specified sub-account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsenseplatform_platforms_accounts_sites_get_execute()` or `adsenseplatform_platforms_accounts_sites_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_sites_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Site = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites/{sitesId}
/// Gets a site from a specified sub-account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsenseplatform_platforms_accounts_sites_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsenseplatform_platforms_accounts_sites_get_task()`.
/// For the simplest API, use `adsenseplatform_platforms_accounts_sites_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_sites_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsenseplatform_platforms_accounts_sites_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adsenseplatform_platforms_accounts_sites_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsenseplatform_platforms_accounts_sites_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseplatformPlatformsAccountsSitesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites/{sitesId}
/// Gets a site from a specified sub-account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_sites_get_builder()` + `adsenseplatform_platforms_accounts_sites_get_execute()`.
/// For task-level control, use `adsenseplatform_platforms_accounts_sites_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_get(
    client: &SimpleHttpClient,
    args: &AdsenseplatformPlatformsAccountsSitesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adsenseplatform_platforms_accounts_sites_get_builder(client, &args.name)?;
    adsenseplatform_platforms_accounts_sites_get_execute(builder)
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites
/// Lists sites for a specific account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_sites_list_execute()` to send, or `adsenseplatform_platforms_accounts_sites_list` for simplest API.

pub fn adsenseplatform_platforms_accounts_sites_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adsenseplatform.googleapis.com/v1/platforms/{}/accounts/{}/sites",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites
/// Lists sites for a specific account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsenseplatform_platforms_accounts_sites_list_execute()` or `adsenseplatform_platforms_accounts_sites_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_sites_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSitesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites
/// Lists sites for a specific account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsenseplatform_platforms_accounts_sites_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsenseplatform_platforms_accounts_sites_list_task()`.
/// For the simplest API, use `adsenseplatform_platforms_accounts_sites_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_sites_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsenseplatform_platforms_accounts_sites_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adsenseplatform_platforms_accounts_sites_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsenseplatform_platforms_accounts_sites_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseplatformPlatformsAccountsSitesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites
/// Lists sites for a specific account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_sites_list_builder()` + `adsenseplatform_platforms_accounts_sites_list_execute()`.
/// For task-level control, use `adsenseplatform_platforms_accounts_sites_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_list(
    client: &SimpleHttpClient,
    args: &AdsenseplatformPlatformsAccountsSitesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adsenseplatform_platforms_accounts_sites_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adsenseplatform_platforms_accounts_sites_list_execute(builder)
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites/{sitesId}:requestReview
/// Requests the review of a site. The site should be in REQUIRES_REVIEW or NEEDS_ATTENTION state. Note: Make sure you place an [ad tag](<https://developers.google.`com/adsense/platforms/direct/ad-tags`>) on your site before requesting a review.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsenseplatform_platforms_accounts_sites_request_review_execute()` to send, or `adsenseplatform_platforms_accounts_sites_request_review` for simplest API.

pub fn adsenseplatform_platforms_accounts_sites_request_review_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adsenseplatform.googleapis.com/v1/platforms/{}/accounts/{}/sites/{}:requestReview",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites/{sitesId}:requestReview
/// Requests the review of a site. The site should be in REQUIRES_REVIEW or NEEDS_ATTENTION state. Note: Make sure you place an [ad tag](<https://developers.google.`com/adsense/platforms/direct/ad-tags`>) on your site before requesting a review.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsenseplatform_platforms_accounts_sites_request_review_execute()` or `adsenseplatform_platforms_accounts_sites_request_review`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_sites_request_review_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_request_review_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<RequestSiteReviewResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RequestSiteReviewResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites/{sitesId}:requestReview
/// Requests the review of a site. The site should be in REQUIRES_REVIEW or NEEDS_ATTENTION state. Note: Make sure you place an [ad tag](<https://developers.google.`com/adsense/platforms/direct/ad-tags`>) on your site before requesting a review.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsenseplatform_platforms_accounts_sites_request_review_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsenseplatform_platforms_accounts_sites_request_review_task()`.
/// For the simplest API, use `adsenseplatform_platforms_accounts_sites_request_review()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsenseplatform_platforms_accounts_sites_request_review_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsenseplatform_platforms_accounts_sites_request_review_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RequestSiteReviewResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adsenseplatform_platforms_accounts_sites_request_review_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsenseplatform_platforms_accounts_sites_request_review`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseplatformPlatformsAccountsSitesRequestReviewArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/platforms/{platformsId}/accounts/{accountsId}/sites/{sitesId}:requestReview
/// Requests the review of a site. The site should be in REQUIRES_REVIEW or NEEDS_ATTENTION state. Note: Make sure you place an [ad tag](<https://developers.google.`com/adsense/platforms/direct/ad-tags`>) on your site before requesting a review.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsenseplatform_platforms_accounts_sites_request_review_builder()` + `adsenseplatform_platforms_accounts_sites_request_review_execute()`.
/// For task-level control, use `adsenseplatform_platforms_accounts_sites_request_review_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsenseplatform_platforms_accounts_sites_request_review(
    client: &SimpleHttpClient,
    args: &AdsenseplatformPlatformsAccountsSitesRequestReviewArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RequestSiteReviewResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        adsenseplatform_platforms_accounts_sites_request_review_builder(client, &args.name)?;
    adsenseplatform_platforms_accounts_sites_request_review_execute(builder)
}
