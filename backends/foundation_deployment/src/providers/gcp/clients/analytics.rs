//! Auto-generated API clients for gcp/analytics.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET data/ga
/// Returns Analytics data for a view (profile).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_data_ga_get_execute()` to send, or `analytics_data_ga_get` for simplest API.

pub fn analytics_data_ga_get_builder(
    client: &SimpleHttpClient,
    dimensions: Option<&str>,
    end_date: Option<&str>,
    filters: Option<&str>,
    ids: Option<&str>,
    include_empty_rows: Option<bool>,
    max_results: Option<i32>,
    metrics: Option<&str>,
    output: Option<&str>,
    samplingLevel: Option<&str>,
    segment: Option<&str>,
    sort: Option<&str>,
    start_date: Option<&str>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comdata/ga",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "dimensions",
            dimensions
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "end_date",
            end_date
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filters",
            filters
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "ids",
            ids.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "include_empty_rows",
            include_empty_rows
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "metrics",
            metrics
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "output",
            output
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "samplingLevel",
            samplingLevel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "segment",
            segment
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sort",
            sort.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_date",
            start_date
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET data/ga
/// Returns Analytics data for a view (profile).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_data_ga_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_data_ga_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_data_ga_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GaData>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GaData = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET data/ga
/// Returns Analytics data for a view (profile).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_data_ga_get_builder()` + `analytics_data_ga_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_data_ga_get(
    client: &SimpleHttpClient,
    dimensions: Option<&str>,
    end_date: Option<&str>,
    filters: Option<&str>,
    ids: Option<&str>,
    include_empty_rows: Option<bool>,
    max_results: Option<i32>,
    metrics: Option<&str>,
    output: Option<&str>,
    samplingLevel: Option<&str>,
    segment: Option<&str>,
    sort: Option<&str>,
    start_date: Option<&str>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GaData>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_data_ga_get_builder(
        client,
        dimensions,
        end_date,
        filters,
        ids,
        include_empty_rows,
        max_results,
        metrics,
        output,
        samplingLevel,
        segment,
        sort,
        start_date,
        start_index,
    )?;
    analytics_data_ga_get_execute(builder)
}

/// GET data/mcf
/// Returns Analytics Multi-Channel Funnels data for a view (profile).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_data_mcf_get_execute()` to send, or `analytics_data_mcf_get` for simplest API.

pub fn analytics_data_mcf_get_builder(
    client: &SimpleHttpClient,
    dimensions: Option<&str>,
    end_date: Option<&str>,
    filters: Option<&str>,
    ids: Option<&str>,
    max_results: Option<i32>,
    metrics: Option<&str>,
    samplingLevel: Option<&str>,
    sort: Option<&str>,
    start_date: Option<&str>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comdata/mcf",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "dimensions",
            dimensions
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "end_date",
            end_date
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filters",
            filters
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "ids",
            ids.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "metrics",
            metrics
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "samplingLevel",
            samplingLevel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sort",
            sort.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_date",
            start_date
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET data/mcf
/// Returns Analytics Multi-Channel Funnels data for a view (profile).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_data_mcf_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_data_mcf_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_data_mcf_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<McfData>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: McfData = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET data/mcf
/// Returns Analytics Multi-Channel Funnels data for a view (profile).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_data_mcf_get_builder()` + `analytics_data_mcf_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_data_mcf_get(
    client: &SimpleHttpClient,
    dimensions: Option<&str>,
    end_date: Option<&str>,
    filters: Option<&str>,
    ids: Option<&str>,
    max_results: Option<i32>,
    metrics: Option<&str>,
    samplingLevel: Option<&str>,
    sort: Option<&str>,
    start_date: Option<&str>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<McfData>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_data_mcf_get_builder(
        client,
        dimensions,
        end_date,
        filters,
        ids,
        max_results,
        metrics,
        samplingLevel,
        sort,
        start_date,
        start_index,
    )?;
    analytics_data_mcf_get_execute(builder)
}

/// GET data/realtime
/// Returns real time data for a view (profile).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_data_realtime_get_execute()` to send, or `analytics_data_realtime_get` for simplest API.

pub fn analytics_data_realtime_get_builder(
    client: &SimpleHttpClient,
    dimensions: Option<&str>,
    filters: Option<&str>,
    ids: Option<&str>,
    max_results: Option<i32>,
    metrics: Option<&str>,
    sort: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comdata/realtime",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "dimensions",
            dimensions
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filters",
            filters
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "ids",
            ids.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "metrics",
            metrics
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sort",
            sort.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET data/realtime
/// Returns real time data for a view (profile).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_data_realtime_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_data_realtime_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_data_realtime_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RealtimeData>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RealtimeData = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET data/realtime
/// Returns real time data for a view (profile).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_data_realtime_get_builder()` + `analytics_data_realtime_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_data_realtime_get(
    client: &SimpleHttpClient,
    dimensions: Option<&str>,
    filters: Option<&str>,
    ids: Option<&str>,
    max_results: Option<i32>,
    metrics: Option<&str>,
    sort: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RealtimeData>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_data_realtime_get_builder(
        client,
        dimensions,
        filters,
        ids,
        max_results,
        metrics,
        sort,
    )?;
    analytics_data_realtime_get_execute(builder)
}

/// GET management/accountSummaries
/// Lists account summaries (lightweight tree comprised of `accounts/properties/profiles`) to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_account_summaries_list_execute()` to send, or `analytics_management_account_summaries_list` for simplest API.

pub fn analytics_management_account_summaries_list_builder(
    client: &SimpleHttpClient,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.commanagement/accountSummaries",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accountSummaries
/// Lists account summaries (lightweight tree comprised of `accounts/properties/profiles`) to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_account_summaries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_account_summaries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_account_summaries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountSummaries>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountSummaries = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accountSummaries
/// Lists account summaries (lightweight tree comprised of `accounts/properties/profiles`) to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_account_summaries_list_builder()` + `analytics_management_account_summaries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_account_summaries_list(
    client: &SimpleHttpClient,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountSummaries>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        analytics_management_account_summaries_list_builder(client, max_results, start_index)?;
    analytics_management_account_summaries_list_execute(builder)
}

/// GET management/accounts/{accountId}/entityUserLinks/{linkId}
/// Removes a user from the given account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_account_user_links_delete_execute()` to send, or `analytics_management_account_user_links_delete` for simplest API.

pub fn analytics_management_account_user_links_delete_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/entityUserLinks/{}",
        accountId, linkId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/entityUserLinks/{linkId}
/// Removes a user from the given account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_account_user_links_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_account_user_links_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_account_user_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/entityUserLinks/{linkId}
/// Removes a user from the given account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_account_user_links_delete_builder()` + `analytics_management_account_user_links_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_account_user_links_delete(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        analytics_management_account_user_links_delete_builder(client, accountId, linkId)?;
    analytics_management_account_user_links_delete_execute(builder)
}

/// GET management/accounts/{accountId}/entityUserLinks
/// Adds a new user to the given account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_account_user_links_insert_execute()` to send, or `analytics_management_account_user_links_insert` for simplest API.

pub fn analytics_management_account_user_links_insert_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    body: &EntityUserLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/entityUserLinks",
        accountId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/entityUserLinks
/// Adds a new user to the given account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_account_user_links_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_account_user_links_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_account_user_links_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntityUserLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/entityUserLinks
/// Adds a new user to the given account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_account_user_links_insert_builder()` + `analytics_management_account_user_links_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_account_user_links_insert(
    client: &SimpleHttpClient,
    accountId: &str,
    body: &EntityUserLink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_account_user_links_insert_builder(client, accountId, body)?;
    analytics_management_account_user_links_insert_execute(builder)
}

/// GET management/accounts/{accountId}/entityUserLinks
/// Lists account-user links for a given account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_account_user_links_list_execute()` to send, or `analytics_management_account_user_links_list` for simplest API.

pub fn analytics_management_account_user_links_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/entityUserLinks",
        accountId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accounts/{accountId}/entityUserLinks
/// Lists account-user links for a given account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_account_user_links_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_account_user_links_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_account_user_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLinks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntityUserLinks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/entityUserLinks
/// Lists account-user links for a given account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_account_user_links_list_builder()` + `analytics_management_account_user_links_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_account_user_links_list(
    client: &SimpleHttpClient,
    accountId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLinks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_account_user_links_list_builder(
        client,
        accountId,
        max_results,
        start_index,
    )?;
    analytics_management_account_user_links_list_execute(builder)
}

/// GET management/accounts/{accountId}/entityUserLinks/{linkId}
/// Updates permissions for an existing user on the given account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_account_user_links_update_execute()` to send, or `analytics_management_account_user_links_update` for simplest API.

pub fn analytics_management_account_user_links_update_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    body: &EntityUserLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/entityUserLinks/{}",
        accountId, linkId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/entityUserLinks/{linkId}
/// Updates permissions for an existing user on the given account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_account_user_links_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_account_user_links_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_account_user_links_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntityUserLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/entityUserLinks/{linkId}
/// Updates permissions for an existing user on the given account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_account_user_links_update_builder()` + `analytics_management_account_user_links_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_account_user_links_update(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    body: &EntityUserLink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        analytics_management_account_user_links_update_builder(client, accountId, linkId, body)?;
    analytics_management_account_user_links_update_execute(builder)
}

/// GET management/accounts
/// Lists all accounts to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_accounts_list_execute()` to send, or `analytics_management_accounts_list` for simplest API.

pub fn analytics_management_accounts_list_builder(
    client: &SimpleHttpClient,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.commanagement/accounts",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accounts
/// Lists all accounts to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_accounts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_accounts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_accounts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Accounts>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Accounts = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts
/// Lists all accounts to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_accounts_list_builder()` + `analytics_management_accounts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_accounts_list(
    client: &SimpleHttpClient,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Accounts>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_accounts_list_builder(client, max_results, start_index)?;
    analytics_management_accounts_list_execute(builder)
}

/// GET management/clientId:hashClientId
/// Hashes the given Client ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_client_id_hash_client_id_execute()` to send, or `analytics_management_client_id_hash_client_id` for simplest API.

pub fn analytics_management_client_id_hash_client_id_builder(
    client: &SimpleHttpClient,
    body: &HashClientIdRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.commanagement/clientId:hashClientId",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/clientId:hashClientId
/// Hashes the given Client ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_client_id_hash_client_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_client_id_hash_client_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_client_id_hash_client_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HashClientIdResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HashClientIdResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/clientId:hashClientId
/// Hashes the given Client ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_client_id_hash_client_id_builder()` + `analytics_management_client_id_hash_client_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_client_id_hash_client_id(
    client: &SimpleHttpClient,
    body: &HashClientIdRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HashClientIdResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_client_id_hash_client_id_builder(client, body)?;
    analytics_management_client_id_hash_client_id_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources
/// List custom data sources to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_custom_data_sources_list_execute()` to send, or `analytics_management_custom_data_sources_list` for simplest API.

pub fn analytics_management_custom_data_sources_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/customDataSources",
        accountId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources
/// List custom data sources to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_custom_data_sources_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_custom_data_sources_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_data_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomDataSources>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomDataSources = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources
/// List custom data sources to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_custom_data_sources_list_builder()` + `analytics_management_custom_data_sources_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_data_sources_list(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomDataSources>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_custom_data_sources_list_builder(
        client,
        accountId,
        webPropertyId,
        max_results,
        start_index,
    )?;
    analytics_management_custom_data_sources_list_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions/{customDimensionId}
/// Get a custom dimension to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_custom_dimensions_get_execute()` to send, or `analytics_management_custom_dimensions_get` for simplest API.

pub fn analytics_management_custom_dimensions_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    customDimensionId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/customDimensions/{}",
        accountId, customDimensionId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions/{customDimensionId}
/// Get a custom dimension to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_custom_dimensions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_custom_dimensions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_dimensions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomDimension>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomDimension = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions/{customDimensionId}
/// Get a custom dimension to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_custom_dimensions_get_builder()` + `analytics_management_custom_dimensions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_dimensions_get(
    client: &SimpleHttpClient,
    accountId: &str,
    customDimensionId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomDimension>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_custom_dimensions_get_builder(
        client,
        accountId,
        customDimensionId,
        webPropertyId,
    )?;
    analytics_management_custom_dimensions_get_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions
/// Create a new custom dimension.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_custom_dimensions_insert_execute()` to send, or `analytics_management_custom_dimensions_insert` for simplest API.

pub fn analytics_management_custom_dimensions_insert_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    body: &CustomDimension,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/customDimensions",
        accountId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions
/// Create a new custom dimension.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_custom_dimensions_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_custom_dimensions_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_dimensions_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomDimension>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomDimension = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions
/// Create a new custom dimension.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_custom_dimensions_insert_builder()` + `analytics_management_custom_dimensions_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_dimensions_insert(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    body: &CustomDimension,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomDimension>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_custom_dimensions_insert_builder(
        client,
        accountId,
        webPropertyId,
        body,
    )?;
    analytics_management_custom_dimensions_insert_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions
/// Lists custom dimensions to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_custom_dimensions_list_execute()` to send, or `analytics_management_custom_dimensions_list` for simplest API.

pub fn analytics_management_custom_dimensions_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/customDimensions",
        accountId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions
/// Lists custom dimensions to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_custom_dimensions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_custom_dimensions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_dimensions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomDimensions>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomDimensions = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions
/// Lists custom dimensions to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_custom_dimensions_list_builder()` + `analytics_management_custom_dimensions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_dimensions_list(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomDimensions>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_custom_dimensions_list_builder(
        client,
        accountId,
        webPropertyId,
        max_results,
        start_index,
    )?;
    analytics_management_custom_dimensions_list_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions/{customDimensionId}
/// Updates an existing custom dimension. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_custom_dimensions_patch_execute()` to send, or `analytics_management_custom_dimensions_patch` for simplest API.

pub fn analytics_management_custom_dimensions_patch_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    customDimensionId: &str,
    webPropertyId: &str,
    ignoreCustomDataSourceLinks: Option<bool>,
    body: &CustomDimension,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/customDimensions/{}",
        accountId, customDimensionId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "ignoreCustomDataSourceLinks",
            ignoreCustomDataSourceLinks
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions/{customDimensionId}
/// Updates an existing custom dimension. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_custom_dimensions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_custom_dimensions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_dimensions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomDimension>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomDimension = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions/{customDimensionId}
/// Updates an existing custom dimension. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_custom_dimensions_patch_builder()` + `analytics_management_custom_dimensions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_dimensions_patch(
    client: &SimpleHttpClient,
    accountId: &str,
    customDimensionId: &str,
    webPropertyId: &str,
    ignoreCustomDataSourceLinks: Option<bool>,
    body: &CustomDimension,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomDimension>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_custom_dimensions_patch_builder(
        client,
        accountId,
        customDimensionId,
        webPropertyId,
        ignoreCustomDataSourceLinks,
        body,
    )?;
    analytics_management_custom_dimensions_patch_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions/{customDimensionId}
/// Updates an existing custom dimension.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_custom_dimensions_update_execute()` to send, or `analytics_management_custom_dimensions_update` for simplest API.

pub fn analytics_management_custom_dimensions_update_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    customDimensionId: &str,
    webPropertyId: &str,
    ignoreCustomDataSourceLinks: Option<bool>,
    body: &CustomDimension,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/customDimensions/{}",
        accountId, customDimensionId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "ignoreCustomDataSourceLinks",
            ignoreCustomDataSourceLinks
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions/{customDimensionId}
/// Updates an existing custom dimension.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_custom_dimensions_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_custom_dimensions_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_dimensions_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomDimension>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomDimension = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions/{customDimensionId}
/// Updates an existing custom dimension.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_custom_dimensions_update_builder()` + `analytics_management_custom_dimensions_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_dimensions_update(
    client: &SimpleHttpClient,
    accountId: &str,
    customDimensionId: &str,
    webPropertyId: &str,
    ignoreCustomDataSourceLinks: Option<bool>,
    body: &CustomDimension,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomDimension>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_custom_dimensions_update_builder(
        client,
        accountId,
        customDimensionId,
        webPropertyId,
        ignoreCustomDataSourceLinks,
        body,
    )?;
    analytics_management_custom_dimensions_update_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics/{customMetricId}
/// Get a custom metric to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_custom_metrics_get_execute()` to send, or `analytics_management_custom_metrics_get` for simplest API.

pub fn analytics_management_custom_metrics_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    customMetricId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/customMetrics/{}",
        accountId, customMetricId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics/{customMetricId}
/// Get a custom metric to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_custom_metrics_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_custom_metrics_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_metrics_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomMetric>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomMetric = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics/{customMetricId}
/// Get a custom metric to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_custom_metrics_get_builder()` + `analytics_management_custom_metrics_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_metrics_get(
    client: &SimpleHttpClient,
    accountId: &str,
    customMetricId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomMetric>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_custom_metrics_get_builder(
        client,
        accountId,
        customMetricId,
        webPropertyId,
    )?;
    analytics_management_custom_metrics_get_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics
/// Create a new custom metric.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_custom_metrics_insert_execute()` to send, or `analytics_management_custom_metrics_insert` for simplest API.

pub fn analytics_management_custom_metrics_insert_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    body: &CustomMetric,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/customMetrics",
        accountId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics
/// Create a new custom metric.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_custom_metrics_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_custom_metrics_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_metrics_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomMetric>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomMetric = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics
/// Create a new custom metric.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_custom_metrics_insert_builder()` + `analytics_management_custom_metrics_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_metrics_insert(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    body: &CustomMetric,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomMetric>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        analytics_management_custom_metrics_insert_builder(client, accountId, webPropertyId, body)?;
    analytics_management_custom_metrics_insert_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics
/// Lists custom metrics to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_custom_metrics_list_execute()` to send, or `analytics_management_custom_metrics_list` for simplest API.

pub fn analytics_management_custom_metrics_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/customMetrics",
        accountId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics
/// Lists custom metrics to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_custom_metrics_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_custom_metrics_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_metrics_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomMetrics>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomMetrics = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics
/// Lists custom metrics to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_custom_metrics_list_builder()` + `analytics_management_custom_metrics_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_metrics_list(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomMetrics>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_custom_metrics_list_builder(
        client,
        accountId,
        webPropertyId,
        max_results,
        start_index,
    )?;
    analytics_management_custom_metrics_list_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics/{customMetricId}
/// Updates an existing custom metric. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_custom_metrics_patch_execute()` to send, or `analytics_management_custom_metrics_patch` for simplest API.

pub fn analytics_management_custom_metrics_patch_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    customMetricId: &str,
    webPropertyId: &str,
    ignoreCustomDataSourceLinks: Option<bool>,
    body: &CustomMetric,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/customMetrics/{}",
        accountId, customMetricId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "ignoreCustomDataSourceLinks",
            ignoreCustomDataSourceLinks
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics/{customMetricId}
/// Updates an existing custom metric. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_custom_metrics_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_custom_metrics_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_metrics_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomMetric>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomMetric = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics/{customMetricId}
/// Updates an existing custom metric. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_custom_metrics_patch_builder()` + `analytics_management_custom_metrics_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_metrics_patch(
    client: &SimpleHttpClient,
    accountId: &str,
    customMetricId: &str,
    webPropertyId: &str,
    ignoreCustomDataSourceLinks: Option<bool>,
    body: &CustomMetric,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomMetric>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_custom_metrics_patch_builder(
        client,
        accountId,
        customMetricId,
        webPropertyId,
        ignoreCustomDataSourceLinks,
        body,
    )?;
    analytics_management_custom_metrics_patch_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics/{customMetricId}
/// Updates an existing custom metric.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_custom_metrics_update_execute()` to send, or `analytics_management_custom_metrics_update` for simplest API.

pub fn analytics_management_custom_metrics_update_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    customMetricId: &str,
    webPropertyId: &str,
    ignoreCustomDataSourceLinks: Option<bool>,
    body: &CustomMetric,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/customMetrics/{}",
        accountId, customMetricId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "ignoreCustomDataSourceLinks",
            ignoreCustomDataSourceLinks
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics/{customMetricId}
/// Updates an existing custom metric.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_custom_metrics_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_custom_metrics_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_metrics_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomMetric>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomMetric = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics/{customMetricId}
/// Updates an existing custom metric.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_custom_metrics_update_builder()` + `analytics_management_custom_metrics_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_custom_metrics_update(
    client: &SimpleHttpClient,
    accountId: &str,
    customMetricId: &str,
    webPropertyId: &str,
    ignoreCustomDataSourceLinks: Option<bool>,
    body: &CustomMetric,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomMetric>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_custom_metrics_update_builder(
        client,
        accountId,
        customMetricId,
        webPropertyId,
        ignoreCustomDataSourceLinks,
        body,
    )?;
    analytics_management_custom_metrics_update_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}
/// Delete an experiment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_experiments_delete_execute()` to send, or `analytics_management_experiments_delete` for simplest API.

pub fn analytics_management_experiments_delete_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    experimentId: &str,
    profileId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/experiments/{}",
        accountId, experimentId, profileId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}
/// Delete an experiment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_experiments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_experiments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_experiments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}
/// Delete an experiment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_experiments_delete_builder()` + `analytics_management_experiments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_experiments_delete(
    client: &SimpleHttpClient,
    accountId: &str,
    experimentId: &str,
    profileId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_experiments_delete_builder(
        client,
        accountId,
        experimentId,
        profileId,
        webPropertyId,
    )?;
    analytics_management_experiments_delete_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}
/// Returns an experiment to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_experiments_get_execute()` to send, or `analytics_management_experiments_get` for simplest API.

pub fn analytics_management_experiments_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    experimentId: &str,
    profileId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/experiments/{}",
        accountId, experimentId, profileId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}
/// Returns an experiment to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_experiments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_experiments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_experiments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Experiment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Experiment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}
/// Returns an experiment to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_experiments_get_builder()` + `analytics_management_experiments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_experiments_get(
    client: &SimpleHttpClient,
    accountId: &str,
    experimentId: &str,
    profileId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Experiment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_experiments_get_builder(
        client,
        accountId,
        experimentId,
        profileId,
        webPropertyId,
    )?;
    analytics_management_experiments_get_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments
/// Create a new experiment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_experiments_insert_execute()` to send, or `analytics_management_experiments_insert` for simplest API.

pub fn analytics_management_experiments_insert_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &Experiment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/experiments",
        accountId, profileId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments
/// Create a new experiment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_experiments_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_experiments_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_experiments_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Experiment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Experiment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments
/// Create a new experiment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_experiments_insert_builder()` + `analytics_management_experiments_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_experiments_insert(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &Experiment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Experiment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_experiments_insert_builder(
        client,
        accountId,
        profileId,
        webPropertyId,
        body,
    )?;
    analytics_management_experiments_insert_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments
/// Lists experiments to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_experiments_list_execute()` to send, or `analytics_management_experiments_list` for simplest API.

pub fn analytics_management_experiments_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/experiments",
        accountId, profileId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments
/// Lists experiments to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_experiments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_experiments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_experiments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Experiments>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Experiments = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments
/// Lists experiments to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_experiments_list_builder()` + `analytics_management_experiments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_experiments_list(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Experiments>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_experiments_list_builder(
        client,
        accountId,
        profileId,
        webPropertyId,
        max_results,
        start_index,
    )?;
    analytics_management_experiments_list_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}
/// Update an existing experiment. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_experiments_patch_execute()` to send, or `analytics_management_experiments_patch` for simplest API.

pub fn analytics_management_experiments_patch_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    experimentId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &Experiment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/experiments/{}",
        accountId, experimentId, profileId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}
/// Update an existing experiment. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_experiments_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_experiments_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_experiments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Experiment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Experiment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}
/// Update an existing experiment. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_experiments_patch_builder()` + `analytics_management_experiments_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_experiments_patch(
    client: &SimpleHttpClient,
    accountId: &str,
    experimentId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &Experiment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Experiment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_experiments_patch_builder(
        client,
        accountId,
        experimentId,
        profileId,
        webPropertyId,
        body,
    )?;
    analytics_management_experiments_patch_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}
/// Update an existing experiment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_experiments_update_execute()` to send, or `analytics_management_experiments_update` for simplest API.

pub fn analytics_management_experiments_update_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    experimentId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &Experiment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/experiments/{}",
        accountId, experimentId, profileId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}
/// Update an existing experiment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_experiments_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_experiments_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_experiments_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Experiment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Experiment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}
/// Update an existing experiment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_experiments_update_builder()` + `analytics_management_experiments_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_experiments_update(
    client: &SimpleHttpClient,
    accountId: &str,
    experimentId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &Experiment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Experiment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_experiments_update_builder(
        client,
        accountId,
        experimentId,
        profileId,
        webPropertyId,
        body,
    )?;
    analytics_management_experiments_update_execute(builder)
}

/// GET management/accounts/{accountId}/filters/{filterId}
/// Delete a filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_filters_delete_execute()` to send, or `analytics_management_filters_delete` for simplest API.

pub fn analytics_management_filters_delete_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    filterId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/filters/{}",
        accountId, filterId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/filters/{filterId}
/// Delete a filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_filters_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_filters_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_filters_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Filter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Filter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/filters/{filterId}
/// Delete a filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_filters_delete_builder()` + `analytics_management_filters_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_filters_delete(
    client: &SimpleHttpClient,
    accountId: &str,
    filterId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Filter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_filters_delete_builder(client, accountId, filterId)?;
    analytics_management_filters_delete_execute(builder)
}

/// GET management/accounts/{accountId}/filters/{filterId}
/// Returns filters to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_filters_get_execute()` to send, or `analytics_management_filters_get` for simplest API.

pub fn analytics_management_filters_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    filterId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/filters/{}",
        accountId, filterId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/filters/{filterId}
/// Returns filters to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_filters_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_filters_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_filters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Filter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Filter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/filters/{filterId}
/// Returns filters to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_filters_get_builder()` + `analytics_management_filters_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_filters_get(
    client: &SimpleHttpClient,
    accountId: &str,
    filterId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Filter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_filters_get_builder(client, accountId, filterId)?;
    analytics_management_filters_get_execute(builder)
}

/// GET management/accounts/{accountId}/filters
/// Create a new filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_filters_insert_execute()` to send, or `analytics_management_filters_insert` for simplest API.

pub fn analytics_management_filters_insert_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    body: &Filter,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/filters",
        accountId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/filters
/// Create a new filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_filters_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_filters_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_filters_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Filter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Filter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/filters
/// Create a new filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_filters_insert_builder()` + `analytics_management_filters_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_filters_insert(
    client: &SimpleHttpClient,
    accountId: &str,
    body: &Filter,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Filter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_filters_insert_builder(client, accountId, body)?;
    analytics_management_filters_insert_execute(builder)
}

/// GET management/accounts/{accountId}/filters
/// Lists all filters for an account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_filters_list_execute()` to send, or `analytics_management_filters_list` for simplest API.

pub fn analytics_management_filters_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/filters",
        accountId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accounts/{accountId}/filters
/// Lists all filters for an account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_filters_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_filters_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_filters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Filters>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Filters = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/filters
/// Lists all filters for an account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_filters_list_builder()` + `analytics_management_filters_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_filters_list(
    client: &SimpleHttpClient,
    accountId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Filters>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        analytics_management_filters_list_builder(client, accountId, max_results, start_index)?;
    analytics_management_filters_list_execute(builder)
}

/// GET management/accounts/{accountId}/filters/{filterId}
/// Updates an existing filter. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_filters_patch_execute()` to send, or `analytics_management_filters_patch` for simplest API.

pub fn analytics_management_filters_patch_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    filterId: &str,
    body: &Filter,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/filters/{}",
        accountId, filterId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/filters/{filterId}
/// Updates an existing filter. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_filters_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_filters_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_filters_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Filter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Filter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/filters/{filterId}
/// Updates an existing filter. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_filters_patch_builder()` + `analytics_management_filters_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_filters_patch(
    client: &SimpleHttpClient,
    accountId: &str,
    filterId: &str,
    body: &Filter,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Filter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_filters_patch_builder(client, accountId, filterId, body)?;
    analytics_management_filters_patch_execute(builder)
}

/// GET management/accounts/{accountId}/filters/{filterId}
/// Updates an existing filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_filters_update_execute()` to send, or `analytics_management_filters_update` for simplest API.

pub fn analytics_management_filters_update_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    filterId: &str,
    body: &Filter,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/filters/{}",
        accountId, filterId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/filters/{filterId}
/// Updates an existing filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_filters_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_filters_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_filters_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Filter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Filter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/filters/{filterId}
/// Updates an existing filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_filters_update_builder()` + `analytics_management_filters_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_filters_update(
    client: &SimpleHttpClient,
    accountId: &str,
    filterId: &str,
    body: &Filter,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Filter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_filters_update_builder(client, accountId, filterId, body)?;
    analytics_management_filters_update_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals/{goalId}
/// Gets a goal to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_goals_get_execute()` to send, or `analytics_management_goals_get` for simplest API.

pub fn analytics_management_goals_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    goalId: &str,
    profileId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/goals/{}",
        accountId, goalId, profileId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals/{goalId}
/// Gets a goal to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_goals_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_goals_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_goals_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Goal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Goal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals/{goalId}
/// Gets a goal to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_goals_get_builder()` + `analytics_management_goals_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_goals_get(
    client: &SimpleHttpClient,
    accountId: &str,
    goalId: &str,
    profileId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Goal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_goals_get_builder(
        client,
        accountId,
        goalId,
        profileId,
        webPropertyId,
    )?;
    analytics_management_goals_get_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals
/// Create a new goal.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_goals_insert_execute()` to send, or `analytics_management_goals_insert` for simplest API.

pub fn analytics_management_goals_insert_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &Goal,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/goals",
        accountId, profileId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals
/// Create a new goal.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_goals_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_goals_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_goals_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Goal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Goal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals
/// Create a new goal.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_goals_insert_builder()` + `analytics_management_goals_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_goals_insert(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &Goal,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Goal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_goals_insert_builder(
        client,
        accountId,
        profileId,
        webPropertyId,
        body,
    )?;
    analytics_management_goals_insert_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals
/// Lists goals to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_goals_list_execute()` to send, or `analytics_management_goals_list` for simplest API.

pub fn analytics_management_goals_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/goals",
        accountId, profileId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals
/// Lists goals to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_goals_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_goals_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_goals_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Goals>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Goals = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals
/// Lists goals to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_goals_list_builder()` + `analytics_management_goals_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_goals_list(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Goals>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_goals_list_builder(
        client,
        accountId,
        profileId,
        webPropertyId,
        max_results,
        start_index,
    )?;
    analytics_management_goals_list_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals/{goalId}
/// Updates an existing goal. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_goals_patch_execute()` to send, or `analytics_management_goals_patch` for simplest API.

pub fn analytics_management_goals_patch_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    goalId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &Goal,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/goals/{}",
        accountId, goalId, profileId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals/{goalId}
/// Updates an existing goal. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_goals_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_goals_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_goals_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Goal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Goal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals/{goalId}
/// Updates an existing goal. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_goals_patch_builder()` + `analytics_management_goals_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_goals_patch(
    client: &SimpleHttpClient,
    accountId: &str,
    goalId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &Goal,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Goal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_goals_patch_builder(
        client,
        accountId,
        goalId,
        profileId,
        webPropertyId,
        body,
    )?;
    analytics_management_goals_patch_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals/{goalId}
/// Updates an existing goal.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_goals_update_execute()` to send, or `analytics_management_goals_update` for simplest API.

pub fn analytics_management_goals_update_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    goalId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &Goal,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/goals/{}",
        accountId, goalId, profileId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals/{goalId}
/// Updates an existing goal.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_goals_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_goals_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_goals_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Goal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Goal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals/{goalId}
/// Updates an existing goal.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_goals_update_builder()` + `analytics_management_goals_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_goals_update(
    client: &SimpleHttpClient,
    accountId: &str,
    goalId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &Goal,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Goal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_goals_update_builder(
        client,
        accountId,
        goalId,
        profileId,
        webPropertyId,
        body,
    )?;
    analytics_management_goals_update_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}
/// Delete a profile filter link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_profile_filter_links_delete_execute()` to send, or `analytics_management_profile_filter_links_delete` for simplest API.

pub fn analytics_management_profile_filter_links_delete_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    profileId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/profileFilterLinks/{}",
        accountId,
        linkId,
        profileId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}
/// Delete a profile filter link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_profile_filter_links_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_profile_filter_links_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_filter_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}
/// Delete a profile filter link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_profile_filter_links_delete_builder()` + `analytics_management_profile_filter_links_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_filter_links_delete(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    profileId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_profile_filter_links_delete_builder(
        client,
        accountId,
        linkId,
        profileId,
        webPropertyId,
    )?;
    analytics_management_profile_filter_links_delete_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}
/// Returns a single profile filter link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_profile_filter_links_get_execute()` to send, or `analytics_management_profile_filter_links_get` for simplest API.

pub fn analytics_management_profile_filter_links_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    profileId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/profileFilterLinks/{}",
        accountId,
        linkId,
        profileId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}
/// Returns a single profile filter link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_profile_filter_links_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_profile_filter_links_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_filter_links_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProfileFilterLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProfileFilterLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}
/// Returns a single profile filter link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_profile_filter_links_get_builder()` + `analytics_management_profile_filter_links_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_filter_links_get(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    profileId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProfileFilterLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_profile_filter_links_get_builder(
        client,
        accountId,
        linkId,
        profileId,
        webPropertyId,
    )?;
    analytics_management_profile_filter_links_get_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks
/// Create a new profile filter link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_profile_filter_links_insert_execute()` to send, or `analytics_management_profile_filter_links_insert` for simplest API.

pub fn analytics_management_profile_filter_links_insert_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &ProfileFilterLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/profileFilterLinks",
        accountId,
        profileId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks
/// Create a new profile filter link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_profile_filter_links_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_profile_filter_links_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_filter_links_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProfileFilterLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProfileFilterLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks
/// Create a new profile filter link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_profile_filter_links_insert_builder()` + `analytics_management_profile_filter_links_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_filter_links_insert(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &ProfileFilterLink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProfileFilterLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_profile_filter_links_insert_builder(
        client,
        accountId,
        profileId,
        webPropertyId,
        body,
    )?;
    analytics_management_profile_filter_links_insert_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks
/// Lists all profile filter links for a profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_profile_filter_links_list_execute()` to send, or `analytics_management_profile_filter_links_list` for simplest API.

pub fn analytics_management_profile_filter_links_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/profileFilterLinks",
        accountId,
        profileId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks
/// Lists all profile filter links for a profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_profile_filter_links_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_profile_filter_links_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_filter_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProfileFilterLinks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProfileFilterLinks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks
/// Lists all profile filter links for a profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_profile_filter_links_list_builder()` + `analytics_management_profile_filter_links_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_filter_links_list(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProfileFilterLinks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_profile_filter_links_list_builder(
        client,
        accountId,
        profileId,
        webPropertyId,
        max_results,
        start_index,
    )?;
    analytics_management_profile_filter_links_list_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}
/// Update an existing profile filter link. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_profile_filter_links_patch_execute()` to send, or `analytics_management_profile_filter_links_patch` for simplest API.

pub fn analytics_management_profile_filter_links_patch_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &ProfileFilterLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/profileFilterLinks/{}",
        accountId,
        linkId,
        profileId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}
/// Update an existing profile filter link. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_profile_filter_links_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_profile_filter_links_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_filter_links_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProfileFilterLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProfileFilterLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}
/// Update an existing profile filter link. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_profile_filter_links_patch_builder()` + `analytics_management_profile_filter_links_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_filter_links_patch(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &ProfileFilterLink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProfileFilterLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_profile_filter_links_patch_builder(
        client,
        accountId,
        linkId,
        profileId,
        webPropertyId,
        body,
    )?;
    analytics_management_profile_filter_links_patch_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}
/// Update an existing profile filter link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_profile_filter_links_update_execute()` to send, or `analytics_management_profile_filter_links_update` for simplest API.

pub fn analytics_management_profile_filter_links_update_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &ProfileFilterLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/profileFilterLinks/{}",
        accountId,
        linkId,
        profileId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}
/// Update an existing profile filter link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_profile_filter_links_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_profile_filter_links_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_filter_links_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProfileFilterLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProfileFilterLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}
/// Update an existing profile filter link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_profile_filter_links_update_builder()` + `analytics_management_profile_filter_links_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_filter_links_update(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &ProfileFilterLink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProfileFilterLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_profile_filter_links_update_builder(
        client,
        accountId,
        linkId,
        profileId,
        webPropertyId,
        body,
    )?;
    analytics_management_profile_filter_links_update_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks/{linkId}
/// Removes a user from the given view (profile).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_profile_user_links_delete_execute()` to send, or `analytics_management_profile_user_links_delete` for simplest API.

pub fn analytics_management_profile_user_links_delete_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    profileId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/entityUserLinks/{}",
        accountId,
        linkId,
        profileId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks/{linkId}
/// Removes a user from the given view (profile).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_profile_user_links_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_profile_user_links_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_user_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks/{linkId}
/// Removes a user from the given view (profile).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_profile_user_links_delete_builder()` + `analytics_management_profile_user_links_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_user_links_delete(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    profileId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_profile_user_links_delete_builder(
        client,
        accountId,
        linkId,
        profileId,
        webPropertyId,
    )?;
    analytics_management_profile_user_links_delete_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks
/// Adds a new user to the given view (profile).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_profile_user_links_insert_execute()` to send, or `analytics_management_profile_user_links_insert` for simplest API.

pub fn analytics_management_profile_user_links_insert_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &EntityUserLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/entityUserLinks",
        accountId,
        profileId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks
/// Adds a new user to the given view (profile).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_profile_user_links_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_profile_user_links_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_user_links_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntityUserLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks
/// Adds a new user to the given view (profile).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_profile_user_links_insert_builder()` + `analytics_management_profile_user_links_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_user_links_insert(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &EntityUserLink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_profile_user_links_insert_builder(
        client,
        accountId,
        profileId,
        webPropertyId,
        body,
    )?;
    analytics_management_profile_user_links_insert_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks
/// Lists profile-user links for a given view (profile).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_profile_user_links_list_execute()` to send, or `analytics_management_profile_user_links_list` for simplest API.

pub fn analytics_management_profile_user_links_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/entityUserLinks",
        accountId,
        profileId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks
/// Lists profile-user links for a given view (profile).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_profile_user_links_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_profile_user_links_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_user_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLinks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntityUserLinks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks
/// Lists profile-user links for a given view (profile).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_profile_user_links_list_builder()` + `analytics_management_profile_user_links_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_user_links_list(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLinks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_profile_user_links_list_builder(
        client,
        accountId,
        profileId,
        webPropertyId,
        max_results,
        start_index,
    )?;
    analytics_management_profile_user_links_list_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks/{linkId}
/// Updates permissions for an existing user on the given view (profile).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_profile_user_links_update_execute()` to send, or `analytics_management_profile_user_links_update` for simplest API.

pub fn analytics_management_profile_user_links_update_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &EntityUserLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/entityUserLinks/{}",
        accountId,
        linkId,
        profileId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks/{linkId}
/// Updates permissions for an existing user on the given view (profile).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_profile_user_links_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_profile_user_links_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_user_links_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntityUserLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks/{linkId}
/// Updates permissions for an existing user on the given view (profile).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_profile_user_links_update_builder()` + `analytics_management_profile_user_links_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profile_user_links_update(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &EntityUserLink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_profile_user_links_update_builder(
        client,
        accountId,
        linkId,
        profileId,
        webPropertyId,
        body,
    )?;
    analytics_management_profile_user_links_update_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}
/// Deletes a view (profile).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_profiles_delete_execute()` to send, or `analytics_management_profiles_delete` for simplest API.

pub fn analytics_management_profiles_delete_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}",
        accountId, profileId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}
/// Deletes a view (profile).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_profiles_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_profiles_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}
/// Deletes a view (profile).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_profiles_delete_builder()` + `analytics_management_profiles_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profiles_delete(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        analytics_management_profiles_delete_builder(client, accountId, profileId, webPropertyId)?;
    analytics_management_profiles_delete_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}
/// Gets a view (profile) to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_profiles_get_execute()` to send, or `analytics_management_profiles_get` for simplest API.

pub fn analytics_management_profiles_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}",
        accountId, profileId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}
/// Gets a view (profile) to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_profiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_profiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Profile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}
/// Gets a view (profile) to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_profiles_get_builder()` + `analytics_management_profiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profiles_get(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        analytics_management_profiles_get_builder(client, accountId, profileId, webPropertyId)?;
    analytics_management_profiles_get_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles
/// Create a new view (profile).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_profiles_insert_execute()` to send, or `analytics_management_profiles_insert` for simplest API.

pub fn analytics_management_profiles_insert_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    body: &Profile,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles",
        accountId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles
/// Create a new view (profile).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_profiles_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_profiles_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profiles_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Profile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles
/// Create a new view (profile).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_profiles_insert_builder()` + `analytics_management_profiles_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profiles_insert(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    body: &Profile,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        analytics_management_profiles_insert_builder(client, accountId, webPropertyId, body)?;
    analytics_management_profiles_insert_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles
/// Lists views (profiles) to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_profiles_list_execute()` to send, or `analytics_management_profiles_list` for simplest API.

pub fn analytics_management_profiles_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles",
        accountId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles
/// Lists views (profiles) to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_profiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_profiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profiles>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Profiles = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles
/// Lists views (profiles) to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_profiles_list_builder()` + `analytics_management_profiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profiles_list(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profiles>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_profiles_list_builder(
        client,
        accountId,
        webPropertyId,
        max_results,
        start_index,
    )?;
    analytics_management_profiles_list_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}
/// Updates an existing view (profile). This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_profiles_patch_execute()` to send, or `analytics_management_profiles_patch` for simplest API.

pub fn analytics_management_profiles_patch_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &Profile,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}",
        accountId, profileId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}
/// Updates an existing view (profile). This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_profiles_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_profiles_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profiles_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Profile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}
/// Updates an existing view (profile). This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_profiles_patch_builder()` + `analytics_management_profiles_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profiles_patch(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &Profile,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_profiles_patch_builder(
        client,
        accountId,
        profileId,
        webPropertyId,
        body,
    )?;
    analytics_management_profiles_patch_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}
/// Updates an existing view (profile).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_profiles_update_execute()` to send, or `analytics_management_profiles_update` for simplest API.

pub fn analytics_management_profiles_update_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &Profile,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}",
        accountId, profileId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}
/// Updates an existing view (profile).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_profiles_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_profiles_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profiles_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Profile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}
/// Updates an existing view (profile).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_profiles_update_builder()` + `analytics_management_profiles_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_profiles_update(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &Profile,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_profiles_update_builder(
        client,
        accountId,
        profileId,
        webPropertyId,
        body,
    )?;
    analytics_management_profiles_update_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}
/// Delete a remarketing audience.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_remarketing_audience_delete_execute()` to send, or `analytics_management_remarketing_audience_delete` for simplest API.

pub fn analytics_management_remarketing_audience_delete_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    remarketingAudienceId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/remarketingAudiences/{}",
        accountId, remarketingAudienceId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}
/// Delete a remarketing audience.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_remarketing_audience_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_remarketing_audience_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_remarketing_audience_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}
/// Delete a remarketing audience.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_remarketing_audience_delete_builder()` + `analytics_management_remarketing_audience_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_remarketing_audience_delete(
    client: &SimpleHttpClient,
    accountId: &str,
    remarketingAudienceId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_remarketing_audience_delete_builder(
        client,
        accountId,
        remarketingAudienceId,
        webPropertyId,
    )?;
    analytics_management_remarketing_audience_delete_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}
/// Gets a remarketing audience to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_remarketing_audience_get_execute()` to send, or `analytics_management_remarketing_audience_get` for simplest API.

pub fn analytics_management_remarketing_audience_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    remarketingAudienceId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/remarketingAudiences/{}",
        accountId, remarketingAudienceId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}
/// Gets a remarketing audience to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_remarketing_audience_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_remarketing_audience_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_remarketing_audience_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingAudience>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemarketingAudience = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}
/// Gets a remarketing audience to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_remarketing_audience_get_builder()` + `analytics_management_remarketing_audience_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_remarketing_audience_get(
    client: &SimpleHttpClient,
    accountId: &str,
    remarketingAudienceId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingAudience>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_remarketing_audience_get_builder(
        client,
        accountId,
        remarketingAudienceId,
        webPropertyId,
    )?;
    analytics_management_remarketing_audience_get_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences
/// Creates a new remarketing audience.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_remarketing_audience_insert_execute()` to send, or `analytics_management_remarketing_audience_insert` for simplest API.

pub fn analytics_management_remarketing_audience_insert_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    body: &RemarketingAudience,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/remarketingAudiences",
        accountId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences
/// Creates a new remarketing audience.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_remarketing_audience_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_remarketing_audience_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_remarketing_audience_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingAudience>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemarketingAudience = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences
/// Creates a new remarketing audience.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_remarketing_audience_insert_builder()` + `analytics_management_remarketing_audience_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_remarketing_audience_insert(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    body: &RemarketingAudience,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingAudience>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_remarketing_audience_insert_builder(
        client,
        accountId,
        webPropertyId,
        body,
    )?;
    analytics_management_remarketing_audience_insert_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences
/// Lists remarketing audiences to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_remarketing_audience_list_execute()` to send, or `analytics_management_remarketing_audience_list` for simplest API.

pub fn analytics_management_remarketing_audience_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/remarketingAudiences",
        accountId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "type",
            type_rs
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences
/// Lists remarketing audiences to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_remarketing_audience_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_remarketing_audience_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_remarketing_audience_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingAudiences>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemarketingAudiences = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences
/// Lists remarketing audiences to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_remarketing_audience_list_builder()` + `analytics_management_remarketing_audience_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_remarketing_audience_list(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
    type_rs: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingAudiences>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_remarketing_audience_list_builder(
        client,
        accountId,
        webPropertyId,
        max_results,
        start_index,
        type_rs,
    )?;
    analytics_management_remarketing_audience_list_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}
/// Updates an existing remarketing audience. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_remarketing_audience_patch_execute()` to send, or `analytics_management_remarketing_audience_patch` for simplest API.

pub fn analytics_management_remarketing_audience_patch_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    remarketingAudienceId: &str,
    webPropertyId: &str,
    body: &RemarketingAudience,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/remarketingAudiences/{}",
        accountId, remarketingAudienceId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}
/// Updates an existing remarketing audience. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_remarketing_audience_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_remarketing_audience_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_remarketing_audience_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingAudience>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemarketingAudience = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}
/// Updates an existing remarketing audience. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_remarketing_audience_patch_builder()` + `analytics_management_remarketing_audience_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_remarketing_audience_patch(
    client: &SimpleHttpClient,
    accountId: &str,
    remarketingAudienceId: &str,
    webPropertyId: &str,
    body: &RemarketingAudience,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingAudience>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_remarketing_audience_patch_builder(
        client,
        accountId,
        remarketingAudienceId,
        webPropertyId,
        body,
    )?;
    analytics_management_remarketing_audience_patch_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}
/// Updates an existing remarketing audience.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_remarketing_audience_update_execute()` to send, or `analytics_management_remarketing_audience_update` for simplest API.

pub fn analytics_management_remarketing_audience_update_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    remarketingAudienceId: &str,
    webPropertyId: &str,
    body: &RemarketingAudience,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/remarketingAudiences/{}",
        accountId, remarketingAudienceId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}
/// Updates an existing remarketing audience.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_remarketing_audience_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_remarketing_audience_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_remarketing_audience_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingAudience>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemarketingAudience = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}
/// Updates an existing remarketing audience.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_remarketing_audience_update_builder()` + `analytics_management_remarketing_audience_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_remarketing_audience_update(
    client: &SimpleHttpClient,
    accountId: &str,
    remarketingAudienceId: &str,
    webPropertyId: &str,
    body: &RemarketingAudience,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingAudience>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_remarketing_audience_update_builder(
        client,
        accountId,
        remarketingAudienceId,
        webPropertyId,
        body,
    )?;
    analytics_management_remarketing_audience_update_execute(builder)
}

/// GET management/segments
/// Lists segments to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_segments_list_execute()` to send, or `analytics_management_segments_list` for simplest API.

pub fn analytics_management_segments_list_builder(
    client: &SimpleHttpClient,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.commanagement/segments",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/segments
/// Lists segments to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_segments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_segments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_segments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Segments>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Segments = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/segments
/// Lists segments to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_segments_list_builder()` + `analytics_management_segments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_segments_list(
    client: &SimpleHttpClient,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Segments>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_segments_list_builder(client, max_results, start_index)?;
    analytics_management_segments_list_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports/{unsampledReportId}
/// Deletes an unsampled report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_unsampled_reports_delete_execute()` to send, or `analytics_management_unsampled_reports_delete` for simplest API.

pub fn analytics_management_unsampled_reports_delete_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    unsampledReportId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/unsampledReports/{}",
        accountId,
        profileId,
        unsampledReportId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports/{unsampledReportId}
/// Deletes an unsampled report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_unsampled_reports_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_unsampled_reports_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_unsampled_reports_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports/{unsampledReportId}
/// Deletes an unsampled report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_unsampled_reports_delete_builder()` + `analytics_management_unsampled_reports_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_unsampled_reports_delete(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    unsampledReportId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_unsampled_reports_delete_builder(
        client,
        accountId,
        profileId,
        unsampledReportId,
        webPropertyId,
    )?;
    analytics_management_unsampled_reports_delete_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports/{unsampledReportId}
/// Returns a single unsampled report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_unsampled_reports_get_execute()` to send, or `analytics_management_unsampled_reports_get` for simplest API.

pub fn analytics_management_unsampled_reports_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    unsampledReportId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/unsampledReports/{}",
        accountId,
        profileId,
        unsampledReportId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports/{unsampledReportId}
/// Returns a single unsampled report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_unsampled_reports_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_unsampled_reports_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_unsampled_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnsampledReport>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UnsampledReport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports/{unsampledReportId}
/// Returns a single unsampled report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_unsampled_reports_get_builder()` + `analytics_management_unsampled_reports_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_unsampled_reports_get(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    unsampledReportId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnsampledReport>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_unsampled_reports_get_builder(
        client,
        accountId,
        profileId,
        unsampledReportId,
        webPropertyId,
    )?;
    analytics_management_unsampled_reports_get_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports
/// Create a new unsampled report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_unsampled_reports_insert_execute()` to send, or `analytics_management_unsampled_reports_insert` for simplest API.

pub fn analytics_management_unsampled_reports_insert_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &UnsampledReport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/unsampledReports",
        accountId,
        profileId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports
/// Create a new unsampled report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_unsampled_reports_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_unsampled_reports_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_unsampled_reports_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnsampledReport>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UnsampledReport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports
/// Create a new unsampled report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_unsampled_reports_insert_builder()` + `analytics_management_unsampled_reports_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_unsampled_reports_insert(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    body: &UnsampledReport,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnsampledReport>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_unsampled_reports_insert_builder(
        client,
        accountId,
        profileId,
        webPropertyId,
        body,
    )?;
    analytics_management_unsampled_reports_insert_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports
/// Lists unsampled reports to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_unsampled_reports_list_execute()` to send, or `analytics_management_unsampled_reports_list` for simplest API.

pub fn analytics_management_unsampled_reports_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/profiles/{}/unsampledReports",
        accountId,
        profileId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports
/// Lists unsampled reports to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_unsampled_reports_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_unsampled_reports_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_unsampled_reports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnsampledReports>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UnsampledReports = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports
/// Lists unsampled reports to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_unsampled_reports_list_builder()` + `analytics_management_unsampled_reports_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_unsampled_reports_list(
    client: &SimpleHttpClient,
    accountId: &str,
    profileId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnsampledReports>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_unsampled_reports_list_builder(
        client,
        accountId,
        profileId,
        webPropertyId,
        max_results,
        start_index,
    )?;
    analytics_management_unsampled_reports_list_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/deleteUploadData
/// Delete data associated with a previous upload.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_uploads_delete_upload_data_execute()` to send, or `analytics_management_uploads_delete_upload_data` for simplest API.

pub fn analytics_management_uploads_delete_upload_data_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    customDataSourceId: &str,
    webPropertyId: &str,
    body: &AnalyticsDataimportDeleteUploadDataRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/customDataSources/{}/deleteUploadData",
        accountId,
        customDataSourceId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/deleteUploadData
/// Delete data associated with a previous upload.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_uploads_delete_upload_data_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_uploads_delete_upload_data_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_uploads_delete_upload_data_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/deleteUploadData
/// Delete data associated with a previous upload.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_uploads_delete_upload_data_builder()` + `analytics_management_uploads_delete_upload_data_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_uploads_delete_upload_data(
    client: &SimpleHttpClient,
    accountId: &str,
    customDataSourceId: &str,
    webPropertyId: &str,
    body: &AnalyticsDataimportDeleteUploadDataRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_uploads_delete_upload_data_builder(
        client,
        accountId,
        customDataSourceId,
        webPropertyId,
        body,
    )?;
    analytics_management_uploads_delete_upload_data_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/uploads/{uploadId}
/// List uploads to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_uploads_get_execute()` to send, or `analytics_management_uploads_get` for simplest API.

pub fn analytics_management_uploads_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    customDataSourceId: &str,
    uploadId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/customDataSources/{}/uploads/{}",
        accountId,
        customDataSourceId,
        uploadId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/uploads/{uploadId}
/// List uploads to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_uploads_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_uploads_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_uploads_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Upload>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Upload = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/uploads/{uploadId}
/// List uploads to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_uploads_get_builder()` + `analytics_management_uploads_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_uploads_get(
    client: &SimpleHttpClient,
    accountId: &str,
    customDataSourceId: &str,
    uploadId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Upload>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_uploads_get_builder(
        client,
        accountId,
        customDataSourceId,
        uploadId,
        webPropertyId,
    )?;
    analytics_management_uploads_get_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/uploads
/// List uploads to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_uploads_list_execute()` to send, or `analytics_management_uploads_list` for simplest API.

pub fn analytics_management_uploads_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    customDataSourceId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/customDataSources/{}/uploads",
        accountId,
        customDataSourceId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/uploads
/// List uploads to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_uploads_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_uploads_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_uploads_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Uploads>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Uploads = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/uploads
/// List uploads to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_uploads_list_builder()` + `analytics_management_uploads_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_uploads_list(
    client: &SimpleHttpClient,
    accountId: &str,
    customDataSourceId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Uploads>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_uploads_list_builder(
        client,
        accountId,
        customDataSourceId,
        webPropertyId,
        max_results,
        start_index,
    )?;
    analytics_management_uploads_list_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/uploads
/// Upload data for a custom data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_uploads_upload_data_execute()` to send, or `analytics_management_uploads_upload_data` for simplest API.

pub fn analytics_management_uploads_upload_data_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    customDataSourceId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/customDataSources/{}/uploads",
        accountId,
        customDataSourceId,
        webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/uploads
/// Upload data for a custom data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_uploads_upload_data_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_uploads_upload_data_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_uploads_upload_data_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Upload>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Upload = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/uploads
/// Upload data for a custom data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_uploads_upload_data_builder()` + `analytics_management_uploads_upload_data_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_uploads_upload_data(
    client: &SimpleHttpClient,
    accountId: &str,
    customDataSourceId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Upload>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_uploads_upload_data_builder(
        client,
        accountId,
        customDataSourceId,
        webPropertyId,
    )?;
    analytics_management_uploads_upload_data_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}
/// Deletes a web property-Google Ads link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_web_property_ad_words_links_delete_execute()` to send, or `analytics_management_web_property_ad_words_links_delete` for simplest API.

pub fn analytics_management_web_property_ad_words_links_delete_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyAdWordsLinkId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/entityAdWordsLinks/{}",
        accountId, webPropertyAdWordsLinkId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}
/// Deletes a web property-Google Ads link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_web_property_ad_words_links_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_web_property_ad_words_links_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_web_property_ad_words_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}
/// Deletes a web property-Google Ads link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_web_property_ad_words_links_delete_builder()` + `analytics_management_web_property_ad_words_links_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_web_property_ad_words_links_delete(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyAdWordsLinkId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_web_property_ad_words_links_delete_builder(
        client,
        accountId,
        webPropertyAdWordsLinkId,
        webPropertyId,
    )?;
    analytics_management_web_property_ad_words_links_delete_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}
/// Returns a web property-Google Ads link to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_web_property_ad_words_links_get_execute()` to send, or `analytics_management_web_property_ad_words_links_get` for simplest API.

pub fn analytics_management_web_property_ad_words_links_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyAdWordsLinkId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/entityAdWordsLinks/{}",
        accountId, webPropertyAdWordsLinkId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}
/// Returns a web property-Google Ads link to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_web_property_ad_words_links_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_web_property_ad_words_links_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_web_property_ad_words_links_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityAdWordsLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntityAdWordsLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}
/// Returns a web property-Google Ads link to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_web_property_ad_words_links_get_builder()` + `analytics_management_web_property_ad_words_links_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_web_property_ad_words_links_get(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyAdWordsLinkId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityAdWordsLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_web_property_ad_words_links_get_builder(
        client,
        accountId,
        webPropertyAdWordsLinkId,
        webPropertyId,
    )?;
    analytics_management_web_property_ad_words_links_get_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks
/// Creates a `webProperty`-Google Ads link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_web_property_ad_words_links_insert_execute()` to send, or `analytics_management_web_property_ad_words_links_insert` for simplest API.

pub fn analytics_management_web_property_ad_words_links_insert_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    body: &EntityAdWordsLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/entityAdWordsLinks",
        accountId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks
/// Creates a `webProperty`-Google Ads link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_web_property_ad_words_links_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_web_property_ad_words_links_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_web_property_ad_words_links_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityAdWordsLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntityAdWordsLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks
/// Creates a `webProperty`-Google Ads link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_web_property_ad_words_links_insert_builder()` + `analytics_management_web_property_ad_words_links_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_web_property_ad_words_links_insert(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    body: &EntityAdWordsLink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityAdWordsLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_web_property_ad_words_links_insert_builder(
        client,
        accountId,
        webPropertyId,
        body,
    )?;
    analytics_management_web_property_ad_words_links_insert_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks
/// Lists `webProperty`-Google Ads links for a given web property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_web_property_ad_words_links_list_execute()` to send, or `analytics_management_web_property_ad_words_links_list` for simplest API.

pub fn analytics_management_web_property_ad_words_links_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/entityAdWordsLinks",
        accountId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks
/// Lists `webProperty`-Google Ads links for a given web property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_web_property_ad_words_links_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_web_property_ad_words_links_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_web_property_ad_words_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityAdWordsLinks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntityAdWordsLinks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks
/// Lists `webProperty`-Google Ads links for a given web property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_web_property_ad_words_links_list_builder()` + `analytics_management_web_property_ad_words_links_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_web_property_ad_words_links_list(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityAdWordsLinks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_web_property_ad_words_links_list_builder(
        client,
        accountId,
        webPropertyId,
        max_results,
        start_index,
    )?;
    analytics_management_web_property_ad_words_links_list_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}
/// Updates an existing `webProperty`-Google Ads link. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_web_property_ad_words_links_patch_execute()` to send, or `analytics_management_web_property_ad_words_links_patch` for simplest API.

pub fn analytics_management_web_property_ad_words_links_patch_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyAdWordsLinkId: &str,
    webPropertyId: &str,
    body: &EntityAdWordsLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/entityAdWordsLinks/{}",
        accountId, webPropertyAdWordsLinkId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}
/// Updates an existing `webProperty`-Google Ads link. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_web_property_ad_words_links_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_web_property_ad_words_links_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_web_property_ad_words_links_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityAdWordsLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntityAdWordsLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}
/// Updates an existing `webProperty`-Google Ads link. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_web_property_ad_words_links_patch_builder()` + `analytics_management_web_property_ad_words_links_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_web_property_ad_words_links_patch(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyAdWordsLinkId: &str,
    webPropertyId: &str,
    body: &EntityAdWordsLink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityAdWordsLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_web_property_ad_words_links_patch_builder(
        client,
        accountId,
        webPropertyAdWordsLinkId,
        webPropertyId,
        body,
    )?;
    analytics_management_web_property_ad_words_links_patch_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}
/// Updates an existing `webProperty`-Google Ads link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_web_property_ad_words_links_update_execute()` to send, or `analytics_management_web_property_ad_words_links_update` for simplest API.

pub fn analytics_management_web_property_ad_words_links_update_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyAdWordsLinkId: &str,
    webPropertyId: &str,
    body: &EntityAdWordsLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/entityAdWordsLinks/{}",
        accountId, webPropertyAdWordsLinkId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}
/// Updates an existing `webProperty`-Google Ads link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_web_property_ad_words_links_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_web_property_ad_words_links_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_web_property_ad_words_links_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityAdWordsLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntityAdWordsLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}
/// Updates an existing `webProperty`-Google Ads link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_web_property_ad_words_links_update_builder()` + `analytics_management_web_property_ad_words_links_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_web_property_ad_words_links_update(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyAdWordsLinkId: &str,
    webPropertyId: &str,
    body: &EntityAdWordsLink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityAdWordsLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_web_property_ad_words_links_update_builder(
        client,
        accountId,
        webPropertyAdWordsLinkId,
        webPropertyId,
        body,
    )?;
    analytics_management_web_property_ad_words_links_update_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}
/// Gets a web property to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_webproperties_get_execute()` to send, or `analytics_management_webproperties_get` for simplest API.

pub fn analytics_management_webproperties_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}",
        accountId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}
/// Gets a web property to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_webproperties_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_webproperties_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperties_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Webproperty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Webproperty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}
/// Gets a web property to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_webproperties_get_builder()` + `analytics_management_webproperties_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperties_get(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Webproperty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_webproperties_get_builder(client, accountId, webPropertyId)?;
    analytics_management_webproperties_get_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties
/// Create a new property if the account has fewer than 20 properties. Web properties are visible in the Google Analytics interface only if they have at least one profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_webproperties_insert_execute()` to send, or `analytics_management_webproperties_insert` for simplest API.

pub fn analytics_management_webproperties_insert_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    body: &Webproperty,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties",
        accountId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties
/// Create a new property if the account has fewer than 20 properties. Web properties are visible in the Google Analytics interface only if they have at least one profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_webproperties_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_webproperties_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperties_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Webproperty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Webproperty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties
/// Create a new property if the account has fewer than 20 properties. Web properties are visible in the Google Analytics interface only if they have at least one profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_webproperties_insert_builder()` + `analytics_management_webproperties_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperties_insert(
    client: &SimpleHttpClient,
    accountId: &str,
    body: &Webproperty,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Webproperty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_webproperties_insert_builder(client, accountId, body)?;
    analytics_management_webproperties_insert_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties
/// Lists web properties to which the user has access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_webproperties_list_execute()` to send, or `analytics_management_webproperties_list` for simplest API.

pub fn analytics_management_webproperties_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties",
        accountId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accounts/{accountId}/webproperties
/// Lists web properties to which the user has access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_webproperties_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_webproperties_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperties_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Webproperties>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Webproperties = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties
/// Lists web properties to which the user has access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_webproperties_list_builder()` + `analytics_management_webproperties_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperties_list(
    client: &SimpleHttpClient,
    accountId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Webproperties>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_webproperties_list_builder(
        client,
        accountId,
        max_results,
        start_index,
    )?;
    analytics_management_webproperties_list_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}
/// Updates an existing web property. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_webproperties_patch_execute()` to send, or `analytics_management_webproperties_patch` for simplest API.

pub fn analytics_management_webproperties_patch_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    body: &Webproperty,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}",
        accountId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}
/// Updates an existing web property. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_webproperties_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_webproperties_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperties_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Webproperty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Webproperty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}
/// Updates an existing web property. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_webproperties_patch_builder()` + `analytics_management_webproperties_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperties_patch(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    body: &Webproperty,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Webproperty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        analytics_management_webproperties_patch_builder(client, accountId, webPropertyId, body)?;
    analytics_management_webproperties_patch_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}
/// Updates an existing web property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_webproperties_update_execute()` to send, or `analytics_management_webproperties_update` for simplest API.

pub fn analytics_management_webproperties_update_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    body: &Webproperty,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}",
        accountId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}
/// Updates an existing web property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_webproperties_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_webproperties_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperties_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Webproperty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Webproperty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}
/// Updates an existing web property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_webproperties_update_builder()` + `analytics_management_webproperties_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperties_update(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    body: &Webproperty,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Webproperty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        analytics_management_webproperties_update_builder(client, accountId, webPropertyId, body)?;
    analytics_management_webproperties_update_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks/{linkId}
/// Removes a user from the given web property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_webproperty_user_links_delete_execute()` to send, or `analytics_management_webproperty_user_links_delete` for simplest API.

pub fn analytics_management_webproperty_user_links_delete_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    webPropertyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/entityUserLinks/{}",
        accountId, linkId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks/{linkId}
/// Removes a user from the given web property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_webproperty_user_links_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_webproperty_user_links_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperty_user_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks/{linkId}
/// Removes a user from the given web property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_webproperty_user_links_delete_builder()` + `analytics_management_webproperty_user_links_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperty_user_links_delete(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    webPropertyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_management_webproperty_user_links_delete_builder(
        client,
        accountId,
        linkId,
        webPropertyId,
    )?;
    analytics_management_webproperty_user_links_delete_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks
/// Adds a new user to the given web property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_webproperty_user_links_insert_execute()` to send, or `analytics_management_webproperty_user_links_insert` for simplest API.

pub fn analytics_management_webproperty_user_links_insert_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    body: &EntityUserLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/entityUserLinks",
        accountId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks
/// Adds a new user to the given web property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_webproperty_user_links_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_webproperty_user_links_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperty_user_links_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntityUserLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks
/// Adds a new user to the given web property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_webproperty_user_links_insert_builder()` + `analytics_management_webproperty_user_links_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperty_user_links_insert(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    body: &EntityUserLink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_webproperty_user_links_insert_builder(
        client,
        accountId,
        webPropertyId,
        body,
    )?;
    analytics_management_webproperty_user_links_insert_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks
/// Lists `webProperty`-user links for a given web property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_webproperty_user_links_list_execute()` to send, or `analytics_management_webproperty_user_links_list` for simplest API.

pub fn analytics_management_webproperty_user_links_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/entityUserLinks",
        accountId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "max_results",
            max_results
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start_index",
            start_index
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks
/// Lists `webProperty`-user links for a given web property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_webproperty_user_links_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_webproperty_user_links_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperty_user_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLinks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntityUserLinks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks
/// Lists `webProperty`-user links for a given web property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_webproperty_user_links_list_builder()` + `analytics_management_webproperty_user_links_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperty_user_links_list(
    client: &SimpleHttpClient,
    accountId: &str,
    webPropertyId: &str,
    max_results: Option<i32>,
    start_index: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLinks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_webproperty_user_links_list_builder(
        client,
        accountId,
        webPropertyId,
        max_results,
        start_index,
    )?;
    analytics_management_webproperty_user_links_list_execute(builder)
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks/{linkId}
/// Updates permissions for an existing user on the given web property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_management_webproperty_user_links_update_execute()` to send, or `analytics_management_webproperty_user_links_update` for simplest API.

pub fn analytics_management_webproperty_user_links_update_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    webPropertyId: &str,
    body: &EntityUserLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.commanagement/accounts/{}/webproperties/{}/entityUserLinks/{}",
        accountId, linkId, webPropertyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks/{linkId}
/// Updates permissions for an existing user on the given web property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_management_webproperty_user_links_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_management_webproperty_user_links_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperty_user_links_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntityUserLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks/{linkId}
/// Updates permissions for an existing user on the given web property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_management_webproperty_user_links_update_builder()` + `analytics_management_webproperty_user_links_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_management_webproperty_user_links_update(
    client: &SimpleHttpClient,
    accountId: &str,
    linkId: &str,
    webPropertyId: &str,
    body: &EntityUserLink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntityUserLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_management_webproperty_user_links_update_builder(
        client,
        accountId,
        linkId,
        webPropertyId,
        body,
    )?;
    analytics_management_webproperty_user_links_update_execute(builder)
}

/// GET metadata/{reportType}/columns
/// Lists all columns for a report type
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_metadata_columns_list_execute()` to send, or `analytics_metadata_columns_list` for simplest API.

pub fn analytics_metadata_columns_list_builder(
    client: &SimpleHttpClient,
    reportType: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.commetadata/{}/columns", reportType,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET metadata/{reportType}/columns
/// Lists all columns for a report type
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_metadata_columns_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_metadata_columns_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_metadata_columns_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Columns>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Columns = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET metadata/{reportType}/columns
/// Lists all columns for a report type
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_metadata_columns_list_builder()` + `analytics_metadata_columns_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_metadata_columns_list(
    client: &SimpleHttpClient,
    reportType: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Columns>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analytics_metadata_columns_list_builder(client, reportType)?;
    analytics_metadata_columns_list_execute(builder)
}

/// GET provisioning/createAccountTicket
/// Creates an account ticket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_provisioning_create_account_ticket_execute()` to send, or `analytics_provisioning_create_account_ticket` for simplest API.

pub fn analytics_provisioning_create_account_ticket_builder(
    client: &SimpleHttpClient,
    body: &AccountTicket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprovisioning/createAccountTicket",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET provisioning/createAccountTicket
/// Creates an account ticket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_provisioning_create_account_ticket_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_provisioning_create_account_ticket_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_provisioning_create_account_ticket_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountTicket>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountTicket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET provisioning/createAccountTicket
/// Creates an account ticket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_provisioning_create_account_ticket_builder()` + `analytics_provisioning_create_account_ticket_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_provisioning_create_account_ticket(
    client: &SimpleHttpClient,
    body: &AccountTicket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountTicket>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_provisioning_create_account_ticket_builder(client, body)?;
    analytics_provisioning_create_account_ticket_execute(builder)
}

/// GET provisioning/createAccountTree
/// Provision account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_provisioning_create_account_tree_execute()` to send, or `analytics_provisioning_create_account_tree` for simplest API.

pub fn analytics_provisioning_create_account_tree_builder(
    client: &SimpleHttpClient,
    body: &AccountTreeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprovisioning/createAccountTree",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET provisioning/createAccountTree
/// Provision account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_provisioning_create_account_tree_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_provisioning_create_account_tree_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_provisioning_create_account_tree_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountTreeResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountTreeResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET provisioning/createAccountTree
/// Provision account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_provisioning_create_account_tree_builder()` + `analytics_provisioning_create_account_tree_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_provisioning_create_account_tree(
    client: &SimpleHttpClient,
    body: &AccountTreeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountTreeResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_provisioning_create_account_tree_builder(client, body)?;
    analytics_provisioning_create_account_tree_execute(builder)
}

/// GET userDeletion/userDeletionRequests:upsert
/// Insert or update a user deletion requests.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analytics_user_deletion_user_deletion_request_upsert_execute()` to send, or `analytics_user_deletion_user_deletion_request_upsert` for simplest API.

pub fn analytics_user_deletion_user_deletion_request_upsert_builder(
    client: &SimpleHttpClient,
    body: &UserDeletionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comuserDeletion/userDeletionRequests:upsert",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET userDeletion/userDeletionRequests:upsert
/// Insert or update a user deletion requests.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analytics_user_deletion_user_deletion_request_upsert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analytics_user_deletion_user_deletion_request_upsert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_user_deletion_user_deletion_request_upsert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserDeletionRequest>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserDeletionRequest = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET userDeletion/userDeletionRequests:upsert
/// Insert or update a user deletion requests.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analytics_user_deletion_user_deletion_request_upsert_builder()` + `analytics_user_deletion_user_deletion_request_upsert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analytics_user_deletion_user_deletion_request_upsert(
    client: &SimpleHttpClient,
    body: &UserDeletionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserDeletionRequest>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analytics_user_deletion_user_deletion_request_upsert_builder(client, body)?;
    analytics_user_deletion_user_deletion_request_upsert_execute(builder)
}
