//! Auto-generated API clients for gcp/indexing.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v3/urlNotifications/metadata
/// Gets metadata about a Web Document. This method can _only_ be used to query URLs that were previously seen in successful Indexing API notifications. Includes the latest UrlNotification received via this API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `indexing_url_notifications_get_metadata_execute()` to send, or `indexing_url_notifications_get_metadata` for simplest API.

pub fn indexing_url_notifications_get_metadata_builder(
    client: &SimpleHttpClient,
    url: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/urlNotifications/metadata",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "url",
            url.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/urlNotifications/metadata
/// Gets metadata about a Web Document. This method can _only_ be used to query URLs that were previously seen in successful Indexing API notifications. Includes the latest UrlNotification received via this API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `indexing_url_notifications_get_metadata_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from indexing_url_notifications_get_metadata_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn indexing_url_notifications_get_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlNotificationMetadata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UrlNotificationMetadata = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/urlNotifications/metadata
/// Gets metadata about a Web Document. This method can _only_ be used to query URLs that were previously seen in successful Indexing API notifications. Includes the latest UrlNotification received via this API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `indexing_url_notifications_get_metadata_builder()` + `indexing_url_notifications_get_metadata_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn indexing_url_notifications_get_metadata(
    client: &SimpleHttpClient,
    url: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlNotificationMetadata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = indexing_url_notifications_get_metadata_builder(client, url)?;
    indexing_url_notifications_get_metadata_execute(builder)
}

/// GET v3/urlNotifications:publish
/// Notifies that a URL has been updated or deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `indexing_url_notifications_publish_execute()` to send, or `indexing_url_notifications_publish` for simplest API.

pub fn indexing_url_notifications_publish_builder(
    client: &SimpleHttpClient,
    body: &UrlNotification,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/urlNotifications:publish",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/urlNotifications:publish
/// Notifies that a URL has been updated or deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `indexing_url_notifications_publish_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from indexing_url_notifications_publish_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn indexing_url_notifications_publish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PublishUrlNotificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublishUrlNotificationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/urlNotifications:publish
/// Notifies that a URL has been updated or deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `indexing_url_notifications_publish_builder()` + `indexing_url_notifications_publish_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn indexing_url_notifications_publish(
    client: &SimpleHttpClient,
    body: &UrlNotification,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PublishUrlNotificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = indexing_url_notifications_publish_builder(client, body)?;
    indexing_url_notifications_publish_execute(builder)
}
