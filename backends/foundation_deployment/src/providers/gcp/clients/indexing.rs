//! Auto-generated API clients for gcp/indexing.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v3/urlNotifications/metadata
/// Gets metadata about a Web Document. This method can _only_ be used to query URLs that were previously seen in successful Indexing API notifications. Includes the latest UrlNotification received via this API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `indexing_url_notifications_get_metadata_execute()` to send, or `indexing_url_notifications_get_metadata` for simplest API.

pub fn indexing_url_notifications_get_metadata_builder(
    client: &SimpleHttpClient,
    url: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://indexing.googleapis.com/v3/urlNotifications/metadata",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = url.as_ref() {
        query_parts.push(format!("url={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/urlNotifications/metadata
/// Gets metadata about a Web Document. This method can _only_ be used to query URLs that were previously seen in successful Indexing API notifications. Includes the latest UrlNotification received via this API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `indexing_url_notifications_get_metadata_execute()` or `indexing_url_notifications_get_metadata`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `indexing_url_notifications_get_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn indexing_url_notifications_get_metadata_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UrlNotificationMetadata>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UrlNotificationMetadata = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/urlNotifications/metadata
/// Gets metadata about a Web Document. This method can _only_ be used to query URLs that were previously seen in successful Indexing API notifications. Includes the latest UrlNotification received via this API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `indexing_url_notifications_get_metadata_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `indexing_url_notifications_get_metadata_task()`.
/// For the simplest API, use `indexing_url_notifications_get_metadata()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `indexing_url_notifications_get_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn indexing_url_notifications_get_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlNotificationMetadata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = indexing_url_notifications_get_metadata_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`indexing_url_notifications_get_metadata`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IndexingUrlNotificationsGetMetadataArgs {
    /// Query parameter: url
    pub url: Option<Option<String>>,
}

/// GET v3/urlNotifications/metadata
/// Gets metadata about a Web Document. This method can _only_ be used to query URLs that were previously seen in successful Indexing API notifications. Includes the latest UrlNotification received via this API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `indexing_url_notifications_get_metadata_builder()` + `indexing_url_notifications_get_metadata_execute()`.
/// For task-level control, use `indexing_url_notifications_get_metadata_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn indexing_url_notifications_get_metadata(
    client: &SimpleHttpClient,
    args: &IndexingUrlNotificationsGetMetadataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlNotificationMetadata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = indexing_url_notifications_get_metadata_builder(client, &args.url)?;
    indexing_url_notifications_get_metadata_execute(builder)
}

/// POST v3/urlNotifications:publish
/// Notifies that a URL has been updated or deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `indexing_url_notifications_publish_execute()` to send, or `indexing_url_notifications_publish` for simplest API.

pub fn indexing_url_notifications_publish_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://indexing.googleapis.com/v3/urlNotifications:publish",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/urlNotifications:publish
/// Notifies that a URL has been updated or deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `indexing_url_notifications_publish_execute()` or `indexing_url_notifications_publish`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `indexing_url_notifications_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn indexing_url_notifications_publish_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublishUrlNotificationResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublishUrlNotificationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/urlNotifications:publish
/// Notifies that a URL has been updated or deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `indexing_url_notifications_publish_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `indexing_url_notifications_publish_task()`.
/// For the simplest API, use `indexing_url_notifications_publish()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `indexing_url_notifications_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn indexing_url_notifications_publish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PublishUrlNotificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = indexing_url_notifications_publish_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v3/urlNotifications:publish
/// Notifies that a URL has been updated or deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `indexing_url_notifications_publish_builder()` + `indexing_url_notifications_publish_execute()`.
/// For task-level control, use `indexing_url_notifications_publish_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn indexing_url_notifications_publish(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PublishUrlNotificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = indexing_url_notifications_publish_builder(client)?;
    indexing_url_notifications_publish_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for UrlNotificationMetadata
// =============================================================================

/// ResourceIdentifier implementation for UrlNotificationMetadata with IndexingUrlNotificationsGetMetadataArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IndexingUrlNotificationsGetMetadataArgs> for UrlNotificationMetadata {
    fn generate_resource_id(&self, input: &IndexingUrlNotificationsGetMetadataArgs) -> String {
        "gcp::indexing::UrlNotificationMetadata".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::indexing::UrlNotificationMetadata"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PublishUrlNotificationResponse
// =============================================================================

/// ResourceIdentifier implementation for PublishUrlNotificationResponse with IndexingUrlNotificationsPublishArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IndexingUrlNotificationsPublishArgs> for PublishUrlNotificationResponse {
    fn generate_resource_id(&self, input: &IndexingUrlNotificationsPublishArgs) -> String {
        "gcp::indexing::PublishUrlNotificationResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::indexing::PublishUrlNotificationResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
