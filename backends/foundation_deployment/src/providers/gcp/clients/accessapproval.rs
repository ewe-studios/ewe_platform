//! Auto-generated API clients for gcp/accessapproval.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/folders/{foldersId}/accessApprovalSettings
/// Deletes the settings associated with a project, folder, or organization. This will have the effect of disabling Access Approval for the resource. Access Approval may remain active based on parent resource settings. To confirm the effective settings, call GetAccessApprovalSettings and verify effective setting is disabled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `accessapproval_folders_delete_access_approval_settings_execute()` to send, or `accessapproval_folders_delete_access_approval_settings` for simplest API.

pub fn accessapproval_folders_delete_access_approval_settings_builder(
    client: &SimpleHttpClient,
    name: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://accessapproval.googleapis.com/v1/folders/{}/accessApprovalSettings",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/accessApprovalSettings
/// Deletes the settings associated with a project, folder, or organization. This will have the effect of disabling Access Approval for the resource. Access Approval may remain active based on parent resource settings. To confirm the effective settings, call GetAccessApprovalSettings and verify effective setting is disabled.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `accessapproval_folders_delete_access_approval_settings_execute()` or `accessapproval_folders_delete_access_approval_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_folders_delete_access_approval_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_folders_delete_access_approval_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/accessApprovalSettings
/// Deletes the settings associated with a project, folder, or organization. This will have the effect of disabling Access Approval for the resource. Access Approval may remain active based on parent resource settings. To confirm the effective settings, call GetAccessApprovalSettings and verify effective setting is disabled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `accessapproval_folders_delete_access_approval_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `accessapproval_folders_delete_access_approval_settings_task()`.
/// For the simplest API, use `accessapproval_folders_delete_access_approval_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_folders_delete_access_approval_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn accessapproval_folders_delete_access_approval_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = accessapproval_folders_delete_access_approval_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`accessapproval_folders_delete_access_approval_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AccessapprovalFoldersDeleteAccessApprovalSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/folders/{foldersId}/accessApprovalSettings
/// Deletes the settings associated with a project, folder, or organization. This will have the effect of disabling Access Approval for the resource. Access Approval may remain active based on parent resource settings. To confirm the effective settings, call GetAccessApprovalSettings and verify effective setting is disabled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `accessapproval_folders_delete_access_approval_settings_builder()` + `accessapproval_folders_delete_access_approval_settings_execute()`.
/// For task-level control, use `accessapproval_folders_delete_access_approval_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_folders_delete_access_approval_settings(
    client: &SimpleHttpClient,
    args: &AccessapprovalFoldersDeleteAccessApprovalSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        accessapproval_folders_delete_access_approval_settings_builder(client, args.name.clone())?;
    accessapproval_folders_delete_access_approval_settings_execute(builder)
}

/// GET v1/folders/{foldersId}/serviceAccount
/// Retrieves the service account that is used by Access Approval to access KMS keys for signing approved approval requests.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `accessapproval_folders_get_service_account_execute()` to send, or `accessapproval_folders_get_service_account` for simplest API.

pub fn accessapproval_folders_get_service_account_builder(
    client: &SimpleHttpClient,
    name: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://accessapproval.googleapis.com/v1/folders/{}/serviceAccount",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/serviceAccount
/// Retrieves the service account that is used by Access Approval to access KMS keys for signing approved approval requests.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `accessapproval_folders_get_service_account_execute()` or `accessapproval_folders_get_service_account`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_folders_get_service_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_folders_get_service_account_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccessApprovalServiceAccount>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessApprovalServiceAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/serviceAccount
/// Retrieves the service account that is used by Access Approval to access KMS keys for signing approved approval requests.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `accessapproval_folders_get_service_account_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `accessapproval_folders_get_service_account_task()`.
/// For the simplest API, use `accessapproval_folders_get_service_account()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_folders_get_service_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn accessapproval_folders_get_service_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessApprovalServiceAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = accessapproval_folders_get_service_account_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`accessapproval_folders_get_service_account`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AccessapprovalFoldersGetServiceAccountArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/folders/{foldersId}/serviceAccount
/// Retrieves the service account that is used by Access Approval to access KMS keys for signing approved approval requests.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `accessapproval_folders_get_service_account_builder()` + `accessapproval_folders_get_service_account_execute()`.
/// For task-level control, use `accessapproval_folders_get_service_account_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_folders_get_service_account(
    client: &SimpleHttpClient,
    args: &AccessapprovalFoldersGetServiceAccountArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessApprovalServiceAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = accessapproval_folders_get_service_account_builder(client, args.name.clone())?;
    accessapproval_folders_get_service_account_execute(builder)
}

/// GET v1/folders/{foldersId}/approvalRequests
/// Lists approval requests associated with a project, folder, or organization. Approval requests can be filtered by state (pending, active, dismissed). The order is reverse chronological.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `accessapproval_folders_approval_requests_list_execute()` to send, or `accessapproval_folders_approval_requests_list` for simplest API.

pub fn accessapproval_folders_approval_requests_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    filter: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://accessapproval.googleapis.com/v1/folders/{}/approvalRequests",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/approvalRequests
/// Lists approval requests associated with a project, folder, or organization. Approval requests can be filtered by state (pending, active, dismissed). The order is reverse chronological.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `accessapproval_folders_approval_requests_list_execute()` or `accessapproval_folders_approval_requests_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_folders_approval_requests_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_folders_approval_requests_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListApprovalRequestsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListApprovalRequestsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/approvalRequests
/// Lists approval requests associated with a project, folder, or organization. Approval requests can be filtered by state (pending, active, dismissed). The order is reverse chronological.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `accessapproval_folders_approval_requests_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `accessapproval_folders_approval_requests_list_task()`.
/// For the simplest API, use `accessapproval_folders_approval_requests_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_folders_approval_requests_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn accessapproval_folders_approval_requests_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListApprovalRequestsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = accessapproval_folders_approval_requests_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`accessapproval_folders_approval_requests_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AccessapprovalFoldersApprovalRequestsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/approvalRequests
/// Lists approval requests associated with a project, folder, or organization. Approval requests can be filtered by state (pending, active, dismissed). The order is reverse chronological.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `accessapproval_folders_approval_requests_list_builder()` + `accessapproval_folders_approval_requests_list_execute()`.
/// For task-level control, use `accessapproval_folders_approval_requests_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_folders_approval_requests_list(
    client: &SimpleHttpClient,
    args: &AccessapprovalFoldersApprovalRequestsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListApprovalRequestsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = accessapproval_folders_approval_requests_list_builder(
        client,
        args.parent.clone(),
        args.filter.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    accessapproval_folders_approval_requests_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/accessApprovalSettings
/// Deletes the settings associated with a project, folder, or organization. This will have the effect of disabling Access Approval for the resource. Access Approval may remain active based on parent resource settings. To confirm the effective settings, call GetAccessApprovalSettings and verify effective setting is disabled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `accessapproval_organizations_delete_access_approval_settings_execute()` to send, or `accessapproval_organizations_delete_access_approval_settings` for simplest API.

pub fn accessapproval_organizations_delete_access_approval_settings_builder(
    client: &SimpleHttpClient,
    name: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://accessapproval.googleapis.com/v1/organizations/{}/accessApprovalSettings",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/accessApprovalSettings
/// Deletes the settings associated with a project, folder, or organization. This will have the effect of disabling Access Approval for the resource. Access Approval may remain active based on parent resource settings. To confirm the effective settings, call GetAccessApprovalSettings and verify effective setting is disabled.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `accessapproval_organizations_delete_access_approval_settings_execute()` or `accessapproval_organizations_delete_access_approval_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_organizations_delete_access_approval_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_organizations_delete_access_approval_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/accessApprovalSettings
/// Deletes the settings associated with a project, folder, or organization. This will have the effect of disabling Access Approval for the resource. Access Approval may remain active based on parent resource settings. To confirm the effective settings, call GetAccessApprovalSettings and verify effective setting is disabled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `accessapproval_organizations_delete_access_approval_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `accessapproval_organizations_delete_access_approval_settings_task()`.
/// For the simplest API, use `accessapproval_organizations_delete_access_approval_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_organizations_delete_access_approval_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn accessapproval_organizations_delete_access_approval_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = accessapproval_organizations_delete_access_approval_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`accessapproval_organizations_delete_access_approval_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AccessapprovalOrganizationsDeleteAccessApprovalSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/accessApprovalSettings
/// Deletes the settings associated with a project, folder, or organization. This will have the effect of disabling Access Approval for the resource. Access Approval may remain active based on parent resource settings. To confirm the effective settings, call GetAccessApprovalSettings and verify effective setting is disabled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `accessapproval_organizations_delete_access_approval_settings_builder()` + `accessapproval_organizations_delete_access_approval_settings_execute()`.
/// For task-level control, use `accessapproval_organizations_delete_access_approval_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_organizations_delete_access_approval_settings(
    client: &SimpleHttpClient,
    args: &AccessapprovalOrganizationsDeleteAccessApprovalSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = accessapproval_organizations_delete_access_approval_settings_builder(
        client,
        args.name.clone(),
    )?;
    accessapproval_organizations_delete_access_approval_settings_execute(builder)
}

/// GET v1/organizations/{organizationsId}/serviceAccount
/// Retrieves the service account that is used by Access Approval to access KMS keys for signing approved approval requests.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `accessapproval_organizations_get_service_account_execute()` to send, or `accessapproval_organizations_get_service_account` for simplest API.

pub fn accessapproval_organizations_get_service_account_builder(
    client: &SimpleHttpClient,
    name: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://accessapproval.googleapis.com/v1/organizations/{}/serviceAccount",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/serviceAccount
/// Retrieves the service account that is used by Access Approval to access KMS keys for signing approved approval requests.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `accessapproval_organizations_get_service_account_execute()` or `accessapproval_organizations_get_service_account`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_organizations_get_service_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_organizations_get_service_account_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccessApprovalServiceAccount>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessApprovalServiceAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/serviceAccount
/// Retrieves the service account that is used by Access Approval to access KMS keys for signing approved approval requests.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `accessapproval_organizations_get_service_account_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `accessapproval_organizations_get_service_account_task()`.
/// For the simplest API, use `accessapproval_organizations_get_service_account()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_organizations_get_service_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn accessapproval_organizations_get_service_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessApprovalServiceAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = accessapproval_organizations_get_service_account_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`accessapproval_organizations_get_service_account`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AccessapprovalOrganizationsGetServiceAccountArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/serviceAccount
/// Retrieves the service account that is used by Access Approval to access KMS keys for signing approved approval requests.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `accessapproval_organizations_get_service_account_builder()` + `accessapproval_organizations_get_service_account_execute()`.
/// For task-level control, use `accessapproval_organizations_get_service_account_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_organizations_get_service_account(
    client: &SimpleHttpClient,
    args: &AccessapprovalOrganizationsGetServiceAccountArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessApprovalServiceAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        accessapproval_organizations_get_service_account_builder(client, args.name.clone())?;
    accessapproval_organizations_get_service_account_execute(builder)
}

/// GET v1/organizations/{organizationsId}/approvalRequests
/// Lists approval requests associated with a project, folder, or organization. Approval requests can be filtered by state (pending, active, dismissed). The order is reverse chronological.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `accessapproval_organizations_approval_requests_list_execute()` to send, or `accessapproval_organizations_approval_requests_list` for simplest API.

pub fn accessapproval_organizations_approval_requests_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    filter: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://accessapproval.googleapis.com/v1/organizations/{}/approvalRequests",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/approvalRequests
/// Lists approval requests associated with a project, folder, or organization. Approval requests can be filtered by state (pending, active, dismissed). The order is reverse chronological.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `accessapproval_organizations_approval_requests_list_execute()` or `accessapproval_organizations_approval_requests_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_organizations_approval_requests_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_organizations_approval_requests_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListApprovalRequestsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListApprovalRequestsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/approvalRequests
/// Lists approval requests associated with a project, folder, or organization. Approval requests can be filtered by state (pending, active, dismissed). The order is reverse chronological.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `accessapproval_organizations_approval_requests_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `accessapproval_organizations_approval_requests_list_task()`.
/// For the simplest API, use `accessapproval_organizations_approval_requests_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_organizations_approval_requests_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn accessapproval_organizations_approval_requests_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListApprovalRequestsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = accessapproval_organizations_approval_requests_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`accessapproval_organizations_approval_requests_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AccessapprovalOrganizationsApprovalRequestsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/approvalRequests
/// Lists approval requests associated with a project, folder, or organization. Approval requests can be filtered by state (pending, active, dismissed). The order is reverse chronological.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `accessapproval_organizations_approval_requests_list_builder()` + `accessapproval_organizations_approval_requests_list_execute()`.
/// For task-level control, use `accessapproval_organizations_approval_requests_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_organizations_approval_requests_list(
    client: &SimpleHttpClient,
    args: &AccessapprovalOrganizationsApprovalRequestsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListApprovalRequestsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = accessapproval_organizations_approval_requests_list_builder(
        client,
        args.parent.clone(),
        args.filter.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    accessapproval_organizations_approval_requests_list_execute(builder)
}

/// GET v1/projects/{projectsId}/accessApprovalSettings
/// Deletes the settings associated with a project, folder, or organization. This will have the effect of disabling Access Approval for the resource. Access Approval may remain active based on parent resource settings. To confirm the effective settings, call GetAccessApprovalSettings and verify effective setting is disabled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `accessapproval_projects_delete_access_approval_settings_execute()` to send, or `accessapproval_projects_delete_access_approval_settings` for simplest API.

pub fn accessapproval_projects_delete_access_approval_settings_builder(
    client: &SimpleHttpClient,
    name: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://accessapproval.googleapis.com/v1/projects/{}/accessApprovalSettings",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/accessApprovalSettings
/// Deletes the settings associated with a project, folder, or organization. This will have the effect of disabling Access Approval for the resource. Access Approval may remain active based on parent resource settings. To confirm the effective settings, call GetAccessApprovalSettings and verify effective setting is disabled.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `accessapproval_projects_delete_access_approval_settings_execute()` or `accessapproval_projects_delete_access_approval_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_projects_delete_access_approval_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_projects_delete_access_approval_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/accessApprovalSettings
/// Deletes the settings associated with a project, folder, or organization. This will have the effect of disabling Access Approval for the resource. Access Approval may remain active based on parent resource settings. To confirm the effective settings, call GetAccessApprovalSettings and verify effective setting is disabled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `accessapproval_projects_delete_access_approval_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `accessapproval_projects_delete_access_approval_settings_task()`.
/// For the simplest API, use `accessapproval_projects_delete_access_approval_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_projects_delete_access_approval_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn accessapproval_projects_delete_access_approval_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = accessapproval_projects_delete_access_approval_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`accessapproval_projects_delete_access_approval_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AccessapprovalProjectsDeleteAccessApprovalSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/accessApprovalSettings
/// Deletes the settings associated with a project, folder, or organization. This will have the effect of disabling Access Approval for the resource. Access Approval may remain active based on parent resource settings. To confirm the effective settings, call GetAccessApprovalSettings and verify effective setting is disabled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `accessapproval_projects_delete_access_approval_settings_builder()` + `accessapproval_projects_delete_access_approval_settings_execute()`.
/// For task-level control, use `accessapproval_projects_delete_access_approval_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_projects_delete_access_approval_settings(
    client: &SimpleHttpClient,
    args: &AccessapprovalProjectsDeleteAccessApprovalSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        accessapproval_projects_delete_access_approval_settings_builder(client, args.name.clone())?;
    accessapproval_projects_delete_access_approval_settings_execute(builder)
}

/// GET v1/projects/{projectsId}/serviceAccount
/// Retrieves the service account that is used by Access Approval to access KMS keys for signing approved approval requests.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `accessapproval_projects_get_service_account_execute()` to send, or `accessapproval_projects_get_service_account` for simplest API.

pub fn accessapproval_projects_get_service_account_builder(
    client: &SimpleHttpClient,
    name: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://accessapproval.googleapis.com/v1/projects/{}/serviceAccount",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/serviceAccount
/// Retrieves the service account that is used by Access Approval to access KMS keys for signing approved approval requests.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `accessapproval_projects_get_service_account_execute()` or `accessapproval_projects_get_service_account`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_projects_get_service_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_projects_get_service_account_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccessApprovalServiceAccount>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessApprovalServiceAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/serviceAccount
/// Retrieves the service account that is used by Access Approval to access KMS keys for signing approved approval requests.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `accessapproval_projects_get_service_account_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `accessapproval_projects_get_service_account_task()`.
/// For the simplest API, use `accessapproval_projects_get_service_account()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_projects_get_service_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn accessapproval_projects_get_service_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessApprovalServiceAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = accessapproval_projects_get_service_account_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`accessapproval_projects_get_service_account`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AccessapprovalProjectsGetServiceAccountArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/serviceAccount
/// Retrieves the service account that is used by Access Approval to access KMS keys for signing approved approval requests.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `accessapproval_projects_get_service_account_builder()` + `accessapproval_projects_get_service_account_execute()`.
/// For task-level control, use `accessapproval_projects_get_service_account_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_projects_get_service_account(
    client: &SimpleHttpClient,
    args: &AccessapprovalProjectsGetServiceAccountArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessApprovalServiceAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = accessapproval_projects_get_service_account_builder(client, args.name.clone())?;
    accessapproval_projects_get_service_account_execute(builder)
}

/// GET v1/projects/{projectsId}/approvalRequests
/// Lists approval requests associated with a project, folder, or organization. Approval requests can be filtered by state (pending, active, dismissed). The order is reverse chronological.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `accessapproval_projects_approval_requests_list_execute()` to send, or `accessapproval_projects_approval_requests_list` for simplest API.

pub fn accessapproval_projects_approval_requests_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    filter: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://accessapproval.googleapis.com/v1/projects/{}/approvalRequests",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/approvalRequests
/// Lists approval requests associated with a project, folder, or organization. Approval requests can be filtered by state (pending, active, dismissed). The order is reverse chronological.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `accessapproval_projects_approval_requests_list_execute()` or `accessapproval_projects_approval_requests_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_projects_approval_requests_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_projects_approval_requests_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListApprovalRequestsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListApprovalRequestsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/approvalRequests
/// Lists approval requests associated with a project, folder, or organization. Approval requests can be filtered by state (pending, active, dismissed). The order is reverse chronological.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `accessapproval_projects_approval_requests_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `accessapproval_projects_approval_requests_list_task()`.
/// For the simplest API, use `accessapproval_projects_approval_requests_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `accessapproval_projects_approval_requests_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn accessapproval_projects_approval_requests_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListApprovalRequestsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = accessapproval_projects_approval_requests_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`accessapproval_projects_approval_requests_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AccessapprovalProjectsApprovalRequestsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/approvalRequests
/// Lists approval requests associated with a project, folder, or organization. Approval requests can be filtered by state (pending, active, dismissed). The order is reverse chronological.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `accessapproval_projects_approval_requests_list_builder()` + `accessapproval_projects_approval_requests_list_execute()`.
/// For task-level control, use `accessapproval_projects_approval_requests_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn accessapproval_projects_approval_requests_list(
    client: &SimpleHttpClient,
    args: &AccessapprovalProjectsApprovalRequestsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListApprovalRequestsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = accessapproval_projects_approval_requests_list_builder(
        client,
        args.parent.clone(),
        args.filter.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    accessapproval_projects_approval_requests_list_execute(builder)
}
