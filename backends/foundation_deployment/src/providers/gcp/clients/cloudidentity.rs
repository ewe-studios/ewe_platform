//! Auto-generated API clients for gcp/cloudidentity.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/customers/{customersId}/userinvitations
/// Retrieves a list of UserInvitation resources. **Note:** New consumer accounts with the customer's verified domain created within the previous 48 hours will not appear in the result. This delay also applies to newly-verified domains.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_customers_userinvitations_list_execute()` to send, or `cloudidentity_customers_userinvitations_list` for simplest API.

pub fn cloudidentity_customers_userinvitations_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://cloudidentity.googleapis.com/v1/customers/{}/userinvitations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/userinvitations
/// Retrieves a list of UserInvitation resources. **Note:** New consumer accounts with the customer's verified domain created within the previous 48 hours will not appear in the result. This delay also applies to newly-verified domains.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_customers_userinvitations_list_execute()` or `cloudidentity_customers_userinvitations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_customers_userinvitations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_customers_userinvitations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListUserInvitationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUserInvitationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/userinvitations
/// Retrieves a list of UserInvitation resources. **Note:** New consumer accounts with the customer's verified domain created within the previous 48 hours will not appear in the result. This delay also applies to newly-verified domains.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_customers_userinvitations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_customers_userinvitations_list_task()`.
/// For the simplest API, use `cloudidentity_customers_userinvitations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_customers_userinvitations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_customers_userinvitations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUserInvitationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudidentity_customers_userinvitations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_customers_userinvitations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityCustomersUserinvitationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/customers/{customersId}/userinvitations
/// Retrieves a list of UserInvitation resources. **Note:** New consumer accounts with the customer's verified domain created within the previous 48 hours will not appear in the result. This delay also applies to newly-verified domains.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_customers_userinvitations_list_builder()` + `cloudidentity_customers_userinvitations_list_execute()`.
/// For task-level control, use `cloudidentity_customers_userinvitations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_customers_userinvitations_list(
    client: &SimpleHttpClient,
    args: &CloudidentityCustomersUserinvitationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUserInvitationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_customers_userinvitations_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    cloudidentity_customers_userinvitations_list_execute(builder)
}

/// GET v1/devices/{devicesId}:cancelWipe
/// Cancels an unfinished device wipe. This operation can be used to cancel device wipe in the gap between the wipe operation returning success and the device being wiped. This operation is possible when the device is in a "pending wipe" state. The device enters the "pending wipe" state when a wipe device command is issued, but has not yet been sent to the device. The cancel wipe will fail if the wipe command has already been issued to the device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_cancel_wipe_execute()` to send, or `cloudidentity_devices_cancel_wipe` for simplest API.

pub fn cloudidentity_devices_cancel_wipe_builder(
    client: &SimpleHttpClient,
    name: &String,
    body: &GoogleAppsCloudidentityDevicesV1CancelWipeDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/devices/{}:cancelWipe",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}:cancelWipe
/// Cancels an unfinished device wipe. This operation can be used to cancel device wipe in the gap between the wipe operation returning success and the device being wiped. This operation is possible when the device is in a "pending wipe" state. The device enters the "pending wipe" state when a wipe device command is issued, but has not yet been sent to the device. The cancel wipe will fail if the wipe command has already been issued to the device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_devices_cancel_wipe_execute()` or `cloudidentity_devices_cancel_wipe`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_devices_cancel_wipe_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_cancel_wipe_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/devices/{devicesId}:cancelWipe
/// Cancels an unfinished device wipe. This operation can be used to cancel device wipe in the gap between the wipe operation returning success and the device being wiped. This operation is possible when the device is in a "pending wipe" state. The device enters the "pending wipe" state when a wipe device command is issued, but has not yet been sent to the device. The cancel wipe will fail if the wipe command has already been issued to the device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_devices_cancel_wipe_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_devices_cancel_wipe_task()`.
/// For the simplest API, use `cloudidentity_devices_cancel_wipe()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_devices_cancel_wipe_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_devices_cancel_wipe_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudidentity_devices_cancel_wipe_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_devices_cancel_wipe`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityDevicesCancelWipeArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleAppsCloudidentityDevicesV1CancelWipeDeviceRequest,
}

/// GET v1/devices/{devicesId}:cancelWipe
/// Cancels an unfinished device wipe. This operation can be used to cancel device wipe in the gap between the wipe operation returning success and the device being wiped. This operation is possible when the device is in a "pending wipe" state. The device enters the "pending wipe" state when a wipe device command is issued, but has not yet been sent to the device. The cancel wipe will fail if the wipe command has already been issued to the device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_cancel_wipe_builder()` + `cloudidentity_devices_cancel_wipe_execute()`.
/// For task-level control, use `cloudidentity_devices_cancel_wipe_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_cancel_wipe(
    client: &SimpleHttpClient,
    args: &CloudidentityDevicesCancelWipeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_cancel_wipe_builder(client, &args.name, &args.body)?;
    cloudidentity_devices_cancel_wipe_execute(builder)
}

/// GET v1/devices
/// Creates a device. Only company-owned device may be created. **Note**: This method is available only to customers who have one of the following SKUs: Enterprise Standard, Enterprise Plus, Enterprise for Education, and Cloud Identity Premium
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_create_execute()` to send, or `cloudidentity_devices_create` for simplest API.

pub fn cloudidentity_devices_create_builder(
    client: &SimpleHttpClient,
    customer: &Option<String>,
    body: &GoogleAppsCloudidentityDevicesV1Device,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/devices",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer.as_ref() {
        query_parts.push(format!("customer={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices
/// Creates a device. Only company-owned device may be created. **Note**: This method is available only to customers who have one of the following SKUs: Enterprise Standard, Enterprise Plus, Enterprise for Education, and Cloud Identity Premium
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_devices_create_execute()` or `cloudidentity_devices_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_devices_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/devices
/// Creates a device. Only company-owned device may be created. **Note**: This method is available only to customers who have one of the following SKUs: Enterprise Standard, Enterprise Plus, Enterprise for Education, and Cloud Identity Premium
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_devices_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_devices_create_task()`.
/// For the simplest API, use `cloudidentity_devices_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_devices_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudidentity_devices_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_devices_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityDevicesCreateArgs {
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Request body.
    pub body: GoogleAppsCloudidentityDevicesV1Device,
}

/// GET v1/devices
/// Creates a device. Only company-owned device may be created. **Note**: This method is available only to customers who have one of the following SKUs: Enterprise Standard, Enterprise Plus, Enterprise for Education, and Cloud Identity Premium
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_create_builder()` + `cloudidentity_devices_create_execute()`.
/// For task-level control, use `cloudidentity_devices_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_create(
    client: &SimpleHttpClient,
    args: &CloudidentityDevicesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_create_builder(client, &args.customer, &args.body)?;
    cloudidentity_devices_create_execute(builder)
}

/// GET v1/devices/{devicesId}
/// Deletes the specified device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_delete_execute()` to send, or `cloudidentity_devices_delete` for simplest API.

pub fn cloudidentity_devices_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
    customer: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/devices/{}",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer.as_ref() {
        query_parts.push(format!("customer={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/devices/{devicesId}
/// Deletes the specified device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_devices_delete_execute()` or `cloudidentity_devices_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_devices_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/devices/{devicesId}
/// Deletes the specified device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_devices_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_devices_delete_task()`.
/// For the simplest API, use `cloudidentity_devices_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_devices_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_devices_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudidentity_devices_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_devices_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityDevicesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: customer
    pub customer: Option<String>,
}

/// GET v1/devices/{devicesId}
/// Deletes the specified device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_delete_builder()` + `cloudidentity_devices_delete_execute()`.
/// For task-level control, use `cloudidentity_devices_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_delete(
    client: &SimpleHttpClient,
    args: &CloudidentityDevicesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_delete_builder(client, &args.name, &args.customer)?;
    cloudidentity_devices_delete_execute(builder)
}

/// GET v1/devices/{devicesId}:wipe
/// Wipes all data on the specified device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_wipe_execute()` to send, or `cloudidentity_devices_wipe` for simplest API.

pub fn cloudidentity_devices_wipe_builder(
    client: &SimpleHttpClient,
    name: &String,
    body: &GoogleAppsCloudidentityDevicesV1WipeDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/devices/{}:wipe",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}:wipe
/// Wipes all data on the specified device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_devices_wipe_execute()` or `cloudidentity_devices_wipe`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_devices_wipe_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_wipe_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/devices/{devicesId}:wipe
/// Wipes all data on the specified device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_devices_wipe_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_devices_wipe_task()`.
/// For the simplest API, use `cloudidentity_devices_wipe()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_devices_wipe_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_devices_wipe_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudidentity_devices_wipe_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_devices_wipe`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityDevicesWipeArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleAppsCloudidentityDevicesV1WipeDeviceRequest,
}

/// GET v1/devices/{devicesId}:wipe
/// Wipes all data on the specified device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_wipe_builder()` + `cloudidentity_devices_wipe_execute()`.
/// For task-level control, use `cloudidentity_devices_wipe_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_wipe(
    client: &SimpleHttpClient,
    args: &CloudidentityDevicesWipeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_wipe_builder(client, &args.name, &args.body)?;
    cloudidentity_devices_wipe_execute(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers
/// L`ists/Searches` DeviceUsers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_device_users_list_execute()` to send, or `cloudidentity_devices_device_users_list` for simplest API.

pub fn cloudidentity_devices_device_users_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    customer: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/devices/{}/deviceUsers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer.as_ref() {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers
/// L`ists/Searches` DeviceUsers.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_devices_device_users_list_execute()` or `cloudidentity_devices_device_users_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_devices_device_users_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsCloudidentityDevicesV1ListDeviceUsersResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsCloudidentityDevicesV1ListDeviceUsersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/devices/{devicesId}/deviceUsers
/// L`ists/Searches` DeviceUsers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_devices_device_users_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_devices_device_users_list_task()`.
/// For the simplest API, use `cloudidentity_devices_device_users_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_devices_device_users_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_devices_device_users_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAppsCloudidentityDevicesV1ListDeviceUsersResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudidentity_devices_device_users_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_devices_device_users_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityDevicesDeviceUsersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/devices/{devicesId}/deviceUsers
/// L`ists/Searches` DeviceUsers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_device_users_list_builder()` + `cloudidentity_devices_device_users_list_execute()`.
/// For task-level control, use `cloudidentity_devices_device_users_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_list(
    client: &SimpleHttpClient,
    args: &CloudidentityDevicesDeviceUsersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAppsCloudidentityDevicesV1ListDeviceUsersResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_device_users_list_builder(
        client,
        &args.parent,
        &args.customer,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    cloudidentity_devices_device_users_list_execute(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers:lookup
/// Looks up resource names of the DeviceUsers associated with the caller's credentials, as well as the properties provided in the request. This method must be called with end-user credentials with the scope: <https://www.googleapis.`com/auth/cloud-identity`.devices.lookup> If multiple properties are provided, only DeviceUsers having all of these properties are considered as matches - i.e. the query behaves like an AND. Different platforms require different amounts of information from the caller to ensure that the DeviceUser is uniquely identified. - iOS: If either the partner or ios_device_id field is provided, then both fields are required. - Android: Specifying the android_id field is required. - Desktop: Specifying the raw_resource_id field is required.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_device_users_lookup_execute()` to send, or `cloudidentity_devices_device_users_lookup` for simplest API.

pub fn cloudidentity_devices_device_users_lookup_builder(
    client: &SimpleHttpClient,
    parent: &String,
    androidId: &Option<String>,
    iosDeviceId: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
    partner: &Option<String>,
    rawResourceId: &Option<String>,
    userId: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://cloudidentity.googleapis.com/v1/devices/{}/deviceUsers:lookup",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = androidId.as_ref() {
        query_parts.push(format!("androidId={}", val));
    }
    if let Some(val) = iosDeviceId.as_ref() {
        query_parts.push(format!("iosDeviceId={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = partner.as_ref() {
        query_parts.push(format!("partner={}", val));
    }
    if let Some(val) = rawResourceId.as_ref() {
        query_parts.push(format!("rawResourceId={}", val));
    }
    if let Some(val) = userId.as_ref() {
        query_parts.push(format!("userId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers:lookup
/// Looks up resource names of the DeviceUsers associated with the caller's credentials, as well as the properties provided in the request. This method must be called with end-user credentials with the scope: <https://www.googleapis.`com/auth/cloud-identity`.devices.lookup> If multiple properties are provided, only DeviceUsers having all of these properties are considered as matches - i.e. the query behaves like an AND. Different platforms require different amounts of information from the caller to ensure that the DeviceUser is uniquely identified. - iOS: If either the partner or ios_device_id field is provided, then both fields are required. - Android: Specifying the android_id field is required. - Desktop: Specifying the raw_resource_id field is required.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_devices_device_users_lookup_execute()` or `cloudidentity_devices_device_users_lookup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_devices_device_users_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_lookup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsCloudidentityDevicesV1LookupSelfDeviceUsersResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsCloudidentityDevicesV1LookupSelfDeviceUsersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/devices/{devicesId}/deviceUsers:lookup
/// Looks up resource names of the DeviceUsers associated with the caller's credentials, as well as the properties provided in the request. This method must be called with end-user credentials with the scope: <https://www.googleapis.`com/auth/cloud-identity`.devices.lookup> If multiple properties are provided, only DeviceUsers having all of these properties are considered as matches - i.e. the query behaves like an AND. Different platforms require different amounts of information from the caller to ensure that the DeviceUser is uniquely identified. - iOS: If either the partner or ios_device_id field is provided, then both fields are required. - Android: Specifying the android_id field is required. - Desktop: Specifying the raw_resource_id field is required.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_devices_device_users_lookup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_devices_device_users_lookup_task()`.
/// For the simplest API, use `cloudidentity_devices_device_users_lookup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_devices_device_users_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_devices_device_users_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAppsCloudidentityDevicesV1LookupSelfDeviceUsersResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudidentity_devices_device_users_lookup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_devices_device_users_lookup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityDevicesDeviceUsersLookupArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: androidId
    pub androidId: Option<String>,
    /// Query parameter: iosDeviceId
    pub iosDeviceId: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: partner
    pub partner: Option<String>,
    /// Query parameter: rawResourceId
    pub rawResourceId: Option<String>,
    /// Query parameter: userId
    pub userId: Option<String>,
}

/// GET v1/devices/{devicesId}/deviceUsers:lookup
/// Looks up resource names of the DeviceUsers associated with the caller's credentials, as well as the properties provided in the request. This method must be called with end-user credentials with the scope: <https://www.googleapis.`com/auth/cloud-identity`.devices.lookup> If multiple properties are provided, only DeviceUsers having all of these properties are considered as matches - i.e. the query behaves like an AND. Different platforms require different amounts of information from the caller to ensure that the DeviceUser is uniquely identified. - iOS: If either the partner or ios_device_id field is provided, then both fields are required. - Android: Specifying the android_id field is required. - Desktop: Specifying the raw_resource_id field is required.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_device_users_lookup_builder()` + `cloudidentity_devices_device_users_lookup_execute()`.
/// For task-level control, use `cloudidentity_devices_device_users_lookup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_lookup(
    client: &SimpleHttpClient,
    args: &CloudidentityDevicesDeviceUsersLookupArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAppsCloudidentityDevicesV1LookupSelfDeviceUsersResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_device_users_lookup_builder(
        client,
        &args.parent,
        &args.androidId,
        &args.iosDeviceId,
        &args.pageSize,
        &args.pageToken,
        &args.partner,
        &args.rawResourceId,
        &args.userId,
    )?;
    cloudidentity_devices_device_users_lookup_execute(builder)
}

/// GET v1/groups
/// Creates a Group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_create_execute()` to send, or `cloudidentity_groups_create` for simplest API.

pub fn cloudidentity_groups_create_builder(
    client: &SimpleHttpClient,
    initialGroupConfig: &Option<String>,
    body: &Group,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/groups",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = initialGroupConfig.as_ref() {
        query_parts.push(format!("initialGroupConfig={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups
/// Creates a Group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_groups_create_execute()` or `cloudidentity_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/groups
/// Creates a Group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_groups_create_task()`.
/// For the simplest API, use `cloudidentity_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudidentity_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityGroupsCreateArgs {
    /// Query parameter: initialGroupConfig
    pub initialGroupConfig: Option<String>,
    /// Request body.
    pub body: Group,
}

/// GET v1/groups
/// Creates a Group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_create_builder()` + `cloudidentity_groups_create_execute()`.
/// For task-level control, use `cloudidentity_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_create(
    client: &SimpleHttpClient,
    args: &CloudidentityGroupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_groups_create_builder(client, &args.initialGroupConfig, &args.body)?;
    cloudidentity_groups_create_execute(builder)
}

/// GET v1/groups/{groupsId}
/// Deletes a Group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_delete_execute()` to send, or `cloudidentity_groups_delete` for simplest API.

pub fn cloudidentity_groups_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/groups/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}
/// Deletes a Group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_groups_delete_execute()` or `cloudidentity_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/groups/{groupsId}
/// Deletes a Group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_groups_delete_task()`.
/// For the simplest API, use `cloudidentity_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudidentity_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityGroupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/groups/{groupsId}
/// Deletes a Group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_delete_builder()` + `cloudidentity_groups_delete_execute()`.
/// For task-level control, use `cloudidentity_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_delete(
    client: &SimpleHttpClient,
    args: &CloudidentityGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_delete_builder(client, &args.name)?;
    cloudidentity_groups_delete_execute(builder)
}

/// GET v1/groups/{groupsId}/securitySettings
/// Get Security Settings
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_get_security_settings_execute()` to send, or `cloudidentity_groups_get_security_settings` for simplest API.

pub fn cloudidentity_groups_get_security_settings_builder(
    client: &SimpleHttpClient,
    name: &String,
    readMask: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://cloudidentity.googleapis.com/v1/groups/{}/securitySettings",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}/securitySettings
/// Get Security Settings
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_groups_get_security_settings_execute()` or `cloudidentity_groups_get_security_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_get_security_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_get_security_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecuritySettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecuritySettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/groups/{groupsId}/securitySettings
/// Get Security Settings
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_groups_get_security_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_groups_get_security_settings_task()`.
/// For the simplest API, use `cloudidentity_groups_get_security_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_get_security_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_groups_get_security_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecuritySettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudidentity_groups_get_security_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_groups_get_security_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityGroupsGetSecuritySettingsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/groups/{groupsId}/securitySettings
/// Get Security Settings
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_get_security_settings_builder()` + `cloudidentity_groups_get_security_settings_execute()`.
/// For task-level control, use `cloudidentity_groups_get_security_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_get_security_settings(
    client: &SimpleHttpClient,
    args: &CloudidentityGroupsGetSecuritySettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecuritySettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_groups_get_security_settings_builder(client, &args.name, &args.readMask)?;
    cloudidentity_groups_get_security_settings_execute(builder)
}

/// GET v1/groups:lookup
/// Looks up the [resource name](<https://cloud.google.`com/apis/design/resource_names`>) of a Group by its EntityKey.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_lookup_execute()` to send, or `cloudidentity_groups_lookup` for simplest API.

pub fn cloudidentity_groups_lookup_builder(
    client: &SimpleHttpClient,
    groupKey_id: &Option<String>,
    groupKey_namespace: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/groups:lookup",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = groupKey_id.as_ref() {
        query_parts.push(format!("groupKey.id={}", val));
    }
    if let Some(val) = groupKey_namespace.as_ref() {
        query_parts.push(format!("groupKey.namespace={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups:lookup
/// Looks up the [resource name](<https://cloud.google.`com/apis/design/resource_names`>) of a Group by its EntityKey.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_groups_lookup_execute()` or `cloudidentity_groups_lookup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_lookup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LookupGroupNameResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LookupGroupNameResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/groups:lookup
/// Looks up the [resource name](<https://cloud.google.`com/apis/design/resource_names`>) of a Group by its EntityKey.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_groups_lookup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_groups_lookup_task()`.
/// For the simplest API, use `cloudidentity_groups_lookup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_groups_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LookupGroupNameResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudidentity_groups_lookup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_groups_lookup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityGroupsLookupArgs {
    /// Query parameter: groupKey_id
    pub groupKey_id: Option<String>,
    /// Query parameter: groupKey_namespace
    pub groupKey_namespace: Option<String>,
}

/// GET v1/groups:lookup
/// Looks up the [resource name](<https://cloud.google.`com/apis/design/resource_names`>) of a Group by its EntityKey.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_lookup_builder()` + `cloudidentity_groups_lookup_execute()`.
/// For task-level control, use `cloudidentity_groups_lookup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_lookup(
    client: &SimpleHttpClient,
    args: &CloudidentityGroupsLookupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LookupGroupNameResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_groups_lookup_builder(client, &args.groupKey_id, &args.groupKey_namespace)?;
    cloudidentity_groups_lookup_execute(builder)
}

/// GET v1/groups:search
/// Searches for Group resources matching a specified query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_search_execute()` to send, or `cloudidentity_groups_search` for simplest API.

pub fn cloudidentity_groups_search_builder(
    client: &SimpleHttpClient,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
    query: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/groups:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups:search
/// Searches for Group resources matching a specified query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_groups_search_execute()` or `cloudidentity_groups_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/groups:search
/// Searches for Group resources matching a specified query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_groups_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_groups_search_task()`.
/// For the simplest API, use `cloudidentity_groups_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_groups_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudidentity_groups_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_groups_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityGroupsSearchArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/groups:search
/// Searches for Group resources matching a specified query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_search_builder()` + `cloudidentity_groups_search_execute()`.
/// For task-level control, use `cloudidentity_groups_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_search(
    client: &SimpleHttpClient,
    args: &CloudidentityGroupsSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_search_builder(
        client,
        &args.pageSize,
        &args.pageToken,
        &args.query,
        &args.view,
    )?;
    cloudidentity_groups_search_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships:checkTransitiveMembership
/// Check a potential member for membership in a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A member has membership to a group as long as there is a single viewable transitive membership between the group and the member. The actor must have view permissions to at least one transitive membership between the member and group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_check_transitive_membership_execute()` to send, or `cloudidentity_groups_memberships_check_transitive_membership` for simplest API.

pub fn cloudidentity_groups_memberships_check_transitive_membership_builder(
    client: &SimpleHttpClient,
    parent: &String,
    query: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/memberships:checkTransitiveMembership",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}/memberships:checkTransitiveMembership
/// Check a potential member for membership in a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A member has membership to a group as long as there is a single viewable transitive membership between the group and the member. The actor must have view permissions to at least one transitive membership between the member and group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_groups_memberships_check_transitive_membership_execute()` or `cloudidentity_groups_memberships_check_transitive_membership`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_memberships_check_transitive_membership_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_check_transitive_membership_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CheckTransitiveMembershipResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckTransitiveMembershipResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/groups/{groupsId}/memberships:checkTransitiveMembership
/// Check a potential member for membership in a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A member has membership to a group as long as there is a single viewable transitive membership between the group and the member. The actor must have view permissions to at least one transitive membership between the member and group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_groups_memberships_check_transitive_membership_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_groups_memberships_check_transitive_membership_task()`.
/// For the simplest API, use `cloudidentity_groups_memberships_check_transitive_membership()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_memberships_check_transitive_membership_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_groups_memberships_check_transitive_membership_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CheckTransitiveMembershipResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudidentity_groups_memberships_check_transitive_membership_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_groups_memberships_check_transitive_membership`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityGroupsMembershipsCheckTransitiveMembershipArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET v1/groups/{groupsId}/memberships:checkTransitiveMembership
/// Check a potential member for membership in a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A member has membership to a group as long as there is a single viewable transitive membership between the group and the member. The actor must have view permissions to at least one transitive membership between the member and group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_check_transitive_membership_builder()` + `cloudidentity_groups_memberships_check_transitive_membership_execute()`.
/// For task-level control, use `cloudidentity_groups_memberships_check_transitive_membership_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_check_transitive_membership(
    client: &SimpleHttpClient,
    args: &CloudidentityGroupsMembershipsCheckTransitiveMembershipArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CheckTransitiveMembershipResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_memberships_check_transitive_membership_builder(
        client,
        &args.parent,
        &args.query,
    )?;
    cloudidentity_groups_memberships_check_transitive_membership_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships
/// Creates a Membership.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_create_execute()` to send, or `cloudidentity_groups_memberships_create` for simplest API.

pub fn cloudidentity_groups_memberships_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &Membership,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/groups/{}/memberships",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}/memberships
/// Creates a Membership.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_groups_memberships_create_execute()` or `cloudidentity_groups_memberships_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_memberships_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/groups/{groupsId}/memberships
/// Creates a Membership.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_groups_memberships_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_groups_memberships_create_task()`.
/// For the simplest API, use `cloudidentity_groups_memberships_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_memberships_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_groups_memberships_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudidentity_groups_memberships_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_groups_memberships_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityGroupsMembershipsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: Membership,
}

/// GET v1/groups/{groupsId}/memberships
/// Creates a Membership.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_create_builder()` + `cloudidentity_groups_memberships_create_execute()`.
/// For task-level control, use `cloudidentity_groups_memberships_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_create(
    client: &SimpleHttpClient,
    args: &CloudidentityGroupsMembershipsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_groups_memberships_create_builder(client, &args.parent, &args.body)?;
    cloudidentity_groups_memberships_create_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships:getMembershipGraph
/// Get a membership graph of just a member or both a member and a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. Given a member, the response will contain all membership paths from the member. Given both a group and a member, the response will contain all membership paths between the group and the member.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_get_membership_graph_execute()` to send, or `cloudidentity_groups_memberships_get_membership_graph` for simplest API.

pub fn cloudidentity_groups_memberships_get_membership_graph_builder(
    client: &SimpleHttpClient,
    parent: &String,
    query: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/memberships:getMembershipGraph",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}/memberships:getMembershipGraph
/// Get a membership graph of just a member or both a member and a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. Given a member, the response will contain all membership paths from the member. Given both a group and a member, the response will contain all membership paths between the group and the member.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_groups_memberships_get_membership_graph_execute()` or `cloudidentity_groups_memberships_get_membership_graph`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_memberships_get_membership_graph_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_get_membership_graph_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/groups/{groupsId}/memberships:getMembershipGraph
/// Get a membership graph of just a member or both a member and a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. Given a member, the response will contain all membership paths from the member. Given both a group and a member, the response will contain all membership paths between the group and the member.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_groups_memberships_get_membership_graph_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_groups_memberships_get_membership_graph_task()`.
/// For the simplest API, use `cloudidentity_groups_memberships_get_membership_graph()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_memberships_get_membership_graph_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_groups_memberships_get_membership_graph_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudidentity_groups_memberships_get_membership_graph_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_groups_memberships_get_membership_graph`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityGroupsMembershipsGetMembershipGraphArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET v1/groups/{groupsId}/memberships:getMembershipGraph
/// Get a membership graph of just a member or both a member and a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. Given a member, the response will contain all membership paths from the member. Given both a group and a member, the response will contain all membership paths between the group and the member.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_get_membership_graph_builder()` + `cloudidentity_groups_memberships_get_membership_graph_execute()`.
/// For task-level control, use `cloudidentity_groups_memberships_get_membership_graph_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_get_membership_graph(
    client: &SimpleHttpClient,
    args: &CloudidentityGroupsMembershipsGetMembershipGraphArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_memberships_get_membership_graph_builder(
        client,
        &args.parent,
        &args.query,
    )?;
    cloudidentity_groups_memberships_get_membership_graph_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships:lookup
/// Looks up the [resource name](<https://cloud.google.`com/apis/design/resource_names`>) of a Membership by its EntityKey.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_lookup_execute()` to send, or `cloudidentity_groups_memberships_lookup` for simplest API.

pub fn cloudidentity_groups_memberships_lookup_builder(
    client: &SimpleHttpClient,
    parent: &String,
    memberKey_id: &Option<String>,
    memberKey_namespace: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://cloudidentity.googleapis.com/v1/groups/{}/memberships:lookup",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = memberKey_id.as_ref() {
        query_parts.push(format!("memberKey.id={}", val));
    }
    if let Some(val) = memberKey_namespace.as_ref() {
        query_parts.push(format!("memberKey.namespace={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}/memberships:lookup
/// Looks up the [resource name](<https://cloud.google.`com/apis/design/resource_names`>) of a Membership by its EntityKey.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_groups_memberships_lookup_execute()` or `cloudidentity_groups_memberships_lookup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_memberships_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_lookup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LookupMembershipNameResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LookupMembershipNameResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/groups/{groupsId}/memberships:lookup
/// Looks up the [resource name](<https://cloud.google.`com/apis/design/resource_names`>) of a Membership by its EntityKey.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_groups_memberships_lookup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_groups_memberships_lookup_task()`.
/// For the simplest API, use `cloudidentity_groups_memberships_lookup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_memberships_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_groups_memberships_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LookupMembershipNameResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudidentity_groups_memberships_lookup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_groups_memberships_lookup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityGroupsMembershipsLookupArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: memberKey_id
    pub memberKey_id: Option<String>,
    /// Query parameter: memberKey_namespace
    pub memberKey_namespace: Option<String>,
}

/// GET v1/groups/{groupsId}/memberships:lookup
/// Looks up the [resource name](<https://cloud.google.`com/apis/design/resource_names`>) of a Membership by its EntityKey.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_lookup_builder()` + `cloudidentity_groups_memberships_lookup_execute()`.
/// For task-level control, use `cloudidentity_groups_memberships_lookup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_lookup(
    client: &SimpleHttpClient,
    args: &CloudidentityGroupsMembershipsLookupArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LookupMembershipNameResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_memberships_lookup_builder(
        client,
        &args.parent,
        &args.memberKey_id,
        &args.memberKey_namespace,
    )?;
    cloudidentity_groups_memberships_lookup_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships:searchDirectGroups
/// Searches direct groups of a member.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_search_direct_groups_execute()` to send, or `cloudidentity_groups_memberships_search_direct_groups` for simplest API.

pub fn cloudidentity_groups_memberships_search_direct_groups_builder(
    client: &SimpleHttpClient,
    parent: &String,
    orderBy: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
    query: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/memberships:searchDirectGroups",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}/memberships:searchDirectGroups
/// Searches direct groups of a member.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_groups_memberships_search_direct_groups_execute()` or `cloudidentity_groups_memberships_search_direct_groups`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_memberships_search_direct_groups_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_search_direct_groups_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchDirectGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchDirectGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/groups/{groupsId}/memberships:searchDirectGroups
/// Searches direct groups of a member.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_groups_memberships_search_direct_groups_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_groups_memberships_search_direct_groups_task()`.
/// For the simplest API, use `cloudidentity_groups_memberships_search_direct_groups()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_memberships_search_direct_groups_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_groups_memberships_search_direct_groups_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchDirectGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudidentity_groups_memberships_search_direct_groups_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_groups_memberships_search_direct_groups`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityGroupsMembershipsSearchDirectGroupsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET v1/groups/{groupsId}/memberships:searchDirectGroups
/// Searches direct groups of a member.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_search_direct_groups_builder()` + `cloudidentity_groups_memberships_search_direct_groups_execute()`.
/// For task-level control, use `cloudidentity_groups_memberships_search_direct_groups_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_search_direct_groups(
    client: &SimpleHttpClient,
    args: &CloudidentityGroupsMembershipsSearchDirectGroupsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchDirectGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_memberships_search_direct_groups_builder(
        client,
        &args.parent,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.query,
    )?;
    cloudidentity_groups_memberships_search_direct_groups_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships:searchTransitiveGroups
/// Search transitive groups of a member. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A transitive group is any group that has a direct or indirect membership to the member. Actor must have view permissions all transitive groups.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_search_transitive_groups_execute()` to send, or `cloudidentity_groups_memberships_search_transitive_groups` for simplest API.

pub fn cloudidentity_groups_memberships_search_transitive_groups_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
    query: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/memberships:searchTransitiveGroups",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}/memberships:searchTransitiveGroups
/// Search transitive groups of a member. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A transitive group is any group that has a direct or indirect membership to the member. Actor must have view permissions all transitive groups.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_groups_memberships_search_transitive_groups_execute()` or `cloudidentity_groups_memberships_search_transitive_groups`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_memberships_search_transitive_groups_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_search_transitive_groups_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchTransitiveGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchTransitiveGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/groups/{groupsId}/memberships:searchTransitiveGroups
/// Search transitive groups of a member. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A transitive group is any group that has a direct or indirect membership to the member. Actor must have view permissions all transitive groups.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_groups_memberships_search_transitive_groups_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_groups_memberships_search_transitive_groups_task()`.
/// For the simplest API, use `cloudidentity_groups_memberships_search_transitive_groups()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_memberships_search_transitive_groups_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_groups_memberships_search_transitive_groups_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchTransitiveGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudidentity_groups_memberships_search_transitive_groups_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_groups_memberships_search_transitive_groups`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityGroupsMembershipsSearchTransitiveGroupsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET v1/groups/{groupsId}/memberships:searchTransitiveGroups
/// Search transitive groups of a member. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A transitive group is any group that has a direct or indirect membership to the member. Actor must have view permissions all transitive groups.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_search_transitive_groups_builder()` + `cloudidentity_groups_memberships_search_transitive_groups_execute()`.
/// For task-level control, use `cloudidentity_groups_memberships_search_transitive_groups_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_search_transitive_groups(
    client: &SimpleHttpClient,
    args: &CloudidentityGroupsMembershipsSearchTransitiveGroupsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchTransitiveGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_memberships_search_transitive_groups_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.query,
    )?;
    cloudidentity_groups_memberships_search_transitive_groups_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships:searchTransitiveMemberships
/// Search transitive memberships of a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the group is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A transitive membership is any direct or indirect membership of a group. Actor must have view permissions to all transitive memberships.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_search_transitive_memberships_execute()` to send, or `cloudidentity_groups_memberships_search_transitive_memberships` for simplest API.

pub fn cloudidentity_groups_memberships_search_transitive_memberships_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/memberships:searchTransitiveMemberships",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}/memberships:searchTransitiveMemberships
/// Search transitive memberships of a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the group is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A transitive membership is any direct or indirect membership of a group. Actor must have view permissions to all transitive memberships.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_groups_memberships_search_transitive_memberships_execute()` or `cloudidentity_groups_memberships_search_transitive_memberships`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_memberships_search_transitive_memberships_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_search_transitive_memberships_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchTransitiveMembershipsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchTransitiveMembershipsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/groups/{groupsId}/memberships:searchTransitiveMemberships
/// Search transitive memberships of a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the group is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A transitive membership is any direct or indirect membership of a group. Actor must have view permissions to all transitive memberships.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_groups_memberships_search_transitive_memberships_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_groups_memberships_search_transitive_memberships_task()`.
/// For the simplest API, use `cloudidentity_groups_memberships_search_transitive_memberships()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_groups_memberships_search_transitive_memberships_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_groups_memberships_search_transitive_memberships_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchTransitiveMembershipsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudidentity_groups_memberships_search_transitive_memberships_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_groups_memberships_search_transitive_memberships`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityGroupsMembershipsSearchTransitiveMembershipsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/groups/{groupsId}/memberships:searchTransitiveMemberships
/// Search transitive memberships of a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the group is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A transitive membership is any direct or indirect membership of a group. Actor must have view permissions to all transitive memberships.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_search_transitive_memberships_builder()` + `cloudidentity_groups_memberships_search_transitive_memberships_execute()`.
/// For task-level control, use `cloudidentity_groups_memberships_search_transitive_memberships_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_search_transitive_memberships(
    client: &SimpleHttpClient,
    args: &CloudidentityGroupsMembershipsSearchTransitiveMembershipsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchTransitiveMembershipsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_memberships_search_transitive_memberships_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    cloudidentity_groups_memberships_search_transitive_memberships_execute(builder)
}

/// GET v1/inboundOidcSsoProfiles
/// Creates an InboundOidcSsoProfile for a customer. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_oidc_sso_profiles_create_execute()` to send, or `cloudidentity_inbound_oidc_sso_profiles_create` for simplest API.

pub fn cloudidentity_inbound_oidc_sso_profiles_create_builder(
    client: &SimpleHttpClient,
    body: &InboundOidcSsoProfile,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/inboundOidcSsoProfiles",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundOidcSsoProfiles
/// Creates an InboundOidcSsoProfile for a customer. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_inbound_oidc_sso_profiles_create_execute()` or `cloudidentity_inbound_oidc_sso_profiles_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_inbound_oidc_sso_profiles_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_oidc_sso_profiles_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/inboundOidcSsoProfiles
/// Creates an InboundOidcSsoProfile for a customer. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_inbound_oidc_sso_profiles_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_inbound_oidc_sso_profiles_create_task()`.
/// For the simplest API, use `cloudidentity_inbound_oidc_sso_profiles_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_inbound_oidc_sso_profiles_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_inbound_oidc_sso_profiles_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudidentity_inbound_oidc_sso_profiles_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_inbound_oidc_sso_profiles_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityInboundOidcSsoProfilesCreateArgs {
    /// Request body.
    pub body: InboundOidcSsoProfile,
}

/// GET v1/inboundOidcSsoProfiles
/// Creates an InboundOidcSsoProfile for a customer. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_oidc_sso_profiles_create_builder()` + `cloudidentity_inbound_oidc_sso_profiles_create_execute()`.
/// For task-level control, use `cloudidentity_inbound_oidc_sso_profiles_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_oidc_sso_profiles_create(
    client: &SimpleHttpClient,
    args: &CloudidentityInboundOidcSsoProfilesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_oidc_sso_profiles_create_builder(client, &args.body)?;
    cloudidentity_inbound_oidc_sso_profiles_create_execute(builder)
}

/// GET v1/inboundOidcSsoProfiles/{inboundOidcSsoProfilesId}
/// Deletes an InboundOidcSsoProfile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_oidc_sso_profiles_delete_execute()` to send, or `cloudidentity_inbound_oidc_sso_profiles_delete` for simplest API.

pub fn cloudidentity_inbound_oidc_sso_profiles_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://cloudidentity.googleapis.com/v1/inboundOidcSsoProfiles/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/inboundOidcSsoProfiles/{inboundOidcSsoProfilesId}
/// Deletes an InboundOidcSsoProfile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_inbound_oidc_sso_profiles_delete_execute()` or `cloudidentity_inbound_oidc_sso_profiles_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_inbound_oidc_sso_profiles_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_oidc_sso_profiles_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/inboundOidcSsoProfiles/{inboundOidcSsoProfilesId}
/// Deletes an InboundOidcSsoProfile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_inbound_oidc_sso_profiles_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_inbound_oidc_sso_profiles_delete_task()`.
/// For the simplest API, use `cloudidentity_inbound_oidc_sso_profiles_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_inbound_oidc_sso_profiles_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_inbound_oidc_sso_profiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudidentity_inbound_oidc_sso_profiles_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_inbound_oidc_sso_profiles_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityInboundOidcSsoProfilesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/inboundOidcSsoProfiles/{inboundOidcSsoProfilesId}
/// Deletes an InboundOidcSsoProfile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_oidc_sso_profiles_delete_builder()` + `cloudidentity_inbound_oidc_sso_profiles_delete_execute()`.
/// For task-level control, use `cloudidentity_inbound_oidc_sso_profiles_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_oidc_sso_profiles_delete(
    client: &SimpleHttpClient,
    args: &CloudidentityInboundOidcSsoProfilesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_oidc_sso_profiles_delete_builder(client, &args.name)?;
    cloudidentity_inbound_oidc_sso_profiles_delete_execute(builder)
}

/// GET v1/inboundSamlSsoProfiles
/// Creates an InboundSamlSsoProfile for a customer. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_saml_sso_profiles_create_execute()` to send, or `cloudidentity_inbound_saml_sso_profiles_create` for simplest API.

pub fn cloudidentity_inbound_saml_sso_profiles_create_builder(
    client: &SimpleHttpClient,
    body: &InboundSamlSsoProfile,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/inboundSamlSsoProfiles",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSamlSsoProfiles
/// Creates an InboundSamlSsoProfile for a customer. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_inbound_saml_sso_profiles_create_execute()` or `cloudidentity_inbound_saml_sso_profiles_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_inbound_saml_sso_profiles_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/inboundSamlSsoProfiles
/// Creates an InboundSamlSsoProfile for a customer. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_inbound_saml_sso_profiles_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_inbound_saml_sso_profiles_create_task()`.
/// For the simplest API, use `cloudidentity_inbound_saml_sso_profiles_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_inbound_saml_sso_profiles_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_inbound_saml_sso_profiles_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudidentity_inbound_saml_sso_profiles_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_inbound_saml_sso_profiles_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityInboundSamlSsoProfilesCreateArgs {
    /// Request body.
    pub body: InboundSamlSsoProfile,
}

/// GET v1/inboundSamlSsoProfiles
/// Creates an InboundSamlSsoProfile for a customer. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_create_builder()` + `cloudidentity_inbound_saml_sso_profiles_create_execute()`.
/// For task-level control, use `cloudidentity_inbound_saml_sso_profiles_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_create(
    client: &SimpleHttpClient,
    args: &CloudidentityInboundSamlSsoProfilesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_saml_sso_profiles_create_builder(client, &args.body)?;
    cloudidentity_inbound_saml_sso_profiles_create_execute(builder)
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}
/// Deletes an InboundSamlSsoProfile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_saml_sso_profiles_delete_execute()` to send, or `cloudidentity_inbound_saml_sso_profiles_delete` for simplest API.

pub fn cloudidentity_inbound_saml_sso_profiles_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://cloudidentity.googleapis.com/v1/inboundSamlSsoProfiles/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}
/// Deletes an InboundSamlSsoProfile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_inbound_saml_sso_profiles_delete_execute()` or `cloudidentity_inbound_saml_sso_profiles_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_inbound_saml_sso_profiles_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}
/// Deletes an InboundSamlSsoProfile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_inbound_saml_sso_profiles_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_inbound_saml_sso_profiles_delete_task()`.
/// For the simplest API, use `cloudidentity_inbound_saml_sso_profiles_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_inbound_saml_sso_profiles_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_inbound_saml_sso_profiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudidentity_inbound_saml_sso_profiles_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_inbound_saml_sso_profiles_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityInboundSamlSsoProfilesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}
/// Deletes an InboundSamlSsoProfile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_delete_builder()` + `cloudidentity_inbound_saml_sso_profiles_delete_execute()`.
/// For task-level control, use `cloudidentity_inbound_saml_sso_profiles_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_delete(
    client: &SimpleHttpClient,
    args: &CloudidentityInboundSamlSsoProfilesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_saml_sso_profiles_delete_builder(client, &args.name)?;
    cloudidentity_inbound_saml_sso_profiles_delete_execute(builder)
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials:add
/// Adds an IdpCredential. Up to 2 credentials are allowed. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_execute()` to send, or `cloudidentity_inbound_saml_sso_profiles_idp_credentials_add` for simplest API.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &AddIdpCredentialRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://cloudidentity.googleapis.com/v1/inboundSamlSsoProfiles/{}/idpCredentials:add",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials:add
/// Adds an IdpCredential. Up to 2 credentials are allowed. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_execute()` or `cloudidentity_inbound_saml_sso_profiles_idp_credentials_add`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials:add
/// Adds an IdpCredential. Up to 2 credentials are allowed. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_task()`.
/// For the simplest API, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_add()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_inbound_saml_sso_profiles_idp_credentials_add`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityInboundSamlSsoProfilesIdpCredentialsAddArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: AddIdpCredentialRequest,
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials:add
/// Adds an IdpCredential. Up to 2 credentials are allowed. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_builder()` + `cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_execute()`.
/// For task-level control, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_add(
    client: &SimpleHttpClient,
    args: &CloudidentityInboundSamlSsoProfilesIdpCredentialsAddArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_execute(builder)
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials
/// Returns a list of IdpCredentials in an InboundSamlSsoProfile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_execute()` to send, or `cloudidentity_inbound_saml_sso_profiles_idp_credentials_list` for simplest API.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://cloudidentity.googleapis.com/v1/inboundSamlSsoProfiles/{}/idpCredentials",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials
/// Returns a list of IdpCredentials in an InboundSamlSsoProfile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_execute()` or `cloudidentity_inbound_saml_sso_profiles_idp_credentials_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListIdpCredentialsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListIdpCredentialsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials
/// Returns a list of IdpCredentials in an InboundSamlSsoProfile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_task()`.
/// For the simplest API, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListIdpCredentialsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_inbound_saml_sso_profiles_idp_credentials_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityInboundSamlSsoProfilesIdpCredentialsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials
/// Returns a list of IdpCredentials in an InboundSamlSsoProfile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_builder()` + `cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_execute()`.
/// For task-level control, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_list(
    client: &SimpleHttpClient,
    args: &CloudidentityInboundSamlSsoProfilesIdpCredentialsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListIdpCredentialsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_execute(builder)
}

/// GET v1/inboundSsoAssignments
/// Creates an InboundSsoAssignment for users and devices in a Customer under a given Group or OrgUnit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_sso_assignments_create_execute()` to send, or `cloudidentity_inbound_sso_assignments_create` for simplest API.

pub fn cloudidentity_inbound_sso_assignments_create_builder(
    client: &SimpleHttpClient,
    body: &InboundSsoAssignment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/inboundSsoAssignments",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSsoAssignments
/// Creates an InboundSsoAssignment for users and devices in a Customer under a given Group or OrgUnit.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_inbound_sso_assignments_create_execute()` or `cloudidentity_inbound_sso_assignments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_inbound_sso_assignments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_sso_assignments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/inboundSsoAssignments
/// Creates an InboundSsoAssignment for users and devices in a Customer under a given Group or OrgUnit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_inbound_sso_assignments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_inbound_sso_assignments_create_task()`.
/// For the simplest API, use `cloudidentity_inbound_sso_assignments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_inbound_sso_assignments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_inbound_sso_assignments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudidentity_inbound_sso_assignments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_inbound_sso_assignments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityInboundSsoAssignmentsCreateArgs {
    /// Request body.
    pub body: InboundSsoAssignment,
}

/// GET v1/inboundSsoAssignments
/// Creates an InboundSsoAssignment for users and devices in a Customer under a given Group or OrgUnit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_sso_assignments_create_builder()` + `cloudidentity_inbound_sso_assignments_create_execute()`.
/// For task-level control, use `cloudidentity_inbound_sso_assignments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_sso_assignments_create(
    client: &SimpleHttpClient,
    args: &CloudidentityInboundSsoAssignmentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_sso_assignments_create_builder(client, &args.body)?;
    cloudidentity_inbound_sso_assignments_create_execute(builder)
}

/// GET v1/inboundSsoAssignments/{inboundSsoAssignmentsId}
/// Deletes an InboundSsoAssignment. To disable SSO, Create (or Update) an assignment that has sso_mode == SSO_OFF.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_sso_assignments_delete_execute()` to send, or `cloudidentity_inbound_sso_assignments_delete` for simplest API.

pub fn cloudidentity_inbound_sso_assignments_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/inboundSsoAssignments/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/inboundSsoAssignments/{inboundSsoAssignmentsId}
/// Deletes an InboundSsoAssignment. To disable SSO, Create (or Update) an assignment that has sso_mode == SSO_OFF.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_inbound_sso_assignments_delete_execute()` or `cloudidentity_inbound_sso_assignments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_inbound_sso_assignments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_sso_assignments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/inboundSsoAssignments/{inboundSsoAssignmentsId}
/// Deletes an InboundSsoAssignment. To disable SSO, Create (or Update) an assignment that has sso_mode == SSO_OFF.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_inbound_sso_assignments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_inbound_sso_assignments_delete_task()`.
/// For the simplest API, use `cloudidentity_inbound_sso_assignments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_inbound_sso_assignments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_inbound_sso_assignments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudidentity_inbound_sso_assignments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_inbound_sso_assignments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityInboundSsoAssignmentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/inboundSsoAssignments/{inboundSsoAssignmentsId}
/// Deletes an InboundSsoAssignment. To disable SSO, Create (or Update) an assignment that has sso_mode == SSO_OFF.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_sso_assignments_delete_builder()` + `cloudidentity_inbound_sso_assignments_delete_execute()`.
/// For task-level control, use `cloudidentity_inbound_sso_assignments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_sso_assignments_delete(
    client: &SimpleHttpClient,
    args: &CloudidentityInboundSsoAssignmentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_sso_assignments_delete_builder(client, &args.name)?;
    cloudidentity_inbound_sso_assignments_delete_execute(builder)
}

/// GET v1/policies/{policiesId}
/// Get a policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_policies_get_execute()` to send, or `cloudidentity_policies_get` for simplest API.

pub fn cloudidentity_policies_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/policies/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/policies/{policiesId}
/// Get a policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_policies_get_execute()` or `cloudidentity_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/policies/{policiesId}
/// Get a policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_policies_get_task()`.
/// For the simplest API, use `cloudidentity_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudidentity_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/policies/{policiesId}
/// Get a policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_policies_get_builder()` + `cloudidentity_policies_get_execute()`.
/// For task-level control, use `cloudidentity_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_policies_get(
    client: &SimpleHttpClient,
    args: &CloudidentityPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_policies_get_builder(client, &args.name)?;
    cloudidentity_policies_get_execute(builder)
}

/// GET v1/policies
/// List policies.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_policies_list_execute()` to send, or `cloudidentity_policies_list` for simplest API.

pub fn cloudidentity_policies_list_builder(
    client: &SimpleHttpClient,
    filter: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/policies",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/policies
/// List policies.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudidentity_policies_list_execute()` or `cloudidentity_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPoliciesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/policies
/// List policies.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudidentity_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudidentity_policies_list_task()`.
/// For the simplest API, use `cloudidentity_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudidentity_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudidentity_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudidentity_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudidentity_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityPoliciesListArgs {
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/policies
/// List policies.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_policies_list_builder()` + `cloudidentity_policies_list_execute()`.
/// For task-level control, use `cloudidentity_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_policies_list(
    client: &SimpleHttpClient,
    args: &CloudidentityPoliciesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_policies_list_builder(client, &args.filter, &args.pageSize, &args.pageToken)?;
    cloudidentity_policies_list_execute(builder)
}
