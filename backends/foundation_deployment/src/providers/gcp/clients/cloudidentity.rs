//! Auto-generated API clients for gcp/cloudidentity.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/cloudidentity`

#![cfg(feature = "gcp/cloudidentity")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/customers/{customersId}/userinvitations/{userinvitationsId}:cancel
/// Cancels a UserInvitation that was already sent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_customers_userinvitations_cancel_execute()` to send, or `cloudidentity_customers_userinvitations_cancel` for simplest API.

pub fn cloudidentity_customers_userinvitations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelUserInvitationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/customers/{}/userinvitations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/customers/{customersId}/userinvitations/{userinvitationsId}:cancel
/// Cancels a UserInvitation that was already sent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_customers_userinvitations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_customers_userinvitations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_customers_userinvitations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/customers/{customersId}/userinvitations/{userinvitationsId}:cancel
/// Cancels a UserInvitation that was already sent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_customers_userinvitations_cancel_builder()` + `cloudidentity_customers_userinvitations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_customers_userinvitations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelUserInvitationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_customers_userinvitations_cancel_builder(client, name, body)?;
    cloudidentity_customers_userinvitations_cancel_execute(builder)
}

/// GET v1/customers/{customersId}/userinvitations/{userinvitationsId}
/// Retrieves a UserInvitation resource. **Note:** New consumer accounts with the customer's verified domain created within the previous 48 hours will not appear in the result. This delay also applies to newly-verified domains.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_customers_userinvitations_get_execute()` to send, or `cloudidentity_customers_userinvitations_get` for simplest API.

pub fn cloudidentity_customers_userinvitations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/customers/{}/userinvitations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/userinvitations/{userinvitationsId}
/// Retrieves a UserInvitation resource. **Note:** New consumer accounts with the customer's verified domain created within the previous 48 hours will not appear in the result. This delay also applies to newly-verified domains.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_customers_userinvitations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_customers_userinvitations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_customers_userinvitations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserInvitation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserInvitation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/customers/{customersId}/userinvitations/{userinvitationsId}
/// Retrieves a UserInvitation resource. **Note:** New consumer accounts with the customer's verified domain created within the previous 48 hours will not appear in the result. This delay also applies to newly-verified domains.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_customers_userinvitations_get_builder()` + `cloudidentity_customers_userinvitations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_customers_userinvitations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserInvitation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_customers_userinvitations_get_builder(client, name)?;
    cloudidentity_customers_userinvitations_get_execute(builder)
}

/// GET v1/customers/{customersId}/userinvitations/{userinvitationsId}:isInvitableUser
/// Verifies whether a user account is eligible to receive a UserInvitation (is an unmanaged account). Eligibility is based on the following criteria: * the email address is a consumer account and it's the primary email address of the account, and * the domain of the email address matches an existing verified Google Workspace or Cloud Identity domain If both conditions are met, the user is eligible. **Note:** This method is not supported for Workspace Essentials customers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_customers_userinvitations_is_invitable_user_execute()` to send, or `cloudidentity_customers_userinvitations_is_invitable_user` for simplest API.

pub fn cloudidentity_customers_userinvitations_is_invitable_user_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/customers/{}/userinvitations/{}:isInvitableUser",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/userinvitations/{userinvitationsId}:isInvitableUser
/// Verifies whether a user account is eligible to receive a UserInvitation (is an unmanaged account). Eligibility is based on the following criteria: * the email address is a consumer account and it's the primary email address of the account, and * the domain of the email address matches an existing verified Google Workspace or Cloud Identity domain If both conditions are met, the user is eligible. **Note:** This method is not supported for Workspace Essentials customers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_customers_userinvitations_is_invitable_user_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_customers_userinvitations_is_invitable_user_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_customers_userinvitations_is_invitable_user_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IsInvitableUserResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IsInvitableUserResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/customers/{customersId}/userinvitations/{userinvitationsId}:isInvitableUser
/// Verifies whether a user account is eligible to receive a UserInvitation (is an unmanaged account). Eligibility is based on the following criteria: * the email address is a consumer account and it's the primary email address of the account, and * the domain of the email address matches an existing verified Google Workspace or Cloud Identity domain If both conditions are met, the user is eligible. **Note:** This method is not supported for Workspace Essentials customers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_customers_userinvitations_is_invitable_user_builder()` + `cloudidentity_customers_userinvitations_is_invitable_user_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_customers_userinvitations_is_invitable_user(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IsInvitableUserResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_customers_userinvitations_is_invitable_user_builder(client, name)?;
    cloudidentity_customers_userinvitations_is_invitable_user_execute(builder)
}

/// GET v1/customers/{customersId}/userinvitations
/// Retrieves a list of UserInvitation resources. **Note:** New consumer accounts with the customer's verified domain created within the previous 48 hours will not appear in the result. This delay also applies to newly-verified domains.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_customers_userinvitations_list_execute()` to send, or `cloudidentity_customers_userinvitations_list` for simplest API.

pub fn cloudidentity_customers_userinvitations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/customers/{}/userinvitations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/userinvitations
/// Retrieves a list of UserInvitation resources. **Note:** New consumer accounts with the customer's verified domain created within the previous 48 hours will not appear in the result. This delay also applies to newly-verified domains.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_customers_userinvitations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_customers_userinvitations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_customers_userinvitations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUserInvitationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUserInvitationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/customers/{customersId}/userinvitations
/// Retrieves a list of UserInvitation resources. **Note:** New consumer accounts with the customer's verified domain created within the previous 48 hours will not appear in the result. This delay also applies to newly-verified domains.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_customers_userinvitations_list_builder()` + `cloudidentity_customers_userinvitations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_customers_userinvitations_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUserInvitationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_customers_userinvitations_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    cloudidentity_customers_userinvitations_list_execute(builder)
}

/// GET v1/customers/{customersId}/userinvitations/{userinvitationsId}:send
/// Sends a UserInvitation to email. If the UserInvitation does not exist for this request and it is a valid request, the request creates a UserInvitation. **Note:** The get and list methods have a 48-hour delay where newly-created consumer accounts will not appear in the results. You can still send a UserInvitation to those accounts if you know the unmanaged email address and IsInvitableUser==True.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_customers_userinvitations_send_execute()` to send, or `cloudidentity_customers_userinvitations_send` for simplest API.

pub fn cloudidentity_customers_userinvitations_send_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SendUserInvitationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/customers/{}/userinvitations/{}:send",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/customers/{customersId}/userinvitations/{userinvitationsId}:send
/// Sends a UserInvitation to email. If the UserInvitation does not exist for this request and it is a valid request, the request creates a UserInvitation. **Note:** The get and list methods have a 48-hour delay where newly-created consumer accounts will not appear in the results. You can still send a UserInvitation to those accounts if you know the unmanaged email address and IsInvitableUser==True.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_customers_userinvitations_send_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_customers_userinvitations_send_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_customers_userinvitations_send_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/customers/{customersId}/userinvitations/{userinvitationsId}:send
/// Sends a UserInvitation to email. If the UserInvitation does not exist for this request and it is a valid request, the request creates a UserInvitation. **Note:** The get and list methods have a 48-hour delay where newly-created consumer accounts will not appear in the results. You can still send a UserInvitation to those accounts if you know the unmanaged email address and IsInvitableUser==True.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_customers_userinvitations_send_builder()` + `cloudidentity_customers_userinvitations_send_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_customers_userinvitations_send(
    client: &SimpleHttpClient,
    name: &str,
    body: &SendUserInvitationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_customers_userinvitations_send_builder(client, name, body)?;
    cloudidentity_customers_userinvitations_send_execute(builder)
}

/// GET v1/devices/{devicesId}:cancelWipe
/// Cancels an unfinished device wipe. This operation can be used to cancel device wipe in the gap between the wipe operation returning success and the device being wiped. This operation is possible when the device is in a "pending wipe" state. The device enters the "pending wipe" state when a wipe device command is issued, but has not yet been sent to the device. The cancel wipe will fail if the wipe command has already been issued to the device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_cancel_wipe_execute()` to send, or `cloudidentity_devices_cancel_wipe` for simplest API.

pub fn cloudidentity_devices_cancel_wipe_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsCloudidentityDevicesV1CancelWipeDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/devices/{}:cancelWipe",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}:cancelWipe
/// Cancels an unfinished device wipe. This operation can be used to cancel device wipe in the gap between the wipe operation returning success and the device being wiped. This operation is possible when the device is in a "pending wipe" state. The device enters the "pending wipe" state when a wipe device command is issued, but has not yet been sent to the device. The cancel wipe will fail if the wipe command has already been issued to the device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_devices_cancel_wipe_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_devices_cancel_wipe_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_cancel_wipe_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}:cancelWipe
/// Cancels an unfinished device wipe. This operation can be used to cancel device wipe in the gap between the wipe operation returning success and the device being wiped. This operation is possible when the device is in a "pending wipe" state. The device enters the "pending wipe" state when a wipe device command is issued, but has not yet been sent to the device. The cancel wipe will fail if the wipe command has already been issued to the device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_cancel_wipe_builder()` + `cloudidentity_devices_cancel_wipe_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_cancel_wipe(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsCloudidentityDevicesV1CancelWipeDeviceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_cancel_wipe_builder(client, name, body)?;
    cloudidentity_devices_cancel_wipe_execute(builder)
}

/// GET v1/devices
/// Creates a device. Only company-owned device may be created. **Note**: This method is available only to customers who have one of the following SKUs: Enterprise Standard, Enterprise Plus, Enterprise for Education, and Cloud Identity Premium
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_create_execute()` to send, or `cloudidentity_devices_create` for simplest API.

pub fn cloudidentity_devices_create_builder(
    client: &SimpleHttpClient,
    customer: Option<&str>,
    body: &GoogleAppsCloudidentityDevicesV1Device,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/devices",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices
/// Creates a device. Only company-owned device may be created. **Note**: This method is available only to customers who have one of the following SKUs: Enterprise Standard, Enterprise Plus, Enterprise for Education, and Cloud Identity Premium
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_devices_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_devices_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices
/// Creates a device. Only company-owned device may be created. **Note**: This method is available only to customers who have one of the following SKUs: Enterprise Standard, Enterprise Plus, Enterprise for Education, and Cloud Identity Premium
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_create_builder()` + `cloudidentity_devices_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_create(
    client: &SimpleHttpClient,
    customer: Option<&str>,
    body: &GoogleAppsCloudidentityDevicesV1Device,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_create_builder(client, customer, body)?;
    cloudidentity_devices_create_execute(builder)
}

/// GET v1/devices/{devicesId}
/// Deletes the specified device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_delete_execute()` to send, or `cloudidentity_devices_delete` for simplest API.

pub fn cloudidentity_devices_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    customer: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/devices/{}", name,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/devices/{devicesId}
/// Deletes the specified device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_devices_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_devices_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}
/// Deletes the specified device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_delete_builder()` + `cloudidentity_devices_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_delete(
    client: &SimpleHttpClient,
    name: &str,
    customer: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_delete_builder(client, name, customer)?;
    cloudidentity_devices_delete_execute(builder)
}

/// GET v1/devices/{devicesId}
/// Retrieves the specified device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_get_execute()` to send, or `cloudidentity_devices_get` for simplest API.

pub fn cloudidentity_devices_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    customer: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/devices/{}", name,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/devices/{devicesId}
/// Retrieves the specified device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_devices_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_devices_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsCloudidentityDevicesV1Device>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsCloudidentityDevicesV1Device = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}
/// Retrieves the specified device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_get_builder()` + `cloudidentity_devices_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_get(
    client: &SimpleHttpClient,
    name: &str,
    customer: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsCloudidentityDevicesV1Device>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_get_builder(client, name, customer)?;
    cloudidentity_devices_get_execute(builder)
}

/// GET v1/devices
/// L`ists/Searches` devices.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_list_execute()` to send, or `cloudidentity_devices_list` for simplest API.

pub fn cloudidentity_devices_list_builder(
    client: &SimpleHttpClient,
    customer: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/devices",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/devices
/// L`ists/Searches` devices.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsCloudidentityDevicesV1ListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsCloudidentityDevicesV1ListDevicesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices
/// L`ists/Searches` devices.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_list_builder()` + `cloudidentity_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_list(
    client: &SimpleHttpClient,
    customer: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsCloudidentityDevicesV1ListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_list_builder(
        client, customer, filter, orderBy, pageSize, pageToken, view,
    )?;
    cloudidentity_devices_list_execute(builder)
}

/// GET v1/devices/{devicesId}:wipe
/// Wipes all data on the specified device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_wipe_execute()` to send, or `cloudidentity_devices_wipe` for simplest API.

pub fn cloudidentity_devices_wipe_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsCloudidentityDevicesV1WipeDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/devices/{}:wipe",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}:wipe
/// Wipes all data on the specified device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_devices_wipe_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_devices_wipe_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_wipe_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}:wipe
/// Wipes all data on the specified device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_wipe_builder()` + `cloudidentity_devices_wipe_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_wipe(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsCloudidentityDevicesV1WipeDeviceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_wipe_builder(client, name, body)?;
    cloudidentity_devices_wipe_execute(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}:approve
/// Approves device to access user data.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_device_users_approve_execute()` to send, or `cloudidentity_devices_device_users_approve` for simplest API.

pub fn cloudidentity_devices_device_users_approve_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsCloudidentityDevicesV1ApproveDeviceUserRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/devices/{}/deviceUsers/{}:approve",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}:approve
/// Approves device to access user data.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_devices_device_users_approve_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_devices_device_users_approve_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_approve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}:approve
/// Approves device to access user data.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_device_users_approve_builder()` + `cloudidentity_devices_device_users_approve_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_approve(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsCloudidentityDevicesV1ApproveDeviceUserRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_device_users_approve_builder(client, name, body)?;
    cloudidentity_devices_device_users_approve_execute(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}:block
/// Blocks device from accessing user data
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_device_users_block_execute()` to send, or `cloudidentity_devices_device_users_block` for simplest API.

pub fn cloudidentity_devices_device_users_block_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsCloudidentityDevicesV1BlockDeviceUserRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/devices/{}/deviceUsers/{}:block",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}:block
/// Blocks device from accessing user data
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_devices_device_users_block_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_devices_device_users_block_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_block_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}:block
/// Blocks device from accessing user data
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_device_users_block_builder()` + `cloudidentity_devices_device_users_block_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_block(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsCloudidentityDevicesV1BlockDeviceUserRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_device_users_block_builder(client, name, body)?;
    cloudidentity_devices_device_users_block_execute(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}:cancelWipe
/// Cancels an unfinished user account wipe. This operation can be used to cancel device wipe in the gap between the wipe operation returning success and the device being wiped.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_device_users_cancel_wipe_execute()` to send, or `cloudidentity_devices_device_users_cancel_wipe` for simplest API.

pub fn cloudidentity_devices_device_users_cancel_wipe_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsCloudidentityDevicesV1CancelWipeDeviceUserRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/devices/{}/deviceUsers/{}:cancelWipe",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}:cancelWipe
/// Cancels an unfinished user account wipe. This operation can be used to cancel device wipe in the gap between the wipe operation returning success and the device being wiped.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_devices_device_users_cancel_wipe_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_devices_device_users_cancel_wipe_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_cancel_wipe_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}:cancelWipe
/// Cancels an unfinished user account wipe. This operation can be used to cancel device wipe in the gap between the wipe operation returning success and the device being wiped.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_device_users_cancel_wipe_builder()` + `cloudidentity_devices_device_users_cancel_wipe_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_cancel_wipe(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsCloudidentityDevicesV1CancelWipeDeviceUserRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_device_users_cancel_wipe_builder(client, name, body)?;
    cloudidentity_devices_device_users_cancel_wipe_execute(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}
/// Deletes the specified DeviceUser. This also revokes the user's access to device data.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_device_users_delete_execute()` to send, or `cloudidentity_devices_device_users_delete` for simplest API.

pub fn cloudidentity_devices_device_users_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    customer: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/devices/{}/deviceUsers/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}
/// Deletes the specified DeviceUser. This also revokes the user's access to device data.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_devices_device_users_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_devices_device_users_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}
/// Deletes the specified DeviceUser. This also revokes the user's access to device data.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_device_users_delete_builder()` + `cloudidentity_devices_device_users_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_delete(
    client: &SimpleHttpClient,
    name: &str,
    customer: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_device_users_delete_builder(client, name, customer)?;
    cloudidentity_devices_device_users_delete_execute(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}
/// Retrieves the specified DeviceUser
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_device_users_get_execute()` to send, or `cloudidentity_devices_device_users_get` for simplest API.

pub fn cloudidentity_devices_device_users_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    customer: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/devices/{}/deviceUsers/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}
/// Retrieves the specified DeviceUser
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_devices_device_users_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_devices_device_users_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsCloudidentityDevicesV1DeviceUser>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsCloudidentityDevicesV1DeviceUser =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}
/// Retrieves the specified DeviceUser
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_device_users_get_builder()` + `cloudidentity_devices_device_users_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_get(
    client: &SimpleHttpClient,
    name: &str,
    customer: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsCloudidentityDevicesV1DeviceUser>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_device_users_get_builder(client, name, customer)?;
    cloudidentity_devices_device_users_get_execute(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers
/// L`ists/Searches` DeviceUsers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_device_users_list_execute()` to send, or `cloudidentity_devices_device_users_list` for simplest API.

pub fn cloudidentity_devices_device_users_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    customer: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/devices/{}/deviceUsers",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers
/// L`ists/Searches` DeviceUsers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_devices_device_users_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_devices_device_users_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAppsCloudidentityDevicesV1ListDeviceUsersResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsCloudidentityDevicesV1ListDeviceUsersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}/deviceUsers
/// L`ists/Searches` DeviceUsers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_device_users_list_builder()` + `cloudidentity_devices_device_users_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_list(
    client: &SimpleHttpClient,
    parent: &str,
    customer: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAppsCloudidentityDevicesV1ListDeviceUsersResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_device_users_list_builder(
        client, parent, customer, filter, orderBy, pageSize, pageToken,
    )?;
    cloudidentity_devices_device_users_list_execute(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers:lookup
/// Looks up resource names of the DeviceUsers associated with the caller's credentials, as well as the properties provided in the request. This method must be called with end-user credentials with the scope: <https://www.googleapis.`com/auth/cloud-identity`.devices.lookup> If multiple properties are provided, only DeviceUsers having all of these properties are considered as matches - i.e. the query behaves like an AND. Different platforms require different amounts of information from the caller to ensure that the DeviceUser is uniquely identified. - iOS: If either the partner or ios_device_id field is provided, then both fields are required. - Android: Specifying the android_id field is required. - Desktop: Specifying the raw_resource_id field is required.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_device_users_lookup_execute()` to send, or `cloudidentity_devices_device_users_lookup` for simplest API.

pub fn cloudidentity_devices_device_users_lookup_builder(
    client: &SimpleHttpClient,
    parent: &str,
    androidId: Option<&str>,
    iosDeviceId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    partner: Option<&str>,
    rawResourceId: Option<&str>,
    userId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/devices/{}/deviceUsers:lookup",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = androidId {
        query_parts.push(format!("androidId={}", val));
    }
    if let Some(val) = iosDeviceId {
        query_parts.push(format!("iosDeviceId={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = partner {
        query_parts.push(format!("partner={}", val));
    }
    if let Some(val) = rawResourceId {
        query_parts.push(format!("rawResourceId={}", val));
    }
    if let Some(val) = userId {
        query_parts.push(format!("userId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers:lookup
/// Looks up resource names of the DeviceUsers associated with the caller's credentials, as well as the properties provided in the request. This method must be called with end-user credentials with the scope: <https://www.googleapis.`com/auth/cloud-identity`.devices.lookup> If multiple properties are provided, only DeviceUsers having all of these properties are considered as matches - i.e. the query behaves like an AND. Different platforms require different amounts of information from the caller to ensure that the DeviceUser is uniquely identified. - iOS: If either the partner or ios_device_id field is provided, then both fields are required. - Android: Specifying the android_id field is required. - Desktop: Specifying the raw_resource_id field is required.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_devices_device_users_lookup_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_devices_device_users_lookup_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAppsCloudidentityDevicesV1LookupSelfDeviceUsersResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsCloudidentityDevicesV1LookupSelfDeviceUsersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}/deviceUsers:lookup
/// Looks up resource names of the DeviceUsers associated with the caller's credentials, as well as the properties provided in the request. This method must be called with end-user credentials with the scope: <https://www.googleapis.`com/auth/cloud-identity`.devices.lookup> If multiple properties are provided, only DeviceUsers having all of these properties are considered as matches - i.e. the query behaves like an AND. Different platforms require different amounts of information from the caller to ensure that the DeviceUser is uniquely identified. - iOS: If either the partner or ios_device_id field is provided, then both fields are required. - Android: Specifying the android_id field is required. - Desktop: Specifying the raw_resource_id field is required.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_device_users_lookup_builder()` + `cloudidentity_devices_device_users_lookup_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_lookup(
    client: &SimpleHttpClient,
    parent: &str,
    androidId: Option<&str>,
    iosDeviceId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    partner: Option<&str>,
    rawResourceId: Option<&str>,
    userId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAppsCloudidentityDevicesV1LookupSelfDeviceUsersResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_device_users_lookup_builder(
        client,
        parent,
        androidId,
        iosDeviceId,
        pageSize,
        pageToken,
        partner,
        rawResourceId,
        userId,
    )?;
    cloudidentity_devices_device_users_lookup_execute(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}:wipe
/// Wipes the user's account on a device. Other data on the device that is not associated with the user's work account is not affected. For example, if a Gmail app is installed on a device that is used for personal and work purposes, and the user is logged in to the Gmail app with their personal account as well as their work account, wiping the "`deviceUser`" by their work administrator will not affect their personal account within Gmail or other apps such as Photos.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_device_users_wipe_execute()` to send, or `cloudidentity_devices_device_users_wipe` for simplest API.

pub fn cloudidentity_devices_device_users_wipe_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsCloudidentityDevicesV1WipeDeviceUserRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/devices/{}/deviceUsers/{}:wipe",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}:wipe
/// Wipes the user's account on a device. Other data on the device that is not associated with the user's work account is not affected. For example, if a Gmail app is installed on a device that is used for personal and work purposes, and the user is logged in to the Gmail app with their personal account as well as their work account, wiping the "`deviceUser`" by their work administrator will not affect their personal account within Gmail or other apps such as Photos.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_devices_device_users_wipe_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_devices_device_users_wipe_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_wipe_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}:wipe
/// Wipes the user's account on a device. Other data on the device that is not associated with the user's work account is not affected. For example, if a Gmail app is installed on a device that is used for personal and work purposes, and the user is logged in to the Gmail app with their personal account as well as their work account, wiping the "`deviceUser`" by their work administrator will not affect their personal account within Gmail or other apps such as Photos.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_device_users_wipe_builder()` + `cloudidentity_devices_device_users_wipe_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_wipe(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsCloudidentityDevicesV1WipeDeviceUserRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_device_users_wipe_builder(client, name, body)?;
    cloudidentity_devices_device_users_wipe_execute(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}/clientStates/{clientStatesId}
/// Gets the client state for the device user
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_device_users_client_states_get_execute()` to send, or `cloudidentity_devices_device_users_client_states_get` for simplest API.

pub fn cloudidentity_devices_device_users_client_states_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    customer: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/devices/{}/deviceUsers/{}/clientStates/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}/clientStates/{clientStatesId}
/// Gets the client state for the device user
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_devices_device_users_client_states_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_devices_device_users_client_states_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_client_states_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsCloudidentityDevicesV1ClientState>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsCloudidentityDevicesV1ClientState =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}/clientStates/{clientStatesId}
/// Gets the client state for the device user
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_device_users_client_states_get_builder()` + `cloudidentity_devices_device_users_client_states_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_client_states_get(
    client: &SimpleHttpClient,
    name: &str,
    customer: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsCloudidentityDevicesV1ClientState>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_devices_device_users_client_states_get_builder(client, name, customer)?;
    cloudidentity_devices_device_users_client_states_get_execute(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}/clientStates
/// Lists the client states for the given search query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_device_users_client_states_list_execute()` to send, or `cloudidentity_devices_device_users_client_states_list` for simplest API.

pub fn cloudidentity_devices_device_users_client_states_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    customer: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/devices/{}/deviceUsers/{}/clientStates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}/clientStates
/// Lists the client states for the given search query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_devices_device_users_client_states_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_devices_device_users_client_states_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_client_states_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAppsCloudidentityDevicesV1ListClientStatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsCloudidentityDevicesV1ListClientStatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}/clientStates
/// Lists the client states for the given search query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_device_users_client_states_list_builder()` + `cloudidentity_devices_device_users_client_states_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_client_states_list(
    client: &SimpleHttpClient,
    parent: &str,
    customer: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAppsCloudidentityDevicesV1ListClientStatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_device_users_client_states_list_builder(
        client, parent, customer, filter, orderBy, pageToken,
    )?;
    cloudidentity_devices_device_users_client_states_list_execute(builder)
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}/clientStates/{clientStatesId}
/// Updates the client state for the device user **Note**: This method is available only to customers who have one of the following SKUs: Enterprise Standard, Enterprise Plus, Enterprise for Education, and Cloud Identity Premium
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_devices_device_users_client_states_patch_execute()` to send, or `cloudidentity_devices_device_users_client_states_patch` for simplest API.

pub fn cloudidentity_devices_device_users_client_states_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    customer: Option<&str>,
    updateMask: Option<&str>,
    body: &GoogleAppsCloudidentityDevicesV1ClientState,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/devices/{}/deviceUsers/{}/clientStates/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}/clientStates/{clientStatesId}
/// Updates the client state for the device user **Note**: This method is available only to customers who have one of the following SKUs: Enterprise Standard, Enterprise Plus, Enterprise for Education, and Cloud Identity Premium
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_devices_device_users_client_states_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_devices_device_users_client_states_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_client_states_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices/{devicesId}/deviceUsers/{deviceUsersId}/clientStates/{clientStatesId}
/// Updates the client state for the device user **Note**: This method is available only to customers who have one of the following SKUs: Enterprise Standard, Enterprise Plus, Enterprise for Education, and Cloud Identity Premium
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_devices_device_users_client_states_patch_builder()` + `cloudidentity_devices_device_users_client_states_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_devices_device_users_client_states_patch(
    client: &SimpleHttpClient,
    name: &str,
    customer: Option<&str>,
    updateMask: Option<&str>,
    body: &GoogleAppsCloudidentityDevicesV1ClientState,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_devices_device_users_client_states_patch_builder(
        client, name, customer, updateMask, body,
    )?;
    cloudidentity_devices_device_users_client_states_patch_execute(builder)
}

/// GET v1/groups
/// Creates a Group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_create_execute()` to send, or `cloudidentity_groups_create` for simplest API.

pub fn cloudidentity_groups_create_builder(
    client: &SimpleHttpClient,
    initialGroupConfig: Option<&str>,
    body: &Group,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/groups",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = initialGroupConfig {
        query_parts.push(format!("initialGroupConfig={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups
/// Creates a Group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups
/// Creates a Group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_create_builder()` + `cloudidentity_groups_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_create(
    client: &SimpleHttpClient,
    initialGroupConfig: Option<&str>,
    body: &Group,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_create_builder(client, initialGroupConfig, body)?;
    cloudidentity_groups_create_execute(builder)
}

/// GET v1/groups/{groupsId}
/// Deletes a Group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_delete_execute()` to send, or `cloudidentity_groups_delete` for simplest API.

pub fn cloudidentity_groups_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/groups/{}", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}
/// Deletes a Group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}
/// Deletes a Group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_delete_builder()` + `cloudidentity_groups_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_delete_builder(client, name)?;
    cloudidentity_groups_delete_execute(builder)
}

/// GET v1/groups/{groupsId}
/// Retrieves a Group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_get_execute()` to send, or `cloudidentity_groups_get` for simplest API.

pub fn cloudidentity_groups_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/groups/{}", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}
/// Retrieves a Group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Group = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}
/// Retrieves a Group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_get_builder()` + `cloudidentity_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_get_builder(client, name)?;
    cloudidentity_groups_get_execute(builder)
}

/// GET v1/groups/{groupsId}/securitySettings
/// Get Security Settings
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_get_security_settings_execute()` to send, or `cloudidentity_groups_get_security_settings` for simplest API.

pub fn cloudidentity_groups_get_security_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/securitySettings",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}/securitySettings
/// Get Security Settings
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_get_security_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_get_security_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_get_security_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecuritySettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecuritySettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}/securitySettings
/// Get Security Settings
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_get_security_settings_builder()` + `cloudidentity_groups_get_security_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_get_security_settings(
    client: &SimpleHttpClient,
    name: &str,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecuritySettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_get_security_settings_builder(client, name, readMask)?;
    cloudidentity_groups_get_security_settings_execute(builder)
}

/// GET v1/groups
/// Lists the Group resources under a customer or namespace.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_list_execute()` to send, or `cloudidentity_groups_list` for simplest API.

pub fn cloudidentity_groups_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/groups",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups
/// Lists the Group resources under a customer or namespace.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups
/// Lists the Group resources under a customer or namespace.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_list_builder()` + `cloudidentity_groups_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_list(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_list_builder(client, pageSize, pageToken, parent, view)?;
    cloudidentity_groups_list_execute(builder)
}

/// GET v1/groups:lookup
/// Looks up the [resource name](<https://cloud.google.`com/apis/design/resource_names`>) of a Group by its EntityKey.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_lookup_execute()` to send, or `cloudidentity_groups_lookup` for simplest API.

pub fn cloudidentity_groups_lookup_builder(
    client: &SimpleHttpClient,
    groupKey_id: Option<&str>,
    groupKey_namespace: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/groups:lookup",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = groupKey_id {
        query_parts.push(format!("groupKey_id={}", val));
    }
    if let Some(val) = groupKey_namespace {
        query_parts.push(format!("groupKey_namespace={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups:lookup
/// Looks up the [resource name](<https://cloud.google.`com/apis/design/resource_names`>) of a Group by its EntityKey.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_lookup_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_lookup_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LookupGroupNameResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LookupGroupNameResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups:lookup
/// Looks up the [resource name](<https://cloud.google.`com/apis/design/resource_names`>) of a Group by its EntityKey.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_lookup_builder()` + `cloudidentity_groups_lookup_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_lookup(
    client: &SimpleHttpClient,
    groupKey_id: Option<&str>,
    groupKey_namespace: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LookupGroupNameResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_lookup_builder(client, groupKey_id, groupKey_namespace)?;
    cloudidentity_groups_lookup_execute(builder)
}

/// GET v1/groups/{groupsId}
/// Updates a Group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_patch_execute()` to send, or `cloudidentity_groups_patch` for simplest API.

pub fn cloudidentity_groups_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Group,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/groups/{}", name,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}
/// Updates a Group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}
/// Updates a Group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_patch_builder()` + `cloudidentity_groups_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Group,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_patch_builder(client, name, updateMask, body)?;
    cloudidentity_groups_patch_execute(builder)
}

/// GET v1/groups:search
/// Searches for Group resources matching a specified query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_search_execute()` to send, or `cloudidentity_groups_search` for simplest API.

pub fn cloudidentity_groups_search_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/groups:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups:search
/// Searches for Group resources matching a specified query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups:search
/// Searches for Group resources matching a specified query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_search_builder()` + `cloudidentity_groups_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_search(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_search_builder(client, pageSize, pageToken, query, view)?;
    cloudidentity_groups_search_execute(builder)
}

/// GET v1/groups/{groupsId}/securitySettings
/// Update Security Settings
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_update_security_settings_execute()` to send, or `cloudidentity_groups_update_security_settings` for simplest API.

pub fn cloudidentity_groups_update_security_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SecuritySettings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/securitySettings",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}/securitySettings
/// Update Security Settings
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_update_security_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_update_security_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_update_security_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}/securitySettings
/// Update Security Settings
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_update_security_settings_builder()` + `cloudidentity_groups_update_security_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_update_security_settings(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SecuritySettings,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_groups_update_security_settings_builder(client, name, updateMask, body)?;
    cloudidentity_groups_update_security_settings_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships:checkTransitiveMembership
/// Check a potential member for membership in a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A member has membership to a group as long as there is a single viewable transitive membership between the group and the member. The actor must have view permissions to at least one transitive membership between the member and group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_check_transitive_membership_execute()` to send, or `cloudidentity_groups_memberships_check_transitive_membership` for simplest API.

pub fn cloudidentity_groups_memberships_check_transitive_membership_builder(
    client: &SimpleHttpClient,
    parent: &str,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/memberships:checkTransitiveMembership",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}/memberships:checkTransitiveMembership
/// Check a potential member for membership in a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A member has membership to a group as long as there is a single viewable transitive membership between the group and the member. The actor must have view permissions to at least one transitive membership between the member and group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_memberships_check_transitive_membership_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_memberships_check_transitive_membership_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_check_transitive_membership_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CheckTransitiveMembershipResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckTransitiveMembershipResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}/memberships:checkTransitiveMembership
/// Check a potential member for membership in a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A member has membership to a group as long as there is a single viewable transitive membership between the group and the member. The actor must have view permissions to at least one transitive membership between the member and group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_check_transitive_membership_builder()` + `cloudidentity_groups_memberships_check_transitive_membership_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_check_transitive_membership(
    client: &SimpleHttpClient,
    parent: &str,
    query: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CheckTransitiveMembershipResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_memberships_check_transitive_membership_builder(
        client, parent, query,
    )?;
    cloudidentity_groups_memberships_check_transitive_membership_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships
/// Creates a Membership.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_create_execute()` to send, or `cloudidentity_groups_memberships_create` for simplest API.

pub fn cloudidentity_groups_memberships_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Membership,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/memberships",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}/memberships
/// Creates a Membership.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_memberships_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_memberships_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}/memberships
/// Creates a Membership.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_create_builder()` + `cloudidentity_groups_memberships_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Membership,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_memberships_create_builder(client, parent, body)?;
    cloudidentity_groups_memberships_create_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships/{membershipsId}
/// Deletes a Membership.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_delete_execute()` to send, or `cloudidentity_groups_memberships_delete` for simplest API.

pub fn cloudidentity_groups_memberships_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/memberships/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}/memberships/{membershipsId}
/// Deletes a Membership.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_memberships_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_memberships_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}/memberships/{membershipsId}
/// Deletes a Membership.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_delete_builder()` + `cloudidentity_groups_memberships_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_memberships_delete_builder(client, name)?;
    cloudidentity_groups_memberships_delete_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships/{membershipsId}
/// Retrieves a Membership.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_get_execute()` to send, or `cloudidentity_groups_memberships_get` for simplest API.

pub fn cloudidentity_groups_memberships_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/memberships/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}/memberships/{membershipsId}
/// Retrieves a Membership.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_memberships_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_memberships_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Membership>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Membership = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}/memberships/{membershipsId}
/// Retrieves a Membership.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_get_builder()` + `cloudidentity_groups_memberships_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Membership>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_memberships_get_builder(client, name)?;
    cloudidentity_groups_memberships_get_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships:getMembershipGraph
/// Get a membership graph of just a member or both a member and a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. Given a member, the response will contain all membership paths from the member. Given both a group and a member, the response will contain all membership paths between the group and the member.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_get_membership_graph_execute()` to send, or `cloudidentity_groups_memberships_get_membership_graph` for simplest API.

pub fn cloudidentity_groups_memberships_get_membership_graph_builder(
    client: &SimpleHttpClient,
    parent: &str,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/memberships:getMembershipGraph",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}/memberships:getMembershipGraph
/// Get a membership graph of just a member or both a member and a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. Given a member, the response will contain all membership paths from the member. Given both a group and a member, the response will contain all membership paths between the group and the member.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_memberships_get_membership_graph_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_memberships_get_membership_graph_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_get_membership_graph_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}/memberships:getMembershipGraph
/// Get a membership graph of just a member or both a member and a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. Given a member, the response will contain all membership paths from the member. Given both a group and a member, the response will contain all membership paths between the group and the member.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_get_membership_graph_builder()` + `cloudidentity_groups_memberships_get_membership_graph_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_get_membership_graph(
    client: &SimpleHttpClient,
    parent: &str,
    query: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_groups_memberships_get_membership_graph_builder(client, parent, query)?;
    cloudidentity_groups_memberships_get_membership_graph_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships
/// Lists the Memberships within a Group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_list_execute()` to send, or `cloudidentity_groups_memberships_list` for simplest API.

pub fn cloudidentity_groups_memberships_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/memberships",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}/memberships
/// Lists the Memberships within a Group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_memberships_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_memberships_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMembershipsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMembershipsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}/memberships
/// Lists the Memberships within a Group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_list_builder()` + `cloudidentity_groups_memberships_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMembershipsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_groups_memberships_list_builder(client, parent, pageSize, pageToken, view)?;
    cloudidentity_groups_memberships_list_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships:lookup
/// Looks up the [resource name](<https://cloud.google.`com/apis/design/resource_names`>) of a Membership by its EntityKey.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_lookup_execute()` to send, or `cloudidentity_groups_memberships_lookup` for simplest API.

pub fn cloudidentity_groups_memberships_lookup_builder(
    client: &SimpleHttpClient,
    parent: &str,
    memberKey_id: Option<&str>,
    memberKey_namespace: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/memberships:lookup",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = memberKey_id {
        query_parts.push(format!("memberKey_id={}", val));
    }
    if let Some(val) = memberKey_namespace {
        query_parts.push(format!("memberKey_namespace={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}/memberships:lookup
/// Looks up the [resource name](<https://cloud.google.`com/apis/design/resource_names`>) of a Membership by its EntityKey.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_memberships_lookup_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_memberships_lookup_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LookupMembershipNameResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LookupMembershipNameResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}/memberships:lookup
/// Looks up the [resource name](<https://cloud.google.`com/apis/design/resource_names`>) of a Membership by its EntityKey.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_lookup_builder()` + `cloudidentity_groups_memberships_lookup_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_lookup(
    client: &SimpleHttpClient,
    parent: &str,
    memberKey_id: Option<&str>,
    memberKey_namespace: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LookupMembershipNameResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_memberships_lookup_builder(
        client,
        parent,
        memberKey_id,
        memberKey_namespace,
    )?;
    cloudidentity_groups_memberships_lookup_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships/{membershipsId}:modifyMembershipRoles
/// Modifies the MembershipRoles of a Membership.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_modify_membership_roles_execute()` to send, or `cloudidentity_groups_memberships_modify_membership_roles` for simplest API.

pub fn cloudidentity_groups_memberships_modify_membership_roles_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ModifyMembershipRolesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/memberships/{}:modifyMembershipRoles",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}/memberships/{membershipsId}:modifyMembershipRoles
/// Modifies the MembershipRoles of a Membership.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_memberships_modify_membership_roles_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_memberships_modify_membership_roles_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_modify_membership_roles_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ModifyMembershipRolesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ModifyMembershipRolesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}/memberships/{membershipsId}:modifyMembershipRoles
/// Modifies the MembershipRoles of a Membership.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_modify_membership_roles_builder()` + `cloudidentity_groups_memberships_modify_membership_roles_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_modify_membership_roles(
    client: &SimpleHttpClient,
    name: &str,
    body: &ModifyMembershipRolesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ModifyMembershipRolesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_groups_memberships_modify_membership_roles_builder(client, name, body)?;
    cloudidentity_groups_memberships_modify_membership_roles_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships:searchDirectGroups
/// Searches direct groups of a member.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_search_direct_groups_execute()` to send, or `cloudidentity_groups_memberships_search_direct_groups` for simplest API.

pub fn cloudidentity_groups_memberships_search_direct_groups_builder(
    client: &SimpleHttpClient,
    parent: &str,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/memberships:searchDirectGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}/memberships:searchDirectGroups
/// Searches direct groups of a member.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_memberships_search_direct_groups_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_memberships_search_direct_groups_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_search_direct_groups_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchDirectGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchDirectGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}/memberships:searchDirectGroups
/// Searches direct groups of a member.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_search_direct_groups_builder()` + `cloudidentity_groups_memberships_search_direct_groups_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_search_direct_groups(
    client: &SimpleHttpClient,
    parent: &str,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchDirectGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_memberships_search_direct_groups_builder(
        client, parent, orderBy, pageSize, pageToken, query,
    )?;
    cloudidentity_groups_memberships_search_direct_groups_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships:searchTransitiveGroups
/// Search transitive groups of a member. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A transitive group is any group that has a direct or indirect membership to the member. Actor must have view permissions all transitive groups.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_search_transitive_groups_execute()` to send, or `cloudidentity_groups_memberships_search_transitive_groups` for simplest API.

pub fn cloudidentity_groups_memberships_search_transitive_groups_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/memberships:searchTransitiveGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}/memberships:searchTransitiveGroups
/// Search transitive groups of a member. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A transitive group is any group that has a direct or indirect membership to the member. Actor must have view permissions all transitive groups.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_memberships_search_transitive_groups_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_memberships_search_transitive_groups_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_search_transitive_groups_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchTransitiveGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchTransitiveGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}/memberships:searchTransitiveGroups
/// Search transitive groups of a member. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the member is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A transitive group is any group that has a direct or indirect membership to the member. Actor must have view permissions all transitive groups.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_search_transitive_groups_builder()` + `cloudidentity_groups_memberships_search_transitive_groups_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_search_transitive_groups(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchTransitiveGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_memberships_search_transitive_groups_builder(
        client, parent, pageSize, pageToken, query,
    )?;
    cloudidentity_groups_memberships_search_transitive_groups_execute(builder)
}

/// GET v1/groups/{groupsId}/memberships:searchTransitiveMemberships
/// Search transitive memberships of a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the group is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A transitive membership is any direct or indirect membership of a group. Actor must have view permissions to all transitive memberships.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_groups_memberships_search_transitive_memberships_execute()` to send, or `cloudidentity_groups_memberships_search_transitive_memberships` for simplest API.

pub fn cloudidentity_groups_memberships_search_transitive_memberships_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{}/memberships:searchTransitiveMemberships",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/groups/{groupsId}/memberships:searchTransitiveMemberships
/// Search transitive memberships of a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the group is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A transitive membership is any direct or indirect membership of a group. Actor must have view permissions to all transitive memberships.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_groups_memberships_search_transitive_memberships_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_groups_memberships_search_transitive_memberships_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_search_transitive_memberships_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchTransitiveMembershipsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchTransitiveMembershipsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/groups/{groupsId}/memberships:searchTransitiveMemberships
/// Search transitive memberships of a group. **Note:** This feature is only available to Google Workspace Enterprise Standard, Enterprise Plus, and Enterprise for Education; and Cloud Identity Premium accounts. If the account of the group is not one of these, a 403 (PERMISSION_DENIED) HTTP status code will be returned. A transitive membership is any direct or indirect membership of a group. Actor must have view permissions to all transitive memberships.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_groups_memberships_search_transitive_memberships_builder()` + `cloudidentity_groups_memberships_search_transitive_memberships_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_groups_memberships_search_transitive_memberships(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchTransitiveMembershipsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_groups_memberships_search_transitive_memberships_builder(
        client, parent, pageSize, pageToken,
    )?;
    cloudidentity_groups_memberships_search_transitive_memberships_execute(builder)
}

/// GET v1/inboundOidcSsoProfiles
/// Creates an InboundOidcSsoProfile for a customer. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_oidc_sso_profiles_create_execute()` to send, or `cloudidentity_inbound_oidc_sso_profiles_create` for simplest API.

pub fn cloudidentity_inbound_oidc_sso_profiles_create_builder(
    client: &SimpleHttpClient,
    body: &InboundOidcSsoProfile,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/inboundOidcSsoProfiles",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundOidcSsoProfiles
/// Creates an InboundOidcSsoProfile for a customer. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_oidc_sso_profiles_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_oidc_sso_profiles_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_oidc_sso_profiles_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundOidcSsoProfiles
/// Creates an InboundOidcSsoProfile for a customer. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_oidc_sso_profiles_create_builder()` + `cloudidentity_inbound_oidc_sso_profiles_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_oidc_sso_profiles_create(
    client: &SimpleHttpClient,
    body: &InboundOidcSsoProfile,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_oidc_sso_profiles_create_builder(client, body)?;
    cloudidentity_inbound_oidc_sso_profiles_create_execute(builder)
}

/// GET v1/inboundOidcSsoProfiles/{inboundOidcSsoProfilesId}
/// Deletes an InboundOidcSsoProfile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_oidc_sso_profiles_delete_execute()` to send, or `cloudidentity_inbound_oidc_sso_profiles_delete` for simplest API.

pub fn cloudidentity_inbound_oidc_sso_profiles_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/inboundOidcSsoProfiles/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/inboundOidcSsoProfiles/{inboundOidcSsoProfilesId}
/// Deletes an InboundOidcSsoProfile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_oidc_sso_profiles_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_oidc_sso_profiles_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_oidc_sso_profiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundOidcSsoProfiles/{inboundOidcSsoProfilesId}
/// Deletes an InboundOidcSsoProfile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_oidc_sso_profiles_delete_builder()` + `cloudidentity_inbound_oidc_sso_profiles_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_oidc_sso_profiles_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_oidc_sso_profiles_delete_builder(client, name)?;
    cloudidentity_inbound_oidc_sso_profiles_delete_execute(builder)
}

/// GET v1/inboundOidcSsoProfiles/{inboundOidcSsoProfilesId}
/// Gets an InboundOidcSsoProfile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_oidc_sso_profiles_get_execute()` to send, or `cloudidentity_inbound_oidc_sso_profiles_get` for simplest API.

pub fn cloudidentity_inbound_oidc_sso_profiles_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/inboundOidcSsoProfiles/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/inboundOidcSsoProfiles/{inboundOidcSsoProfilesId}
/// Gets an InboundOidcSsoProfile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_oidc_sso_profiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_oidc_sso_profiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_oidc_sso_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InboundOidcSsoProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InboundOidcSsoProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundOidcSsoProfiles/{inboundOidcSsoProfilesId}
/// Gets an InboundOidcSsoProfile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_oidc_sso_profiles_get_builder()` + `cloudidentity_inbound_oidc_sso_profiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_oidc_sso_profiles_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InboundOidcSsoProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_oidc_sso_profiles_get_builder(client, name)?;
    cloudidentity_inbound_oidc_sso_profiles_get_execute(builder)
}

/// GET v1/inboundOidcSsoProfiles
/// Lists InboundOidcSsoProfile objects for a Google enterprise customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_oidc_sso_profiles_list_execute()` to send, or `cloudidentity_inbound_oidc_sso_profiles_list` for simplest API.

pub fn cloudidentity_inbound_oidc_sso_profiles_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/inboundOidcSsoProfiles",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/inboundOidcSsoProfiles
/// Lists InboundOidcSsoProfile objects for a Google enterprise customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_oidc_sso_profiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_oidc_sso_profiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_oidc_sso_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInboundOidcSsoProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInboundOidcSsoProfilesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundOidcSsoProfiles
/// Lists InboundOidcSsoProfile objects for a Google enterprise customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_oidc_sso_profiles_list_builder()` + `cloudidentity_inbound_oidc_sso_profiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_oidc_sso_profiles_list(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInboundOidcSsoProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_inbound_oidc_sso_profiles_list_builder(client, filter, pageSize, pageToken)?;
    cloudidentity_inbound_oidc_sso_profiles_list_execute(builder)
}

/// GET v1/inboundOidcSsoProfiles/{inboundOidcSsoProfilesId}
/// Updates an InboundOidcSsoProfile. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_oidc_sso_profiles_patch_execute()` to send, or `cloudidentity_inbound_oidc_sso_profiles_patch` for simplest API.

pub fn cloudidentity_inbound_oidc_sso_profiles_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &InboundOidcSsoProfile,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/inboundOidcSsoProfiles/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundOidcSsoProfiles/{inboundOidcSsoProfilesId}
/// Updates an InboundOidcSsoProfile. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_oidc_sso_profiles_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_oidc_sso_profiles_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_oidc_sso_profiles_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundOidcSsoProfiles/{inboundOidcSsoProfilesId}
/// Updates an InboundOidcSsoProfile. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_oidc_sso_profiles_patch_builder()` + `cloudidentity_inbound_oidc_sso_profiles_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_oidc_sso_profiles_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &InboundOidcSsoProfile,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_inbound_oidc_sso_profiles_patch_builder(client, name, updateMask, body)?;
    cloudidentity_inbound_oidc_sso_profiles_patch_execute(builder)
}

/// GET v1/inboundSamlSsoProfiles
/// Creates an InboundSamlSsoProfile for a customer. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_saml_sso_profiles_create_execute()` to send, or `cloudidentity_inbound_saml_sso_profiles_create` for simplest API.

pub fn cloudidentity_inbound_saml_sso_profiles_create_builder(
    client: &SimpleHttpClient,
    body: &InboundSamlSsoProfile,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/inboundSamlSsoProfiles",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSamlSsoProfiles
/// Creates an InboundSamlSsoProfile for a customer. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_saml_sso_profiles_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSamlSsoProfiles
/// Creates an InboundSamlSsoProfile for a customer. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_create_builder()` + `cloudidentity_inbound_saml_sso_profiles_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_create(
    client: &SimpleHttpClient,
    body: &InboundSamlSsoProfile,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_saml_sso_profiles_create_builder(client, body)?;
    cloudidentity_inbound_saml_sso_profiles_create_execute(builder)
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}
/// Deletes an InboundSamlSsoProfile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_saml_sso_profiles_delete_execute()` to send, or `cloudidentity_inbound_saml_sso_profiles_delete` for simplest API.

pub fn cloudidentity_inbound_saml_sso_profiles_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/inboundSamlSsoProfiles/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}
/// Deletes an InboundSamlSsoProfile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_saml_sso_profiles_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}
/// Deletes an InboundSamlSsoProfile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_delete_builder()` + `cloudidentity_inbound_saml_sso_profiles_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_saml_sso_profiles_delete_builder(client, name)?;
    cloudidentity_inbound_saml_sso_profiles_delete_execute(builder)
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}
/// Gets an InboundSamlSsoProfile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_saml_sso_profiles_get_execute()` to send, or `cloudidentity_inbound_saml_sso_profiles_get` for simplest API.

pub fn cloudidentity_inbound_saml_sso_profiles_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/inboundSamlSsoProfiles/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}
/// Gets an InboundSamlSsoProfile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_saml_sso_profiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InboundSamlSsoProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InboundSamlSsoProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}
/// Gets an InboundSamlSsoProfile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_get_builder()` + `cloudidentity_inbound_saml_sso_profiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InboundSamlSsoProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_saml_sso_profiles_get_builder(client, name)?;
    cloudidentity_inbound_saml_sso_profiles_get_execute(builder)
}

/// GET v1/inboundSamlSsoProfiles
/// Lists InboundSamlSsoProfiles for a customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_saml_sso_profiles_list_execute()` to send, or `cloudidentity_inbound_saml_sso_profiles_list` for simplest API.

pub fn cloudidentity_inbound_saml_sso_profiles_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/inboundSamlSsoProfiles",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/inboundSamlSsoProfiles
/// Lists InboundSamlSsoProfiles for a customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_saml_sso_profiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInboundSamlSsoProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInboundSamlSsoProfilesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSamlSsoProfiles
/// Lists InboundSamlSsoProfiles for a customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_list_builder()` + `cloudidentity_inbound_saml_sso_profiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_list(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInboundSamlSsoProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_inbound_saml_sso_profiles_list_builder(client, filter, pageSize, pageToken)?;
    cloudidentity_inbound_saml_sso_profiles_list_execute(builder)
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}
/// Updates an InboundSamlSsoProfile. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_saml_sso_profiles_patch_execute()` to send, or `cloudidentity_inbound_saml_sso_profiles_patch` for simplest API.

pub fn cloudidentity_inbound_saml_sso_profiles_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &InboundSamlSsoProfile,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/inboundSamlSsoProfiles/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}
/// Updates an InboundSamlSsoProfile. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_saml_sso_profiles_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}
/// Updates an InboundSamlSsoProfile. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_patch_builder()` + `cloudidentity_inbound_saml_sso_profiles_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &InboundSamlSsoProfile,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_inbound_saml_sso_profiles_patch_builder(client, name, updateMask, body)?;
    cloudidentity_inbound_saml_sso_profiles_patch_execute(builder)
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials:add
/// Adds an IdpCredential. Up to 2 credentials are allowed. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_execute()` to send, or `cloudidentity_inbound_saml_sso_profiles_idp_credentials_add` for simplest API.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AddIdpCredentialRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/inboundSamlSsoProfiles/{}/idpCredentials:add",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials:add
/// Adds an IdpCredential. Up to 2 credentials are allowed. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials:add
/// Adds an IdpCredential. Up to 2 credentials are allowed. When the target customer has enabled [Multi-party approval for sensitive actions](<https://support.google.`com/a/answer/13790448`>), the Operation in the response will have "done": `false`, it will not have a response, and the metadata will have "state": "awaiting-multi-party-approval".
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_builder()` + `cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_add(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AddIdpCredentialRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_builder(client, parent, body)?;
    cloudidentity_inbound_saml_sso_profiles_idp_credentials_add_execute(builder)
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials/{idpCredentialsId}
/// Deletes an IdpCredential.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_delete_execute()` to send, or `cloudidentity_inbound_saml_sso_profiles_idp_credentials_delete` for simplest API.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/inboundSamlSsoProfiles/{}/idpCredentials/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials/{idpCredentialsId}
/// Deletes an IdpCredential.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_saml_sso_profiles_idp_credentials_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials/{idpCredentialsId}
/// Deletes an IdpCredential.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_delete_builder()` + `cloudidentity_inbound_saml_sso_profiles_idp_credentials_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_inbound_saml_sso_profiles_idp_credentials_delete_builder(client, name)?;
    cloudidentity_inbound_saml_sso_profiles_idp_credentials_delete_execute(builder)
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials/{idpCredentialsId}
/// Gets an IdpCredential.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_get_execute()` to send, or `cloudidentity_inbound_saml_sso_profiles_idp_credentials_get` for simplest API.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/inboundSamlSsoProfiles/{}/idpCredentials/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials/{idpCredentialsId}
/// Gets an IdpCredential.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_saml_sso_profiles_idp_credentials_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IdpCredential>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdpCredential = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials/{idpCredentialsId}
/// Gets an IdpCredential.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_get_builder()` + `cloudidentity_inbound_saml_sso_profiles_idp_credentials_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IdpCredential>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_inbound_saml_sso_profiles_idp_credentials_get_builder(client, name)?;
    cloudidentity_inbound_saml_sso_profiles_idp_credentials_get_execute(builder)
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials
/// Returns a list of IdpCredentials in an InboundSamlSsoProfile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_execute()` to send, or `cloudidentity_inbound_saml_sso_profiles_idp_credentials_list` for simplest API.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/inboundSamlSsoProfiles/{}/idpCredentials",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials
/// Returns a list of IdpCredentials in an InboundSamlSsoProfile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListIdpCredentialsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListIdpCredentialsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSamlSsoProfiles/{inboundSamlSsoProfilesId}/idpCredentials
/// Returns a list of IdpCredentials in an InboundSamlSsoProfile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_builder()` + `cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_saml_sso_profiles_idp_credentials_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListIdpCredentialsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    cloudidentity_inbound_saml_sso_profiles_idp_credentials_list_execute(builder)
}

/// GET v1/inboundSsoAssignments
/// Creates an InboundSsoAssignment for users and devices in a Customer under a given Group or OrgUnit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_sso_assignments_create_execute()` to send, or `cloudidentity_inbound_sso_assignments_create` for simplest API.

pub fn cloudidentity_inbound_sso_assignments_create_builder(
    client: &SimpleHttpClient,
    body: &InboundSsoAssignment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/inboundSsoAssignments",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSsoAssignments
/// Creates an InboundSsoAssignment for users and devices in a Customer under a given Group or OrgUnit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_sso_assignments_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_sso_assignments_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_sso_assignments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSsoAssignments
/// Creates an InboundSsoAssignment for users and devices in a Customer under a given Group or OrgUnit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_sso_assignments_create_builder()` + `cloudidentity_inbound_sso_assignments_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_sso_assignments_create(
    client: &SimpleHttpClient,
    body: &InboundSsoAssignment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_sso_assignments_create_builder(client, body)?;
    cloudidentity_inbound_sso_assignments_create_execute(builder)
}

/// GET v1/inboundSsoAssignments/{inboundSsoAssignmentsId}
/// Deletes an InboundSsoAssignment. To disable SSO, Create (or Update) an assignment that has sso_mode == SSO_OFF.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_sso_assignments_delete_execute()` to send, or `cloudidentity_inbound_sso_assignments_delete` for simplest API.

pub fn cloudidentity_inbound_sso_assignments_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/inboundSsoAssignments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/inboundSsoAssignments/{inboundSsoAssignmentsId}
/// Deletes an InboundSsoAssignment. To disable SSO, Create (or Update) an assignment that has sso_mode == SSO_OFF.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_sso_assignments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_sso_assignments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_sso_assignments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSsoAssignments/{inboundSsoAssignmentsId}
/// Deletes an InboundSsoAssignment. To disable SSO, Create (or Update) an assignment that has sso_mode == SSO_OFF.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_sso_assignments_delete_builder()` + `cloudidentity_inbound_sso_assignments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_sso_assignments_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_sso_assignments_delete_builder(client, name)?;
    cloudidentity_inbound_sso_assignments_delete_execute(builder)
}

/// GET v1/inboundSsoAssignments/{inboundSsoAssignmentsId}
/// Gets an InboundSsoAssignment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_sso_assignments_get_execute()` to send, or `cloudidentity_inbound_sso_assignments_get` for simplest API.

pub fn cloudidentity_inbound_sso_assignments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/inboundSsoAssignments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/inboundSsoAssignments/{inboundSsoAssignmentsId}
/// Gets an InboundSsoAssignment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_sso_assignments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_sso_assignments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_sso_assignments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InboundSsoAssignment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InboundSsoAssignment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSsoAssignments/{inboundSsoAssignmentsId}
/// Gets an InboundSsoAssignment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_sso_assignments_get_builder()` + `cloudidentity_inbound_sso_assignments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_sso_assignments_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InboundSsoAssignment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_inbound_sso_assignments_get_builder(client, name)?;
    cloudidentity_inbound_sso_assignments_get_execute(builder)
}

/// GET v1/inboundSsoAssignments
/// Lists the InboundSsoAssignments for a Customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_sso_assignments_list_execute()` to send, or `cloudidentity_inbound_sso_assignments_list` for simplest API.

pub fn cloudidentity_inbound_sso_assignments_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/inboundSsoAssignments",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/inboundSsoAssignments
/// Lists the InboundSsoAssignments for a Customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_sso_assignments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_sso_assignments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_sso_assignments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInboundSsoAssignmentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInboundSsoAssignmentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSsoAssignments
/// Lists the InboundSsoAssignments for a Customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_sso_assignments_list_builder()` + `cloudidentity_inbound_sso_assignments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_sso_assignments_list(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInboundSsoAssignmentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_inbound_sso_assignments_list_builder(client, filter, pageSize, pageToken)?;
    cloudidentity_inbound_sso_assignments_list_execute(builder)
}

/// GET v1/inboundSsoAssignments/{inboundSsoAssignmentsId}
/// Updates an InboundSsoAssignment. The body of this request is the inbound_sso_assignment field and the update_mask is relative to that. For example: a PATCH to /v1/`inboundSsoA``ssignments/0abcdefg1234567`&update_mask=rank with a body of { "rank": 1 } moves that (presumably group-targeted) SSO assignment to the highest priority and shifts any other group-targeted assignments down in priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_inbound_sso_assignments_patch_execute()` to send, or `cloudidentity_inbound_sso_assignments_patch` for simplest API.

pub fn cloudidentity_inbound_sso_assignments_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &InboundSsoAssignment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudidentity.googleapis.com/v1/inboundSsoAssignments/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSsoAssignments/{inboundSsoAssignmentsId}
/// Updates an InboundSsoAssignment. The body of this request is the inbound_sso_assignment field and the update_mask is relative to that. For example: a PATCH to /v1/`inboundSsoA``ssignments/0abcdefg1234567`&update_mask=rank with a body of { "rank": 1 } moves that (presumably group-targeted) SSO assignment to the highest priority and shifts any other group-targeted assignments down in priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_inbound_sso_assignments_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_inbound_sso_assignments_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_sso_assignments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/inboundSsoAssignments/{inboundSsoAssignmentsId}
/// Updates an InboundSsoAssignment. The body of this request is the inbound_sso_assignment field and the update_mask is relative to that. For example: a PATCH to /v1/`inboundSsoA``ssignments/0abcdefg1234567`&update_mask=rank with a body of { "rank": 1 } moves that (presumably group-targeted) SSO assignment to the highest priority and shifts any other group-targeted assignments down in priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_inbound_sso_assignments_patch_builder()` + `cloudidentity_inbound_sso_assignments_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_inbound_sso_assignments_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &InboundSsoAssignment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudidentity_inbound_sso_assignments_patch_builder(client, name, updateMask, body)?;
    cloudidentity_inbound_sso_assignments_patch_execute(builder)
}

/// GET v1/policies/{policiesId}
/// Get a policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_policies_get_execute()` to send, or `cloudidentity_policies_get` for simplest API.

pub fn cloudidentity_policies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/policies/{}", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/policies/{policiesId}
/// Get a policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/policies/{policiesId}
/// Get a policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_policies_get_builder()` + `cloudidentity_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_policies_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudidentity_policies_get_builder(client, name)?;
    cloudidentity_policies_get_execute(builder)
}

/// GET v1/policies
/// List policies.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudidentity_policies_list_execute()` to send, or `cloudidentity_policies_list` for simplest API.

pub fn cloudidentity_policies_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudidentity.googleapis.com/v1/policies",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/policies
/// List policies.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudidentity_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudidentity_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/policies
/// List policies.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudidentity_policies_list_builder()` + `cloudidentity_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudidentity_policies_list(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudidentity_policies_list_builder(client, filter, pageSize, pageToken)?;
    cloudidentity_policies_list_execute(builder)
}
