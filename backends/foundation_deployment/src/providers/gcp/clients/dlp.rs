//! Auto-generated API clients for Google Cloud Platform - dlp.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v2/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_info_types_list_execute()` to send, or `dlp_info_types_list` for simplest API.

pub fn dlp_info_types_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    languageCode: Option<&str>,
    locationId: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/infoTypes",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "languageCode",
            languageCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_info_types_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_info_types_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_info_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInfoTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_info_types_list_builder()` + `dlp_info_types_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_info_types_list(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    languageCode: Option<&str>,
    locationId: Option<&str>,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_info_types_list_builder(client, filter, languageCode, locationId, parent)?;
    dlp_info_types_list_execute(builder)
}

/// GET v2/{+parent}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_locations_info_types_list_execute()` to send, or `dlp_locations_info_types_list` for simplest API.

pub fn dlp_locations_info_types_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    languageCode: Option<&str>,
    locationId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/infoTypes", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "languageCode",
            languageCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_locations_info_types_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_locations_info_types_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_locations_info_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInfoTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_locations_info_types_list_builder()` + `dlp_locations_info_types_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_locations_info_types_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    languageCode: Option<&str>,
    locationId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_locations_info_types_list_builder(client, parent, filter, languageCode, locationId)?;
    dlp_locations_info_types_list_execute(builder)
}

/// GET v2/{+parent}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_deidentify_templates_create_execute()` to send, or `dlp_organizations_deidentify_templates_create` for simplest API.

pub fn dlp_organizations_deidentify_templates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateDeidentifyTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/deidentifyTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_deidentify_templates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_deidentify_templates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_deidentify_templates_create_builder()` + `dlp_organizations_deidentify_templates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateDeidentifyTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_deidentify_templates_create_builder(client, parent, body)?;
    dlp_organizations_deidentify_templates_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_deidentify_templates_delete_execute()` to send, or `dlp_organizations_deidentify_templates_delete` for simplest API.

pub fn dlp_organizations_deidentify_templates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_deidentify_templates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_deidentify_templates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_deidentify_templates_delete_builder()` + `dlp_organizations_deidentify_templates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_deidentify_templates_delete_builder(client, name)?;
    dlp_organizations_deidentify_templates_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_deidentify_templates_get_execute()` to send, or `dlp_organizations_deidentify_templates_get` for simplest API.

pub fn dlp_organizations_deidentify_templates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_deidentify_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_deidentify_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_deidentify_templates_get_builder()` + `dlp_organizations_deidentify_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_deidentify_templates_get_builder(client, name)?;
    dlp_organizations_deidentify_templates_get_execute(builder)
}

/// GET v2/{+parent}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_deidentify_templates_list_execute()` to send, or `dlp_organizations_deidentify_templates_list` for simplest API.

pub fn dlp_organizations_deidentify_templates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/deidentifyTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_deidentify_templates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_deidentify_templates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDeidentifyTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_deidentify_templates_list_builder()` + `dlp_organizations_deidentify_templates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_list(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_deidentify_templates_list_builder(
        client, parent, locationId, orderBy, pageSize, pageToken,
    )?;
    dlp_organizations_deidentify_templates_list_execute(builder)
}

/// GET v2/{+name}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_deidentify_templates_patch_execute()` to send, or `dlp_organizations_deidentify_templates_patch` for simplest API.

pub fn dlp_organizations_deidentify_templates_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateDeidentifyTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_deidentify_templates_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_deidentify_templates_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_deidentify_templates_patch_builder()` + `dlp_organizations_deidentify_templates_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateDeidentifyTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_deidentify_templates_patch_builder(client, name, body)?;
    dlp_organizations_deidentify_templates_patch_execute(builder)
}

/// GET v2/{+parent}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_inspect_templates_create_execute()` to send, or `dlp_organizations_inspect_templates_create` for simplest API.

pub fn dlp_organizations_inspect_templates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateInspectTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/inspectTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_inspect_templates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_inspect_templates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_inspect_templates_create_builder()` + `dlp_organizations_inspect_templates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateInspectTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_inspect_templates_create_builder(client, parent, body)?;
    dlp_organizations_inspect_templates_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_inspect_templates_delete_execute()` to send, or `dlp_organizations_inspect_templates_delete` for simplest API.

pub fn dlp_organizations_inspect_templates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_inspect_templates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_inspect_templates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_inspect_templates_delete_builder()` + `dlp_organizations_inspect_templates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_inspect_templates_delete_builder(client, name)?;
    dlp_organizations_inspect_templates_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_inspect_templates_get_execute()` to send, or `dlp_organizations_inspect_templates_get` for simplest API.

pub fn dlp_organizations_inspect_templates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_inspect_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_inspect_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_inspect_templates_get_builder()` + `dlp_organizations_inspect_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_inspect_templates_get_builder(client, name)?;
    dlp_organizations_inspect_templates_get_execute(builder)
}

/// GET v2/{+parent}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_inspect_templates_list_execute()` to send, or `dlp_organizations_inspect_templates_list` for simplest API.

pub fn dlp_organizations_inspect_templates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/inspectTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_inspect_templates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_inspect_templates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInspectTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_inspect_templates_list_builder()` + `dlp_organizations_inspect_templates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_list(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_inspect_templates_list_builder(
        client, parent, locationId, orderBy, pageSize, pageToken,
    )?;
    dlp_organizations_inspect_templates_list_execute(builder)
}

/// GET v2/{+name}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_inspect_templates_patch_execute()` to send, or `dlp_organizations_inspect_templates_patch` for simplest API.

pub fn dlp_organizations_inspect_templates_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateInspectTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_inspect_templates_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_inspect_templates_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_inspect_templates_patch_builder()` + `dlp_organizations_inspect_templates_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateInspectTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_inspect_templates_patch_builder(client, name, body)?;
    dlp_organizations_inspect_templates_patch_execute(builder)
}

/// GET v2/{+name}
/// Gets a column data profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_column_data_profiles_get_execute()` to send, or `dlp_organizations_locations_column_data_profiles_get` for simplest API.

pub fn dlp_organizations_locations_column_data_profiles_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a column data profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_column_data_profiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_column_data_profiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_column_data_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ColumnDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ColumnDataProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a column data profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_column_data_profiles_get_builder()` + `dlp_organizations_locations_column_data_profiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_column_data_profiles_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ColumnDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_column_data_profiles_get_builder(client, name)?;
    dlp_organizations_locations_column_data_profiles_get_execute(builder)
}

/// GET v2/{+parent}/columnDataProfiles
/// Lists column data profiles for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_column_data_profiles_list_execute()` to send, or `dlp_organizations_locations_column_data_profiles_list` for simplest API.

pub fn dlp_organizations_locations_column_data_profiles_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/columnDataProfiles",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/columnDataProfiles
/// Lists column data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_column_data_profiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_column_data_profiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_column_data_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListColumnDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListColumnDataProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/columnDataProfiles
/// Lists column data profiles for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_column_data_profiles_list_builder()` + `dlp_organizations_locations_column_data_profiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_column_data_profiles_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListColumnDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_column_data_profiles_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    dlp_organizations_locations_column_data_profiles_list_execute(builder)
}

/// GET v2/{+parent}/connections
/// Create a Connection to an external data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_connections_create_execute()` to send, or `dlp_organizations_locations_connections_create` for simplest API.

pub fn dlp_organizations_locations_connections_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateConnectionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/connections", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/connections
/// Create a Connection to an external data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_connections_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_connections_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2Connection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/connections
/// Create a Connection to an external data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_connections_create_builder()` + `dlp_organizations_locations_connections_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateConnectionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_connections_create_builder(client, parent, body)?;
    dlp_organizations_locations_connections_create_execute(builder)
}

/// GET v2/{+name}
/// Delete a Connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_connections_delete_execute()` to send, or `dlp_organizations_locations_connections_delete` for simplest API.

pub fn dlp_organizations_locations_connections_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Delete a Connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_connections_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_connections_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Delete a Connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_connections_delete_builder()` + `dlp_organizations_locations_connections_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_connections_delete_builder(client, name)?;
    dlp_organizations_locations_connections_delete_execute(builder)
}

/// GET v2/{+name}
/// Get a Connection by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_connections_get_execute()` to send, or `dlp_organizations_locations_connections_get` for simplest API.

pub fn dlp_organizations_locations_connections_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get a Connection by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_connections_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_connections_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2Connection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get a Connection by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_connections_get_builder()` + `dlp_organizations_locations_connections_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_connections_get_builder(client, name)?;
    dlp_organizations_locations_connections_get_execute(builder)
}

/// GET v2/{+parent}/connections
/// Lists Connections in a parent. Use SearchConnections to see all connections within an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_connections_list_execute()` to send, or `dlp_organizations_locations_connections_list` for simplest API.

pub fn dlp_organizations_locations_connections_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/connections", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/connections
/// Lists Connections in a parent. Use SearchConnections to see all connections within an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_connections_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_connections_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListConnectionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/connections
/// Lists Connections in a parent. Use SearchConnections to see all connections within an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_connections_list_builder()` + `dlp_organizations_locations_connections_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_connections_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    dlp_organizations_locations_connections_list_execute(builder)
}

/// GET v2/{+name}
/// Update a Connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_connections_patch_execute()` to send, or `dlp_organizations_locations_connections_patch` for simplest API.

pub fn dlp_organizations_locations_connections_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateConnectionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Update a Connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_connections_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_connections_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2Connection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Update a Connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_connections_patch_builder()` + `dlp_organizations_locations_connections_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateConnectionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_connections_patch_builder(client, name, body)?;
    dlp_organizations_locations_connections_patch_execute(builder)
}

/// GET v2/{+parent}/connections:search
/// Searches for Connections in a parent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_connections_search_execute()` to send, or `dlp_organizations_locations_connections_search` for simplest API.

pub fn dlp_organizations_locations_connections_search_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/connections:search",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/connections:search
/// Searches for Connections in a parent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_connections_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_connections_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2SearchConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2SearchConnectionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/connections:search
/// Searches for Connections in a parent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_connections_search_builder()` + `dlp_organizations_locations_connections_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_search(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2SearchConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_connections_search_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    dlp_organizations_locations_connections_search_execute(builder)
}

/// GET v2/{+parent}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_deidentify_templates_create_execute()` to send, or `dlp_organizations_locations_deidentify_templates_create` for simplest API.

pub fn dlp_organizations_locations_deidentify_templates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateDeidentifyTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/deidentifyTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_deidentify_templates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_deidentify_templates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_deidentify_templates_create_builder()` + `dlp_organizations_locations_deidentify_templates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateDeidentifyTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_organizations_locations_deidentify_templates_create_builder(client, parent, body)?;
    dlp_organizations_locations_deidentify_templates_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_deidentify_templates_delete_execute()` to send, or `dlp_organizations_locations_deidentify_templates_delete` for simplest API.

pub fn dlp_organizations_locations_deidentify_templates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_deidentify_templates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_deidentify_templates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_deidentify_templates_delete_builder()` + `dlp_organizations_locations_deidentify_templates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_deidentify_templates_delete_builder(client, name)?;
    dlp_organizations_locations_deidentify_templates_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_deidentify_templates_get_execute()` to send, or `dlp_organizations_locations_deidentify_templates_get` for simplest API.

pub fn dlp_organizations_locations_deidentify_templates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_deidentify_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_deidentify_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_deidentify_templates_get_builder()` + `dlp_organizations_locations_deidentify_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_deidentify_templates_get_builder(client, name)?;
    dlp_organizations_locations_deidentify_templates_get_execute(builder)
}

/// GET v2/{+parent}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_deidentify_templates_list_execute()` to send, or `dlp_organizations_locations_deidentify_templates_list` for simplest API.

pub fn dlp_organizations_locations_deidentify_templates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/deidentifyTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_deidentify_templates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_deidentify_templates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDeidentifyTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_deidentify_templates_list_builder()` + `dlp_organizations_locations_deidentify_templates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_list(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_deidentify_templates_list_builder(
        client, parent, locationId, orderBy, pageSize, pageToken,
    )?;
    dlp_organizations_locations_deidentify_templates_list_execute(builder)
}

/// GET v2/{+name}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_deidentify_templates_patch_execute()` to send, or `dlp_organizations_locations_deidentify_templates_patch` for simplest API.

pub fn dlp_organizations_locations_deidentify_templates_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateDeidentifyTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_deidentify_templates_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_deidentify_templates_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_deidentify_templates_patch_builder()` + `dlp_organizations_locations_deidentify_templates_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateDeidentifyTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_organizations_locations_deidentify_templates_patch_builder(client, name, body)?;
    dlp_organizations_locations_deidentify_templates_patch_execute(builder)
}

/// GET v2/{+parent}/discoveryConfigs
/// Creates a config for discovery to scan and profile storage.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_discovery_configs_create_execute()` to send, or `dlp_organizations_locations_discovery_configs_create` for simplest API.

pub fn dlp_organizations_locations_discovery_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateDiscoveryConfigRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/discoveryConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/discoveryConfigs
/// Creates a config for discovery to scan and profile storage.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_discovery_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_discovery_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DiscoveryConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/discoveryConfigs
/// Creates a config for discovery to scan and profile storage.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_discovery_configs_create_builder()` + `dlp_organizations_locations_discovery_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateDiscoveryConfigRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_organizations_locations_discovery_configs_create_builder(client, parent, body)?;
    dlp_organizations_locations_discovery_configs_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_discovery_configs_delete_execute()` to send, or `dlp_organizations_locations_discovery_configs_delete` for simplest API.

pub fn dlp_organizations_locations_discovery_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_discovery_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_discovery_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_discovery_configs_delete_builder()` + `dlp_organizations_locations_discovery_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_discovery_configs_delete_builder(client, name)?;
    dlp_organizations_locations_discovery_configs_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_discovery_configs_get_execute()` to send, or `dlp_organizations_locations_discovery_configs_get` for simplest API.

pub fn dlp_organizations_locations_discovery_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_discovery_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_discovery_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DiscoveryConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_discovery_configs_get_builder()` + `dlp_organizations_locations_discovery_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_discovery_configs_get_builder(client, name)?;
    dlp_organizations_locations_discovery_configs_get_execute(builder)
}

/// GET v2/{+parent}/discoveryConfigs
/// Lists discovery configurations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_discovery_configs_list_execute()` to send, or `dlp_organizations_locations_discovery_configs_list` for simplest API.

pub fn dlp_organizations_locations_discovery_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/discoveryConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/discoveryConfigs
/// Lists discovery configurations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_discovery_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_discovery_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDiscoveryConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDiscoveryConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/discoveryConfigs
/// Lists discovery configurations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_discovery_configs_list_builder()` + `dlp_organizations_locations_discovery_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDiscoveryConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_discovery_configs_list_builder(
        client, parent, orderBy, pageSize, pageToken,
    )?;
    dlp_organizations_locations_discovery_configs_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_discovery_configs_patch_execute()` to send, or `dlp_organizations_locations_discovery_configs_patch` for simplest API.

pub fn dlp_organizations_locations_discovery_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateDiscoveryConfigRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_discovery_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_discovery_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DiscoveryConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_discovery_configs_patch_builder()` + `dlp_organizations_locations_discovery_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateDiscoveryConfigRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_discovery_configs_patch_builder(client, name, body)?;
    dlp_organizations_locations_discovery_configs_patch_execute(builder)
}

/// GET v2/{+parent}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_dlp_jobs_list_execute()` to send, or `dlp_organizations_locations_dlp_jobs_list` for simplest API.

pub fn dlp_organizations_locations_dlp_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/dlpJobs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "type",
            type_rs
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_dlp_jobs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_dlp_jobs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_dlp_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDlpJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDlpJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_dlp_jobs_list_builder()` + `dlp_organizations_locations_dlp_jobs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_dlp_jobs_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    type_rs: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDlpJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_dlp_jobs_list_builder(
        client, parent, filter, locationId, orderBy, pageSize, pageToken, type_rs,
    )?;
    dlp_organizations_locations_dlp_jobs_list_execute(builder)
}

/// GET v2/{+name}
/// Delete a FileStoreDataProfile. Will not prevent the profile from being regenerated if the resource is still included in a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_file_store_data_profiles_delete_execute()` to send, or `dlp_organizations_locations_file_store_data_profiles_delete` for simplest API.

pub fn dlp_organizations_locations_file_store_data_profiles_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Delete a FileStoreDataProfile. Will not prevent the profile from being regenerated if the resource is still included in a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_file_store_data_profiles_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_file_store_data_profiles_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_file_store_data_profiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Delete a FileStoreDataProfile. Will not prevent the profile from being regenerated if the resource is still included in a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_file_store_data_profiles_delete_builder()` + `dlp_organizations_locations_file_store_data_profiles_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_file_store_data_profiles_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_organizations_locations_file_store_data_profiles_delete_builder(client, name)?;
    dlp_organizations_locations_file_store_data_profiles_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a file store data profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_file_store_data_profiles_get_execute()` to send, or `dlp_organizations_locations_file_store_data_profiles_get` for simplest API.

pub fn dlp_organizations_locations_file_store_data_profiles_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a file store data profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_file_store_data_profiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_file_store_data_profiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_file_store_data_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2FileStoreDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2FileStoreDataProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a file store data profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_file_store_data_profiles_get_builder()` + `dlp_organizations_locations_file_store_data_profiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_file_store_data_profiles_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2FileStoreDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_file_store_data_profiles_get_builder(client, name)?;
    dlp_organizations_locations_file_store_data_profiles_get_execute(builder)
}

/// GET v2/{+parent}/fileStoreDataProfiles
/// Lists file store data profiles for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_file_store_data_profiles_list_execute()` to send, or `dlp_organizations_locations_file_store_data_profiles_list` for simplest API.

pub fn dlp_organizations_locations_file_store_data_profiles_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/fileStoreDataProfiles",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/fileStoreDataProfiles
/// Lists file store data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_file_store_data_profiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_file_store_data_profiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_file_store_data_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListFileStoreDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListFileStoreDataProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/fileStoreDataProfiles
/// Lists file store data profiles for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_file_store_data_profiles_list_builder()` + `dlp_organizations_locations_file_store_data_profiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_file_store_data_profiles_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListFileStoreDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_file_store_data_profiles_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    dlp_organizations_locations_file_store_data_profiles_list_execute(builder)
}

/// GET v2/{+parent}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_info_types_list_execute()` to send, or `dlp_organizations_locations_info_types_list` for simplest API.

pub fn dlp_organizations_locations_info_types_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    languageCode: Option<&str>,
    locationId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/infoTypes", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "languageCode",
            languageCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_info_types_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_info_types_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_info_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInfoTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_info_types_list_builder()` + `dlp_organizations_locations_info_types_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_info_types_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    languageCode: Option<&str>,
    locationId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_info_types_list_builder(
        client,
        parent,
        filter,
        languageCode,
        locationId,
    )?;
    dlp_organizations_locations_info_types_list_execute(builder)
}

/// GET v2/{+parent}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_inspect_templates_create_execute()` to send, or `dlp_organizations_locations_inspect_templates_create` for simplest API.

pub fn dlp_organizations_locations_inspect_templates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateInspectTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/inspectTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_inspect_templates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_inspect_templates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_inspect_templates_create_builder()` + `dlp_organizations_locations_inspect_templates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateInspectTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_organizations_locations_inspect_templates_create_builder(client, parent, body)?;
    dlp_organizations_locations_inspect_templates_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_inspect_templates_delete_execute()` to send, or `dlp_organizations_locations_inspect_templates_delete` for simplest API.

pub fn dlp_organizations_locations_inspect_templates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_inspect_templates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_inspect_templates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_inspect_templates_delete_builder()` + `dlp_organizations_locations_inspect_templates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_inspect_templates_delete_builder(client, name)?;
    dlp_organizations_locations_inspect_templates_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_inspect_templates_get_execute()` to send, or `dlp_organizations_locations_inspect_templates_get` for simplest API.

pub fn dlp_organizations_locations_inspect_templates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_inspect_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_inspect_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_inspect_templates_get_builder()` + `dlp_organizations_locations_inspect_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_inspect_templates_get_builder(client, name)?;
    dlp_organizations_locations_inspect_templates_get_execute(builder)
}

/// GET v2/{+parent}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_inspect_templates_list_execute()` to send, or `dlp_organizations_locations_inspect_templates_list` for simplest API.

pub fn dlp_organizations_locations_inspect_templates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/inspectTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_inspect_templates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_inspect_templates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInspectTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_inspect_templates_list_builder()` + `dlp_organizations_locations_inspect_templates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_list(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_inspect_templates_list_builder(
        client, parent, locationId, orderBy, pageSize, pageToken,
    )?;
    dlp_organizations_locations_inspect_templates_list_execute(builder)
}

/// GET v2/{+name}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_inspect_templates_patch_execute()` to send, or `dlp_organizations_locations_inspect_templates_patch` for simplest API.

pub fn dlp_organizations_locations_inspect_templates_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateInspectTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_inspect_templates_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_inspect_templates_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_inspect_templates_patch_builder()` + `dlp_organizations_locations_inspect_templates_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateInspectTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_inspect_templates_patch_builder(client, name, body)?;
    dlp_organizations_locations_inspect_templates_patch_execute(builder)
}

/// GET v2/{+parent}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_job_triggers_create_execute()` to send, or `dlp_organizations_locations_job_triggers_create` for simplest API.

pub fn dlp_organizations_locations_job_triggers_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateJobTriggerRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/jobTriggers", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_job_triggers_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_job_triggers_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_job_triggers_create_builder()` + `dlp_organizations_locations_job_triggers_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateJobTriggerRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_job_triggers_create_builder(client, parent, body)?;
    dlp_organizations_locations_job_triggers_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_job_triggers_delete_execute()` to send, or `dlp_organizations_locations_job_triggers_delete` for simplest API.

pub fn dlp_organizations_locations_job_triggers_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_job_triggers_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_job_triggers_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_job_triggers_delete_builder()` + `dlp_organizations_locations_job_triggers_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_job_triggers_delete_builder(client, name)?;
    dlp_organizations_locations_job_triggers_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_job_triggers_get_execute()` to send, or `dlp_organizations_locations_job_triggers_get` for simplest API.

pub fn dlp_organizations_locations_job_triggers_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_job_triggers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_job_triggers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_job_triggers_get_builder()` + `dlp_organizations_locations_job_triggers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_job_triggers_get_builder(client, name)?;
    dlp_organizations_locations_job_triggers_get_execute(builder)
}

/// GET v2/{+parent}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_job_triggers_list_execute()` to send, or `dlp_organizations_locations_job_triggers_list` for simplest API.

pub fn dlp_organizations_locations_job_triggers_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/jobTriggers", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "type",
            type_rs
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_job_triggers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_job_triggers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListJobTriggersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListJobTriggersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_job_triggers_list_builder()` + `dlp_organizations_locations_job_triggers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    type_rs: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListJobTriggersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_job_triggers_list_builder(
        client, parent, filter, locationId, orderBy, pageSize, pageToken, type_rs,
    )?;
    dlp_organizations_locations_job_triggers_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_job_triggers_patch_execute()` to send, or `dlp_organizations_locations_job_triggers_patch` for simplest API.

pub fn dlp_organizations_locations_job_triggers_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateJobTriggerRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_job_triggers_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_job_triggers_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_job_triggers_patch_builder()` + `dlp_organizations_locations_job_triggers_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateJobTriggerRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_job_triggers_patch_builder(client, name, body)?;
    dlp_organizations_locations_job_triggers_patch_execute(builder)
}

/// GET v2/{+name}
/// Gets a project data profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_project_data_profiles_get_execute()` to send, or `dlp_organizations_locations_project_data_profiles_get` for simplest API.

pub fn dlp_organizations_locations_project_data_profiles_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a project data profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_project_data_profiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_project_data_profiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_project_data_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ProjectDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ProjectDataProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a project data profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_project_data_profiles_get_builder()` + `dlp_organizations_locations_project_data_profiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_project_data_profiles_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ProjectDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_project_data_profiles_get_builder(client, name)?;
    dlp_organizations_locations_project_data_profiles_get_execute(builder)
}

/// GET v2/{+parent}/projectDataProfiles
/// Lists project data profiles for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_project_data_profiles_list_execute()` to send, or `dlp_organizations_locations_project_data_profiles_list` for simplest API.

pub fn dlp_organizations_locations_project_data_profiles_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/projectDataProfiles",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/projectDataProfiles
/// Lists project data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_project_data_profiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_project_data_profiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_project_data_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListProjectDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListProjectDataProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/projectDataProfiles
/// Lists project data profiles for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_project_data_profiles_list_builder()` + `dlp_organizations_locations_project_data_profiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_project_data_profiles_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListProjectDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_project_data_profiles_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    dlp_organizations_locations_project_data_profiles_list_execute(builder)
}

/// GET v2/{+parent}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_stored_info_types_create_execute()` to send, or `dlp_organizations_locations_stored_info_types_create` for simplest API.

pub fn dlp_organizations_locations_stored_info_types_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateStoredInfoTypeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/storedInfoTypes",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_stored_info_types_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_stored_info_types_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_stored_info_types_create_builder()` + `dlp_organizations_locations_stored_info_types_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateStoredInfoTypeRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_organizations_locations_stored_info_types_create_builder(client, parent, body)?;
    dlp_organizations_locations_stored_info_types_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_stored_info_types_delete_execute()` to send, or `dlp_organizations_locations_stored_info_types_delete` for simplest API.

pub fn dlp_organizations_locations_stored_info_types_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_stored_info_types_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_stored_info_types_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_stored_info_types_delete_builder()` + `dlp_organizations_locations_stored_info_types_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_stored_info_types_delete_builder(client, name)?;
    dlp_organizations_locations_stored_info_types_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_stored_info_types_get_execute()` to send, or `dlp_organizations_locations_stored_info_types_get` for simplest API.

pub fn dlp_organizations_locations_stored_info_types_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_stored_info_types_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_stored_info_types_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_stored_info_types_get_builder()` + `dlp_organizations_locations_stored_info_types_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_stored_info_types_get_builder(client, name)?;
    dlp_organizations_locations_stored_info_types_get_execute(builder)
}

/// GET v2/{+parent}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_stored_info_types_list_execute()` to send, or `dlp_organizations_locations_stored_info_types_list` for simplest API.

pub fn dlp_organizations_locations_stored_info_types_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/storedInfoTypes",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_stored_info_types_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_stored_info_types_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListStoredInfoTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_stored_info_types_list_builder()` + `dlp_organizations_locations_stored_info_types_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_list(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_stored_info_types_list_builder(
        client, parent, locationId, orderBy, pageSize, pageToken,
    )?;
    dlp_organizations_locations_stored_info_types_list_execute(builder)
}

/// GET v2/{+name}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_stored_info_types_patch_execute()` to send, or `dlp_organizations_locations_stored_info_types_patch` for simplest API.

pub fn dlp_organizations_locations_stored_info_types_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateStoredInfoTypeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_stored_info_types_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_stored_info_types_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_stored_info_types_patch_builder()` + `dlp_organizations_locations_stored_info_types_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateStoredInfoTypeRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_stored_info_types_patch_builder(client, name, body)?;
    dlp_organizations_locations_stored_info_types_patch_execute(builder)
}

/// GET v2/{+name}
/// Delete a TableDataProfile. Will not prevent the profile from being regenerated if the table is still included in a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_table_data_profiles_delete_execute()` to send, or `dlp_organizations_locations_table_data_profiles_delete` for simplest API.

pub fn dlp_organizations_locations_table_data_profiles_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Delete a TableDataProfile. Will not prevent the profile from being regenerated if the table is still included in a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_table_data_profiles_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_table_data_profiles_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_table_data_profiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Delete a TableDataProfile. Will not prevent the profile from being regenerated if the table is still included in a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_table_data_profiles_delete_builder()` + `dlp_organizations_locations_table_data_profiles_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_table_data_profiles_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_table_data_profiles_delete_builder(client, name)?;
    dlp_organizations_locations_table_data_profiles_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a table data profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_table_data_profiles_get_execute()` to send, or `dlp_organizations_locations_table_data_profiles_get` for simplest API.

pub fn dlp_organizations_locations_table_data_profiles_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a table data profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_table_data_profiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_table_data_profiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_table_data_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2TableDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2TableDataProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a table data profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_table_data_profiles_get_builder()` + `dlp_organizations_locations_table_data_profiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_table_data_profiles_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2TableDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_table_data_profiles_get_builder(client, name)?;
    dlp_organizations_locations_table_data_profiles_get_execute(builder)
}

/// GET v2/{+parent}/tableDataProfiles
/// Lists table data profiles for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_table_data_profiles_list_execute()` to send, or `dlp_organizations_locations_table_data_profiles_list` for simplest API.

pub fn dlp_organizations_locations_table_data_profiles_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/tableDataProfiles",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/tableDataProfiles
/// Lists table data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_locations_table_data_profiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_locations_table_data_profiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_table_data_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListTableDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListTableDataProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/tableDataProfiles
/// Lists table data profiles for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_table_data_profiles_list_builder()` + `dlp_organizations_locations_table_data_profiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_table_data_profiles_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListTableDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_table_data_profiles_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    dlp_organizations_locations_table_data_profiles_list_execute(builder)
}

/// GET v2/{+parent}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_stored_info_types_create_execute()` to send, or `dlp_organizations_stored_info_types_create` for simplest API.

pub fn dlp_organizations_stored_info_types_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateStoredInfoTypeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/storedInfoTypes",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_stored_info_types_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_stored_info_types_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_stored_info_types_create_builder()` + `dlp_organizations_stored_info_types_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateStoredInfoTypeRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_stored_info_types_create_builder(client, parent, body)?;
    dlp_organizations_stored_info_types_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_stored_info_types_delete_execute()` to send, or `dlp_organizations_stored_info_types_delete` for simplest API.

pub fn dlp_organizations_stored_info_types_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_stored_info_types_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_stored_info_types_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_stored_info_types_delete_builder()` + `dlp_organizations_stored_info_types_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_stored_info_types_delete_builder(client, name)?;
    dlp_organizations_stored_info_types_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_stored_info_types_get_execute()` to send, or `dlp_organizations_stored_info_types_get` for simplest API.

pub fn dlp_organizations_stored_info_types_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_stored_info_types_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_stored_info_types_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_stored_info_types_get_builder()` + `dlp_organizations_stored_info_types_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_stored_info_types_get_builder(client, name)?;
    dlp_organizations_stored_info_types_get_execute(builder)
}

/// GET v2/{+parent}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_stored_info_types_list_execute()` to send, or `dlp_organizations_stored_info_types_list` for simplest API.

pub fn dlp_organizations_stored_info_types_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/storedInfoTypes",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_stored_info_types_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_stored_info_types_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListStoredInfoTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_stored_info_types_list_builder()` + `dlp_organizations_stored_info_types_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_list(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_stored_info_types_list_builder(
        client, parent, locationId, orderBy, pageSize, pageToken,
    )?;
    dlp_organizations_stored_info_types_list_execute(builder)
}

/// GET v2/{+name}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_stored_info_types_patch_execute()` to send, or `dlp_organizations_stored_info_types_patch` for simplest API.

pub fn dlp_organizations_stored_info_types_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateStoredInfoTypeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_organizations_stored_info_types_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_organizations_stored_info_types_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_stored_info_types_patch_builder()` + `dlp_organizations_stored_info_types_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateStoredInfoTypeRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_stored_info_types_patch_builder(client, name, body)?;
    dlp_organizations_stored_info_types_patch_execute(builder)
}

/// GET v2/{+parent}/content:deidentify
/// De-identifies potentially sensitive info from a ContentItem. This method has limits on input size and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/deidentify-sensitive-data`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_content_deidentify_execute()` to send, or `dlp_projects_content_deidentify` for simplest API.

pub fn dlp_projects_content_deidentify_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2DeidentifyContentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/content:deidentify",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/content:deidentify
/// De-identifies potentially sensitive info from a ContentItem. This method has limits on input size and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/deidentify-sensitive-data`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_content_deidentify_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_content_deidentify_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_content_deidentify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/content:deidentify
/// De-identifies potentially sensitive info from a ContentItem. This method has limits on input size and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/deidentify-sensitive-data`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_content_deidentify_builder()` + `dlp_projects_content_deidentify_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_content_deidentify(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2DeidentifyContentRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_content_deidentify_builder(client, parent, body)?;
    dlp_projects_content_deidentify_execute(builder)
}

/// GET v2/{+parent}/content:inspect
/// Finds potentially sensitive info in content. This method has limits on input size, processing time, and output size. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. For how to guides, see <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-images`> and <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-text`,>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_content_inspect_execute()` to send, or `dlp_projects_content_inspect` for simplest API.

pub fn dlp_projects_content_inspect_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2InspectContentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/content:inspect",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/content:inspect
/// Finds potentially sensitive info in content. This method has limits on input size, processing time, and output size. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. For how to guides, see <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-images`> and <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-text`,>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_content_inspect_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_content_inspect_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_content_inspect_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectContentResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/content:inspect
/// Finds potentially sensitive info in content. This method has limits on input size, processing time, and output size. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. For how to guides, see <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-images`> and <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-text`,>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_content_inspect_builder()` + `dlp_projects_content_inspect_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_content_inspect(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2InspectContentRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_content_inspect_builder(client, parent, body)?;
    dlp_projects_content_inspect_execute(builder)
}

/// GET v2/{+parent}/content:reidentify
/// Re-identifies content that has been de-identified. See <https://cloud.google.`com/sensitive-data-protection/docs/pseudonymization`#re-identification_in_free_text_code_example> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_content_reidentify_execute()` to send, or `dlp_projects_content_reidentify` for simplest API.

pub fn dlp_projects_content_reidentify_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2ReidentifyContentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/content:reidentify",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/content:reidentify
/// Re-identifies content that has been de-identified. See <https://cloud.google.`com/sensitive-data-protection/docs/pseudonymization`#re-identification_in_free_text_code_example> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_content_reidentify_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_content_reidentify_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_content_reidentify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ReidentifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ReidentifyContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/content:reidentify
/// Re-identifies content that has been de-identified. See <https://cloud.google.`com/sensitive-data-protection/docs/pseudonymization`#re-identification_in_free_text_code_example> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_content_reidentify_builder()` + `dlp_projects_content_reidentify_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_content_reidentify(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2ReidentifyContentRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ReidentifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_content_reidentify_builder(client, parent, body)?;
    dlp_projects_content_reidentify_execute(builder)
}

/// GET v2/{+parent}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_deidentify_templates_create_execute()` to send, or `dlp_projects_deidentify_templates_create` for simplest API.

pub fn dlp_projects_deidentify_templates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateDeidentifyTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/deidentifyTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_deidentify_templates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_deidentify_templates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_deidentify_templates_create_builder()` + `dlp_projects_deidentify_templates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateDeidentifyTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_deidentify_templates_create_builder(client, parent, body)?;
    dlp_projects_deidentify_templates_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_deidentify_templates_delete_execute()` to send, or `dlp_projects_deidentify_templates_delete` for simplest API.

pub fn dlp_projects_deidentify_templates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_deidentify_templates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_deidentify_templates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_deidentify_templates_delete_builder()` + `dlp_projects_deidentify_templates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_deidentify_templates_delete_builder(client, name)?;
    dlp_projects_deidentify_templates_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_deidentify_templates_get_execute()` to send, or `dlp_projects_deidentify_templates_get` for simplest API.

pub fn dlp_projects_deidentify_templates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_deidentify_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_deidentify_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_deidentify_templates_get_builder()` + `dlp_projects_deidentify_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_deidentify_templates_get_builder(client, name)?;
    dlp_projects_deidentify_templates_get_execute(builder)
}

/// GET v2/{+parent}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_deidentify_templates_list_execute()` to send, or `dlp_projects_deidentify_templates_list` for simplest API.

pub fn dlp_projects_deidentify_templates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/deidentifyTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_deidentify_templates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_deidentify_templates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDeidentifyTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_deidentify_templates_list_builder()` + `dlp_projects_deidentify_templates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_list(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_deidentify_templates_list_builder(
        client, parent, locationId, orderBy, pageSize, pageToken,
    )?;
    dlp_projects_deidentify_templates_list_execute(builder)
}

/// GET v2/{+name}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_deidentify_templates_patch_execute()` to send, or `dlp_projects_deidentify_templates_patch` for simplest API.

pub fn dlp_projects_deidentify_templates_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateDeidentifyTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_deidentify_templates_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_deidentify_templates_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_deidentify_templates_patch_builder()` + `dlp_projects_deidentify_templates_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateDeidentifyTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_deidentify_templates_patch_builder(client, name, body)?;
    dlp_projects_deidentify_templates_patch_execute(builder)
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running DlpJob. The server makes a best effort to cancel the DlpJob, but success is not guaranteed. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_dlp_jobs_cancel_execute()` to send, or `dlp_projects_dlp_jobs_cancel` for simplest API.

pub fn dlp_projects_dlp_jobs_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2CancelDlpJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running DlpJob. The server makes a best effort to cancel the DlpJob, but success is not guaranteed. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_dlp_jobs_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_dlp_jobs_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running DlpJob. The server makes a best effort to cancel the DlpJob, but success is not guaranteed. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_dlp_jobs_cancel_builder()` + `dlp_projects_dlp_jobs_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2CancelDlpJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_dlp_jobs_cancel_builder(client, name, body)?;
    dlp_projects_dlp_jobs_cancel_execute(builder)
}

/// GET v2/{+parent}/dlpJobs
/// Creates a new job to inspect storage or calculate risk metrics. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more. When no InfoTypes or CustomInfoTypes are specified in inspect jobs, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_dlp_jobs_create_execute()` to send, or `dlp_projects_dlp_jobs_create` for simplest API.

pub fn dlp_projects_dlp_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateDlpJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/dlpJobs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/dlpJobs
/// Creates a new job to inspect storage or calculate risk metrics. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more. When no InfoTypes or CustomInfoTypes are specified in inspect jobs, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_dlp_jobs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_dlp_jobs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DlpJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/dlpJobs
/// Creates a new job to inspect storage or calculate risk metrics. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more. When no InfoTypes or CustomInfoTypes are specified in inspect jobs, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_dlp_jobs_create_builder()` + `dlp_projects_dlp_jobs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateDlpJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_dlp_jobs_create_builder(client, parent, body)?;
    dlp_projects_dlp_jobs_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a long-running DlpJob. This method indicates that the client is no longer interested in the DlpJob result. The job will be canceled if possible. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_dlp_jobs_delete_execute()` to send, or `dlp_projects_dlp_jobs_delete` for simplest API.

pub fn dlp_projects_dlp_jobs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a long-running DlpJob. This method indicates that the client is no longer interested in the DlpJob result. The job will be canceled if possible. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_dlp_jobs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_dlp_jobs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a long-running DlpJob. This method indicates that the client is no longer interested in the DlpJob result. The job will be canceled if possible. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_dlp_jobs_delete_builder()` + `dlp_projects_dlp_jobs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_dlp_jobs_delete_builder(client, name)?;
    dlp_projects_dlp_jobs_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets the latest state of a long-running DlpJob. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_dlp_jobs_get_execute()` to send, or `dlp_projects_dlp_jobs_get` for simplest API.

pub fn dlp_projects_dlp_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the latest state of a long-running DlpJob. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_dlp_jobs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_dlp_jobs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DlpJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the latest state of a long-running DlpJob. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_dlp_jobs_get_builder()` + `dlp_projects_dlp_jobs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_dlp_jobs_get_builder(client, name)?;
    dlp_projects_dlp_jobs_get_execute(builder)
}

/// GET v2/{+parent}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_dlp_jobs_list_execute()` to send, or `dlp_projects_dlp_jobs_list` for simplest API.

pub fn dlp_projects_dlp_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/dlpJobs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "type",
            type_rs
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_dlp_jobs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_dlp_jobs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDlpJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDlpJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_dlp_jobs_list_builder()` + `dlp_projects_dlp_jobs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    type_rs: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDlpJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_dlp_jobs_list_builder(
        client, parent, filter, locationId, orderBy, pageSize, pageToken, type_rs,
    )?;
    dlp_projects_dlp_jobs_list_execute(builder)
}

/// GET v2/{+parent}/image:redact
/// Redacts potentially sensitive info from an image. This method has limits on input size, processing time, and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/redacting-sensitive-data-images`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. Only the first frame of each multiframe image is redacted. Metadata and other frames are omitted in the response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_image_redact_execute()` to send, or `dlp_projects_image_redact` for simplest API.

pub fn dlp_projects_image_redact_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2RedactImageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/image:redact", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/image:redact
/// Redacts potentially sensitive info from an image. This method has limits on input size, processing time, and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/redacting-sensitive-data-images`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. Only the first frame of each multiframe image is redacted. Metadata and other frames are omitted in the response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_image_redact_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_image_redact_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_image_redact_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2RedactImageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2RedactImageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/image:redact
/// Redacts potentially sensitive info from an image. This method has limits on input size, processing time, and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/redacting-sensitive-data-images`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. Only the first frame of each multiframe image is redacted. Metadata and other frames are omitted in the response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_image_redact_builder()` + `dlp_projects_image_redact_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_image_redact(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2RedactImageRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2RedactImageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_image_redact_builder(client, parent, body)?;
    dlp_projects_image_redact_execute(builder)
}

/// GET v2/{+parent}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_inspect_templates_create_execute()` to send, or `dlp_projects_inspect_templates_create` for simplest API.

pub fn dlp_projects_inspect_templates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateInspectTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/inspectTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_inspect_templates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_inspect_templates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_inspect_templates_create_builder()` + `dlp_projects_inspect_templates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateInspectTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_inspect_templates_create_builder(client, parent, body)?;
    dlp_projects_inspect_templates_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_inspect_templates_delete_execute()` to send, or `dlp_projects_inspect_templates_delete` for simplest API.

pub fn dlp_projects_inspect_templates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_inspect_templates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_inspect_templates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_inspect_templates_delete_builder()` + `dlp_projects_inspect_templates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_inspect_templates_delete_builder(client, name)?;
    dlp_projects_inspect_templates_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_inspect_templates_get_execute()` to send, or `dlp_projects_inspect_templates_get` for simplest API.

pub fn dlp_projects_inspect_templates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_inspect_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_inspect_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_inspect_templates_get_builder()` + `dlp_projects_inspect_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_inspect_templates_get_builder(client, name)?;
    dlp_projects_inspect_templates_get_execute(builder)
}

/// GET v2/{+parent}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_inspect_templates_list_execute()` to send, or `dlp_projects_inspect_templates_list` for simplest API.

pub fn dlp_projects_inspect_templates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/inspectTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_inspect_templates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_inspect_templates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInspectTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_inspect_templates_list_builder()` + `dlp_projects_inspect_templates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_list(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_inspect_templates_list_builder(
        client, parent, locationId, orderBy, pageSize, pageToken,
    )?;
    dlp_projects_inspect_templates_list_execute(builder)
}

/// GET v2/{+name}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_inspect_templates_patch_execute()` to send, or `dlp_projects_inspect_templates_patch` for simplest API.

pub fn dlp_projects_inspect_templates_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateInspectTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_inspect_templates_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_inspect_templates_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_inspect_templates_patch_builder()` + `dlp_projects_inspect_templates_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateInspectTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_inspect_templates_patch_builder(client, name, body)?;
    dlp_projects_inspect_templates_patch_execute(builder)
}

/// GET v2/{+name}:activate
/// Activate a job trigger. Causes the immediate execute of a trigger instead of waiting on the trigger event to occur.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_job_triggers_activate_execute()` to send, or `dlp_projects_job_triggers_activate` for simplest API.

pub fn dlp_projects_job_triggers_activate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2ActivateJobTriggerRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:activate", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:activate
/// Activate a job trigger. Causes the immediate execute of a trigger instead of waiting on the trigger event to occur.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_job_triggers_activate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_job_triggers_activate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DlpJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:activate
/// Activate a job trigger. Causes the immediate execute of a trigger instead of waiting on the trigger event to occur.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_job_triggers_activate_builder()` + `dlp_projects_job_triggers_activate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_activate(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2ActivateJobTriggerRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_job_triggers_activate_builder(client, name, body)?;
    dlp_projects_job_triggers_activate_execute(builder)
}

/// GET v2/{+parent}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_job_triggers_create_execute()` to send, or `dlp_projects_job_triggers_create` for simplest API.

pub fn dlp_projects_job_triggers_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateJobTriggerRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/jobTriggers", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_job_triggers_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_job_triggers_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_job_triggers_create_builder()` + `dlp_projects_job_triggers_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateJobTriggerRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_job_triggers_create_builder(client, parent, body)?;
    dlp_projects_job_triggers_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_job_triggers_delete_execute()` to send, or `dlp_projects_job_triggers_delete` for simplest API.

pub fn dlp_projects_job_triggers_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_job_triggers_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_job_triggers_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_job_triggers_delete_builder()` + `dlp_projects_job_triggers_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_job_triggers_delete_builder(client, name)?;
    dlp_projects_job_triggers_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_job_triggers_get_execute()` to send, or `dlp_projects_job_triggers_get` for simplest API.

pub fn dlp_projects_job_triggers_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_job_triggers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_job_triggers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_job_triggers_get_builder()` + `dlp_projects_job_triggers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_job_triggers_get_builder(client, name)?;
    dlp_projects_job_triggers_get_execute(builder)
}

/// GET v2/{+parent}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_job_triggers_list_execute()` to send, or `dlp_projects_job_triggers_list` for simplest API.

pub fn dlp_projects_job_triggers_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/jobTriggers", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "type",
            type_rs
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_job_triggers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_job_triggers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListJobTriggersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListJobTriggersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_job_triggers_list_builder()` + `dlp_projects_job_triggers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    type_rs: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListJobTriggersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_job_triggers_list_builder(
        client, parent, filter, locationId, orderBy, pageSize, pageToken, type_rs,
    )?;
    dlp_projects_job_triggers_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_job_triggers_patch_execute()` to send, or `dlp_projects_job_triggers_patch` for simplest API.

pub fn dlp_projects_job_triggers_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateJobTriggerRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_job_triggers_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_job_triggers_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_job_triggers_patch_builder()` + `dlp_projects_job_triggers_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateJobTriggerRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_job_triggers_patch_builder(client, name, body)?;
    dlp_projects_job_triggers_patch_execute(builder)
}

/// GET v2/{+name}
/// Gets a column data profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_column_data_profiles_get_execute()` to send, or `dlp_projects_locations_column_data_profiles_get` for simplest API.

pub fn dlp_projects_locations_column_data_profiles_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a column data profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_column_data_profiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_column_data_profiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_column_data_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ColumnDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ColumnDataProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a column data profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_column_data_profiles_get_builder()` + `dlp_projects_locations_column_data_profiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_column_data_profiles_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ColumnDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_column_data_profiles_get_builder(client, name)?;
    dlp_projects_locations_column_data_profiles_get_execute(builder)
}

/// GET v2/{+parent}/columnDataProfiles
/// Lists column data profiles for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_column_data_profiles_list_execute()` to send, or `dlp_projects_locations_column_data_profiles_list` for simplest API.

pub fn dlp_projects_locations_column_data_profiles_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/columnDataProfiles",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/columnDataProfiles
/// Lists column data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_column_data_profiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_column_data_profiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_column_data_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListColumnDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListColumnDataProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/columnDataProfiles
/// Lists column data profiles for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_column_data_profiles_list_builder()` + `dlp_projects_locations_column_data_profiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_column_data_profiles_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListColumnDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_column_data_profiles_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    dlp_projects_locations_column_data_profiles_list_execute(builder)
}

/// GET v2/{+parent}/connections
/// Create a Connection to an external data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_connections_create_execute()` to send, or `dlp_projects_locations_connections_create` for simplest API.

pub fn dlp_projects_locations_connections_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateConnectionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/connections", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/connections
/// Create a Connection to an external data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_connections_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_connections_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2Connection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/connections
/// Create a Connection to an external data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_connections_create_builder()` + `dlp_projects_locations_connections_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateConnectionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_connections_create_builder(client, parent, body)?;
    dlp_projects_locations_connections_create_execute(builder)
}

/// GET v2/{+name}
/// Delete a Connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_connections_delete_execute()` to send, or `dlp_projects_locations_connections_delete` for simplest API.

pub fn dlp_projects_locations_connections_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Delete a Connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_connections_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_connections_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Delete a Connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_connections_delete_builder()` + `dlp_projects_locations_connections_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_connections_delete_builder(client, name)?;
    dlp_projects_locations_connections_delete_execute(builder)
}

/// GET v2/{+name}
/// Get a Connection by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_connections_get_execute()` to send, or `dlp_projects_locations_connections_get` for simplest API.

pub fn dlp_projects_locations_connections_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get a Connection by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_connections_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_connections_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2Connection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get a Connection by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_connections_get_builder()` + `dlp_projects_locations_connections_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_connections_get_builder(client, name)?;
    dlp_projects_locations_connections_get_execute(builder)
}

/// GET v2/{+parent}/connections
/// Lists Connections in a parent. Use SearchConnections to see all connections within an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_connections_list_execute()` to send, or `dlp_projects_locations_connections_list` for simplest API.

pub fn dlp_projects_locations_connections_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/connections", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/connections
/// Lists Connections in a parent. Use SearchConnections to see all connections within an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_connections_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_connections_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListConnectionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/connections
/// Lists Connections in a parent. Use SearchConnections to see all connections within an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_connections_list_builder()` + `dlp_projects_locations_connections_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_connections_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    dlp_projects_locations_connections_list_execute(builder)
}

/// GET v2/{+name}
/// Update a Connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_connections_patch_execute()` to send, or `dlp_projects_locations_connections_patch` for simplest API.

pub fn dlp_projects_locations_connections_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateConnectionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Update a Connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_connections_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_connections_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2Connection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Update a Connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_connections_patch_builder()` + `dlp_projects_locations_connections_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateConnectionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_connections_patch_builder(client, name, body)?;
    dlp_projects_locations_connections_patch_execute(builder)
}

/// GET v2/{+parent}/connections:search
/// Searches for Connections in a parent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_connections_search_execute()` to send, or `dlp_projects_locations_connections_search` for simplest API.

pub fn dlp_projects_locations_connections_search_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/connections:search",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/connections:search
/// Searches for Connections in a parent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_connections_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_connections_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2SearchConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2SearchConnectionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/connections:search
/// Searches for Connections in a parent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_connections_search_builder()` + `dlp_projects_locations_connections_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_search(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2SearchConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_connections_search_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    dlp_projects_locations_connections_search_execute(builder)
}

/// GET v2/{+parent}/content:deidentify
/// De-identifies potentially sensitive info from a ContentItem. This method has limits on input size and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/deidentify-sensitive-data`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_content_deidentify_execute()` to send, or `dlp_projects_locations_content_deidentify` for simplest API.

pub fn dlp_projects_locations_content_deidentify_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2DeidentifyContentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/content:deidentify",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/content:deidentify
/// De-identifies potentially sensitive info from a ContentItem. This method has limits on input size and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/deidentify-sensitive-data`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_content_deidentify_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_content_deidentify_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_content_deidentify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/content:deidentify
/// De-identifies potentially sensitive info from a ContentItem. This method has limits on input size and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/deidentify-sensitive-data`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_content_deidentify_builder()` + `dlp_projects_locations_content_deidentify_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_content_deidentify(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2DeidentifyContentRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_content_deidentify_builder(client, parent, body)?;
    dlp_projects_locations_content_deidentify_execute(builder)
}

/// GET v2/{+parent}/content:inspect
/// Finds potentially sensitive info in content. This method has limits on input size, processing time, and output size. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. For how to guides, see <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-images`> and <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-text`,>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_content_inspect_execute()` to send, or `dlp_projects_locations_content_inspect` for simplest API.

pub fn dlp_projects_locations_content_inspect_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2InspectContentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/content:inspect",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/content:inspect
/// Finds potentially sensitive info in content. This method has limits on input size, processing time, and output size. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. For how to guides, see <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-images`> and <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-text`,>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_content_inspect_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_content_inspect_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_content_inspect_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectContentResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/content:inspect
/// Finds potentially sensitive info in content. This method has limits on input size, processing time, and output size. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. For how to guides, see <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-images`> and <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-text`,>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_content_inspect_builder()` + `dlp_projects_locations_content_inspect_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_content_inspect(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2InspectContentRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_content_inspect_builder(client, parent, body)?;
    dlp_projects_locations_content_inspect_execute(builder)
}

/// GET v2/{+parent}/content:reidentify
/// Re-identifies content that has been de-identified. See <https://cloud.google.`com/sensitive-data-protection/docs/pseudonymization`#re-identification_in_free_text_code_example> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_content_reidentify_execute()` to send, or `dlp_projects_locations_content_reidentify` for simplest API.

pub fn dlp_projects_locations_content_reidentify_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2ReidentifyContentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/content:reidentify",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/content:reidentify
/// Re-identifies content that has been de-identified. See <https://cloud.google.`com/sensitive-data-protection/docs/pseudonymization`#re-identification_in_free_text_code_example> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_content_reidentify_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_content_reidentify_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_content_reidentify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ReidentifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ReidentifyContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/content:reidentify
/// Re-identifies content that has been de-identified. See <https://cloud.google.`com/sensitive-data-protection/docs/pseudonymization`#re-identification_in_free_text_code_example> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_content_reidentify_builder()` + `dlp_projects_locations_content_reidentify_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_content_reidentify(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2ReidentifyContentRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ReidentifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_content_reidentify_builder(client, parent, body)?;
    dlp_projects_locations_content_reidentify_execute(builder)
}

/// GET v2/{+parent}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_deidentify_templates_create_execute()` to send, or `dlp_projects_locations_deidentify_templates_create` for simplest API.

pub fn dlp_projects_locations_deidentify_templates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateDeidentifyTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/deidentifyTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_deidentify_templates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_deidentify_templates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_deidentify_templates_create_builder()` + `dlp_projects_locations_deidentify_templates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateDeidentifyTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_deidentify_templates_create_builder(client, parent, body)?;
    dlp_projects_locations_deidentify_templates_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_deidentify_templates_delete_execute()` to send, or `dlp_projects_locations_deidentify_templates_delete` for simplest API.

pub fn dlp_projects_locations_deidentify_templates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_deidentify_templates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_deidentify_templates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_deidentify_templates_delete_builder()` + `dlp_projects_locations_deidentify_templates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_deidentify_templates_delete_builder(client, name)?;
    dlp_projects_locations_deidentify_templates_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_deidentify_templates_get_execute()` to send, or `dlp_projects_locations_deidentify_templates_get` for simplest API.

pub fn dlp_projects_locations_deidentify_templates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_deidentify_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_deidentify_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_deidentify_templates_get_builder()` + `dlp_projects_locations_deidentify_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_deidentify_templates_get_builder(client, name)?;
    dlp_projects_locations_deidentify_templates_get_execute(builder)
}

/// GET v2/{+parent}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_deidentify_templates_list_execute()` to send, or `dlp_projects_locations_deidentify_templates_list` for simplest API.

pub fn dlp_projects_locations_deidentify_templates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/deidentifyTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_deidentify_templates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_deidentify_templates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDeidentifyTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_deidentify_templates_list_builder()` + `dlp_projects_locations_deidentify_templates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_list(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_deidentify_templates_list_builder(
        client, parent, locationId, orderBy, pageSize, pageToken,
    )?;
    dlp_projects_locations_deidentify_templates_list_execute(builder)
}

/// GET v2/{+name}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_deidentify_templates_patch_execute()` to send, or `dlp_projects_locations_deidentify_templates_patch` for simplest API.

pub fn dlp_projects_locations_deidentify_templates_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateDeidentifyTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_deidentify_templates_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_deidentify_templates_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_deidentify_templates_patch_builder()` + `dlp_projects_locations_deidentify_templates_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateDeidentifyTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_deidentify_templates_patch_builder(client, name, body)?;
    dlp_projects_locations_deidentify_templates_patch_execute(builder)
}

/// GET v2/{+parent}/discoveryConfigs
/// Creates a config for discovery to scan and profile storage.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_discovery_configs_create_execute()` to send, or `dlp_projects_locations_discovery_configs_create` for simplest API.

pub fn dlp_projects_locations_discovery_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateDiscoveryConfigRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/discoveryConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/discoveryConfigs
/// Creates a config for discovery to scan and profile storage.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_discovery_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_discovery_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DiscoveryConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/discoveryConfigs
/// Creates a config for discovery to scan and profile storage.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_discovery_configs_create_builder()` + `dlp_projects_locations_discovery_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateDiscoveryConfigRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_discovery_configs_create_builder(client, parent, body)?;
    dlp_projects_locations_discovery_configs_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_discovery_configs_delete_execute()` to send, or `dlp_projects_locations_discovery_configs_delete` for simplest API.

pub fn dlp_projects_locations_discovery_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_discovery_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_discovery_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_discovery_configs_delete_builder()` + `dlp_projects_locations_discovery_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_discovery_configs_delete_builder(client, name)?;
    dlp_projects_locations_discovery_configs_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_discovery_configs_get_execute()` to send, or `dlp_projects_locations_discovery_configs_get` for simplest API.

pub fn dlp_projects_locations_discovery_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_discovery_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_discovery_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DiscoveryConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_discovery_configs_get_builder()` + `dlp_projects_locations_discovery_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_discovery_configs_get_builder(client, name)?;
    dlp_projects_locations_discovery_configs_get_execute(builder)
}

/// GET v2/{+parent}/discoveryConfigs
/// Lists discovery configurations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_discovery_configs_list_execute()` to send, or `dlp_projects_locations_discovery_configs_list` for simplest API.

pub fn dlp_projects_locations_discovery_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/discoveryConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/discoveryConfigs
/// Lists discovery configurations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_discovery_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_discovery_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDiscoveryConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDiscoveryConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/discoveryConfigs
/// Lists discovery configurations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_discovery_configs_list_builder()` + `dlp_projects_locations_discovery_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDiscoveryConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_discovery_configs_list_builder(
        client, parent, orderBy, pageSize, pageToken,
    )?;
    dlp_projects_locations_discovery_configs_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_discovery_configs_patch_execute()` to send, or `dlp_projects_locations_discovery_configs_patch` for simplest API.

pub fn dlp_projects_locations_discovery_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateDiscoveryConfigRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_discovery_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_discovery_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DiscoveryConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_discovery_configs_patch_builder()` + `dlp_projects_locations_discovery_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateDiscoveryConfigRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_discovery_configs_patch_builder(client, name, body)?;
    dlp_projects_locations_discovery_configs_patch_execute(builder)
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running DlpJob. The server makes a best effort to cancel the DlpJob, but success is not guaranteed. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_dlp_jobs_cancel_execute()` to send, or `dlp_projects_locations_dlp_jobs_cancel` for simplest API.

pub fn dlp_projects_locations_dlp_jobs_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2CancelDlpJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running DlpJob. The server makes a best effort to cancel the DlpJob, but success is not guaranteed. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_dlp_jobs_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_dlp_jobs_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running DlpJob. The server makes a best effort to cancel the DlpJob, but success is not guaranteed. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_dlp_jobs_cancel_builder()` + `dlp_projects_locations_dlp_jobs_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2CancelDlpJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_dlp_jobs_cancel_builder(client, name, body)?;
    dlp_projects_locations_dlp_jobs_cancel_execute(builder)
}

/// GET v2/{+parent}/dlpJobs
/// Creates a new job to inspect storage or calculate risk metrics. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more. When no InfoTypes or CustomInfoTypes are specified in inspect jobs, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_dlp_jobs_create_execute()` to send, or `dlp_projects_locations_dlp_jobs_create` for simplest API.

pub fn dlp_projects_locations_dlp_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateDlpJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/dlpJobs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/dlpJobs
/// Creates a new job to inspect storage or calculate risk metrics. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more. When no InfoTypes or CustomInfoTypes are specified in inspect jobs, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_dlp_jobs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_dlp_jobs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DlpJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/dlpJobs
/// Creates a new job to inspect storage or calculate risk metrics. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more. When no InfoTypes or CustomInfoTypes are specified in inspect jobs, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_dlp_jobs_create_builder()` + `dlp_projects_locations_dlp_jobs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateDlpJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_dlp_jobs_create_builder(client, parent, body)?;
    dlp_projects_locations_dlp_jobs_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a long-running DlpJob. This method indicates that the client is no longer interested in the DlpJob result. The job will be canceled if possible. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_dlp_jobs_delete_execute()` to send, or `dlp_projects_locations_dlp_jobs_delete` for simplest API.

pub fn dlp_projects_locations_dlp_jobs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a long-running DlpJob. This method indicates that the client is no longer interested in the DlpJob result. The job will be canceled if possible. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_dlp_jobs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_dlp_jobs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a long-running DlpJob. This method indicates that the client is no longer interested in the DlpJob result. The job will be canceled if possible. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_dlp_jobs_delete_builder()` + `dlp_projects_locations_dlp_jobs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_dlp_jobs_delete_builder(client, name)?;
    dlp_projects_locations_dlp_jobs_delete_execute(builder)
}

/// GET v2/{+name}:finish
/// Finish a running hybrid DlpJob. Triggers the finalization steps and running of any enabled actions that have not yet run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_dlp_jobs_finish_execute()` to send, or `dlp_projects_locations_dlp_jobs_finish` for simplest API.

pub fn dlp_projects_locations_dlp_jobs_finish_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2FinishDlpJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:finish", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:finish
/// Finish a running hybrid DlpJob. Triggers the finalization steps and running of any enabled actions that have not yet run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_dlp_jobs_finish_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_dlp_jobs_finish_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_finish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:finish
/// Finish a running hybrid DlpJob. Triggers the finalization steps and running of any enabled actions that have not yet run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_dlp_jobs_finish_builder()` + `dlp_projects_locations_dlp_jobs_finish_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_finish(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2FinishDlpJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_dlp_jobs_finish_builder(client, name, body)?;
    dlp_projects_locations_dlp_jobs_finish_execute(builder)
}

/// GET v2/{+name}
/// Gets the latest state of a long-running DlpJob. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_dlp_jobs_get_execute()` to send, or `dlp_projects_locations_dlp_jobs_get` for simplest API.

pub fn dlp_projects_locations_dlp_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the latest state of a long-running DlpJob. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_dlp_jobs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_dlp_jobs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DlpJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the latest state of a long-running DlpJob. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_dlp_jobs_get_builder()` + `dlp_projects_locations_dlp_jobs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_dlp_jobs_get_builder(client, name)?;
    dlp_projects_locations_dlp_jobs_get_execute(builder)
}

/// GET v2/{+name}:hybridInspect
/// Inspect hybrid content and store findings to a job. To review the findings, inspect the job. Inspection will occur asynchronously.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_dlp_jobs_hybrid_inspect_execute()` to send, or `dlp_projects_locations_dlp_jobs_hybrid_inspect` for simplest API.

pub fn dlp_projects_locations_dlp_jobs_hybrid_inspect_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2HybridInspectDlpJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:hybridInspect", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:hybridInspect
/// Inspect hybrid content and store findings to a job. To review the findings, inspect the job. Inspection will occur asynchronously.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_dlp_jobs_hybrid_inspect_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_dlp_jobs_hybrid_inspect_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_hybrid_inspect_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2HybridInspectResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2HybridInspectResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:hybridInspect
/// Inspect hybrid content and store findings to a job. To review the findings, inspect the job. Inspection will occur asynchronously.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_dlp_jobs_hybrid_inspect_builder()` + `dlp_projects_locations_dlp_jobs_hybrid_inspect_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_hybrid_inspect(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2HybridInspectDlpJobRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2HybridInspectResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_dlp_jobs_hybrid_inspect_builder(client, name, body)?;
    dlp_projects_locations_dlp_jobs_hybrid_inspect_execute(builder)
}

/// GET v2/{+parent}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_dlp_jobs_list_execute()` to send, or `dlp_projects_locations_dlp_jobs_list` for simplest API.

pub fn dlp_projects_locations_dlp_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/dlpJobs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "type",
            type_rs
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_dlp_jobs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_dlp_jobs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDlpJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDlpJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_dlp_jobs_list_builder()` + `dlp_projects_locations_dlp_jobs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    type_rs: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDlpJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_dlp_jobs_list_builder(
        client, parent, filter, locationId, orderBy, pageSize, pageToken, type_rs,
    )?;
    dlp_projects_locations_dlp_jobs_list_execute(builder)
}

/// GET v2/{+name}
/// Delete a FileStoreDataProfile. Will not prevent the profile from being regenerated if the resource is still included in a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_file_store_data_profiles_delete_execute()` to send, or `dlp_projects_locations_file_store_data_profiles_delete` for simplest API.

pub fn dlp_projects_locations_file_store_data_profiles_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Delete a FileStoreDataProfile. Will not prevent the profile from being regenerated if the resource is still included in a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_file_store_data_profiles_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_file_store_data_profiles_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_file_store_data_profiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Delete a FileStoreDataProfile. Will not prevent the profile from being regenerated if the resource is still included in a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_file_store_data_profiles_delete_builder()` + `dlp_projects_locations_file_store_data_profiles_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_file_store_data_profiles_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_file_store_data_profiles_delete_builder(client, name)?;
    dlp_projects_locations_file_store_data_profiles_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a file store data profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_file_store_data_profiles_get_execute()` to send, or `dlp_projects_locations_file_store_data_profiles_get` for simplest API.

pub fn dlp_projects_locations_file_store_data_profiles_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a file store data profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_file_store_data_profiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_file_store_data_profiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_file_store_data_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2FileStoreDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2FileStoreDataProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a file store data profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_file_store_data_profiles_get_builder()` + `dlp_projects_locations_file_store_data_profiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_file_store_data_profiles_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2FileStoreDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_file_store_data_profiles_get_builder(client, name)?;
    dlp_projects_locations_file_store_data_profiles_get_execute(builder)
}

/// GET v2/{+parent}/fileStoreDataProfiles
/// Lists file store data profiles for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_file_store_data_profiles_list_execute()` to send, or `dlp_projects_locations_file_store_data_profiles_list` for simplest API.

pub fn dlp_projects_locations_file_store_data_profiles_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/fileStoreDataProfiles",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/fileStoreDataProfiles
/// Lists file store data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_file_store_data_profiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_file_store_data_profiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_file_store_data_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListFileStoreDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListFileStoreDataProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/fileStoreDataProfiles
/// Lists file store data profiles for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_file_store_data_profiles_list_builder()` + `dlp_projects_locations_file_store_data_profiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_file_store_data_profiles_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListFileStoreDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_file_store_data_profiles_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    dlp_projects_locations_file_store_data_profiles_list_execute(builder)
}

/// GET v2/{+parent}/image:redact
/// Redacts potentially sensitive info from an image. This method has limits on input size, processing time, and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/redacting-sensitive-data-images`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. Only the first frame of each multiframe image is redacted. Metadata and other frames are omitted in the response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_image_redact_execute()` to send, or `dlp_projects_locations_image_redact` for simplest API.

pub fn dlp_projects_locations_image_redact_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2RedactImageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/image:redact", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/image:redact
/// Redacts potentially sensitive info from an image. This method has limits on input size, processing time, and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/redacting-sensitive-data-images`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. Only the first frame of each multiframe image is redacted. Metadata and other frames are omitted in the response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_image_redact_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_image_redact_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_image_redact_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2RedactImageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2RedactImageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/image:redact
/// Redacts potentially sensitive info from an image. This method has limits on input size, processing time, and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/redacting-sensitive-data-images`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. Only the first frame of each multiframe image is redacted. Metadata and other frames are omitted in the response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_image_redact_builder()` + `dlp_projects_locations_image_redact_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_image_redact(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2RedactImageRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2RedactImageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_image_redact_builder(client, parent, body)?;
    dlp_projects_locations_image_redact_execute(builder)
}

/// GET v2/{+parent}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_info_types_list_execute()` to send, or `dlp_projects_locations_info_types_list` for simplest API.

pub fn dlp_projects_locations_info_types_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    languageCode: Option<&str>,
    locationId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/infoTypes", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "languageCode",
            languageCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_info_types_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_info_types_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_info_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInfoTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_info_types_list_builder()` + `dlp_projects_locations_info_types_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_info_types_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    languageCode: Option<&str>,
    locationId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_info_types_list_builder(
        client,
        parent,
        filter,
        languageCode,
        locationId,
    )?;
    dlp_projects_locations_info_types_list_execute(builder)
}

/// GET v2/{+parent}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_inspect_templates_create_execute()` to send, or `dlp_projects_locations_inspect_templates_create` for simplest API.

pub fn dlp_projects_locations_inspect_templates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateInspectTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/inspectTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_inspect_templates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_inspect_templates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_inspect_templates_create_builder()` + `dlp_projects_locations_inspect_templates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateInspectTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_inspect_templates_create_builder(client, parent, body)?;
    dlp_projects_locations_inspect_templates_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_inspect_templates_delete_execute()` to send, or `dlp_projects_locations_inspect_templates_delete` for simplest API.

pub fn dlp_projects_locations_inspect_templates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_inspect_templates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_inspect_templates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_inspect_templates_delete_builder()` + `dlp_projects_locations_inspect_templates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_inspect_templates_delete_builder(client, name)?;
    dlp_projects_locations_inspect_templates_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_inspect_templates_get_execute()` to send, or `dlp_projects_locations_inspect_templates_get` for simplest API.

pub fn dlp_projects_locations_inspect_templates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_inspect_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_inspect_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_inspect_templates_get_builder()` + `dlp_projects_locations_inspect_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_inspect_templates_get_builder(client, name)?;
    dlp_projects_locations_inspect_templates_get_execute(builder)
}

/// GET v2/{+parent}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_inspect_templates_list_execute()` to send, or `dlp_projects_locations_inspect_templates_list` for simplest API.

pub fn dlp_projects_locations_inspect_templates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/inspectTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_inspect_templates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_inspect_templates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInspectTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_inspect_templates_list_builder()` + `dlp_projects_locations_inspect_templates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_list(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_inspect_templates_list_builder(
        client, parent, locationId, orderBy, pageSize, pageToken,
    )?;
    dlp_projects_locations_inspect_templates_list_execute(builder)
}

/// GET v2/{+name}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_inspect_templates_patch_execute()` to send, or `dlp_projects_locations_inspect_templates_patch` for simplest API.

pub fn dlp_projects_locations_inspect_templates_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateInspectTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_inspect_templates_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_inspect_templates_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_inspect_templates_patch_builder()` + `dlp_projects_locations_inspect_templates_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateInspectTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_inspect_templates_patch_builder(client, name, body)?;
    dlp_projects_locations_inspect_templates_patch_execute(builder)
}

/// GET v2/{+name}:activate
/// Activate a job trigger. Causes the immediate execute of a trigger instead of waiting on the trigger event to occur.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_job_triggers_activate_execute()` to send, or `dlp_projects_locations_job_triggers_activate` for simplest API.

pub fn dlp_projects_locations_job_triggers_activate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2ActivateJobTriggerRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:activate", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:activate
/// Activate a job trigger. Causes the immediate execute of a trigger instead of waiting on the trigger event to occur.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_job_triggers_activate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_job_triggers_activate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DlpJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:activate
/// Activate a job trigger. Causes the immediate execute of a trigger instead of waiting on the trigger event to occur.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_job_triggers_activate_builder()` + `dlp_projects_locations_job_triggers_activate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_activate(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2ActivateJobTriggerRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_job_triggers_activate_builder(client, name, body)?;
    dlp_projects_locations_job_triggers_activate_execute(builder)
}

/// GET v2/{+parent}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_job_triggers_create_execute()` to send, or `dlp_projects_locations_job_triggers_create` for simplest API.

pub fn dlp_projects_locations_job_triggers_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateJobTriggerRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/jobTriggers", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_job_triggers_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_job_triggers_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_job_triggers_create_builder()` + `dlp_projects_locations_job_triggers_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateJobTriggerRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_job_triggers_create_builder(client, parent, body)?;
    dlp_projects_locations_job_triggers_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_job_triggers_delete_execute()` to send, or `dlp_projects_locations_job_triggers_delete` for simplest API.

pub fn dlp_projects_locations_job_triggers_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_job_triggers_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_job_triggers_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_job_triggers_delete_builder()` + `dlp_projects_locations_job_triggers_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_job_triggers_delete_builder(client, name)?;
    dlp_projects_locations_job_triggers_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_job_triggers_get_execute()` to send, or `dlp_projects_locations_job_triggers_get` for simplest API.

pub fn dlp_projects_locations_job_triggers_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_job_triggers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_job_triggers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_job_triggers_get_builder()` + `dlp_projects_locations_job_triggers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_job_triggers_get_builder(client, name)?;
    dlp_projects_locations_job_triggers_get_execute(builder)
}

/// GET v2/{+name}:hybridInspect
/// Inspect hybrid content and store findings to a trigger. The inspection will be processed asynchronously. To review the findings monitor the jobs within the trigger.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_job_triggers_hybrid_inspect_execute()` to send, or `dlp_projects_locations_job_triggers_hybrid_inspect` for simplest API.

pub fn dlp_projects_locations_job_triggers_hybrid_inspect_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2HybridInspectJobTriggerRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:hybridInspect", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:hybridInspect
/// Inspect hybrid content and store findings to a trigger. The inspection will be processed asynchronously. To review the findings monitor the jobs within the trigger.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_job_triggers_hybrid_inspect_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_job_triggers_hybrid_inspect_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_hybrid_inspect_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2HybridInspectResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2HybridInspectResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:hybridInspect
/// Inspect hybrid content and store findings to a trigger. The inspection will be processed asynchronously. To review the findings monitor the jobs within the trigger.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_job_triggers_hybrid_inspect_builder()` + `dlp_projects_locations_job_triggers_hybrid_inspect_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_hybrid_inspect(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2HybridInspectJobTriggerRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2HybridInspectResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_job_triggers_hybrid_inspect_builder(client, name, body)?;
    dlp_projects_locations_job_triggers_hybrid_inspect_execute(builder)
}

/// GET v2/{+parent}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_job_triggers_list_execute()` to send, or `dlp_projects_locations_job_triggers_list` for simplest API.

pub fn dlp_projects_locations_job_triggers_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/jobTriggers", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "type",
            type_rs
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_job_triggers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_job_triggers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListJobTriggersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListJobTriggersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_job_triggers_list_builder()` + `dlp_projects_locations_job_triggers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    type_rs: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListJobTriggersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_job_triggers_list_builder(
        client, parent, filter, locationId, orderBy, pageSize, pageToken, type_rs,
    )?;
    dlp_projects_locations_job_triggers_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_job_triggers_patch_execute()` to send, or `dlp_projects_locations_job_triggers_patch` for simplest API.

pub fn dlp_projects_locations_job_triggers_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateJobTriggerRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_job_triggers_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_job_triggers_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_job_triggers_patch_builder()` + `dlp_projects_locations_job_triggers_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateJobTriggerRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_job_triggers_patch_builder(client, name, body)?;
    dlp_projects_locations_job_triggers_patch_execute(builder)
}

/// GET v2/{+name}
/// Gets a project data profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_project_data_profiles_get_execute()` to send, or `dlp_projects_locations_project_data_profiles_get` for simplest API.

pub fn dlp_projects_locations_project_data_profiles_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a project data profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_project_data_profiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_project_data_profiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_project_data_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ProjectDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ProjectDataProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a project data profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_project_data_profiles_get_builder()` + `dlp_projects_locations_project_data_profiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_project_data_profiles_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ProjectDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_project_data_profiles_get_builder(client, name)?;
    dlp_projects_locations_project_data_profiles_get_execute(builder)
}

/// GET v2/{+parent}/projectDataProfiles
/// Lists project data profiles for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_project_data_profiles_list_execute()` to send, or `dlp_projects_locations_project_data_profiles_list` for simplest API.

pub fn dlp_projects_locations_project_data_profiles_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/projectDataProfiles",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/projectDataProfiles
/// Lists project data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_project_data_profiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_project_data_profiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_project_data_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListProjectDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListProjectDataProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/projectDataProfiles
/// Lists project data profiles for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_project_data_profiles_list_builder()` + `dlp_projects_locations_project_data_profiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_project_data_profiles_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListProjectDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_project_data_profiles_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    dlp_projects_locations_project_data_profiles_list_execute(builder)
}

/// GET v2/{+parent}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_stored_info_types_create_execute()` to send, or `dlp_projects_locations_stored_info_types_create` for simplest API.

pub fn dlp_projects_locations_stored_info_types_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateStoredInfoTypeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/storedInfoTypes",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_stored_info_types_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_stored_info_types_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_stored_info_types_create_builder()` + `dlp_projects_locations_stored_info_types_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateStoredInfoTypeRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_stored_info_types_create_builder(client, parent, body)?;
    dlp_projects_locations_stored_info_types_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_stored_info_types_delete_execute()` to send, or `dlp_projects_locations_stored_info_types_delete` for simplest API.

pub fn dlp_projects_locations_stored_info_types_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_stored_info_types_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_stored_info_types_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_stored_info_types_delete_builder()` + `dlp_projects_locations_stored_info_types_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_stored_info_types_delete_builder(client, name)?;
    dlp_projects_locations_stored_info_types_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_stored_info_types_get_execute()` to send, or `dlp_projects_locations_stored_info_types_get` for simplest API.

pub fn dlp_projects_locations_stored_info_types_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_stored_info_types_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_stored_info_types_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_stored_info_types_get_builder()` + `dlp_projects_locations_stored_info_types_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_stored_info_types_get_builder(client, name)?;
    dlp_projects_locations_stored_info_types_get_execute(builder)
}

/// GET v2/{+parent}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_stored_info_types_list_execute()` to send, or `dlp_projects_locations_stored_info_types_list` for simplest API.

pub fn dlp_projects_locations_stored_info_types_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/storedInfoTypes",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_stored_info_types_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_stored_info_types_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListStoredInfoTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_stored_info_types_list_builder()` + `dlp_projects_locations_stored_info_types_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_list(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_stored_info_types_list_builder(
        client, parent, locationId, orderBy, pageSize, pageToken,
    )?;
    dlp_projects_locations_stored_info_types_list_execute(builder)
}

/// GET v2/{+name}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_stored_info_types_patch_execute()` to send, or `dlp_projects_locations_stored_info_types_patch` for simplest API.

pub fn dlp_projects_locations_stored_info_types_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateStoredInfoTypeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_stored_info_types_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_stored_info_types_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_stored_info_types_patch_builder()` + `dlp_projects_locations_stored_info_types_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateStoredInfoTypeRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_stored_info_types_patch_builder(client, name, body)?;
    dlp_projects_locations_stored_info_types_patch_execute(builder)
}

/// GET v2/{+name}
/// Delete a TableDataProfile. Will not prevent the profile from being regenerated if the table is still included in a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_table_data_profiles_delete_execute()` to send, or `dlp_projects_locations_table_data_profiles_delete` for simplest API.

pub fn dlp_projects_locations_table_data_profiles_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Delete a TableDataProfile. Will not prevent the profile from being regenerated if the table is still included in a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_table_data_profiles_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_table_data_profiles_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_table_data_profiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Delete a TableDataProfile. Will not prevent the profile from being regenerated if the table is still included in a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_table_data_profiles_delete_builder()` + `dlp_projects_locations_table_data_profiles_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_table_data_profiles_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_table_data_profiles_delete_builder(client, name)?;
    dlp_projects_locations_table_data_profiles_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a table data profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_table_data_profiles_get_execute()` to send, or `dlp_projects_locations_table_data_profiles_get` for simplest API.

pub fn dlp_projects_locations_table_data_profiles_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a table data profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_table_data_profiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_table_data_profiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_table_data_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2TableDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2TableDataProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a table data profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_table_data_profiles_get_builder()` + `dlp_projects_locations_table_data_profiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_table_data_profiles_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2TableDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_table_data_profiles_get_builder(client, name)?;
    dlp_projects_locations_table_data_profiles_get_execute(builder)
}

/// GET v2/{+parent}/tableDataProfiles
/// Lists table data profiles for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_table_data_profiles_list_execute()` to send, or `dlp_projects_locations_table_data_profiles_list` for simplest API.

pub fn dlp_projects_locations_table_data_profiles_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/tableDataProfiles",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/tableDataProfiles
/// Lists table data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_locations_table_data_profiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_locations_table_data_profiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_table_data_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListTableDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListTableDataProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/tableDataProfiles
/// Lists table data profiles for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_table_data_profiles_list_builder()` + `dlp_projects_locations_table_data_profiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_table_data_profiles_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListTableDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_table_data_profiles_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    dlp_projects_locations_table_data_profiles_list_execute(builder)
}

/// GET v2/{+parent}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_stored_info_types_create_execute()` to send, or `dlp_projects_stored_info_types_create` for simplest API.

pub fn dlp_projects_stored_info_types_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateStoredInfoTypeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/storedInfoTypes",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_stored_info_types_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_stored_info_types_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_stored_info_types_create_builder()` + `dlp_projects_stored_info_types_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GooglePrivacyDlpV2CreateStoredInfoTypeRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_stored_info_types_create_builder(client, parent, body)?;
    dlp_projects_stored_info_types_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_stored_info_types_delete_execute()` to send, or `dlp_projects_stored_info_types_delete` for simplest API.

pub fn dlp_projects_stored_info_types_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_stored_info_types_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_stored_info_types_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_stored_info_types_delete_builder()` + `dlp_projects_stored_info_types_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_stored_info_types_delete_builder(client, name)?;
    dlp_projects_stored_info_types_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_stored_info_types_get_execute()` to send, or `dlp_projects_stored_info_types_get` for simplest API.

pub fn dlp_projects_stored_info_types_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_stored_info_types_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_stored_info_types_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_stored_info_types_get_builder()` + `dlp_projects_stored_info_types_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_stored_info_types_get_builder(client, name)?;
    dlp_projects_stored_info_types_get_execute(builder)
}

/// GET v2/{+parent}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_stored_info_types_list_execute()` to send, or `dlp_projects_stored_info_types_list` for simplest API.

pub fn dlp_projects_stored_info_types_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/storedInfoTypes",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locationId",
            locationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_stored_info_types_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_stored_info_types_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListStoredInfoTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_stored_info_types_list_builder()` + `dlp_projects_stored_info_types_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_list(
    client: &SimpleHttpClient,
    parent: &str,
    locationId: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_stored_info_types_list_builder(
        client, parent, locationId, orderBy, pageSize, pageToken,
    )?;
    dlp_projects_stored_info_types_list_execute(builder)
}

/// GET v2/{+name}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_stored_info_types_patch_execute()` to send, or `dlp_projects_stored_info_types_patch` for simplest API.

pub fn dlp_projects_stored_info_types_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateStoredInfoTypeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dlp_projects_stored_info_types_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dlp_projects_stored_info_types_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_stored_info_types_patch_builder()` + `dlp_projects_stored_info_types_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePrivacyDlpV2UpdateStoredInfoTypeRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_stored_info_types_patch_builder(client, name, body)?;
    dlp_projects_stored_info_types_patch_execute(builder)
}
