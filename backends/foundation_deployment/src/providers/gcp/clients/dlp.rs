//! Auto-generated API clients for gcp/dlp.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v2/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_info_types_list_execute()` to send, or `dlp_info_types_list` for simplest API.

pub fn dlp_info_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    filter: &Option<String>,
    languageCode: &Option<String>,
    locationId: &Option<String>,
    parent: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://dlp.googleapis.com/v2/infoTypes",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = parent.as_ref() {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_info_types_list_execute()` or `dlp_info_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_info_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_info_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInfoTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_info_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_info_types_list_task()`.
/// For the simplest API, use `dlp_info_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_info_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_info_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_info_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_info_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpInfoTypesListArgs {
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v2/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_info_types_list_builder()` + `dlp_info_types_list_execute()`.
/// For task-level control, use `dlp_info_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_info_types_list(
    client: &SimpleHttpClient,
    args: &DlpInfoTypesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_info_types_list_builder(
        client,
        &args.filter,
        &args.languageCode,
        &args.locationId,
        &args.parent,
    )?;
    dlp_info_types_list_execute(builder)
}

/// GET v2/locations/{locationsId}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_locations_info_types_list_execute()` to send, or `dlp_locations_info_types_list` for simplest API.

pub fn dlp_locations_info_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    languageCode: &Option<String>,
    locationId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/locations/{}/infoTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/locations/{locationsId}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_locations_info_types_list_execute()` or `dlp_locations_info_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_locations_info_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_locations_info_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInfoTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/locations/{locationsId}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_locations_info_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_locations_info_types_list_task()`.
/// For the simplest API, use `dlp_locations_info_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_locations_info_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_locations_info_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_locations_info_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_locations_info_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpLocationsInfoTypesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: locationId
    pub locationId: Option<String>,
}

/// GET v2/locations/{locationsId}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_locations_info_types_list_builder()` + `dlp_locations_info_types_list_execute()`.
/// For task-level control, use `dlp_locations_info_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_locations_info_types_list(
    client: &SimpleHttpClient,
    args: &DlpLocationsInfoTypesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_locations_info_types_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.languageCode,
        &args.locationId,
    )?;
    dlp_locations_info_types_list_execute(builder)
}

/// POST v2/organizations/{organizationsId}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_deidentify_templates_create_execute()` to send, or `dlp_organizations_deidentify_templates_create` for simplest API.

pub fn dlp_organizations_deidentify_templates_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/deidentifyTemplates",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_deidentify_templates_create_execute()` or `dlp_organizations_deidentify_templates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_deidentify_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_deidentify_templates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_deidentify_templates_create_task()`.
/// For the simplest API, use `dlp_organizations_deidentify_templates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_deidentify_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_deidentify_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_deidentify_templates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_deidentify_templates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsDeidentifyTemplatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/organizations/{organizationsId}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_deidentify_templates_create_builder()` + `dlp_organizations_deidentify_templates_create_execute()`.
/// For task-level control, use `dlp_organizations_deidentify_templates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_create(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsDeidentifyTemplatesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_deidentify_templates_create_builder(client, &args.parent)?;
    dlp_organizations_deidentify_templates_create_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_deidentify_templates_delete_execute()` to send, or `dlp_organizations_deidentify_templates_delete` for simplest API.

pub fn dlp_organizations_deidentify_templates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/deidentifyTemplates/{deidentifyTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_deidentify_templates_delete_execute()` or `dlp_organizations_deidentify_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_deidentify_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_deidentify_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_deidentify_templates_delete_task()`.
/// For the simplest API, use `dlp_organizations_deidentify_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_deidentify_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_deidentify_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_deidentify_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_deidentify_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsDeidentifyTemplatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/organizations/{organizationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_deidentify_templates_delete_builder()` + `dlp_organizations_deidentify_templates_delete_execute()`.
/// For task-level control, use `dlp_organizations_deidentify_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_delete(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsDeidentifyTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_deidentify_templates_delete_builder(client, &args.name)?;
    dlp_organizations_deidentify_templates_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_deidentify_templates_get_execute()` to send, or `dlp_organizations_deidentify_templates_get` for simplest API.

pub fn dlp_organizations_deidentify_templates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/deidentifyTemplates/{deidentifyTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_deidentify_templates_get_execute()` or `dlp_organizations_deidentify_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_deidentify_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_deidentify_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_deidentify_templates_get_task()`.
/// For the simplest API, use `dlp_organizations_deidentify_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_deidentify_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_deidentify_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_deidentify_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_deidentify_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsDeidentifyTemplatesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_deidentify_templates_get_builder()` + `dlp_organizations_deidentify_templates_get_execute()`.
/// For task-level control, use `dlp_organizations_deidentify_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_get(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsDeidentifyTemplatesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_deidentify_templates_get_builder(client, &args.name)?;
    dlp_organizations_deidentify_templates_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_deidentify_templates_list_execute()` to send, or `dlp_organizations_deidentify_templates_list` for simplest API.

pub fn dlp_organizations_deidentify_templates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/deidentifyTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_deidentify_templates_list_execute()` or `dlp_organizations_deidentify_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_deidentify_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDeidentifyTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_deidentify_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_deidentify_templates_list_task()`.
/// For the simplest API, use `dlp_organizations_deidentify_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_deidentify_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_deidentify_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_deidentify_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_deidentify_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsDeidentifyTemplatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_deidentify_templates_list_builder()` + `dlp_organizations_deidentify_templates_list_execute()`.
/// For task-level control, use `dlp_organizations_deidentify_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_list(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsDeidentifyTemplatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_deidentify_templates_list_builder(
        client,
        &args.parent,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_organizations_deidentify_templates_list_execute(builder)
}

/// PATCH v2/organizations/{organizationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_deidentify_templates_patch_execute()` to send, or `dlp_organizations_deidentify_templates_patch` for simplest API.

pub fn dlp_organizations_deidentify_templates_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/deidentifyTemplates/{deidentifyTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/organizations/{organizationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_deidentify_templates_patch_execute()` or `dlp_organizations_deidentify_templates_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_deidentify_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/organizations/{organizationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_deidentify_templates_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_deidentify_templates_patch_task()`.
/// For the simplest API, use `dlp_organizations_deidentify_templates_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_deidentify_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_deidentify_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_deidentify_templates_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_deidentify_templates_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsDeidentifyTemplatesPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/organizations/{organizationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_deidentify_templates_patch_builder()` + `dlp_organizations_deidentify_templates_patch_execute()`.
/// For task-level control, use `dlp_organizations_deidentify_templates_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_deidentify_templates_patch(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsDeidentifyTemplatesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_deidentify_templates_patch_builder(client, &args.name)?;
    dlp_organizations_deidentify_templates_patch_execute(builder)
}

/// POST v2/organizations/{organizationsId}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_inspect_templates_create_execute()` to send, or `dlp_organizations_inspect_templates_create` for simplest API.

pub fn dlp_organizations_inspect_templates_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/inspectTemplates",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_inspect_templates_create_execute()` or `dlp_organizations_inspect_templates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_inspect_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_inspect_templates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_inspect_templates_create_task()`.
/// For the simplest API, use `dlp_organizations_inspect_templates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_inspect_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_inspect_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_inspect_templates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_inspect_templates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsInspectTemplatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/organizations/{organizationsId}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_inspect_templates_create_builder()` + `dlp_organizations_inspect_templates_create_execute()`.
/// For task-level control, use `dlp_organizations_inspect_templates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_create(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsInspectTemplatesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_inspect_templates_create_builder(client, &args.parent)?;
    dlp_organizations_inspect_templates_create_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/inspectTemplates/{inspectTemplatesId}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_inspect_templates_delete_execute()` to send, or `dlp_organizations_inspect_templates_delete` for simplest API.

pub fn dlp_organizations_inspect_templates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/inspectTemplates/{inspectTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/inspectTemplates/{inspectTemplatesId}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_inspect_templates_delete_execute()` or `dlp_organizations_inspect_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_inspect_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/inspectTemplates/{inspectTemplatesId}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_inspect_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_inspect_templates_delete_task()`.
/// For the simplest API, use `dlp_organizations_inspect_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_inspect_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_inspect_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_inspect_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_inspect_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsInspectTemplatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/organizations/{organizationsId}/inspectTemplates/{inspectTemplatesId}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_inspect_templates_delete_builder()` + `dlp_organizations_inspect_templates_delete_execute()`.
/// For task-level control, use `dlp_organizations_inspect_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_delete(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsInspectTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_inspect_templates_delete_builder(client, &args.name)?;
    dlp_organizations_inspect_templates_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/inspectTemplates/{inspectTemplatesId}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_inspect_templates_get_execute()` to send, or `dlp_organizations_inspect_templates_get` for simplest API.

pub fn dlp_organizations_inspect_templates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/inspectTemplates/{inspectTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/inspectTemplates/{inspectTemplatesId}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_inspect_templates_get_execute()` or `dlp_organizations_inspect_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_inspect_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/inspectTemplates/{inspectTemplatesId}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_inspect_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_inspect_templates_get_task()`.
/// For the simplest API, use `dlp_organizations_inspect_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_inspect_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_inspect_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_inspect_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_inspect_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsInspectTemplatesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/inspectTemplates/{inspectTemplatesId}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_inspect_templates_get_builder()` + `dlp_organizations_inspect_templates_get_execute()`.
/// For task-level control, use `dlp_organizations_inspect_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_get(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsInspectTemplatesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_inspect_templates_get_builder(client, &args.name)?;
    dlp_organizations_inspect_templates_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_inspect_templates_list_execute()` to send, or `dlp_organizations_inspect_templates_list` for simplest API.

pub fn dlp_organizations_inspect_templates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/inspectTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_inspect_templates_list_execute()` or `dlp_organizations_inspect_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_inspect_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInspectTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_inspect_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_inspect_templates_list_task()`.
/// For the simplest API, use `dlp_organizations_inspect_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_inspect_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_inspect_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_inspect_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_inspect_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsInspectTemplatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_inspect_templates_list_builder()` + `dlp_organizations_inspect_templates_list_execute()`.
/// For task-level control, use `dlp_organizations_inspect_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_list(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsInspectTemplatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_inspect_templates_list_builder(
        client,
        &args.parent,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_organizations_inspect_templates_list_execute(builder)
}

/// PATCH v2/organizations/{organizationsId}/inspectTemplates/{inspectTemplatesId}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_inspect_templates_patch_execute()` to send, or `dlp_organizations_inspect_templates_patch` for simplest API.

pub fn dlp_organizations_inspect_templates_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/inspectTemplates/{inspectTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/organizations/{organizationsId}/inspectTemplates/{inspectTemplatesId}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_inspect_templates_patch_execute()` or `dlp_organizations_inspect_templates_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_inspect_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/organizations/{organizationsId}/inspectTemplates/{inspectTemplatesId}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_inspect_templates_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_inspect_templates_patch_task()`.
/// For the simplest API, use `dlp_organizations_inspect_templates_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_inspect_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_inspect_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_inspect_templates_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_inspect_templates_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsInspectTemplatesPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/organizations/{organizationsId}/inspectTemplates/{inspectTemplatesId}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_inspect_templates_patch_builder()` + `dlp_organizations_inspect_templates_patch_execute()`.
/// For task-level control, use `dlp_organizations_inspect_templates_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_inspect_templates_patch(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsInspectTemplatesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_inspect_templates_patch_builder(client, &args.name)?;
    dlp_organizations_inspect_templates_patch_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/columnDataProfiles/{columnDataProfilesId}
/// Gets a column data profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_column_data_profiles_get_execute()` to send, or `dlp_organizations_locations_column_data_profiles_get` for simplest API.

pub fn dlp_organizations_locations_column_data_profiles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/columnDataProfiles/{columnDataProfilesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/columnDataProfiles/{columnDataProfilesId}
/// Gets a column data profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_column_data_profiles_get_execute()` or `dlp_organizations_locations_column_data_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_column_data_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_column_data_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ColumnDataProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ColumnDataProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/columnDataProfiles/{columnDataProfilesId}
/// Gets a column data profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_column_data_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_column_data_profiles_get_task()`.
/// For the simplest API, use `dlp_organizations_locations_column_data_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_column_data_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_column_data_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ColumnDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_column_data_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_column_data_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsColumnDataProfilesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/columnDataProfiles/{columnDataProfilesId}
/// Gets a column data profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_column_data_profiles_get_builder()` + `dlp_organizations_locations_column_data_profiles_get_execute()`.
/// For task-level control, use `dlp_organizations_locations_column_data_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_column_data_profiles_get(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsColumnDataProfilesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ColumnDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_column_data_profiles_get_builder(client, &args.name)?;
    dlp_organizations_locations_column_data_profiles_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/columnDataProfiles
/// Lists column data profiles for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_column_data_profiles_list_execute()` to send, or `dlp_organizations_locations_column_data_profiles_list` for simplest API.

pub fn dlp_organizations_locations_column_data_profiles_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/columnDataProfiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/columnDataProfiles
/// Lists column data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_column_data_profiles_list_execute()` or `dlp_organizations_locations_column_data_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_column_data_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_column_data_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListColumnDataProfilesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListColumnDataProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/columnDataProfiles
/// Lists column data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_column_data_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_column_data_profiles_list_task()`.
/// For the simplest API, use `dlp_organizations_locations_column_data_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_column_data_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_column_data_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListColumnDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_column_data_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_column_data_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsColumnDataProfilesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/columnDataProfiles
/// Lists column data profiles for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_column_data_profiles_list_builder()` + `dlp_organizations_locations_column_data_profiles_list_execute()`.
/// For task-level control, use `dlp_organizations_locations_column_data_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_column_data_profiles_list(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsColumnDataProfilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListColumnDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_column_data_profiles_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_organizations_locations_column_data_profiles_list_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/connections
/// Create a Connection to an external data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_connections_create_execute()` to send, or `dlp_organizations_locations_connections_create` for simplest API.

pub fn dlp_organizations_locations_connections_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/connections",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/connections
/// Create a Connection to an external data source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_connections_create_execute()` or `dlp_organizations_locations_connections_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_connections_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2Connection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/connections
/// Create a Connection to an external data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_connections_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_connections_create_task()`.
/// For the simplest API, use `dlp_organizations_locations_connections_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_connections_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_connections_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_connections_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_connections_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsConnectionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/connections
/// Create a Connection to an external data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_connections_create_builder()` + `dlp_organizations_locations_connections_create_execute()`.
/// For task-level control, use `dlp_organizations_locations_connections_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_create(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsConnectionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_connections_create_builder(client, &args.parent)?;
    dlp_organizations_locations_connections_create_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/connections/{connectionsId}
/// Delete a Connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_connections_delete_execute()` to send, or `dlp_organizations_locations_connections_delete` for simplest API.

pub fn dlp_organizations_locations_connections_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/connections/{connectionsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/connections/{connectionsId}
/// Delete a Connection.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_connections_delete_execute()` or `dlp_organizations_locations_connections_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_connections_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/connections/{connectionsId}
/// Delete a Connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_connections_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_connections_delete_task()`.
/// For the simplest API, use `dlp_organizations_locations_connections_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_connections_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_connections_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_connections_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_connections_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsConnectionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/connections/{connectionsId}
/// Delete a Connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_connections_delete_builder()` + `dlp_organizations_locations_connections_delete_execute()`.
/// For task-level control, use `dlp_organizations_locations_connections_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_delete(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsConnectionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_connections_delete_builder(client, &args.name)?;
    dlp_organizations_locations_connections_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/connections/{connectionsId}
/// Get a Connection by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_connections_get_execute()` to send, or `dlp_organizations_locations_connections_get` for simplest API.

pub fn dlp_organizations_locations_connections_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/connections/{connectionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/connections/{connectionsId}
/// Get a Connection by name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_connections_get_execute()` or `dlp_organizations_locations_connections_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_connections_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2Connection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/connections/{connectionsId}
/// Get a Connection by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_connections_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_connections_get_task()`.
/// For the simplest API, use `dlp_organizations_locations_connections_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_connections_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_connections_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_connections_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_connections_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsConnectionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/connections/{connectionsId}
/// Get a Connection by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_connections_get_builder()` + `dlp_organizations_locations_connections_get_execute()`.
/// For task-level control, use `dlp_organizations_locations_connections_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_get(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsConnectionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_connections_get_builder(client, &args.name)?;
    dlp_organizations_locations_connections_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/connections
/// Lists Connections in a parent. Use SearchConnections to see all connections within an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_connections_list_execute()` to send, or `dlp_organizations_locations_connections_list` for simplest API.

pub fn dlp_organizations_locations_connections_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/connections",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/connections
/// Lists Connections in a parent. Use SearchConnections to see all connections within an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_connections_list_execute()` or `dlp_organizations_locations_connections_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_connections_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListConnectionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListConnectionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/connections
/// Lists Connections in a parent. Use SearchConnections to see all connections within an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_connections_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_connections_list_task()`.
/// For the simplest API, use `dlp_organizations_locations_connections_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_connections_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_connections_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_connections_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_connections_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsConnectionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/connections
/// Lists Connections in a parent. Use SearchConnections to see all connections within an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_connections_list_builder()` + `dlp_organizations_locations_connections_list_execute()`.
/// For task-level control, use `dlp_organizations_locations_connections_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_list(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsConnectionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_connections_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_organizations_locations_connections_list_execute(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/connections/{connectionsId}
/// Update a Connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_connections_patch_execute()` to send, or `dlp_organizations_locations_connections_patch` for simplest API.

pub fn dlp_organizations_locations_connections_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/connections/{connectionsId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/connections/{connectionsId}
/// Update a Connection.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_connections_patch_execute()` or `dlp_organizations_locations_connections_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_connections_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2Connection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/connections/{connectionsId}
/// Update a Connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_connections_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_connections_patch_task()`.
/// For the simplest API, use `dlp_organizations_locations_connections_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_connections_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_connections_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_connections_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_connections_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsConnectionsPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/connections/{connectionsId}
/// Update a Connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_connections_patch_builder()` + `dlp_organizations_locations_connections_patch_execute()`.
/// For task-level control, use `dlp_organizations_locations_connections_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_patch(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsConnectionsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_connections_patch_builder(client, &args.name)?;
    dlp_organizations_locations_connections_patch_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/connections:search
/// Searches for Connections in a parent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_connections_search_execute()` to send, or `dlp_organizations_locations_connections_search` for simplest API.

pub fn dlp_organizations_locations_connections_search_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/connections:search",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/connections:search
/// Searches for Connections in a parent.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_connections_search_execute()` or `dlp_organizations_locations_connections_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_connections_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2SearchConnectionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2SearchConnectionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/connections:search
/// Searches for Connections in a parent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_connections_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_connections_search_task()`.
/// For the simplest API, use `dlp_organizations_locations_connections_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_connections_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_connections_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2SearchConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_connections_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_connections_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsConnectionsSearchArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/connections:search
/// Searches for Connections in a parent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_connections_search_builder()` + `dlp_organizations_locations_connections_search_execute()`.
/// For task-level control, use `dlp_organizations_locations_connections_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_connections_search(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsConnectionsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2SearchConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_connections_search_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_organizations_locations_connections_search_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_deidentify_templates_create_execute()` to send, or `dlp_organizations_locations_deidentify_templates_create` for simplest API.

pub fn dlp_organizations_locations_deidentify_templates_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/deidentifyTemplates",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_deidentify_templates_create_execute()` or `dlp_organizations_locations_deidentify_templates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_deidentify_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_deidentify_templates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_deidentify_templates_create_task()`.
/// For the simplest API, use `dlp_organizations_locations_deidentify_templates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_deidentify_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_deidentify_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_deidentify_templates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_deidentify_templates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsDeidentifyTemplatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_deidentify_templates_create_builder()` + `dlp_organizations_locations_deidentify_templates_create_execute()`.
/// For task-level control, use `dlp_organizations_locations_deidentify_templates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_create(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsDeidentifyTemplatesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_organizations_locations_deidentify_templates_create_builder(client, &args.parent)?;
    dlp_organizations_locations_deidentify_templates_create_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_deidentify_templates_delete_execute()` to send, or `dlp_organizations_locations_deidentify_templates_delete` for simplest API.

pub fn dlp_organizations_locations_deidentify_templates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_deidentify_templates_delete_execute()` or `dlp_organizations_locations_deidentify_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_deidentify_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_deidentify_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_deidentify_templates_delete_task()`.
/// For the simplest API, use `dlp_organizations_locations_deidentify_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_deidentify_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_deidentify_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_deidentify_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_deidentify_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsDeidentifyTemplatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_deidentify_templates_delete_builder()` + `dlp_organizations_locations_deidentify_templates_delete_execute()`.
/// For task-level control, use `dlp_organizations_locations_deidentify_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_delete(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsDeidentifyTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_organizations_locations_deidentify_templates_delete_builder(client, &args.name)?;
    dlp_organizations_locations_deidentify_templates_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_deidentify_templates_get_execute()` to send, or `dlp_organizations_locations_deidentify_templates_get` for simplest API.

pub fn dlp_organizations_locations_deidentify_templates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_deidentify_templates_get_execute()` or `dlp_organizations_locations_deidentify_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_deidentify_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_deidentify_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_deidentify_templates_get_task()`.
/// For the simplest API, use `dlp_organizations_locations_deidentify_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_deidentify_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_deidentify_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_deidentify_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_deidentify_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsDeidentifyTemplatesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_deidentify_templates_get_builder()` + `dlp_organizations_locations_deidentify_templates_get_execute()`.
/// For task-level control, use `dlp_organizations_locations_deidentify_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_get(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsDeidentifyTemplatesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_deidentify_templates_get_builder(client, &args.name)?;
    dlp_organizations_locations_deidentify_templates_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_deidentify_templates_list_execute()` to send, or `dlp_organizations_locations_deidentify_templates_list` for simplest API.

pub fn dlp_organizations_locations_deidentify_templates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/deidentifyTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_deidentify_templates_list_execute()` or `dlp_organizations_locations_deidentify_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_deidentify_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDeidentifyTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_deidentify_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_deidentify_templates_list_task()`.
/// For the simplest API, use `dlp_organizations_locations_deidentify_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_deidentify_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_deidentify_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_deidentify_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_deidentify_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsDeidentifyTemplatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_deidentify_templates_list_builder()` + `dlp_organizations_locations_deidentify_templates_list_execute()`.
/// For task-level control, use `dlp_organizations_locations_deidentify_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_list(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsDeidentifyTemplatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_deidentify_templates_list_builder(
        client,
        &args.parent,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_organizations_locations_deidentify_templates_list_execute(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_deidentify_templates_patch_execute()` to send, or `dlp_organizations_locations_deidentify_templates_patch` for simplest API.

pub fn dlp_organizations_locations_deidentify_templates_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_deidentify_templates_patch_execute()` or `dlp_organizations_locations_deidentify_templates_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_deidentify_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_deidentify_templates_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_deidentify_templates_patch_task()`.
/// For the simplest API, use `dlp_organizations_locations_deidentify_templates_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_deidentify_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_deidentify_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_deidentify_templates_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_deidentify_templates_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsDeidentifyTemplatesPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_deidentify_templates_patch_builder()` + `dlp_organizations_locations_deidentify_templates_patch_execute()`.
/// For task-level control, use `dlp_organizations_locations_deidentify_templates_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_deidentify_templates_patch(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsDeidentifyTemplatesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_organizations_locations_deidentify_templates_patch_builder(client, &args.name)?;
    dlp_organizations_locations_deidentify_templates_patch_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs
/// Creates a config for discovery to scan and profile storage.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_discovery_configs_create_execute()` to send, or `dlp_organizations_locations_discovery_configs_create` for simplest API.

pub fn dlp_organizations_locations_discovery_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/discoveryConfigs",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs
/// Creates a config for discovery to scan and profile storage.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_discovery_configs_create_execute()` or `dlp_organizations_locations_discovery_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_discovery_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DiscoveryConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs
/// Creates a config for discovery to scan and profile storage.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_discovery_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_discovery_configs_create_task()`.
/// For the simplest API, use `dlp_organizations_locations_discovery_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_discovery_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_discovery_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_discovery_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_discovery_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsDiscoveryConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs
/// Creates a config for discovery to scan and profile storage.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_discovery_configs_create_builder()` + `dlp_organizations_locations_discovery_configs_create_execute()`.
/// For task-level control, use `dlp_organizations_locations_discovery_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_create(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsDiscoveryConfigsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_organizations_locations_discovery_configs_create_builder(client, &args.parent)?;
    dlp_organizations_locations_discovery_configs_create_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Deletes a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_discovery_configs_delete_execute()` to send, or `dlp_organizations_locations_discovery_configs_delete` for simplest API.

pub fn dlp_organizations_locations_discovery_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Deletes a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_discovery_configs_delete_execute()` or `dlp_organizations_locations_discovery_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_discovery_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Deletes a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_discovery_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_discovery_configs_delete_task()`.
/// For the simplest API, use `dlp_organizations_locations_discovery_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_discovery_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_discovery_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_discovery_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_discovery_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsDiscoveryConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Deletes a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_discovery_configs_delete_builder()` + `dlp_organizations_locations_discovery_configs_delete_execute()`.
/// For task-level control, use `dlp_organizations_locations_discovery_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_delete(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsDiscoveryConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_discovery_configs_delete_builder(client, &args.name)?;
    dlp_organizations_locations_discovery_configs_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Gets a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_discovery_configs_get_execute()` to send, or `dlp_organizations_locations_discovery_configs_get` for simplest API.

pub fn dlp_organizations_locations_discovery_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Gets a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_discovery_configs_get_execute()` or `dlp_organizations_locations_discovery_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_discovery_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DiscoveryConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Gets a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_discovery_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_discovery_configs_get_task()`.
/// For the simplest API, use `dlp_organizations_locations_discovery_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_discovery_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_discovery_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_discovery_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_discovery_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsDiscoveryConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Gets a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_discovery_configs_get_builder()` + `dlp_organizations_locations_discovery_configs_get_execute()`.
/// For task-level control, use `dlp_organizations_locations_discovery_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_get(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsDiscoveryConfigsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_discovery_configs_get_builder(client, &args.name)?;
    dlp_organizations_locations_discovery_configs_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs
/// Lists discovery configurations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_discovery_configs_list_execute()` to send, or `dlp_organizations_locations_discovery_configs_list` for simplest API.

pub fn dlp_organizations_locations_discovery_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/discoveryConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs
/// Lists discovery configurations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_discovery_configs_list_execute()` or `dlp_organizations_locations_discovery_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_discovery_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListDiscoveryConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDiscoveryConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs
/// Lists discovery configurations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_discovery_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_discovery_configs_list_task()`.
/// For the simplest API, use `dlp_organizations_locations_discovery_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_discovery_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_discovery_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDiscoveryConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_discovery_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_discovery_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsDiscoveryConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs
/// Lists discovery configurations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_discovery_configs_list_builder()` + `dlp_organizations_locations_discovery_configs_list_execute()`.
/// For task-level control, use `dlp_organizations_locations_discovery_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_list(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsDiscoveryConfigsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDiscoveryConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_discovery_configs_list_builder(
        client,
        &args.parent,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_organizations_locations_discovery_configs_list_execute(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Updates a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_discovery_configs_patch_execute()` to send, or `dlp_organizations_locations_discovery_configs_patch` for simplest API.

pub fn dlp_organizations_locations_discovery_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Updates a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_discovery_configs_patch_execute()` or `dlp_organizations_locations_discovery_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_discovery_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DiscoveryConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Updates a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_discovery_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_discovery_configs_patch_task()`.
/// For the simplest API, use `dlp_organizations_locations_discovery_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_discovery_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_discovery_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_discovery_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_discovery_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsDiscoveryConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Updates a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_discovery_configs_patch_builder()` + `dlp_organizations_locations_discovery_configs_patch_execute()`.
/// For task-level control, use `dlp_organizations_locations_discovery_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_discovery_configs_patch(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsDiscoveryConfigsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_discovery_configs_patch_builder(client, &args.name)?;
    dlp_organizations_locations_discovery_configs_patch_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_dlp_jobs_list_execute()` to send, or `dlp_organizations_locations_dlp_jobs_list` for simplest API.

pub fn dlp_organizations_locations_dlp_jobs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    type_rs: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/dlpJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = type_rs.as_ref() {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_dlp_jobs_list_execute()` or `dlp_organizations_locations_dlp_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_dlp_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_dlp_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListDlpJobsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDlpJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_dlp_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_dlp_jobs_list_task()`.
/// For the simplest API, use `dlp_organizations_locations_dlp_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_dlp_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_dlp_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDlpJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_dlp_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_dlp_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsDlpJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_dlp_jobs_list_builder()` + `dlp_organizations_locations_dlp_jobs_list_execute()`.
/// For task-level control, use `dlp_organizations_locations_dlp_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_dlp_jobs_list(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsDlpJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDlpJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_dlp_jobs_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.type_rs,
    )?;
    dlp_organizations_locations_dlp_jobs_list_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}
/// Delete a FileStoreDataProfile. Will not prevent the profile from being regenerated if the resource is still included in a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_file_store_data_profiles_delete_execute()` to send, or `dlp_organizations_locations_file_store_data_profiles_delete` for simplest API.

pub fn dlp_organizations_locations_file_store_data_profiles_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}
/// Delete a FileStoreDataProfile. Will not prevent the profile from being regenerated if the resource is still included in a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_file_store_data_profiles_delete_execute()` or `dlp_organizations_locations_file_store_data_profiles_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_file_store_data_profiles_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_file_store_data_profiles_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}
/// Delete a FileStoreDataProfile. Will not prevent the profile from being regenerated if the resource is still included in a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_file_store_data_profiles_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_file_store_data_profiles_delete_task()`.
/// For the simplest API, use `dlp_organizations_locations_file_store_data_profiles_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_file_store_data_profiles_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_file_store_data_profiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_file_store_data_profiles_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_file_store_data_profiles_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsFileStoreDataProfilesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}
/// Delete a FileStoreDataProfile. Will not prevent the profile from being regenerated if the resource is still included in a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_file_store_data_profiles_delete_builder()` + `dlp_organizations_locations_file_store_data_profiles_delete_execute()`.
/// For task-level control, use `dlp_organizations_locations_file_store_data_profiles_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_file_store_data_profiles_delete(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsFileStoreDataProfilesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_organizations_locations_file_store_data_profiles_delete_builder(client, &args.name)?;
    dlp_organizations_locations_file_store_data_profiles_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}
/// Gets a file store data profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_file_store_data_profiles_get_execute()` to send, or `dlp_organizations_locations_file_store_data_profiles_get` for simplest API.

pub fn dlp_organizations_locations_file_store_data_profiles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}
/// Gets a file store data profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_file_store_data_profiles_get_execute()` or `dlp_organizations_locations_file_store_data_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_file_store_data_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_file_store_data_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2FileStoreDataProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2FileStoreDataProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}
/// Gets a file store data profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_file_store_data_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_file_store_data_profiles_get_task()`.
/// For the simplest API, use `dlp_organizations_locations_file_store_data_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_file_store_data_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_file_store_data_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2FileStoreDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_file_store_data_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_file_store_data_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsFileStoreDataProfilesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}
/// Gets a file store data profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_file_store_data_profiles_get_builder()` + `dlp_organizations_locations_file_store_data_profiles_get_execute()`.
/// For task-level control, use `dlp_organizations_locations_file_store_data_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_file_store_data_profiles_get(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsFileStoreDataProfilesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2FileStoreDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_organizations_locations_file_store_data_profiles_get_builder(client, &args.name)?;
    dlp_organizations_locations_file_store_data_profiles_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/fileStoreDataProfiles
/// Lists file store data profiles for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_file_store_data_profiles_list_execute()` to send, or `dlp_organizations_locations_file_store_data_profiles_list` for simplest API.

pub fn dlp_organizations_locations_file_store_data_profiles_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/fileStoreDataProfiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/fileStoreDataProfiles
/// Lists file store data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_file_store_data_profiles_list_execute()` or `dlp_organizations_locations_file_store_data_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_file_store_data_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_file_store_data_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePrivacyDlpV2ListFileStoreDataProfilesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListFileStoreDataProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/fileStoreDataProfiles
/// Lists file store data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_file_store_data_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_file_store_data_profiles_list_task()`.
/// For the simplest API, use `dlp_organizations_locations_file_store_data_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_file_store_data_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_file_store_data_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListFileStoreDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_file_store_data_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_file_store_data_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsFileStoreDataProfilesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/fileStoreDataProfiles
/// Lists file store data profiles for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_file_store_data_profiles_list_builder()` + `dlp_organizations_locations_file_store_data_profiles_list_execute()`.
/// For task-level control, use `dlp_organizations_locations_file_store_data_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_file_store_data_profiles_list(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsFileStoreDataProfilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListFileStoreDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_file_store_data_profiles_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_organizations_locations_file_store_data_profiles_list_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_info_types_list_execute()` to send, or `dlp_organizations_locations_info_types_list` for simplest API.

pub fn dlp_organizations_locations_info_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    languageCode: &Option<String>,
    locationId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/infoTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_info_types_list_execute()` or `dlp_organizations_locations_info_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_info_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_info_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInfoTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_info_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_info_types_list_task()`.
/// For the simplest API, use `dlp_organizations_locations_info_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_info_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_info_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_info_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_info_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsInfoTypesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: locationId
    pub locationId: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_info_types_list_builder()` + `dlp_organizations_locations_info_types_list_execute()`.
/// For task-level control, use `dlp_organizations_locations_info_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_info_types_list(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsInfoTypesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_info_types_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.languageCode,
        &args.locationId,
    )?;
    dlp_organizations_locations_info_types_list_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_inspect_templates_create_execute()` to send, or `dlp_organizations_locations_inspect_templates_create` for simplest API.

pub fn dlp_organizations_locations_inspect_templates_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/inspectTemplates",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_inspect_templates_create_execute()` or `dlp_organizations_locations_inspect_templates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_inspect_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_inspect_templates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_inspect_templates_create_task()`.
/// For the simplest API, use `dlp_organizations_locations_inspect_templates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_inspect_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_inspect_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_inspect_templates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_inspect_templates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsInspectTemplatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_inspect_templates_create_builder()` + `dlp_organizations_locations_inspect_templates_create_execute()`.
/// For task-level control, use `dlp_organizations_locations_inspect_templates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_create(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsInspectTemplatesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_organizations_locations_inspect_templates_create_builder(client, &args.parent)?;
    dlp_organizations_locations_inspect_templates_create_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_inspect_templates_delete_execute()` to send, or `dlp_organizations_locations_inspect_templates_delete` for simplest API.

pub fn dlp_organizations_locations_inspect_templates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_inspect_templates_delete_execute()` or `dlp_organizations_locations_inspect_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_inspect_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_inspect_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_inspect_templates_delete_task()`.
/// For the simplest API, use `dlp_organizations_locations_inspect_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_inspect_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_inspect_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_inspect_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_inspect_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsInspectTemplatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_inspect_templates_delete_builder()` + `dlp_organizations_locations_inspect_templates_delete_execute()`.
/// For task-level control, use `dlp_organizations_locations_inspect_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_delete(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsInspectTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_inspect_templates_delete_builder(client, &args.name)?;
    dlp_organizations_locations_inspect_templates_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_inspect_templates_get_execute()` to send, or `dlp_organizations_locations_inspect_templates_get` for simplest API.

pub fn dlp_organizations_locations_inspect_templates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_inspect_templates_get_execute()` or `dlp_organizations_locations_inspect_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_inspect_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_inspect_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_inspect_templates_get_task()`.
/// For the simplest API, use `dlp_organizations_locations_inspect_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_inspect_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_inspect_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_inspect_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_inspect_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsInspectTemplatesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_inspect_templates_get_builder()` + `dlp_organizations_locations_inspect_templates_get_execute()`.
/// For task-level control, use `dlp_organizations_locations_inspect_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_get(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsInspectTemplatesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_inspect_templates_get_builder(client, &args.name)?;
    dlp_organizations_locations_inspect_templates_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_inspect_templates_list_execute()` to send, or `dlp_organizations_locations_inspect_templates_list` for simplest API.

pub fn dlp_organizations_locations_inspect_templates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/inspectTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_inspect_templates_list_execute()` or `dlp_organizations_locations_inspect_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_inspect_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInspectTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_inspect_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_inspect_templates_list_task()`.
/// For the simplest API, use `dlp_organizations_locations_inspect_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_inspect_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_inspect_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_inspect_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_inspect_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsInspectTemplatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_inspect_templates_list_builder()` + `dlp_organizations_locations_inspect_templates_list_execute()`.
/// For task-level control, use `dlp_organizations_locations_inspect_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_list(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsInspectTemplatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_inspect_templates_list_builder(
        client,
        &args.parent,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_organizations_locations_inspect_templates_list_execute(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_inspect_templates_patch_execute()` to send, or `dlp_organizations_locations_inspect_templates_patch` for simplest API.

pub fn dlp_organizations_locations_inspect_templates_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_inspect_templates_patch_execute()` or `dlp_organizations_locations_inspect_templates_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_inspect_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_inspect_templates_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_inspect_templates_patch_task()`.
/// For the simplest API, use `dlp_organizations_locations_inspect_templates_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_inspect_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_inspect_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_inspect_templates_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_inspect_templates_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsInspectTemplatesPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_inspect_templates_patch_builder()` + `dlp_organizations_locations_inspect_templates_patch_execute()`.
/// For task-level control, use `dlp_organizations_locations_inspect_templates_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_inspect_templates_patch(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsInspectTemplatesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_inspect_templates_patch_builder(client, &args.name)?;
    dlp_organizations_locations_inspect_templates_patch_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_job_triggers_create_execute()` to send, or `dlp_organizations_locations_job_triggers_create` for simplest API.

pub fn dlp_organizations_locations_job_triggers_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/jobTriggers",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_job_triggers_create_execute()` or `dlp_organizations_locations_job_triggers_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_job_triggers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_job_triggers_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_job_triggers_create_task()`.
/// For the simplest API, use `dlp_organizations_locations_job_triggers_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_job_triggers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_job_triggers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_job_triggers_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_job_triggers_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsJobTriggersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_job_triggers_create_builder()` + `dlp_organizations_locations_job_triggers_create_execute()`.
/// For task-level control, use `dlp_organizations_locations_job_triggers_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_create(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsJobTriggersCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_job_triggers_create_builder(client, &args.parent)?;
    dlp_organizations_locations_job_triggers_create_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_job_triggers_delete_execute()` to send, or `dlp_organizations_locations_job_triggers_delete` for simplest API.

pub fn dlp_organizations_locations_job_triggers_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/jobTriggers/{jobTriggersId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_job_triggers_delete_execute()` or `dlp_organizations_locations_job_triggers_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_job_triggers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_job_triggers_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_job_triggers_delete_task()`.
/// For the simplest API, use `dlp_organizations_locations_job_triggers_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_job_triggers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_job_triggers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_job_triggers_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_job_triggers_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsJobTriggersDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_job_triggers_delete_builder()` + `dlp_organizations_locations_job_triggers_delete_execute()`.
/// For task-level control, use `dlp_organizations_locations_job_triggers_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_delete(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsJobTriggersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_job_triggers_delete_builder(client, &args.name)?;
    dlp_organizations_locations_job_triggers_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_job_triggers_get_execute()` to send, or `dlp_organizations_locations_job_triggers_get` for simplest API.

pub fn dlp_organizations_locations_job_triggers_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/jobTriggers/{jobTriggersId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_job_triggers_get_execute()` or `dlp_organizations_locations_job_triggers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_job_triggers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_job_triggers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_job_triggers_get_task()`.
/// For the simplest API, use `dlp_organizations_locations_job_triggers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_job_triggers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_job_triggers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_job_triggers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_job_triggers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsJobTriggersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_job_triggers_get_builder()` + `dlp_organizations_locations_job_triggers_get_execute()`.
/// For task-level control, use `dlp_organizations_locations_job_triggers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_get(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsJobTriggersGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_job_triggers_get_builder(client, &args.name)?;
    dlp_organizations_locations_job_triggers_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_job_triggers_list_execute()` to send, or `dlp_organizations_locations_job_triggers_list` for simplest API.

pub fn dlp_organizations_locations_job_triggers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    type_rs: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/jobTriggers",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = type_rs.as_ref() {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_job_triggers_list_execute()` or `dlp_organizations_locations_job_triggers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_job_triggers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListJobTriggersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListJobTriggersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_job_triggers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_job_triggers_list_task()`.
/// For the simplest API, use `dlp_organizations_locations_job_triggers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_job_triggers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_job_triggers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListJobTriggersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_job_triggers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_job_triggers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsJobTriggersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_job_triggers_list_builder()` + `dlp_organizations_locations_job_triggers_list_execute()`.
/// For task-level control, use `dlp_organizations_locations_job_triggers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_list(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsJobTriggersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListJobTriggersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_job_triggers_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.type_rs,
    )?;
    dlp_organizations_locations_job_triggers_list_execute(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_job_triggers_patch_execute()` to send, or `dlp_organizations_locations_job_triggers_patch` for simplest API.

pub fn dlp_organizations_locations_job_triggers_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/jobTriggers/{jobTriggersId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_job_triggers_patch_execute()` or `dlp_organizations_locations_job_triggers_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_job_triggers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_job_triggers_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_job_triggers_patch_task()`.
/// For the simplest API, use `dlp_organizations_locations_job_triggers_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_job_triggers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_job_triggers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_job_triggers_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_job_triggers_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsJobTriggersPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_job_triggers_patch_builder()` + `dlp_organizations_locations_job_triggers_patch_execute()`.
/// For task-level control, use `dlp_organizations_locations_job_triggers_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_job_triggers_patch(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsJobTriggersPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_job_triggers_patch_builder(client, &args.name)?;
    dlp_organizations_locations_job_triggers_patch_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/projectDataProfiles/{projectDataProfilesId}
/// Gets a project data profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_project_data_profiles_get_execute()` to send, or `dlp_organizations_locations_project_data_profiles_get` for simplest API.

pub fn dlp_organizations_locations_project_data_profiles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/projectDataProfiles/{projectDataProfilesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/projectDataProfiles/{projectDataProfilesId}
/// Gets a project data profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_project_data_profiles_get_execute()` or `dlp_organizations_locations_project_data_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_project_data_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_project_data_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ProjectDataProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ProjectDataProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/projectDataProfiles/{projectDataProfilesId}
/// Gets a project data profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_project_data_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_project_data_profiles_get_task()`.
/// For the simplest API, use `dlp_organizations_locations_project_data_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_project_data_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_project_data_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ProjectDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_project_data_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_project_data_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsProjectDataProfilesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/projectDataProfiles/{projectDataProfilesId}
/// Gets a project data profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_project_data_profiles_get_builder()` + `dlp_organizations_locations_project_data_profiles_get_execute()`.
/// For task-level control, use `dlp_organizations_locations_project_data_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_project_data_profiles_get(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsProjectDataProfilesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ProjectDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_organizations_locations_project_data_profiles_get_builder(client, &args.name)?;
    dlp_organizations_locations_project_data_profiles_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/projectDataProfiles
/// Lists project data profiles for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_project_data_profiles_list_execute()` to send, or `dlp_organizations_locations_project_data_profiles_list` for simplest API.

pub fn dlp_organizations_locations_project_data_profiles_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/projectDataProfiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/projectDataProfiles
/// Lists project data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_project_data_profiles_list_execute()` or `dlp_organizations_locations_project_data_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_project_data_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_project_data_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePrivacyDlpV2ListProjectDataProfilesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListProjectDataProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/projectDataProfiles
/// Lists project data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_project_data_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_project_data_profiles_list_task()`.
/// For the simplest API, use `dlp_organizations_locations_project_data_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_project_data_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_project_data_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListProjectDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_project_data_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_project_data_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsProjectDataProfilesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/projectDataProfiles
/// Lists project data profiles for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_project_data_profiles_list_builder()` + `dlp_organizations_locations_project_data_profiles_list_execute()`.
/// For task-level control, use `dlp_organizations_locations_project_data_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_project_data_profiles_list(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsProjectDataProfilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListProjectDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_project_data_profiles_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_organizations_locations_project_data_profiles_list_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_stored_info_types_create_execute()` to send, or `dlp_organizations_locations_stored_info_types_create` for simplest API.

pub fn dlp_organizations_locations_stored_info_types_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/storedInfoTypes",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_stored_info_types_create_execute()` or `dlp_organizations_locations_stored_info_types_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_stored_info_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_stored_info_types_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_stored_info_types_create_task()`.
/// For the simplest API, use `dlp_organizations_locations_stored_info_types_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_stored_info_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_stored_info_types_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_stored_info_types_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_stored_info_types_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsStoredInfoTypesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_stored_info_types_create_builder()` + `dlp_organizations_locations_stored_info_types_create_execute()`.
/// For task-level control, use `dlp_organizations_locations_stored_info_types_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_create(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsStoredInfoTypesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_organizations_locations_stored_info_types_create_builder(client, &args.parent)?;
    dlp_organizations_locations_stored_info_types_create_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_stored_info_types_delete_execute()` to send, or `dlp_organizations_locations_stored_info_types_delete` for simplest API.

pub fn dlp_organizations_locations_stored_info_types_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_stored_info_types_delete_execute()` or `dlp_organizations_locations_stored_info_types_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_stored_info_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_stored_info_types_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_stored_info_types_delete_task()`.
/// For the simplest API, use `dlp_organizations_locations_stored_info_types_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_stored_info_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_stored_info_types_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_stored_info_types_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_stored_info_types_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsStoredInfoTypesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_stored_info_types_delete_builder()` + `dlp_organizations_locations_stored_info_types_delete_execute()`.
/// For task-level control, use `dlp_organizations_locations_stored_info_types_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_delete(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsStoredInfoTypesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_stored_info_types_delete_builder(client, &args.name)?;
    dlp_organizations_locations_stored_info_types_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_stored_info_types_get_execute()` to send, or `dlp_organizations_locations_stored_info_types_get` for simplest API.

pub fn dlp_organizations_locations_stored_info_types_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_stored_info_types_get_execute()` or `dlp_organizations_locations_stored_info_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_stored_info_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_stored_info_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_stored_info_types_get_task()`.
/// For the simplest API, use `dlp_organizations_locations_stored_info_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_stored_info_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_stored_info_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_stored_info_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_stored_info_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsStoredInfoTypesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_stored_info_types_get_builder()` + `dlp_organizations_locations_stored_info_types_get_execute()`.
/// For task-level control, use `dlp_organizations_locations_stored_info_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_get(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsStoredInfoTypesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_stored_info_types_get_builder(client, &args.name)?;
    dlp_organizations_locations_stored_info_types_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_stored_info_types_list_execute()` to send, or `dlp_organizations_locations_stored_info_types_list` for simplest API.

pub fn dlp_organizations_locations_stored_info_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/storedInfoTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_stored_info_types_list_execute()` or `dlp_organizations_locations_stored_info_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_stored_info_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListStoredInfoTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_stored_info_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_stored_info_types_list_task()`.
/// For the simplest API, use `dlp_organizations_locations_stored_info_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_stored_info_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_stored_info_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_stored_info_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_stored_info_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsStoredInfoTypesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_stored_info_types_list_builder()` + `dlp_organizations_locations_stored_info_types_list_execute()`.
/// For task-level control, use `dlp_organizations_locations_stored_info_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_list(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsStoredInfoTypesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_stored_info_types_list_builder(
        client,
        &args.parent,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_organizations_locations_stored_info_types_list_execute(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_stored_info_types_patch_execute()` to send, or `dlp_organizations_locations_stored_info_types_patch` for simplest API.

pub fn dlp_organizations_locations_stored_info_types_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_stored_info_types_patch_execute()` or `dlp_organizations_locations_stored_info_types_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_stored_info_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_stored_info_types_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_stored_info_types_patch_task()`.
/// For the simplest API, use `dlp_organizations_locations_stored_info_types_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_stored_info_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_stored_info_types_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_stored_info_types_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_stored_info_types_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsStoredInfoTypesPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_stored_info_types_patch_builder()` + `dlp_organizations_locations_stored_info_types_patch_execute()`.
/// For task-level control, use `dlp_organizations_locations_stored_info_types_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_stored_info_types_patch(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsStoredInfoTypesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_stored_info_types_patch_builder(client, &args.name)?;
    dlp_organizations_locations_stored_info_types_patch_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}
/// Delete a TableDataProfile. Will not prevent the profile from being regenerated if the table is still included in a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_table_data_profiles_delete_execute()` to send, or `dlp_organizations_locations_table_data_profiles_delete` for simplest API.

pub fn dlp_organizations_locations_table_data_profiles_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}
/// Delete a TableDataProfile. Will not prevent the profile from being regenerated if the table is still included in a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_table_data_profiles_delete_execute()` or `dlp_organizations_locations_table_data_profiles_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_table_data_profiles_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_table_data_profiles_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}
/// Delete a TableDataProfile. Will not prevent the profile from being regenerated if the table is still included in a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_table_data_profiles_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_table_data_profiles_delete_task()`.
/// For the simplest API, use `dlp_organizations_locations_table_data_profiles_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_table_data_profiles_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_table_data_profiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_table_data_profiles_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_table_data_profiles_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsTableDataProfilesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}
/// Delete a TableDataProfile. Will not prevent the profile from being regenerated if the table is still included in a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_table_data_profiles_delete_builder()` + `dlp_organizations_locations_table_data_profiles_delete_execute()`.
/// For task-level control, use `dlp_organizations_locations_table_data_profiles_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_table_data_profiles_delete(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsTableDataProfilesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_organizations_locations_table_data_profiles_delete_builder(client, &args.name)?;
    dlp_organizations_locations_table_data_profiles_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}
/// Gets a table data profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_table_data_profiles_get_execute()` to send, or `dlp_organizations_locations_table_data_profiles_get` for simplest API.

pub fn dlp_organizations_locations_table_data_profiles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}
/// Gets a table data profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_table_data_profiles_get_execute()` or `dlp_organizations_locations_table_data_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_table_data_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_table_data_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2TableDataProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2TableDataProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}
/// Gets a table data profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_table_data_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_table_data_profiles_get_task()`.
/// For the simplest API, use `dlp_organizations_locations_table_data_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_table_data_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_table_data_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2TableDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_table_data_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_table_data_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsTableDataProfilesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}
/// Gets a table data profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_table_data_profiles_get_builder()` + `dlp_organizations_locations_table_data_profiles_get_execute()`.
/// For task-level control, use `dlp_organizations_locations_table_data_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_table_data_profiles_get(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsTableDataProfilesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2TableDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_table_data_profiles_get_builder(client, &args.name)?;
    dlp_organizations_locations_table_data_profiles_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/tableDataProfiles
/// Lists table data profiles for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_locations_table_data_profiles_list_execute()` to send, or `dlp_organizations_locations_table_data_profiles_list` for simplest API.

pub fn dlp_organizations_locations_table_data_profiles_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/locations/{locationsId}/tableDataProfiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/tableDataProfiles
/// Lists table data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_locations_table_data_profiles_list_execute()` or `dlp_organizations_locations_table_data_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_table_data_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_table_data_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListTableDataProfilesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListTableDataProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/tableDataProfiles
/// Lists table data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_locations_table_data_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_locations_table_data_profiles_list_task()`.
/// For the simplest API, use `dlp_organizations_locations_table_data_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_locations_table_data_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_locations_table_data_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListTableDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_locations_table_data_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_locations_table_data_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsTableDataProfilesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/tableDataProfiles
/// Lists table data profiles for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_locations_table_data_profiles_list_builder()` + `dlp_organizations_locations_table_data_profiles_list_execute()`.
/// For task-level control, use `dlp_organizations_locations_table_data_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_locations_table_data_profiles_list(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsLocationsTableDataProfilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListTableDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_locations_table_data_profiles_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_organizations_locations_table_data_profiles_list_execute(builder)
}

/// POST v2/organizations/{organizationsId}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_stored_info_types_create_execute()` to send, or `dlp_organizations_stored_info_types_create` for simplest API.

pub fn dlp_organizations_stored_info_types_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/storedInfoTypes",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_stored_info_types_create_execute()` or `dlp_organizations_stored_info_types_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_stored_info_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_stored_info_types_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_stored_info_types_create_task()`.
/// For the simplest API, use `dlp_organizations_stored_info_types_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_stored_info_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_stored_info_types_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_stored_info_types_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_stored_info_types_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsStoredInfoTypesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/organizations/{organizationsId}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_stored_info_types_create_builder()` + `dlp_organizations_stored_info_types_create_execute()`.
/// For task-level control, use `dlp_organizations_stored_info_types_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_create(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsStoredInfoTypesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_stored_info_types_create_builder(client, &args.parent)?;
    dlp_organizations_stored_info_types_create_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/storedInfoTypes/{storedInfoTypesId}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_stored_info_types_delete_execute()` to send, or `dlp_organizations_stored_info_types_delete` for simplest API.

pub fn dlp_organizations_stored_info_types_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/storedInfoTypes/{storedInfoTypesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/storedInfoTypes/{storedInfoTypesId}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_stored_info_types_delete_execute()` or `dlp_organizations_stored_info_types_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_stored_info_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/storedInfoTypes/{storedInfoTypesId}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_stored_info_types_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_stored_info_types_delete_task()`.
/// For the simplest API, use `dlp_organizations_stored_info_types_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_stored_info_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_stored_info_types_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_stored_info_types_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_stored_info_types_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsStoredInfoTypesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/organizations/{organizationsId}/storedInfoTypes/{storedInfoTypesId}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_stored_info_types_delete_builder()` + `dlp_organizations_stored_info_types_delete_execute()`.
/// For task-level control, use `dlp_organizations_stored_info_types_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_delete(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsStoredInfoTypesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_stored_info_types_delete_builder(client, &args.name)?;
    dlp_organizations_stored_info_types_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/storedInfoTypes/{storedInfoTypesId}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_stored_info_types_get_execute()` to send, or `dlp_organizations_stored_info_types_get` for simplest API.

pub fn dlp_organizations_stored_info_types_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/storedInfoTypes/{storedInfoTypesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/storedInfoTypes/{storedInfoTypesId}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_stored_info_types_get_execute()` or `dlp_organizations_stored_info_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_stored_info_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/storedInfoTypes/{storedInfoTypesId}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_stored_info_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_stored_info_types_get_task()`.
/// For the simplest API, use `dlp_organizations_stored_info_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_stored_info_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_stored_info_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_stored_info_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_stored_info_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsStoredInfoTypesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/storedInfoTypes/{storedInfoTypesId}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_stored_info_types_get_builder()` + `dlp_organizations_stored_info_types_get_execute()`.
/// For task-level control, use `dlp_organizations_stored_info_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_get(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsStoredInfoTypesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_stored_info_types_get_builder(client, &args.name)?;
    dlp_organizations_stored_info_types_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_stored_info_types_list_execute()` to send, or `dlp_organizations_stored_info_types_list` for simplest API.

pub fn dlp_organizations_stored_info_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/storedInfoTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_stored_info_types_list_execute()` or `dlp_organizations_stored_info_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_stored_info_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListStoredInfoTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_stored_info_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_stored_info_types_list_task()`.
/// For the simplest API, use `dlp_organizations_stored_info_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_stored_info_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_stored_info_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_stored_info_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_stored_info_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsStoredInfoTypesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_stored_info_types_list_builder()` + `dlp_organizations_stored_info_types_list_execute()`.
/// For task-level control, use `dlp_organizations_stored_info_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_list(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsStoredInfoTypesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_stored_info_types_list_builder(
        client,
        &args.parent,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_organizations_stored_info_types_list_execute(builder)
}

/// PATCH v2/organizations/{organizationsId}/storedInfoTypes/{storedInfoTypesId}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_organizations_stored_info_types_patch_execute()` to send, or `dlp_organizations_stored_info_types_patch` for simplest API.

pub fn dlp_organizations_stored_info_types_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{}/storedInfoTypes/{storedInfoTypesId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/organizations/{organizationsId}/storedInfoTypes/{storedInfoTypesId}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_organizations_stored_info_types_patch_execute()` or `dlp_organizations_stored_info_types_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_stored_info_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/organizations/{organizationsId}/storedInfoTypes/{storedInfoTypesId}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_organizations_stored_info_types_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_organizations_stored_info_types_patch_task()`.
/// For the simplest API, use `dlp_organizations_stored_info_types_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_organizations_stored_info_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_organizations_stored_info_types_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_organizations_stored_info_types_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_organizations_stored_info_types_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsStoredInfoTypesPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/organizations/{organizationsId}/storedInfoTypes/{storedInfoTypesId}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_organizations_stored_info_types_patch_builder()` + `dlp_organizations_stored_info_types_patch_execute()`.
/// For task-level control, use `dlp_organizations_stored_info_types_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_organizations_stored_info_types_patch(
    client: &SimpleHttpClient,
    args: &DlpOrganizationsStoredInfoTypesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_organizations_stored_info_types_patch_builder(client, &args.name)?;
    dlp_organizations_stored_info_types_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/content:deidentify
/// De-identifies potentially sensitive info from a ContentItem. This method has limits on input size and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/deidentify-sensitive-data`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_content_deidentify_execute()` to send, or `dlp_projects_content_deidentify` for simplest API.

pub fn dlp_projects_content_deidentify_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/content:deidentify",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/content:deidentify
/// De-identifies potentially sensitive info from a ContentItem. This method has limits on input size and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/deidentify-sensitive-data`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_content_deidentify_execute()` or `dlp_projects_content_deidentify`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_content_deidentify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_content_deidentify_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyContentResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/content:deidentify
/// De-identifies potentially sensitive info from a ContentItem. This method has limits on input size and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/deidentify-sensitive-data`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_content_deidentify_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_content_deidentify_task()`.
/// For the simplest API, use `dlp_projects_content_deidentify()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_content_deidentify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_content_deidentify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_content_deidentify_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_content_deidentify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsContentDeidentifyArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/content:deidentify
/// De-identifies potentially sensitive info from a ContentItem. This method has limits on input size and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/deidentify-sensitive-data`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_content_deidentify_builder()` + `dlp_projects_content_deidentify_execute()`.
/// For task-level control, use `dlp_projects_content_deidentify_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_content_deidentify(
    client: &SimpleHttpClient,
    args: &DlpProjectsContentDeidentifyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_content_deidentify_builder(client, &args.parent)?;
    dlp_projects_content_deidentify_execute(builder)
}

/// POST v2/projects/{projectsId}/content:inspect
/// Finds potentially sensitive info in content. This method has limits on input size, processing time, and output size. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. For how to guides, see <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-images`> and <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-text`,>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_content_inspect_execute()` to send, or `dlp_projects_content_inspect` for simplest API.

pub fn dlp_projects_content_inspect_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/content:inspect",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/content:inspect
/// Finds potentially sensitive info in content. This method has limits on input size, processing time, and output size. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. For how to guides, see <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-images`> and <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-text`,>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_content_inspect_execute()` or `dlp_projects_content_inspect`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_content_inspect_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_content_inspect_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2InspectContentResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectContentResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/content:inspect
/// Finds potentially sensitive info in content. This method has limits on input size, processing time, and output size. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. For how to guides, see <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-images`> and <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-text`,>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_content_inspect_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_content_inspect_task()`.
/// For the simplest API, use `dlp_projects_content_inspect()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_content_inspect_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_content_inspect_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_content_inspect_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_content_inspect`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsContentInspectArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/content:inspect
/// Finds potentially sensitive info in content. This method has limits on input size, processing time, and output size. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. For how to guides, see <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-images`> and <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-text`,>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_content_inspect_builder()` + `dlp_projects_content_inspect_execute()`.
/// For task-level control, use `dlp_projects_content_inspect_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_content_inspect(
    client: &SimpleHttpClient,
    args: &DlpProjectsContentInspectArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_content_inspect_builder(client, &args.parent)?;
    dlp_projects_content_inspect_execute(builder)
}

/// POST v2/projects/{projectsId}/content:reidentify
/// Re-identifies content that has been de-identified. See <https://cloud.google.`com/sensitive-data-protection/docs/pseudonymization`#re-identification_in_free_text_code_example> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_content_reidentify_execute()` to send, or `dlp_projects_content_reidentify` for simplest API.

pub fn dlp_projects_content_reidentify_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/content:reidentify",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/content:reidentify
/// Re-identifies content that has been de-identified. See <https://cloud.google.`com/sensitive-data-protection/docs/pseudonymization`#re-identification_in_free_text_code_example> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_content_reidentify_execute()` or `dlp_projects_content_reidentify`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_content_reidentify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_content_reidentify_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ReidentifyContentResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ReidentifyContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/content:reidentify
/// Re-identifies content that has been de-identified. See <https://cloud.google.`com/sensitive-data-protection/docs/pseudonymization`#re-identification_in_free_text_code_example> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_content_reidentify_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_content_reidentify_task()`.
/// For the simplest API, use `dlp_projects_content_reidentify()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_content_reidentify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_content_reidentify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ReidentifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_content_reidentify_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_content_reidentify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsContentReidentifyArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/content:reidentify
/// Re-identifies content that has been de-identified. See <https://cloud.google.`com/sensitive-data-protection/docs/pseudonymization`#re-identification_in_free_text_code_example> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_content_reidentify_builder()` + `dlp_projects_content_reidentify_execute()`.
/// For task-level control, use `dlp_projects_content_reidentify_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_content_reidentify(
    client: &SimpleHttpClient,
    args: &DlpProjectsContentReidentifyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ReidentifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_content_reidentify_builder(client, &args.parent)?;
    dlp_projects_content_reidentify_execute(builder)
}

/// POST v2/projects/{projectsId}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_deidentify_templates_create_execute()` to send, or `dlp_projects_deidentify_templates_create` for simplest API.

pub fn dlp_projects_deidentify_templates_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/deidentifyTemplates",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_deidentify_templates_create_execute()` or `dlp_projects_deidentify_templates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_deidentify_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_deidentify_templates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_deidentify_templates_create_task()`.
/// For the simplest API, use `dlp_projects_deidentify_templates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_deidentify_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_deidentify_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_deidentify_templates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_deidentify_templates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsDeidentifyTemplatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_deidentify_templates_create_builder()` + `dlp_projects_deidentify_templates_create_execute()`.
/// For task-level control, use `dlp_projects_deidentify_templates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_create(
    client: &SimpleHttpClient,
    args: &DlpProjectsDeidentifyTemplatesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_deidentify_templates_create_builder(client, &args.parent)?;
    dlp_projects_deidentify_templates_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_deidentify_templates_delete_execute()` to send, or `dlp_projects_deidentify_templates_delete` for simplest API.

pub fn dlp_projects_deidentify_templates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/deidentifyTemplates/{deidentifyTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_deidentify_templates_delete_execute()` or `dlp_projects_deidentify_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_deidentify_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_deidentify_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_deidentify_templates_delete_task()`.
/// For the simplest API, use `dlp_projects_deidentify_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_deidentify_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_deidentify_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_deidentify_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_deidentify_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsDeidentifyTemplatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_deidentify_templates_delete_builder()` + `dlp_projects_deidentify_templates_delete_execute()`.
/// For task-level control, use `dlp_projects_deidentify_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_delete(
    client: &SimpleHttpClient,
    args: &DlpProjectsDeidentifyTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_deidentify_templates_delete_builder(client, &args.name)?;
    dlp_projects_deidentify_templates_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_deidentify_templates_get_execute()` to send, or `dlp_projects_deidentify_templates_get` for simplest API.

pub fn dlp_projects_deidentify_templates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/deidentifyTemplates/{deidentifyTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_deidentify_templates_get_execute()` or `dlp_projects_deidentify_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_deidentify_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_deidentify_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_deidentify_templates_get_task()`.
/// For the simplest API, use `dlp_projects_deidentify_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_deidentify_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_deidentify_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_deidentify_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_deidentify_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsDeidentifyTemplatesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_deidentify_templates_get_builder()` + `dlp_projects_deidentify_templates_get_execute()`.
/// For task-level control, use `dlp_projects_deidentify_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_get(
    client: &SimpleHttpClient,
    args: &DlpProjectsDeidentifyTemplatesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_deidentify_templates_get_builder(client, &args.name)?;
    dlp_projects_deidentify_templates_get_execute(builder)
}

/// GET v2/projects/{projectsId}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_deidentify_templates_list_execute()` to send, or `dlp_projects_deidentify_templates_list` for simplest API.

pub fn dlp_projects_deidentify_templates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/deidentifyTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_deidentify_templates_list_execute()` or `dlp_projects_deidentify_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_deidentify_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDeidentifyTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_deidentify_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_deidentify_templates_list_task()`.
/// For the simplest API, use `dlp_projects_deidentify_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_deidentify_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_deidentify_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_deidentify_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_deidentify_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsDeidentifyTemplatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_deidentify_templates_list_builder()` + `dlp_projects_deidentify_templates_list_execute()`.
/// For task-level control, use `dlp_projects_deidentify_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_list(
    client: &SimpleHttpClient,
    args: &DlpProjectsDeidentifyTemplatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_deidentify_templates_list_builder(
        client,
        &args.parent,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_projects_deidentify_templates_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_deidentify_templates_patch_execute()` to send, or `dlp_projects_deidentify_templates_patch` for simplest API.

pub fn dlp_projects_deidentify_templates_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/deidentifyTemplates/{deidentifyTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_deidentify_templates_patch_execute()` or `dlp_projects_deidentify_templates_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_deidentify_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_deidentify_templates_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_deidentify_templates_patch_task()`.
/// For the simplest API, use `dlp_projects_deidentify_templates_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_deidentify_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_deidentify_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_deidentify_templates_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_deidentify_templates_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsDeidentifyTemplatesPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/projects/{projectsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_deidentify_templates_patch_builder()` + `dlp_projects_deidentify_templates_patch_execute()`.
/// For task-level control, use `dlp_projects_deidentify_templates_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_deidentify_templates_patch(
    client: &SimpleHttpClient,
    args: &DlpProjectsDeidentifyTemplatesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_deidentify_templates_patch_builder(client, &args.name)?;
    dlp_projects_deidentify_templates_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/dlpJobs/{dlpJobsId}:cancel
/// Starts asynchronous cancellation on a long-running DlpJob. The server makes a best effort to cancel the DlpJob, but success is not guaranteed. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_dlp_jobs_cancel_execute()` to send, or `dlp_projects_dlp_jobs_cancel` for simplest API.

pub fn dlp_projects_dlp_jobs_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/dlpJobs/{dlpJobsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/dlpJobs/{dlpJobsId}:cancel
/// Starts asynchronous cancellation on a long-running DlpJob. The server makes a best effort to cancel the DlpJob, but success is not guaranteed. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_dlp_jobs_cancel_execute()` or `dlp_projects_dlp_jobs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_dlp_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/dlpJobs/{dlpJobsId}:cancel
/// Starts asynchronous cancellation on a long-running DlpJob. The server makes a best effort to cancel the DlpJob, but success is not guaranteed. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_dlp_jobs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_dlp_jobs_cancel_task()`.
/// For the simplest API, use `dlp_projects_dlp_jobs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_dlp_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_dlp_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_dlp_jobs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_dlp_jobs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsDlpJobsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/dlpJobs/{dlpJobsId}:cancel
/// Starts asynchronous cancellation on a long-running DlpJob. The server makes a best effort to cancel the DlpJob, but success is not guaranteed. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_dlp_jobs_cancel_builder()` + `dlp_projects_dlp_jobs_cancel_execute()`.
/// For task-level control, use `dlp_projects_dlp_jobs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_cancel(
    client: &SimpleHttpClient,
    args: &DlpProjectsDlpJobsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_dlp_jobs_cancel_builder(client, &args.name)?;
    dlp_projects_dlp_jobs_cancel_execute(builder)
}

/// POST v2/projects/{projectsId}/dlpJobs
/// Creates a new job to inspect storage or calculate risk metrics. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more. When no InfoTypes or CustomInfoTypes are specified in inspect jobs, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_dlp_jobs_create_execute()` to send, or `dlp_projects_dlp_jobs_create` for simplest API.

pub fn dlp_projects_dlp_jobs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://dlp.googleapis.com/v2/projects/{}/dlpJobs", parent,);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/dlpJobs
/// Creates a new job to inspect storage or calculate risk metrics. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more. When no InfoTypes or CustomInfoTypes are specified in inspect jobs, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_dlp_jobs_create_execute()` or `dlp_projects_dlp_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_dlp_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DlpJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/dlpJobs
/// Creates a new job to inspect storage or calculate risk metrics. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more. When no InfoTypes or CustomInfoTypes are specified in inspect jobs, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_dlp_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_dlp_jobs_create_task()`.
/// For the simplest API, use `dlp_projects_dlp_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_dlp_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_dlp_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_dlp_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_dlp_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsDlpJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/dlpJobs
/// Creates a new job to inspect storage or calculate risk metrics. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more. When no InfoTypes or CustomInfoTypes are specified in inspect jobs, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_dlp_jobs_create_builder()` + `dlp_projects_dlp_jobs_create_execute()`.
/// For task-level control, use `dlp_projects_dlp_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_create(
    client: &SimpleHttpClient,
    args: &DlpProjectsDlpJobsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_dlp_jobs_create_builder(client, &args.parent)?;
    dlp_projects_dlp_jobs_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/dlpJobs/{dlpJobsId}
/// Deletes a long-running DlpJob. This method indicates that the client is no longer interested in the DlpJob result. The job will be canceled if possible. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_dlp_jobs_delete_execute()` to send, or `dlp_projects_dlp_jobs_delete` for simplest API.

pub fn dlp_projects_dlp_jobs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/dlpJobs/{dlpJobsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/dlpJobs/{dlpJobsId}
/// Deletes a long-running DlpJob. This method indicates that the client is no longer interested in the DlpJob result. The job will be canceled if possible. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_dlp_jobs_delete_execute()` or `dlp_projects_dlp_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_dlp_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/dlpJobs/{dlpJobsId}
/// Deletes a long-running DlpJob. This method indicates that the client is no longer interested in the DlpJob result. The job will be canceled if possible. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_dlp_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_dlp_jobs_delete_task()`.
/// For the simplest API, use `dlp_projects_dlp_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_dlp_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_dlp_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_dlp_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_dlp_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsDlpJobsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/dlpJobs/{dlpJobsId}
/// Deletes a long-running DlpJob. This method indicates that the client is no longer interested in the DlpJob result. The job will be canceled if possible. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_dlp_jobs_delete_builder()` + `dlp_projects_dlp_jobs_delete_execute()`.
/// For task-level control, use `dlp_projects_dlp_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_delete(
    client: &SimpleHttpClient,
    args: &DlpProjectsDlpJobsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_dlp_jobs_delete_builder(client, &args.name)?;
    dlp_projects_dlp_jobs_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/dlpJobs/{dlpJobsId}
/// Gets the latest state of a long-running DlpJob. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_dlp_jobs_get_execute()` to send, or `dlp_projects_dlp_jobs_get` for simplest API.

pub fn dlp_projects_dlp_jobs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/dlpJobs/{dlpJobsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/dlpJobs/{dlpJobsId}
/// Gets the latest state of a long-running DlpJob. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_dlp_jobs_get_execute()` or `dlp_projects_dlp_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_dlp_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DlpJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/dlpJobs/{dlpJobsId}
/// Gets the latest state of a long-running DlpJob. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_dlp_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_dlp_jobs_get_task()`.
/// For the simplest API, use `dlp_projects_dlp_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_dlp_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_dlp_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_dlp_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_dlp_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsDlpJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/dlpJobs/{dlpJobsId}
/// Gets the latest state of a long-running DlpJob. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_dlp_jobs_get_builder()` + `dlp_projects_dlp_jobs_get_execute()`.
/// For task-level control, use `dlp_projects_dlp_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_get(
    client: &SimpleHttpClient,
    args: &DlpProjectsDlpJobsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_dlp_jobs_get_builder(client, &args.name)?;
    dlp_projects_dlp_jobs_get_execute(builder)
}

/// GET v2/projects/{projectsId}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_dlp_jobs_list_execute()` to send, or `dlp_projects_dlp_jobs_list` for simplest API.

pub fn dlp_projects_dlp_jobs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    type_rs: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://dlp.googleapis.com/v2/projects/{}/dlpJobs", parent,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = type_rs.as_ref() {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_dlp_jobs_list_execute()` or `dlp_projects_dlp_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_dlp_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListDlpJobsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDlpJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_dlp_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_dlp_jobs_list_task()`.
/// For the simplest API, use `dlp_projects_dlp_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_dlp_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_dlp_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDlpJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_dlp_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_dlp_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsDlpJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET v2/projects/{projectsId}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_dlp_jobs_list_builder()` + `dlp_projects_dlp_jobs_list_execute()`.
/// For task-level control, use `dlp_projects_dlp_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_dlp_jobs_list(
    client: &SimpleHttpClient,
    args: &DlpProjectsDlpJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDlpJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_dlp_jobs_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.type_rs,
    )?;
    dlp_projects_dlp_jobs_list_execute(builder)
}

/// POST v2/projects/{projectsId}/image:redact
/// Redacts potentially sensitive info from an image. This method has limits on input size, processing time, and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/redacting-sensitive-data-images`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. Only the first frame of each multiframe image is redacted. Metadata and other frames are omitted in the response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_image_redact_execute()` to send, or `dlp_projects_image_redact` for simplest API.

pub fn dlp_projects_image_redact_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/image:redact",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/image:redact
/// Redacts potentially sensitive info from an image. This method has limits on input size, processing time, and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/redacting-sensitive-data-images`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. Only the first frame of each multiframe image is redacted. Metadata and other frames are omitted in the response.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_image_redact_execute()` or `dlp_projects_image_redact`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_image_redact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_image_redact_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2RedactImageResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2RedactImageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/image:redact
/// Redacts potentially sensitive info from an image. This method has limits on input size, processing time, and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/redacting-sensitive-data-images`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. Only the first frame of each multiframe image is redacted. Metadata and other frames are omitted in the response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_image_redact_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_image_redact_task()`.
/// For the simplest API, use `dlp_projects_image_redact()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_image_redact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_image_redact_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2RedactImageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_image_redact_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_image_redact`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsImageRedactArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/image:redact
/// Redacts potentially sensitive info from an image. This method has limits on input size, processing time, and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/redacting-sensitive-data-images`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. Only the first frame of each multiframe image is redacted. Metadata and other frames are omitted in the response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_image_redact_builder()` + `dlp_projects_image_redact_execute()`.
/// For task-level control, use `dlp_projects_image_redact_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_image_redact(
    client: &SimpleHttpClient,
    args: &DlpProjectsImageRedactArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2RedactImageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_image_redact_builder(client, &args.parent)?;
    dlp_projects_image_redact_execute(builder)
}

/// POST v2/projects/{projectsId}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_inspect_templates_create_execute()` to send, or `dlp_projects_inspect_templates_create` for simplest API.

pub fn dlp_projects_inspect_templates_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/inspectTemplates",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_inspect_templates_create_execute()` or `dlp_projects_inspect_templates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_inspect_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_inspect_templates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_inspect_templates_create_task()`.
/// For the simplest API, use `dlp_projects_inspect_templates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_inspect_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_inspect_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_inspect_templates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_inspect_templates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsInspectTemplatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_inspect_templates_create_builder()` + `dlp_projects_inspect_templates_create_execute()`.
/// For task-level control, use `dlp_projects_inspect_templates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_create(
    client: &SimpleHttpClient,
    args: &DlpProjectsInspectTemplatesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_inspect_templates_create_builder(client, &args.parent)?;
    dlp_projects_inspect_templates_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/inspectTemplates/{inspectTemplatesId}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_inspect_templates_delete_execute()` to send, or `dlp_projects_inspect_templates_delete` for simplest API.

pub fn dlp_projects_inspect_templates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/inspectTemplates/{inspectTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/inspectTemplates/{inspectTemplatesId}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_inspect_templates_delete_execute()` or `dlp_projects_inspect_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_inspect_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/inspectTemplates/{inspectTemplatesId}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_inspect_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_inspect_templates_delete_task()`.
/// For the simplest API, use `dlp_projects_inspect_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_inspect_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_inspect_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_inspect_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_inspect_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsInspectTemplatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/inspectTemplates/{inspectTemplatesId}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_inspect_templates_delete_builder()` + `dlp_projects_inspect_templates_delete_execute()`.
/// For task-level control, use `dlp_projects_inspect_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_delete(
    client: &SimpleHttpClient,
    args: &DlpProjectsInspectTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_inspect_templates_delete_builder(client, &args.name)?;
    dlp_projects_inspect_templates_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/inspectTemplates/{inspectTemplatesId}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_inspect_templates_get_execute()` to send, or `dlp_projects_inspect_templates_get` for simplest API.

pub fn dlp_projects_inspect_templates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/inspectTemplates/{inspectTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/inspectTemplates/{inspectTemplatesId}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_inspect_templates_get_execute()` or `dlp_projects_inspect_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_inspect_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/inspectTemplates/{inspectTemplatesId}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_inspect_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_inspect_templates_get_task()`.
/// For the simplest API, use `dlp_projects_inspect_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_inspect_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_inspect_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_inspect_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_inspect_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsInspectTemplatesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/inspectTemplates/{inspectTemplatesId}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_inspect_templates_get_builder()` + `dlp_projects_inspect_templates_get_execute()`.
/// For task-level control, use `dlp_projects_inspect_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_get(
    client: &SimpleHttpClient,
    args: &DlpProjectsInspectTemplatesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_inspect_templates_get_builder(client, &args.name)?;
    dlp_projects_inspect_templates_get_execute(builder)
}

/// GET v2/projects/{projectsId}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_inspect_templates_list_execute()` to send, or `dlp_projects_inspect_templates_list` for simplest API.

pub fn dlp_projects_inspect_templates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/inspectTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_inspect_templates_list_execute()` or `dlp_projects_inspect_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_inspect_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInspectTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_inspect_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_inspect_templates_list_task()`.
/// For the simplest API, use `dlp_projects_inspect_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_inspect_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_inspect_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_inspect_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_inspect_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsInspectTemplatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_inspect_templates_list_builder()` + `dlp_projects_inspect_templates_list_execute()`.
/// For task-level control, use `dlp_projects_inspect_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_list(
    client: &SimpleHttpClient,
    args: &DlpProjectsInspectTemplatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_inspect_templates_list_builder(
        client,
        &args.parent,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_projects_inspect_templates_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/inspectTemplates/{inspectTemplatesId}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_inspect_templates_patch_execute()` to send, or `dlp_projects_inspect_templates_patch` for simplest API.

pub fn dlp_projects_inspect_templates_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/inspectTemplates/{inspectTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/inspectTemplates/{inspectTemplatesId}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_inspect_templates_patch_execute()` or `dlp_projects_inspect_templates_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_inspect_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/inspectTemplates/{inspectTemplatesId}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_inspect_templates_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_inspect_templates_patch_task()`.
/// For the simplest API, use `dlp_projects_inspect_templates_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_inspect_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_inspect_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_inspect_templates_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_inspect_templates_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsInspectTemplatesPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/projects/{projectsId}/inspectTemplates/{inspectTemplatesId}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_inspect_templates_patch_builder()` + `dlp_projects_inspect_templates_patch_execute()`.
/// For task-level control, use `dlp_projects_inspect_templates_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_inspect_templates_patch(
    client: &SimpleHttpClient,
    args: &DlpProjectsInspectTemplatesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_inspect_templates_patch_builder(client, &args.name)?;
    dlp_projects_inspect_templates_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/jobTriggers/{jobTriggersId}:activate
/// Activate a job trigger. Causes the immediate execute of a trigger instead of waiting on the trigger event to occur.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_job_triggers_activate_execute()` to send, or `dlp_projects_job_triggers_activate` for simplest API.

pub fn dlp_projects_job_triggers_activate_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/jobTriggers/{jobTriggersId}:activate",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/jobTriggers/{jobTriggersId}:activate
/// Activate a job trigger. Causes the immediate execute of a trigger instead of waiting on the trigger event to occur.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_job_triggers_activate_execute()` or `dlp_projects_job_triggers_activate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_job_triggers_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_activate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DlpJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/jobTriggers/{jobTriggersId}:activate
/// Activate a job trigger. Causes the immediate execute of a trigger instead of waiting on the trigger event to occur.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_job_triggers_activate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_job_triggers_activate_task()`.
/// For the simplest API, use `dlp_projects_job_triggers_activate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_job_triggers_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_job_triggers_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_job_triggers_activate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_job_triggers_activate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsJobTriggersActivateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/jobTriggers/{jobTriggersId}:activate
/// Activate a job trigger. Causes the immediate execute of a trigger instead of waiting on the trigger event to occur.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_job_triggers_activate_builder()` + `dlp_projects_job_triggers_activate_execute()`.
/// For task-level control, use `dlp_projects_job_triggers_activate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_activate(
    client: &SimpleHttpClient,
    args: &DlpProjectsJobTriggersActivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_job_triggers_activate_builder(client, &args.name)?;
    dlp_projects_job_triggers_activate_execute(builder)
}

/// POST v2/projects/{projectsId}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_job_triggers_create_execute()` to send, or `dlp_projects_job_triggers_create` for simplest API.

pub fn dlp_projects_job_triggers_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/jobTriggers",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_job_triggers_create_execute()` or `dlp_projects_job_triggers_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_job_triggers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_job_triggers_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_job_triggers_create_task()`.
/// For the simplest API, use `dlp_projects_job_triggers_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_job_triggers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_job_triggers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_job_triggers_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_job_triggers_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsJobTriggersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_job_triggers_create_builder()` + `dlp_projects_job_triggers_create_execute()`.
/// For task-level control, use `dlp_projects_job_triggers_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_create(
    client: &SimpleHttpClient,
    args: &DlpProjectsJobTriggersCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_job_triggers_create_builder(client, &args.parent)?;
    dlp_projects_job_triggers_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/jobTriggers/{jobTriggersId}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_job_triggers_delete_execute()` to send, or `dlp_projects_job_triggers_delete` for simplest API.

pub fn dlp_projects_job_triggers_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/jobTriggers/{jobTriggersId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/jobTriggers/{jobTriggersId}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_job_triggers_delete_execute()` or `dlp_projects_job_triggers_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_job_triggers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/jobTriggers/{jobTriggersId}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_job_triggers_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_job_triggers_delete_task()`.
/// For the simplest API, use `dlp_projects_job_triggers_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_job_triggers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_job_triggers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_job_triggers_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_job_triggers_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsJobTriggersDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/jobTriggers/{jobTriggersId}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_job_triggers_delete_builder()` + `dlp_projects_job_triggers_delete_execute()`.
/// For task-level control, use `dlp_projects_job_triggers_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_delete(
    client: &SimpleHttpClient,
    args: &DlpProjectsJobTriggersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_job_triggers_delete_builder(client, &args.name)?;
    dlp_projects_job_triggers_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/jobTriggers/{jobTriggersId}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_job_triggers_get_execute()` to send, or `dlp_projects_job_triggers_get` for simplest API.

pub fn dlp_projects_job_triggers_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/jobTriggers/{jobTriggersId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/jobTriggers/{jobTriggersId}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_job_triggers_get_execute()` or `dlp_projects_job_triggers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_job_triggers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/jobTriggers/{jobTriggersId}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_job_triggers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_job_triggers_get_task()`.
/// For the simplest API, use `dlp_projects_job_triggers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_job_triggers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_job_triggers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_job_triggers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_job_triggers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsJobTriggersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/jobTriggers/{jobTriggersId}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_job_triggers_get_builder()` + `dlp_projects_job_triggers_get_execute()`.
/// For task-level control, use `dlp_projects_job_triggers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_get(
    client: &SimpleHttpClient,
    args: &DlpProjectsJobTriggersGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_job_triggers_get_builder(client, &args.name)?;
    dlp_projects_job_triggers_get_execute(builder)
}

/// GET v2/projects/{projectsId}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_job_triggers_list_execute()` to send, or `dlp_projects_job_triggers_list` for simplest API.

pub fn dlp_projects_job_triggers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    type_rs: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/jobTriggers",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = type_rs.as_ref() {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_job_triggers_list_execute()` or `dlp_projects_job_triggers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_job_triggers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListJobTriggersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListJobTriggersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_job_triggers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_job_triggers_list_task()`.
/// For the simplest API, use `dlp_projects_job_triggers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_job_triggers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_job_triggers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListJobTriggersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_job_triggers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_job_triggers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsJobTriggersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET v2/projects/{projectsId}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_job_triggers_list_builder()` + `dlp_projects_job_triggers_list_execute()`.
/// For task-level control, use `dlp_projects_job_triggers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_list(
    client: &SimpleHttpClient,
    args: &DlpProjectsJobTriggersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListJobTriggersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_job_triggers_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.type_rs,
    )?;
    dlp_projects_job_triggers_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/jobTriggers/{jobTriggersId}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_job_triggers_patch_execute()` to send, or `dlp_projects_job_triggers_patch` for simplest API.

pub fn dlp_projects_job_triggers_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/jobTriggers/{jobTriggersId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/jobTriggers/{jobTriggersId}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_job_triggers_patch_execute()` or `dlp_projects_job_triggers_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_job_triggers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/jobTriggers/{jobTriggersId}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_job_triggers_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_job_triggers_patch_task()`.
/// For the simplest API, use `dlp_projects_job_triggers_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_job_triggers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_job_triggers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_job_triggers_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_job_triggers_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsJobTriggersPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/projects/{projectsId}/jobTriggers/{jobTriggersId}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_job_triggers_patch_builder()` + `dlp_projects_job_triggers_patch_execute()`.
/// For task-level control, use `dlp_projects_job_triggers_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_job_triggers_patch(
    client: &SimpleHttpClient,
    args: &DlpProjectsJobTriggersPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_job_triggers_patch_builder(client, &args.name)?;
    dlp_projects_job_triggers_patch_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/columnDataProfiles/{columnDataProfilesId}
/// Gets a column data profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_column_data_profiles_get_execute()` to send, or `dlp_projects_locations_column_data_profiles_get` for simplest API.

pub fn dlp_projects_locations_column_data_profiles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/columnDataProfiles/{columnDataProfilesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/columnDataProfiles/{columnDataProfilesId}
/// Gets a column data profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_column_data_profiles_get_execute()` or `dlp_projects_locations_column_data_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_column_data_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_column_data_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ColumnDataProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ColumnDataProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/columnDataProfiles/{columnDataProfilesId}
/// Gets a column data profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_column_data_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_column_data_profiles_get_task()`.
/// For the simplest API, use `dlp_projects_locations_column_data_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_column_data_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_column_data_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ColumnDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_column_data_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_column_data_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsColumnDataProfilesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/columnDataProfiles/{columnDataProfilesId}
/// Gets a column data profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_column_data_profiles_get_builder()` + `dlp_projects_locations_column_data_profiles_get_execute()`.
/// For task-level control, use `dlp_projects_locations_column_data_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_column_data_profiles_get(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsColumnDataProfilesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ColumnDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_column_data_profiles_get_builder(client, &args.name)?;
    dlp_projects_locations_column_data_profiles_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/columnDataProfiles
/// Lists column data profiles for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_column_data_profiles_list_execute()` to send, or `dlp_projects_locations_column_data_profiles_list` for simplest API.

pub fn dlp_projects_locations_column_data_profiles_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/columnDataProfiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/columnDataProfiles
/// Lists column data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_column_data_profiles_list_execute()` or `dlp_projects_locations_column_data_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_column_data_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_column_data_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListColumnDataProfilesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListColumnDataProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/columnDataProfiles
/// Lists column data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_column_data_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_column_data_profiles_list_task()`.
/// For the simplest API, use `dlp_projects_locations_column_data_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_column_data_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_column_data_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListColumnDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_column_data_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_column_data_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsColumnDataProfilesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/columnDataProfiles
/// Lists column data profiles for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_column_data_profiles_list_builder()` + `dlp_projects_locations_column_data_profiles_list_execute()`.
/// For task-level control, use `dlp_projects_locations_column_data_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_column_data_profiles_list(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsColumnDataProfilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListColumnDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_column_data_profiles_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_projects_locations_column_data_profiles_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/connections
/// Create a Connection to an external data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_connections_create_execute()` to send, or `dlp_projects_locations_connections_create` for simplest API.

pub fn dlp_projects_locations_connections_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/connections",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/connections
/// Create a Connection to an external data source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_connections_create_execute()` or `dlp_projects_locations_connections_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_connections_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2Connection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/connections
/// Create a Connection to an external data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_connections_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_connections_create_task()`.
/// For the simplest API, use `dlp_projects_locations_connections_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_connections_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_connections_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_connections_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_connections_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsConnectionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/connections
/// Create a Connection to an external data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_connections_create_builder()` + `dlp_projects_locations_connections_create_execute()`.
/// For task-level control, use `dlp_projects_locations_connections_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_create(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsConnectionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_connections_create_builder(client, &args.parent)?;
    dlp_projects_locations_connections_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Delete a Connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_connections_delete_execute()` to send, or `dlp_projects_locations_connections_delete` for simplest API.

pub fn dlp_projects_locations_connections_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/connections/{connectionsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Delete a Connection.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_connections_delete_execute()` or `dlp_projects_locations_connections_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_connections_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Delete a Connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_connections_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_connections_delete_task()`.
/// For the simplest API, use `dlp_projects_locations_connections_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_connections_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_connections_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_connections_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_connections_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsConnectionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Delete a Connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_connections_delete_builder()` + `dlp_projects_locations_connections_delete_execute()`.
/// For task-level control, use `dlp_projects_locations_connections_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_delete(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsConnectionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_connections_delete_builder(client, &args.name)?;
    dlp_projects_locations_connections_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Get a Connection by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_connections_get_execute()` to send, or `dlp_projects_locations_connections_get` for simplest API.

pub fn dlp_projects_locations_connections_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/connections/{connectionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Get a Connection by name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_connections_get_execute()` or `dlp_projects_locations_connections_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_connections_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2Connection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Get a Connection by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_connections_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_connections_get_task()`.
/// For the simplest API, use `dlp_projects_locations_connections_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_connections_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_connections_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_connections_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_connections_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsConnectionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Get a Connection by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_connections_get_builder()` + `dlp_projects_locations_connections_get_execute()`.
/// For task-level control, use `dlp_projects_locations_connections_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_get(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsConnectionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_connections_get_builder(client, &args.name)?;
    dlp_projects_locations_connections_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections
/// Lists Connections in a parent. Use SearchConnections to see all connections within an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_connections_list_execute()` to send, or `dlp_projects_locations_connections_list` for simplest API.

pub fn dlp_projects_locations_connections_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/connections",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections
/// Lists Connections in a parent. Use SearchConnections to see all connections within an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_connections_list_execute()` or `dlp_projects_locations_connections_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_connections_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListConnectionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListConnectionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections
/// Lists Connections in a parent. Use SearchConnections to see all connections within an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_connections_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_connections_list_task()`.
/// For the simplest API, use `dlp_projects_locations_connections_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_connections_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_connections_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_connections_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_connections_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsConnectionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections
/// Lists Connections in a parent. Use SearchConnections to see all connections within an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_connections_list_builder()` + `dlp_projects_locations_connections_list_execute()`.
/// For task-level control, use `dlp_projects_locations_connections_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_list(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsConnectionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_connections_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_projects_locations_connections_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Update a Connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_connections_patch_execute()` to send, or `dlp_projects_locations_connections_patch` for simplest API.

pub fn dlp_projects_locations_connections_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/connections/{connectionsId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Update a Connection.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_connections_patch_execute()` or `dlp_projects_locations_connections_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_connections_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2Connection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Update a Connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_connections_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_connections_patch_task()`.
/// For the simplest API, use `dlp_projects_locations_connections_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_connections_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_connections_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_connections_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_connections_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsConnectionsPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Update a Connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_connections_patch_builder()` + `dlp_projects_locations_connections_patch_execute()`.
/// For task-level control, use `dlp_projects_locations_connections_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_patch(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsConnectionsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2Connection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_connections_patch_builder(client, &args.name)?;
    dlp_projects_locations_connections_patch_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections:search
/// Searches for Connections in a parent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_connections_search_execute()` to send, or `dlp_projects_locations_connections_search` for simplest API.

pub fn dlp_projects_locations_connections_search_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/connections:search",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections:search
/// Searches for Connections in a parent.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_connections_search_execute()` or `dlp_projects_locations_connections_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_connections_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2SearchConnectionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2SearchConnectionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections:search
/// Searches for Connections in a parent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_connections_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_connections_search_task()`.
/// For the simplest API, use `dlp_projects_locations_connections_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_connections_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_connections_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2SearchConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_connections_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_connections_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsConnectionsSearchArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/connections:search
/// Searches for Connections in a parent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_connections_search_builder()` + `dlp_projects_locations_connections_search_execute()`.
/// For task-level control, use `dlp_projects_locations_connections_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_connections_search(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsConnectionsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2SearchConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_connections_search_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_projects_locations_connections_search_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/content:deidentify
/// De-identifies potentially sensitive info from a ContentItem. This method has limits on input size and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/deidentify-sensitive-data`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_content_deidentify_execute()` to send, or `dlp_projects_locations_content_deidentify` for simplest API.

pub fn dlp_projects_locations_content_deidentify_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/content:deidentify",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/content:deidentify
/// De-identifies potentially sensitive info from a ContentItem. This method has limits on input size and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/deidentify-sensitive-data`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_content_deidentify_execute()` or `dlp_projects_locations_content_deidentify`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_content_deidentify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_content_deidentify_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyContentResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/content:deidentify
/// De-identifies potentially sensitive info from a ContentItem. This method has limits on input size and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/deidentify-sensitive-data`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_content_deidentify_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_content_deidentify_task()`.
/// For the simplest API, use `dlp_projects_locations_content_deidentify()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_content_deidentify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_content_deidentify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_content_deidentify_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_content_deidentify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsContentDeidentifyArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/content:deidentify
/// De-identifies potentially sensitive info from a ContentItem. This method has limits on input size and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/deidentify-sensitive-data`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_content_deidentify_builder()` + `dlp_projects_locations_content_deidentify_execute()`.
/// For task-level control, use `dlp_projects_locations_content_deidentify_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_content_deidentify(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsContentDeidentifyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_content_deidentify_builder(client, &args.parent)?;
    dlp_projects_locations_content_deidentify_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/content:inspect
/// Finds potentially sensitive info in content. This method has limits on input size, processing time, and output size. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. For how to guides, see <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-images`> and <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-text`,>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_content_inspect_execute()` to send, or `dlp_projects_locations_content_inspect` for simplest API.

pub fn dlp_projects_locations_content_inspect_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/content:inspect",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/content:inspect
/// Finds potentially sensitive info in content. This method has limits on input size, processing time, and output size. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. For how to guides, see <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-images`> and <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-text`,>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_content_inspect_execute()` or `dlp_projects_locations_content_inspect`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_content_inspect_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_content_inspect_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2InspectContentResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectContentResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/content:inspect
/// Finds potentially sensitive info in content. This method has limits on input size, processing time, and output size. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. For how to guides, see <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-images`> and <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-text`,>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_content_inspect_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_content_inspect_task()`.
/// For the simplest API, use `dlp_projects_locations_content_inspect()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_content_inspect_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_content_inspect_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_content_inspect_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_content_inspect`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsContentInspectArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/content:inspect
/// Finds potentially sensitive info in content. This method has limits on input size, processing time, and output size. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. For how to guides, see <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-images`> and <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-text`,>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_content_inspect_builder()` + `dlp_projects_locations_content_inspect_execute()`.
/// For task-level control, use `dlp_projects_locations_content_inspect_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_content_inspect(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsContentInspectArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_content_inspect_builder(client, &args.parent)?;
    dlp_projects_locations_content_inspect_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/content:reidentify
/// Re-identifies content that has been de-identified. See <https://cloud.google.`com/sensitive-data-protection/docs/pseudonymization`#re-identification_in_free_text_code_example> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_content_reidentify_execute()` to send, or `dlp_projects_locations_content_reidentify` for simplest API.

pub fn dlp_projects_locations_content_reidentify_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/content:reidentify",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/content:reidentify
/// Re-identifies content that has been de-identified. See <https://cloud.google.`com/sensitive-data-protection/docs/pseudonymization`#re-identification_in_free_text_code_example> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_content_reidentify_execute()` or `dlp_projects_locations_content_reidentify`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_content_reidentify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_content_reidentify_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ReidentifyContentResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ReidentifyContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/content:reidentify
/// Re-identifies content that has been de-identified. See <https://cloud.google.`com/sensitive-data-protection/docs/pseudonymization`#re-identification_in_free_text_code_example> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_content_reidentify_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_content_reidentify_task()`.
/// For the simplest API, use `dlp_projects_locations_content_reidentify()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_content_reidentify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_content_reidentify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ReidentifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_content_reidentify_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_content_reidentify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsContentReidentifyArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/content:reidentify
/// Re-identifies content that has been de-identified. See <https://cloud.google.`com/sensitive-data-protection/docs/pseudonymization`#re-identification_in_free_text_code_example> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_content_reidentify_builder()` + `dlp_projects_locations_content_reidentify_execute()`.
/// For task-level control, use `dlp_projects_locations_content_reidentify_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_content_reidentify(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsContentReidentifyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ReidentifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_content_reidentify_builder(client, &args.parent)?;
    dlp_projects_locations_content_reidentify_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_deidentify_templates_create_execute()` to send, or `dlp_projects_locations_deidentify_templates_create` for simplest API.

pub fn dlp_projects_locations_deidentify_templates_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/deidentifyTemplates",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_deidentify_templates_create_execute()` or `dlp_projects_locations_deidentify_templates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_deidentify_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_deidentify_templates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_deidentify_templates_create_task()`.
/// For the simplest API, use `dlp_projects_locations_deidentify_templates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_deidentify_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_deidentify_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_deidentify_templates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_deidentify_templates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsDeidentifyTemplatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates
/// Creates a DeidentifyTemplate for reusing frequently used configuration for de-identifying content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_deidentify_templates_create_builder()` + `dlp_projects_locations_deidentify_templates_create_execute()`.
/// For task-level control, use `dlp_projects_locations_deidentify_templates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_create(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsDeidentifyTemplatesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_deidentify_templates_create_builder(client, &args.parent)?;
    dlp_projects_locations_deidentify_templates_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_deidentify_templates_delete_execute()` to send, or `dlp_projects_locations_deidentify_templates_delete` for simplest API.

pub fn dlp_projects_locations_deidentify_templates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_deidentify_templates_delete_execute()` or `dlp_projects_locations_deidentify_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_deidentify_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_deidentify_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_deidentify_templates_delete_task()`.
/// For the simplest API, use `dlp_projects_locations_deidentify_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_deidentify_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_deidentify_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_deidentify_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_deidentify_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsDeidentifyTemplatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Deletes a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_deidentify_templates_delete_builder()` + `dlp_projects_locations_deidentify_templates_delete_execute()`.
/// For task-level control, use `dlp_projects_locations_deidentify_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_delete(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsDeidentifyTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_deidentify_templates_delete_builder(client, &args.name)?;
    dlp_projects_locations_deidentify_templates_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_deidentify_templates_get_execute()` to send, or `dlp_projects_locations_deidentify_templates_get` for simplest API.

pub fn dlp_projects_locations_deidentify_templates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_deidentify_templates_get_execute()` or `dlp_projects_locations_deidentify_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_deidentify_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_deidentify_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_deidentify_templates_get_task()`.
/// For the simplest API, use `dlp_projects_locations_deidentify_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_deidentify_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_deidentify_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_deidentify_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_deidentify_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsDeidentifyTemplatesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Gets a DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_deidentify_templates_get_builder()` + `dlp_projects_locations_deidentify_templates_get_execute()`.
/// For task-level control, use `dlp_projects_locations_deidentify_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_get(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsDeidentifyTemplatesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_deidentify_templates_get_builder(client, &args.name)?;
    dlp_projects_locations_deidentify_templates_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_deidentify_templates_list_execute()` to send, or `dlp_projects_locations_deidentify_templates_list` for simplest API.

pub fn dlp_projects_locations_deidentify_templates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/deidentifyTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_deidentify_templates_list_execute()` or `dlp_projects_locations_deidentify_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_deidentify_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDeidentifyTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_deidentify_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_deidentify_templates_list_task()`.
/// For the simplest API, use `dlp_projects_locations_deidentify_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_deidentify_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_deidentify_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_deidentify_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_deidentify_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsDeidentifyTemplatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates
/// Lists DeidentifyTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_deidentify_templates_list_builder()` + `dlp_projects_locations_deidentify_templates_list_execute()`.
/// For task-level control, use `dlp_projects_locations_deidentify_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_list(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsDeidentifyTemplatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDeidentifyTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_deidentify_templates_list_builder(
        client,
        &args.parent,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_projects_locations_deidentify_templates_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_deidentify_templates_patch_execute()` to send, or `dlp_projects_locations_deidentify_templates_patch` for simplest API.

pub fn dlp_projects_locations_deidentify_templates_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_deidentify_templates_patch_execute()` or `dlp_projects_locations_deidentify_templates_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_deidentify_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DeidentifyTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_deidentify_templates_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_deidentify_templates_patch_task()`.
/// For the simplest API, use `dlp_projects_locations_deidentify_templates_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_deidentify_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_deidentify_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_deidentify_templates_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_deidentify_templates_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsDeidentifyTemplatesPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/deidentifyTemplates/{deidentifyTemplatesId}
/// Updates the DeidentifyTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates-deid`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_deidentify_templates_patch_builder()` + `dlp_projects_locations_deidentify_templates_patch_execute()`.
/// For task-level control, use `dlp_projects_locations_deidentify_templates_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_deidentify_templates_patch(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsDeidentifyTemplatesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DeidentifyTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_deidentify_templates_patch_builder(client, &args.name)?;
    dlp_projects_locations_deidentify_templates_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs
/// Creates a config for discovery to scan and profile storage.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_discovery_configs_create_execute()` to send, or `dlp_projects_locations_discovery_configs_create` for simplest API.

pub fn dlp_projects_locations_discovery_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/discoveryConfigs",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs
/// Creates a config for discovery to scan and profile storage.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_discovery_configs_create_execute()` or `dlp_projects_locations_discovery_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_discovery_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DiscoveryConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs
/// Creates a config for discovery to scan and profile storage.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_discovery_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_discovery_configs_create_task()`.
/// For the simplest API, use `dlp_projects_locations_discovery_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_discovery_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_discovery_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_discovery_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_discovery_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsDiscoveryConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs
/// Creates a config for discovery to scan and profile storage.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_discovery_configs_create_builder()` + `dlp_projects_locations_discovery_configs_create_execute()`.
/// For task-level control, use `dlp_projects_locations_discovery_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_create(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsDiscoveryConfigsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_discovery_configs_create_builder(client, &args.parent)?;
    dlp_projects_locations_discovery_configs_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Deletes a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_discovery_configs_delete_execute()` to send, or `dlp_projects_locations_discovery_configs_delete` for simplest API.

pub fn dlp_projects_locations_discovery_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Deletes a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_discovery_configs_delete_execute()` or `dlp_projects_locations_discovery_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_discovery_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Deletes a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_discovery_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_discovery_configs_delete_task()`.
/// For the simplest API, use `dlp_projects_locations_discovery_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_discovery_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_discovery_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_discovery_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_discovery_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsDiscoveryConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Deletes a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_discovery_configs_delete_builder()` + `dlp_projects_locations_discovery_configs_delete_execute()`.
/// For task-level control, use `dlp_projects_locations_discovery_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_delete(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsDiscoveryConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_discovery_configs_delete_builder(client, &args.name)?;
    dlp_projects_locations_discovery_configs_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Gets a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_discovery_configs_get_execute()` to send, or `dlp_projects_locations_discovery_configs_get` for simplest API.

pub fn dlp_projects_locations_discovery_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Gets a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_discovery_configs_get_execute()` or `dlp_projects_locations_discovery_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_discovery_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DiscoveryConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Gets a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_discovery_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_discovery_configs_get_task()`.
/// For the simplest API, use `dlp_projects_locations_discovery_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_discovery_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_discovery_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_discovery_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_discovery_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsDiscoveryConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Gets a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_discovery_configs_get_builder()` + `dlp_projects_locations_discovery_configs_get_execute()`.
/// For task-level control, use `dlp_projects_locations_discovery_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_get(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsDiscoveryConfigsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_discovery_configs_get_builder(client, &args.name)?;
    dlp_projects_locations_discovery_configs_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs
/// Lists discovery configurations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_discovery_configs_list_execute()` to send, or `dlp_projects_locations_discovery_configs_list` for simplest API.

pub fn dlp_projects_locations_discovery_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/discoveryConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs
/// Lists discovery configurations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_discovery_configs_list_execute()` or `dlp_projects_locations_discovery_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_discovery_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListDiscoveryConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDiscoveryConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs
/// Lists discovery configurations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_discovery_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_discovery_configs_list_task()`.
/// For the simplest API, use `dlp_projects_locations_discovery_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_discovery_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_discovery_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDiscoveryConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_discovery_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_discovery_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsDiscoveryConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs
/// Lists discovery configurations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_discovery_configs_list_builder()` + `dlp_projects_locations_discovery_configs_list_execute()`.
/// For task-level control, use `dlp_projects_locations_discovery_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_list(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsDiscoveryConfigsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDiscoveryConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_discovery_configs_list_builder(
        client,
        &args.parent,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_projects_locations_discovery_configs_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Updates a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_discovery_configs_patch_execute()` to send, or `dlp_projects_locations_discovery_configs_patch` for simplest API.

pub fn dlp_projects_locations_discovery_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Updates a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_discovery_configs_patch_execute()` or `dlp_projects_locations_discovery_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_discovery_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DiscoveryConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Updates a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_discovery_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_discovery_configs_patch_task()`.
/// For the simplest API, use `dlp_projects_locations_discovery_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_discovery_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_discovery_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_discovery_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_discovery_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsDiscoveryConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/discoveryConfigs/{discoveryConfigsId}
/// Updates a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_discovery_configs_patch_builder()` + `dlp_projects_locations_discovery_configs_patch_execute()`.
/// For task-level control, use `dlp_projects_locations_discovery_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_discovery_configs_patch(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsDiscoveryConfigsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2DiscoveryConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_discovery_configs_patch_builder(client, &args.name)?;
    dlp_projects_locations_discovery_configs_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}:cancel
/// Starts asynchronous cancellation on a long-running DlpJob. The server makes a best effort to cancel the DlpJob, but success is not guaranteed. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_dlp_jobs_cancel_execute()` to send, or `dlp_projects_locations_dlp_jobs_cancel` for simplest API.

pub fn dlp_projects_locations_dlp_jobs_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/dlpJobs/{dlpJobsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}:cancel
/// Starts asynchronous cancellation on a long-running DlpJob. The server makes a best effort to cancel the DlpJob, but success is not guaranteed. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_dlp_jobs_cancel_execute()` or `dlp_projects_locations_dlp_jobs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_dlp_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}:cancel
/// Starts asynchronous cancellation on a long-running DlpJob. The server makes a best effort to cancel the DlpJob, but success is not guaranteed. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_dlp_jobs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_dlp_jobs_cancel_task()`.
/// For the simplest API, use `dlp_projects_locations_dlp_jobs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_dlp_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_dlp_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_dlp_jobs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_dlp_jobs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsDlpJobsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}:cancel
/// Starts asynchronous cancellation on a long-running DlpJob. The server makes a best effort to cancel the DlpJob, but success is not guaranteed. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_dlp_jobs_cancel_builder()` + `dlp_projects_locations_dlp_jobs_cancel_execute()`.
/// For task-level control, use `dlp_projects_locations_dlp_jobs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_cancel(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsDlpJobsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_dlp_jobs_cancel_builder(client, &args.name)?;
    dlp_projects_locations_dlp_jobs_cancel_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dlpJobs
/// Creates a new job to inspect storage or calculate risk metrics. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more. When no InfoTypes or CustomInfoTypes are specified in inspect jobs, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_dlp_jobs_create_execute()` to send, or `dlp_projects_locations_dlp_jobs_create` for simplest API.

pub fn dlp_projects_locations_dlp_jobs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/dlpJobs",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dlpJobs
/// Creates a new job to inspect storage or calculate risk metrics. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more. When no InfoTypes or CustomInfoTypes are specified in inspect jobs, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_dlp_jobs_create_execute()` or `dlp_projects_locations_dlp_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_dlp_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DlpJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dlpJobs
/// Creates a new job to inspect storage or calculate risk metrics. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more. When no InfoTypes or CustomInfoTypes are specified in inspect jobs, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_dlp_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_dlp_jobs_create_task()`.
/// For the simplest API, use `dlp_projects_locations_dlp_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_dlp_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_dlp_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_dlp_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_dlp_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsDlpJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dlpJobs
/// Creates a new job to inspect storage or calculate risk metrics. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more. When no InfoTypes or CustomInfoTypes are specified in inspect jobs, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_dlp_jobs_create_builder()` + `dlp_projects_locations_dlp_jobs_create_execute()`.
/// For task-level control, use `dlp_projects_locations_dlp_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_create(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsDlpJobsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_dlp_jobs_create_builder(client, &args.parent)?;
    dlp_projects_locations_dlp_jobs_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}
/// Deletes a long-running DlpJob. This method indicates that the client is no longer interested in the DlpJob result. The job will be canceled if possible. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_dlp_jobs_delete_execute()` to send, or `dlp_projects_locations_dlp_jobs_delete` for simplest API.

pub fn dlp_projects_locations_dlp_jobs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/dlpJobs/{dlpJobsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}
/// Deletes a long-running DlpJob. This method indicates that the client is no longer interested in the DlpJob result. The job will be canceled if possible. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_dlp_jobs_delete_execute()` or `dlp_projects_locations_dlp_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_dlp_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}
/// Deletes a long-running DlpJob. This method indicates that the client is no longer interested in the DlpJob result. The job will be canceled if possible. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_dlp_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_dlp_jobs_delete_task()`.
/// For the simplest API, use `dlp_projects_locations_dlp_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_dlp_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_dlp_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_dlp_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_dlp_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsDlpJobsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}
/// Deletes a long-running DlpJob. This method indicates that the client is no longer interested in the DlpJob result. The job will be canceled if possible. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_dlp_jobs_delete_builder()` + `dlp_projects_locations_dlp_jobs_delete_execute()`.
/// For task-level control, use `dlp_projects_locations_dlp_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_delete(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsDlpJobsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_dlp_jobs_delete_builder(client, &args.name)?;
    dlp_projects_locations_dlp_jobs_delete_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}:finish
/// Finish a running hybrid DlpJob. Triggers the finalization steps and running of any enabled actions that have not yet run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_dlp_jobs_finish_execute()` to send, or `dlp_projects_locations_dlp_jobs_finish` for simplest API.

pub fn dlp_projects_locations_dlp_jobs_finish_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/dlpJobs/{dlpJobsId}:finish",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}:finish
/// Finish a running hybrid DlpJob. Triggers the finalization steps and running of any enabled actions that have not yet run.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_dlp_jobs_finish_execute()` or `dlp_projects_locations_dlp_jobs_finish`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_dlp_jobs_finish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_finish_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}:finish
/// Finish a running hybrid DlpJob. Triggers the finalization steps and running of any enabled actions that have not yet run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_dlp_jobs_finish_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_dlp_jobs_finish_task()`.
/// For the simplest API, use `dlp_projects_locations_dlp_jobs_finish()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_dlp_jobs_finish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_dlp_jobs_finish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_dlp_jobs_finish_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_dlp_jobs_finish`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsDlpJobsFinishArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}:finish
/// Finish a running hybrid DlpJob. Triggers the finalization steps and running of any enabled actions that have not yet run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_dlp_jobs_finish_builder()` + `dlp_projects_locations_dlp_jobs_finish_execute()`.
/// For task-level control, use `dlp_projects_locations_dlp_jobs_finish_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_finish(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsDlpJobsFinishArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_dlp_jobs_finish_builder(client, &args.name)?;
    dlp_projects_locations_dlp_jobs_finish_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}
/// Gets the latest state of a long-running DlpJob. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_dlp_jobs_get_execute()` to send, or `dlp_projects_locations_dlp_jobs_get` for simplest API.

pub fn dlp_projects_locations_dlp_jobs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/dlpJobs/{dlpJobsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}
/// Gets the latest state of a long-running DlpJob. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_dlp_jobs_get_execute()` or `dlp_projects_locations_dlp_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_dlp_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DlpJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}
/// Gets the latest state of a long-running DlpJob. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_dlp_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_dlp_jobs_get_task()`.
/// For the simplest API, use `dlp_projects_locations_dlp_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_dlp_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_dlp_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_dlp_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_dlp_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsDlpJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}
/// Gets the latest state of a long-running DlpJob. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_dlp_jobs_get_builder()` + `dlp_projects_locations_dlp_jobs_get_execute()`.
/// For task-level control, use `dlp_projects_locations_dlp_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_get(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsDlpJobsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_dlp_jobs_get_builder(client, &args.name)?;
    dlp_projects_locations_dlp_jobs_get_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}:hybridInspect
/// Inspect hybrid content and store findings to a job. To review the findings, inspect the job. Inspection will occur asynchronously.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_dlp_jobs_hybrid_inspect_execute()` to send, or `dlp_projects_locations_dlp_jobs_hybrid_inspect` for simplest API.

pub fn dlp_projects_locations_dlp_jobs_hybrid_inspect_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/dlpJobs/{dlpJobsId}:hybridInspect",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}:hybridInspect
/// Inspect hybrid content and store findings to a job. To review the findings, inspect the job. Inspection will occur asynchronously.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_dlp_jobs_hybrid_inspect_execute()` or `dlp_projects_locations_dlp_jobs_hybrid_inspect`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_dlp_jobs_hybrid_inspect_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_hybrid_inspect_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2HybridInspectResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2HybridInspectResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}:hybridInspect
/// Inspect hybrid content and store findings to a job. To review the findings, inspect the job. Inspection will occur asynchronously.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_dlp_jobs_hybrid_inspect_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_dlp_jobs_hybrid_inspect_task()`.
/// For the simplest API, use `dlp_projects_locations_dlp_jobs_hybrid_inspect()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_dlp_jobs_hybrid_inspect_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_dlp_jobs_hybrid_inspect_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2HybridInspectResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_dlp_jobs_hybrid_inspect_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_dlp_jobs_hybrid_inspect`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsDlpJobsHybridInspectArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dlpJobs/{dlpJobsId}:hybridInspect
/// Inspect hybrid content and store findings to a job. To review the findings, inspect the job. Inspection will occur asynchronously.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_dlp_jobs_hybrid_inspect_builder()` + `dlp_projects_locations_dlp_jobs_hybrid_inspect_execute()`.
/// For task-level control, use `dlp_projects_locations_dlp_jobs_hybrid_inspect_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_hybrid_inspect(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsDlpJobsHybridInspectArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2HybridInspectResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_dlp_jobs_hybrid_inspect_builder(client, &args.name)?;
    dlp_projects_locations_dlp_jobs_hybrid_inspect_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_dlp_jobs_list_execute()` to send, or `dlp_projects_locations_dlp_jobs_list` for simplest API.

pub fn dlp_projects_locations_dlp_jobs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    type_rs: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/dlpJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = type_rs.as_ref() {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_dlp_jobs_list_execute()` or `dlp_projects_locations_dlp_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_dlp_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListDlpJobsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListDlpJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_dlp_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_dlp_jobs_list_task()`.
/// For the simplest API, use `dlp_projects_locations_dlp_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_dlp_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_dlp_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDlpJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_dlp_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_dlp_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsDlpJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/dlpJobs
/// Lists DlpJobs that match the specified filter in the request. See <https://cloud.google.`com/sensitive-data-protection/docs/inspecting-storage`> and <https://cloud.google.`com/sensitive-data-protection/docs/compute-risk-analysis`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_dlp_jobs_list_builder()` + `dlp_projects_locations_dlp_jobs_list_execute()`.
/// For task-level control, use `dlp_projects_locations_dlp_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_dlp_jobs_list(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsDlpJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListDlpJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_dlp_jobs_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.type_rs,
    )?;
    dlp_projects_locations_dlp_jobs_list_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}
/// Delete a FileStoreDataProfile. Will not prevent the profile from being regenerated if the resource is still included in a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_file_store_data_profiles_delete_execute()` to send, or `dlp_projects_locations_file_store_data_profiles_delete` for simplest API.

pub fn dlp_projects_locations_file_store_data_profiles_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}
/// Delete a FileStoreDataProfile. Will not prevent the profile from being regenerated if the resource is still included in a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_file_store_data_profiles_delete_execute()` or `dlp_projects_locations_file_store_data_profiles_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_file_store_data_profiles_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_file_store_data_profiles_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}
/// Delete a FileStoreDataProfile. Will not prevent the profile from being regenerated if the resource is still included in a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_file_store_data_profiles_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_file_store_data_profiles_delete_task()`.
/// For the simplest API, use `dlp_projects_locations_file_store_data_profiles_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_file_store_data_profiles_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_file_store_data_profiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_file_store_data_profiles_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_file_store_data_profiles_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsFileStoreDataProfilesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}
/// Delete a FileStoreDataProfile. Will not prevent the profile from being regenerated if the resource is still included in a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_file_store_data_profiles_delete_builder()` + `dlp_projects_locations_file_store_data_profiles_delete_execute()`.
/// For task-level control, use `dlp_projects_locations_file_store_data_profiles_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_file_store_data_profiles_delete(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsFileStoreDataProfilesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dlp_projects_locations_file_store_data_profiles_delete_builder(client, &args.name)?;
    dlp_projects_locations_file_store_data_profiles_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}
/// Gets a file store data profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_file_store_data_profiles_get_execute()` to send, or `dlp_projects_locations_file_store_data_profiles_get` for simplest API.

pub fn dlp_projects_locations_file_store_data_profiles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}
/// Gets a file store data profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_file_store_data_profiles_get_execute()` or `dlp_projects_locations_file_store_data_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_file_store_data_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_file_store_data_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2FileStoreDataProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2FileStoreDataProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}
/// Gets a file store data profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_file_store_data_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_file_store_data_profiles_get_task()`.
/// For the simplest API, use `dlp_projects_locations_file_store_data_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_file_store_data_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_file_store_data_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2FileStoreDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_file_store_data_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_file_store_data_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsFileStoreDataProfilesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/fileStoreDataProfiles/{fileStoreDataProfilesId}
/// Gets a file store data profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_file_store_data_profiles_get_builder()` + `dlp_projects_locations_file_store_data_profiles_get_execute()`.
/// For task-level control, use `dlp_projects_locations_file_store_data_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_file_store_data_profiles_get(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsFileStoreDataProfilesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2FileStoreDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_file_store_data_profiles_get_builder(client, &args.name)?;
    dlp_projects_locations_file_store_data_profiles_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/fileStoreDataProfiles
/// Lists file store data profiles for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_file_store_data_profiles_list_execute()` to send, or `dlp_projects_locations_file_store_data_profiles_list` for simplest API.

pub fn dlp_projects_locations_file_store_data_profiles_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/fileStoreDataProfiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/fileStoreDataProfiles
/// Lists file store data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_file_store_data_profiles_list_execute()` or `dlp_projects_locations_file_store_data_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_file_store_data_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_file_store_data_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePrivacyDlpV2ListFileStoreDataProfilesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListFileStoreDataProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/fileStoreDataProfiles
/// Lists file store data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_file_store_data_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_file_store_data_profiles_list_task()`.
/// For the simplest API, use `dlp_projects_locations_file_store_data_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_file_store_data_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_file_store_data_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListFileStoreDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_file_store_data_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_file_store_data_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsFileStoreDataProfilesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/fileStoreDataProfiles
/// Lists file store data profiles for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_file_store_data_profiles_list_builder()` + `dlp_projects_locations_file_store_data_profiles_list_execute()`.
/// For task-level control, use `dlp_projects_locations_file_store_data_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_file_store_data_profiles_list(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsFileStoreDataProfilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListFileStoreDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_file_store_data_profiles_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_projects_locations_file_store_data_profiles_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/image:redact
/// Redacts potentially sensitive info from an image. This method has limits on input size, processing time, and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/redacting-sensitive-data-images`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. Only the first frame of each multiframe image is redacted. Metadata and other frames are omitted in the response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_image_redact_execute()` to send, or `dlp_projects_locations_image_redact` for simplest API.

pub fn dlp_projects_locations_image_redact_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/image:redact",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/image:redact
/// Redacts potentially sensitive info from an image. This method has limits on input size, processing time, and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/redacting-sensitive-data-images`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. Only the first frame of each multiframe image is redacted. Metadata and other frames are omitted in the response.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_image_redact_execute()` or `dlp_projects_locations_image_redact`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_image_redact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_image_redact_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2RedactImageResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2RedactImageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/image:redact
/// Redacts potentially sensitive info from an image. This method has limits on input size, processing time, and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/redacting-sensitive-data-images`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. Only the first frame of each multiframe image is redacted. Metadata and other frames are omitted in the response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_image_redact_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_image_redact_task()`.
/// For the simplest API, use `dlp_projects_locations_image_redact()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_image_redact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_image_redact_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2RedactImageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_image_redact_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_image_redact`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsImageRedactArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/image:redact
/// Redacts potentially sensitive info from an image. This method has limits on input size, processing time, and output size. See <https://cloud.google.`com/sensitive-data-protection/docs/redacting-sensitive-data-images`> to learn more. When no InfoTypes or CustomInfoTypes are specified in this request, the system will automatically choose what detectors to run. By default this may be all types, but may change over time as detectors are updated. Only the first frame of each multiframe image is redacted. Metadata and other frames are omitted in the response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_image_redact_builder()` + `dlp_projects_locations_image_redact_execute()`.
/// For task-level control, use `dlp_projects_locations_image_redact_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_image_redact(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsImageRedactArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2RedactImageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_image_redact_builder(client, &args.parent)?;
    dlp_projects_locations_image_redact_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_info_types_list_execute()` to send, or `dlp_projects_locations_info_types_list` for simplest API.

pub fn dlp_projects_locations_info_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    languageCode: &Option<String>,
    locationId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/infoTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_info_types_list_execute()` or `dlp_projects_locations_info_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_info_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_info_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInfoTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_info_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_info_types_list_task()`.
/// For the simplest API, use `dlp_projects_locations_info_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_info_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_info_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_info_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_info_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsInfoTypesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: locationId
    pub locationId: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/infoTypes
/// Returns a list of the sensitive information types that the DLP API supports. See <https://cloud.google.`com/sensitive-data-protection/docs/infotypes-reference`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_info_types_list_builder()` + `dlp_projects_locations_info_types_list_execute()`.
/// For task-level control, use `dlp_projects_locations_info_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_info_types_list(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsInfoTypesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_info_types_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.languageCode,
        &args.locationId,
    )?;
    dlp_projects_locations_info_types_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_inspect_templates_create_execute()` to send, or `dlp_projects_locations_inspect_templates_create` for simplest API.

pub fn dlp_projects_locations_inspect_templates_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/inspectTemplates",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_inspect_templates_create_execute()` or `dlp_projects_locations_inspect_templates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_inspect_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_inspect_templates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_inspect_templates_create_task()`.
/// For the simplest API, use `dlp_projects_locations_inspect_templates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_inspect_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_inspect_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_inspect_templates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_inspect_templates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsInspectTemplatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates
/// Creates an InspectTemplate for reusing frequently used configuration for inspecting content, images, and storage. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_inspect_templates_create_builder()` + `dlp_projects_locations_inspect_templates_create_execute()`.
/// For task-level control, use `dlp_projects_locations_inspect_templates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_create(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsInspectTemplatesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_inspect_templates_create_builder(client, &args.parent)?;
    dlp_projects_locations_inspect_templates_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_inspect_templates_delete_execute()` to send, or `dlp_projects_locations_inspect_templates_delete` for simplest API.

pub fn dlp_projects_locations_inspect_templates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_inspect_templates_delete_execute()` or `dlp_projects_locations_inspect_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_inspect_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_inspect_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_inspect_templates_delete_task()`.
/// For the simplest API, use `dlp_projects_locations_inspect_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_inspect_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_inspect_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_inspect_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_inspect_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsInspectTemplatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Deletes an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_inspect_templates_delete_builder()` + `dlp_projects_locations_inspect_templates_delete_execute()`.
/// For task-level control, use `dlp_projects_locations_inspect_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_delete(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsInspectTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_inspect_templates_delete_builder(client, &args.name)?;
    dlp_projects_locations_inspect_templates_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_inspect_templates_get_execute()` to send, or `dlp_projects_locations_inspect_templates_get` for simplest API.

pub fn dlp_projects_locations_inspect_templates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_inspect_templates_get_execute()` or `dlp_projects_locations_inspect_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_inspect_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_inspect_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_inspect_templates_get_task()`.
/// For the simplest API, use `dlp_projects_locations_inspect_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_inspect_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_inspect_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_inspect_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_inspect_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsInspectTemplatesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Gets an InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_inspect_templates_get_builder()` + `dlp_projects_locations_inspect_templates_get_execute()`.
/// For task-level control, use `dlp_projects_locations_inspect_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_get(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsInspectTemplatesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_inspect_templates_get_builder(client, &args.name)?;
    dlp_projects_locations_inspect_templates_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_inspect_templates_list_execute()` to send, or `dlp_projects_locations_inspect_templates_list` for simplest API.

pub fn dlp_projects_locations_inspect_templates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/inspectTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_inspect_templates_list_execute()` or `dlp_projects_locations_inspect_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_inspect_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInspectTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_inspect_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_inspect_templates_list_task()`.
/// For the simplest API, use `dlp_projects_locations_inspect_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_inspect_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_inspect_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_inspect_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_inspect_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsInspectTemplatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates
/// Lists InspectTemplates. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_inspect_templates_list_builder()` + `dlp_projects_locations_inspect_templates_list_execute()`.
/// For task-level control, use `dlp_projects_locations_inspect_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_list(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsInspectTemplatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListInspectTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_inspect_templates_list_builder(
        client,
        &args.parent,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_projects_locations_inspect_templates_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_inspect_templates_patch_execute()` to send, or `dlp_projects_locations_inspect_templates_patch` for simplest API.

pub fn dlp_projects_locations_inspect_templates_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_inspect_templates_patch_execute()` or `dlp_projects_locations_inspect_templates_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_inspect_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2InspectTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_inspect_templates_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_inspect_templates_patch_task()`.
/// For the simplest API, use `dlp_projects_locations_inspect_templates_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_inspect_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_inspect_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_inspect_templates_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_inspect_templates_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsInspectTemplatesPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/inspectTemplates/{inspectTemplatesId}
/// Updates the InspectTemplate. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-templates`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_inspect_templates_patch_builder()` + `dlp_projects_locations_inspect_templates_patch_execute()`.
/// For task-level control, use `dlp_projects_locations_inspect_templates_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_inspect_templates_patch(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsInspectTemplatesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2InspectTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_inspect_templates_patch_builder(client, &args.name)?;
    dlp_projects_locations_inspect_templates_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}:activate
/// Activate a job trigger. Causes the immediate execute of a trigger instead of waiting on the trigger event to occur.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_job_triggers_activate_execute()` to send, or `dlp_projects_locations_job_triggers_activate` for simplest API.

pub fn dlp_projects_locations_job_triggers_activate_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/jobTriggers/{jobTriggersId}:activate",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}:activate
/// Activate a job trigger. Causes the immediate execute of a trigger instead of waiting on the trigger event to occur.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_job_triggers_activate_execute()` or `dlp_projects_locations_job_triggers_activate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_job_triggers_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_activate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2DlpJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}:activate
/// Activate a job trigger. Causes the immediate execute of a trigger instead of waiting on the trigger event to occur.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_job_triggers_activate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_job_triggers_activate_task()`.
/// For the simplest API, use `dlp_projects_locations_job_triggers_activate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_job_triggers_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_job_triggers_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_job_triggers_activate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_job_triggers_activate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsJobTriggersActivateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}:activate
/// Activate a job trigger. Causes the immediate execute of a trigger instead of waiting on the trigger event to occur.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_job_triggers_activate_builder()` + `dlp_projects_locations_job_triggers_activate_execute()`.
/// For task-level control, use `dlp_projects_locations_job_triggers_activate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_activate(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsJobTriggersActivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GooglePrivacyDlpV2DlpJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_job_triggers_activate_builder(client, &args.name)?;
    dlp_projects_locations_job_triggers_activate_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_job_triggers_create_execute()` to send, or `dlp_projects_locations_job_triggers_create` for simplest API.

pub fn dlp_projects_locations_job_triggers_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/jobTriggers",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_job_triggers_create_execute()` or `dlp_projects_locations_job_triggers_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_job_triggers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_job_triggers_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_job_triggers_create_task()`.
/// For the simplest API, use `dlp_projects_locations_job_triggers_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_job_triggers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_job_triggers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_job_triggers_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_job_triggers_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsJobTriggersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/jobTriggers
/// Creates a job trigger to run DLP actions such as scanning storage for sensitive information on a set schedule. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_job_triggers_create_builder()` + `dlp_projects_locations_job_triggers_create_execute()`.
/// For task-level control, use `dlp_projects_locations_job_triggers_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_create(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsJobTriggersCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_job_triggers_create_builder(client, &args.parent)?;
    dlp_projects_locations_job_triggers_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_job_triggers_delete_execute()` to send, or `dlp_projects_locations_job_triggers_delete` for simplest API.

pub fn dlp_projects_locations_job_triggers_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/jobTriggers/{jobTriggersId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_job_triggers_delete_execute()` or `dlp_projects_locations_job_triggers_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_job_triggers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_job_triggers_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_job_triggers_delete_task()`.
/// For the simplest API, use `dlp_projects_locations_job_triggers_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_job_triggers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_job_triggers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_job_triggers_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_job_triggers_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsJobTriggersDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Deletes a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_job_triggers_delete_builder()` + `dlp_projects_locations_job_triggers_delete_execute()`.
/// For task-level control, use `dlp_projects_locations_job_triggers_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_delete(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsJobTriggersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_job_triggers_delete_builder(client, &args.name)?;
    dlp_projects_locations_job_triggers_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_job_triggers_get_execute()` to send, or `dlp_projects_locations_job_triggers_get` for simplest API.

pub fn dlp_projects_locations_job_triggers_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/jobTriggers/{jobTriggersId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_job_triggers_get_execute()` or `dlp_projects_locations_job_triggers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_job_triggers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_job_triggers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_job_triggers_get_task()`.
/// For the simplest API, use `dlp_projects_locations_job_triggers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_job_triggers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_job_triggers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_job_triggers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_job_triggers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsJobTriggersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Gets a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_job_triggers_get_builder()` + `dlp_projects_locations_job_triggers_get_execute()`.
/// For task-level control, use `dlp_projects_locations_job_triggers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_get(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsJobTriggersGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_job_triggers_get_builder(client, &args.name)?;
    dlp_projects_locations_job_triggers_get_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}:hybridInspect
/// Inspect hybrid content and store findings to a trigger. The inspection will be processed asynchronously. To review the findings monitor the jobs within the trigger.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_job_triggers_hybrid_inspect_execute()` to send, or `dlp_projects_locations_job_triggers_hybrid_inspect` for simplest API.

pub fn dlp_projects_locations_job_triggers_hybrid_inspect_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/jobTriggers/{jobTriggersId}:hybridInspect",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}:hybridInspect
/// Inspect hybrid content and store findings to a trigger. The inspection will be processed asynchronously. To review the findings monitor the jobs within the trigger.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_job_triggers_hybrid_inspect_execute()` or `dlp_projects_locations_job_triggers_hybrid_inspect`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_job_triggers_hybrid_inspect_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_hybrid_inspect_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2HybridInspectResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2HybridInspectResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}:hybridInspect
/// Inspect hybrid content and store findings to a trigger. The inspection will be processed asynchronously. To review the findings monitor the jobs within the trigger.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_job_triggers_hybrid_inspect_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_job_triggers_hybrid_inspect_task()`.
/// For the simplest API, use `dlp_projects_locations_job_triggers_hybrid_inspect()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_job_triggers_hybrid_inspect_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_job_triggers_hybrid_inspect_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2HybridInspectResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_job_triggers_hybrid_inspect_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_job_triggers_hybrid_inspect`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsJobTriggersHybridInspectArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}:hybridInspect
/// Inspect hybrid content and store findings to a trigger. The inspection will be processed asynchronously. To review the findings monitor the jobs within the trigger.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_job_triggers_hybrid_inspect_builder()` + `dlp_projects_locations_job_triggers_hybrid_inspect_execute()`.
/// For task-level control, use `dlp_projects_locations_job_triggers_hybrid_inspect_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_hybrid_inspect(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsJobTriggersHybridInspectArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2HybridInspectResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_job_triggers_hybrid_inspect_builder(client, &args.name)?;
    dlp_projects_locations_job_triggers_hybrid_inspect_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_job_triggers_list_execute()` to send, or `dlp_projects_locations_job_triggers_list` for simplest API.

pub fn dlp_projects_locations_job_triggers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    type_rs: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/jobTriggers",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = type_rs.as_ref() {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_job_triggers_list_execute()` or `dlp_projects_locations_job_triggers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_job_triggers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListJobTriggersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListJobTriggersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_job_triggers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_job_triggers_list_task()`.
/// For the simplest API, use `dlp_projects_locations_job_triggers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_job_triggers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_job_triggers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListJobTriggersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_job_triggers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_job_triggers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsJobTriggersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/jobTriggers
/// Lists job triggers. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_job_triggers_list_builder()` + `dlp_projects_locations_job_triggers_list_execute()`.
/// For task-level control, use `dlp_projects_locations_job_triggers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_list(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsJobTriggersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListJobTriggersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_job_triggers_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.type_rs,
    )?;
    dlp_projects_locations_job_triggers_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_job_triggers_patch_execute()` to send, or `dlp_projects_locations_job_triggers_patch` for simplest API.

pub fn dlp_projects_locations_job_triggers_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/jobTriggers/{jobTriggersId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_job_triggers_patch_execute()` or `dlp_projects_locations_job_triggers_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_job_triggers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2JobTrigger = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_job_triggers_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_job_triggers_patch_task()`.
/// For the simplest API, use `dlp_projects_locations_job_triggers_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_job_triggers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_job_triggers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_job_triggers_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_job_triggers_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsJobTriggersPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/jobTriggers/{jobTriggersId}
/// Updates a job trigger. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-job-triggers`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_job_triggers_patch_builder()` + `dlp_projects_locations_job_triggers_patch_execute()`.
/// For task-level control, use `dlp_projects_locations_job_triggers_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_job_triggers_patch(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsJobTriggersPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2JobTrigger>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_job_triggers_patch_builder(client, &args.name)?;
    dlp_projects_locations_job_triggers_patch_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/projectDataProfiles/{projectDataProfilesId}
/// Gets a project data profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_project_data_profiles_get_execute()` to send, or `dlp_projects_locations_project_data_profiles_get` for simplest API.

pub fn dlp_projects_locations_project_data_profiles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/projectDataProfiles/{projectDataProfilesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/projectDataProfiles/{projectDataProfilesId}
/// Gets a project data profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_project_data_profiles_get_execute()` or `dlp_projects_locations_project_data_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_project_data_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_project_data_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ProjectDataProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ProjectDataProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/projectDataProfiles/{projectDataProfilesId}
/// Gets a project data profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_project_data_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_project_data_profiles_get_task()`.
/// For the simplest API, use `dlp_projects_locations_project_data_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_project_data_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_project_data_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ProjectDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_project_data_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_project_data_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsProjectDataProfilesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/projectDataProfiles/{projectDataProfilesId}
/// Gets a project data profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_project_data_profiles_get_builder()` + `dlp_projects_locations_project_data_profiles_get_execute()`.
/// For task-level control, use `dlp_projects_locations_project_data_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_project_data_profiles_get(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsProjectDataProfilesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ProjectDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_project_data_profiles_get_builder(client, &args.name)?;
    dlp_projects_locations_project_data_profiles_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/projectDataProfiles
/// Lists project data profiles for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_project_data_profiles_list_execute()` to send, or `dlp_projects_locations_project_data_profiles_list` for simplest API.

pub fn dlp_projects_locations_project_data_profiles_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/projectDataProfiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/projectDataProfiles
/// Lists project data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_project_data_profiles_list_execute()` or `dlp_projects_locations_project_data_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_project_data_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_project_data_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePrivacyDlpV2ListProjectDataProfilesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListProjectDataProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/projectDataProfiles
/// Lists project data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_project_data_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_project_data_profiles_list_task()`.
/// For the simplest API, use `dlp_projects_locations_project_data_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_project_data_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_project_data_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListProjectDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_project_data_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_project_data_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsProjectDataProfilesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/projectDataProfiles
/// Lists project data profiles for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_project_data_profiles_list_builder()` + `dlp_projects_locations_project_data_profiles_list_execute()`.
/// For task-level control, use `dlp_projects_locations_project_data_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_project_data_profiles_list(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsProjectDataProfilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListProjectDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_project_data_profiles_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_projects_locations_project_data_profiles_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_stored_info_types_create_execute()` to send, or `dlp_projects_locations_stored_info_types_create` for simplest API.

pub fn dlp_projects_locations_stored_info_types_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/storedInfoTypes",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_stored_info_types_create_execute()` or `dlp_projects_locations_stored_info_types_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_stored_info_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_stored_info_types_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_stored_info_types_create_task()`.
/// For the simplest API, use `dlp_projects_locations_stored_info_types_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_stored_info_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_stored_info_types_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_stored_info_types_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_stored_info_types_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsStoredInfoTypesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_stored_info_types_create_builder()` + `dlp_projects_locations_stored_info_types_create_execute()`.
/// For task-level control, use `dlp_projects_locations_stored_info_types_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_create(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsStoredInfoTypesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_stored_info_types_create_builder(client, &args.parent)?;
    dlp_projects_locations_stored_info_types_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_stored_info_types_delete_execute()` to send, or `dlp_projects_locations_stored_info_types_delete` for simplest API.

pub fn dlp_projects_locations_stored_info_types_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_stored_info_types_delete_execute()` or `dlp_projects_locations_stored_info_types_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_stored_info_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_stored_info_types_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_stored_info_types_delete_task()`.
/// For the simplest API, use `dlp_projects_locations_stored_info_types_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_stored_info_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_stored_info_types_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_stored_info_types_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_stored_info_types_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsStoredInfoTypesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_stored_info_types_delete_builder()` + `dlp_projects_locations_stored_info_types_delete_execute()`.
/// For task-level control, use `dlp_projects_locations_stored_info_types_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_delete(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsStoredInfoTypesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_stored_info_types_delete_builder(client, &args.name)?;
    dlp_projects_locations_stored_info_types_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_stored_info_types_get_execute()` to send, or `dlp_projects_locations_stored_info_types_get` for simplest API.

pub fn dlp_projects_locations_stored_info_types_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_stored_info_types_get_execute()` or `dlp_projects_locations_stored_info_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_stored_info_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_stored_info_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_stored_info_types_get_task()`.
/// For the simplest API, use `dlp_projects_locations_stored_info_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_stored_info_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_stored_info_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_stored_info_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_stored_info_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsStoredInfoTypesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_stored_info_types_get_builder()` + `dlp_projects_locations_stored_info_types_get_execute()`.
/// For task-level control, use `dlp_projects_locations_stored_info_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_get(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsStoredInfoTypesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_stored_info_types_get_builder(client, &args.name)?;
    dlp_projects_locations_stored_info_types_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_stored_info_types_list_execute()` to send, or `dlp_projects_locations_stored_info_types_list` for simplest API.

pub fn dlp_projects_locations_stored_info_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/storedInfoTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_stored_info_types_list_execute()` or `dlp_projects_locations_stored_info_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_stored_info_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListStoredInfoTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_stored_info_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_stored_info_types_list_task()`.
/// For the simplest API, use `dlp_projects_locations_stored_info_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_stored_info_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_stored_info_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_stored_info_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_stored_info_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsStoredInfoTypesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_stored_info_types_list_builder()` + `dlp_projects_locations_stored_info_types_list_execute()`.
/// For task-level control, use `dlp_projects_locations_stored_info_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_list(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsStoredInfoTypesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_stored_info_types_list_builder(
        client,
        &args.parent,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_projects_locations_stored_info_types_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_stored_info_types_patch_execute()` to send, or `dlp_projects_locations_stored_info_types_patch` for simplest API.

pub fn dlp_projects_locations_stored_info_types_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_stored_info_types_patch_execute()` or `dlp_projects_locations_stored_info_types_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_stored_info_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_stored_info_types_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_stored_info_types_patch_task()`.
/// For the simplest API, use `dlp_projects_locations_stored_info_types_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_stored_info_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_stored_info_types_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_stored_info_types_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_stored_info_types_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsStoredInfoTypesPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/storedInfoTypes/{storedInfoTypesId}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_stored_info_types_patch_builder()` + `dlp_projects_locations_stored_info_types_patch_execute()`.
/// For task-level control, use `dlp_projects_locations_stored_info_types_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_stored_info_types_patch(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsStoredInfoTypesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_stored_info_types_patch_builder(client, &args.name)?;
    dlp_projects_locations_stored_info_types_patch_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}
/// Delete a TableDataProfile. Will not prevent the profile from being regenerated if the table is still included in a discovery configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_table_data_profiles_delete_execute()` to send, or `dlp_projects_locations_table_data_profiles_delete` for simplest API.

pub fn dlp_projects_locations_table_data_profiles_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}
/// Delete a TableDataProfile. Will not prevent the profile from being regenerated if the table is still included in a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_table_data_profiles_delete_execute()` or `dlp_projects_locations_table_data_profiles_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_table_data_profiles_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_table_data_profiles_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}
/// Delete a TableDataProfile. Will not prevent the profile from being regenerated if the table is still included in a discovery configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_table_data_profiles_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_table_data_profiles_delete_task()`.
/// For the simplest API, use `dlp_projects_locations_table_data_profiles_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_table_data_profiles_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_table_data_profiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_table_data_profiles_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_table_data_profiles_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsTableDataProfilesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}
/// Delete a TableDataProfile. Will not prevent the profile from being regenerated if the table is still included in a discovery configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_table_data_profiles_delete_builder()` + `dlp_projects_locations_table_data_profiles_delete_execute()`.
/// For task-level control, use `dlp_projects_locations_table_data_profiles_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_table_data_profiles_delete(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsTableDataProfilesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_table_data_profiles_delete_builder(client, &args.name)?;
    dlp_projects_locations_table_data_profiles_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}
/// Gets a table data profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_table_data_profiles_get_execute()` to send, or `dlp_projects_locations_table_data_profiles_get` for simplest API.

pub fn dlp_projects_locations_table_data_profiles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}
/// Gets a table data profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_table_data_profiles_get_execute()` or `dlp_projects_locations_table_data_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_table_data_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_table_data_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2TableDataProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2TableDataProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}
/// Gets a table data profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_table_data_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_table_data_profiles_get_task()`.
/// For the simplest API, use `dlp_projects_locations_table_data_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_table_data_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_table_data_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2TableDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_table_data_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_table_data_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsTableDataProfilesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/tableDataProfiles/{tableDataProfilesId}
/// Gets a table data profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_table_data_profiles_get_builder()` + `dlp_projects_locations_table_data_profiles_get_execute()`.
/// For task-level control, use `dlp_projects_locations_table_data_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_table_data_profiles_get(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsTableDataProfilesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2TableDataProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_table_data_profiles_get_builder(client, &args.name)?;
    dlp_projects_locations_table_data_profiles_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/tableDataProfiles
/// Lists table data profiles for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_locations_table_data_profiles_list_execute()` to send, or `dlp_projects_locations_table_data_profiles_list` for simplest API.

pub fn dlp_projects_locations_table_data_profiles_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/locations/{locationsId}/tableDataProfiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/tableDataProfiles
/// Lists table data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_locations_table_data_profiles_list_execute()` or `dlp_projects_locations_table_data_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_table_data_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_table_data_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListTableDataProfilesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListTableDataProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/tableDataProfiles
/// Lists table data profiles for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_locations_table_data_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_locations_table_data_profiles_list_task()`.
/// For the simplest API, use `dlp_projects_locations_table_data_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_locations_table_data_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_locations_table_data_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListTableDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_locations_table_data_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_locations_table_data_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsTableDataProfilesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/tableDataProfiles
/// Lists table data profiles for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_locations_table_data_profiles_list_builder()` + `dlp_projects_locations_table_data_profiles_list_execute()`.
/// For task-level control, use `dlp_projects_locations_table_data_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_locations_table_data_profiles_list(
    client: &SimpleHttpClient,
    args: &DlpProjectsLocationsTableDataProfilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListTableDataProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_locations_table_data_profiles_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_projects_locations_table_data_profiles_list_execute(builder)
}

/// POST v2/projects/{projectsId}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_stored_info_types_create_execute()` to send, or `dlp_projects_stored_info_types_create` for simplest API.

pub fn dlp_projects_stored_info_types_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/storedInfoTypes",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_stored_info_types_create_execute()` or `dlp_projects_stored_info_types_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_stored_info_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_stored_info_types_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_stored_info_types_create_task()`.
/// For the simplest API, use `dlp_projects_stored_info_types_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_stored_info_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_stored_info_types_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_stored_info_types_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_stored_info_types_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsStoredInfoTypesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/storedInfoTypes
/// Creates a pre-built stored `infoType` to be used for inspection. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_stored_info_types_create_builder()` + `dlp_projects_stored_info_types_create_execute()`.
/// For task-level control, use `dlp_projects_stored_info_types_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_create(
    client: &SimpleHttpClient,
    args: &DlpProjectsStoredInfoTypesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_stored_info_types_create_builder(client, &args.parent)?;
    dlp_projects_stored_info_types_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/storedInfoTypes/{storedInfoTypesId}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_stored_info_types_delete_execute()` to send, or `dlp_projects_stored_info_types_delete` for simplest API.

pub fn dlp_projects_stored_info_types_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/storedInfoTypes/{storedInfoTypesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/storedInfoTypes/{storedInfoTypesId}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_stored_info_types_delete_execute()` or `dlp_projects_stored_info_types_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_stored_info_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/storedInfoTypes/{storedInfoTypesId}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_stored_info_types_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_stored_info_types_delete_task()`.
/// For the simplest API, use `dlp_projects_stored_info_types_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_stored_info_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_stored_info_types_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_stored_info_types_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_stored_info_types_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsStoredInfoTypesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/storedInfoTypes/{storedInfoTypesId}
/// Deletes a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_stored_info_types_delete_builder()` + `dlp_projects_stored_info_types_delete_execute()`.
/// For task-level control, use `dlp_projects_stored_info_types_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_delete(
    client: &SimpleHttpClient,
    args: &DlpProjectsStoredInfoTypesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_stored_info_types_delete_builder(client, &args.name)?;
    dlp_projects_stored_info_types_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/storedInfoTypes/{storedInfoTypesId}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_stored_info_types_get_execute()` to send, or `dlp_projects_stored_info_types_get` for simplest API.

pub fn dlp_projects_stored_info_types_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/storedInfoTypes/{storedInfoTypesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/storedInfoTypes/{storedInfoTypesId}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_stored_info_types_get_execute()` or `dlp_projects_stored_info_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_stored_info_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/storedInfoTypes/{storedInfoTypesId}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_stored_info_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_stored_info_types_get_task()`.
/// For the simplest API, use `dlp_projects_stored_info_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_stored_info_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_stored_info_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_stored_info_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_stored_info_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsStoredInfoTypesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/storedInfoTypes/{storedInfoTypesId}
/// Gets a stored `infoType`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_stored_info_types_get_builder()` + `dlp_projects_stored_info_types_get_execute()`.
/// For task-level control, use `dlp_projects_stored_info_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_get(
    client: &SimpleHttpClient,
    args: &DlpProjectsStoredInfoTypesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_stored_info_types_get_builder(client, &args.name)?;
    dlp_projects_stored_info_types_get_execute(builder)
}

/// GET v2/projects/{projectsId}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_stored_info_types_list_execute()` to send, or `dlp_projects_stored_info_types_list` for simplest API.

pub fn dlp_projects_stored_info_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    locationId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/storedInfoTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locationId.as_ref() {
        query_parts.push(format!("locationId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_stored_info_types_list_execute()` or `dlp_projects_stored_info_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_stored_info_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListStoredInfoTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_stored_info_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_stored_info_types_list_task()`.
/// For the simplest API, use `dlp_projects_stored_info_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_stored_info_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_stored_info_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_stored_info_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_stored_info_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsStoredInfoTypesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: locationId
    pub locationId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/storedInfoTypes
/// Lists stored `infoTypes`. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_stored_info_types_list_builder()` + `dlp_projects_stored_info_types_list_execute()`.
/// For task-level control, use `dlp_projects_stored_info_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_list(
    client: &SimpleHttpClient,
    args: &DlpProjectsStoredInfoTypesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2ListStoredInfoTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_stored_info_types_list_builder(
        client,
        &args.parent,
        &args.locationId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    dlp_projects_stored_info_types_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/storedInfoTypes/{storedInfoTypesId}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dlp_projects_stored_info_types_patch_execute()` to send, or `dlp_projects_stored_info_types_patch` for simplest API.

pub fn dlp_projects_stored_info_types_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{}/storedInfoTypes/{storedInfoTypesId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/storedInfoTypes/{storedInfoTypesId}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dlp_projects_stored_info_types_patch_execute()` or `dlp_projects_stored_info_types_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_stored_info_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2StoredInfoType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/storedInfoTypes/{storedInfoTypesId}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dlp_projects_stored_info_types_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dlp_projects_stored_info_types_patch_task()`.
/// For the simplest API, use `dlp_projects_stored_info_types_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dlp_projects_stored_info_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dlp_projects_stored_info_types_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dlp_projects_stored_info_types_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dlp_projects_stored_info_types_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsStoredInfoTypesPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v2/projects/{projectsId}/storedInfoTypes/{storedInfoTypesId}
/// Updates the stored `infoType` by creating a new version. The existing version will continue to be used until the new version is ready. See <https://cloud.google.`com/sensitive-data-protection/docs/creating-stored-infotypes`> to learn more.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dlp_projects_stored_info_types_patch_builder()` + `dlp_projects_stored_info_types_patch_execute()`.
/// For task-level control, use `dlp_projects_stored_info_types_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dlp_projects_stored_info_types_patch(
    client: &SimpleHttpClient,
    args: &DlpProjectsStoredInfoTypesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePrivacyDlpV2StoredInfoType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dlp_projects_stored_info_types_patch_builder(client, &args.name)?;
    dlp_projects_stored_info_types_patch_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListInfoTypesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListInfoTypesResponse with DlpInfoTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpInfoTypesListArgs> for GooglePrivacyDlpV2ListInfoTypesResponse {
    fn generate_resource_id(&self, input: &DlpInfoTypesListArgs) -> String {
        "gcp::dlp::GooglePrivacyDlpV2ListInfoTypesResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListInfoTypesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListInfoTypesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListInfoTypesResponse with DlpLocationsInfoTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpLocationsInfoTypesListArgs> for GooglePrivacyDlpV2ListInfoTypesResponse {
    fn generate_resource_id(&self, input: &DlpLocationsInfoTypesListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListInfoTypesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListInfoTypesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate with DlpOrganizationsDeidentifyTemplatesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsDeidentifyTemplatesCreateArgs>
    for GooglePrivacyDlpV2DeidentifyTemplate
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsDeidentifyTemplatesCreateArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpOrganizationsDeidentifyTemplatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsDeidentifyTemplatesDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsDeidentifyTemplatesDeleteArgs,
    ) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate with DlpOrganizationsDeidentifyTemplatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsDeidentifyTemplatesGetArgs>
    for GooglePrivacyDlpV2DeidentifyTemplate
{
    fn generate_resource_id(&self, input: &DlpOrganizationsDeidentifyTemplatesGetArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDeidentifyTemplatesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDeidentifyTemplatesResponse with DlpOrganizationsDeidentifyTemplatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsDeidentifyTemplatesListArgs>
    for GooglePrivacyDlpV2ListDeidentifyTemplatesResponse
{
    fn generate_resource_id(&self, input: &DlpOrganizationsDeidentifyTemplatesListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListDeidentifyTemplatesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListDeidentifyTemplatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate with DlpOrganizationsDeidentifyTemplatesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsDeidentifyTemplatesPatchArgs>
    for GooglePrivacyDlpV2DeidentifyTemplate
{
    fn generate_resource_id(&self, input: &DlpOrganizationsDeidentifyTemplatesPatchArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate with DlpOrganizationsInspectTemplatesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsInspectTemplatesCreateArgs>
    for GooglePrivacyDlpV2InspectTemplate
{
    fn generate_resource_id(&self, input: &DlpOrganizationsInspectTemplatesCreateArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2InspectTemplate/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2InspectTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpOrganizationsInspectTemplatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsInspectTemplatesDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(&self, input: &DlpOrganizationsInspectTemplatesDeleteArgs) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate with DlpOrganizationsInspectTemplatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsInspectTemplatesGetArgs>
    for GooglePrivacyDlpV2InspectTemplate
{
    fn generate_resource_id(&self, input: &DlpOrganizationsInspectTemplatesGetArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2InspectTemplate/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2InspectTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListInspectTemplatesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListInspectTemplatesResponse with DlpOrganizationsInspectTemplatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsInspectTemplatesListArgs>
    for GooglePrivacyDlpV2ListInspectTemplatesResponse
{
    fn generate_resource_id(&self, input: &DlpOrganizationsInspectTemplatesListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListInspectTemplatesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListInspectTemplatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate with DlpOrganizationsInspectTemplatesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsInspectTemplatesPatchArgs>
    for GooglePrivacyDlpV2InspectTemplate
{
    fn generate_resource_id(&self, input: &DlpOrganizationsInspectTemplatesPatchArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2InspectTemplate/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2InspectTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ColumnDataProfile
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ColumnDataProfile with DlpOrganizationsLocationsColumnDataProfilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsColumnDataProfilesGetArgs>
    for GooglePrivacyDlpV2ColumnDataProfile
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsColumnDataProfilesGetArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ColumnDataProfile/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ColumnDataProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListColumnDataProfilesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListColumnDataProfilesResponse with DlpOrganizationsLocationsColumnDataProfilesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsColumnDataProfilesListArgs>
    for GooglePrivacyDlpV2ListColumnDataProfilesResponse
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsColumnDataProfilesListArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListColumnDataProfilesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListColumnDataProfilesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2Connection
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2Connection with DlpOrganizationsLocationsConnectionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsConnectionsCreateArgs>
    for GooglePrivacyDlpV2Connection
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsConnectionsCreateArgs,
    ) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2Connection/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2Connection"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpOrganizationsLocationsConnectionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsConnectionsDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsConnectionsDeleteArgs,
    ) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2Connection
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2Connection with DlpOrganizationsLocationsConnectionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsConnectionsGetArgs>
    for GooglePrivacyDlpV2Connection
{
    fn generate_resource_id(&self, input: &DlpOrganizationsLocationsConnectionsGetArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2Connection/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2Connection"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListConnectionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListConnectionsResponse with DlpOrganizationsLocationsConnectionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsConnectionsListArgs>
    for GooglePrivacyDlpV2ListConnectionsResponse
{
    fn generate_resource_id(&self, input: &DlpOrganizationsLocationsConnectionsListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListConnectionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListConnectionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2Connection
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2Connection with DlpOrganizationsLocationsConnectionsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsConnectionsPatchArgs>
    for GooglePrivacyDlpV2Connection
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsConnectionsPatchArgs,
    ) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2Connection/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2Connection"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2SearchConnectionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2SearchConnectionsResponse with DlpOrganizationsLocationsConnectionsSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsConnectionsSearchArgs>
    for GooglePrivacyDlpV2SearchConnectionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsConnectionsSearchArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2SearchConnectionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2SearchConnectionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate with DlpOrganizationsLocationsDeidentifyTemplatesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsDeidentifyTemplatesCreateArgs>
    for GooglePrivacyDlpV2DeidentifyTemplate
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsDeidentifyTemplatesCreateArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpOrganizationsLocationsDeidentifyTemplatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsDeidentifyTemplatesDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsDeidentifyTemplatesDeleteArgs,
    ) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate with DlpOrganizationsLocationsDeidentifyTemplatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsDeidentifyTemplatesGetArgs>
    for GooglePrivacyDlpV2DeidentifyTemplate
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsDeidentifyTemplatesGetArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDeidentifyTemplatesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDeidentifyTemplatesResponse with DlpOrganizationsLocationsDeidentifyTemplatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsDeidentifyTemplatesListArgs>
    for GooglePrivacyDlpV2ListDeidentifyTemplatesResponse
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsDeidentifyTemplatesListArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListDeidentifyTemplatesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListDeidentifyTemplatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate with DlpOrganizationsLocationsDeidentifyTemplatesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsDeidentifyTemplatesPatchArgs>
    for GooglePrivacyDlpV2DeidentifyTemplate
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsDeidentifyTemplatesPatchArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DiscoveryConfig
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DiscoveryConfig with DlpOrganizationsLocationsDiscoveryConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsDiscoveryConfigsCreateArgs>
    for GooglePrivacyDlpV2DiscoveryConfig
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsDiscoveryConfigsCreateArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2DiscoveryConfig/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DiscoveryConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpOrganizationsLocationsDiscoveryConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsDiscoveryConfigsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsDiscoveryConfigsDeleteArgs,
    ) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DiscoveryConfig
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DiscoveryConfig with DlpOrganizationsLocationsDiscoveryConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsDiscoveryConfigsGetArgs>
    for GooglePrivacyDlpV2DiscoveryConfig
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsDiscoveryConfigsGetArgs,
    ) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2DiscoveryConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DiscoveryConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDiscoveryConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDiscoveryConfigsResponse with DlpOrganizationsLocationsDiscoveryConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsDiscoveryConfigsListArgs>
    for GooglePrivacyDlpV2ListDiscoveryConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsDiscoveryConfigsListArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListDiscoveryConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListDiscoveryConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DiscoveryConfig
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DiscoveryConfig with DlpOrganizationsLocationsDiscoveryConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsDiscoveryConfigsPatchArgs>
    for GooglePrivacyDlpV2DiscoveryConfig
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsDiscoveryConfigsPatchArgs,
    ) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2DiscoveryConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DiscoveryConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDlpJobsResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDlpJobsResponse with DlpOrganizationsLocationsDlpJobsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsDlpJobsListArgs>
    for GooglePrivacyDlpV2ListDlpJobsResponse
{
    fn generate_resource_id(&self, input: &DlpOrganizationsLocationsDlpJobsListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListDlpJobsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListDlpJobsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpOrganizationsLocationsFileStoreDataProfilesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsFileStoreDataProfilesDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsFileStoreDataProfilesDeleteArgs,
    ) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2FileStoreDataProfile
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2FileStoreDataProfile with DlpOrganizationsLocationsFileStoreDataProfilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsFileStoreDataProfilesGetArgs>
    for GooglePrivacyDlpV2FileStoreDataProfile
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsFileStoreDataProfilesGetArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2FileStoreDataProfile/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2FileStoreDataProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListFileStoreDataProfilesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListFileStoreDataProfilesResponse with DlpOrganizationsLocationsFileStoreDataProfilesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsFileStoreDataProfilesListArgs>
    for GooglePrivacyDlpV2ListFileStoreDataProfilesResponse
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsFileStoreDataProfilesListArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListFileStoreDataProfilesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListFileStoreDataProfilesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListInfoTypesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListInfoTypesResponse with DlpOrganizationsLocationsInfoTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsInfoTypesListArgs>
    for GooglePrivacyDlpV2ListInfoTypesResponse
{
    fn generate_resource_id(&self, input: &DlpOrganizationsLocationsInfoTypesListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListInfoTypesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListInfoTypesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate with DlpOrganizationsLocationsInspectTemplatesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsInspectTemplatesCreateArgs>
    for GooglePrivacyDlpV2InspectTemplate
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsInspectTemplatesCreateArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2InspectTemplate/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2InspectTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpOrganizationsLocationsInspectTemplatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsInspectTemplatesDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsInspectTemplatesDeleteArgs,
    ) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate with DlpOrganizationsLocationsInspectTemplatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsInspectTemplatesGetArgs>
    for GooglePrivacyDlpV2InspectTemplate
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsInspectTemplatesGetArgs,
    ) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2InspectTemplate/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2InspectTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListInspectTemplatesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListInspectTemplatesResponse with DlpOrganizationsLocationsInspectTemplatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsInspectTemplatesListArgs>
    for GooglePrivacyDlpV2ListInspectTemplatesResponse
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsInspectTemplatesListArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListInspectTemplatesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListInspectTemplatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate with DlpOrganizationsLocationsInspectTemplatesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsInspectTemplatesPatchArgs>
    for GooglePrivacyDlpV2InspectTemplate
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsInspectTemplatesPatchArgs,
    ) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2InspectTemplate/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2InspectTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger with DlpOrganizationsLocationsJobTriggersCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsJobTriggersCreateArgs>
    for GooglePrivacyDlpV2JobTrigger
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsJobTriggersCreateArgs,
    ) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2JobTrigger/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2JobTrigger"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpOrganizationsLocationsJobTriggersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsJobTriggersDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsJobTriggersDeleteArgs,
    ) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger with DlpOrganizationsLocationsJobTriggersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsJobTriggersGetArgs>
    for GooglePrivacyDlpV2JobTrigger
{
    fn generate_resource_id(&self, input: &DlpOrganizationsLocationsJobTriggersGetArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2JobTrigger/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2JobTrigger"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListJobTriggersResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListJobTriggersResponse with DlpOrganizationsLocationsJobTriggersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsJobTriggersListArgs>
    for GooglePrivacyDlpV2ListJobTriggersResponse
{
    fn generate_resource_id(&self, input: &DlpOrganizationsLocationsJobTriggersListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListJobTriggersResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListJobTriggersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger with DlpOrganizationsLocationsJobTriggersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsJobTriggersPatchArgs>
    for GooglePrivacyDlpV2JobTrigger
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsJobTriggersPatchArgs,
    ) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2JobTrigger/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2JobTrigger"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ProjectDataProfile
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ProjectDataProfile with DlpOrganizationsLocationsProjectDataProfilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsProjectDataProfilesGetArgs>
    for GooglePrivacyDlpV2ProjectDataProfile
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsProjectDataProfilesGetArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ProjectDataProfile/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ProjectDataProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListProjectDataProfilesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListProjectDataProfilesResponse with DlpOrganizationsLocationsProjectDataProfilesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsProjectDataProfilesListArgs>
    for GooglePrivacyDlpV2ListProjectDataProfilesResponse
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsProjectDataProfilesListArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListProjectDataProfilesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListProjectDataProfilesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType with DlpOrganizationsLocationsStoredInfoTypesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsStoredInfoTypesCreateArgs>
    for GooglePrivacyDlpV2StoredInfoType
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsStoredInfoTypesCreateArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2StoredInfoType/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2StoredInfoType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpOrganizationsLocationsStoredInfoTypesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsStoredInfoTypesDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsStoredInfoTypesDeleteArgs,
    ) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType with DlpOrganizationsLocationsStoredInfoTypesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsStoredInfoTypesGetArgs>
    for GooglePrivacyDlpV2StoredInfoType
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsStoredInfoTypesGetArgs,
    ) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2StoredInfoType/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2StoredInfoType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListStoredInfoTypesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListStoredInfoTypesResponse with DlpOrganizationsLocationsStoredInfoTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsStoredInfoTypesListArgs>
    for GooglePrivacyDlpV2ListStoredInfoTypesResponse
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsStoredInfoTypesListArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListStoredInfoTypesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListStoredInfoTypesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType with DlpOrganizationsLocationsStoredInfoTypesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsStoredInfoTypesPatchArgs>
    for GooglePrivacyDlpV2StoredInfoType
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsStoredInfoTypesPatchArgs,
    ) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2StoredInfoType/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2StoredInfoType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpOrganizationsLocationsTableDataProfilesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsTableDataProfilesDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsTableDataProfilesDeleteArgs,
    ) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2TableDataProfile
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2TableDataProfile with DlpOrganizationsLocationsTableDataProfilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsTableDataProfilesGetArgs>
    for GooglePrivacyDlpV2TableDataProfile
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsTableDataProfilesGetArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2TableDataProfile/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2TableDataProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListTableDataProfilesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListTableDataProfilesResponse with DlpOrganizationsLocationsTableDataProfilesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsLocationsTableDataProfilesListArgs>
    for GooglePrivacyDlpV2ListTableDataProfilesResponse
{
    fn generate_resource_id(
        &self,
        input: &DlpOrganizationsLocationsTableDataProfilesListArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListTableDataProfilesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListTableDataProfilesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType with DlpOrganizationsStoredInfoTypesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsStoredInfoTypesCreateArgs>
    for GooglePrivacyDlpV2StoredInfoType
{
    fn generate_resource_id(&self, input: &DlpOrganizationsStoredInfoTypesCreateArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2StoredInfoType/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2StoredInfoType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpOrganizationsStoredInfoTypesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsStoredInfoTypesDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(&self, input: &DlpOrganizationsStoredInfoTypesDeleteArgs) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType with DlpOrganizationsStoredInfoTypesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsStoredInfoTypesGetArgs>
    for GooglePrivacyDlpV2StoredInfoType
{
    fn generate_resource_id(&self, input: &DlpOrganizationsStoredInfoTypesGetArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2StoredInfoType/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2StoredInfoType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListStoredInfoTypesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListStoredInfoTypesResponse with DlpOrganizationsStoredInfoTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsStoredInfoTypesListArgs>
    for GooglePrivacyDlpV2ListStoredInfoTypesResponse
{
    fn generate_resource_id(&self, input: &DlpOrganizationsStoredInfoTypesListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListStoredInfoTypesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListStoredInfoTypesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType with DlpOrganizationsStoredInfoTypesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpOrganizationsStoredInfoTypesPatchArgs>
    for GooglePrivacyDlpV2StoredInfoType
{
    fn generate_resource_id(&self, input: &DlpOrganizationsStoredInfoTypesPatchArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2StoredInfoType/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2StoredInfoType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyContentResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyContentResponse with DlpProjectsContentDeidentifyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsContentDeidentifyArgs>
    for GooglePrivacyDlpV2DeidentifyContentResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsContentDeidentifyArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2DeidentifyContentResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DeidentifyContentResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectContentResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectContentResponse with DlpProjectsContentInspectArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsContentInspectArgs>
    for GooglePrivacyDlpV2InspectContentResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsContentInspectArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2InspectContentResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2InspectContentResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ReidentifyContentResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ReidentifyContentResponse with DlpProjectsContentReidentifyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsContentReidentifyArgs>
    for GooglePrivacyDlpV2ReidentifyContentResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsContentReidentifyArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ReidentifyContentResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ReidentifyContentResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate with DlpProjectsDeidentifyTemplatesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsDeidentifyTemplatesCreateArgs>
    for GooglePrivacyDlpV2DeidentifyTemplate
{
    fn generate_resource_id(&self, input: &DlpProjectsDeidentifyTemplatesCreateArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpProjectsDeidentifyTemplatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsDeidentifyTemplatesDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(&self, input: &DlpProjectsDeidentifyTemplatesDeleteArgs) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate with DlpProjectsDeidentifyTemplatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsDeidentifyTemplatesGetArgs>
    for GooglePrivacyDlpV2DeidentifyTemplate
{
    fn generate_resource_id(&self, input: &DlpProjectsDeidentifyTemplatesGetArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDeidentifyTemplatesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDeidentifyTemplatesResponse with DlpProjectsDeidentifyTemplatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsDeidentifyTemplatesListArgs>
    for GooglePrivacyDlpV2ListDeidentifyTemplatesResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsDeidentifyTemplatesListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListDeidentifyTemplatesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListDeidentifyTemplatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate with DlpProjectsDeidentifyTemplatesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsDeidentifyTemplatesPatchArgs>
    for GooglePrivacyDlpV2DeidentifyTemplate
{
    fn generate_resource_id(&self, input: &DlpProjectsDeidentifyTemplatesPatchArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpProjectsDlpJobsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsDlpJobsCancelArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(&self, input: &DlpProjectsDlpJobsCancelArgs) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DlpJob
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DlpJob with DlpProjectsDlpJobsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsDlpJobsCreateArgs> for GooglePrivacyDlpV2DlpJob {
    fn generate_resource_id(&self, input: &DlpProjectsDlpJobsCreateArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2DlpJob/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DlpJob"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpProjectsDlpJobsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsDlpJobsDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(&self, input: &DlpProjectsDlpJobsDeleteArgs) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DlpJob
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DlpJob with DlpProjectsDlpJobsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsDlpJobsGetArgs> for GooglePrivacyDlpV2DlpJob {
    fn generate_resource_id(&self, input: &DlpProjectsDlpJobsGetArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2DlpJob/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DlpJob"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDlpJobsResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDlpJobsResponse with DlpProjectsDlpJobsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsDlpJobsListArgs> for GooglePrivacyDlpV2ListDlpJobsResponse {
    fn generate_resource_id(&self, input: &DlpProjectsDlpJobsListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListDlpJobsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListDlpJobsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2RedactImageResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2RedactImageResponse with DlpProjectsImageRedactArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsImageRedactArgs> for GooglePrivacyDlpV2RedactImageResponse {
    fn generate_resource_id(&self, input: &DlpProjectsImageRedactArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2RedactImageResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2RedactImageResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate with DlpProjectsInspectTemplatesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsInspectTemplatesCreateArgs>
    for GooglePrivacyDlpV2InspectTemplate
{
    fn generate_resource_id(&self, input: &DlpProjectsInspectTemplatesCreateArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2InspectTemplate/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2InspectTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpProjectsInspectTemplatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsInspectTemplatesDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(&self, input: &DlpProjectsInspectTemplatesDeleteArgs) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate with DlpProjectsInspectTemplatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsInspectTemplatesGetArgs> for GooglePrivacyDlpV2InspectTemplate {
    fn generate_resource_id(&self, input: &DlpProjectsInspectTemplatesGetArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2InspectTemplate/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2InspectTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListInspectTemplatesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListInspectTemplatesResponse with DlpProjectsInspectTemplatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsInspectTemplatesListArgs>
    for GooglePrivacyDlpV2ListInspectTemplatesResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsInspectTemplatesListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListInspectTemplatesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListInspectTemplatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate with DlpProjectsInspectTemplatesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsInspectTemplatesPatchArgs>
    for GooglePrivacyDlpV2InspectTemplate
{
    fn generate_resource_id(&self, input: &DlpProjectsInspectTemplatesPatchArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2InspectTemplate/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2InspectTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DlpJob
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DlpJob with DlpProjectsJobTriggersActivateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsJobTriggersActivateArgs> for GooglePrivacyDlpV2DlpJob {
    fn generate_resource_id(&self, input: &DlpProjectsJobTriggersActivateArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2DlpJob/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DlpJob"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger with DlpProjectsJobTriggersCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsJobTriggersCreateArgs> for GooglePrivacyDlpV2JobTrigger {
    fn generate_resource_id(&self, input: &DlpProjectsJobTriggersCreateArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2JobTrigger/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2JobTrigger"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpProjectsJobTriggersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsJobTriggersDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(&self, input: &DlpProjectsJobTriggersDeleteArgs) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger with DlpProjectsJobTriggersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsJobTriggersGetArgs> for GooglePrivacyDlpV2JobTrigger {
    fn generate_resource_id(&self, input: &DlpProjectsJobTriggersGetArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2JobTrigger/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2JobTrigger"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListJobTriggersResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListJobTriggersResponse with DlpProjectsJobTriggersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsJobTriggersListArgs>
    for GooglePrivacyDlpV2ListJobTriggersResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsJobTriggersListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListJobTriggersResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListJobTriggersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger with DlpProjectsJobTriggersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsJobTriggersPatchArgs> for GooglePrivacyDlpV2JobTrigger {
    fn generate_resource_id(&self, input: &DlpProjectsJobTriggersPatchArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2JobTrigger/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2JobTrigger"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ColumnDataProfile
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ColumnDataProfile with DlpProjectsLocationsColumnDataProfilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsColumnDataProfilesGetArgs>
    for GooglePrivacyDlpV2ColumnDataProfile
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsColumnDataProfilesGetArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ColumnDataProfile/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ColumnDataProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListColumnDataProfilesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListColumnDataProfilesResponse with DlpProjectsLocationsColumnDataProfilesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsColumnDataProfilesListArgs>
    for GooglePrivacyDlpV2ListColumnDataProfilesResponse
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsColumnDataProfilesListArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListColumnDataProfilesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListColumnDataProfilesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2Connection
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2Connection with DlpProjectsLocationsConnectionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsConnectionsCreateArgs>
    for GooglePrivacyDlpV2Connection
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsConnectionsCreateArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2Connection/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2Connection"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpProjectsLocationsConnectionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsConnectionsDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(&self, input: &DlpProjectsLocationsConnectionsDeleteArgs) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2Connection
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2Connection with DlpProjectsLocationsConnectionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsConnectionsGetArgs> for GooglePrivacyDlpV2Connection {
    fn generate_resource_id(&self, input: &DlpProjectsLocationsConnectionsGetArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2Connection/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2Connection"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListConnectionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListConnectionsResponse with DlpProjectsLocationsConnectionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsConnectionsListArgs>
    for GooglePrivacyDlpV2ListConnectionsResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsConnectionsListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListConnectionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListConnectionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2Connection
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2Connection with DlpProjectsLocationsConnectionsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsConnectionsPatchArgs> for GooglePrivacyDlpV2Connection {
    fn generate_resource_id(&self, input: &DlpProjectsLocationsConnectionsPatchArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2Connection/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2Connection"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2SearchConnectionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2SearchConnectionsResponse with DlpProjectsLocationsConnectionsSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsConnectionsSearchArgs>
    for GooglePrivacyDlpV2SearchConnectionsResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsConnectionsSearchArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2SearchConnectionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2SearchConnectionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyContentResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyContentResponse with DlpProjectsLocationsContentDeidentifyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsContentDeidentifyArgs>
    for GooglePrivacyDlpV2DeidentifyContentResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsContentDeidentifyArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2DeidentifyContentResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DeidentifyContentResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectContentResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectContentResponse with DlpProjectsLocationsContentInspectArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsContentInspectArgs>
    for GooglePrivacyDlpV2InspectContentResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsContentInspectArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2InspectContentResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2InspectContentResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ReidentifyContentResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ReidentifyContentResponse with DlpProjectsLocationsContentReidentifyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsContentReidentifyArgs>
    for GooglePrivacyDlpV2ReidentifyContentResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsContentReidentifyArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ReidentifyContentResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ReidentifyContentResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate with DlpProjectsLocationsDeidentifyTemplatesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsDeidentifyTemplatesCreateArgs>
    for GooglePrivacyDlpV2DeidentifyTemplate
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsDeidentifyTemplatesCreateArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpProjectsLocationsDeidentifyTemplatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsDeidentifyTemplatesDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsDeidentifyTemplatesDeleteArgs,
    ) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate with DlpProjectsLocationsDeidentifyTemplatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsDeidentifyTemplatesGetArgs>
    for GooglePrivacyDlpV2DeidentifyTemplate
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsDeidentifyTemplatesGetArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDeidentifyTemplatesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDeidentifyTemplatesResponse with DlpProjectsLocationsDeidentifyTemplatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsDeidentifyTemplatesListArgs>
    for GooglePrivacyDlpV2ListDeidentifyTemplatesResponse
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsDeidentifyTemplatesListArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListDeidentifyTemplatesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListDeidentifyTemplatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DeidentifyTemplate with DlpProjectsLocationsDeidentifyTemplatesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsDeidentifyTemplatesPatchArgs>
    for GooglePrivacyDlpV2DeidentifyTemplate
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsDeidentifyTemplatesPatchArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DeidentifyTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DiscoveryConfig
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DiscoveryConfig with DlpProjectsLocationsDiscoveryConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsDiscoveryConfigsCreateArgs>
    for GooglePrivacyDlpV2DiscoveryConfig
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsDiscoveryConfigsCreateArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2DiscoveryConfig/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DiscoveryConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpProjectsLocationsDiscoveryConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsDiscoveryConfigsDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsDiscoveryConfigsDeleteArgs,
    ) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DiscoveryConfig
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DiscoveryConfig with DlpProjectsLocationsDiscoveryConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsDiscoveryConfigsGetArgs>
    for GooglePrivacyDlpV2DiscoveryConfig
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsDiscoveryConfigsGetArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2DiscoveryConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DiscoveryConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDiscoveryConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDiscoveryConfigsResponse with DlpProjectsLocationsDiscoveryConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsDiscoveryConfigsListArgs>
    for GooglePrivacyDlpV2ListDiscoveryConfigsResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsDiscoveryConfigsListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListDiscoveryConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListDiscoveryConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DiscoveryConfig
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DiscoveryConfig with DlpProjectsLocationsDiscoveryConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsDiscoveryConfigsPatchArgs>
    for GooglePrivacyDlpV2DiscoveryConfig
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsDiscoveryConfigsPatchArgs,
    ) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2DiscoveryConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DiscoveryConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpProjectsLocationsDlpJobsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsDlpJobsCancelArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(&self, input: &DlpProjectsLocationsDlpJobsCancelArgs) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DlpJob
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DlpJob with DlpProjectsLocationsDlpJobsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsDlpJobsCreateArgs> for GooglePrivacyDlpV2DlpJob {
    fn generate_resource_id(&self, input: &DlpProjectsLocationsDlpJobsCreateArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2DlpJob/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DlpJob"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpProjectsLocationsDlpJobsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsDlpJobsDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(&self, input: &DlpProjectsLocationsDlpJobsDeleteArgs) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpProjectsLocationsDlpJobsFinishArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsDlpJobsFinishArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(&self, input: &DlpProjectsLocationsDlpJobsFinishArgs) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DlpJob
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DlpJob with DlpProjectsLocationsDlpJobsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsDlpJobsGetArgs> for GooglePrivacyDlpV2DlpJob {
    fn generate_resource_id(&self, input: &DlpProjectsLocationsDlpJobsGetArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2DlpJob/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DlpJob"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2HybridInspectResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2HybridInspectResponse with DlpProjectsLocationsDlpJobsHybridInspectArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsDlpJobsHybridInspectArgs>
    for GooglePrivacyDlpV2HybridInspectResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsDlpJobsHybridInspectArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2HybridInspectResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2HybridInspectResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDlpJobsResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListDlpJobsResponse with DlpProjectsLocationsDlpJobsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsDlpJobsListArgs>
    for GooglePrivacyDlpV2ListDlpJobsResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsDlpJobsListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListDlpJobsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListDlpJobsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpProjectsLocationsFileStoreDataProfilesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsFileStoreDataProfilesDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsFileStoreDataProfilesDeleteArgs,
    ) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2FileStoreDataProfile
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2FileStoreDataProfile with DlpProjectsLocationsFileStoreDataProfilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsFileStoreDataProfilesGetArgs>
    for GooglePrivacyDlpV2FileStoreDataProfile
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsFileStoreDataProfilesGetArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2FileStoreDataProfile/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2FileStoreDataProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListFileStoreDataProfilesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListFileStoreDataProfilesResponse with DlpProjectsLocationsFileStoreDataProfilesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsFileStoreDataProfilesListArgs>
    for GooglePrivacyDlpV2ListFileStoreDataProfilesResponse
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsFileStoreDataProfilesListArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListFileStoreDataProfilesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListFileStoreDataProfilesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2RedactImageResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2RedactImageResponse with DlpProjectsLocationsImageRedactArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsImageRedactArgs>
    for GooglePrivacyDlpV2RedactImageResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsImageRedactArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2RedactImageResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2RedactImageResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListInfoTypesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListInfoTypesResponse with DlpProjectsLocationsInfoTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsInfoTypesListArgs>
    for GooglePrivacyDlpV2ListInfoTypesResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsInfoTypesListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListInfoTypesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListInfoTypesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate with DlpProjectsLocationsInspectTemplatesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsInspectTemplatesCreateArgs>
    for GooglePrivacyDlpV2InspectTemplate
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsInspectTemplatesCreateArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2InspectTemplate/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2InspectTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpProjectsLocationsInspectTemplatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsInspectTemplatesDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsInspectTemplatesDeleteArgs,
    ) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate with DlpProjectsLocationsInspectTemplatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsInspectTemplatesGetArgs>
    for GooglePrivacyDlpV2InspectTemplate
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsInspectTemplatesGetArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2InspectTemplate/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2InspectTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListInspectTemplatesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListInspectTemplatesResponse with DlpProjectsLocationsInspectTemplatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsInspectTemplatesListArgs>
    for GooglePrivacyDlpV2ListInspectTemplatesResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsInspectTemplatesListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListInspectTemplatesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListInspectTemplatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2InspectTemplate with DlpProjectsLocationsInspectTemplatesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsInspectTemplatesPatchArgs>
    for GooglePrivacyDlpV2InspectTemplate
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsInspectTemplatesPatchArgs,
    ) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2InspectTemplate/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2InspectTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2DlpJob
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2DlpJob with DlpProjectsLocationsJobTriggersActivateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsJobTriggersActivateArgs> for GooglePrivacyDlpV2DlpJob {
    fn generate_resource_id(&self, input: &DlpProjectsLocationsJobTriggersActivateArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2DlpJob/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2DlpJob"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger with DlpProjectsLocationsJobTriggersCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsJobTriggersCreateArgs>
    for GooglePrivacyDlpV2JobTrigger
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsJobTriggersCreateArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2JobTrigger/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2JobTrigger"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpProjectsLocationsJobTriggersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsJobTriggersDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(&self, input: &DlpProjectsLocationsJobTriggersDeleteArgs) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger with DlpProjectsLocationsJobTriggersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsJobTriggersGetArgs> for GooglePrivacyDlpV2JobTrigger {
    fn generate_resource_id(&self, input: &DlpProjectsLocationsJobTriggersGetArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2JobTrigger/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2JobTrigger"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2HybridInspectResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2HybridInspectResponse with DlpProjectsLocationsJobTriggersHybridInspectArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsJobTriggersHybridInspectArgs>
    for GooglePrivacyDlpV2HybridInspectResponse
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsJobTriggersHybridInspectArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2HybridInspectResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2HybridInspectResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListJobTriggersResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListJobTriggersResponse with DlpProjectsLocationsJobTriggersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsJobTriggersListArgs>
    for GooglePrivacyDlpV2ListJobTriggersResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsJobTriggersListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListJobTriggersResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListJobTriggersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2JobTrigger with DlpProjectsLocationsJobTriggersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsJobTriggersPatchArgs> for GooglePrivacyDlpV2JobTrigger {
    fn generate_resource_id(&self, input: &DlpProjectsLocationsJobTriggersPatchArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2JobTrigger/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2JobTrigger"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ProjectDataProfile
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ProjectDataProfile with DlpProjectsLocationsProjectDataProfilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsProjectDataProfilesGetArgs>
    for GooglePrivacyDlpV2ProjectDataProfile
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsProjectDataProfilesGetArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ProjectDataProfile/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ProjectDataProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListProjectDataProfilesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListProjectDataProfilesResponse with DlpProjectsLocationsProjectDataProfilesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsProjectDataProfilesListArgs>
    for GooglePrivacyDlpV2ListProjectDataProfilesResponse
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsProjectDataProfilesListArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListProjectDataProfilesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListProjectDataProfilesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType with DlpProjectsLocationsStoredInfoTypesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsStoredInfoTypesCreateArgs>
    for GooglePrivacyDlpV2StoredInfoType
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsStoredInfoTypesCreateArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2StoredInfoType/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2StoredInfoType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpProjectsLocationsStoredInfoTypesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsStoredInfoTypesDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsStoredInfoTypesDeleteArgs,
    ) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType with DlpProjectsLocationsStoredInfoTypesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsStoredInfoTypesGetArgs>
    for GooglePrivacyDlpV2StoredInfoType
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsStoredInfoTypesGetArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2StoredInfoType/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2StoredInfoType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListStoredInfoTypesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListStoredInfoTypesResponse with DlpProjectsLocationsStoredInfoTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsStoredInfoTypesListArgs>
    for GooglePrivacyDlpV2ListStoredInfoTypesResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsStoredInfoTypesListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListStoredInfoTypesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListStoredInfoTypesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType with DlpProjectsLocationsStoredInfoTypesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsStoredInfoTypesPatchArgs>
    for GooglePrivacyDlpV2StoredInfoType
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsStoredInfoTypesPatchArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2StoredInfoType/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2StoredInfoType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpProjectsLocationsTableDataProfilesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsTableDataProfilesDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsTableDataProfilesDeleteArgs,
    ) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2TableDataProfile
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2TableDataProfile with DlpProjectsLocationsTableDataProfilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsTableDataProfilesGetArgs>
    for GooglePrivacyDlpV2TableDataProfile
{
    fn generate_resource_id(&self, input: &DlpProjectsLocationsTableDataProfilesGetArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2TableDataProfile/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2TableDataProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListTableDataProfilesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListTableDataProfilesResponse with DlpProjectsLocationsTableDataProfilesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsLocationsTableDataProfilesListArgs>
    for GooglePrivacyDlpV2ListTableDataProfilesResponse
{
    fn generate_resource_id(
        &self,
        input: &DlpProjectsLocationsTableDataProfilesListArgs,
    ) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListTableDataProfilesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListTableDataProfilesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType with DlpProjectsStoredInfoTypesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsStoredInfoTypesCreateArgs> for GooglePrivacyDlpV2StoredInfoType {
    fn generate_resource_id(&self, input: &DlpProjectsStoredInfoTypesCreateArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2StoredInfoType/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2StoredInfoType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DlpProjectsStoredInfoTypesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsStoredInfoTypesDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(&self, input: &DlpProjectsStoredInfoTypesDeleteArgs) -> String {
        format!("gcp::dlp::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType with DlpProjectsStoredInfoTypesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsStoredInfoTypesGetArgs> for GooglePrivacyDlpV2StoredInfoType {
    fn generate_resource_id(&self, input: &DlpProjectsStoredInfoTypesGetArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2StoredInfoType/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2StoredInfoType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2ListStoredInfoTypesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2ListStoredInfoTypesResponse with DlpProjectsStoredInfoTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsStoredInfoTypesListArgs>
    for GooglePrivacyDlpV2ListStoredInfoTypesResponse
{
    fn generate_resource_id(&self, input: &DlpProjectsStoredInfoTypesListArgs) -> String {
        format!(
            "gcp::dlp::GooglePrivacyDlpV2ListStoredInfoTypesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2ListStoredInfoTypesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType
// =============================================================================

/// ResourceIdentifier implementation for GooglePrivacyDlpV2StoredInfoType with DlpProjectsStoredInfoTypesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DlpProjectsStoredInfoTypesPatchArgs> for GooglePrivacyDlpV2StoredInfoType {
    fn generate_resource_id(&self, input: &DlpProjectsStoredInfoTypesPatchArgs) -> String {
        format!("gcp::dlp::GooglePrivacyDlpV2StoredInfoType/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dlp::GooglePrivacyDlpV2StoredInfoType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
